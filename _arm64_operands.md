# ARM64 Per-Instruction Reference

Generated from ARM ISA XML A64 profile (2025-12).

Total: 2291 instructions, 4623 encodings.

---

## ABS -- A64

### Encoding: `ABS_32_dp_1src`  [32-bit]  — ABS

**Assembly:** `ABS  <Wd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

### Encoding: `ABS_64_dp_1src`  [64-bit]  — ABS

**Assembly:** `ABS  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## ABS -- A64

### Encoding: `ABS_asisdmisc_R`  — ABS

**Assembly:** `ABS  D<d>, D<n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `ABS_asimdmisc_R`  — ABS

**Assembly:** `ABS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## ABS -- A64

### Encoding: `abs_z_p_z_m`  — ABS

**Assembly:** `ABS  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `abs_z_p_z_z`  — ABS

**Assembly:** `ABS  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## ADC -- A64

### Encoding: `ADC_32_addsub_carry`  [32-bit]  — ADC

**Assembly:** `ADC  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `ADC_64_addsub_carry`  [64-bit]  — ADC

**Assembly:** `ADC  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## ADCLB -- A64

### Encoding: `adclb_z_zzz_`  — ADCLB

**Assembly:** `ADCLB  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## ADCLT -- A64

### Encoding: `adclt_z_zzz_`  — ADCLT

**Assembly:** `ADCLT  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## ADCS -- A64

### Encoding: `ADCS_32_addsub_carry`  [32-bit]  — ADCS

**Assembly:** `ADCS  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `ADCS_64_addsub_carry`  [64-bit]  — ADCS

**Assembly:** `ADCS  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## ADD (extended register) -- A64

### Encoding: `ADD_32_addsub_ext`  [32-bit]  — ADD

**Assembly:** `ADD  <Wd|WSP>, <Wn|WSP>, <Wm>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd\|WSP>` | Rd (5b) | Is the 32-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field | if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;<br>let shift : integer{} = UInt(imm3) as integer{0..4}; |

**Pseudocode (Decode):**
```
if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer{} = UInt(imm3) as integer{0..4};
let datasize : integer{} = 32 << UInt(sf);
let extend_type : ExtendType = DecodeRegExtend(option);
```

### Encoding: `ADD_64_addsub_ext`  [64-bit]  — ADD

**Assembly:** `ADD  <Xd|SP>, <Xn|SP>, <R><m>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<R>` | — |  |  |
| `<m>` | Rm (5b) | Is the number [0-30] of the second general-purpose source register or the name ZR (31), encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field | if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;<br>let shift : integer{} = UInt(imm3) as integer{0..4}; |

**Pseudocode (Decode):**
```
if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer{} = UInt(imm3) as integer{0..4};
let datasize : integer{} = 32 << UInt(sf);
let extend_type : ExtendType = DecodeRegExtend(option);
```

---

## ADD (immediate) -- A64

### Encoding: `ADD_32_addsub_imm`  [32-bit]  — ADD

**Assembly:** `ADD  <Wd|WSP>, <Wn|WSP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd\|WSP>` | Rd (5b) | Is the 32-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. | let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12}; |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);

let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12};
```

### Encoding: `ADD_64_addsub_imm`  [64-bit]  — ADD

**Assembly:** `ADD  <Xd|SP>, <Xn|SP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. | let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12}; |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);

let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12};
```

---

## ADD (immediate) -- A64

### Encoding: `add_z_zi_`  — ADD

**Assembly:** `ADD  <Zdn>.<T>, <Zdn>.<T>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is an unsigned immediate in the range 0 to 255, encoded in the "imm8" field. | var imm : integer = UInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
var imm : integer = UInt(imm8);
if sh == '1' then imm = imm << 8; end;
```

---

## ADD (shifted register) -- A64

### Encoding: `ADD_32_addsub_shift`  [32-bit]  — ADD

**Assembly:** `ADD  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if shift == '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `ADD_64_addsub_shift`  [64-bit]  — ADD

**Assembly:** `ADD  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if shift == '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## ADD (to array, array and multiple vectors) -- A64

### Encoding: `add_za_zw_2x2`  — ADD

**Assembly:** `ADD  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the source multi-vector gr | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" times 2 plus 1. | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `add_za_zw_4x4`  — ADD

**Assembly:** `ADD  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the source multi-vector g | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zm" times 4 plus 3. | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## ADD (to array, multiple and single vector) -- A64

### Encoding: `add_za_zzv_2x1`  — ADD

**Assembly:** `ADD  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<T>-<Zn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `add_za_zzv_4x1`  — ADD

**Assembly:** `ADD  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<T>-<Zn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## ADD (to array, multiple vectors) -- A64

### Encoding: `add_za_zzw_2x2`  — ADD

**Assembly:** `ADD  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<T>-<Zn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `add_za_zzw_4x4`  — ADD

**Assembly:** `ADD  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<T>-<Zn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## ADD (to vector, multiple vectors) -- A64

### Encoding: `add_mz_zzv_2x1`  — ADD

**Assembly:** `ADD  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `add_mz_zzv_4x1`  — ADD

**Assembly:** `ADD  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## ADD (vector) -- A64

### Encoding: `ADD_asisdsame_only`  — ADD

**Assembly:** `ADD  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `ADD_asimdsame_only`  — ADD

**Assembly:** `ADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## ADD (vectors, predicated) -- A64

### Encoding: `add_z_p_zz_`  — ADD

**Assembly:** `ADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## ADD (vectors, unpredicated) -- A64

### Encoding: `add_z_zz_`  — ADD

**Assembly:** `ADD  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ADDG -- A64

### Encoding: `ADDG_64_addsub_immtags`  — ADDG

**Assembly:** `ADDG  <Xd|SP>, <Xn|SP>, #<uimm6>, #<uimm4>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<uimm6>` | imm6 (6b) | Is an unsigned immediate, a multiple of 16 in the range 0 to 1008, encoded in the "imm6" field. | let offset : bits(64) = LSL(ZeroExtend{64}(imm6), LOG2_TAG_GRANULE); |
| `<uimm4>` | imm4 (4b) | Is an unsigned immediate, in the range 0 to 15, encoded in the "imm4" field. | let tag_offset : bits(4) = imm4; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let tag_offset : bits(4) = imm4;
let offset : bits(64) = LSL(ZeroExtend{64}(imm6), LOG2_TAG_GRANULE);
```

---

## ADDHA -- A64

### Encoding: `addha_za_pp_z_32`  — ADDHA

**Assembly:** `ADDHA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let da : integer = UInt(ZAda);
```

### Encoding: `addha_za_pp_z_64`  — ADDHA

**Assembly:** `ADDHA  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let da : integer = UInt(ZAda);
```

---

## ADDHN, ADDHN2 -- A64

### Encoding: `ADDHN_asimddiff_N`  — ADDHN

**Assembly:** `ADDHN{2}  <Vd>.<Tb>, <Vn>.<Ta>, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
let round : boolean = FALSE;
```

---

## ADDHNB -- A64

### Encoding: `addhnb_z_zz_`  — ADDHNB

**Assembly:** `ADDHNB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ADDHNT -- A64

### Encoding: `addhnt_z_zz_`  — ADDHNT

**Assembly:** `ADDHNT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ADDP (scalar) -- A64

### Encoding: `ADDP_asisdpair_only`  — ADDP

**Assembly:** `ADDP  D<d>, <Vn>.2D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 64;
let datasize : integer{} = 128;
```

---

## ADDP (vector) -- A64

### Encoding: `ADDP_asimdsame_only`  — ADDP

**Assembly:** `ADDP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## ADDP -- A64

### Encoding: `addp_z_p_zz_`  — ADDP

**Assembly:** `ADDP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## ADDPL -- A64

### Encoding: `addpl_r_ri_`  — ADDPL

**Assembly:** `ADDPL  <Xd|SP>, <Xn|SP>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the signed immediate operand, in the range -32 to 31, encoded in the "imm6" field. | let imm : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let d : integer = UInt(Rd);
let imm : integer = SInt(imm6);
```

---

## ADDPT (predicated) -- A64

### Encoding: `addpt_z_p_zz_`  — ADDPT

**Assembly:** `ADDPT  <Zdn>.D, <Pg>/M, <Zdn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer =  UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer =  UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_CPA) then
    EndOfDecode(Decode_UNDEF);
end;
let g : integer =  UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer =  UInt(Zm);
```

---

## ADDPT (unpredicated) -- A64

### Encoding: `addpt_z_zz_`  — ADDPT

**Assembly:** `ADDPT  <Zd>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_CPA) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ADDPT -- A64

### Encoding: `ADDPT_64_addsub_pt`  — ADDPT

**Assembly:** `ADDPT  <Xd|SP>, <Xn|SP>, <Xm>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<amount>` | imm3 (3b) | Is the left shift amount, in the range 0 to 7, defaulting to 0, encoded in the "imm3" field. | let shift : integer = UInt(imm3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CPA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer = UInt(imm3);
```

---

## ADDQP -- A64

### Encoding: `addqp_z_zz_`  — ADDQP

**Assembly:** `ADDQP  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ADDQV -- A64

### Encoding: `addqv_z_p_z_`  — ADDQV

**Assembly:** `ADDQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let unsigned : boolean = TRUE;
```

---

## ADDS (extended register) -- A64

### Encoding: `ADDS_32S_addsub_ext`  [32-bit]  — ADDS

**Assembly:** `ADDS  <Wd>, <Wn|WSP>, <Wm>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field | if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;<br>let shift : integer{} = UInt(imm3) as integer{0..4}; |

**Pseudocode (Decode):**
```
if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer{} = UInt(imm3) as integer{0..4};
let datasize : integer{} = 32 << UInt(sf);
let extend_type : ExtendType = DecodeRegExtend(option);
```

### Encoding: `ADDS_64S_addsub_ext`  [64-bit]  — ADDS

**Assembly:** `ADDS  <Xd>, <Xn|SP>, <R><m>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<R>` | — |  |  |
| `<m>` | Rm (5b) | Is the number [0-30] of the second general-purpose source register or the name ZR (31), encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field | if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;<br>let shift : integer{} = UInt(imm3) as integer{0..4}; |

**Pseudocode (Decode):**
```
if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer{} = UInt(imm3) as integer{0..4};
let datasize : integer{} = 32 << UInt(sf);
let extend_type : ExtendType = DecodeRegExtend(option);
```

---

## ADDS (immediate) -- A64

### Encoding: `ADDS_32S_addsub_imm`  [32-bit]  — ADDS

**Assembly:** `ADDS  <Wd>, <Wn|WSP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. | let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12}; |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);

let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12};
```

### Encoding: `ADDS_64S_addsub_imm`  [64-bit]  — ADDS

**Assembly:** `ADDS  <Xd>, <Xn|SP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. | let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12}; |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);

let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12};
```

---

## ADDS (shifted register) -- A64

### Encoding: `ADDS_32_addsub_shift`  [32-bit]  — ADDS

**Assembly:** `ADDS  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if shift == '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `ADDS_64_addsub_shift`  [64-bit]  — ADDS

**Assembly:** `ADDS  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if shift == '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## ADDSPL -- A64

### Encoding: `addspl_r_ri_`  — ADDSPL

**Assembly:** `ADDSPL  <Xd|SP>, <Xn|SP>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the signed immediate operand, in the range -32 to 31, encoded in the "imm6" field. | let imm : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let d : integer = UInt(Rd);
let imm : integer = SInt(imm6);
```

---

## ADDSUBP -- A64

### Encoding: `addsubp_z_zz_`  — ADDSUBP

**Assembly:** `ADDSUBP  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ADDSVL -- A64

### Encoding: `addsvl_r_ri_`  — ADDSVL

**Assembly:** `ADDSVL  <Xd|SP>, <Xn|SP>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the signed immediate operand, in the range -32 to 31, encoded in the "imm6" field. | let imm : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let d : integer = UInt(Rd);
let imm : integer = SInt(imm6);
```

---

## ADDV -- A64

### Encoding: `ADDV_asimdall_only`  — ADDV

**Assembly:** `ADDV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '100' then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
```

---

## ADDVA -- A64

### Encoding: `addva_za_pp_z_32`  — ADDVA

**Assembly:** `ADDVA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let da : integer = UInt(ZAda);
```

### Encoding: `addva_za_pp_z_64`  — ADDVA

**Assembly:** `ADDVA  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let da : integer = UInt(ZAda);
```

---

## ADDVL -- A64

### Encoding: `addvl_r_ri_`  — ADDVL

**Assembly:** `ADDVL  <Xd|SP>, <Xn|SP>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the signed immediate operand, in the range -32 to 31, encoded in the "imm6" field. | let imm : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let d : integer = UInt(Rd);
let imm : integer = SInt(imm6);
```

---

## ADR -- A64

### Encoding: `ADR_only_pcreladdr`  — ADR

**Assembly:** `ADR  <Xd>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<label>` | (immhi :: immlo) | Is the program label whose address is to be calculated. Its offset from the address of this instruction, in the range +/ |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let imm : bits(64) = SignExtend{}(immhi::immlo);
```

---

## ADR -- A64

### Encoding: `adr_z_az_sd_same_scaled`  — ADR

**Assembly:** `ADR  <Zd>.<T>, [<Zn>.<T>, <Zm>.<T>{, <mod> <amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let osize : integer{} = esize;
let unsigned : boolean = TRUE;
let mbytes : integer{} = 1 << UInt(msz);
```

### Encoding: `adr_z_az_d_s32_scaled`  — ADR

**Assembly:** `ADR  <Zd>.D, [<Zn>.D, <Zm>.D, SXTW{<amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let osize : integer{} = 32;
let unsigned : boolean = FALSE;
let mbytes : integer{} = 1 << UInt(msz);
```

### Encoding: `adr_z_az_d_u32_scaled`  — ADR

**Assembly:** `ADR  <Zd>.D, [<Zn>.D, <Zm>.D, UXTW{<amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let osize : integer{} = 32;
let unsigned : boolean = TRUE;
let mbytes : integer{} = 1 << UInt(msz);
```

---

## ADRP -- A64

### Encoding: `ADRP_only_pcreladdr`  — ADRP

**Assembly:** `ADRP  <Xd>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<label>` | (immhi :: immlo) | Is the program label whose 4KB page address is to be calculated. Its offset from the page address of this instruction, i |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let imm : bits(64) = SignExtend{}(immhi::immlo::Zeros{12});
```

---

## AESD (indexed) -- A64

### Encoding: `aesd_mz_zzi_2x1`  — AESD

**Assembly:** `AESD  { <Zdn1>.B-<Zdn2>.B }, { <Zdn1>.B-<Zdn2>.B }, <Zm>.Q[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | Is the round key index, in the range 0 to 3, encoded in the "i2" field. | var index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn::'0');
var index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `aesd_mz_zzi_4x1`  — AESD

**Assembly:** `AESD  { <Zdn1>.B-<Zdn4>.B }, { <Zdn1>.B-<Zdn4>.B }, <Zm>.Q[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | Is the round key index, in the range 0 to 3, encoded in the "i2" field. | var index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn::'00');
var index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## AESD (vectors) -- A64

### Encoding: `aesd_z_zz_`  — AESD

**Assembly:** `AESD  <Zdn>.B, <Zdn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## AESD -- A64

### Encoding: `AESD_B_cryptoaes`  — AESD

**Assembly:** `AESD  <Vd>.16B, <Vn>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AES) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## AESDIMC -- A64

### Encoding: `aesdimc_mz_zzi_2x1`  — AESDIMC

**Assembly:** `AESDIMC  { <Zdn1>.B-<Zdn2>.B }, { <Zdn1>.B-<Zdn2>.B }, <Zm>.Q[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | Is the round key index, in the range 0 to 3, encoded in the "i2" field. | var index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn::'0');
var index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `aesdimc_mz_zzi_4x1`  — AESDIMC

**Assembly:** `AESDIMC  { <Zdn1>.B-<Zdn4>.B }, { <Zdn1>.B-<Zdn4>.B }, <Zm>.Q[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | Is the round key index, in the range 0 to 3, encoded in the "i2" field. | var index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn::'00');
var index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## AESE (indexed) -- A64

### Encoding: `aese_mz_zzi_2x1`  — AESE

**Assembly:** `AESE  { <Zdn1>.B-<Zdn2>.B }, { <Zdn1>.B-<Zdn2>.B }, <Zm>.Q[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | Is the round key index, in the range 0 to 3, encoded in the "i2" field. | var index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn::'0');
var index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `aese_mz_zzi_4x1`  — AESE

**Assembly:** `AESE  { <Zdn1>.B-<Zdn4>.B }, { <Zdn1>.B-<Zdn4>.B }, <Zm>.Q[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | Is the round key index, in the range 0 to 3, encoded in the "i2" field. | var index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn::'00');
var index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## AESE (vectors) -- A64

### Encoding: `aese_z_zz_`  — AESE

**Assembly:** `AESE  <Zdn>.B, <Zdn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## AESE -- A64

### Encoding: `AESE_B_cryptoaes`  — AESE

**Assembly:** `AESE  <Vd>.16B, <Vn>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AES) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## AESEMC -- A64

### Encoding: `aesemc_mz_zzi_2x1`  — AESEMC

**Assembly:** `AESEMC  { <Zdn1>.B-<Zdn2>.B }, { <Zdn1>.B-<Zdn2>.B }, <Zm>.Q[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | Is the round key index, in the range 0 to 3, encoded in the "i2" field. | var index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn::'0');
var index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `aesemc_mz_zzi_4x1`  — AESEMC

**Assembly:** `AESEMC  { <Zdn1>.B-<Zdn4>.B }, { <Zdn1>.B-<Zdn4>.B }, <Zm>.Q[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | Is the round key index, in the range 0 to 3, encoded in the "i2" field. | var index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn::'00');
var index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## AESIMC -- A64

### Encoding: `AESIMC_B_cryptoaes`  — AESIMC

**Assembly:** `AESIMC  <Vd>.16B, <Vn>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AES) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## AESIMC -- A64

### Encoding: `aesimc_z_z_`  — AESIMC

**Assembly:** `AESIMC  <Zdn>.B, <Zdn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES) then EndOfDecode(Decode_UNDEF); end;
let dn : integer = UInt(Zdn);
```

---

## AESMC -- A64

### Encoding: `AESMC_B_cryptoaes`  — AESMC

**Assembly:** `AESMC  <Vd>.16B, <Vn>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AES) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## AESMC -- A64

### Encoding: `aesmc_z_z_`  — AESMC

**Assembly:** `AESMC  <Zdn>.B, <Zdn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES) then EndOfDecode(Decode_UNDEF); end;
let dn : integer = UInt(Zdn);
```

---

## AND (immediate) -- A64

### Encoding: `AND_32_log_imm`  [32-bit]  — AND

**Assembly:** `AND  <Wd|WSP>, <Wn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd\|WSP>` | Rd (5b) | Is the 32-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | (imms :: immr) | For the "32-bit" variant: is the bitmask immediate, encoded in "imms:immr". |  |

**Pseudocode (Decode):**
```
if sf == '0' && N != '0' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
var imm : bits(datasize);
(imm, -) = DecodeBitMasks{datasize}(N, imms, immr, TRUE);
```

### Encoding: `AND_64_log_imm`  [64-bit]  — AND

**Assembly:** `AND  <Xd|SP>, <Xn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | (N :: imms :: immr) | For the "64-bit" variant: is the bitmask immediate, encoded in "N:imms:immr". |  |

**Pseudocode (Decode):**
```
if sf == '0' && N != '0' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
var imm : bits(datasize);
(imm, -) = DecodeBitMasks{datasize}(N, imms, immr, TRUE);
```

---

## AND (immediate) -- A64

### Encoding: `and_z_zi_`  — AND

**Assembly:** `AND  <Zdn>.<T>, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<const>` | imm13 (13b) | Is a 64, 32, 16 or 8-bit bitmask consisting of replicated 2, 4, 8, 16, 32 or 64 bit fields, each field containing a rota | (imm, -) = DecodeBitMasks{64}(imm13[12], imm13[5:0], imm13[11:6], TRUE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn);
var imm : bits(64);
(imm, -) = DecodeBitMasks{64}(imm13[12], imm13[5:0], imm13[11:6], TRUE);
```

---

## AND (predicates) -- A64

### Encoding: `and_p_p_pp_z`  — AND

**Assembly:** `AND  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## AND (shifted register) -- A64

### Encoding: `AND_32_log_shift`  [32-bit]  — AND

**Assembly:** `AND  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `AND_64_log_shift`  [64-bit]  — AND

**Assembly:** `AND  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## AND (vector) -- A64

### Encoding: `AND_asimdsame_only`  — AND

**Assembly:** `AND  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
```

---

## AND (vectors, predicated) -- A64

### Encoding: `and_z_p_zz_`  — AND

**Assembly:** `AND  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## AND (vectors, unpredicated) -- A64

### Encoding: `and_z_zz_`  — AND

**Assembly:** `AND  <Zd>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ANDQV -- A64

### Encoding: `andqv_z_p_z_`  — ANDQV

**Assembly:** `ANDQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## ANDS (immediate) -- A64

### Encoding: `ANDS_32S_log_imm`  [32-bit]  — ANDS

**Assembly:** `ANDS  <Wd>, <Wn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | (imms :: immr) | For the "32-bit" variant: is the bitmask immediate, encoded in "imms:immr". |  |

**Pseudocode (Decode):**
```
if sf == '0' && N != '0' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
var imm : bits(datasize);
(imm, -) = DecodeBitMasks{datasize}(N, imms, immr, TRUE);
```

### Encoding: `ANDS_64S_log_imm`  [64-bit]  — ANDS

**Assembly:** `ANDS  <Xd>, <Xn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | (N :: imms :: immr) | For the "64-bit" variant: is the bitmask immediate, encoded in "N:imms:immr". |  |

**Pseudocode (Decode):**
```
if sf == '0' && N != '0' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
var imm : bits(datasize);
(imm, -) = DecodeBitMasks{datasize}(N, imms, immr, TRUE);
```

---

## ANDS (shifted register) -- A64

### Encoding: `ANDS_32_log_shift`  [32-bit]  — ANDS

**Assembly:** `ANDS  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `ANDS_64_log_shift`  [64-bit]  — ANDS

**Assembly:** `ANDS  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## ANDS -- A64

### Encoding: `ands_p_p_pp_z`  — ANDS

**Assembly:** `ANDS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## ANDV -- A64

### Encoding: `andv_r_p_z_`  — ANDV

**Assembly:** `ANDV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## APAS -- A64 *(alias for SYS)*

### Encoding: `APAS_SYS_CR_systeminstrs`  — SYS

**Assembly:** `APAS  <Xt> / SYS  #6, C7, C0, #0, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## ASR (immediate) -- A64 *(alias for SBFM)*

### Encoding: `ASR_SBFM_32M_bitfield`  [32-bit]  — SBFM

**Assembly:** `ASR  <Wd>, <Wn>, #<shift> / SBFM  <Wd>, <Wn>, #<shift>, #31`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<shift>` | immr (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, encoded in the "immr" field. |  |
| `SBFM` | — |  |  |

### Encoding: `ASR_SBFM_64M_bitfield`  [64-bit]  — SBFM

**Assembly:** `ASR  <Xd>, <Xn>, #<shift> / SBFM  <Xd>, <Xn>, #<shift>, #63`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<shift>` | immr (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, encoded in the "immr" field. |  |
| `SBFM` | — |  |  |

---

## ASR (immediate, predicated) -- A64

### Encoding: `asr_z_p_zi_`  — ASR

**Assembly:** `ASR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## ASR (immediate, unpredicated) -- A64

### Encoding: `asr_z_zi_`  — ASR

**Assembly:** `ASR  <Zd>.<T>, <Zn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## ASR (register) -- A64 *(alias for ASRV)*

### Encoding: `ASR_ASRV_32_dp_2src`  [32-bit]  — ASRV

**Assembly:** `ASR  <Wd>, <Wn>, <Wm> / ASRV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding a shift amount from 0 to 31 in its bottom 5 bit |  |
| `ASRV` | — |  |  |

### Encoding: `ASR_ASRV_64_dp_2src`  [64-bit]  — ASRV

**Assembly:** `ASR  <Xd>, <Xn>, <Xm> / ASRV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding a shift amount from 0 to 63 in its bottom 6 bit |  |
| `ASRV` | — |  |  |

---

## ASR (vectors) -- A64

### Encoding: `asr_z_p_zz_`  — ASR

**Assembly:** `ASR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## ASR (wide elements, predicated) -- A64

### Encoding: `asr_z_p_zw_`  — ASR

**Assembly:** `ASR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## ASR (wide elements, unpredicated) -- A64

### Encoding: `asr_z_zw_`  — ASR

**Assembly:** `ASR  <Zd>.<T>, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ASRD -- A64

### Encoding: `asrd_z_p_zi_`  — ASRD

**Assembly:** `ASRD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## ASRR -- A64

### Encoding: `asrr_z_p_zz_`  — ASRR

**Assembly:** `ASRR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## ASRV -- A64

### Encoding: `ASRV_32_dp_2src`  [32-bit]  — ASRV

**Assembly:** `ASRV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding a shift amount from 0 to 31 in its bottom 5 bit | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(op2);
```

### Encoding: `ASRV_64_dp_2src`  [64-bit]  — ASRV

**Assembly:** `ASRV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding a shift amount from 0 to 63 in its bottom 6 bit | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(op2);
```

---

## AT -- A64 *(alias for SYS)*

### Encoding: `AT_SYS_CR_systeminstrs`  — SYS

**Assembly:** `AT  <at_op>, <Xt> / SYS  #<op1>, C7, <Cm>, #<op2>, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<at_op>` | — |  |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## AUTDA, AUTDZA -- A64

### Encoding: `AUTDA_64P_dp_1src`  [AUTDA]  — AUTDA

**Assembly:** `AUTDA  <Xd>, <Xn|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rn" field. | if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;
let auth_combined : boolean = FALSE;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let source_is_sp : boolean = Z == '0' && n == 31;
```

### Encoding: `AUTDZA_64Z_dp_1src`  [AUTDZA]  — AUTDZA

**Assembly:** `AUTDZA  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;
let auth_combined : boolean = FALSE;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let source_is_sp : boolean = Z == '0' && n == 31;
```

---

## AUTDB, AUTDZB -- A64

### Encoding: `AUTDB_64P_dp_1src`  [AUTDB]  — AUTDB

**Assembly:** `AUTDB  <Xd>, <Xn|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rn" field. | if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;
let auth_combined : boolean = FALSE;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let source_is_sp : boolean = Z == '0' && n == 31;
```

### Encoding: `AUTDZB_64Z_dp_1src`  [AUTDZB]  — AUTDZB

**Assembly:** `AUTDZB  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;
let auth_combined : boolean = FALSE;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let source_is_sp : boolean = Z == '0' && n == 31;
```

---

## AUTIA, AUTIA1716, AUTIASP, AUTIAZ, AUTIZA -- A64

### Encoding: `AUTIA_64P_dp_1src`  [AUTIA]  — AUTIA

**Assembly:** `AUTIA  <Xd>, <Xn|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rn" field. | if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;
let autia1716 : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let auth_combined : boolean = FALSE;

let source_is_sp : boolean = Z == '0' && n == 31;
```

### Encoding: `AUTIZA_64Z_dp_1src`  [AUTIZA]  — AUTIZA

**Assembly:** `AUTIZA  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;
let autia1716 : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let auth_combined : boolean = FALSE;

let source_is_sp : boolean = Z == '0' && n == 31;
```

### Encoding: `AUTIA1716_HI_hints`  [AUTIA1716]  — AUTIA1716

**Assembly:** `AUTIA1716`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var autia1716 : boolean = FALSE;
let auth_combined : boolean = FALSE;

case CRm::op2 of
    when '0011 100' => // AUTIAZ
        d = 30;
        n = 31;
    when '0011 101' => // AUTIASP
        d = 30;
        source_is_sp = TRUE;
    when '0001 100' => // AUTIA1716
        d = 17;
        n = 16;
        autia1716 = TRUE;
end;
```

### Encoding: `AUTIASP_HI_hints`  [AUTIASP]  — AUTIASP

**Assembly:** `AUTIASP`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var autia1716 : boolean = FALSE;
let auth_combined : boolean = FALSE;

case CRm::op2 of
    when '0011 100' => // AUTIAZ
        d = 30;
        n = 31;
    when '0011 101' => // AUTIASP
        d = 30;
        source_is_sp = TRUE;
    when '0001 100' => // AUTIA1716
        d = 17;
        n = 16;
        autia1716 = TRUE;
end;
```

### Encoding: `AUTIAZ_HI_hints`  [AUTIAZ]  — AUTIAZ

**Assembly:** `AUTIAZ`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var autia1716 : boolean = FALSE;
let auth_combined : boolean = FALSE;

case CRm::op2 of
    when '0011 100' => // AUTIAZ
        d = 30;
        n = 31;
    when '0011 101' => // AUTIASP
        d = 30;
        source_is_sp = TRUE;
    when '0001 100' => // AUTIA1716
        d = 17;
        n = 16;
        autia1716 = TRUE;
end;
```

---

## AUTIA171615 -- A64

### Encoding: `AUTIA171615_64LR_dp_1src`  — AUTIA171615

**Assembly:** `AUTIA171615`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;
```

---

## AUTIASPPC -- A64

### Encoding: `AUTIASPPC_only_dp_1src_imm`  — AUTIASPPC

**Assembly:** `AUTIASPPC  <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<label>` | imm16 (16b) | Is the program label whose address is to be calculated. Its negative offset from the address of this instruction, a mult | let offset : bits(64) = ZeroExtend{}(imm16::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let d : integer = 30;
let offset : bits(64) = ZeroExtend{}(imm16::'00');
let auth_combined : boolean = FALSE;
```

---

## AUTIASPPCR -- A64

### Encoding: `AUTIASPPCR_64LRR_dp_1src`  — AUTIASPPCR

**Assembly:** `AUTIASPPCR  <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let d : integer = 30;
let n : integer = UInt(Rn);
let auth_combined : boolean = FALSE;
```

---

## AUTIB, AUTIB1716, AUTIBSP, AUTIBZ, AUTIZB -- A64

### Encoding: `AUTIB_64P_dp_1src`  [AUTIB]  — AUTIB

**Assembly:** `AUTIB  <Xd>, <Xn|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rn" field. | if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;
let autib1716 : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let auth_combined : boolean = FALSE;

let source_is_sp : boolean = Z == '0' && n == 31;
```

### Encoding: `AUTIZB_64Z_dp_1src`  [AUTIZB]  — AUTIZB

**Assembly:** `AUTIZB  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '1' && Rn != '11111' then EndOfDecode(Decode_UNDEF); end;
let autib1716 : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let auth_combined : boolean = FALSE;

let source_is_sp : boolean = Z == '0' && n == 31;
```

### Encoding: `AUTIB1716_HI_hints`  [AUTIB1716]  — AUTIB1716

**Assembly:** `AUTIB1716`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var autib1716 : boolean = FALSE;
let auth_combined : boolean = FALSE;

case CRm::op2 of
    when '0011 110' => // AUTIBZ
        d = 30;
        n = 31;
    when '0011 111' => // AUTIBSP
        d = 30;
        source_is_sp = TRUE;
    when '0001 110' => // AUTIB1716
        d = 17;
        n = 16;
        autib1716 = TRUE;
end;
```

### Encoding: `AUTIBSP_HI_hints`  [AUTIBSP]  — AUTIBSP

**Assembly:** `AUTIBSP`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var autib1716 : boolean = FALSE;
let auth_combined : boolean = FALSE;

case CRm::op2 of
    when '0011 110' => // AUTIBZ
        d = 30;
        n = 31;
    when '0011 111' => // AUTIBSP
        d = 30;
        source_is_sp = TRUE;
    when '0001 110' => // AUTIB1716
        d = 17;
        n = 16;
        autib1716 = TRUE;
end;
```

### Encoding: `AUTIBZ_HI_hints`  [AUTIBZ]  — AUTIBZ

**Assembly:** `AUTIBZ`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var autib1716 : boolean = FALSE;
let auth_combined : boolean = FALSE;

case CRm::op2 of
    when '0011 110' => // AUTIBZ
        d = 30;
        n = 31;
    when '0011 111' => // AUTIBSP
        d = 30;
        source_is_sp = TRUE;
    when '0001 110' => // AUTIB1716
        d = 17;
        n = 16;
        autib1716 = TRUE;
end;
```

---

## AUTIB171615 -- A64

### Encoding: `AUTIB171615_64LR_dp_1src`  — AUTIB171615

**Assembly:** `AUTIB171615`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;
```

---

## AUTIBSPPC -- A64

### Encoding: `AUTIBSPPC_only_dp_1src_imm`  — AUTIBSPPC

**Assembly:** `AUTIBSPPC  <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<label>` | imm16 (16b) | Is the program label whose address is to be calculated. Its negative offset from the address of this instruction, a mult | let offset : bits(64) = ZeroExtend{}(imm16::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let d : integer = 30;
let offset : bits(64) = ZeroExtend{}(imm16::'00');
let auth_combined : boolean = FALSE;
```

---

## AUTIBSPPCR -- A64

### Encoding: `AUTIBSPPCR_64LRR_dp_1src`  — AUTIBSPPCR

**Assembly:** `AUTIBSPPCR  <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let d : integer = 30;
let n : integer = UInt(Rn);
let auth_combined : boolean = FALSE;
```

---

## AXFLAG -- A64

### Encoding: `AXFLAG_M_pstate`  — AXFLAG

**Assembly:** `AXFLAG`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FlagM2) then EndOfDecode(Decode_UNDEF); end;
```

---

## B -- A64

### Encoding: `B_only_branch_imm`  — B

**Assembly:** `B  <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<label>` | imm26 (26b) | Is the program label to be unconditionally branched to. Its offset from the address of this instruction, in the range +/ | let offset : bits(64) = SignExtend{}(imm26::'00'); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm26::'00');
```

---

## B.cond -- A64

### Encoding: `B_only_condbranch`  — B

**Assembly:** `B.<cond>  <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<cond>` | — |  |  |
| `<label>` | imm19 (19b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range +/-1 | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm19::'00');
let condition : bits(4) = cond;
```

---

## BC.cond -- A64

### Encoding: `BC_only_condbranch`  — BC

**Assembly:** `BC.<cond>  <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<cond>` | — |  |  |
| `<label>` | imm19 (19b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range +/-1 | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_HBC) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm19::'00');
let condition : bits(4) = cond;
```

---

## BCAX -- A64

### Encoding: `BCAX_VVV16_crypto4`  — BCAX

**Assembly:** `BCAX  <Vd>.16B, <Vn>.16B, <Vm>.16B, <Va>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Va>` | Ra (5b) | Is the name of the third SIMD&FP source register, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
```

---

## BCAX -- A64

### Encoding: `bcax_z_zzz_`  — BCAX

**Assembly:** `BCAX  <Zdn>.D, <Zdn>.D, <Zm>.D, <Zk>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | Zk (5b) | Is the name of the third source scalable vector register, encoded in the "Zk" field. | let k : integer = UInt(Zk); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let m : integer = UInt(Zm);
let k : integer = UInt(Zk);
let dn : integer = UInt(Zdn);
```

---

## BDEP -- A64

### Encoding: `bdep_z_zz_`  — BDEP

**Assembly:** `BDEP  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_BitPerm) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## BEXT -- A64

### Encoding: `bext_z_zz_`  — BEXT

**Assembly:** `BEXT  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_BitPerm) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## BF1CVT, BF2CVT -- A64

### Encoding: `bf1cvt_mz2_z8_`  — BF1CVT

**Assembly:** `BF1CVT  { <Zd1>.H-<Zd2>.H }, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd:: '0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd:: '0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd:: '0');
let issrc2 : boolean = FALSE;
```

### Encoding: `bf2cvt_mz2_z8_`  — BF2CVT

**Assembly:** `BF2CVT  { <Zd1>.H-<Zd2>.H }, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd:: '0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd:: '0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd:: '0');
let issrc2 : boolean = TRUE;
```

---

## BF1CVT, BF2CVT -- A64

### Encoding: `bf1cvt_z_z8_b2bf`  — BF1CVT

**Assembly:** `BF1CVT  <Zd>.H, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 8;
let d_esize : integer{} = 16;
let issrc2 : boolean = FALSE;
```

### Encoding: `bf2cvt_z_z8_b2bf`  — BF2CVT

**Assembly:** `BF2CVT  <Zd>.H, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 8;
let d_esize : integer{} = 16;
let issrc2 : boolean = TRUE;
```

---

## BF1CVTL, BF1CVTL2, BF2CVTL, BF2CVTL2 -- A64

### Encoding: `BF1CVTL_asimdmisc_V`  [BF1CVTL{2}]  — BF1CVTL

**Assembly:** `BF1CVTL{2}  <Vd>.8H, <Vn>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let part : integer = UInt(Q);
let elements : integer = 64 DIV 8;
let issrc2 : boolean = size == '11';
```

### Encoding: `BF2CVTL_asimdmisc_V`  [BF2CVTL{2}]  — BF2CVTL

**Assembly:** `BF2CVTL{2}  <Vd>.8H, <Vn>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let part : integer = UInt(Q);
let elements : integer = 64 DIV 8;
let issrc2 : boolean = size == '11';
```

---

## BF1CVTL, BF2CVTL -- A64

### Encoding: `bf1cvtl_mz2_z8_`  — BF1CVTL

**Assembly:** `BF1CVTL  { <Zd1>.H-<Zd2>.H }, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd:: '0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd:: '0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd:: '0');
let issrc2 : boolean = FALSE;
```

### Encoding: `bf2cvtl_mz2_z8_`  — BF2CVTL

**Assembly:** `BF2CVTL  { <Zd1>.H-<Zd2>.H }, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd:: '0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd:: '0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd:: '0');
let issrc2 : boolean = TRUE;
```

---

## BF1CVTLT, BF2CVTLT -- A64

### Encoding: `bf1cvtlt_z_z8_b2bf`  — BF1CVTLT

**Assembly:** `BF1CVTLT  <Zd>.H, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let issrc2 : boolean = FALSE;
```

### Encoding: `bf2cvtlt_z_z8_b2bf`  — BF2CVTLT

**Assembly:** `BF2CVTLT  <Zd>.H, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let issrc2 : boolean = TRUE;
```

---

## BFADD (predicated) -- A64

### Encoding: `bfadd_z_p_zz_`  — BFADD

**Assembly:** `BFADD  <Zdn>.H, <Pg>/M, <Zdn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## BFADD (unpredicated) -- A64

### Encoding: `bfadd_z_zz_`  — BFADD

**Assembly:** `BFADD  <Zd>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## BFADD -- A64

### Encoding: `bfadd_za_zw_2x2_16`  — BFADD

**Assembly:** `BFADD  ZA.H[<Wv>, <offs>{, VGx2}], { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the source multi-vector gr | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" times 2 plus 1. | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `bfadd_za_zw_4x4_16`  — BFADD

**Assembly:** `BFADD  ZA.H[<Wv>, <offs>{, VGx4}], { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the source multi-vector g | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zm" times 4 plus 3. | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## BFC -- A64 *(alias for BFM)*

### Encoding: `BFC_BFM_32M_bitfield`  [32-bit]  — BFM

**Assembly:** `BFC  <Wd>, #<lsb>, #<width> / BFM  <Wd>, WZR, #(-<lsb>  MOD  32), #(<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<lsb>` | immr (6b) | For the "32-bit" variant: is the bit number of the lsb of the destination bitfield, in the range 0 to 31. |  |
| `<width>` | (imms :: immr) | For the "32-bit" variant: is the width of the bitfield, in the range 1 to 32-<lsb>. |  |
| `BFM` | — |  |  |

### Encoding: `BFC_BFM_64M_bitfield`  [64-bit]  — BFM

**Assembly:** `BFC  <Xd>, #<lsb>, #<width> / BFM  <Xd>, XZR, #(-<lsb>  MOD  64), #(<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<lsb>` | immr (6b) | For the "64-bit" variant: is the bit number of the lsb of the destination bitfield, in the range 0 to 63. |  |
| `<width>` | (imms :: immr) | For the "64-bit" variant: is the width of the bitfield, in the range 1 to 64-<lsb>. |  |
| `BFM` | — |  |  |

---

## BFCLAMP -- A64

### Encoding: `bfclamp_mz_zz_2`  — BFCLAMP

**Assembly:** `BFCLAMP  { <Zd1>.H-<Zd2>.H }, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfclamp_mz_zz_4`  — BFCLAMP

**Assembly:** `BFCLAMP  { <Zd1>.H-<Zd4>.H }, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
```

---

## BFCLAMP -- A64

### Encoding: `bfclamp_z_zz_`  — BFCLAMP

**Assembly:** `BFCLAMP  <Zd>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## BFCVT (BFloat16 to 8-bit floating-point) -- A64

### Encoding: `bfcvt_z8_mz2_`  — BFCVT

**Assembly:** `BFCVT  <Zd>.B, { <Zn1>.H-<Zn2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## BFCVT (single-precision to BFloat16) -- A64

### Encoding: `bfcvt_z_mz2_`  — BFCVT

**Assembly:** `BFCVT  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## BFCVT -- A64

### Encoding: `BFCVT_BS_floatdp1`  — BFCVT

**Assembly:** `BFCVT  <Hd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_BF16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## BFCVT -- A64

### Encoding: `bfcvt_z_p_z_s2bf`  — BFCVT

**Assembly:** `BFCVT  <Zd>.H, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_BF16)) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `bfcvt_z_p_z_s2bfz`  — BFCVT

**Assembly:** `BFCVT  <Zd>.H, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## BFCVTN -- A64

### Encoding: `bfcvtn_z8_mz2_bf2b`  — BFCVTN

**Assembly:** `BFCVTN  <Zd>.B, { <Zn1>.H-<Zn2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## BFCVTN -- A64

### Encoding: `bfcvtn_z_mz2_`  — BFCVTN

**Assembly:** `BFCVTN  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## BFCVTN, BFCVTN2 -- A64

### Encoding: `BFCVTN_asimdmisc_4S`  — BFCVTN

**Assembly:** `BFCVTN{2}  <Vd>.<Ta>, <Vn>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_BF16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let part : integer = UInt(Q);
let elements : integer = 64 DIV 16;
```

---

## BFCVTNT -- A64

### Encoding: `bfcvtnt_z_p_z_s2bf`  — BFCVTNT

**Assembly:** `BFCVTNT  <Zd>.H, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_BF16)) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `bfcvtnt_z_p_z_s2bfz`  — BFCVTNT

**Assembly:** `BFCVTNT  <Zd>.H, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## BFDOT (by element) -- A64

### Encoding: `BFDOT_asimdelem_E`  — BFDOT

**Assembly:** `BFDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.2H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | (M :: Rm) | Is the name of the second SIMD&FP source register, encoded in the "M:Rm" fields. |  |
| `<index>` | (H :: L) | Is the immediate index of a pair of 16-bit elements in the range 0 to 3, encoded in the "H:L" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_BF16) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(M::Rm);
let d : integer = UInt(Rd);
let i : integer = UInt(H::L);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 32;
```

---

## BFDOT (indexed) -- A64

### Encoding: `bfdot_z_zzzi_`  — BFDOT

**Assembly:** `BFDOT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_BF16)) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i2);
```

---

## BFDOT (multiple and indexed vector) -- A64

### Encoding: `bfdot_za_zzi_2xi`  — BFDOT

**Assembly:** `BFDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `bfdot_za_zzi_4xi`  — BFDOT

**Assembly:** `BFDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## BFDOT (multiple and single vector) -- A64

### Encoding: `bfdot_za_zzv_2x1`  — BFDOT

**Assembly:** `BFDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `bfdot_za_zzv_4x1`  — BFDOT

**Assembly:** `BFDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## BFDOT (multiple vectors) -- A64

### Encoding: `bfdot_za_zzw_2x2`  — BFDOT

**Assembly:** `BFDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `bfdot_za_zzw_4x4`  — BFDOT

**Assembly:** `BFDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## BFDOT (vector) -- A64

### Encoding: `BFDOT_asimdsame2_D`  — BFDOT

**Assembly:** `BFDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_BF16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 32;
```

---

## BFDOT (vectors) -- A64

### Encoding: `bfdot_z_zzz_`  — BFDOT

**Assembly:** `BFDOT  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_BF16)) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## BFI -- A64 *(alias for BFM)*

### Encoding: `BFI_BFM_32M_bitfield`  [32-bit]  — BFM

**Assembly:** `BFI  <Wd>, <Wn>, #<lsb>, #<width> / BFM  <Wd>, <Wn>, #(-<lsb>  MOD  32), #(<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "32-bit" variant: is the bit number of the lsb of the destination bitfield, in the range 0 to 31. |  |
| `<width>` | (imms :: immr) | For the "32-bit" variant: is the width of the bitfield, in the range 1 to 32-<lsb>. |  |
| `BFM` | — |  |  |

### Encoding: `BFI_BFM_64M_bitfield`  [64-bit]  — BFM

**Assembly:** `BFI  <Xd>, <Xn>, #<lsb>, #<width> / BFM  <Xd>, <Xn>, #(-<lsb>  MOD  64), #(<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "64-bit" variant: is the bit number of the lsb of the destination bitfield, in the range 0 to 63. |  |
| `<width>` | (imms :: immr) | For the "64-bit" variant: is the width of the bitfield, in the range 1 to 64-<lsb>. |  |
| `BFM` | — |  |  |

---

## BFM -- A64

### Encoding: `BFM_32M_bitfield`  [32-bit]  — BFM

**Assembly:** `BFM  <Wd>, <Wn>, #<immr>, #<imms>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<immr>` | immr (6b) | For the "32-bit" variant: is the right rotate amount, in the range 0 to 31, encoded in the "immr" field. | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let r : integer = UInt(immr);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |
| `<imms>` | imms (6b) | For the "32-bit" variant: is the leftmost bit number to be moved from the source, in the range 0 to 31, encoded in the " | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let s : integer = UInt(imms);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |

**Pseudocode (Decode):**
```
if sf == '1' && N != '1' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && (N != '0' || immr[5] != '0' || imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let s : integer = UInt(imms);
let r : integer = UInt(immr);

var wmask : bits(datasize);
var tmask : bits(datasize);
(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE);
```

### Encoding: `BFM_64M_bitfield`  [64-bit]  — BFM

**Assembly:** `BFM  <Xd>, <Xn>, #<immr>, #<imms>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<immr>` | immr (6b) | For the "64-bit" variant: is the right rotate amount, in the range 0 to 63, encoded in the "immr" field. | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let r : integer = UInt(immr);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |
| `<imms>` | imms (6b) | For the "64-bit" variant: is the leftmost bit number to be moved from the source, in the range 0 to 63, encoded in the " | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let s : integer = UInt(imms);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |

**Pseudocode (Decode):**
```
if sf == '1' && N != '1' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && (N != '0' || immr[5] != '0' || imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let s : integer = UInt(imms);
let r : integer = UInt(immr);

var wmask : bits(datasize);
var tmask : bits(datasize);
(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE);
```

---

## BFMAX (multiple and single vector) -- A64

### Encoding: `bfmax_mz_zzv_2x1`  — BFMAX

**Assembly:** `BFMAX  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `bfmax_mz_zzv_4x1`  — BFMAX

**Assembly:** `BFMAX  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## BFMAX (multiple vectors) -- A64

### Encoding: `bfmax_mz_zzw_2x2`  — BFMAX

**Assembly:** `BFMAX  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfmax_mz_zzw_4x4`  — BFMAX

**Assembly:** `BFMAX  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## BFMAX -- A64

### Encoding: `bfmax_z_p_zz_`  — BFMAX

**Assembly:** `BFMAX  <Zdn>.H, <Pg>/M, <Zdn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## BFMAXNM (multiple and single vector) -- A64

### Encoding: `bfmaxnm_mz_zzv_2x1`  — BFMAXNM

**Assembly:** `BFMAXNM  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `bfmaxnm_mz_zzv_4x1`  — BFMAXNM

**Assembly:** `BFMAXNM  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## BFMAXNM (multiple vectors) -- A64

### Encoding: `bfmaxnm_mz_zzw_2x2`  — BFMAXNM

**Assembly:** `BFMAXNM  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfmaxnm_mz_zzw_4x4`  — BFMAXNM

**Assembly:** `BFMAXNM  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## BFMAXNM -- A64

### Encoding: `bfmaxnm_z_p_zz_`  — BFMAXNM

**Assembly:** `BFMAXNM  <Zdn>.H, <Pg>/M, <Zdn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## BFMIN (multiple and single vector) -- A64

### Encoding: `bfmin_mz_zzv_2x1`  — BFMIN

**Assembly:** `BFMIN  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `bfmin_mz_zzv_4x1`  — BFMIN

**Assembly:** `BFMIN  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## BFMIN (multiple vectors) -- A64

### Encoding: `bfmin_mz_zzw_2x2`  — BFMIN

**Assembly:** `BFMIN  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfmin_mz_zzw_4x4`  — BFMIN

**Assembly:** `BFMIN  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## BFMIN -- A64

### Encoding: `bfmin_z_p_zz_`  — BFMIN

**Assembly:** `BFMIN  <Zdn>.H, <Pg>/M, <Zdn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## BFMINNM (multiple and single vector) -- A64

### Encoding: `bfminnm_mz_zzv_2x1`  — BFMINNM

**Assembly:** `BFMINNM  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `bfminnm_mz_zzv_4x1`  — BFMINNM

**Assembly:** `BFMINNM  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## BFMINNM (multiple vectors) -- A64

### Encoding: `bfminnm_mz_zzw_2x2`  — BFMINNM

**Assembly:** `BFMINNM  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfminnm_mz_zzw_4x4`  — BFMINNM

**Assembly:** `BFMINNM  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## BFMINNM -- A64

### Encoding: `bfminnm_z_p_zz_`  — BFMINNM

**Assembly:** `BFMINNM  <Zdn>.H, <Pg>/M, <Zdn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## BFMLA (indexed) -- A64

### Encoding: `bfmla_z_zzzi_h`  — BFMLA

**Assembly:** `BFMLA  <Zda>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
let op3_neg : boolean = FALSE;
```

---

## BFMLA (multiple and indexed vector) -- A64

### Encoding: `bfmla_za_zzi_h2xi`  — BFMLA

**Assembly:** `BFMLA  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `bfmla_za_zzi_h4xi`  — BFMLA

**Assembly:** `BFMLA  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## BFMLA (multiple and single vector) -- A64

### Encoding: `bfmla_za_zzv_2x1_16`  — BFMLA

**Assembly:** `BFMLA  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `bfmla_za_zzv_4x1_16`  — BFMLA

**Assembly:** `BFMLA  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## BFMLA (multiple vectors) -- A64

### Encoding: `bfmla_za_zzw_2x2_16`  — BFMLA

**Assembly:** `BFMLA  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `bfmla_za_zzw_4x4_16`  — BFMLA

**Assembly:** `BFMLA  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## BFMLA (vectors) -- A64

### Encoding: `bfmla_z_p_zzz_`  — BFMLA

**Assembly:** `BFMLA  <Zda>.H, <Pg>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
let op3_neg : boolean = FALSE;
```

---

## BFMLAL (multiple and indexed vector) -- A64

### Encoding: `bfmlal_za_zzi_1`  — BFMLAL

**Assembly:** `BFMLAL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `bfmlal_za_zzi_2xi`  — BFMLAL

**Assembly:** `BFMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `bfmlal_za_zzi_4xi`  — BFMLAL

**Assembly:** `BFMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## BFMLAL (multiple and single vector) -- A64

### Encoding: `bfmlal_za_zzv_1`  — BFMLAL

**Assembly:** `BFMLAL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let nreg : integer{} = 1;
```

### Encoding: `bfmlal_za_zzv_2x1`  — BFMLAL

**Assembly:** `BFMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfmlal_za_zzv_4x1`  — BFMLAL

**Assembly:** `BFMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## BFMLAL (multiple vectors) -- A64

### Encoding: `bfmlal_za_zzw_2x2`  — BFMLAL

**Assembly:** `BFMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfmlal_za_zzw_4x4`  — BFMLAL

**Assembly:** `BFMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## BFMLALB (indexed) -- A64

### Encoding: `bfmlalb_z_zzzi_`  — BFMLALB

**Assembly:** `BFMLALB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_BF16)) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i3h::i3l);
let op1_neg : boolean = FALSE;
```

---

## BFMLALB (vectors) -- A64

### Encoding: `bfmlalb_z_zzz_`  — BFMLALB

**Assembly:** `BFMLALB  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_BF16)) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
```

---

## BFMLALB, BFMLALT (by element) -- A64

### Encoding: `BFMLAL_asimdelem_F`  — BFMLAL

**Assembly:** `BFMLAL<bt>  <Vd>.4S, <Vn>.8H, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<bt>` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, in the range V0 to V15, encoded in the "Rm" field. | let m : integer = UInt('0'::Rm); |
| `<index>` | (H :: L :: M) | Is the element index, in the range 0 to 7, encoded in the "H:L:M" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_BF16) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt('0'::Rm);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M);

let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q);
```

---

## BFMLALB, BFMLALT (vector) -- A64

### Encoding: `BFMLAL_asimdsame2_F_`  — BFMLAL

**Assembly:** `BFMLAL<bt>  <Vd>.4S, <Vn>.8H, <Vm>.8H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<bt>` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_BF16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q);
```

---

## BFMLALT (indexed) -- A64

### Encoding: `bfmlalt_z_zzzi_`  — BFMLALT

**Assembly:** `BFMLALT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_BF16)) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i3h::i3l);
let op1_neg : boolean = FALSE;
```

---

## BFMLALT (vectors) -- A64

### Encoding: `bfmlalt_z_zzz_`  — BFMLALT

**Assembly:** `BFMLALT  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_BF16)) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
```

---

## BFMLS (indexed) -- A64

### Encoding: `bfmls_z_zzzi_h`  — BFMLS

**Assembly:** `BFMLS  <Zda>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
let op3_neg : boolean = FALSE;
```

---

## BFMLS (multiple and indexed vector) -- A64

### Encoding: `bfmls_za_zzi_h2xi`  — BFMLS

**Assembly:** `BFMLS  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `bfmls_za_zzi_h4xi`  — BFMLS

**Assembly:** `BFMLS  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## BFMLS (multiple and single vector) -- A64

### Encoding: `bfmls_za_zzv_2x1_16`  — BFMLS

**Assembly:** `BFMLS  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `bfmls_za_zzv_4x1_16`  — BFMLS

**Assembly:** `BFMLS  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## BFMLS (multiple vectors) -- A64

### Encoding: `bfmls_za_zzw_2x2_16`  — BFMLS

**Assembly:** `BFMLS  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `bfmls_za_zzw_4x4_16`  — BFMLS

**Assembly:** `BFMLS  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## BFMLS (vectors) -- A64

### Encoding: `bfmls_z_p_zzz_`  — BFMLS

**Assembly:** `BFMLS  <Zda>.H, <Pg>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
let op3_neg : boolean = FALSE;
```

---

## BFMLSL (multiple and indexed vector) -- A64

### Encoding: `bfmlsl_za_zzi_1`  — BFMLSL

**Assembly:** `BFMLSL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `bfmlsl_za_zzi_2xi`  — BFMLSL

**Assembly:** `BFMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `bfmlsl_za_zzi_4xi`  — BFMLSL

**Assembly:** `BFMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## BFMLSL (multiple and single vector) -- A64

### Encoding: `bfmlsl_za_zzv_1`  — BFMLSL

**Assembly:** `BFMLSL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let nreg : integer{} = 1;
```

### Encoding: `bfmlsl_za_zzv_2x1`  — BFMLSL

**Assembly:** `BFMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfmlsl_za_zzv_4x1`  — BFMLSL

**Assembly:** `BFMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## BFMLSL (multiple vectors) -- A64

### Encoding: `bfmlsl_za_zzw_2x2`  — BFMLSL

**Assembly:** `BFMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfmlsl_za_zzw_4x4`  — BFMLSL

**Assembly:** `BFMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## BFMLSLB (indexed) -- A64

### Encoding: `bfmlslb_z_zzzi_`  — BFMLSLB

**Assembly:** `BFMLSLB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i3h::i3l);
let op1_neg : boolean = TRUE;
```

---

## BFMLSLB (vectors) -- A64

### Encoding: `bfmlslb_z_zzz_`  — BFMLSLB

**Assembly:** `BFMLSLB  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
```

---

## BFMLSLT (indexed) -- A64

### Encoding: `bfmlslt_z_zzzi_`  — BFMLSLT

**Assembly:** `BFMLSLT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i3h::i3l);
let op1_neg : boolean = TRUE;
```

---

## BFMLSLT (vectors) -- A64

### Encoding: `bfmlslt_z_zzz_`  — BFMLSLT

**Assembly:** `BFMLSLT  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
```

---

## BFMMLA (non-widening) -- A64

### Encoding: `bfmmla_z_zzz_h`  — BFMMLA

**Assembly:** `BFMMLA  <Zda>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16MM) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## BFMMLA (widening) -- A64

### Encoding: `BFMMLA_asimdsame2_E`  — BFMMLA

**Assembly:** `BFMMLA  <Vd>.4S, <Vn>.8H, <Vm>.8H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_BF16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## BFMMLA (widening) -- A64

### Encoding: `bfmmla_z_zzz_`  — BFMMLA

**Assembly:** `BFMMLA  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_BF16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## BFMOP4A (non-widening) -- A64

### Encoding: `bfmop4a_za_zz_h1x2`  — BFMOP4A

**Assembly:** `BFMOP4A  <ZAda>.H, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `bfmop4a_za_zz_h1x1`  — BFMOP4A

**Assembly:** `BFMOP4A  <ZAda>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `bfmop4a_za_zz_h2x1`  — BFMOP4A

**Assembly:** `BFMOP4A  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `bfmop4a_za_zz_h2x2`  — BFMOP4A

**Assembly:** `BFMOP4A  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

---

## BFMOP4A (widening) -- A64

### Encoding: `bfmop4a_za32_zz_h1x2`  — BFMOP4A

**Assembly:** `BFMOP4A  <ZAda>.S, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `bfmop4a_za32_zz_h1x1`  — BFMOP4A

**Assembly:** `BFMOP4A  <ZAda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `bfmop4a_za32_zz_h2x1`  — BFMOP4A

**Assembly:** `BFMOP4A  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `bfmop4a_za32_zz_h2x2`  — BFMOP4A

**Assembly:** `BFMOP4A  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

---

## BFMOP4S (non-widening) -- A64

### Encoding: `bfmop4s_za_zz_h1x2`  — BFMOP4S

**Assembly:** `BFMOP4S  <ZAda>.H, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `bfmop4s_za_zz_h1x1`  — BFMOP4S

**Assembly:** `BFMOP4S  <ZAda>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `bfmop4s_za_zz_h2x1`  — BFMOP4S

**Assembly:** `BFMOP4S  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `bfmop4s_za_zz_h2x2`  — BFMOP4S

**Assembly:** `BFMOP4S  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

---

## BFMOP4S (widening) -- A64

### Encoding: `bfmop4s_za32_zz_h1x2`  — BFMOP4S

**Assembly:** `BFMOP4S  <ZAda>.S, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `bfmop4s_za32_zz_h1x1`  — BFMOP4S

**Assembly:** `BFMOP4S  <ZAda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `bfmop4s_za32_zz_h2x1`  — BFMOP4S

**Assembly:** `BFMOP4S  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `bfmop4s_za32_zz_h2x2`  — BFMOP4S

**Assembly:** `BFMOP4S  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

---

## BFMOPA (non-widening) -- A64

### Encoding: `bfmopa_za_pp_zz_16`  — BFMOPA

**Assembly:** `BFMOPA  <ZAda>.H, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## BFMOPA (widening) -- A64

### Encoding: `bfmopa_za32_pp_zz_`  — BFMOPA

**Assembly:** `BFMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## BFMOPS (non-widening) -- A64

### Encoding: `bfmops_za_pp_zz_16`  — BFMOPS

**Assembly:** `BFMOPS  <ZAda>.H, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## BFMOPS (widening) -- A64

### Encoding: `bfmops_za32_pp_zz_`  — BFMOPS

**Assembly:** `BFMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## BFMUL (indexed) -- A64

### Encoding: `bfmul_z_zzi_h`  — BFMUL

**Assembly:** `BFMUL  <Zd>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## BFMUL (multiple and single vector) -- A64

### Encoding: `bfmul_mz_zzv_2x1`  — BFMUL

**Assembly:** `BFMUL  { <Zd1>.H-<Zd2>.H }, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the first source multi-vector grou | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_BFSCALE) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer = UInt(Zd::'0');
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `bfmul_mz_zzv_4x1`  — BFMUL

**Assembly:** `BFMUL  { <Zd1>.H-<Zd4>.H }, { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the first source multi-vector gro | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_BFSCALE) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer = UInt(Zd::'00');
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## BFMUL (multiple vectors) -- A64

### Encoding: `bfmul_mz_zzw_2x2`  — BFMUL

**Assembly:** `BFMUL  { <Zd1>.H-<Zd2>.H }, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the first source multi-vector grou | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_BFSCALE) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer = UInt(Zd::'0');
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfmul_mz_zzw_4x4`  — BFMUL

**Assembly:** `BFMUL  { <Zd1>.H-<Zd4>.H }, { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the first source multi-vector gro | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_BFSCALE) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer = UInt(Zd::'00');
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## BFMUL (vectors, predicated) -- A64

### Encoding: `bfmul_z_p_zz_`  — BFMUL

**Assembly:** `BFMUL  <Zdn>.H, <Pg>/M, <Zdn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## BFMUL (vectors, unpredicated) -- A64

### Encoding: `bfmul_z_zz_`  — BFMUL

**Assembly:** `BFMUL  <Zd>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## BFSCALE (multiple and single vector) -- A64

### Encoding: `bfscale_mz_zzv_2x1`  — BFSCALE

**Assembly:** `BFSCALE  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_BFSCALE) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `bfscale_mz_zzv_4x1`  — BFSCALE

**Assembly:** `BFSCALE  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_BFSCALE) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## BFSCALE (multiple vectors) -- A64

### Encoding: `bfscale_mz_zzw_2x2`  — BFSCALE

**Assembly:** `BFSCALE  { <Zdn1>.H-<Zdn2>.H }, { <Zdn1>.H-<Zdn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_BFSCALE) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `bfscale_mz_zzw_4x4`  — BFSCALE

**Assembly:** `BFSCALE  { <Zdn1>.H-<Zdn4>.H }, { <Zdn1>.H-<Zdn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_SVE_BFSCALE) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## BFSCALE -- A64

### Encoding: `bfscale_z_p_zz_`  — BFSCALE

**Assembly:** `BFSCALE  <Zdn>.H, <Pg>/M, <Zdn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_BFSCALE) then EndOfDecode(Decode_UNDEF); end;

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## BFSUB (predicated) -- A64

### Encoding: `bfsub_z_p_zz_`  — BFSUB

**Assembly:** `BFSUB  <Zdn>.H, <Pg>/M, <Zdn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## BFSUB (unpredicated) -- A64

### Encoding: `bfsub_z_zz_`  — BFSUB

**Assembly:** `BFSUB  <Zd>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_B16B16) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## BFSUB -- A64

### Encoding: `bfsub_za_zw_2x2_16`  — BFSUB

**Assembly:** `BFSUB  ZA.H[<Wv>, <offs>{, VGx2}], { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the source multi-vector gr | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" times 2 plus 1. | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `bfsub_za_zw_4x4_16`  — BFSUB

**Assembly:** `BFSUB  ZA.H[<Wv>, <offs>{, VGx4}], { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the source multi-vector g | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zm" times 4 plus 3. | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_B16B16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## BFTMOPA (non-widening) -- A64

### Encoding: `bftmopa_za_zzzi_h2x1`  — BFTMOPA

**Assembly:** `BFTMOPA  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, <Zm>.H, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) || !IsFeatureImplemented(FEAT_SME_B16B16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
```

---

## BFTMOPA (widening) -- A64

### Encoding: `bftmopa_za32_zzzi_h2x1`  — BFTMOPA

**Assembly:** `BFTMOPA  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
```

---

## BFVDOT -- A64

### Encoding: `bfvdot_za_zzi_2xi`  — BFVDOT

**Assembly:** `BFVDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
```

---

## BFXIL -- A64 *(alias for BFM)*

### Encoding: `BFXIL_BFM_32M_bitfield`  [32-bit]  — BFM

**Assembly:** `BFXIL  <Wd>, <Wn>, #<lsb>, #<width> / BFM  <Wd>, <Wn>, #<lsb>, #(<lsb>+<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "32-bit" variant: is the bit number of the lsb of the source bitfield, in the range 0 to 31. |  |
| `<width>` | (imms :: immr) | For the "32-bit" variant: is the width of the bitfield, in the range 1 to 32-<lsb>. |  |
| `BFM` | — |  |  |

### Encoding: `BFXIL_BFM_64M_bitfield`  [64-bit]  — BFM

**Assembly:** `BFXIL  <Xd>, <Xn>, #<lsb>, #<width> / BFM  <Xd>, <Xn>, #<lsb>, #(<lsb>+<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "64-bit" variant: is the bit number of the lsb of the source bitfield, in the range 0 to 63. |  |
| `<width>` | (imms :: immr) | For the "64-bit" variant: is the width of the bitfield, in the range 1 to 64-<lsb>. |  |
| `BFM` | — |  |  |

---

## BGRP -- A64

### Encoding: `bgrp_z_zz_`  — BGRP

**Assembly:** `BGRP  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_BitPerm) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## BIC (immediate) -- A64 *(alias for and_z_zi)*

### Encoding: `bic_z_zi__and_z_zi_`  — AND

**Assembly:** `BIC  <Zdn>.<T>, <Zdn>.<T>, #<const> / AND  <Zdn>.<T>, <Zdn>.<T>, #(-<const> - 1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. |  |
| `<T>` | — |  |  |
| `<const>` | imm13 (13b) | Is a 64, 32, 16 or 8-bit bitmask consisting of replicated 2, 4, 8, 16, 32 or 64 bit fields, each field containing a rota |  |
| `AND` | — |  |  |

---

## BIC (predicates) -- A64

### Encoding: `bic_p_p_pp_z`  — BIC

**Assembly:** `BIC  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## BIC (shifted register) -- A64

### Encoding: `BIC_32_log_shift`  [32-bit]  — BIC

**Assembly:** `BIC  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `BIC_64_log_shift`  [64-bit]  — BIC

**Assembly:** `BIC  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## BIC (vector, immediate) -- A64

### Encoding: `BIC_asimdimm_L_hl`  [16-bit]  — BIC

**Assembly:** `BIC  <Vd>.<T>, #<imm8>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `BIC_asimdimm_L_sl`  [32-bit]  — BIC

**Assembly:** `BIC  <Vd>.<T>, #<imm8>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

---

## BIC (vector, register) -- A64

### Encoding: `BIC_asimdsame_only`  — BIC

**Assembly:** `BIC  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
```

---

## BIC (vectors, predicated) -- A64

### Encoding: `bic_z_p_zz_`  — BIC

**Assembly:** `BIC  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## BIC (vectors, unpredicated) -- A64

### Encoding: `bic_z_zz_`  — BIC

**Assembly:** `BIC  <Zd>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## BICS (shifted register) -- A64

### Encoding: `BICS_32_log_shift`  [32-bit]  — BICS

**Assembly:** `BICS  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `BICS_64_log_shift`  [64-bit]  — BICS

**Assembly:** `BICS  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## BICS -- A64

### Encoding: `bics_p_p_pp_z`  — BICS

**Assembly:** `BICS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## BIF -- A64

### Encoding: `BIF_asimdsame_only`  — BIF

**Assembly:** `BIF  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
```

---

## BIT -- A64

### Encoding: `BIT_asimdsame_only`  — BIT

**Assembly:** `BIT  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
```

---

## BL -- A64

### Encoding: `BL_only_branch_imm`  — BL

**Assembly:** `BL  <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<label>` | imm26 (26b) | Is the program label to be unconditionally branched to. Its offset from the address of this instruction, in the range +/ | let offset : bits(64) = SignExtend{}(imm26::'00'); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm26::'00');
let d : integer = 30;
```

---

## BLR -- A64

### Encoding: `BLR_64_branch_reg`  — BLR

**Assembly:** `BLR  <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
```

---

## BLRAA, BLRAAZ, BLRAB, BLRABZ -- A64

### Encoding: `BLRAA_64P_branch_reg`  [Key A, register modifier]  — BLRAA

**Assembly:** `BLRAA  <Xn>, <Xm|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm\|SP>` | Rm (5b) | Is the 64-bit name of the general-purpose source register or stack pointer holding the modifier, encoded in the "Rm" fie | if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let use_key_a : boolean = (M == '0');
let source_is_sp : boolean = ((Z == '1') && (m == 31));
let auth_then_branch : boolean = TRUE;
```

### Encoding: `BLRAAZ_64_branch_reg`  [Key A, zero modifier]  — BLRAAZ

**Assembly:** `BLRAAZ  <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let use_key_a : boolean = (M == '0');
let source_is_sp : boolean = ((Z == '1') && (m == 31));
let auth_then_branch : boolean = TRUE;
```

### Encoding: `BLRAB_64P_branch_reg`  [Key B, register modifier]  — BLRAB

**Assembly:** `BLRAB  <Xn>, <Xm|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm\|SP>` | Rm (5b) | Is the 64-bit name of the general-purpose source register or stack pointer holding the modifier, encoded in the "Rm" fie | if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let use_key_a : boolean = (M == '0');
let source_is_sp : boolean = ((Z == '1') && (m == 31));
let auth_then_branch : boolean = TRUE;
```

### Encoding: `BLRABZ_64_branch_reg`  [Key B, zero modifier]  — BLRABZ

**Assembly:** `BLRABZ  <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let use_key_a : boolean = (M == '0');
let source_is_sp : boolean = ((Z == '1') && (m == 31));
let auth_then_branch : boolean = TRUE;
```

---

## BMOPA -- A64

### Encoding: `bmopa_za_pp_zz_32`  — BMOPA

**Assembly:** `BMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.S, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## BMOPS -- A64

### Encoding: `bmops_za_pp_zz_32`  — BMOPS

**Assembly:** `BMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.S, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## BR -- A64

### Encoding: `BR_64_branch_reg`  — BR

**Assembly:** `BR  <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
```

---

## BRAA, BRAAZ, BRAB, BRABZ -- A64

### Encoding: `BRAA_64P_branch_reg`  [Key A, register modifier]  — BRAA

**Assembly:** `BRAA  <Xn>, <Xm|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm\|SP>` | Rm (5b) | Is the 64-bit name of the general-purpose source register or stack pointer holding the modifier, encoded in the "Rm" fie | if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let use_key_a : boolean = (M == '0');
let source_is_sp : boolean = ((Z == '1') && (m == 31));
let auth_then_branch : boolean = TRUE;
```

### Encoding: `BRAAZ_64_branch_reg`  [Key A, zero modifier]  — BRAAZ

**Assembly:** `BRAAZ  <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let use_key_a : boolean = (M == '0');
let source_is_sp : boolean = ((Z == '1') && (m == 31));
let auth_then_branch : boolean = TRUE;
```

### Encoding: `BRAB_64P_branch_reg`  [Key B, register modifier]  — BRAB

**Assembly:** `BRAB  <Xn>, <Xm|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm\|SP>` | Rm (5b) | Is the 64-bit name of the general-purpose source register or stack pointer holding the modifier, encoded in the "Rm" fie | if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let use_key_a : boolean = (M == '0');
let source_is_sp : boolean = ((Z == '1') && (m == 31));
let auth_then_branch : boolean = TRUE;
```

### Encoding: `BRABZ_64_branch_reg`  [Key B, zero modifier]  — BRABZ

**Assembly:** `BRABZ  <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
if Z == '0' && Rm != '11111' then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let use_key_a : boolean = (M == '0');
let source_is_sp : boolean = ((Z == '1') && (m == 31));
let auth_then_branch : boolean = TRUE;
```

---

## BRB -- A64 *(alias for SYS)*

### Encoding: `BRB_SYS_CR_systeminstrs`  — SYS

**Assembly:** `BRB  <brb_op> / SYS  #1, C7, C2, #<op2>{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<brb_op>` | — |  |  |
| `SYS` | — |  |  |

---

## BRK -- A64

### Encoding: `BRK_EX_exception`  — BRK

**Assembly:** `BRK  #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | imm16 (16b) | Is a 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let comment : bits(16) = imm16; |

**Pseudocode (Decode):**
```
let comment : bits(16) = imm16;
if IsFeatureImplemented(FEAT_BTI) then
    SetBTypeCompatible(TRUE);
end;
```

---

## BRKA -- A64

### Encoding: `brka_p_p_p_`  — BRKA

**Assembly:** `BRKA  <Pd>.B, <Pg>/<ZM>, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<ZM>` | — |  |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let d : integer = UInt(Pd);
let merging : boolean = (M == '1');
let setflags : boolean = FALSE;
```

---

## BRKAS -- A64

### Encoding: `brkas_p_p_p_z`  — BRKAS

**Assembly:** `BRKAS  <Pd>.B, <Pg>/Z, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let d : integer = UInt(Pd);
let merging : boolean = FALSE;
let setflags : boolean = TRUE;
```

---

## BRKB -- A64

### Encoding: `brkb_p_p_p_`  — BRKB

**Assembly:** `BRKB  <Pd>.B, <Pg>/<ZM>, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<ZM>` | — |  |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let d : integer = UInt(Pd);
let merging : boolean = (M == '1');
let setflags : boolean = FALSE;
```

---

## BRKBS -- A64

### Encoding: `brkbs_p_p_p_z`  — BRKBS

**Assembly:** `BRKBS  <Pd>.B, <Pg>/Z, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let d : integer = UInt(Pd);
let merging : boolean = FALSE;
let setflags : boolean = TRUE;
```

---

## BRKN -- A64

### Encoding: `brkn_p_p_pp_`  — BRKN

**Assembly:** `BRKN  <Pdm>.B, <Pg>/Z, <Pn>.B, <Pdm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pdm>` | Pdm (4b) | Is the name of the second source and destination scalable predicate register, encoded in the "Pdm" field. | let dm : integer = UInt(Pdm); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let dm : integer = UInt(Pdm);
let setflags : boolean = FALSE;
```

---

## BRKNS -- A64

### Encoding: `brkns_p_p_pp_`  — BRKNS

**Assembly:** `BRKNS  <Pdm>.B, <Pg>/Z, <Pn>.B, <Pdm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pdm>` | Pdm (4b) | Is the name of the second source and destination scalable predicate register, encoded in the "Pdm" field. | let dm : integer = UInt(Pdm); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let dm : integer = UInt(Pdm);
let setflags : boolean = TRUE;
```

---

## BRKPA -- A64

### Encoding: `brkpa_p_p_pp_`  — BRKPA

**Assembly:** `BRKPA  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## BRKPAS -- A64

### Encoding: `brkpas_p_p_pp_`  — BRKPAS

**Assembly:** `BRKPAS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## BRKPB -- A64

### Encoding: `brkpb_p_p_pp_`  — BRKPB

**Assembly:** `BRKPB  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## BRKPBS -- A64

### Encoding: `brkpbs_p_p_pp_`  — BRKPBS

**Assembly:** `BRKPBS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## BSL -- A64

### Encoding: `BSL_asimdsame_only`  — BSL

**Assembly:** `BSL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
```

---

## BSL -- A64

### Encoding: `bsl_z_zzz_`  — BSL

**Assembly:** `BSL  <Zdn>.D, <Zdn>.D, <Zm>.D, <Zk>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | Zk (5b) | Is the name of the third source scalable vector register, encoded in the "Zk" field. | let k : integer = UInt(Zk); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let m : integer = UInt(Zm);
let k : integer = UInt(Zk);
let dn : integer = UInt(Zdn);
```

---

## BSL1N -- A64

### Encoding: `bsl1n_z_zzz_`  — BSL1N

**Assembly:** `BSL1N  <Zdn>.D, <Zdn>.D, <Zm>.D, <Zk>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | Zk (5b) | Is the name of the third source scalable vector register, encoded in the "Zk" field. | let k : integer = UInt(Zk); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let m : integer = UInt(Zm);
let k : integer = UInt(Zk);
let dn : integer = UInt(Zdn);
```

---

## BSL2N -- A64

### Encoding: `bsl2n_z_zzz_`  — BSL2N

**Assembly:** `BSL2N  <Zdn>.D, <Zdn>.D, <Zm>.D, <Zk>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | Zk (5b) | Is the name of the third source scalable vector register, encoded in the "Zk" field. | let k : integer = UInt(Zk); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let m : integer = UInt(Zm);
let k : integer = UInt(Zk);
let dn : integer = UInt(Zdn);
```

---

## BTI -- A64

### Encoding: `BTI_HB_hints`  — BTI

**Assembly:** `BTI  {<targets>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<targets>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_BTI) then EndOfDecode(Decode_NOP); end;

// Check branch target compatibility between BTI instruction and PSTATE.BTYPE
SetBTypeCompatible(BTypeCompatible_BTI(op2[2:1]));
```

---

## CADD -- A64

### Encoding: `cadd_z_zz_`  — CADD

**Assembly:** `CADD  <Zdn>.<T>, <Zdn>.<T>, <Zm>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
let sub_i : boolean = (rot == '0');
let sub_r : boolean = (rot == '1');
```

---

## CAS, CASA, CASAL, CASL -- A64

### Encoding: `CAS_C32_comswap`  [32-bit CAS]  — CAS

**Assembly:** `CAS  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASA_C32_comswap`  [32-bit CASA]  — CASA

**Assembly:** `CASA  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASAL_C32_comswap`  [32-bit CASAL]  — CASAL

**Assembly:** `CASAL  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASL_C32_comswap`  [32-bit CASL]  — CASL

**Assembly:** `CASL  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CAS_C64_comswap`  [64-bit CAS]  — CAS

**Assembly:** `CAS  <Xs>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASA_C64_comswap`  [64-bit CASA]  — CASA

**Assembly:** `CASA  <Xs>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASAL_C64_comswap`  [64-bit CASAL]  — CASAL

**Assembly:** `CASAL  <Xs>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASL_C64_comswap`  [64-bit CASL]  — CASL

**Assembly:** `CASL  <Xs>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

---

## CASB, CASAB, CASALB, CASLB -- A64

### Encoding: `CASB_C32_comswap`  [CASB]  — CASB

**Assembly:** `CASB  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASAB_C32_comswap`  [CASAB]  — CASAB

**Assembly:** `CASAB  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASALB_C32_comswap`  [CASALB]  — CASALB

**Assembly:** `CASALB  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASLB_C32_comswap`  [CASLB]  — CASLB

**Assembly:** `CASLB  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

---

## CASH, CASAH, CASALH, CASLH -- A64

### Encoding: `CASH_C32_comswap`  [CASH]  — CASH

**Assembly:** `CASH  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASAH_C32_comswap`  [CASAH]  — CASAH

**Assembly:** `CASAH  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASALH_C32_comswap`  [CASALH]  — CASALH

**Assembly:** `CASALH  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASLH_C32_comswap`  [CASLH]  — CASLH

**Assembly:** `CASLH  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

---

## CASP, CASPA, CASPAL, CASPL -- A64

### Encoding: `CASP_CP32_comswappr`  [32-bit CASP]  — CASP

**Assembly:** `CASP  <Ws>, <W(s+1)>, <Wt>, <W(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Ws> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<W(s+1)>` | Rs (5b) | Is the 32-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Wt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<W(t+1)>` | Rt (5b) | Is the 32-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sz);
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASPA_CP32_comswappr`  [32-bit CASPA]  — CASPA

**Assembly:** `CASPA  <Ws>, <W(s+1)>, <Wt>, <W(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Ws> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<W(s+1)>` | Rs (5b) | Is the 32-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Wt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<W(t+1)>` | Rt (5b) | Is the 32-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sz);
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASPAL_CP32_comswappr`  [32-bit CASPAL]  — CASPAL

**Assembly:** `CASPAL  <Ws>, <W(s+1)>, <Wt>, <W(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Ws> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<W(s+1)>` | Rs (5b) | Is the 32-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Wt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<W(t+1)>` | Rt (5b) | Is the 32-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sz);
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASPL_CP32_comswappr`  [32-bit CASPL]  — CASPL

**Assembly:** `CASPL  <Ws>, <W(s+1)>, <Wt>, <W(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Ws> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<W(s+1)>` | Rs (5b) | Is the 32-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Wt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<W(t+1)>` | Rt (5b) | Is the 32-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sz);
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASP_CP64_comswappr`  [64-bit CASP]  — CASP

**Assembly:** `CASP  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sz);
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASPA_CP64_comswappr`  [64-bit CASPA]  — CASPA

**Assembly:** `CASPA  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sz);
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASPAL_CP64_comswappr`  [64-bit CASPAL]  — CASPAL

**Assembly:** `CASPAL  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sz);
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASPL_CP64_comswappr`  [64-bit CASPL]  — CASPL

**Assembly:** `CASPL  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sz);
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

---

## CASPT, CASPAT, CASPALT, CASPLT -- A64

### Encoding: `CASPT_CP64_comswappr_unpriv`  [CASPT]  — CASPT

**Assembly:** `CASPT  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 64;
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASPAT_CP64_comswappr_unpriv`  [CASPAT]  — CASPAT

**Assembly:** `CASPAT  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 64;
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASPALT_CP64_comswappr_unpriv`  [CASPALT]  — CASPALT

**Assembly:** `CASPALT  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 64;
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASPLT_CP64_comswappr_unpriv`  [CASPLT]  — CASPLT

**Assembly:** `CASPLT  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rs[0] == '1' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
if Rs[0] == '1' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 64;
let acquire : boolean = L == '1';
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

---

## CAST, CASAT, CASALT, CASLT -- A64

### Encoding: `CAST_C64_comswap_unpriv`  [CAST]  — CAST

**Assembly:** `CAST  <Xs>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASAT_C64_comswap_unpriv`  [CASAT]  — CASAT

**Assembly:** `CASAT  <Xs>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASALT_C64_comswap_unpriv`  [CASALT]  — CASALT

**Assembly:** `CASALT  <Xs>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `CASLT_C64_comswap_unpriv`  [CASLT]  — CASLT

**Assembly:** `CASLT  <Xs>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = L == '1' && t != 31;
let release : boolean = o0 == '1';
let tagchecked : boolean = n != 31;
```

---

## CB<cc> (immediate) -- A64

### Encoding: `CBGT_32_imm`  [32-bit greater than]  — CBGT

**Assembly:** `CBGT  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBLT_32_imm`  [32-bit less than]  — CBLT

**Assembly:** `CBLT  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBHI_32_imm`  [32-bit higher]  — CBHI

**Assembly:** `CBHI  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBLO_32_imm`  [32-bit lower]  — CBLO

**Assembly:** `CBLO  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBEQ_32_imm`  [32-bit equal]  — CBEQ

**Assembly:** `CBEQ  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBNE_32_imm`  [32-bit not equal]  — CBNE

**Assembly:** `CBNE  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBGT_64_imm`  [64-bit greater than]  — CBGT

**Assembly:** `CBGT  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBLT_64_imm`  [64-bit less than]  — CBLT

**Assembly:** `CBLT  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBHI_64_imm`  [64-bit higher]  — CBHI

**Assembly:** `CBHI  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBLO_64_imm`  [64-bit lower]  — CBLO

**Assembly:** `CBLO  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBEQ_64_imm`  [64-bit equal]  — CBEQ

**Assembly:** `CBEQ  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

### Encoding: `CBNE_64_imm`  [64-bit not equal]  — CBNE

**Assembly:** `CBNE  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | imm6 (6b) | Is an unsigned immediate, in the range 0 to 63, encoded in the "imm6" field. | let value2 : integer = UInt(imm6); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_LT; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_LT; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
let value2 : integer = UInt(imm6);
```

---

## CB<cc> (register) -- A64

### Encoding: `CBGT_32_regs`  [32-bit greater than]  — CBGT

**Assembly:** `CBGT  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBGE_32_regs`  [32-bit greater than or equal]  — CBGE

**Assembly:** `CBGE  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBHI_32_regs`  [32-bit higher]  — CBHI

**Assembly:** `CBHI  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBHS_32_regs`  [32-bit higher or same]  — CBHS

**Assembly:** `CBHS  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBEQ_32_regs`  [32-bit equal]  — CBEQ

**Assembly:** `CBEQ  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBNE_32_regs`  [32-bit not equal]  — CBNE

**Assembly:** `CBNE  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBGT_64_regs`  [64-bit greater than]  — CBGT

**Assembly:** `CBGT  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBGE_64_regs`  [64-bit greater than or equal]  — CBGE

**Assembly:** `CBGE  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBHI_64_regs`  [64-bit higher]  — CBHI

**Assembly:** `CBHI  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBHS_64_regs`  [64-bit higher or same]  — CBHS

**Assembly:** `CBHS  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBEQ_64_regs`  [64-bit equal]  — CBEQ

**Assembly:** `CBEQ  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBNE_64_regs`  [64-bit not equal]  — CBNE

**Assembly:** `CBNE  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## CBB<cc> -- A64

### Encoding: `CBBGT_8_regs`  [Greater than]  — CBBGT

**Assembly:** `CBBGT  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBBGE_8_regs`  [Greater than or equal]  — CBBGE

**Assembly:** `CBBGE  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBBHI_8_regs`  [Higher]  — CBBHI

**Assembly:** `CBBHI  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBBHS_8_regs`  [Higher or same]  — CBBHS

**Assembly:** `CBBHS  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBBEQ_8_regs`  [Equal]  — CBBEQ

**Assembly:** `CBBEQ  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBBNE_8_regs`  [Not equal]  — CBBNE

**Assembly:** `CBBNE  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## CBBLE -- A64 *(alias for CBBcc_regs)*

### Encoding: `CBBLE_CBBGE_8_regs`  — CBBGE

**Assembly:** `CBBLE  <Wm>, <Wt>, <label> / CBBGE  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBBGE` | — |  |  |

---

## CBBLO -- A64 *(alias for CBBcc_regs)*

### Encoding: `CBBLO_CBBHI_8_regs`  — CBBHI

**Assembly:** `CBBLO  <Wm>, <Wt>, <label> / CBBHI  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBBHI` | — |  |  |

---

## CBBLS -- A64 *(alias for CBBcc_regs)*

### Encoding: `CBBLS_CBBHS_8_regs`  — CBBHS

**Assembly:** `CBBLS  <Wm>, <Wt>, <label> / CBBHS  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBBHS` | — |  |  |

---

## CBBLT -- A64 *(alias for CBBcc_regs)*

### Encoding: `CBBLT_CBBGT_8_regs`  — CBBGT

**Assembly:** `CBBLT  <Wm>, <Wt>, <label> / CBBGT  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBBGT` | — |  |  |

---

## CBGE (immediate) -- A64 *(alias for CBcc_imm)*

### Encoding: `CBGE_CBGT_32_imm`  [32-bit greater than]  — CBGT

**Assembly:** `CBGE  <Wt>, #<immp1>, <label> / CBGT  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<immp1>` | imm6 (6b) | Is an unsigned immediate, in the range 1 to 64, encoded as "imm6" plus 1. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBGT` | — |  |  |

### Encoding: `CBGE_CBGT_64_imm`  [64-bit greater than]  — CBGT

**Assembly:** `CBGE  <Xt>, #<immp1>, <label> / CBGT  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<immp1>` | imm6 (6b) | Is an unsigned immediate, in the range 1 to 64, encoded as "imm6" plus 1. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBGT` | — |  |  |

---

## CBH<cc> -- A64

### Encoding: `CBHGT_16_regs`  [Greater than]  — CBHGT

**Assembly:** `CBHGT  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBHGE_16_regs`  [Greater than or equal]  — CBHGE

**Assembly:** `CBHGE  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBHHI_16_regs`  [Higher]  — CBHHI

**Assembly:** `CBHHI  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBHHS_16_regs`  [Higher or same]  — CBHHS

**Assembly:** `CBHHS  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBHEQ_16_regs`  [Equal]  — CBHEQ

**Assembly:** `CBHEQ  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `CBHNE_16_regs`  [Not equal]  — CBHNE

**Assembly:** `CBHNE  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 | let offset : bits(64) = SignExtend{}(imm9::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMPBR) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 8 << UInt(H);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let offset : bits(64) = SignExtend{}(imm9::'00');
var op : CmpOp;
var unsigned : boolean;

case cc of
    when '000' => op = Cmp_GT; unsigned = FALSE;
    when '001' => op = Cmp_GE; unsigned = FALSE;
    when '010' => op = Cmp_GT; unsigned = TRUE;
    when '011' => op = Cmp_GE; unsigned = TRUE;
    when '110' => op = Cmp_EQ; unsigned = TRUE;
    when '111' => op = Cmp_NE; unsigned = TRUE;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## CBHLE -- A64 *(alias for CBHcc_regs)*

### Encoding: `CBHLE_CBHGE_16_regs`  — CBHGE

**Assembly:** `CBHLE  <Wm>, <Wt>, <label> / CBHGE  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHGE` | — |  |  |

---

## CBHLO -- A64 *(alias for CBHcc_regs)*

### Encoding: `CBHLO_CBHHI_16_regs`  — CBHHI

**Assembly:** `CBHLO  <Wm>, <Wt>, <label> / CBHHI  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHHI` | — |  |  |

---

## CBHLS -- A64 *(alias for CBHcc_regs)*

### Encoding: `CBHLS_CBHHS_16_regs`  — CBHHS

**Assembly:** `CBHLS  <Wm>, <Wt>, <label> / CBHHS  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHHS` | — |  |  |

---

## CBHLT -- A64 *(alias for CBHcc_regs)*

### Encoding: `CBHLT_CBHGT_16_regs`  — CBHGT

**Assembly:** `CBHLT  <Wm>, <Wt>, <label> / CBHGT  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHGT` | — |  |  |

---

## CBHS (immediate) -- A64 *(alias for CBcc_imm)*

### Encoding: `CBHS_CBHI_32_imm`  [32-bit higher]  — CBHI

**Assembly:** `CBHS  <Wt>, #<immp1>, <label> / CBHI  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<immp1>` | imm6 (6b) | Is an unsigned immediate, in the range 1 to 64, encoded as "imm6" plus 1. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHI` | — |  |  |

### Encoding: `CBHS_CBHI_64_imm`  [64-bit higher]  — CBHI

**Assembly:** `CBHS  <Xt>, #<immp1>, <label> / CBHI  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<immp1>` | imm6 (6b) | Is an unsigned immediate, in the range 1 to 64, encoded as "imm6" plus 1. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHI` | — |  |  |

---

## CBLE (immediate) -- A64 *(alias for CBcc_imm)*

### Encoding: `CBLE_CBLT_32_imm`  [32-bit less than]  — CBLT

**Assembly:** `CBLE  <Wt>, #<imms1>, <label> / CBLT  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<imms1>` | imm6 (6b) | Is a signed immediate, in the range -1 to 62, encoded as "imm6" minus 1. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBLT` | — |  |  |

### Encoding: `CBLE_CBLT_64_imm`  [64-bit less than]  — CBLT

**Assembly:** `CBLE  <Xt>, #<imms1>, <label> / CBLT  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<imms1>` | imm6 (6b) | Is a signed immediate, in the range -1 to 62, encoded as "imm6" minus 1. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBLT` | — |  |  |

---

## CBLE (register) -- A64 *(alias for CBcc_regs)*

### Encoding: `CBLE_CBGE_32_regs`  [32-bit greater than or equal]  — CBGE

**Assembly:** `CBLE  <Wm>, <Wt>, <label> / CBGE  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBGE` | — |  |  |

### Encoding: `CBLE_CBGE_64_regs`  [64-bit greater than or equal]  — CBGE

**Assembly:** `CBLE  <Xm>, <Xt>, <label> / CBGE  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBGE` | — |  |  |

---

## CBLO (register) -- A64 *(alias for CBcc_regs)*

### Encoding: `CBLO_CBHI_32_regs`  [32-bit higher]  — CBHI

**Assembly:** `CBLO  <Wm>, <Wt>, <label> / CBHI  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHI` | — |  |  |

### Encoding: `CBLO_CBHI_64_regs`  [64-bit higher]  — CBHI

**Assembly:** `CBLO  <Xm>, <Xt>, <label> / CBHI  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHI` | — |  |  |

---

## CBLS (immediate) -- A64 *(alias for CBcc_imm)*

### Encoding: `CBLS_CBLO_32_imm`  [32-bit lower]  — CBLO

**Assembly:** `CBLS  <Wt>, #<imms1>, <label> / CBLO  <Wt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<imms1>` | imm6 (6b) | Is a signed immediate, in the range -1 to 62, encoded as "imm6" minus 1. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBLO` | — |  |  |

### Encoding: `CBLS_CBLO_64_imm`  [64-bit lower]  — CBLO

**Assembly:** `CBLS  <Xt>, #<imms1>, <label> / CBLO  <Xt>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<imms1>` | imm6 (6b) | Is a signed immediate, in the range -1 to 62, encoded as "imm6" minus 1. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBLO` | — |  |  |

---

## CBLS (register) -- A64 *(alias for CBcc_regs)*

### Encoding: `CBLS_CBHS_32_regs`  [32-bit higher or same]  — CBHS

**Assembly:** `CBLS  <Wm>, <Wt>, <label> / CBHS  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHS` | — |  |  |

### Encoding: `CBLS_CBHS_64_regs`  [64-bit higher or same]  — CBHS

**Assembly:** `CBLS  <Xm>, <Xt>, <label> / CBHS  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBHS` | — |  |  |

---

## CBLT (register) -- A64 *(alias for CBcc_regs)*

### Encoding: `CBLT_CBGT_32_regs`  [32-bit greater than]  — CBGT

**Assembly:** `CBLT  <Wm>, <Wt>, <label> / CBGT  <Wt>, <Wm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBGT` | — |  |  |

### Encoding: `CBLT_CBGT_64_regs`  [64-bit greater than]  — CBGT

**Assembly:** `CBLT  <Xm>, <Xt>, <label> / CBGT  <Xt>, <Xm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. |  |
| `<label>` | imm9 (9b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range -102 |  |
| `CBGT` | — |  |  |

---

## CBNZ -- A64

### Encoding: `CBNZ_32_compbranch`  [32-bit]  — CBNZ

**Assembly:** `CBNZ  <Wt>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range +/-1 | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
let t : integer = UInt(Rt);
let datasize : integer{} = 32 << UInt(sf);
let offset : bits(64) = SignExtend{}(imm19::'00');
```

### Encoding: `CBNZ_64_compbranch`  [64-bit]  — CBNZ

**Assembly:** `CBNZ  <Xt>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range +/-1 | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
let t : integer = UInt(Rt);
let datasize : integer{} = 32 << UInt(sf);
let offset : bits(64) = SignExtend{}(imm19::'00');
```

---

## CBZ -- A64

### Encoding: `CBZ_32_compbranch`  [32-bit]  — CBZ

**Assembly:** `CBZ  <Wt>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range +/-1 | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
let t : integer = UInt(Rt);
let datasize : integer{} = 32 << UInt(sf);
let offset : bits(64) = SignExtend{}(imm19::'00');
```

### Encoding: `CBZ_64_compbranch`  [64-bit]  — CBZ

**Assembly:** `CBZ  <Xt>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be tested, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range +/-1 | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
let t : integer = UInt(Rt);
let datasize : integer{} = 32 << UInt(sf);
let offset : bits(64) = SignExtend{}(imm19::'00');
```

---

## CCMN (immediate) -- A64

### Encoding: `CCMN_32_condcmp_imm`  [32-bit]  — CCMN

**Assembly:** `CCMN  <Wn>, #<imm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm5 (5b) | Is a five bit unsigned (positive) immediate encoded in the "imm5" field. | let imm : bits(datasize) = ZeroExtend{}(imm5); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
let imm : bits(datasize) = ZeroExtend{}(imm5);
```

### Encoding: `CCMN_64_condcmp_imm`  [64-bit]  — CCMN

**Assembly:** `CCMN  <Xn>, #<imm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm5 (5b) | Is a five bit unsigned (positive) immediate encoded in the "imm5" field. | let imm : bits(datasize) = ZeroExtend{}(imm5); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
let imm : bits(datasize) = ZeroExtend{}(imm5);
```

---

## CCMN (register) -- A64

### Encoding: `CCMN_32_condcmp_reg`  [32-bit]  — CCMN

**Assembly:** `CCMN  <Wn>, <Wm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

### Encoding: `CCMN_64_condcmp_reg`  [64-bit]  — CCMN

**Assembly:** `CCMN  <Xn>, <Xm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

---

## CCMP (immediate) -- A64

### Encoding: `CCMP_32_condcmp_imm`  [32-bit]  — CCMP

**Assembly:** `CCMP  <Wn>, #<imm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm5 (5b) | Is a five bit unsigned (positive) immediate encoded in the "imm5" field. | let imm : bits(datasize) = ZeroExtend{}(imm5); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
let imm : bits(datasize) = ZeroExtend{}(imm5);
```

### Encoding: `CCMP_64_condcmp_imm`  [64-bit]  — CCMP

**Assembly:** `CCMP  <Xn>, #<imm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm5 (5b) | Is a five bit unsigned (positive) immediate encoded in the "imm5" field. | let imm : bits(datasize) = ZeroExtend{}(imm5); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
let imm : bits(datasize) = ZeroExtend{}(imm5);
```

---

## CCMP (register) -- A64

### Encoding: `CCMP_32_condcmp_reg`  [32-bit]  — CCMP

**Assembly:** `CCMP  <Wn>, <Wm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

### Encoding: `CCMP_64_condcmp_reg`  [64-bit]  — CCMP

**Assembly:** `CCMP  <Xn>, <Xm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

---

## CDOT (indexed) -- A64

### Encoding: `cdot_z_zzzi_s`  — CDOT

**Assembly:** `CDOT  <Zda>.S, <Zn>.B, <Zm>.B[<imm>], <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "8-bit to 32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm"  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "8-bit to 32-bit" variant: is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vec | let index : integer = UInt(i2); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let sub_i : boolean = (rot[0] == rot[1]);
```

### Encoding: `cdot_z_zzzi_d`  — CDOT

**Assembly:** `CDOT  <Zda>.D, <Zn>.H, <Zm>.H[<imm>], <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "16-bit to 64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "16-bit to 64-bit" variant: is the immediate index of a 64-bit group of four 16-bit values within each 128-bit v | let index : integer = UInt(i1); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let sub_i : boolean = (rot[0] == rot[1]);
```

---

## CDOT (vectors) -- A64

### Encoding: `cdot_z_zzz_`  — CDOT

**Assembly:** `CDOT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let sub_i : boolean = (rot[0] == rot[1]);
```

---

## CFINV -- A64

### Encoding: `CFINV_M_pstate`  — CFINV

**Assembly:** `CFINV`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FlagM) then EndOfDecode(Decode_UNDEF); end;
```

---

## CFP -- A64 *(alias for SYS)*

### Encoding: `CFP_SYS_CR_systeminstrs`  — SYS

**Assembly:** `CFP  RCTX, <Xt> / SYS  #3, C7, C3, #4, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## CHKFEAT -- A64

### Encoding: `CHKFEAT_HF_hints`  — CHKFEAT

**Assembly:** `CHKFEAT  X16`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CHK) then EndOfDecode(Decode_NOP); end;
```

---

## CINC -- A64 *(alias for CSINC)*

### Encoding: `CINC_CSINC_32_condsel`  [32-bit]  — CSINC

**Assembly:** `CINC  <Wd>, <Wn>, <invcond> / CSINC  <Wd>, <Wn>, <Wm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" and "Rm" fields. |  |
| `<invcond>` | — |  |  |
| `CSINC` | — |  |  |

### Encoding: `CINC_CSINC_64_condsel`  [64-bit]  — CSINC

**Assembly:** `CINC  <Xd>, <Xn>, <invcond> / CSINC  <Xd>, <Xn>, <Xm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" and "Rm" fields. |  |
| `<invcond>` | — |  |  |
| `CSINC` | — |  |  |

---

## CINV -- A64 *(alias for CSINV)*

### Encoding: `CINV_CSINV_32_condsel`  [32-bit]  — CSINV

**Assembly:** `CINV  <Wd>, <Wn>, <invcond> / CSINV  <Wd>, <Wn>, <Wm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" and "Rm" fields. |  |
| `<invcond>` | — |  |  |
| `CSINV` | — |  |  |

### Encoding: `CINV_CSINV_64_condsel`  [64-bit]  — CSINV

**Assembly:** `CINV  <Xd>, <Xn>, <invcond> / CSINV  <Xd>, <Xn>, <Xm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" and "Rm" fields. |  |
| `<invcond>` | — |  |  |
| `CSINV` | — |  |  |

---

## CLASTA (SIMD&FP scalar) -- A64

### Encoding: `clasta_v_p_z_`  — CLASTA

**Assembly:** `CLASTA  <V><dn>, <Pg>, <V><dn>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<dn>` | Vdn (5b) | Is the number [0-31] of the source and destination SIMD&FP register, encoded in the "Vdn" field. | let dn : integer = UInt(Vdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Vdn);
let m : integer = UInt(Zm);
let isBefore : boolean = FALSE;
```

---

## CLASTA (scalar) -- A64

### Encoding: `clasta_r_p_z_`  — CLASTA

**Assembly:** `CLASTA  <R><dn>, <Pg>, <R><dn>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<R>` | — |  |  |
| `<dn>` | Rdn (5b) | Is the number [0-30] of the source and destination general-purpose register or the name ZR (31), encoded in the "Rdn" fi | let dn : integer = UInt(Rdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Rdn);
let m : integer = UInt(Zm);
let csize : integer{} = if esize < 64 then 32 else 64;
let isBefore : boolean = FALSE;
```

---

## CLASTA (vectors) -- A64

### Encoding: `clasta_z_p_zz_`  — CLASTA

**Assembly:** `CLASTA  <Zdn>.<T>, <Pg>, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let isBefore : boolean = FALSE;
```

---

## CLASTB (SIMD&FP scalar) -- A64

### Encoding: `clastb_v_p_z_`  — CLASTB

**Assembly:** `CLASTB  <V><dn>, <Pg>, <V><dn>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<dn>` | Vdn (5b) | Is the number [0-31] of the source and destination SIMD&FP register, encoded in the "Vdn" field. | let dn : integer = UInt(Vdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Vdn);
let m : integer = UInt(Zm);
let isBefore : boolean = TRUE;
```

---

## CLASTB (scalar) -- A64

### Encoding: `clastb_r_p_z_`  — CLASTB

**Assembly:** `CLASTB  <R><dn>, <Pg>, <R><dn>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<R>` | — |  |  |
| `<dn>` | Rdn (5b) | Is the number [0-30] of the source and destination general-purpose register or the name ZR (31), encoded in the "Rdn" fi | let dn : integer = UInt(Rdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Rdn);
let m : integer = UInt(Zm);
let csize : integer{} = if esize < 64 then 32 else 64;
let isBefore : boolean = TRUE;
```

---

## CLASTB (vectors) -- A64

### Encoding: `clastb_z_p_zz_`  — CLASTB

**Assembly:** `CLASTB  <Zdn>.<T>, <Pg>, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let isBefore : boolean = TRUE;
```

---

## CLRBHB -- A64

### Encoding: `CLRBHB_HI_hints`  — CLRBHB

**Assembly:** `CLRBHB`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CLRBHB) then EndOfDecode(Decode_NOP); end;
```

---

## CLREX -- A64

### Encoding: `CLREX_BN_barriers`  — CLREX

**Assembly:** `CLREX  {#<imm>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | CRm (4b) | Is an optional 4-bit unsigned immediate, in the range 0 to 15, defaulting to 15 and encoded in the "CRm" field. | // CRm field is ignored |

**Pseudocode (Decode):**
```
// CRm field is ignored
```

---

## CLS (vector) -- A64

### Encoding: `CLS_asimdmisc_R`  — CLS

**Assembly:** `CLS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CLS -- A64

### Encoding: `CLS_32_dp_1src`  [32-bit]  — CLS

**Assembly:** `CLS  <Wd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `CLS_64_dp_1src`  [64-bit]  — CLS

**Assembly:** `CLS  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
```

---

## CLS -- A64

### Encoding: `cls_z_p_z_m`  — CLS

**Assembly:** `CLS  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `cls_z_p_z_z`  — CLS

**Assembly:** `CLS  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## CLZ (vector) -- A64

### Encoding: `CLZ_asimdmisc_R`  — CLZ

**Assembly:** `CLZ  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CLZ -- A64

### Encoding: `CLZ_32_dp_1src`  [32-bit]  — CLZ

**Assembly:** `CLZ  <Wd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `CLZ_64_dp_1src`  [64-bit]  — CLZ

**Assembly:** `CLZ  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
```

---

## CLZ -- A64

### Encoding: `clz_z_p_z_m`  — CLZ

**Assembly:** `CLZ  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `clz_z_p_z_z`  — CLZ

**Assembly:** `CLZ  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## CMEQ (register) -- A64

### Encoding: `CMEQ_asisdsame_only`  — CMEQ

**Assembly:** `CMEQ  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMEQ_asimdsame_only`  — CMEQ

**Assembly:** `CMEQ  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMEQ (zero) -- A64

### Encoding: `CMEQ_asisdmisc_Z`  — CMEQ

**Assembly:** `CMEQ  D<d>, D<n>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMEQ_asimdmisc_Z`  — CMEQ

**Assembly:** `CMEQ  <Vd>.<T>, <Vn>.<T>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMGE (register) -- A64

### Encoding: `CMGE_asisdsame_only`  — CMGE

**Assembly:** `CMGE  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMGE_asimdsame_only`  — CMGE

**Assembly:** `CMGE  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMGE (zero) -- A64

### Encoding: `CMGE_asisdmisc_Z`  — CMGE

**Assembly:** `CMGE  D<d>, D<n>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMGE_asimdmisc_Z`  — CMGE

**Assembly:** `CMGE  <Vd>.<T>, <Vn>.<T>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMGT (register) -- A64

### Encoding: `CMGT_asisdsame_only`  — CMGT

**Assembly:** `CMGT  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMGT_asimdsame_only`  — CMGT

**Assembly:** `CMGT  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMGT (zero) -- A64

### Encoding: `CMGT_asisdmisc_Z`  — CMGT

**Assembly:** `CMGT  D<d>, D<n>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMGT_asimdmisc_Z`  — CMGT

**Assembly:** `CMGT  <Vd>.<T>, <Vn>.<T>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMHI (register) -- A64

### Encoding: `CMHI_asisdsame_only`  — CMHI

**Assembly:** `CMHI  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMHI_asimdsame_only`  — CMHI

**Assembly:** `CMHI  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMHS (register) -- A64

### Encoding: `CMHS_asisdsame_only`  — CMHS

**Assembly:** `CMHS  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMHS_asimdsame_only`  — CMHS

**Assembly:** `CMHS  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMLA (indexed) -- A64

### Encoding: `cmla_z_zzzi_h`  — CMLA

**Assembly:** `CMLA  <Zda>.H, <Zn>.H, <Zm>.H[<imm>], <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "16-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let sub_r : boolean = (rot[0] != rot[1]);
let sub_i : boolean = (rot[1] == '1');
```

### Encoding: `cmla_z_zzzi_s`  — CMLA

**Assembly:** `CMLA  <Zda>.S, <Zn>.S, <Zm>.S[<imm>], <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "32-bit" variant: is the element index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let sub_r : boolean = (rot[0] != rot[1]);
let sub_i : boolean = (rot[1] == '1');
```

---

## CMLA (vectors) -- A64

### Encoding: `cmla_z_zzz_`  — CMLA

**Assembly:** `CMLA  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let sub_r : boolean = (rot[0] != rot[1]);
let sub_i : boolean = (rot[1] == '1');
```

---

## CMLE (zero) -- A64

### Encoding: `CMLE_asisdmisc_Z`  — CMLE

**Assembly:** `CMLE  D<d>, D<n>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMLE_asimdmisc_Z`  — CMLE

**Assembly:** `CMLE  <Vd>.<T>, <Vn>.<T>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMLT (zero) -- A64

### Encoding: `CMLT_asisdmisc_Z`  — CMLT

**Assembly:** `CMLT  D<d>, D<n>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMLT_asimdmisc_Z`  — CMLT

**Assembly:** `CMLT  <Vd>.<T>, <Vn>.<T>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CMN (extended register) -- A64 *(alias for ADDS_addsub_ext)*

### Encoding: `CMN_ADDS_32S_addsub_ext`  [32-bit]  — ADDS

**Assembly:** `CMN  <Wn|WSP>, <Wm>{, <extend> {#<amount>}} / ADDS  WZR, <Wn|WSP>, <Wm>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field |  |
| `ADDS` | — |  |  |

### Encoding: `CMN_ADDS_64S_addsub_ext`  [64-bit]  — ADDS

**Assembly:** `CMN  <Xn|SP>, <R><m>{, <extend> {#<amount>}} / ADDS  XZR, <Xn|SP>, <R><m>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `<R>` | — |  |  |
| `<m>` | Rm (5b) | Is the number [0-30] of the second general-purpose source register or the name ZR (31), encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field |  |
| `ADDS` | — |  |  |

---

## CMN (immediate) -- A64 *(alias for ADDS_addsub_imm)*

### Encoding: `CMN_ADDS_32S_addsub_imm`  [32-bit]  — ADDS

**Assembly:** `CMN  <Wn|WSP>, #<imm>{, <shift>} / ADDS  WZR, <Wn|WSP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. |  |
| `<shift>` | — |  |  |
| `ADDS` | — |  |  |

### Encoding: `CMN_ADDS_64S_addsub_imm`  [64-bit]  — ADDS

**Assembly:** `CMN  <Xn|SP>, #<imm>{, <shift>} / ADDS  XZR, <Xn|SP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. |  |
| `<shift>` | — |  |  |
| `ADDS` | — |  |  |

---

## CMN (shifted register) -- A64 *(alias for ADDS_addsub_shift)*

### Encoding: `CMN_ADDS_32_addsub_shift`  [32-bit]  — ADDS

**Assembly:** `CMN  <Wn>, <Wm>{, <shift> #<amount>} / ADDS  WZR, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. |  |
| `ADDS` | — |  |  |

### Encoding: `CMN_ADDS_64_addsub_shift`  [64-bit]  — ADDS

**Assembly:** `CMN  <Xn>, <Xm>{, <shift> #<amount>} / ADDS  XZR, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. |  |
| `ADDS` | — |  |  |

---

## CMP (extended register) -- A64 *(alias for SUBS_addsub_ext)*

### Encoding: `CMP_SUBS_32S_addsub_ext`  [32-bit]  — SUBS

**Assembly:** `CMP  <Wn|WSP>, <Wm>{, <extend> {#<amount>}} / SUBS  WZR, <Wn|WSP>, <Wm>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field |  |
| `SUBS` | — |  |  |

### Encoding: `CMP_SUBS_64S_addsub_ext`  [64-bit]  — SUBS

**Assembly:** `CMP  <Xn|SP>, <R><m>{, <extend> {#<amount>}} / SUBS  XZR, <Xn|SP>, <R><m>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `<R>` | — |  |  |
| `<m>` | Rm (5b) | Is the number [0-30] of the second general-purpose source register or the name ZR (31), encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field |  |
| `SUBS` | — |  |  |

---

## CMP (immediate) -- A64 *(alias for SUBS_addsub_imm)*

### Encoding: `CMP_SUBS_32S_addsub_imm`  [32-bit]  — SUBS

**Assembly:** `CMP  <Wn|WSP>, #<imm>{, <shift>} / SUBS  WZR, <Wn|WSP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. |  |
| `<shift>` | — |  |  |
| `SUBS` | — |  |  |

### Encoding: `CMP_SUBS_64S_addsub_imm`  [64-bit]  — SUBS

**Assembly:** `CMP  <Xn|SP>, #<imm>{, <shift>} / SUBS  XZR, <Xn|SP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. |  |
| `<shift>` | — |  |  |
| `SUBS` | — |  |  |

---

## CMP (shifted register) -- A64 *(alias for SUBS_addsub_shift)*

### Encoding: `CMP_SUBS_32_addsub_shift`  [32-bit]  — SUBS

**Assembly:** `CMP  <Wn>, <Wm>{, <shift> #<amount>} / SUBS  WZR, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. |  |
| `SUBS` | — |  |  |

### Encoding: `CMP_SUBS_64_addsub_shift`  [64-bit]  — SUBS

**Assembly:** `CMP  <Xn>, <Xm>{, <shift> #<amount>} / SUBS  XZR, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. |  |
| `SUBS` | — |  |  |

---

## CMP<cc> (immediate) -- A64

### Encoding: `cmpeq_p_p_zi_`  — CMPEQ

**Assembly:** `CMPEQ  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | For the "Equal", "Greater than or equal", "Greater than", "Less than or equal", "Less than", and "Not equal" variants: i | let imm : integer = SInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_EQ;
let imm : integer = SInt(imm5);
let unsigned : boolean = FALSE;
```

### Encoding: `cmpgt_p_p_zi_`  — CMPGT

**Assembly:** `CMPGT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | For the "Equal", "Greater than or equal", "Greater than", "Less than or equal", "Less than", and "Not equal" variants: i | let imm : integer = SInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GT;
let imm : integer = SInt(imm5);
let unsigned : boolean = FALSE;
```

### Encoding: `cmpge_p_p_zi_`  — CMPGE

**Assembly:** `CMPGE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | For the "Equal", "Greater than or equal", "Greater than", "Less than or equal", "Less than", and "Not equal" variants: i | let imm : integer = SInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GE;
let imm : integer = SInt(imm5);
let unsigned : boolean = FALSE;
```

### Encoding: `cmphi_p_p_zi_`  — CMPHI

**Assembly:** `CMPHI  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm7 (7b) | For the "Higher or same", "Higher", "Lower or same", and "Lower" variants: is the unsigned immediate operand, in the ran | let imm : integer = UInt(imm7); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GT;
let imm : integer = UInt(imm7);
let unsigned : boolean = TRUE;
```

### Encoding: `cmphs_p_p_zi_`  — CMPHS

**Assembly:** `CMPHS  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm7 (7b) | For the "Higher or same", "Higher", "Lower or same", and "Lower" variants: is the unsigned immediate operand, in the ran | let imm : integer = UInt(imm7); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GE;
let imm : integer = UInt(imm7);
let unsigned : boolean = TRUE;
```

### Encoding: `cmplt_p_p_zi_`  — CMPLT

**Assembly:** `CMPLT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | For the "Equal", "Greater than or equal", "Greater than", "Less than or equal", "Less than", and "Not equal" variants: i | let imm : integer = SInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_LT;
let imm : integer = SInt(imm5);
let unsigned : boolean = FALSE;
```

### Encoding: `cmple_p_p_zi_`  — CMPLE

**Assembly:** `CMPLE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | For the "Equal", "Greater than or equal", "Greater than", "Less than or equal", "Less than", and "Not equal" variants: i | let imm : integer = SInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_LE;
let imm : integer = SInt(imm5);
let unsigned : boolean = FALSE;
```

### Encoding: `cmplo_p_p_zi_`  — CMPLO

**Assembly:** `CMPLO  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm7 (7b) | For the "Higher or same", "Higher", "Lower or same", and "Lower" variants: is the unsigned immediate operand, in the ran | let imm : integer = UInt(imm7); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_LT;
let imm : integer = UInt(imm7);
let unsigned : boolean = TRUE;
```

### Encoding: `cmpls_p_p_zi_`  — CMPLS

**Assembly:** `CMPLS  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm7 (7b) | For the "Higher or same", "Higher", "Lower or same", and "Lower" variants: is the unsigned immediate operand, in the ran | let imm : integer = UInt(imm7); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_LE;
let imm : integer = UInt(imm7);
let unsigned : boolean = TRUE;
```

### Encoding: `cmpne_p_p_zi_`  — CMPNE

**Assembly:** `CMPNE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | For the "Equal", "Greater than or equal", "Greater than", "Less than or equal", "Less than", and "Not equal" variants: i | let imm : integer = SInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_NE;
let imm : integer = SInt(imm5);
let unsigned : boolean = FALSE;
```

---

## CMP<cc> (vectors) -- A64

### Encoding: `cmpeq_p_p_zz_`  — CMPEQ

**Assembly:** `CMPEQ  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_EQ;
let unsigned : boolean = FALSE;
```

### Encoding: `cmpgt_p_p_zz_`  — CMPGT

**Assembly:** `CMPGT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GT;
let unsigned : boolean = FALSE;
```

### Encoding: `cmpge_p_p_zz_`  — CMPGE

**Assembly:** `CMPGE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GE;
let unsigned : boolean = FALSE;
```

### Encoding: `cmphi_p_p_zz_`  — CMPHI

**Assembly:** `CMPHI  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GT;
let unsigned : boolean = TRUE;
```

### Encoding: `cmphs_p_p_zz_`  — CMPHS

**Assembly:** `CMPHS  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GE;
let unsigned : boolean = TRUE;
```

### Encoding: `cmpne_p_p_zz_`  — CMPNE

**Assembly:** `CMPNE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_NE;
let unsigned : boolean = FALSE;
```

---

## CMP<cc> (wide elements) -- A64

### Encoding: `cmpeq_p_p_zw_`  — CMPEQ

**Assembly:** `CMPEQ  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_EQ;
let unsigned : boolean = FALSE;
```

### Encoding: `cmpgt_p_p_zw_`  — CMPGT

**Assembly:** `CMPGT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GT;
let unsigned : boolean = FALSE;
```

### Encoding: `cmpge_p_p_zw_`  — CMPGE

**Assembly:** `CMPGE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GE;
let unsigned : boolean = FALSE;
```

### Encoding: `cmphi_p_p_zw_`  — CMPHI

**Assembly:** `CMPHI  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GT;
let unsigned : boolean = TRUE;
```

### Encoding: `cmphs_p_p_zw_`  — CMPHS

**Assembly:** `CMPHS  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GE;
let unsigned : boolean = TRUE;
```

### Encoding: `cmplt_p_p_zw_`  — CMPLT

**Assembly:** `CMPLT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_LT;
let unsigned : boolean = FALSE;
```

### Encoding: `cmple_p_p_zw_`  — CMPLE

**Assembly:** `CMPLE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_LE;
let unsigned : boolean = FALSE;
```

### Encoding: `cmplo_p_p_zw_`  — CMPLO

**Assembly:** `CMPLO  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_LT;
let unsigned : boolean = TRUE;
```

### Encoding: `cmpls_p_p_zw_`  — CMPLS

**Assembly:** `CMPLS  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_LE;
let unsigned : boolean = TRUE;
```

### Encoding: `cmpne_p_p_zw_`  — CMPNE

**Assembly:** `CMPNE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_NE;
let unsigned : boolean = FALSE;
```

---

## CMPLE (vectors) -- A64 *(alias for cmpeq_p_p_zz)*

### Encoding: `cmple_p_p_zz__cmpge_p_p_zz_`  — CMPGE

**Assembly:** `CMPLE  <Pd>.<T>, <Pg>/Z, <Zm>.<T>, <Zn>.<T> / CMPGE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. |  |
| `CMPGE` | — |  |  |

---

## CMPLO (vectors) -- A64 *(alias for cmpeq_p_p_zz)*

### Encoding: `cmplo_p_p_zz__cmphi_p_p_zz_`  — CMPHI

**Assembly:** `CMPLO  <Pd>.<T>, <Pg>/Z, <Zm>.<T>, <Zn>.<T> / CMPHI  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. |  |
| `CMPHI` | — |  |  |

---

## CMPLS (vectors) -- A64 *(alias for cmpeq_p_p_zz)*

### Encoding: `cmpls_p_p_zz__cmphs_p_p_zz_`  — CMPHS

**Assembly:** `CMPLS  <Pd>.<T>, <Pg>/Z, <Zm>.<T>, <Zn>.<T> / CMPHS  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. |  |
| `CMPHS` | — |  |  |

---

## CMPLT (vectors) -- A64 *(alias for cmpeq_p_p_zz)*

### Encoding: `cmplt_p_p_zz__cmpgt_p_p_zz_`  — CMPGT

**Assembly:** `CMPLT  <Pd>.<T>, <Pg>/Z, <Zm>.<T>, <Zn>.<T> / CMPGT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. |  |
| `CMPGT` | — |  |  |

---

## CMPP -- A64 *(alias for SUBPS)*

### Encoding: `CMPP_SUBPS_64S_dp_2src`  — SUBPS

**Assembly:** `CMPP  <Xn|SP>, <Xm|SP> / SUBPS  XZR, <Xn|SP>, <Xm|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `<Xm\|SP>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register or stack pointer, encoded in the "Rm" field. |  |
| `SUBPS` | — |  |  |

---

## CMTST -- A64

### Encoding: `CMTST_asisdsame_only`  — CMTST

**Assembly:** `CMTST  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `CMTST_asimdsame_only`  — CMTST

**Assembly:** `CMTST  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## CNEG -- A64 *(alias for CSNEG)*

### Encoding: `CNEG_CSNEG_32_condsel`  [32-bit]  — CSNEG

**Assembly:** `CNEG  <Wd>, <Wn>, <invcond> / CSNEG  <Wd>, <Wn>, <Wm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" and "Rm" fields. |  |
| `<invcond>` | — |  |  |
| `CSNEG` | — |  |  |

### Encoding: `CNEG_CSNEG_64_condsel`  [64-bit]  — CSNEG

**Assembly:** `CNEG  <Xd>, <Xn>, <invcond> / CSNEG  <Xd>, <Xn>, <Xm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" and "Rm" fields. |  |
| `<invcond>` | — |  |  |
| `CSNEG` | — |  |  |

---

## CNOT -- A64

### Encoding: `cnot_z_p_z_m`  — CNOT

**Assembly:** `CNOT  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `cnot_z_p_z_z`  — CNOT

**Assembly:** `CNOT  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## CNT -- A64

### Encoding: `CNT_32_dp_1src`  [32-bit]  — CNT

**Assembly:** `CNT  <Wd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

### Encoding: `CNT_64_dp_1src`  [64-bit]  — CNT

**Assembly:** `CNT  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## CNT -- A64

### Encoding: `CNT_asimdmisc_R`  — CNT

**Assembly:** `CNT  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '00' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
```

---

## CNT -- A64

### Encoding: `cnt_z_p_z_m`  — CNT

**Assembly:** `CNT  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `cnt_z_p_z_z`  — CNT

**Assembly:** `CNT  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## CNTB, CNTD, CNTH, CNTW -- A64

### Encoding: `cntb_r_s_`  — CNTB

**Assembly:** `CNTB  <Xd>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let d : integer = UInt(Rd);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `cntd_r_s_`  — CNTD

**Assembly:** `CNTD  <Xd>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let d : integer = UInt(Rd);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `cnth_r_s_`  — CNTH

**Assembly:** `CNTH  <Xd>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let d : integer = UInt(Rd);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `cntw_r_s_`  — CNTW

**Assembly:** `CNTW  <Xd>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let d : integer = UInt(Rd);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

---

## CNTP (predicate as counter) -- A64

### Encoding: `cntp_r_pn_`  — CNTP

**Assembly:** `CNTP  <Xd>, <PNn>.<T>, <vl>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<PNn>` | PNn (4b) | Is the name of the source scalable predicate register, with predicate-as-counter encoding, encoded in the "PNn" field. | let n : integer = UInt(PNn); |
| `<T>` | — |  |  |
| `<vl>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(PNn);
let d : integer = UInt(Rd);
let width : integer = 2 << UInt(vl);
```

---

## CNTP (predicate) -- A64

### Encoding: `cntp_r_p_p_`  — CNTP

**Assembly:** `CNTP  <Xd>, <Pg>, <Pn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let d : integer = UInt(Rd);
```

---

## COMPACT -- A64

### Encoding: `compact_z_p_z_s`  — COMPACT

**Assembly:** `COMPACT  <Zd>.<T>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

### Encoding: `compact_z_p_z_`  — COMPACT

**Assembly:** `COMPACT  <Zd>.<T>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## COSP -- A64 *(alias for SYS)*

### Encoding: `COSP_SYS_CR_systeminstrs`  — SYS

**Assembly:** `COSP  RCTX, <Xt> / SYS  #3, C7, C3, #6, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## CPP -- A64 *(alias for SYS)*

### Encoding: `CPP_SYS_CR_systeminstrs`  — SYS

**Assembly:** `CPP  RCTX, <Xt> / SYS  #3, C7, C3, #7, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## CPY (SIMD&FP scalar) -- A64

### Encoding: `cpy_z_p_v_`  — CPY

**Assembly:** `CPY  <Zd>.<T>, <Pg>/M, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<V>` | — |  |  |
| `<n>` | Vn (5b) | Is the number [0-31] of the source SIMD&FP register, encoded in the "Vn" field. | let n : integer = UInt(Vn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Vn);
let d : integer = UInt(Zd);
```

---

## CPY (immediate, merging) -- A64

### Encoding: `cpy_z_p_i_`  — CPY

**Assembly:** `CPY  <Zd>.<T>, <Pg>/M, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<imm>` | imm8 (8b) | Is a signed immediate in the range -128 to 127, encoded in the "imm8" field. | var imm : integer = SInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
var imm : integer = SInt(imm8);
if sh == '1' then imm = imm << 8; end;
```

---

## CPY (immediate, zeroing) -- A64

### Encoding: `cpy_z_o_i_`  — CPY

**Assembly:** `CPY  <Zd>.<T>, <Pg>/Z, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<imm>` | imm8 (8b) | Is a signed immediate in the range -128 to 127, encoded in the "imm8" field. | var imm : integer = SInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
var imm : integer = SInt(imm8);
if sh == '1' then imm = imm << 8; end;
```

---

## CPY (scalar) -- A64

### Encoding: `cpy_z_p_r_`  — CPY

**Assembly:** `CPY  <Zd>.<T>, <Pg>/M, <R><n|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<R>` | — |  |  |
| `<n\|SP>` | Rn (5b) | Is the number [0-30] of the general-purpose source register or the name SP (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let d : integer = UInt(Zd);
```

---

## CPYFP, CPYFM, CPYFE -- A64

### Encoding: `CPYFP_CPY_memcms`  [Prologue]  — CPYFP

**Assembly:** `CPYFP  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFM_CPY_memcms`  [Main]  — CPYFM

**Assembly:** `CPYFM  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFE_CPY_memcms`  [Epilogue]  — CPYFE

**Assembly:** `CPYFE  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPN, CPYFMN, CPYFEN -- A64

### Encoding: `CPYFPN_CPY_memcms`  [Prologue]  — CPYFPN

**Assembly:** `CPYFPN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMN_CPY_memcms`  [Main]  — CPYFMN

**Assembly:** `CPYFMN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFEN_CPY_memcms`  [Epilogue]  — CPYFEN

**Assembly:** `CPYFEN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPRN, CPYFMRN, CPYFERN -- A64

### Encoding: `CPYFPRN_CPY_memcms`  [Prologue]  — CPYFPRN

**Assembly:** `CPYFPRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMRN_CPY_memcms`  [Main]  — CPYFMRN

**Assembly:** `CPYFMRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFERN_CPY_memcms`  [Epilogue]  — CPYFERN

**Assembly:** `CPYFERN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPRT, CPYFMRT, CPYFERT -- A64

### Encoding: `CPYFPRT_CPY_memcms`  [Prologue]  — CPYFPRT

**Assembly:** `CPYFPRT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMRT_CPY_memcms`  [Main]  — CPYFMRT

**Assembly:** `CPYFMRT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFERT_CPY_memcms`  [Epilogue]  — CPYFERT

**Assembly:** `CPYFERT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPRTN, CPYFMRTN, CPYFERTN -- A64

### Encoding: `CPYFPRTN_CPY_memcms`  [Prologue]  — CPYFPRTN

**Assembly:** `CPYFPRTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMRTN_CPY_memcms`  [Main]  — CPYFMRTN

**Assembly:** `CPYFMRTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFERTN_CPY_memcms`  [Epilogue]  — CPYFERTN

**Assembly:** `CPYFERTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPRTRN, CPYFMRTRN, CPYFERTRN -- A64

### Encoding: `CPYFPRTRN_CPY_memcms`  [Prologue]  — CPYFPRTRN

**Assembly:** `CPYFPRTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMRTRN_CPY_memcms`  [Main]  — CPYFMRTRN

**Assembly:** `CPYFMRTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFERTRN_CPY_memcms`  [Epilogue]  — CPYFERTRN

**Assembly:** `CPYFERTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPRTWN, CPYFMRTWN, CPYFERTWN -- A64

### Encoding: `CPYFPRTWN_CPY_memcms`  [Prologue]  — CPYFPRTWN

**Assembly:** `CPYFPRTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMRTWN_CPY_memcms`  [Main]  — CPYFMRTWN

**Assembly:** `CPYFMRTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFERTWN_CPY_memcms`  [Epilogue]  — CPYFERTWN

**Assembly:** `CPYFERTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPT, CPYFMT, CPYFET -- A64

### Encoding: `CPYFPT_CPY_memcms`  [Prologue]  — CPYFPT

**Assembly:** `CPYFPT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMT_CPY_memcms`  [Main]  — CPYFMT

**Assembly:** `CPYFMT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFET_CPY_memcms`  [Epilogue]  — CPYFET

**Assembly:** `CPYFET  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPTN, CPYFMTN, CPYFETN -- A64

### Encoding: `CPYFPTN_CPY_memcms`  [Prologue]  — CPYFPTN

**Assembly:** `CPYFPTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMTN_CPY_memcms`  [Main]  — CPYFMTN

**Assembly:** `CPYFMTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFETN_CPY_memcms`  [Epilogue]  — CPYFETN

**Assembly:** `CPYFETN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPTRN, CPYFMTRN, CPYFETRN -- A64

### Encoding: `CPYFPTRN_CPY_memcms`  [Prologue]  — CPYFPTRN

**Assembly:** `CPYFPTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMTRN_CPY_memcms`  [Main]  — CPYFMTRN

**Assembly:** `CPYFMTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFETRN_CPY_memcms`  [Epilogue]  — CPYFETRN

**Assembly:** `CPYFETRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPTWN, CPYFMTWN, CPYFETWN -- A64

### Encoding: `CPYFPTWN_CPY_memcms`  [Prologue]  — CPYFPTWN

**Assembly:** `CPYFPTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMTWN_CPY_memcms`  [Main]  — CPYFMTWN

**Assembly:** `CPYFMTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFETWN_CPY_memcms`  [Epilogue]  — CPYFETWN

**Assembly:** `CPYFETWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPWN, CPYFMWN, CPYFEWN -- A64

### Encoding: `CPYFPWN_CPY_memcms`  [Prologue]  — CPYFPWN

**Assembly:** `CPYFPWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMWN_CPY_memcms`  [Main]  — CPYFMWN

**Assembly:** `CPYFMWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFEWN_CPY_memcms`  [Epilogue]  — CPYFEWN

**Assembly:** `CPYFEWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPWT, CPYFMWT, CPYFEWT -- A64

### Encoding: `CPYFPWT_CPY_memcms`  [Prologue]  — CPYFPWT

**Assembly:** `CPYFPWT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMWT_CPY_memcms`  [Main]  — CPYFMWT

**Assembly:** `CPYFMWT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFEWT_CPY_memcms`  [Epilogue]  — CPYFEWT

**Assembly:** `CPYFEWT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPWTN, CPYFMWTN, CPYFEWTN -- A64

### Encoding: `CPYFPWTN_CPY_memcms`  [Prologue]  — CPYFPWTN

**Assembly:** `CPYFPWTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMWTN_CPY_memcms`  [Main]  — CPYFMWTN

**Assembly:** `CPYFMWTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFEWTN_CPY_memcms`  [Epilogue]  — CPYFEWTN

**Assembly:** `CPYFEWTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPWTRN, CPYFMWTRN, CPYFEWTRN -- A64

### Encoding: `CPYFPWTRN_CPY_memcms`  [Prologue]  — CPYFPWTRN

**Assembly:** `CPYFPWTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMWTRN_CPY_memcms`  [Main]  — CPYFMWTRN

**Assembly:** `CPYFMWTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFEWTRN_CPY_memcms`  [Epilogue]  — CPYFEWTRN

**Assembly:** `CPYFEWTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYFPWTWN, CPYFMWTWN, CPYFEWTWN -- A64

### Encoding: `CPYFPWTWN_CPY_memcms`  [Prologue]  — CPYFPWTWN

**Assembly:** `CPYFPWTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFMWTWN_CPY_memcms`  [Main]  — CPYFMWTWN

**Assembly:** `CPYFMWTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYFEWTWN_CPY_memcms`  [Epilogue]  — CPYFEWTWN

**Assembly:** `CPYFEWTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYP, CPYM, CPYE -- A64

### Encoding: `CPYP_CPY_memcms`  [Prologue]  — CPYP

**Assembly:** `CPYP  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYM_CPY_memcms`  [Main]  — CPYM

**Assembly:** `CPYM  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYE_CPY_memcms`  [Epilogue]  — CPYE

**Assembly:** `CPYE  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPN, CPYMN, CPYEN -- A64

### Encoding: `CPYPN_CPY_memcms`  [Prologue]  — CPYPN

**Assembly:** `CPYPN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMN_CPY_memcms`  [Main]  — CPYMN

**Assembly:** `CPYMN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYEN_CPY_memcms`  [Epilogue]  — CPYEN

**Assembly:** `CPYEN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPRN, CPYMRN, CPYERN -- A64

### Encoding: `CPYPRN_CPY_memcms`  [Prologue]  — CPYPRN

**Assembly:** `CPYPRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMRN_CPY_memcms`  [Main]  — CPYMRN

**Assembly:** `CPYMRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYERN_CPY_memcms`  [Epilogue]  — CPYERN

**Assembly:** `CPYERN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPRT, CPYMRT, CPYERT -- A64

### Encoding: `CPYPRT_CPY_memcms`  [Prologue]  — CPYPRT

**Assembly:** `CPYPRT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMRT_CPY_memcms`  [Main]  — CPYMRT

**Assembly:** `CPYMRT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYERT_CPY_memcms`  [Epilogue]  — CPYERT

**Assembly:** `CPYERT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPRTN, CPYMRTN, CPYERTN -- A64

### Encoding: `CPYPRTN_CPY_memcms`  [Prologue]  — CPYPRTN

**Assembly:** `CPYPRTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMRTN_CPY_memcms`  [Main]  — CPYMRTN

**Assembly:** `CPYMRTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYERTN_CPY_memcms`  [Epilogue]  — CPYERTN

**Assembly:** `CPYERTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPRTRN, CPYMRTRN, CPYERTRN -- A64

### Encoding: `CPYPRTRN_CPY_memcms`  [Prologue]  — CPYPRTRN

**Assembly:** `CPYPRTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMRTRN_CPY_memcms`  [Main]  — CPYMRTRN

**Assembly:** `CPYMRTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYERTRN_CPY_memcms`  [Epilogue]  — CPYERTRN

**Assembly:** `CPYERTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPRTWN, CPYMRTWN, CPYERTWN -- A64

### Encoding: `CPYPRTWN_CPY_memcms`  [Prologue]  — CPYPRTWN

**Assembly:** `CPYPRTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMRTWN_CPY_memcms`  [Main]  — CPYMRTWN

**Assembly:** `CPYMRTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYERTWN_CPY_memcms`  [Epilogue]  — CPYERTWN

**Assembly:** `CPYERTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPT, CPYMT, CPYET -- A64

### Encoding: `CPYPT_CPY_memcms`  [Prologue]  — CPYPT

**Assembly:** `CPYPT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMT_CPY_memcms`  [Main]  — CPYMT

**Assembly:** `CPYMT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYET_CPY_memcms`  [Epilogue]  — CPYET

**Assembly:** `CPYET  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPTN, CPYMTN, CPYETN -- A64

### Encoding: `CPYPTN_CPY_memcms`  [Prologue]  — CPYPTN

**Assembly:** `CPYPTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMTN_CPY_memcms`  [Main]  — CPYMTN

**Assembly:** `CPYMTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYETN_CPY_memcms`  [Epilogue]  — CPYETN

**Assembly:** `CPYETN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPTRN, CPYMTRN, CPYETRN -- A64

### Encoding: `CPYPTRN_CPY_memcms`  [Prologue]  — CPYPTRN

**Assembly:** `CPYPTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMTRN_CPY_memcms`  [Main]  — CPYMTRN

**Assembly:** `CPYMTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYETRN_CPY_memcms`  [Epilogue]  — CPYETRN

**Assembly:** `CPYETRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPTWN, CPYMTWN, CPYETWN -- A64

### Encoding: `CPYPTWN_CPY_memcms`  [Prologue]  — CPYPTWN

**Assembly:** `CPYPTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMTWN_CPY_memcms`  [Main]  — CPYMTWN

**Assembly:** `CPYMTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYETWN_CPY_memcms`  [Epilogue]  — CPYETWN

**Assembly:** `CPYETWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPWN, CPYMWN, CPYEWN -- A64

### Encoding: `CPYPWN_CPY_memcms`  [Prologue]  — CPYPWN

**Assembly:** `CPYPWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMWN_CPY_memcms`  [Main]  — CPYMWN

**Assembly:** `CPYMWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYEWN_CPY_memcms`  [Epilogue]  — CPYEWN

**Assembly:** `CPYEWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPWT, CPYMWT, CPYEWT -- A64

### Encoding: `CPYPWT_CPY_memcms`  [Prologue]  — CPYPWT

**Assembly:** `CPYPWT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMWT_CPY_memcms`  [Main]  — CPYMWT

**Assembly:** `CPYMWT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYEWT_CPY_memcms`  [Epilogue]  — CPYEWT

**Assembly:** `CPYEWT  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPWTN, CPYMWTN, CPYEWTN -- A64

### Encoding: `CPYPWTN_CPY_memcms`  [Prologue]  — CPYPWTN

**Assembly:** `CPYPWTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMWTN_CPY_memcms`  [Main]  — CPYMWTN

**Assembly:** `CPYMWTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYEWTN_CPY_memcms`  [Epilogue]  — CPYEWTN

**Assembly:** `CPYEWTN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPWTRN, CPYMWTRN, CPYEWTRN -- A64

### Encoding: `CPYPWTRN_CPY_memcms`  [Prologue]  — CPYPWTRN

**Assembly:** `CPYPWTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMWTRN_CPY_memcms`  [Main]  — CPYMWTRN

**Assembly:** `CPYMWTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYEWTRN_CPY_memcms`  [Epilogue]  — CPYEWTRN

**Assembly:** `CPYEWTRN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CPYPWTWN, CPYMWTWN, CPYEWTWN -- A64

### Encoding: `CPYPWTWN_CPY_memcms`  [Prologue]  — CPYPWTWN

**Assembly:** `CPYPWTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the source address and is upda | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be tran | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYMWTWN_CPY_memcms`  [Main]  — CPYMWTWN

**Assembly:** `CPYMWTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

### Encoding: `CPYEWTWN_CPY_memcms`  [Epilogue]  — CPYEWTWN

**Assembly:** `CPYEWTWN  [<Xd>]!, [<Xs>]!, <Xn>!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.d = UInt(Rd); |
| `<Xs>` | Rs (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memcpy.s = UInt(Rs); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memcpy.n = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memcpy : CPYParams;
memcpy.d = UInt(Rd);
memcpy.s = UInt(Rs);
memcpy.n = UInt(Rn);
let options : bits(4) = op2;
let rnontemporal : boolean = options[3] == '1';
let wnontemporal : boolean = options[2] == '1';
case op1 of
    when '00' => memcpy.stage = MOPSStage_Prologue;
    when '01' => memcpy.stage = MOPSStage_Main;
    when '10' => memcpy.stage = MOPSStage_Epilogue;
end;
```

---

## CRC32B, CRC32H, CRC32W, CRC32X -- A64

### Encoding: `CRC32B_32C_dp_2src`  [CRC32B]  — CRC32B

**Assembly:** `CRC32B  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose accumulator output register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose accumulator input register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose data source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CRC32) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if sf == '1' && sz != '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && sz == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
```

### Encoding: `CRC32H_32C_dp_2src`  [CRC32H]  — CRC32H

**Assembly:** `CRC32H  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose accumulator output register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose accumulator input register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose data source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CRC32) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if sf == '1' && sz != '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && sz == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
```

### Encoding: `CRC32W_32C_dp_2src`  [CRC32W]  — CRC32W

**Assembly:** `CRC32W  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose accumulator output register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose accumulator input register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose data source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CRC32) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if sf == '1' && sz != '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && sz == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
```

### Encoding: `CRC32X_64C_dp_2src`  [CRC32X]  — CRC32X

**Assembly:** `CRC32X  <Wd>, <Wn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose accumulator output register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose accumulator input register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose data source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CRC32) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if sf == '1' && sz != '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && sz == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
```

---

## CRC32CB, CRC32CH, CRC32CW, CRC32CX -- A64

### Encoding: `CRC32CB_32C_dp_2src`  [CRC32CB]  — CRC32CB

**Assembly:** `CRC32CB  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose accumulator output register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose accumulator input register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose data source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CRC32) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if sf == '1' && sz != '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && sz == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
```

### Encoding: `CRC32CH_32C_dp_2src`  [CRC32CH]  — CRC32CH

**Assembly:** `CRC32CH  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose accumulator output register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose accumulator input register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose data source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CRC32) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if sf == '1' && sz != '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && sz == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
```

### Encoding: `CRC32CW_32C_dp_2src`  [CRC32CW]  — CRC32CW

**Assembly:** `CRC32CW  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose accumulator output register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose accumulator input register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose data source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CRC32) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if sf == '1' && sz != '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && sz == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
```

### Encoding: `CRC32CX_64C_dp_2src`  [CRC32CX]  — CRC32CX

**Assembly:** `CRC32CX  <Wd>, <Wn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose accumulator output register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose accumulator input register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose data source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CRC32) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if sf == '1' && sz != '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && sz == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
```

---

## CSDB -- A64

### Encoding: `CSDB_HI_hints`  — CSDB

**Assembly:** `CSDB`

**Pseudocode (Decode):**
```
// Empty.
```

---

## CSEL -- A64

### Encoding: `CSEL_32_condsel`  [32-bit]  — CSEL

**Assembly:** `CSEL  <Wd>, <Wn>, <Wm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
```

### Encoding: `CSEL_64_condsel`  [64-bit]  — CSEL

**Assembly:** `CSEL  <Xd>, <Xn>, <Xm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
```

---

## CSET -- A64 *(alias for CSINC)*

### Encoding: `CSET_CSINC_32_condsel`  [32-bit]  — CSINC

**Assembly:** `CSET  <Wd>, <invcond> / CSINC  <Wd>, WZR, WZR, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<invcond>` | — |  |  |
| `CSINC` | — |  |  |

### Encoding: `CSET_CSINC_64_condsel`  [64-bit]  — CSINC

**Assembly:** `CSET  <Xd>, <invcond> / CSINC  <Xd>, XZR, XZR, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<invcond>` | — |  |  |
| `CSINC` | — |  |  |

---

## CSETM -- A64 *(alias for CSINV)*

### Encoding: `CSETM_CSINV_32_condsel`  [32-bit]  — CSINV

**Assembly:** `CSETM  <Wd>, <invcond> / CSINV  <Wd>, WZR, WZR, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<invcond>` | — |  |  |
| `CSINV` | — |  |  |

### Encoding: `CSETM_CSINV_64_condsel`  [64-bit]  — CSINV

**Assembly:** `CSETM  <Xd>, <invcond> / CSINV  <Xd>, XZR, XZR, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<invcond>` | — |  |  |
| `CSINV` | — |  |  |

---

## CSINC -- A64

### Encoding: `CSINC_32_condsel`  [32-bit]  — CSINC

**Assembly:** `CSINC  <Wd>, <Wn>, <Wm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
```

### Encoding: `CSINC_64_condsel`  [64-bit]  — CSINC

**Assembly:** `CSINC  <Xd>, <Xn>, <Xm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
```

---

## CSINV -- A64

### Encoding: `CSINV_32_condsel`  [32-bit]  — CSINV

**Assembly:** `CSINV  <Wd>, <Wn>, <Wm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
```

### Encoding: `CSINV_64_condsel`  [64-bit]  — CSINV

**Assembly:** `CSINV  <Xd>, <Xn>, <Xm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
```

---

## CSNEG -- A64

### Encoding: `CSNEG_32_condsel`  [32-bit]  — CSNEG

**Assembly:** `CSNEG  <Wd>, <Wn>, <Wm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
```

### Encoding: `CSNEG_64_condsel`  [64-bit]  — CSNEG

**Assembly:** `CSNEG  <Xd>, <Xn>, <Xm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let condition : bits(4) = cond;
```

---

## CTERMEQ, CTERMNE -- A64

### Encoding: `ctermeq_rr_`  — CTERMEQ

**Assembly:** `CTERMEQ  <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let cmp_op : CmpOp = Cmp_EQ;
```

### Encoding: `ctermne_rr_`  — CTERMNE

**Assembly:** `CTERMNE  <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let cmp_op : CmpOp = Cmp_NE;
```

---

## CTZ -- A64

### Encoding: `CTZ_32_dp_1src`  [32-bit]  — CTZ

**Assembly:** `CTZ  <Wd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

### Encoding: `CTZ_64_dp_1src`  [64-bit]  — CTZ

**Assembly:** `CTZ  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let datasize : integer{} = 32 << UInt(sf);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## DC -- A64 *(alias for SYS)*

### Encoding: `DC_SYS_CR_systeminstrs`  — SYS

**Assembly:** `DC  <dc_op>, <Xt> / SYS  #<op1>, C7, <Cm>, #<op2>, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<dc_op>` | — |  |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## DCPS1 -- A64

### Encoding: `DCPS1_DC_exception`  — DCPS1

**Assembly:** `DCPS1  {#<imm>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | imm16 (16b) | Is an optional 16-bit unsigned immediate, in the range 0 to 65535, defaulting to 0 and encoded in the "imm16" field. |  |

**Pseudocode (Decode):**
```
// Empty.
```

---

## DCPS2 -- A64

### Encoding: `DCPS2_DC_exception`  — DCPS2

**Assembly:** `DCPS2  {#<imm>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | imm16 (16b) | Is an optional 16-bit unsigned immediate, in the range 0 to 65535, defaulting to 0 and encoded in the "imm16" field. |  |

**Pseudocode (Decode):**
```
// Empty.
```

---

## DCPS3 -- A64

### Encoding: `DCPS3_DC_exception`  — DCPS3

**Assembly:** `DCPS3  {#<imm>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | imm16 (16b) | Is an optional 16-bit unsigned immediate, in the range 0 to 65535, defaulting to 0 and encoded in the "imm16" field. |  |

**Pseudocode (Decode):**
```
// Empty.
```

---

## DECB, DECD, DECH, DECW (scalar) -- A64

### Encoding: `decb_r_rs_`  — DECB

**Assembly:** `DECB  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `decd_r_rs_`  — DECD

**Assembly:** `DECD  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `dech_r_rs_`  — DECH

**Assembly:** `DECH  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `decw_r_rs_`  — DECW

**Assembly:** `DECW  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

---

## DECD, DECH, DECW (vector) -- A64

### Encoding: `decd_z_zs_`  — DECD

**Assembly:** `DECD  <Zdn>.D{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `dech_z_zs_`  — DECH

**Assembly:** `DECH  <Zdn>.H{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `decw_z_zs_`  — DECW

**Assembly:** `DECW  <Zdn>.S{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

---

## DECP (scalar) -- A64

### Encoding: `decp_r_p_r_`  — DECP

**Assembly:** `DECP  <Xdn>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
```

---

## DECP (vector) -- A64

### Encoding: `decp_z_p_z_`  — DECP

**Assembly:** `DECP  <Zdn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Zdn);
```

---

## DGH -- A64

### Encoding: `DGH_HI_hints`  — DGH

**Assembly:** `DGH`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_DGH) then EndOfDecode(Decode_NOP); end;
```

---

## DMB -- A64

### Encoding: `DMB_BO_barriers`  — DMB

**Assembly:** `DMB  (<option>|#<imm>)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<option>` | — |  |  |
| `<imm>` | CRm (4b) | Is a 4-bit unsigned immediate, in the range 0 to 15, encoded in the "CRm" field. | case CRm[3:2] of<br>case CRm[1:0] of |

**Pseudocode (Decode):**
```
var domain : MBReqDomain;
var types : MBReqTypes;
case CRm[3:2] of
    when '00' => domain = MBReqDomain_OuterShareable;
    when '01' => domain = MBReqDomain_Nonshareable;
    when '10' => domain = MBReqDomain_InnerShareable;
    when '11' => domain = MBReqDomain_FullSystem;
end;
case CRm[1:0] of
    when '00' => types = MBReqTypes_All; domain = MBReqDomain_FullSystem;
    when '01' => types = MBReqTypes_Reads;
    when '10' => types = MBReqTypes_Writes;
    when '11' => types = MBReqTypes_All;
end;
```

---

## DRPS -- A64

### Encoding: `DRPS_64E_branch_reg`  — DRPS

**Assembly:** `DRPS`

**Pseudocode (Decode):**
```
// Empty.
```

---

## DSB -- A64

### Encoding: `DSB_BO_barriers`  — DSB

**Assembly:** `DSB  (<option>|#<imm>)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<option>` | — |  |  |
| `<imm>` | CRm (4b) | Is a 4-bit unsigned immediate, in the range 0 to 15, encoded in the "CRm" field. | case CRm of<br>case CRm[3:2] of<br>case CRm[1:0] of |

**Pseudocode (Decode):**
```
var nXS : boolean = FALSE;

var alias : DSBAlias;
case CRm of
    when '0000' => alias = DSBAlias_SSBB;
    when '0100' => alias = DSBAlias_PSSBB;
    otherwise =>   alias = DSBAlias_DSB;
end;

var domain : MBReqDomain;
case CRm[3:2] of
    when '00' => domain = MBReqDomain_OuterShareable;
    when '01' => domain = MBReqDomain_Nonshareable;
    when '10' => domain = MBReqDomain_InnerShareable;
    when '11' => domain = MBReqDomain_FullSystem;
end;

var types : MBReqTypes;
case CRm[1:0] of
    when '00' => types = MBReqTypes_All; domain = MBReqDomain_FullSystem;
    when '01' => types = MBReqTypes_Reads;
    when '10' => types = MBReqTypes_Writes;
    when '11' => types = MBReqTypes_All;
end;
```

### Encoding: `DSB_BOn_barriers`  — DSB

**Assembly:** `DSB  <option>nXS`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<option>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_XS) then EndOfDecode(Decode_UNDEF); end;
let types : MBReqTypes = MBReqTypes_All;
var nXS : boolean = TRUE;
let alias : DSBAlias = DSBAlias_DSB;
var domain : MBReqDomain;

case imm2 of
    when '00' => domain = MBReqDomain_OuterShareable;
    when '01' => domain = MBReqDomain_Nonshareable;
    when '10' => domain = MBReqDomain_InnerShareable;
    when '11' => domain = MBReqDomain_FullSystem;
end;
```

---

## DUP (element) -- A64

### Encoding: `DUP_asisdone_only`  — DUP

**Assembly:** `DUP  <V><d>, <Vn>.<T>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'x0000' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = LowestSetBitNZ(imm5[3:0]);

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let index : integer = UInt(imm5[4:size+1]);
let idxdsize : integer{} = 64 << UInt(imm5[4]);
let esize : integer{} = 8 << size;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `DUP_asimdins_DV_v`  — DUP

**Assembly:** `DUP  <Vd>.<T>, <Vn>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'x0000' then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'x1000' && Q == '0' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = LowestSetBitNZ(imm5[3:0]);

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let index : integer = UInt(imm5[4:size+1]);
let idxdsize : integer{} = 64 << UInt(imm5[4]);
let esize : integer{} = 8 << size;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## DUP (general) -- A64

### Encoding: `DUP_asimdins_DR_r`  — DUP

**Assembly:** `DUP  <Vd>.<T>, <R><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the general-purpose source register or ZR (31), encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'x0000' then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'x1000' && Q == '0' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = LowestSetBitNZ(imm5[3:0]);

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
// imm5<4:size+1> is IGNORED
let esize : integer{} = 8 << size;
let datasize : integer{} = 64 << UInt(Q);
```

---

## DUP (immediate) -- A64

### Encoding: `dup_z_i_`  — DUP

**Assembly:** `DUP  <Zd>.<T>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is a signed immediate in the range -128 to 127, encoded in the "imm8" field. | var imm : integer = SInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt(Zd);
var imm : integer = SInt(imm8);
if sh == '1' then imm = imm << 8; end;
```

---

## DUP (indexed) -- A64

### Encoding: `dup_z_zi_`  — DUP

**Assembly:** `DUP  <Zd>.<T>, <Zn>.<T>[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | (imm2 :: tsz) | Is the immediate index, in the range 0 to one less than the number of elements in 512 bits, encoded in "imm2:tsz". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if tsz == '00000' then EndOfDecode(Decode_UNDEF); end;
let lsb : integer{} = LowestSetBit(tsz);
let esize : integer{} = 8 << lsb;
let imm : bits(7) = imm2::tsz;
let index : integer = UInt(imm[6:(lsb+1)]);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## DUP (scalar) -- A64

### Encoding: `dup_z_r_`  — DUP

**Assembly:** `DUP  <Zd>.<T>, <R><n|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n\|SP>` | Rn (5b) | Is the number [0-30] of the general-purpose source register or the name SP (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Rn);
let d : integer = UInt(Zd);
```

---

## DUPM -- A64

### Encoding: `dupm_z_i_`  — DUPM

**Assembly:** `DUPM  <Zd>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<const>` | imm13 (13b) | Is a 64, 32, 16 or 8-bit bitmask consisting of replicated 2, 4, 8, 16, 32 or 64 bit fields, each field containing a rota | (imm, -) = DecodeBitMasks{esize}(imm13[12], imm13[5:0], imm13[11:6], TRUE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let d : integer = UInt(Zd);
var imm : bits(esize);
(imm, -) = DecodeBitMasks{esize}(imm13[12], imm13[5:0], imm13[11:6], TRUE);
```

---

## DUPQ -- A64

### Encoding: `dupq_z_zi_`  — DUPQ

**Assembly:** `DUPQ  <Zd>.<T>, <Zn>.<T>[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | (i1 :: tsz) | Is the immediate index, in the range 0 to one less than the number of elements in 128 bits, encoded in "i1:tsz". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if tsz == '0000' then EndOfDecode(Decode_UNDEF); end;
let lsb : integer{} = LowestSetBit(tsz);
let esize : integer{} = 8 << lsb;
let imm : bits(5) = i1::tsz;
let index : integer = UInt(imm[4:(lsb+1)]);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## DVP -- A64 *(alias for SYS)*

### Encoding: `DVP_SYS_CR_systeminstrs`  — SYS

**Assembly:** `DVP  RCTX, <Xt> / SYS  #3, C7, C3, #5, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## EON (shifted register) -- A64

### Encoding: `EON_32_log_shift`  [32-bit]  — EON

**Assembly:** `EON  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `EON_64_log_shift`  [64-bit]  — EON

**Assembly:** `EON  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## EON -- A64 *(alias for eor_z_zi)*

### Encoding: `eon_z_zi__eor_z_zi_`  — EOR

**Assembly:** `EON  <Zdn>.<T>, <Zdn>.<T>, #<const> / EOR  <Zdn>.<T>, <Zdn>.<T>, #(-<const> - 1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. |  |
| `<T>` | — |  |  |
| `<const>` | imm13 (13b) | Is a 64, 32, 16 or 8-bit bitmask consisting of replicated 2, 4, 8, 16, 32 or 64 bit fields, each field containing a rota |  |
| `EOR` | — |  |  |

---

## EOR (immediate) -- A64

### Encoding: `EOR_32_log_imm`  [32-bit]  — EOR

**Assembly:** `EOR  <Wd|WSP>, <Wn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd\|WSP>` | Rd (5b) | Is the 32-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | (imms :: immr) | For the "32-bit" variant: is the bitmask immediate, encoded in "imms:immr". |  |

**Pseudocode (Decode):**
```
if sf == '0' && N != '0' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
var imm : bits(datasize);
(imm, -) = DecodeBitMasks{datasize}(N, imms, immr, TRUE);
```

### Encoding: `EOR_64_log_imm`  [64-bit]  — EOR

**Assembly:** `EOR  <Xd|SP>, <Xn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | (N :: imms :: immr) | For the "64-bit" variant: is the bitmask immediate, encoded in "N:imms:immr". |  |

**Pseudocode (Decode):**
```
if sf == '0' && N != '0' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
var imm : bits(datasize);
(imm, -) = DecodeBitMasks{datasize}(N, imms, immr, TRUE);
```

---

## EOR (immediate) -- A64

### Encoding: `eor_z_zi_`  — EOR

**Assembly:** `EOR  <Zdn>.<T>, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<const>` | imm13 (13b) | Is a 64, 32, 16 or 8-bit bitmask consisting of replicated 2, 4, 8, 16, 32 or 64 bit fields, each field containing a rota | (imm, -) = DecodeBitMasks{64}(imm13[12], imm13[5:0], imm13[11:6], TRUE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn);
var imm : bits(64);
(imm, -) = DecodeBitMasks{64}(imm13[12], imm13[5:0], imm13[11:6], TRUE);
```

---

## EOR (predicates) -- A64

### Encoding: `eor_p_p_pp_z`  — EOR

**Assembly:** `EOR  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## EOR (shifted register) -- A64

### Encoding: `EOR_32_log_shift`  [32-bit]  — EOR

**Assembly:** `EOR  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `EOR_64_log_shift`  [64-bit]  — EOR

**Assembly:** `EOR  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## EOR (vector) -- A64

### Encoding: `EOR_asimdsame_only`  — EOR

**Assembly:** `EOR  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
```

---

## EOR (vectors, predicated) -- A64

### Encoding: `eor_z_p_zz_`  — EOR

**Assembly:** `EOR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## EOR (vectors, unpredicated) -- A64

### Encoding: `eor_z_zz_`  — EOR

**Assembly:** `EOR  <Zd>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## EOR3 -- A64

### Encoding: `EOR3_VVV16_crypto4`  — EOR3

**Assembly:** `EOR3  <Vd>.16B, <Vn>.16B, <Vm>.16B, <Va>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Va>` | Ra (5b) | Is the name of the third SIMD&FP source register, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
```

---

## EOR3 -- A64

### Encoding: `eor3_z_zzz_`  — EOR3

**Assembly:** `EOR3  <Zdn>.D, <Zdn>.D, <Zm>.D, <Zk>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | Zk (5b) | Is the name of the third source scalable vector register, encoded in the "Zk" field. | let k : integer = UInt(Zk); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let m : integer = UInt(Zm);
let k : integer = UInt(Zk);
let dn : integer = UInt(Zdn);
```

---

## EORBT -- A64

### Encoding: `eorbt_z_zz_`  — EORBT

**Assembly:** `EORBT  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 0;
let sel2 : integer = 1;
```

---

## EORQV -- A64

### Encoding: `eorqv_z_p_z_`  — EORQV

**Assembly:** `EORQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## EORS -- A64

### Encoding: `eors_p_p_pp_z`  — EORS

**Assembly:** `EORS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## EORTB -- A64

### Encoding: `eortb_z_zz_`  — EORTB

**Assembly:** `EORTB  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 1;
let sel2 : integer = 0;
```

---

## EORV -- A64

### Encoding: `eorv_r_p_z_`  — EORV

**Assembly:** `EORV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## ERET -- A64

### Encoding: `ERET_64E_branch_reg`  — ERET

**Assembly:** `ERET`

**Pseudocode (Decode):**
```
let pac : boolean = FALSE;
let use_key_a : boolean = TRUE;
```

---

## ERETAA, ERETAB -- A64

### Encoding: `ERETAA_64E_branch_reg`  [ERETAA]  — ERETAA

**Assembly:** `ERETAA`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;

let pac : boolean = TRUE;
let use_key_a : boolean = (M == '0');
let auth_then_branch : boolean = TRUE;
```

### Encoding: `ERETAB_64E_branch_reg`  [ERETAB]  — ERETAB

**Assembly:** `ERETAB`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;

let pac : boolean = TRUE;
let use_key_a : boolean = (M == '0');
let auth_then_branch : boolean = TRUE;
```

---

## ESB -- A64

### Encoding: `ESB_HI_hints`  — ESB

**Assembly:** `ESB`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RAS) then EndOfDecode(Decode_NOP); end;
```

---

## EXPAND -- A64

### Encoding: `expand_z_p_z_`  — EXPAND

**Assembly:** `EXPAND  <Zd>.<T>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## EXT -- A64

### Encoding: `EXT_asimdext_only`  — EXT

**Assembly:** `EXT  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>, #<index>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if Q == '0' && imm4[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
let position : integer{} = 8 * UInt(imm4);
```

---

## EXT -- A64

### Encoding: `ext_z_zi_con`  — EXT

**Assembly:** `EXT  <Zd>.B, { <Zn1>.B, <Zn2>.B }, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let dst : integer = UInt(Zd); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the source multi-vector group, encoded in the "Zn" field. | let s1 : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the source multi-vector group, encoded in the "Zn" field. | let s1 : integer = UInt(Zn); |
| `<imm>` | (imm8h :: imm8l) | Is the unsigned immediate operand, in the range 0 to 255, encoded in the "imm8h:imm8l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dst : integer = UInt(Zd);
let s1 : integer = UInt(Zn);
let s2 : integer = (s1 + 1) MOD 32;
let position : integer{} = UInt(imm8h::imm8l) * 8;
```

### Encoding: `ext_z_zi_des`  — EXT

**Assembly:** `EXT  <Zdn>.B, <Zdn>.B, <Zm>.B, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dst : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let s2 : integer = UInt(Zm); |
| `<imm>` | (imm8h :: imm8l) | Is the unsigned immediate operand, in the range 0 to 255, encoded in the "imm8h:imm8l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dst : integer = UInt(Zdn);
let s1 : integer = dst;
let s2 : integer = UInt(Zm);
let position : integer{} = UInt(imm8h::imm8l) * 8;
```

---

## EXTQ -- A64

### Encoding: `extq_z_zi_des`  — EXTQ

**Assembly:** `EXTQ  <Zdn>.B, <Zdn>.B, <Zm>.B, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | imm4 (4b) | Is the unsigned immediate operand, in the range 0 to 15, encoded in the "imm4" field. | let position : integer{} = UInt(imm4) << 3; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let position : integer{} = UInt(imm4) << 3;
```

---

## EXTR -- A64

### Encoding: `EXTR_32_extract`  [32-bit]  — EXTR

**Assembly:** `EXTR  <Wd>, <Wn>, <Wm>, #<lsb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<lsb>` | imms (6b) | For the "32-bit" variant: is the least significant bit position from which to extract, in the range 0 to 31, encoded in  | if sf == '0' && imms[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let lsb : integer{} = UInt(imms); |

**Pseudocode (Decode):**
```
if N != sf then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imms[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let lsb : integer{} = UInt(imms);
```

### Encoding: `EXTR_64_extract`  [64-bit]  — EXTR

**Assembly:** `EXTR  <Xd>, <Xn>, <Xm>, #<lsb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<lsb>` | imms (6b) | For the "64-bit" variant: is the least significant bit position from which to extract, in the range 0 to 63, encoded in  | if sf == '0' && imms[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let lsb : integer{} = UInt(imms); |

**Pseudocode (Decode):**
```
if N != sf then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imms[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let lsb : integer{} = UInt(imms);
```

---

## F1CVT, F2CVT -- A64

### Encoding: `f1cvt_mz2_z8_`  — F1CVT

**Assembly:** `F1CVT  { <Zd1>.H-<Zd2>.H }, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd:: '0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd:: '0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd:: '0');
let issrc2 : boolean = FALSE;
```

### Encoding: `f2cvt_mz2_z8_`  — F2CVT

**Assembly:** `F2CVT  { <Zd1>.H-<Zd2>.H }, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd:: '0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd:: '0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd:: '0');
let issrc2 : boolean = TRUE;
```

---

## F1CVT, F2CVT -- A64

### Encoding: `f1cvt_z_z8_b2h`  — F1CVT

**Assembly:** `F1CVT  <Zd>.H, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 8;
let d_esize : integer{} = 16;
let issrc2 : boolean = FALSE;
```

### Encoding: `f2cvt_z_z8_b2h`  — F2CVT

**Assembly:** `F2CVT  <Zd>.H, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 8;
let d_esize : integer{} = 16;
let issrc2 : boolean = TRUE;
```

---

## F1CVTL, F1CVTL2, F2CVTL, F2CVTL2 -- A64

### Encoding: `F1CVTL_asimdmisc_V`  [F1CVTL{2}]  — F1CVTL

**Assembly:** `F1CVTL{2}  <Vd>.8H, <Vn>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Ta>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let d : integer = UInt(Rd);
let part : integer = UInt(Q);
let elements : integer = 64 DIV 8;
let issrc2 : boolean = size == '01';
```

### Encoding: `F2CVTL_asimdmisc_V`  [F2CVTL{2}]  — F2CVTL

**Assembly:** `F2CVTL{2}  <Vd>.8H, <Vn>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Ta>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let d : integer = UInt(Rd);
let part : integer = UInt(Q);
let elements : integer = 64 DIV 8;
let issrc2 : boolean = size == '01';
```

---

## F1CVTL, F2CVTL -- A64

### Encoding: `f1cvtl_mz2_z8_`  — F1CVTL

**Assembly:** `F1CVTL  { <Zd1>.H-<Zd2>.H }, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd:: '0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd:: '0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd:: '0');
let issrc2 : boolean = FALSE;
```

### Encoding: `f2cvtl_mz2_z8_`  — F2CVTL

**Assembly:** `F2CVTL  { <Zd1>.H-<Zd2>.H }, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd:: '0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd:: '0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd:: '0');
let issrc2 : boolean = TRUE;
```

---

## F1CVTLT, F2CVTLT -- A64

### Encoding: `f1cvtlt_z_z8_b2h`  — F1CVTLT

**Assembly:** `F1CVTLT  <Zd>.H, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let issrc2 : boolean = FALSE;
```

### Encoding: `f2cvtlt_z_z8_b2h`  — F2CVTLT

**Assembly:** `F2CVTLT  <Zd>.H, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let issrc2 : boolean = TRUE;
```

---

## FABD -- A64

### Encoding: `FABD_asisdsamefp16_only`  — FABD

**Assembly:** `FABD  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FABD_asisdsame_only`  — FABD

**Assembly:** `FABD  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FABD_asimdsamefp16_only`  — FABD

**Assembly:** `FABD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FABD_asimdsame_only`  — FABD

**Assembly:** `FABD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FABD -- A64

### Encoding: `fabd_z_p_zz_`  — FABD

**Assembly:** `FABD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FABS (scalar) -- A64

### Encoding: `FABS_H_floatdp1`  [Half-precision]  — FABS

**Assembly:** `FABS  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FABS_S_floatdp1`  [Single-precision]  — FABS

**Assembly:** `FABS  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FABS_D_floatdp1`  [Double-precision]  — FABS

**Assembly:** `FABS  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FABS (vector) -- A64

### Encoding: `FABS_asimdmiscfp16_R`  — FABS

**Assembly:** `FABS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FABS_asimdmisc_R`  — FABS

**Assembly:** `FABS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FABS -- A64

### Encoding: `fabs_z_p_z_m`  — FABS

**Assembly:** `FABS  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `fabs_z_p_z_z`  — FABS

**Assembly:** `FABS  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## FAC<cc> -- A64

### Encoding: `facgt_p_p_zz_`  — FACGT

**Assembly:** `FACGT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GT;
```

### Encoding: `facge_p_p_zz_`  — FACGE

**Assembly:** `FACGE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GE;
```

---

## FACGE -- A64

### Encoding: `FACGE_asisdsamefp16_only`  — FACGE

**Assembly:** `FACGE  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FACGE_asisdsame_only`  — FACGE

**Assembly:** `FACGE  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FACGE_asimdsamefp16_only`  — FACGE

**Assembly:** `FACGE  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FACGE_asimdsame_only`  — FACGE

**Assembly:** `FACGE  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FACGT -- A64

### Encoding: `FACGT_asisdsamefp16_only`  — FACGT

**Assembly:** `FACGT  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FACGT_asisdsame_only`  — FACGT

**Assembly:** `FACGT  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FACGT_asimdsamefp16_only`  — FACGT

**Assembly:** `FACGT  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FACGT_asimdsame_only`  — FACGT

**Assembly:** `FACGT  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FACLE -- A64 *(alias for facge_p_p_zz)*

### Encoding: `facle_p_p_zz__facge_p_p_zz_`  — FACGE

**Assembly:** `FACLE  <Pd>.<T>, <Pg>/Z, <Zm>.<T>, <Zn>.<T> / FACGE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. |  |
| `FACGE` | — |  |  |

---

## FACLT -- A64 *(alias for facge_p_p_zz)*

### Encoding: `faclt_p_p_zz__facgt_p_p_zz_`  — FACGT

**Assembly:** `FACLT  <Pd>.<T>, <Pg>/Z, <Zm>.<T>, <Zn>.<T> / FACGT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. |  |
| `FACGT` | — |  |  |

---

## FADD (immediate) -- A64

### Encoding: `fadd_z_p_zs_`  — FADD

**Assembly:** `FADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let imm : bits(esize) = if i1 == '0' then FPPointFive{esize}('0') else FPOne{esize}('0');
```

---

## FADD (scalar) -- A64

### Encoding: `FADD_H_floatdp2`  [Half-precision]  — FADD

**Assembly:** `FADD  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FADD_S_floatdp2`  [Single-precision]  — FADD

**Assembly:** `FADD  <Sd>, <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FADD_D_floatdp2`  [Double-precision]  — FADD

**Assembly:** `FADD  <Dd>, <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FADD (vector) -- A64

### Encoding: `FADD_asimdsamefp16_only`  — FADD

**Assembly:** `FADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FADD_asimdsame_only`  — FADD

**Assembly:** `FADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FADD (vectors, predicated) -- A64

### Encoding: `fadd_z_p_zz_`  — FADD

**Assembly:** `FADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FADD (vectors, unpredicated) -- A64

### Encoding: `fadd_z_zz_`  — FADD

**Assembly:** `FADD  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## FADD -- A64

### Encoding: `fadd_za_zw_2x2`  — FADD

**Assembly:** `FADD  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" times 2 plus 1. | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fadd_za_zw_2x2_16`  — FADD

**Assembly:** `FADD  ZA.H[<Wv>, <offs>{, VGx2}], { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" times 2 plus 1. | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) && !IsFeatureImplemented(FEAT_SME_F8F16) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fadd_za_zw_4x4`  — FADD

**Assembly:** `FADD  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zm" times 4 plus 3. | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

### Encoding: `fadd_za_zw_4x4_16`  — FADD

**Assembly:** `FADD  ZA.H[<Wv>, <offs>{, VGx4}], { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zm" times 4 plus 3. | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) && !IsFeatureImplemented(FEAT_SME_F8F16) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FADDA -- A64

### Encoding: `fadda_v_p_z_`  — FADDA

**Assembly:** `FADDA  <V><dn>, <Pg>, <V><dn>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<dn>` | Vdn (5b) | Is the number [0-31] of the source and destination SIMD&FP register, encoded in the "Vdn" field. | let dn : integer = UInt(Vdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Vdn);
let m : integer = UInt(Zm);
```

---

## FADDP (scalar) -- A64

### Encoding: `FADDP_asisdpair_only_H`  — FADDP

**Assembly:** `FADDP  H<d>, <Vn>.2H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 32;
```

### Encoding: `FADDP_asisdpair_only_SD`  — FADDP

**Assembly:** `FADDP  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize * 2;
```

---

## FADDP (vector) -- A64

### Encoding: `FADDP_asimdsamefp16_only`  — FADDP

**Assembly:** `FADDP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FADDP_asimdsame_only`  — FADDP

**Assembly:** `FADDP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FADDP -- A64

### Encoding: `faddp_z_p_zz_`  — FADDP

**Assembly:** `FADDP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## FADDQV -- A64

### Encoding: `faddqv_z_p_z_`  — FADDQV

**Assembly:** `FADDQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FADDV -- A64

### Encoding: `faddv_v_p_z_`  — FADDV

**Assembly:** `FADDV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FAMAX -- A64

### Encoding: `FAMAX_asimdsamefp16_only`  — FAMAX

**Assembly:** `FAMAX  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FAMINMAX) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = if Q == '1' then 128 else 64;
let elements : integer = datasize DIV esize;
```

### Encoding: `FAMAX_asimdsame_only`  — FAMAX

**Assembly:** `FAMAX  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FAMINMAX) then
    EndOfDecode(Decode_UNDEF);
end;
if Q == '0' && size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = if Q == '1' then 128 else 64;
let elements : integer = datasize DIV esize;
```

---

## FAMAX -- A64

### Encoding: `famax_mz_zzw_2x2`  — FAMAX

**Assembly:** `FAMAX  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FAMINMAX) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `famax_mz_zzw_4x4`  — FAMAX

**Assembly:** `FAMAX  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FAMINMAX) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## FAMAX -- A64

### Encoding: `famax_z_p_zz_`  — FAMAX

**Assembly:** `FAMAX  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FAMINMAX)) then EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FAMIN -- A64

### Encoding: `FAMIN_asimdsamefp16_only`  — FAMIN

**Assembly:** `FAMIN  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FAMINMAX) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = if Q == '1' then 128 else 64;
let elements : integer = datasize DIV esize;
```

### Encoding: `FAMIN_asimdsame_only`  — FAMIN

**Assembly:** `FAMIN  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FAMINMAX) then
    EndOfDecode(Decode_UNDEF);
end;
if Q == '0' && size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = if Q == '1' then 128 else 64;
let elements : integer = datasize DIV esize;
```

---

## FAMIN -- A64

### Encoding: `famin_mz_zzw_2x2`  — FAMIN

**Assembly:** `FAMIN  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FAMINMAX) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `famin_mz_zzw_4x4`  — FAMIN

**Assembly:** `FAMIN  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FAMINMAX) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## FAMIN -- A64

### Encoding: `famin_z_p_zz_`  — FAMIN

**Assembly:** `FAMIN  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FAMINMAX)) then EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FCADD -- A64

### Encoding: `FCADD_asimdsame2_C`  — FCADD

**Assembly:** `FCADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>, #<rotate>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<rotate>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FCMA) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
if size == '01' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if Q == '0' && size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCADD -- A64

### Encoding: `fcadd_z_p_zz_`  — FCADD

**Assembly:** `FCADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let sub_i : boolean = (rot == '0');
let sub_r : boolean = (rot == '1');
```

---

## FCCMP -- A64

### Encoding: `FCCMP_H_floatccmp`  [Half-precision]  — FCCMP

**Assembly:** `FCCMP  <Hn>, <Hm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 8 << UInt(ftype XOR '10');
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

### Encoding: `FCCMP_S_floatccmp`  [Single-precision]  — FCCMP

**Assembly:** `FCCMP  <Sn>, <Sm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 8 << UInt(ftype XOR '10');
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

### Encoding: `FCCMP_D_floatccmp`  [Double-precision]  — FCCMP

**Assembly:** `FCCMP  <Dn>, <Dm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 8 << UInt(ftype XOR '10');
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

---

## FCCMPE -- A64

### Encoding: `FCCMPE_H_floatccmp`  [Half-precision]  — FCCMPE

**Assembly:** `FCCMPE  <Hn>, <Hm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 8 << UInt(ftype XOR '10');
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

### Encoding: `FCCMPE_S_floatccmp`  [Single-precision]  — FCCMPE

**Assembly:** `FCCMPE  <Sn>, <Sm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 8 << UInt(ftype XOR '10');
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

### Encoding: `FCCMPE_D_floatccmp`  [Double-precision]  — FCCMPE

**Assembly:** `FCCMPE  <Dn>, <Dm>, #<nzcv>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<nzcv>` | nzcv (4b) | Is the flag bit specifier, an immediate in the range 0 to 15, giving the alternative state for the 4-bit NZCV condition  | var flags : bits(4) = nzcv; |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 8 << UInt(ftype XOR '10');
let condition : bits(4) = cond;
var flags : bits(4) = nzcv;
```

---

## FCLAMP -- A64

### Encoding: `fclamp_mz_zz_2`  — FCLAMP

**Assembly:** `FCLAMP  { <Zd1>.<T>-<Zd2>.<T> }, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
```

### Encoding: `fclamp_mz_zz_4`  — FCLAMP

**Assembly:** `FCLAMP  { <Zd1>.<T>-<Zd4>.<T> }, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
```

---

## FCLAMP -- A64

### Encoding: `fclamp_z_zz_`  — FCLAMP

**Assembly:** `FCLAMP  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## FCM<cc> (vectors) -- A64

### Encoding: `fcmeq_p_p_zz_`  — FCMEQ

**Assembly:** `FCMEQ  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_EQ;
```

### Encoding: `fcmgt_p_p_zz_`  — FCMGT

**Assembly:** `FCMGT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GT;
```

### Encoding: `fcmge_p_p_zz_`  — FCMGE

**Assembly:** `FCMGE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_GE;
```

### Encoding: `fcmne_p_p_zz_`  — FCMNE

**Assembly:** `FCMNE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_NE;
```

### Encoding: `fcmuo_p_p_zz_`  — FCMUO

**Assembly:** `FCMUO  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Pd);
let cmp_op : CmpOp = Cmp_UN;
```

---

## FCM<cc> (zero) -- A64

### Encoding: `fcmeq_p_p_z0_`  — FCMEQ

**Assembly:** `FCMEQ  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let op : CmpOp = Cmp_EQ;
```

### Encoding: `fcmgt_p_p_z0_`  — FCMGT

**Assembly:** `FCMGT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let op : CmpOp = Cmp_GT;
```

### Encoding: `fcmge_p_p_z0_`  — FCMGE

**Assembly:** `FCMGE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let op : CmpOp = Cmp_GE;
```

### Encoding: `fcmlt_p_p_z0_`  — FCMLT

**Assembly:** `FCMLT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let op : CmpOp = Cmp_LT;
```

### Encoding: `fcmle_p_p_z0_`  — FCMLE

**Assembly:** `FCMLE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let op : CmpOp = Cmp_LE;
```

### Encoding: `fcmne_p_p_z0_`  — FCMNE

**Assembly:** `FCMNE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let op : CmpOp = Cmp_NE;
```

---

## FCMEQ (register) -- A64

### Encoding: `FCMEQ_asisdsamefp16_only`  — FCMEQ

**Assembly:** `FCMEQ  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMEQ_asisdsame_only`  — FCMEQ

**Assembly:** `FCMEQ  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMEQ_asimdsamefp16_only`  — FCMEQ

**Assembly:** `FCMEQ  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FCMEQ_asimdsame_only`  — FCMEQ

**Assembly:** `FCMEQ  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMEQ (zero) -- A64

### Encoding: `FCMEQ_asisdmiscfp16_FZ`  — FCMEQ

**Assembly:** `FCMEQ  <Hd>, <Hn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMEQ_asisdmisc_FZ`  — FCMEQ

**Assembly:** `FCMEQ  <V><d>, <V><n>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMEQ_asimdmiscfp16_FZ`  — FCMEQ

**Assembly:** `FCMEQ  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FCMEQ_asimdmisc_FZ`  — FCMEQ

**Assembly:** `FCMEQ  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMGE (register) -- A64

### Encoding: `FCMGE_asisdsamefp16_only`  — FCMGE

**Assembly:** `FCMGE  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMGE_asisdsame_only`  — FCMGE

**Assembly:** `FCMGE  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMGE_asimdsamefp16_only`  — FCMGE

**Assembly:** `FCMGE  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FCMGE_asimdsame_only`  — FCMGE

**Assembly:** `FCMGE  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMGE (zero) -- A64

### Encoding: `FCMGE_asisdmiscfp16_FZ`  — FCMGE

**Assembly:** `FCMGE  <Hd>, <Hn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMGE_asisdmisc_FZ`  — FCMGE

**Assembly:** `FCMGE  <V><d>, <V><n>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMGE_asimdmiscfp16_FZ`  — FCMGE

**Assembly:** `FCMGE  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FCMGE_asimdmisc_FZ`  — FCMGE

**Assembly:** `FCMGE  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMGT (register) -- A64

### Encoding: `FCMGT_asisdsamefp16_only`  — FCMGT

**Assembly:** `FCMGT  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMGT_asisdsame_only`  — FCMGT

**Assembly:** `FCMGT  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMGT_asimdsamefp16_only`  — FCMGT

**Assembly:** `FCMGT  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FCMGT_asimdsame_only`  — FCMGT

**Assembly:** `FCMGT  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMGT (zero) -- A64

### Encoding: `FCMGT_asisdmiscfp16_FZ`  — FCMGT

**Assembly:** `FCMGT  <Hd>, <Hn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMGT_asisdmisc_FZ`  — FCMGT

**Assembly:** `FCMGT  <V><d>, <V><n>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMGT_asimdmiscfp16_FZ`  — FCMGT

**Assembly:** `FCMGT  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FCMGT_asimdmisc_FZ`  — FCMGT

**Assembly:** `FCMGT  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMLA (by element) -- A64

### Encoding: `FCMLA_advsimd_elt`  — FCMLA

**Assembly:** `FCMLA  <Vd>.<T>, <Vn>.<T>, <Vm>.<Ts>[<index>], #<rotate>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | (M :: Rm) | Is the name of the second SIMD&FP source register, encoded in the "M:Rm" fields. |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |
| `<rotate>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FCMA) then EndOfDecode(Decode_UNDEF); end;
if size == '00' || size == '11' then EndOfDecode(Decode_UNDEF); end;
if !IsFeatureImplemented(FEAT_FP16) && size == '10' then EndOfDecode(Decode_UNDEF); end;
if size == '10' && (L == '1' || Q == '0') then EndOfDecode(Decode_UNDEF); end;
if size == '01' && H == '1' && Q == '0' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(M::Rm);
var index : integer;
if size == '01' then index = UInt(H::L); end;
if size == '10' then index = UInt(H); end;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMLA (indexed) -- A64

### Encoding: `fcmla_z_zzzi_h`  — FCMLA

**Assembly:** `FCMLA  <Zda>.H, <Zn>.H, <Zm>.H[<imm>], <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "Half-precision" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" f | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "Half-precision" variant: is the index of a Real and Imaginary pair, in the range 0 to 3, encoded in the "i2" fi | let index : integer = UInt(i2); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let neg_i : boolean = (rot[1] == '1');
let neg_r : boolean = (rot[0] != rot[1]);
```

### Encoding: `fcmla_z_zzzi_s`  — FCMLA

**Assembly:** `FCMLA  <Zda>.S, <Zn>.S, <Zm>.S[<imm>], <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "Single-precision" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "Single-precision" variant: is the index of a Real and Imaginary pair, in the range 0 to 1, encoded in the "i1"  | let index : integer = UInt(i1); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let neg_i : boolean = (rot[1] == '1');
let neg_r : boolean = (rot[0] != rot[1]);
```

---

## FCMLA (vectors) -- A64

### Encoding: `fcmla_z_p_zzz_`  — FCMLA

**Assembly:** `FCMLA  <Zda>.<T>, <Pg>/M, <Zn>.<T>, <Zm>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let neg_i : boolean = (rot[1] == '1');
let neg_r : boolean = (rot[0] != rot[1]);
```

---

## FCMLA -- A64

### Encoding: `FCMLA_asimdsame2_C`  — FCMLA

**Assembly:** `FCMLA  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>, #<rotate>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<rotate>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FCMA) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
if !IsFeatureImplemented(FEAT_FP16) && size == '01' then EndOfDecode(Decode_UNDEF); end;
if Q == '0' && size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMLE (vectors) -- A64 *(alias for fcmeq_p_p_zz)*

### Encoding: `fcmle_p_p_zz__fcmge_p_p_zz_`  — FCMGE

**Assembly:** `FCMLE  <Pd>.<T>, <Pg>/Z, <Zm>.<T>, <Zn>.<T> / FCMGE  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. |  |
| `FCMGE` | — |  |  |

---

## FCMLE (zero) -- A64

### Encoding: `FCMLE_asisdmiscfp16_FZ`  — FCMLE

**Assembly:** `FCMLE  <Hd>, <Hn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMLE_asisdmisc_FZ`  — FCMLE

**Assembly:** `FCMLE  <V><d>, <V><n>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMLE_asimdmiscfp16_FZ`  — FCMLE

**Assembly:** `FCMLE  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FCMLE_asimdmisc_FZ`  — FCMLE

**Assembly:** `FCMLE  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMLT (vectors) -- A64 *(alias for fcmeq_p_p_zz)*

### Encoding: `fcmlt_p_p_zz__fcmgt_p_p_zz_`  — FCMGT

**Assembly:** `FCMLT  <Pd>.<T>, <Pg>/Z, <Zm>.<T>, <Zn>.<T> / FCMGT  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. |  |
| `FCMGT` | — |  |  |

---

## FCMLT (zero) -- A64

### Encoding: `FCMLT_asisdmiscfp16_FZ`  — FCMLT

**Assembly:** `FCMLT  <Hd>, <Hn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMLT_asisdmisc_FZ`  — FCMLT

**Assembly:** `FCMLT  <V><d>, <V><n>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FCMLT_asimdmiscfp16_FZ`  — FCMLT

**Assembly:** `FCMLT  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FCMLT_asimdmisc_FZ`  — FCMLT

**Assembly:** `FCMLT  <Vd>.<T>, <Vn>.<T>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCMP -- A64

### Encoding: `FCMP_H_floatcmp`  [Half-precision]  — FCMP

**Assembly:** `FCMP  <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hn>` | Rn (5b) | For the "Half-precision" variant: is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt(Rm);   // ignored when opc<0> == '1' |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = FALSE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMP_HZ_floatcmp`  [Half-precision, zero]  — FCMP

**Assembly:** `FCMP  <Hn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hn>` | Rn (5b) | For the "Half-precision, zero" variant: is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = FALSE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMP_S_floatcmp`  [Single-precision]  — FCMP

**Assembly:** `FCMP  <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sn>` | Rn (5b) | For the "Single-precision" variant: is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt(Rm);   // ignored when opc<0> == '1' |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = FALSE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMP_SZ_floatcmp`  [Single-precision, zero]  — FCMP

**Assembly:** `FCMP  <Sn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sn>` | Rn (5b) | For the "Single-precision, zero" variant: is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = FALSE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMP_D_floatcmp`  [Double-precision]  — FCMP

**Assembly:** `FCMP  <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dn>` | Rn (5b) | For the "Double-precision" variant: is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt(Rm);   // ignored when opc<0> == '1' |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = FALSE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMP_DZ_floatcmp`  [Double-precision, zero]  — FCMP

**Assembly:** `FCMP  <Dn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dn>` | Rn (5b) | For the "Double-precision, zero" variant: is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = FALSE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

---

## FCMPE -- A64

### Encoding: `FCMPE_H_floatcmp`  [Half-precision]  — FCMPE

**Assembly:** `FCMPE  <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hn>` | Rn (5b) | For the "Half-precision" variant: is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt(Rm);   // ignored when opc<0> == '1' |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = TRUE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMPE_HZ_floatcmp`  [Half-precision, zero]  — FCMPE

**Assembly:** `FCMPE  <Hn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hn>` | Rn (5b) | For the "Half-precision, zero" variant: is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = TRUE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMPE_S_floatcmp`  [Single-precision]  — FCMPE

**Assembly:** `FCMPE  <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sn>` | Rn (5b) | For the "Single-precision" variant: is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt(Rm);   // ignored when opc<0> == '1' |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = TRUE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMPE_SZ_floatcmp`  [Single-precision, zero]  — FCMPE

**Assembly:** `FCMPE  <Sn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sn>` | Rn (5b) | For the "Single-precision, zero" variant: is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = TRUE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMPE_D_floatcmp`  [Double-precision]  — FCMPE

**Assembly:** `FCMPE  <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dn>` | Rn (5b) | For the "Double-precision" variant: is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt(Rm);   // ignored when opc<0> == '1' |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = TRUE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

### Encoding: `FCMPE_DZ_floatcmp`  [Double-precision, zero]  — FCMPE

**Assembly:** `FCMPE  <Dn>, #0.0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dn>` | Rn (5b) | For the "Double-precision, zero" variant: is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let n : integer = UInt(Rn);
let m : integer = UInt(Rm);   // ignored when opc<0> == '1'

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let signal_all_nans : boolean = TRUE;
let cmp_with_zero : boolean = (opc[0] == '1');
```

---

## FCPY -- A64

### Encoding: `fcpy_z_p_i_`  — FCPY

**Assembly:** `FCPY  <Zd>.<T>, <Pg>/M, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | imm8 (8b) | Is a floating-point immediate value expressible as ±n÷16×2^r, where n and r are integers such that 16 ≤ n ≤ 31 and -3 ≤  | let imm : bits(esize) = VFPExpandImm{}(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let d : integer = UInt(Zd);
let imm : bits(esize) = VFPExpandImm{}(imm8);
```

---

## FCSEL -- A64

### Encoding: `FCSEL_H_floatsel`  [Half-precision]  — FCSEL

**Assembly:** `FCSEL  <Hd>, <Hn>, <Hm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let condition : bits(4) = cond;
```

### Encoding: `FCSEL_S_floatsel`  [Single-precision]  — FCSEL

**Assembly:** `FCSEL  <Sd>, <Sn>, <Sm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let condition : bits(4) = cond;
```

### Encoding: `FCSEL_D_floatsel`  [Double-precision]  — FCSEL

**Assembly:** `FCSEL  <Dd>, <Dn>, <Dm>, <cond>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<cond>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let condition : bits(4) = cond;
```

---

## FCVT (narrowing, FP16 to FP8) -- A64

### Encoding: `fcvt_z8_mz2_`  — FCVT

**Assembly:** `FCVT  <Zd>.B, { <Zn1>.H-<Zn2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## FCVT (narrowing, FP32 to FP16) -- A64

### Encoding: `fcvt_z_mz2_`  — FCVT

**Assembly:** `FCVT  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## FCVT (narrowing, FP32 to FP8) -- A64

### Encoding: `fcvt_z8_mz4_`  — FCVT

**Assembly:** `FCVT  <Zd>.B, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
```

---

## FCVT (widening) -- A64

### Encoding: `fcvt_mz2_z_`  — FCVT

**Assembly:** `FCVT  { <Zd1>.S-<Zd2>.S }, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd::'0');
```

---

## FCVT -- A64

### Encoding: `FCVT_SH_floatdp1`  [Half-precision to single-precision]  — FCVT

**Assembly:** `FCVT  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == opc || ftype == '10' || opc == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let srcsize : integer{} = 8 << UInt(ftype XOR '10');
let dstsize : integer{} = 8 << UInt(opc XOR '10');
```

### Encoding: `FCVT_DH_floatdp1`  [Half-precision to double-precision]  — FCVT

**Assembly:** `FCVT  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == opc || ftype == '10' || opc == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let srcsize : integer{} = 8 << UInt(ftype XOR '10');
let dstsize : integer{} = 8 << UInt(opc XOR '10');
```

### Encoding: `FCVT_HS_floatdp1`  [Single-precision to half-precision]  — FCVT

**Assembly:** `FCVT  <Hd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == opc || ftype == '10' || opc == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let srcsize : integer{} = 8 << UInt(ftype XOR '10');
let dstsize : integer{} = 8 << UInt(opc XOR '10');
```

### Encoding: `FCVT_DS_floatdp1`  [Single-precision to double-precision]  — FCVT

**Assembly:** `FCVT  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == opc || ftype == '10' || opc == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let srcsize : integer{} = 8 << UInt(ftype XOR '10');
let dstsize : integer{} = 8 << UInt(opc XOR '10');
```

### Encoding: `FCVT_HD_floatdp1`  [Double-precision to half-precision]  — FCVT

**Assembly:** `FCVT  <Hd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == opc || ftype == '10' || opc == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let srcsize : integer{} = 8 << UInt(ftype XOR '10');
let dstsize : integer{} = 8 << UInt(opc XOR '10');
```

### Encoding: `FCVT_SD_floatdp1`  [Double-precision to single-precision]  — FCVT

**Assembly:** `FCVT  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == opc || ftype == '10' || opc == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let srcsize : integer{} = 8 << UInt(ftype XOR '10');
let dstsize : integer{} = 8 << UInt(opc XOR '10');
```

---

## FCVT -- A64

### Encoding: `fcvt_z_p_z_h2s`  — FCVT

**Assembly:** `FCVT  <Zd>.S, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 32;
let merging : boolean = TRUE;
```

### Encoding: `fcvt_z_p_z_h2sz`  — FCVT

**Assembly:** `FCVT  <Zd>.S, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 32;
let merging : boolean = FALSE;
```

### Encoding: `fcvt_z_p_z_h2d`  — FCVT

**Assembly:** `FCVT  <Zd>.D, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 64;
let merging : boolean = TRUE;
```

### Encoding: `fcvt_z_p_z_h2dz`  — FCVT

**Assembly:** `FCVT  <Zd>.D, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 64;
let merging : boolean = FALSE;
```

### Encoding: `fcvt_z_p_z_s2h`  — FCVT

**Assembly:** `FCVT  <Zd>.H, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 16;
let merging : boolean = TRUE;
```

### Encoding: `fcvt_z_p_z_s2hz`  — FCVT

**Assembly:** `FCVT  <Zd>.H, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 16;
let merging : boolean = FALSE;
```

### Encoding: `fcvt_z_p_z_s2d`  — FCVT

**Assembly:** `FCVT  <Zd>.D, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let merging : boolean = TRUE;
```

### Encoding: `fcvt_z_p_z_s2dz`  — FCVT

**Assembly:** `FCVT  <Zd>.D, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let merging : boolean = FALSE;
```

### Encoding: `fcvt_z_p_z_d2h`  — FCVT

**Assembly:** `FCVT  <Zd>.H, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 16;
let merging : boolean = TRUE;
```

### Encoding: `fcvt_z_p_z_d2hz`  — FCVT

**Assembly:** `FCVT  <Zd>.H, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 16;
let merging : boolean = FALSE;
```

### Encoding: `fcvt_z_p_z_d2s`  — FCVT

**Assembly:** `FCVT  <Zd>.S, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let merging : boolean = TRUE;
```

### Encoding: `fcvt_z_p_z_d2sz`  — FCVT

**Assembly:** `FCVT  <Zd>.S, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let merging : boolean = FALSE;
```

---

## FCVTAS (scalar SIMD&FP) -- A64

### Encoding: `FCVTAS_sisd_32H`  [Half-precision to 32-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTAS_sisd_64H`  [Half-precision to 64-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTAS_sisd_64S`  [Single-precision to 64-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTAS_sisd_32D`  [Double-precision to 32-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTAS (scalar) -- A64

### Encoding: `FCVTAS_32H_float2int`  [Half-precision to 32-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTAS_64H_float2int`  [Half-precision to 64-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTAS_32S_float2int`  [Single-precision to 32-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTAS_64S_float2int`  [Single-precision to 64-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTAS_32D_float2int`  [Double-precision to 32-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTAS_64D_float2int`  [Double-precision to 64-bit]  — FCVTAS

**Assembly:** `FCVTAS  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = FALSE;
```

---

## FCVTAS (vector) -- A64

### Encoding: `FCVTAS_asisdmiscfp16_R`  — FCVTAS

**Assembly:** `FCVTAS  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTAS_asisdmisc_R`  — FCVTAS

**Assembly:** `FCVTAS  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTAS_asimdmiscfp16_R`  — FCVTAS

**Assembly:** `FCVTAS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTAS_asimdmisc_R`  — FCVTAS

**Assembly:** `FCVTAS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean = FALSE;
```

---

## FCVTAU (scalar SIMD&FP) -- A64

### Encoding: `FCVTAU_sisd_32H`  [Half-precision to 32-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTAU_sisd_64H`  [Half-precision to 64-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTAU_sisd_64S`  [Single-precision to 64-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTAU_sisd_32D`  [Double-precision to 32-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTAU (scalar) -- A64

### Encoding: `FCVTAU_32H_float2int`  [Half-precision to 32-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTAU_64H_float2int`  [Half-precision to 64-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTAU_32S_float2int`  [Single-precision to 32-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTAU_64S_float2int`  [Single-precision to 64-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTAU_32D_float2int`  [Double-precision to 32-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTAU_64D_float2int`  [Double-precision to 64-bit]  — FCVTAU

**Assembly:** `FCVTAU  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean    = TRUE;
```

---

## FCVTAU (vector) -- A64

### Encoding: `FCVTAU_asisdmiscfp16_R`  — FCVTAU

**Assembly:** `FCVTAU  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTAU_asisdmisc_R`  — FCVTAU

**Assembly:** `FCVTAU  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTAU_asimdmiscfp16_R`  — FCVTAU

**Assembly:** `FCVTAU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTAU_asimdmisc_R`  — FCVTAU

**Assembly:** `FCVTAU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPRounding_TIEAWAY;
let unsigned : boolean = TRUE;
```

---

## FCVTL -- A64

### Encoding: `fcvtl_mz2_z_`  — FCVTL

**Assembly:** `FCVTL  { <Zd1>.S-<Zd2>.S }, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd::'0');
```

---

## FCVTL, FCVTL2 -- A64

### Encoding: `FCVTL_asimdmisc_L`  — FCVTL

**Assembly:** `FCVTL{2}  <Vd>.<Ta>, <Vn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 16 << UInt(sz);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCVTLT -- A64

### Encoding: `fcvtlt_z_p_z_h2s`  — FCVTLT

**Assembly:** `FCVTLT  <Zd>.S, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `fcvtlt_z_p_z_h2sz`  — FCVTLT

**Assembly:** `FCVTLT  <Zd>.S, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

### Encoding: `fcvtlt_z_p_z_s2d`  — FCVTLT

**Assembly:** `FCVTLT  <Zd>.D, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `fcvtlt_z_p_z_s2dz`  — FCVTLT

**Assembly:** `FCVTLT  <Zd>.D, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## FCVTMS (scalar SIMD&FP) -- A64

### Encoding: `FCVTMS_sisd_32H`  [Half-precision to 32-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTMS_sisd_64H`  [Half-precision to 64-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTMS_sisd_64S`  [Single-precision to 64-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTMS_sisd_32D`  [Double-precision to 32-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTMS (scalar) -- A64

### Encoding: `FCVTMS_32H_float2int`  [Half-precision to 32-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTMS_64H_float2int`  [Half-precision to 64-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTMS_32S_float2int`  [Single-precision to 32-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTMS_64S_float2int`  [Single-precision to 64-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTMS_32D_float2int`  [Double-precision to 32-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTMS_64D_float2int`  [Double-precision to 64-bit]  — FCVTMS

**Assembly:** `FCVTMS  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = FALSE;
```

---

## FCVTMS (vector) -- A64

### Encoding: `FCVTMS_asisdmiscfp16_R`  — FCVTMS

**Assembly:** `FCVTMS  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTMS_asisdmisc_R`  — FCVTMS

**Assembly:** `FCVTMS  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTMS_asimdmiscfp16_R`  — FCVTMS

**Assembly:** `FCVTMS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTMS_asimdmisc_R`  — FCVTMS

**Assembly:** `FCVTMS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

---

## FCVTMU (scalar SIMD&FP) -- A64

### Encoding: `FCVTMU_sisd_32H`  [Half-precision to 32-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTMU_sisd_64H`  [Half-precision to 64-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTMU_sisd_64S`  [Single-precision to 64-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTMU_sisd_32D`  [Double-precision to 32-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTMU (scalar) -- A64

### Encoding: `FCVTMU_32H_float2int`  [Half-precision to 32-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTMU_64H_float2int`  [Half-precision to 64-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTMU_32S_float2int`  [Single-precision to 32-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTMU_64S_float2int`  [Single-precision to 64-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTMU_32D_float2int`  [Double-precision to 32-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTMU_64D_float2int`  [Double-precision to 64-bit]  — FCVTMU

**Assembly:** `FCVTMU  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_NEGINF;
let unsigned : boolean    = TRUE;
```

---

## FCVTMU (vector) -- A64

### Encoding: `FCVTMU_asisdmiscfp16_R`  — FCVTMU

**Assembly:** `FCVTMU  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTMU_asisdmisc_R`  — FCVTMU

**Assembly:** `FCVTMU  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTMU_asimdmiscfp16_R`  — FCVTMU

**Assembly:** `FCVTMU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTMU_asimdmisc_R`  — FCVTMU

**Assembly:** `FCVTMU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

---

## FCVTN (FP32 to FP16) -- A64

### Encoding: `fcvtn_z_mz2_`  — FCVTN

**Assembly:** `FCVTN  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## FCVTN (FP32 to FP8) -- A64

### Encoding: `fcvtn_z8_mz4_`  — FCVTN

**Assembly:** `FCVTN  <Zd>.B, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
```

---

## FCVTN (half-precision to 8-bit floating-point) -- A64

### Encoding: `FCVTN_asimdsame2_D`  — FCVTN

**Assembly:** `FCVTN  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = if Q == '1' then 128 else 64;
let elements : integer = datasize DIV 16;
```

---

## FCVTN -- A64

### Encoding: `fcvtn_z8_mz2_h2b`  — FCVTN

**Assembly:** `FCVTN  <Zd>.B, { <Zn1>.H-<Zn2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## FCVTN, FCVTN2 (double to single-precision, single to half-precision) -- A64

### Encoding: `FCVTN_asimdmisc_N`  — FCVTN

**Assembly:** `FCVTN{2}  <Vd>.<Tb>, <Vn>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Ta>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 16 << UInt(sz);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FCVTN, FCVTN2 (single-precision to 8-bit floating-point) -- A64

### Encoding: `FCVTN_asimdsame2_H`  — FCVTN

**Assembly:** `FCVTN{2}  <Vd>.<Ta>, <Vn>.4S, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let part : integer = UInt(Q);
let elements : integer = 128 DIV 32;
```

---

## FCVTNB -- A64

### Encoding: `fcvtnb_z8_mz2_s2b`  — FCVTNB

**Assembly:** `FCVTNB  <Zd>.B, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## FCVTNS (scalar SIMD&FP) -- A64

### Encoding: `FCVTNS_sisd_32H`  [Half-precision to 32-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTNS_sisd_64H`  [Half-precision to 64-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTNS_sisd_64S`  [Single-precision to 64-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTNS_sisd_32D`  [Double-precision to 32-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTNS (scalar) -- A64

### Encoding: `FCVTNS_32H_float2int`  [Half-precision to 32-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTNS_64H_float2int`  [Half-precision to 64-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTNS_32S_float2int`  [Single-precision to 32-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTNS_64S_float2int`  [Single-precision to 64-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTNS_32D_float2int`  [Double-precision to 32-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTNS_64D_float2int`  [Double-precision to 64-bit]  — FCVTNS

**Assembly:** `FCVTNS  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = FALSE;
```

---

## FCVTNS (vector) -- A64

### Encoding: `FCVTNS_asisdmiscfp16_R`  — FCVTNS

**Assembly:** `FCVTNS  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTNS_asisdmisc_R`  — FCVTNS

**Assembly:** `FCVTNS  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTNS_asimdmiscfp16_R`  — FCVTNS

**Assembly:** `FCVTNS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTNS_asimdmisc_R`  — FCVTNS

**Assembly:** `FCVTNS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

---

## FCVTNT (predicated) -- A64

### Encoding: `fcvtnt_z_p_z_s2h`  — FCVTNT

**Assembly:** `FCVTNT  <Zd>.H, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `fcvtnt_z_p_z_s2hz`  — FCVTNT

**Assembly:** `FCVTNT  <Zd>.H, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

### Encoding: `fcvtnt_z_p_z_d2s`  — FCVTNT

**Assembly:** `FCVTNT  <Zd>.S, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `fcvtnt_z_p_z_d2sz`  — FCVTNT

**Assembly:** `FCVTNT  <Zd>.S, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## FCVTNT (unpredicated) -- A64

### Encoding: `fcvtnt_z8_mz2_s2b`  — FCVTNT

**Assembly:** `FCVTNT  <Zd>.B, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_FP8)) then EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## FCVTNU (scalar SIMD&FP) -- A64

### Encoding: `FCVTNU_sisd_32H`  [Half-precision to 32-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTNU_sisd_64H`  [Half-precision to 64-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTNU_sisd_64S`  [Single-precision to 64-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTNU_sisd_32D`  [Double-precision to 32-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTNU (scalar) -- A64

### Encoding: `FCVTNU_32H_float2int`  [Half-precision to 32-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTNU_64H_float2int`  [Half-precision to 64-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTNU_32S_float2int`  [Single-precision to 32-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTNU_64S_float2int`  [Single-precision to 64-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTNU_32D_float2int`  [Double-precision to 32-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTNU_64D_float2int`  [Double-precision to 64-bit]  — FCVTNU

**Assembly:** `FCVTNU  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_TIEEVEN;
let unsigned : boolean    = TRUE;
```

---

## FCVTNU (vector) -- A64

### Encoding: `FCVTNU_asisdmiscfp16_R`  — FCVTNU

**Assembly:** `FCVTNU  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTNU_asisdmisc_R`  — FCVTNU

**Assembly:** `FCVTNU  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTNU_asimdmiscfp16_R`  — FCVTNU

**Assembly:** `FCVTNU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTNU_asimdmisc_R`  — FCVTNU

**Assembly:** `FCVTNU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

---

## FCVTPS (scalar SIMD&FP) -- A64

### Encoding: `FCVTPS_sisd_32H`  [Half-precision to 32-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTPS_sisd_64H`  [Half-precision to 64-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTPS_sisd_64S`  [Single-precision to 64-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTPS_sisd_32D`  [Double-precision to 32-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTPS (scalar) -- A64

### Encoding: `FCVTPS_32H_float2int`  [Half-precision to 32-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTPS_64H_float2int`  [Half-precision to 64-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTPS_32S_float2int`  [Single-precision to 32-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTPS_64S_float2int`  [Single-precision to 64-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTPS_32D_float2int`  [Double-precision to 32-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTPS_64D_float2int`  [Double-precision to 64-bit]  — FCVTPS

**Assembly:** `FCVTPS  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = FALSE;
```

---

## FCVTPS (vector) -- A64

### Encoding: `FCVTPS_asisdmiscfp16_R`  — FCVTPS

**Assembly:** `FCVTPS  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTPS_asisdmisc_R`  — FCVTPS

**Assembly:** `FCVTPS  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTPS_asimdmiscfp16_R`  — FCVTPS

**Assembly:** `FCVTPS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTPS_asimdmisc_R`  — FCVTPS

**Assembly:** `FCVTPS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

---

## FCVTPU (scalar SIMD&FP) -- A64

### Encoding: `FCVTPU_sisd_32H`  [Half-precision to 32-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTPU_sisd_64H`  [Half-precision to 64-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTPU_sisd_64S`  [Single-precision to 64-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTPU_sisd_32D`  [Double-precision to 32-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTPU (scalar) -- A64

### Encoding: `FCVTPU_32H_float2int`  [Half-precision to 32-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTPU_64H_float2int`  [Half-precision to 64-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTPU_32S_float2int`  [Single-precision to 32-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTPU_64S_float2int`  [Single-precision to 64-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTPU_32D_float2int`  [Double-precision to 32-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTPU_64D_float2int`  [Double-precision to 64-bit]  — FCVTPU

**Assembly:** `FCVTPU  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_POSINF;
let unsigned : boolean    = TRUE;
```

---

## FCVTPU (vector) -- A64

### Encoding: `FCVTPU_asisdmiscfp16_R`  — FCVTPU

**Assembly:** `FCVTPU  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTPU_asisdmisc_R`  — FCVTPU

**Assembly:** `FCVTPU  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTPU_asimdmiscfp16_R`  — FCVTPU

**Assembly:** `FCVTPU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTPU_asimdmisc_R`  — FCVTPU

**Assembly:** `FCVTPU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

---

## FCVTX -- A64

### Encoding: `fcvtx_z_p_z_d2s`  — FCVTX

**Assembly:** `FCVTX  <Zd>.S, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let merging : boolean = TRUE;
```

### Encoding: `fcvtx_z_p_z_d2sz`  — FCVTX

**Assembly:** `FCVTX  <Zd>.S, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let merging : boolean = FALSE;
```

---

## FCVTXN, FCVTXN2 -- A64

### Encoding: `FCVTXN_asisdmisc_N`  — FCVTXN

**Assembly:** `FCVTXN  S<d>, D<n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32;
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;
```

### Encoding: `FCVTXN_asimdmisc_N`  — FCVTXN

**Assembly:** `FCVTXN{2}  <Vd>.<Tb>, <Vn>.2D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32;
let datasize : integer{} = 64;
let elements : integer = 2;
let part : integer = UInt(Q);
```

---

## FCVTXNT -- A64

### Encoding: `fcvtxnt_z_p_z_d2s`  — FCVTXNT

**Assembly:** `FCVTXNT  <Zd>.S, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `fcvtxnt_z_p_z_d2sz`  — FCVTXNT

**Assembly:** `FCVTXNT  <Zd>.S, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## FCVTZS (scalar SIMD&FP) -- A64

### Encoding: `FCVTZS_sisd_32H`  [Half-precision to 32-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTZS_sisd_64H`  [Half-precision to 64-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTZS_sisd_64S`  [Single-precision to 64-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTZS_sisd_32D`  [Double-precision to 32-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTZS (scalar, fixed-point) -- A64

### Encoding: `FCVTZS_32H_float2fix`  [Half-precision to 32-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Wd>, <Hn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 32-bit", "Half-precision to 32-bit", and "Single-precision to 32-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTZS_64H_float2fix`  [Half-precision to 64-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Xd>, <Hn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 64-bit", "Half-precision to 64-bit", and "Single-precision to 64-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTZS_32S_float2fix`  [Single-precision to 32-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Wd>, <Sn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 32-bit", "Half-precision to 32-bit", and "Single-precision to 32-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTZS_64S_float2fix`  [Single-precision to 64-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Xd>, <Sn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 64-bit", "Half-precision to 64-bit", and "Single-precision to 64-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTZS_32D_float2fix`  [Double-precision to 32-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Wd>, <Dn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 32-bit", "Half-precision to 32-bit", and "Single-precision to 32-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTZS_64D_float2fix`  [Double-precision to 64-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Xd>, <Dn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 64-bit", "Half-precision to 64-bit", and "Single-precision to 64-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = FALSE;
```

---

## FCVTZS (scalar, integer) -- A64

### Encoding: `FCVTZS_32H_float2int`  [Half-precision to 32-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTZS_64H_float2int`  [Half-precision to 64-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTZS_32S_float2int`  [Single-precision to 32-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTZS_64S_float2int`  [Single-precision to 64-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTZS_32D_float2int`  [Double-precision to 32-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = FALSE;
```

### Encoding: `FCVTZS_64D_float2int`  [Double-precision to 64-bit]  — FCVTZS

**Assembly:** `FCVTZS  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = FALSE;
```

---

## FCVTZS (vector, fixed-point) -- A64

### Encoding: `FCVTZS_asisdshf_C`  — FCVTZS

**Assembly:** `FCVTZS  <V><d>, <V><n>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh IN {'000x'} || (immh IN {'001x'} && !IsFeatureImplemented(FEAT_FP16)) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = if immh IN {'1xxx'} then 64 else if immh IN {'01xx'} then 32 else 16;
let datasize : integer{} = esize;
let elements : integer = 1;

let fracbits : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

### Encoding: `FCVTZS_asimdshf_C`  — FCVTZS

**Assembly:** `FCVTZS  <Vd>.<T>, <Vn>.<T>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh IN {'000x'} || (immh IN {'001x'} && !IsFeatureImplemented(FEAT_FP16)) then
    EndOfDecode(Decode_UNDEF);
end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = if immh IN {'1xxx'} then 64 else if immh IN {'01xx'} then 32 else 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let fracbits : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FCVTZS (vector, integer) -- A64

### Encoding: `FCVTZS_asisdmiscfp16_R`  — FCVTZS

**Assembly:** `FCVTZS  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTZS_asisdmisc_R`  — FCVTZS

**Assembly:** `FCVTZS  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTZS_asimdmiscfp16_R`  — FCVTZS

**Assembly:** `FCVTZS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

### Encoding: `FCVTZS_asimdmisc_R`  — FCVTZS

**Assembly:** `FCVTZS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = FALSE;
```

---

## FCVTZS -- A64

### Encoding: `fcvtzs_mz_z_2`  — FCVTZS

**Assembly:** `FCVTZS  { <Zd1>.S-<Zd2>.S }, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the source multi-vector group, enc | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

### Encoding: `fcvtzs_mz_z_4`  — FCVTZS

**Assembly:** `FCVTZS  { <Zd1>.S-<Zd4>.S }, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the source multi-vector group, en | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FCVTZS -- A64

### Encoding: `fcvtzs_z_p_z_fp162h`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.H, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 16;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzs_z_p_z_fp162hz`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.H, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 16;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzs_z_p_z_fp162w`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.S, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzs_z_p_z_fp162wz`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.S, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzs_z_p_z_fp162x`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.D, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzs_z_p_z_fp162xz`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.D, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzs_z_p_z_s2w`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.S, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzs_z_p_z_s2wz`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.S, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzs_z_p_z_s2x`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.D, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzs_z_p_z_s2xz`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.D, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzs_z_p_z_d2w`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.S, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzs_z_p_z_d2wz`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.S, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzs_z_p_z_d2x`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.D, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzs_z_p_z_d2xz`  — FCVTZS

**Assembly:** `FCVTZS  <Zd>.D, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

---

## FCVTZSN -- A64

### Encoding: `fcvtzsn_z_mz2_`  — FCVTZSN

**Assembly:** `FCVTZSN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FCVTZU (scalar SIMD&FP) -- A64

### Encoding: `FCVTZU_sisd_32H`  [Half-precision to 32-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Sd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTZU_sisd_64H`  [Half-precision to 64-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Dd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTZU_sisd_64S`  [Single-precision to 64-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FCVTZU_sisd_32D`  [Double-precision to 32-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FCVTZU (scalar, fixed-point) -- A64

### Encoding: `FCVTZU_32H_float2fix`  [Half-precision to 32-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Wd>, <Hn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 32-bit", "Half-precision to 32-bit", and "Single-precision to 32-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTZU_64H_float2fix`  [Half-precision to 64-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Xd>, <Hn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 64-bit", "Half-precision to 64-bit", and "Single-precision to 64-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTZU_32S_float2fix`  [Single-precision to 32-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Wd>, <Sn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 32-bit", "Half-precision to 32-bit", and "Single-precision to 32-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTZU_64S_float2fix`  [Single-precision to 64-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Xd>, <Sn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 64-bit", "Half-precision to 64-bit", and "Single-precision to 64-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTZU_32D_float2fix`  [Double-precision to 32-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Wd>, <Dn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 32-bit", "Half-precision to 32-bit", and "Single-precision to 32-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTZU_64D_float2fix`  [Double-precision to 64-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Xd>, <Dn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "Double-precision to 64-bit", "Half-precision to 64-bit", and "Single-precision to 64-bit" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean = TRUE;
```

---

## FCVTZU (scalar, integer) -- A64

### Encoding: `FCVTZU_32H_float2int`  [Half-precision to 32-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTZU_64H_float2int`  [Half-precision to 64-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTZU_32S_float2int`  [Single-precision to 32-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTZU_64S_float2int`  [Single-precision to 64-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Xd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTZU_32D_float2int`  [Double-precision to 32-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = TRUE;
```

### Encoding: `FCVTZU_64D_float2int`  [Double-precision to 64-bit]  — FCVTZU

**Assembly:** `FCVTZU  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');

let rounding : FPRounding = FPRounding_ZERO;
let unsigned : boolean    = TRUE;
```

---

## FCVTZU (vector, fixed-point) -- A64

### Encoding: `FCVTZU_asisdshf_C`  — FCVTZU

**Assembly:** `FCVTZU  <V><d>, <V><n>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh IN {'000x'} || (immh IN {'001x'} && !IsFeatureImplemented(FEAT_FP16)) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = if immh IN {'1xxx'} then 64 else if immh IN {'01xx'} then 32 else 16;
let datasize : integer{} = esize;
let elements : integer = 1;

let fracbits : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
```

### Encoding: `FCVTZU_asimdshf_C`  — FCVTZU

**Assembly:** `FCVTZU  <Vd>.<T>, <Vn>.<T>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh IN {'000x'} || (immh IN {'001x'} && !IsFeatureImplemented(FEAT_FP16)) then
    EndOfDecode(Decode_UNDEF);
end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = if immh IN {'1xxx'} then 64 else if immh IN {'01xx'} then 32 else 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let fracbits : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FCVTZU (vector, integer) -- A64

### Encoding: `FCVTZU_asisdmiscfp16_R`  — FCVTZU

**Assembly:** `FCVTZU  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTZU_asisdmisc_R`  — FCVTZU

**Assembly:** `FCVTZU  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTZU_asimdmiscfp16_R`  — FCVTZU

**Assembly:** `FCVTZU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

### Encoding: `FCVTZU_asimdmisc_R`  — FCVTZU

**Assembly:** `FCVTZU  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : FPRounding = FPDecodeRounding(o1::o2);
let unsigned : boolean = TRUE;
```

---

## FCVTZU -- A64

### Encoding: `fcvtzu_mz_z_2`  — FCVTZU

**Assembly:** `FCVTZU  { <Zd1>.S-<Zd2>.S }, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the source multi-vector group, enc | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
```

### Encoding: `fcvtzu_mz_z_4`  — FCVTZU

**Assembly:** `FCVTZU  { <Zd1>.S-<Zd4>.S }, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the source multi-vector group, en | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FCVTZU -- A64

### Encoding: `fcvtzu_z_p_z_fp162h`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.H, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 16;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzu_z_p_z_fp162hz`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.H, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 16;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzu_z_p_z_fp162w`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.S, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzu_z_p_z_fp162wz`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.S, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzu_z_p_z_fp162x`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.D, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzu_z_p_z_fp162xz`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.D, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzu_z_p_z_s2w`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.S, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzu_z_p_z_s2wz`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.S, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzu_z_p_z_s2x`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.D, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzu_z_p_z_s2xz`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.D, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzu_z_p_z_d2w`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.S, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzu_z_p_z_d2wz`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.S, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `fcvtzu_z_p_z_d2x`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.D, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `fcvtzu_z_p_z_d2xz`  — FCVTZU

**Assembly:** `FCVTZU  <Zd>.D, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

---

## FCVTZUN -- A64

### Encoding: `fcvtzun_z_mz2_`  — FCVTZUN

**Assembly:** `FCVTZUN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FDIV (scalar) -- A64

### Encoding: `FDIV_H_floatdp2`  [Half-precision]  — FDIV

**Assembly:** `FDIV  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FDIV_S_floatdp2`  [Single-precision]  — FDIV

**Assembly:** `FDIV  <Sd>, <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FDIV_D_floatdp2`  [Double-precision]  — FDIV

**Assembly:** `FDIV  <Dd>, <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FDIV (vector) -- A64

### Encoding: `FDIV_asimdsamefp16_only`  — FDIV

**Assembly:** `FDIV  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FDIV_asimdsame_only`  — FDIV

**Assembly:** `FDIV  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FDIV -- A64

### Encoding: `fdiv_z_p_zz_`  — FDIV

**Assembly:** `FDIV  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FDIVR -- A64

### Encoding: `fdivr_z_p_zz_`  — FDIVR

**Assembly:** `FDIVR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FDOT (2-way, indexed, FP16 to FP32) -- A64

### Encoding: `fdot_z_zzzi_`  — FDOT

**Assembly:** `FDOT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i2);
```

---

## FDOT (2-way, indexed, FP8 to FP16) -- A64

### Encoding: `fdot_z_zz8z8i_`  — FDOT

**Assembly:** `FDOT  <Zda>.H, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index of a pair of 8-bit elements within each 128-bit vector segment, in the range 0 to 7, encoded in t |  |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8DOT2() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i3h::i3l);
```

---

## FDOT (2-way, multiple and indexed vector, FP16 to FP32) -- A64

### Encoding: `fdot_za_zzi_2xi`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `fdot_za_zzi_4xi`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## FDOT (2-way, multiple and indexed vector, FP8 to FP16) -- A64

### Encoding: `fdot_za_z8z8i_2xi`  — FDOT

**Assembly:** `FDOT  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the immediate index of a pair of 8-bit elements within each 128-bit vector segment, in the range 0 to 7, encoded in t |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `fdot_za_z8z8i_4xi`  — FDOT

**Assembly:** `FDOT  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the immediate index of a pair of 8-bit elements within each 128-bit vector segment, in the range 0 to 7, encoded in t |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## FDOT (2-way, multiple and single vector, FP16 to FP32) -- A64

### Encoding: `fdot_za_zzv_2x1`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fdot_za_zzv_4x1`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FDOT (2-way, multiple and single vector, FP8 to FP16) -- A64

### Encoding: `fdot_za_z8z8v_2x1`  — FDOT

**Assembly:** `FDOT  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fdot_za_z8z8v_4x1`  — FDOT

**Assembly:** `FDOT  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FDOT (2-way, multiple vectors, FP16 to FP32) -- A64

### Encoding: `fdot_za_zzw_2x2`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fdot_za_zzw_4x4`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FDOT (2-way, multiple vectors, FP8 to FP16) -- A64

### Encoding: `fdot_za_z8z8w_2x2`  — FDOT

**Assembly:** `FDOT  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fdot_za_z8z8w_4x4`  — FDOT

**Assembly:** `FDOT  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, { <Zm1>.B-<Zm4>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FDOT (2-way, vectors, FP16 to FP32) -- A64

### Encoding: `fdot_z_zzz_`  — FDOT

**Assembly:** `FDOT  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FDOT (2-way, vectors, FP8 to FP16) -- A64

### Encoding: `fdot_z_zz8z8_`  — FDOT

**Assembly:** `FDOT  <Zda>.H, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8DOT2() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FDOT (4-way, indexed) -- A64

### Encoding: `fdot_z32_zz8z8i_`  — FDOT

**Assembly:** `FDOT  <Zda>.S, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8DOT4() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i2);
```

---

## FDOT (4-way, multiple and indexed vector) -- A64

### Encoding: `fdot_za32_z8z8i_2xi`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `fdot_za32_z8z8i_4xi`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## FDOT (4-way, multiple and single vector) -- A64

### Encoding: `fdot_za32_z8z8v_2x1`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fdot_za32_z8z8v_4x1`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FDOT (4-way, multiple vectors) -- A64

### Encoding: `fdot_za32_z8z8w_2x2`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fdot_za32_z8z8w_4x4`  — FDOT

**Assembly:** `FDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, { <Zm1>.B-<Zm4>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FDOT (4-way, vectors) -- A64

### Encoding: `fdot_z32_zz8z8_`  — FDOT

**Assembly:** `FDOT  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8DOT4() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FDOT (8-bit floating-point to half-precision, by element) -- A64

### Encoding: `FDOT_asimdelem_G`  — FDOT

**Assembly:** `FDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.2B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, in the range V0 to V15, encoded in the "Rm" field. | let m : integer = UInt('0'::Rm); |
| `<index>` | (H :: L :: M) | Is the immediate index of a pair of 8-bit elements, in the range 0 to 7, encoded in the "H:L:M" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8DOT2) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let d : integer = UInt(Rd);
let m : integer = UInt('0'::Rm);
let i : integer = UInt(H::L::M);

let datasize : integer{} = if Q == '1' then 128 else 64;
let esize : integer{} = 16;
let elements : integer = datasize DIV esize;
```

---

## FDOT (8-bit floating-point to half-precision, vector) -- A64

### Encoding: `FDOT_asimdsame2_D`  — FDOT

**Assembly:** `FDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8DOT2) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = if Q == '1' then 128 else 64;
let esize : integer{} = 16;
let elements : integer = datasize DIV esize;
```

---

## FDOT (8-bit floating-point to single-precision, by element) -- A64

### Encoding: `FDOT_asimdelem_D`  — FDOT

**Assembly:** `FDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.4B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | (M :: Rm) | Is the name of the second SIMD&FP source register, encoded in the "M:Rm" fields. |  |
| `<index>` | (H :: L) | Is the immediate index of a 32-bit group of four 8-bit values, in the range 0 to 3, encoded in the "H:L" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8DOT4) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let d : integer = UInt(Rd);
let m : integer = UInt(M::Rm);
let i : integer = UInt(H::L);

let datasize : integer{} = if Q == '1' then 128 else 64;
let esize : integer{} = 32;
let elements : integer = datasize DIV esize;
```

---

## FDOT (8-bit floating-point to single-precision, vector) -- A64

### Encoding: `FDOT_asimdsame2_DD`  — FDOT

**Assembly:** `FDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8DOT4) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = if Q == '1' then 128 else 64;
let esize : integer{} = 32;
let elements : integer = datasize DIV esize;
```

---

## FDOT (half-precision to single-precision, by element) -- A64

### Encoding: `FDOT_asimdelem_FP16FP32`  — FDOT

**Assembly:** `FDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.2H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | (M :: Rm) | Is the name of the second SIMD&FP source register, encoded in the "M:Rm" fields. |  |
| `<index>` | (H :: L) | Is the immediate index of a pair of 16-bit elements in the range 0 to 3, encoded in the "H:L" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F16F32DOT) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(M::Rm);
let d : integer = UInt(Rd);
let i : integer = UInt(H::L);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 32;
```

---

## FDOT (half-precision to single-precision, vector) -- A64

### Encoding: `FDOT_asimdsame2_FP16FP32`  — FDOT

**Assembly:** `FDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F16F32DOT) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 32;
```

---

## FDUP -- A64

### Encoding: `fdup_z_i_`  — FDUP

**Assembly:** `FDUP  <Zd>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<const>` | imm8 (8b) | Is a floating-point immediate value expressible as ±n÷16×2^r, where n and r are integers such that 16 ≤ n ≤ 31 and -3 ≤  | let imm : bits(esize) = VFPExpandImm{}(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt(Zd);
let imm : bits(esize) = VFPExpandImm{}(imm8);
```

---

## FEXPA -- A64

### Encoding: `fexpa_z_z_`  — FEXPA

**Assembly:** `FEXPA  <Zd>.<T>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SSVE_FEXPA) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## FIRSTP -- A64

### Encoding: `firstp_r_p_p_`  — FIRSTP

**Assembly:** `FIRSTP  <Xd>, <Pg>, <Pn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let d : integer = UInt(Rd);
```

---

## FJCVTZS -- A64

### Encoding: `FJCVTZS_32D_float2int`  — FJCVTZS

**Assembly:** `FJCVTZS  <Wd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_JSCVT) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## FLOGB -- A64

### Encoding: `flogb_z_p_z_m`  — FLOGB

**Assembly:** `FLOGB  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `flogb_z_p_z_z`  — FLOGB

**Assembly:** `FLOGB  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## FMAD -- A64

### Encoding: `fmad_z_p_zzz_`  — FMAD

**Assembly:** `FMAD  <Zdn>.<T>, <Pg>/M, <Zm>.<T>, <Za>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Za>` | Za (5b) | Is the name of the third source scalable vector register, encoded in the "Za" field. | let a : integer = UInt(Za); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let a : integer = UInt(Za);
let op1_neg : boolean = FALSE;
let op3_neg : boolean = FALSE;
```

---

## FMADD -- A64

### Encoding: `FMADD_H_floatdp3`  [Half-precision]  — FMADD

**Assembly:** `FMADD  <Hd>, <Hn>, <Hm>, <Ha>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Ha>` | Ra (5b) | Is the 16-bit name of the third SIMD&FP source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMADD_S_floatdp3`  [Single-precision]  — FMADD

**Assembly:** `FMADD  <Sd>, <Sn>, <Sm>, <Sa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Sa>` | Ra (5b) | Is the 32-bit name of the third SIMD&FP source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMADD_D_floatdp3`  [Double-precision]  — FMADD

**Assembly:** `FMADD  <Dd>, <Dn>, <Dm>, <Da>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Da>` | Ra (5b) | Is the 64-bit name of the third SIMD&FP source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FMAX (immediate) -- A64

### Encoding: `fmax_z_p_zs_`  — FMAX

**Assembly:** `FMAX  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let imm : bits(esize) = if i1 == '0' then Zeros{esize} else FPOne{esize}('0');
```

---

## FMAX (multiple and single vector) -- A64

### Encoding: `fmax_mz_zzv_2x1`  — FMAX

**Assembly:** `FMAX  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `fmax_mz_zzv_4x1`  — FMAX

**Assembly:** `FMAX  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## FMAX (multiple vectors) -- A64

### Encoding: `fmax_mz_zzw_2x2`  — FMAX

**Assembly:** `FMAX  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmax_mz_zzw_4x4`  — FMAX

**Assembly:** `FMAX  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## FMAX (scalar) -- A64

### Encoding: `FMAX_H_floatdp2`  [Half-precision]  — FMAX

**Assembly:** `FMAX  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMAX_S_floatdp2`  [Single-precision]  — FMAX

**Assembly:** `FMAX  <Sd>, <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMAX_D_floatdp2`  [Double-precision]  — FMAX

**Assembly:** `FMAX  <Dd>, <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FMAX (vector) -- A64

### Encoding: `FMAX_asimdsamefp16_only`  — FMAX

**Assembly:** `FMAX  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMAX_asimdsame_only`  — FMAX

**Assembly:** `FMAX  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMAX (vectors) -- A64

### Encoding: `fmax_z_p_zz_`  — FMAX

**Assembly:** `FMAX  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FMAXNM (immediate) -- A64

### Encoding: `fmaxnm_z_p_zs_`  — FMAXNM

**Assembly:** `FMAXNM  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let imm : bits(esize) = if i1 == '0' then Zeros{esize} else FPOne{esize}('0');
```

---

## FMAXNM (multiple and single vector) -- A64

### Encoding: `fmaxnm_mz_zzv_2x1`  — FMAXNM

**Assembly:** `FMAXNM  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `fmaxnm_mz_zzv_4x1`  — FMAXNM

**Assembly:** `FMAXNM  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## FMAXNM (multiple vectors) -- A64

### Encoding: `fmaxnm_mz_zzw_2x2`  — FMAXNM

**Assembly:** `FMAXNM  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmaxnm_mz_zzw_4x4`  — FMAXNM

**Assembly:** `FMAXNM  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## FMAXNM (scalar) -- A64

### Encoding: `FMAXNM_H_floatdp2`  [Half-precision]  — FMAXNM

**Assembly:** `FMAXNM  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMAXNM_S_floatdp2`  [Single-precision]  — FMAXNM

**Assembly:** `FMAXNM  <Sd>, <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMAXNM_D_floatdp2`  [Double-precision]  — FMAXNM

**Assembly:** `FMAXNM  <Dd>, <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FMAXNM (vector) -- A64

### Encoding: `FMAXNM_asimdsamefp16_only`  — FMAXNM

**Assembly:** `FMAXNM  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMAXNM_asimdsame_only`  — FMAXNM

**Assembly:** `FMAXNM  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMAXNM (vectors) -- A64

### Encoding: `fmaxnm_z_p_zz_`  — FMAXNM

**Assembly:** `FMAXNM  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FMAXNMP (scalar) -- A64

### Encoding: `FMAXNMP_asisdpair_only_H`  — FMAXNMP

**Assembly:** `FMAXNMP  H<d>, <Vn>.2H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 32;
```

### Encoding: `FMAXNMP_asisdpair_only_SD`  — FMAXNMP

**Assembly:** `FMAXNMP  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize * 2;
```

---

## FMAXNMP (vector) -- A64

### Encoding: `FMAXNMP_asimdsamefp16_only`  — FMAXNMP

**Assembly:** `FMAXNMP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMAXNMP_asimdsame_only`  — FMAXNMP

**Assembly:** `FMAXNMP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMAXNMP -- A64

### Encoding: `fmaxnmp_z_p_zz_`  — FMAXNMP

**Assembly:** `FMAXNMP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## FMAXNMQV -- A64

### Encoding: `fmaxnmqv_z_p_z_`  — FMAXNMQV

**Assembly:** `FMAXNMQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FMAXNMV -- A64

### Encoding: `FMAXNMV_asimdall_only_H`  — FMAXNMV

**Assembly:** `FMAXNMV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — | Is the destination width specifier, H. |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
```

### Encoding: `FMAXNMV_asimdall_only_SD`  — FMAXNMV

**Assembly:** `FMAXNMV  S<d>, <Vn>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q != '01' then EndOfDecode(Decode_UNDEF); end;    // .4S only

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
```

---

## FMAXNMV -- A64

### Encoding: `fmaxnmv_v_p_z_`  — FMAXNMV

**Assembly:** `FMAXNMV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FMAXP (scalar) -- A64

### Encoding: `FMAXP_asisdpair_only_H`  — FMAXP

**Assembly:** `FMAXP  H<d>, <Vn>.2H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 32;
```

### Encoding: `FMAXP_asisdpair_only_SD`  — FMAXP

**Assembly:** `FMAXP  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize * 2;
```

---

## FMAXP (vector) -- A64

### Encoding: `FMAXP_asimdsamefp16_only`  — FMAXP

**Assembly:** `FMAXP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMAXP_asimdsame_only`  — FMAXP

**Assembly:** `FMAXP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMAXP -- A64

### Encoding: `fmaxp_z_p_zz_`  — FMAXP

**Assembly:** `FMAXP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## FMAXQV -- A64

### Encoding: `fmaxqv_z_p_z_`  — FMAXQV

**Assembly:** `FMAXQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FMAXV -- A64

### Encoding: `FMAXV_asimdall_only_H`  — FMAXV

**Assembly:** `FMAXV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — | Is the destination width specifier, H. |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
```

### Encoding: `FMAXV_asimdall_only_SD`  — FMAXV

**Assembly:** `FMAXV  S<d>, <Vn>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q != '01' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
```

---

## FMAXV -- A64

### Encoding: `fmaxv_v_p_z_`  — FMAXV

**Assembly:** `FMAXV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FMIN (immediate) -- A64

### Encoding: `fmin_z_p_zs_`  — FMIN

**Assembly:** `FMIN  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let imm : bits(esize) = if i1 == '0' then Zeros{esize} else FPOne{esize}('0');
```

---

## FMIN (multiple and single vector) -- A64

### Encoding: `fmin_mz_zzv_2x1`  — FMIN

**Assembly:** `FMIN  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `fmin_mz_zzv_4x1`  — FMIN

**Assembly:** `FMIN  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## FMIN (multiple vectors) -- A64

### Encoding: `fmin_mz_zzw_2x2`  — FMIN

**Assembly:** `FMIN  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmin_mz_zzw_4x4`  — FMIN

**Assembly:** `FMIN  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## FMIN (scalar) -- A64

### Encoding: `FMIN_H_floatdp2`  [Half-precision]  — FMIN

**Assembly:** `FMIN  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMIN_S_floatdp2`  [Single-precision]  — FMIN

**Assembly:** `FMIN  <Sd>, <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMIN_D_floatdp2`  [Double-precision]  — FMIN

**Assembly:** `FMIN  <Dd>, <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FMIN (vector) -- A64

### Encoding: `FMIN_asimdsamefp16_only`  — FMIN

**Assembly:** `FMIN  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMIN_asimdsame_only`  — FMIN

**Assembly:** `FMIN  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMIN (vectors) -- A64

### Encoding: `fmin_z_p_zz_`  — FMIN

**Assembly:** `FMIN  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FMINNM (immediate) -- A64

### Encoding: `fminnm_z_p_zs_`  — FMINNM

**Assembly:** `FMINNM  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let imm : bits(esize) = if i1 == '0' then Zeros{esize} else FPOne{esize}('0');
```

---

## FMINNM (multiple and single vector) -- A64

### Encoding: `fminnm_mz_zzv_2x1`  — FMINNM

**Assembly:** `FMINNM  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `fminnm_mz_zzv_4x1`  — FMINNM

**Assembly:** `FMINNM  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## FMINNM (multiple vectors) -- A64

### Encoding: `fminnm_mz_zzw_2x2`  — FMINNM

**Assembly:** `FMINNM  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `fminnm_mz_zzw_4x4`  — FMINNM

**Assembly:** `FMINNM  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## FMINNM (scalar) -- A64

### Encoding: `FMINNM_H_floatdp2`  [Half-precision]  — FMINNM

**Assembly:** `FMINNM  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMINNM_S_floatdp2`  [Single-precision]  — FMINNM

**Assembly:** `FMINNM  <Sd>, <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMINNM_D_floatdp2`  [Double-precision]  — FMINNM

**Assembly:** `FMINNM  <Dd>, <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FMINNM (vector) -- A64

### Encoding: `FMINNM_asimdsamefp16_only`  — FMINNM

**Assembly:** `FMINNM  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMINNM_asimdsame_only`  — FMINNM

**Assembly:** `FMINNM  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMINNM (vectors) -- A64

### Encoding: `fminnm_z_p_zz_`  — FMINNM

**Assembly:** `FMINNM  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FMINNMP (scalar) -- A64

### Encoding: `FMINNMP_asisdpair_only_H`  — FMINNMP

**Assembly:** `FMINNMP  H<d>, <Vn>.2H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 32;
```

### Encoding: `FMINNMP_asisdpair_only_SD`  — FMINNMP

**Assembly:** `FMINNMP  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize * 2;
```

---

## FMINNMP (vector) -- A64

### Encoding: `FMINNMP_asimdsamefp16_only`  — FMINNMP

**Assembly:** `FMINNMP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMINNMP_asimdsame_only`  — FMINNMP

**Assembly:** `FMINNMP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMINNMP -- A64

### Encoding: `fminnmp_z_p_zz_`  — FMINNMP

**Assembly:** `FMINNMP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## FMINNMQV -- A64

### Encoding: `fminnmqv_z_p_z_`  — FMINNMQV

**Assembly:** `FMINNMQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FMINNMV -- A64

### Encoding: `FMINNMV_asimdall_only_H`  — FMINNMV

**Assembly:** `FMINNMV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — | Is the destination width specifier, H. |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
```

### Encoding: `FMINNMV_asimdall_only_SD`  — FMINNMV

**Assembly:** `FMINNMV  S<d>, <Vn>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q != '01' then EndOfDecode(Decode_UNDEF); end;    // .4S only

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
```

---

## FMINNMV -- A64

### Encoding: `fminnmv_v_p_z_`  — FMINNMV

**Assembly:** `FMINNMV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FMINP (scalar) -- A64

### Encoding: `FMINP_asisdpair_only_H`  — FMINP

**Assembly:** `FMINP  H<d>, <Vn>.2H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 32;
```

### Encoding: `FMINP_asisdpair_only_SD`  — FMINP

**Assembly:** `FMINP  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize * 2;
```

---

## FMINP (vector) -- A64

### Encoding: `FMINP_asimdsamefp16_only`  — FMINP

**Assembly:** `FMINP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMINP_asimdsame_only`  — FMINP

**Assembly:** `FMINP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMINP -- A64

### Encoding: `fminp_z_p_zz_`  — FMINP

**Assembly:** `FMINP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## FMINQV -- A64

### Encoding: `fminqv_z_p_z_`  — FMINQV

**Assembly:** `FMINQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FMINV -- A64

### Encoding: `FMINV_asimdall_only_H`  — FMINV

**Assembly:** `FMINV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — | Is the destination width specifier, H. |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
```

### Encoding: `FMINV_asimdall_only_SD`  — FMINV

**Assembly:** `FMINV  S<d>, <Vn>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q != '01' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
```

---

## FMINV -- A64

### Encoding: `fminv_v_p_z_`  — FMINV

**Assembly:** `FMINV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## FMLA (by element) -- A64

### Encoding: `FMLA_asisdelem_RH_H`  — FMLA

**Assembly:** `FMLA  <Hd>, <Hn>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the name of the second SIMD&FP source registe | let m : integer = UInt(Rm); |
| `<index>` | (H :: L :: M) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the element index, in the range 0 to 7, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let idxdsize : integer{} = 64 << UInt(H);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FMLA_asisdelem_R_SD`  — FMLA

**Assembly:** `FMLA  <V><d>, <V><n>, <Vm>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | (M :: Rm) | For the "Scalar, single-precision and double-precision" and "Vector, single-precision and double-precision" variants: is |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
let Rmhi : bit = M;
case sz::L of
    when '0x' => index = UInt(H::L);
    when '10' => index = UInt(H);
    when '11' => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FMLA_asimdelem_RH_H`  — FMLA

**Assembly:** `FMLA  <Vd>.<T>, <Vn>.<T>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the name of the second SIMD&FP source registe | let m : integer = UInt(Rm); |
| `<index>` | (H :: L :: M) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the element index, in the range 0 to 7, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let idxdsize : integer{} = 64 << UInt(H);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMLA_asimdelem_R_SD`  — FMLA

**Assembly:** `FMLA  <Vd>.<T>, <Vn>.<T>, <Vm>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | (M :: Rm) | For the "Scalar, single-precision and double-precision" and "Vector, single-precision and double-precision" variants: is |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
let Rmhi : bit = M;
case sz::L of
    when '0x' => index = UInt(H::L);
    when '10' => index = UInt(H);
    when '11' => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMLA (indexed) -- A64

### Encoding: `fmla_z_zzzi_h`  — FMLA

**Assembly:** `FMLA  <Zda>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "Half-precision" and "Single-precision" variants: is the name of the second source scalable vector register Z0-Z | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "Half-precision" variant: is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
let op3_neg : boolean = FALSE;
```

### Encoding: `fmla_z_zzzi_s`  — FMLA

**Assembly:** `FMLA  <Zda>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "Half-precision" and "Single-precision" variants: is the name of the second source scalable vector register Z0-Z | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "Single-precision" variant: is the immediate index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
let op3_neg : boolean = FALSE;
```

### Encoding: `fmla_z_zzzi_d`  — FMLA

**Assembly:** `FMLA  <Zda>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "Double-precision" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "Double-precision" variant: is the immediate index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
let op3_neg : boolean = FALSE;
```

---

## FMLA (multiple and indexed vector) -- A64

### Encoding: `fmla_za_zzi_h2xi`  — FMLA

**Assembly:** `FMLA  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of double-precision elements", "Two ZA single-vectors of half-precision elements", and "T | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA single-vectors of half-precision elements" and "Two ZA single-vectors of half-precision elements" varia |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `fmla_za_zzi_s2xi`  — FMLA

**Assembly:** `FMLA  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.S-<Zn2>.S }, <Zm>.S[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of double-precision elements", "Two ZA single-vectors of half-precision elements", and "T | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "Four ZA single-vectors of single-precision elements" and "Two ZA single-vectors of single-precision elements" v | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `fmla_za_zzi_d2xi`  — FMLA

**Assembly:** `FMLA  ZA.D[<Wv>, <offs>{, VGx2}], { <Zn1>.D-<Zn2>.D }, <Zm>.D[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of double-precision elements", "Two ZA single-vectors of half-precision elements", and "T | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "Four ZA single-vectors of double-precision elements" and "Two ZA single-vectors of double-precision elements" v | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_F64F64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
let nreg : integer{} = 2;
```

### Encoding: `fmla_za_zzi_h4xi`  — FMLA

**Assembly:** `FMLA  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of double-precision elements", "Four ZA single-vectors of half-precision elements", and  | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA single-vectors of half-precision elements" and "Two ZA single-vectors of half-precision elements" varia |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

### Encoding: `fmla_za_zzi_s4xi`  — FMLA

**Assembly:** `FMLA  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.S-<Zn4>.S }, <Zm>.S[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of double-precision elements", "Four ZA single-vectors of half-precision elements", and  | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "Four ZA single-vectors of single-precision elements" and "Two ZA single-vectors of single-precision elements" v | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

### Encoding: `fmla_za_zzi_d4xi`  — FMLA

**Assembly:** `FMLA  ZA.D[<Wv>, <offs>{, VGx4}], { <Zn1>.D-<Zn4>.D }, <Zm>.D[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of double-precision elements", "Four ZA single-vectors of half-precision elements", and  | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "Four ZA single-vectors of double-precision elements" and "Two ZA single-vectors of double-precision elements" v | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_F64F64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
let nreg : integer{} = 4;
```

---

## FMLA (multiple and single vector) -- A64

### Encoding: `fmla_za_zzv_2x1`  — FMLA

**Assembly:** `FMLA  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<T>-<Zn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fmla_za_zzv_2x1_16`  — FMLA

**Assembly:** `FMLA  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fmla_za_zzv_4x1`  — FMLA

**Assembly:** `FMLA  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<T>-<Zn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

### Encoding: `fmla_za_zzv_4x1_16`  — FMLA

**Assembly:** `FMLA  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FMLA (multiple vectors) -- A64

### Encoding: `fmla_za_zzw_2x2`  — FMLA

**Assembly:** `FMLA  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<T>-<Zn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fmla_za_zzw_2x2_16`  — FMLA

**Assembly:** `FMLA  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fmla_za_zzw_4x4`  — FMLA

**Assembly:** `FMLA  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<T>-<Zn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

### Encoding: `fmla_za_zzw_4x4_16`  — FMLA

**Assembly:** `FMLA  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FMLA (vector) -- A64

### Encoding: `FMLA_asimdsamefp16_only`  — FMLA

**Assembly:** `FMLA  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMLA_asimdsame_only`  — FMLA

**Assembly:** `FMLA  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMLA (vectors) -- A64

### Encoding: `fmla_z_p_zzz_`  — FMLA

**Assembly:** `FMLA  <Zda>.<T>, <Pg>/M, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
let op3_neg : boolean = FALSE;
```

---

## FMLAL (multiple and indexed vector, FP16 to FP32) -- A64

### Encoding: `fmlal_za_zzi_1`  — FMLAL

**Assembly:** `FMLAL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `fmlal_za_zzi_2xi`  — FMLAL

**Assembly:** `FMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `fmlal_za_zzi_4xi`  — FMLAL

**Assembly:** `FMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## FMLAL (multiple and indexed vector, FP8 to FP16) -- A64

### Encoding: `fmlal_za_z8z8i_1`  — FMLAL

**Assembly:** `FMLAL  ZA.H[<Wv>, <offs1>:<offs2>], <Zn>.B, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4A :: i4B :: i4C) | For the "One ZA double-vector" variant: is the element index, in the range 0 to 15, encoded in the "i4A:i4B:i4C" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let index : integer = UInt(i4A::i4B::i4C);
let nreg : integer{} = 1;
```

### Encoding: `fmlal_za_z8z8i_2xi`  — FMLAL

**Assembly:** `FMLAL  ZA.H[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the element index, in the range 0 to 15, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 2;
```

### Encoding: `fmlal_za_z8z8i_4xi`  — FMLAL

**Assembly:** `FMLAL  ZA.H[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the element index, in the range 0 to 15, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 4;
```

---

## FMLAL (multiple and single vector, FP16 to FP32) -- A64

### Encoding: `fmlal_za_zzv_1`  — FMLAL

**Assembly:** `FMLAL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let nreg : integer{} = 1;
```

### Encoding: `fmlal_za_zzv_2x1`  — FMLAL

**Assembly:** `FMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmlal_za_zzv_4x1`  — FMLAL

**Assembly:** `FMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## FMLAL (multiple and single vector, FP8 to FP16) -- A64

### Encoding: `fmlal_za_z8z8v_1`  — FMLAL

**Assembly:** `FMLAL  ZA.H[<Wv>, <offs1>:<offs2>], <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let nreg : integer{} = 1;
```

### Encoding: `fmlal_za_z8z8v_2x1`  — FMLAL

**Assembly:** `FMLAL  ZA.H[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmlal_za_z8z8v_4x1`  — FMLAL

**Assembly:** `FMLAL  ZA.H[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## FMLAL (multiple vectors, FP16 to FP32) -- A64

### Encoding: `fmlal_za_zzw_2x2`  — FMLAL

**Assembly:** `FMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmlal_za_zzw_4x4`  — FMLAL

**Assembly:** `FMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## FMLAL (multiple vectors, FP8 to FP16) -- A64

### Encoding: `fmlal_za_z8z8w_2x2`  — FMLAL

**Assembly:** `FMLAL  ZA.H[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmlal_za_z8z8w_4x4`  — FMLAL

**Assembly:** `FMLAL  ZA.H[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.B-<Zn4>.B }, { <Zm1>.B-<Zm4>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## FMLAL, FMLAL2 (by element) -- A64

### Encoding: `FMLAL_asimdelem_LH`  — FMLAL

**Assembly:** `FMLAL  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt('0'::Rm);    // Vm can only be in bottom 16 registers. |
| `<index>` | (H :: L :: M) | Is the element index, encoded in the "H:L:M" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FHM) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt('0'::Rm);    // Vm can only be in bottom 16 registers.
let index : integer = UInt(H::L::M);

let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let part : integer = 0;
```

### Encoding: `FMLAL2_asimdelem_LH`  — FMLAL2

**Assembly:** `FMLAL2  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt('0'::Rm);    // Vm can only be in bottom 16 registers. |
| `<index>` | (H :: L :: M) | Is the element index, encoded in the "H:L:M" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FHM) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt('0'::Rm);    // Vm can only be in bottom 16 registers.
let index : integer = UInt(H::L::M);

let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let part : integer = 1;
```

---

## FMLAL, FMLAL2 (vector) -- A64

### Encoding: `FMLAL_asimdsame_F`  — FMLAL

**Assembly:** `FMLAL  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FHM) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = 0;
```

### Encoding: `FMLAL2_asimdsame_F`  — FMLAL2

**Assembly:** `FMLAL2  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FHM) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = 1;
```

---

## FMLALB (indexed, FP16 to FP32) -- A64

### Encoding: `fmlalb_z_zzzi_s`  — FMLALB

**Assembly:** `FMLALB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i3h::i3l);
let op1_neg : boolean = FALSE;
```

---

## FMLALB (indexed, FP8 to FP16) -- A64

### Encoding: `fmlalb_z_z8z8z8i_`  — FMLALB

**Assembly:** `FMLALB  <Zda>.H, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i4h :: i4l) | Is the immediate index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i4h::i4l);
```

---

## FMLALB (vectors, FP16 to FP32) -- A64

### Encoding: `fmlalb_z_zzz_`  — FMLALB

**Assembly:** `FMLALB  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
```

---

## FMLALB (vectors, FP8 to FP16) -- A64

### Encoding: `fmlalb_z_z8z8z8_`  — FMLALB

**Assembly:** `FMLALB  <Zda>.H, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMLALB, FMLALT (by element) -- A64

### Encoding: `FMLALB_asimdelem_H`  [FMLALB]  — FMLALB

**Assembly:** `FMLALB  <Vd>.8H, <Vn>.16B, <Vm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, in the range V0 to V7, encoded in the "Rm<2:0>" field. | let m : integer = UInt('00'::Rm[2:0]);<br>let index : integer = UInt(H::L::M::Rm[3]); |
| `<index>` | (H :: L :: M :: Rm) | Is the element index, in the range 0 to 15, encoded in the "H:L:M:Rm<3>" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt('00'::Rm[2:0]);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M::Rm[3]);
let elements : integer = 128 DIV 16;
let sel : integer = UInt(Q);
```

### Encoding: `FMLALT_asimdelem_H`  [FMLALT]  — FMLALT

**Assembly:** `FMLALT  <Vd>.8H, <Vn>.16B, <Vm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, in the range V0 to V7, encoded in the "Rm<2:0>" field. | let m : integer = UInt('00'::Rm[2:0]);<br>let index : integer = UInt(H::L::M::Rm[3]); |
| `<index>` | (H :: L :: M :: Rm) | Is the element index, in the range 0 to 15, encoded in the "H:L:M:Rm<3>" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt('00'::Rm[2:0]);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M::Rm[3]);
let elements : integer = 128 DIV 16;
let sel : integer = UInt(Q);
```

---

## FMLALB, FMLALT (vector) -- A64

### Encoding: `FMLALB_asimdsame2_J`  [FMLALB]  — FMLALB

**Assembly:** `FMLALB  <Vd>.8H, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let elements : integer = 128 DIV 16;
let sel : integer = UInt(Q);
```

### Encoding: `FMLALT_asimdsame2_J`  [FMLALT]  — FMLALT

**Assembly:** `FMLALT  <Vd>.8H, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let elements : integer = 128 DIV 16;
let sel : integer = UInt(Q);
```

---

## FMLALL (multiple and indexed vector) -- A64

### Encoding: `fmlall_za32_z8z8i_1`  — FMLALL

**Assembly:** `FMLALL  ZA.S[<Wv>, <offs1>:<offs4>], <Zn>.B, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | Is the element index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 1;
```

### Encoding: `fmlall_za32_z8z8i_2xi`  — FMLALL

**Assembly:** `FMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vecto | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | Is the element index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 2;
```

### Encoding: `fmlall_za32_z8z8i_4xi`  — FMLALL

**Assembly:** `FMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vect | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | Is the element index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 4;
```

---

## FMLALL (multiple and single vector) -- A64

### Encoding: `fmlall_za32_z8z8v_1`  — FMLALL

**Assembly:** `FMLALL  ZA.S[<Wv>, <offs1>:<offs4>], <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let nreg : integer{} = 1;
```

### Encoding: `fmlall_za32_z8z8v_2x1`  — FMLALL

**Assembly:** `FMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `fmlall_za32_z8z8v_4x1`  — FMLALL

**Assembly:** `FMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## FMLALL (multiple vectors) -- A64

### Encoding: `fmlall_za32_z8z8w_2x2`  — FMLALL

**Assembly:** `FMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vecto | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vect | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `fmlall_za32_z8z8w_4x4`  — FMLALL

**Assembly:** `FMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, { <Zm1>.B-<Zm4>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vect | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vec | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## FMLALLBB (indexed) -- A64

### Encoding: `fmlallbb_z32_z8z8z8i_`  — FMLALLBB

**Assembly:** `FMLALLBB  <Zda>.S, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i4h :: i4l) | Is the immediate index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i4h::i4l);
```

---

## FMLALLBB (vectors) -- A64

### Encoding: `fmlallbb_z32_z8z8z8_`  — FMLALLBB

**Assembly:** `FMLALLBB  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMLALLBB, FMLALLBT, FMLALLTB, FMLALLTT (by element) -- A64

### Encoding: `FMLALLBB_asimdelem_J`  [FMLALLBB]  — FMLALLBB

**Assembly:** `FMLALLBB  <Vd>.4S, <Vn>.16B, <Vm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, in the range V0 to V7, encoded in the "Rm<2:0>" field. | let m : integer = UInt('00'::Rm[2:0]);<br>let index : integer = UInt(H::L::M::Rm[3]); |
| `<index>` | (H :: L :: M :: Rm) | Is the element index, in the range 0 to 15, encoded in the "H:L:M:Rm<3>" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt('00'::Rm[2:0]);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M::Rm[3]);
let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q::size[0]);
```

### Encoding: `FMLALLBT_asimdelem_J`  [FMLALLBT]  — FMLALLBT

**Assembly:** `FMLALLBT  <Vd>.4S, <Vn>.16B, <Vm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, in the range V0 to V7, encoded in the "Rm<2:0>" field. | let m : integer = UInt('00'::Rm[2:0]);<br>let index : integer = UInt(H::L::M::Rm[3]); |
| `<index>` | (H :: L :: M :: Rm) | Is the element index, in the range 0 to 15, encoded in the "H:L:M:Rm<3>" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt('00'::Rm[2:0]);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M::Rm[3]);
let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q::size[0]);
```

### Encoding: `FMLALLTB_asimdelem_J`  [FMLALLTB]  — FMLALLTB

**Assembly:** `FMLALLTB  <Vd>.4S, <Vn>.16B, <Vm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, in the range V0 to V7, encoded in the "Rm<2:0>" field. | let m : integer = UInt('00'::Rm[2:0]);<br>let index : integer = UInt(H::L::M::Rm[3]); |
| `<index>` | (H :: L :: M :: Rm) | Is the element index, in the range 0 to 15, encoded in the "H:L:M:Rm<3>" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt('00'::Rm[2:0]);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M::Rm[3]);
let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q::size[0]);
```

### Encoding: `FMLALLTT_asimdelem_J`  [FMLALLTT]  — FMLALLTT

**Assembly:** `FMLALLTT  <Vd>.4S, <Vn>.16B, <Vm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, in the range V0 to V7, encoded in the "Rm<2:0>" field. | let m : integer = UInt('00'::Rm[2:0]);<br>let index : integer = UInt(H::L::M::Rm[3]); |
| `<index>` | (H :: L :: M :: Rm) | Is the element index, in the range 0 to 15, encoded in the "H:L:M:Rm<3>" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt('00'::Rm[2:0]);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M::Rm[3]);
let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q::size[0]);
```

---

## FMLALLBB, FMLALLBT, FMLALLTB, FMLALLTT (vector) -- A64

### Encoding: `FMLALLBB_asimdsame2_G`  [FMLALLBB]  — FMLALLBB

**Assembly:** `FMLALLBB  <Vd>.4S, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q::size[0]);
```

### Encoding: `FMLALLBT_asimdsame2_G`  [FMLALLBT]  — FMLALLBT

**Assembly:** `FMLALLBT  <Vd>.4S, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q::size[0]);
```

### Encoding: `FMLALLTB_asimdsame2_G`  [FMLALLTB]  — FMLALLTB

**Assembly:** `FMLALLTB  <Vd>.4S, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q::size[0]);
```

### Encoding: `FMLALLTT_asimdsame2_G`  [FMLALLTT]  — FMLALLTT

**Assembly:** `FMLALLTT  <Vd>.4S, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8FMA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let elements : integer = 128 DIV 32;
let sel : integer = UInt(Q::size[0]);
```

---

## FMLALLBT (indexed) -- A64

### Encoding: `fmlallbt_z32_z8z8z8i_`  — FMLALLBT

**Assembly:** `FMLALLBT  <Zda>.S, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i4h :: i4l) | Is the immediate index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i4h::i4l);
```

---

## FMLALLBT (vectors) -- A64

### Encoding: `fmlallbt_z32_z8z8z8_`  — FMLALLBT

**Assembly:** `FMLALLBT  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMLALLTB (indexed) -- A64

### Encoding: `fmlalltb_z32_z8z8z8i_`  — FMLALLTB

**Assembly:** `FMLALLTB  <Zda>.S, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i4h :: i4l) | Is the immediate index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i4h::i4l);
```

---

## FMLALLTB (vectors) -- A64

### Encoding: `fmlalltb_z32_z8z8z8_`  — FMLALLTB

**Assembly:** `FMLALLTB  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMLALLTT (indexed) -- A64

### Encoding: `fmlalltt_z32_z8z8z8i_`  — FMLALLTT

**Assembly:** `FMLALLTT  <Zda>.S, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i4h :: i4l) | Is the immediate index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i4h::i4l);
```

---

## FMLALLTT (vectors) -- A64

### Encoding: `fmlalltt_z32_z8z8z8_`  — FMLALLTT

**Assembly:** `FMLALLTT  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMLALT (indexed, FP16 to FP32) -- A64

### Encoding: `fmlalt_z_zzzi_s`  — FMLALT

**Assembly:** `FMLALT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i3h::i3l);
let op1_neg : boolean = FALSE;
```

---

## FMLALT (indexed, FP8 to FP16) -- A64

### Encoding: `fmlalt_z_z8z8z8i_`  — FMLALT

**Assembly:** `FMLALT  <Zda>.H, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i4h :: i4l) | Is the immediate index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i4h::i4l);
```

---

## FMLALT (vectors, FP16 to FP32) -- A64

### Encoding: `fmlalt_z_zzz_`  — FMLALT

**Assembly:** `FMLALT  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
```

---

## FMLALT (vectors, FP8 to FP16) -- A64

### Encoding: `fmlalt_z_z8z8z8_`  — FMLALT

**Assembly:** `FMLALT  <Zda>.H, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !HaveSVE2FP8FMA() then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMLS (by element) -- A64

### Encoding: `FMLS_asisdelem_RH_H`  — FMLS

**Assembly:** `FMLS  <Hd>, <Hn>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the name of the second SIMD&FP source registe | let m : integer = UInt(Rm); |
| `<index>` | (H :: L :: M) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the element index, in the range 0 to 7, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let idxdsize : integer{} = 64 << UInt(H);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M);

let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FMLS_asisdelem_R_SD`  — FMLS

**Assembly:** `FMLS  <V><d>, <V><n>, <Vm>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | (M :: Rm) | For the "Scalar, single-precision and double-precision" and "Vector, single-precision and double-precision" variants: is |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
let Rmhi : bit = M;
case sz::L of
    when '0x' => index = UInt(H::L);
    when '10' => index = UInt(H);
    when '11' => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FMLS_asimdelem_RH_H`  — FMLS

**Assembly:** `FMLS  <Vd>.<T>, <Vn>.<T>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (4b) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the name of the second SIMD&FP source registe | let m : integer = UInt(Rm); |
| `<index>` | (H :: L :: M) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the element index, in the range 0 to 7, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let idxdsize : integer{} = 64 << UInt(H);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Rd);
let index : integer = UInt(H::L::M);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMLS_asimdelem_R_SD`  — FMLS

**Assembly:** `FMLS  <Vd>.<T>, <Vn>.<T>, <Vm>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | (M :: Rm) | For the "Scalar, single-precision and double-precision" and "Vector, single-precision and double-precision" variants: is |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
let Rmhi : bit = M;
case sz::L of
    when '0x' => index = UInt(H::L);
    when '10' => index = UInt(H);
    when '11' => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMLS (indexed) -- A64

### Encoding: `fmls_z_zzzi_h`  — FMLS

**Assembly:** `FMLS  <Zda>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "Half-precision" and "Single-precision" variants: is the name of the second source scalable vector register Z0-Z | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "Half-precision" variant: is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
let op3_neg : boolean = FALSE;
```

### Encoding: `fmls_z_zzzi_s`  — FMLS

**Assembly:** `FMLS  <Zda>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "Half-precision" and "Single-precision" variants: is the name of the second source scalable vector register Z0-Z | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "Single-precision" variant: is the immediate index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
let op3_neg : boolean = FALSE;
```

### Encoding: `fmls_z_zzzi_d`  — FMLS

**Assembly:** `FMLS  <Zda>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "Double-precision" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "Double-precision" variant: is the immediate index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
let op3_neg : boolean = FALSE;
```

---

## FMLS (multiple and indexed vector) -- A64

### Encoding: `fmls_za_zzi_h2xi`  — FMLS

**Assembly:** `FMLS  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of double-precision elements", "Two ZA single-vectors of half-precision elements", and "T | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA single-vectors of half-precision elements" and "Two ZA single-vectors of half-precision elements" varia |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `fmls_za_zzi_s2xi`  — FMLS

**Assembly:** `FMLS  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.S-<Zn2>.S }, <Zm>.S[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of double-precision elements", "Two ZA single-vectors of half-precision elements", and "T | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "Four ZA single-vectors of single-precision elements" and "Two ZA single-vectors of single-precision elements" v | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `fmls_za_zzi_d2xi`  — FMLS

**Assembly:** `FMLS  ZA.D[<Wv>, <offs>{, VGx2}], { <Zn1>.D-<Zn2>.D }, <Zm>.D[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of double-precision elements", "Two ZA single-vectors of half-precision elements", and "T | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "Four ZA single-vectors of double-precision elements" and "Two ZA single-vectors of double-precision elements" v | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_F64F64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
let nreg : integer{} = 2;
```

### Encoding: `fmls_za_zzi_h4xi`  — FMLS

**Assembly:** `FMLS  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of double-precision elements", "Four ZA single-vectors of half-precision elements", and  | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA single-vectors of half-precision elements" and "Two ZA single-vectors of half-precision elements" varia |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

### Encoding: `fmls_za_zzi_s4xi`  — FMLS

**Assembly:** `FMLS  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.S-<Zn4>.S }, <Zm>.S[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of double-precision elements", "Four ZA single-vectors of half-precision elements", and  | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "Four ZA single-vectors of single-precision elements" and "Two ZA single-vectors of single-precision elements" v | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

### Encoding: `fmls_za_zzi_d4xi`  — FMLS

**Assembly:** `FMLS  ZA.D[<Wv>, <offs>{, VGx4}], { <Zn1>.D-<Zn4>.D }, <Zm>.D[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of double-precision elements", "Four ZA single-vectors of half-precision elements", and  | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "Four ZA single-vectors of double-precision elements" and "Two ZA single-vectors of double-precision elements" v | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_F64F64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
let nreg : integer{} = 4;
```

---

## FMLS (multiple and single vector) -- A64

### Encoding: `fmls_za_zzv_2x1`  — FMLS

**Assembly:** `FMLS  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<T>-<Zn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fmls_za_zzv_2x1_16`  — FMLS

**Assembly:** `FMLS  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fmls_za_zzv_4x1`  — FMLS

**Assembly:** `FMLS  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<T>-<Zn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

### Encoding: `fmls_za_zzv_4x1_16`  — FMLS

**Assembly:** `FMLS  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FMLS (multiple vectors) -- A64

### Encoding: `fmls_za_zzw_2x2`  — FMLS

**Assembly:** `FMLS  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<T>-<Zn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fmls_za_zzw_2x2_16`  — FMLS

**Assembly:** `FMLS  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fmls_za_zzw_4x4`  — FMLS

**Assembly:** `FMLS  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<T>-<Zn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

### Encoding: `fmls_za_zzw_4x4_16`  — FMLS

**Assembly:** `FMLS  ZA.H[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FMLS (vector) -- A64

### Encoding: `FMLS_asimdsamefp16_only`  — FMLS

**Assembly:** `FMLS  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMLS_asimdsame_only`  — FMLS

**Assembly:** `FMLS  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMLS (vectors) -- A64

### Encoding: `fmls_z_p_zzz_`  — FMLS

**Assembly:** `FMLS  <Zda>.<T>, <Pg>/M, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
let op3_neg : boolean = FALSE;
```

---

## FMLSL (multiple and indexed vector) -- A64

### Encoding: `fmlsl_za_zzi_1`  — FMLSL

**Assembly:** `FMLSL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `fmlsl_za_zzi_2xi`  — FMLSL

**Assembly:** `FMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `fmlsl_za_zzi_4xi`  — FMLSL

**Assembly:** `FMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## FMLSL (multiple and single vector) -- A64

### Encoding: `fmlsl_za_zzv_1`  — FMLSL

**Assembly:** `FMLSL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let nreg : integer{} = 1;
```

### Encoding: `fmlsl_za_zzv_2x1`  — FMLSL

**Assembly:** `FMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmlsl_za_zzv_4x1`  — FMLSL

**Assembly:** `FMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## FMLSL (multiple vectors) -- A64

### Encoding: `fmlsl_za_zzw_2x2`  — FMLSL

**Assembly:** `FMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmlsl_za_zzw_4x4`  — FMLSL

**Assembly:** `FMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## FMLSL, FMLSL2 (by element) -- A64

### Encoding: `FMLSL_asimdelem_LH`  — FMLSL

**Assembly:** `FMLSL  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt('0'::Rm);    // Vm can only be in bottom 16 registers. |
| `<index>` | (H :: L :: M) | Is the element index, encoded in the "H:L:M" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FHM) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt('0'::Rm);    // Vm can only be in bottom 16 registers.
let index : integer = UInt(H::L::M);

let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let part : integer = 0;
```

### Encoding: `FMLSL2_asimdelem_LH`  — FMLSL2

**Assembly:** `FMLSL2  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (4b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt('0'::Rm);    // Vm can only be in bottom 16 registers. |
| `<index>` | (H :: L :: M) | Is the element index, encoded in the "H:L:M" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FHM) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt('0'::Rm);    // Vm can only be in bottom 16 registers.
let index : integer = UInt(H::L::M);

let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let part : integer = 1;
```

---

## FMLSL, FMLSL2 (vector) -- A64

### Encoding: `FMLSL_asimdsame_F`  — FMLSL

**Assembly:** `FMLSL  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FHM) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = 0;
```

### Encoding: `FMLSL2_asimdsame_F`  — FMLSL2

**Assembly:** `FMLSL2  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FHM) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = 1;
```

---

## FMLSLB (indexed) -- A64

### Encoding: `fmlslb_z_zzzi_s`  — FMLSLB

**Assembly:** `FMLSLB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i3h::i3l);
let op1_neg : boolean = TRUE;
```

---

## FMLSLB (vectors) -- A64

### Encoding: `fmlslb_z_zzz_`  — FMLSLB

**Assembly:** `FMLSLB  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
```

---

## FMLSLT (indexed) -- A64

### Encoding: `fmlslt_z_zzzi_s`  — FMLSLT

**Assembly:** `FMLSLT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | Is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let index : integer = UInt(i3h::i3l);
let op1_neg : boolean = TRUE;
```

---

## FMLSLT (vectors) -- A64

### Encoding: `fmlslt_z_zzz_`  — FMLSLT

**Assembly:** `FMLSLT  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
```

---

## FMMLA (non-widening) -- A64

### Encoding: `FMMLA_asimd_FP16FP16`  — FMMLA

**Assembly:** `FMMLA  <Vd>.8H, <Vn>.8H, <Vm>.8H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F16MM) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## FMMLA (non-widening) -- A64

### Encoding: `fmmla_z_zzz_h`  — FMMLA

**Assembly:** `FMMLA  <Zda>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) || !IsFeatureImplemented(FEAT_F16MM) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `fmmla_z_zzz_s`  — FMMLA

**Assembly:** `FMMLA  <Zda>.S, <Zn>.S, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F32MM) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `fmmla_z_zzz_d`  — FMMLA

**Assembly:** `FMMLA  <Zda>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMMLA (widening, 8-bit floating-point to half-precision) -- A64

### Encoding: `FMMLA_asimd_FP8FP16`  — FMMLA

**Assembly:** `FMMLA  <Vd>.8H, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F8F16MM) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Rd);
```

---

## FMMLA (widening, 8-bit floating-point to single-precision) -- A64

### Encoding: `FMMLA_asimd_FP8FP32`  — FMMLA

**Assembly:** `FMMLA  <Vd>.4S, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F8F32MM) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Rd);
```

---

## FMMLA (widening, FP16 to FP32) -- A64

### Encoding: `fmmla_z32_zzz_h`  — FMMLA

**Assembly:** `FMMLA  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_F16F32MM) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMMLA (widening, FP8 to FP16) -- A64

### Encoding: `fmmla_z16_zz8z8_`  — FMMLA

**Assembly:** `FMMLA  <Zda>.H, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) || !IsFeatureImplemented(FEAT_F8F16MM) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMMLA (widening, FP8 to FP32) -- A64

### Encoding: `fmmla_z32_zz8z8_`  — FMMLA

**Assembly:** `FMMLA  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) || !IsFeatureImplemented(FEAT_F8F32MM) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## FMMLA (widening, half-precision to single-precision) -- A64

### Encoding: `FMMLA_asimd_FP16FP32`  — FMMLA

**Assembly:** `FMMLA  <Vd>.4S, <Vn>.8H, <Vm>.8H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F16F32MM) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## FMOP4A (non-widening) -- A64

### Encoding: `fmop4a_za_zz_h1x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.H, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision, multiple and single vectors", "Half-precision, multiple vectors", "Half-precision, single and m | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F16F16) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_h1x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision, multiple and single vectors", "Half-precision, multiple vectors", "Half-precision, single and m | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F16F16) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_h2x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision, multiple and single vectors", "Half-precision, multiple vectors", "Half-precision, single and m | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F16F16) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_h2x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision, multiple and single vectors", "Half-precision, multiple vectors", "Half-precision, single and m | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F16F16) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_s1x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, <Zn>.S, { <Zm1>.S-<Zm2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision, multiple and single vectors", "Single-precision, multiple vectors", "Single-precision, single | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_s1x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, <Zn>.S, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision, multiple and single vectors", "Single-precision, multiple vectors", "Single-precision, single | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_s2x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, { <Zn1>.S-<Zn2>.S }, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision, multiple and single vectors", "Single-precision, multiple vectors", "Single-precision, single | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_s2x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, { <Zn1>.S-<Zn2>.S }, { <Zm1>.S-<Zm2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision, multiple and single vectors", "Single-precision, multiple vectors", "Single-precision, single | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_d1x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.D, <Zn>.D, { <Zm1>.D-<Zm2>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision, multiple and single vectors", "Double-precision, multiple vectors", "Double-precision, single | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F64F64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_d1x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision, multiple and single vectors", "Double-precision, multiple vectors", "Double-precision, single | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F64F64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_d2x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.D, { <Zn1>.D-<Zn2>.D }, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision, multiple and single vectors", "Double-precision, multiple vectors", "Double-precision, single | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F64F64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za_zz_d2x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.D, { <Zn1>.D-<Zn2>.D }, { <Zm1>.D-<Zm2>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision, multiple and single vectors", "Double-precision, multiple vectors", "Double-precision, single | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F64F64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

---

## FMOP4A (widening, 2-way, FP16 to FP32) -- A64

### Encoding: `fmop4a_za32_zz_h1x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za32_zz_h1x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za32_zz_h2x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

### Encoding: `fmop4a_za32_zz_h2x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
```

---

## FMOP4A (widening, 2-way, FP8 to FP16) -- A64

### Encoding: `fmop4a_za16_z8z8_b1x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.H, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F8F16) then
    EndOfDecode(Decode_UNDEF);
end;

let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
```

### Encoding: `fmop4a_za16_z8z8_b1x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.H, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F8F16) then
    EndOfDecode(Decode_UNDEF);
end;

let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
```

### Encoding: `fmop4a_za16_z8z8_b2x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.H, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F8F16) then
    EndOfDecode(Decode_UNDEF);
end;

let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
```

### Encoding: `fmop4a_za16_z8z8_b2x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.H, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F8F16) then
    EndOfDecode(Decode_UNDEF);
end;

let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
```

---

## FMOP4A (widening, 4-way) -- A64

### Encoding: `fmop4a_za32_z8z8_b1x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F8F32) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
```

### Encoding: `fmop4a_za32_z8z8_b1x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F8F32) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
```

### Encoding: `fmop4a_za32_z8z8_b2x1`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F8F32) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
```

### Encoding: `fmop4a_za32_z8z8_b2x2`  — FMOP4A

**Assembly:** `FMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F8F32) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
```

---

## FMOP4S (non-widening) -- A64

### Encoding: `fmop4s_za_zz_h1x2`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.H, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision, multiple and single vectors", "Half-precision, multiple vectors", "Half-precision, single and m | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F16F16) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_h1x1`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.H, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision, multiple and single vectors", "Half-precision, multiple vectors", "Half-precision, single and m | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F16F16) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_h2x1`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision, multiple and single vectors", "Half-precision, multiple vectors", "Half-precision, single and m | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F16F16) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_h2x2`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision, multiple and single vectors", "Half-precision, multiple vectors", "Half-precision, single and m | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F16F16) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_s1x2`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.S, <Zn>.S, { <Zm1>.S-<Zm2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision, multiple and single vectors", "Single-precision, multiple vectors", "Single-precision, single | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_s1x1`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.S, <Zn>.S, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision, multiple and single vectors", "Single-precision, multiple vectors", "Single-precision, single | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_s2x1`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.S, { <Zn1>.S-<Zn2>.S }, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision, multiple and single vectors", "Single-precision, multiple vectors", "Single-precision, single | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_s2x2`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.S, { <Zn1>.S-<Zn2>.S }, { <Zm1>.S-<Zm2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision, multiple and single vectors", "Single-precision, multiple vectors", "Single-precision, single | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_d1x2`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.D, <Zn>.D, { <Zm1>.D-<Zm2>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision, multiple and single vectors", "Double-precision, multiple vectors", "Double-precision, single | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F64F64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_d1x1`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision, multiple and single vectors", "Double-precision, multiple vectors", "Double-precision, single | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F64F64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_d2x1`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.D, { <Zn1>.D-<Zn2>.D }, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision, multiple and single vectors", "Double-precision, multiple vectors", "Double-precision, single | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F64F64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za_zz_d2x2`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.D, { <Zn1>.D-<Zn2>.D }, { <Zm1>.D-<Zm2>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision, multiple and single vectors", "Double-precision, multiple vectors", "Double-precision, single | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_F64F64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

---

## FMOP4S (widening) -- A64

### Encoding: `fmop4s_za32_zz_h1x2`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.S, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za32_zz_h1x1`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za32_zz_h2x1`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

### Encoding: `fmop4s_za32_zz_h2x2`  — FMOP4S

**Assembly:** `FMOP4S  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
```

---

## FMOPA (non-widening) -- A64

### Encoding: `fmopa_za_pp_zz_16`  — FMOPA

**Assembly:** `FMOPA  <ZAda>.H, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision" variant: is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

### Encoding: `fmopa_za_pp_zz_32`  — FMOPA

**Assembly:** `FMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.S, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

### Encoding: `fmopa_za_pp_zz_64`  — FMOPA

**Assembly:** `FMOPA  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## FMOPA (widening, 2-way, FP16 to FP32) -- A64

### Encoding: `fmopa_za32_pp_zz_16`  — FMOPA

**Assembly:** `FMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## FMOPA (widening, 2-way, FP8 to FP16) -- A64

### Encoding: `fmopa_za16_pp_z8z8_8`  — FMOPA

**Assembly:** `FMOPA  <ZAda>.H, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## FMOPA (widening, 4-way) -- A64

### Encoding: `fmopa_za32_pp_z8z8_8`  — FMOPA

**Assembly:** `FMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## FMOPS (non-widening) -- A64

### Encoding: `fmops_za_pp_zz_16`  — FMOPS

**Assembly:** `FMOPS  <ZAda>.H, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision" variant: is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

### Encoding: `fmops_za_pp_zz_32`  — FMOPS

**Assembly:** `FMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.S, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

### Encoding: `fmops_za_pp_zz_64`  — FMOPS

**Assembly:** `FMOPS  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "Double-precision" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## FMOPS (widening) -- A64

### Encoding: `fmops_za32_pp_zz_16`  — FMOPS

**Assembly:** `FMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
```

---

## FMOV (general) -- A64

### Encoding: `FMOV_32H_float2int`  [Half-precision to 32-bit]  — FMOV

**Assembly:** `FMOV  <Wd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

### Encoding: `FMOV_64H_float2int`  [Half-precision to 64-bit]  — FMOV

**Assembly:** `FMOV  <Xd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

### Encoding: `FMOV_H32_float2int`  [32-bit to half-precision]  — FMOV

**Assembly:** `FMOV  <Hd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

### Encoding: `FMOV_S32_float2int`  [32-bit to single-precision]  — FMOV

**Assembly:** `FMOV  <Sd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

### Encoding: `FMOV_32S_float2int`  [Single-precision to 32-bit]  — FMOV

**Assembly:** `FMOV  <Wd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

### Encoding: `FMOV_H64_float2int`  [64-bit to half-precision]  — FMOV

**Assembly:** `FMOV  <Hd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

### Encoding: `FMOV_D64_float2int`  [64-bit to double-precision]  — FMOV

**Assembly:** `FMOV  <Dd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

### Encoding: `FMOV_V64I_float2int`  [64-bit to top half of 128-bit]  — FMOV

**Assembly:** `FMOV  <Vd>.D[1], <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

### Encoding: `FMOV_64D_float2int`  [Double-precision to 64-bit]  — FMOV

**Assembly:** `FMOV  <Xd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

### Encoding: `FMOV_64VX_float2int`  [Top half of 128-bit to 64-bit]  — FMOV

**Assembly:** `FMOV  <Xd>, <Vn>.D[1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' && opcode[2:1]::rmode != '11 01' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = if ftype == '10' then 64 else (8 << UInt(ftype XOR '10'));
let part : integer = UInt(rmode[0]);
var op : FPConvOp;

case opcode[2:1]::rmode of
    when '11 00' =>        // FMOV
        if fltsize != 16 && fltsize != intsize then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    when '11 01' =>        // FMOV D[1]
        if intsize != 64 || ftype != '10' then EndOfDecode(Decode_UNDEF); end;
        op = if opcode[0] == '1' then FPConvOp_MOV_ItoF else FPConvOp_MOV_FtoI;
    otherwise =>
        unreachable;
end;
```

---

## FMOV (immediate, predicated) -- A64 *(alias for fcpy_z_p_i)*

### Encoding: `fmov_z_p_i__fcpy_z_p_i_`  — FCPY

**Assembly:** `FMOV  <Zd>.<T>, <Pg>/M, #<const> / FCPY  <Zd>.<T>, <Pg>/M, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. |  |
| `<const>` | imm8 (8b) | Is a floating-point immediate value expressible as ±n÷16×2^r, where n and r are integers such that 16 ≤ n ≤ 31 and -3 ≤  |  |
| `FCPY` | — |  |  |

---

## FMOV (immediate, unpredicated) -- A64 *(alias for fdup_z_i)*

### Encoding: `fmov_z_i__fdup_z_i_`  — FDUP

**Assembly:** `FMOV  <Zd>.<T>, #<const> / FDUP  <Zd>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<const>` | imm8 (8b) | Is a floating-point immediate value expressible as ±n÷16×2^r, where n and r are integers such that 16 ≤ n ≤ 31 and -3 ≤  |  |
| `FDUP` | — |  |  |

---

## FMOV (register) -- A64

### Encoding: `FMOV_H_floatdp1`  [Half-precision]  — FMOV

**Assembly:** `FMOV  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMOV_S_floatdp1`  [Single-precision]  — FMOV

**Assembly:** `FMOV  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMOV_D_floatdp1`  [Double-precision]  — FMOV

**Assembly:** `FMOV  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FMOV (scalar, immediate) -- A64

### Encoding: `FMOV_H_floatimm`  [Half-precision]  — FMOV

**Assembly:** `FMOV  <Hd>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<imm>` | imm8 (8b) | Is a signed floating-point constant with 3-bit exponent and normalized 4 bits of precision, encoded in the "imm8" field. | let imm : bits(datasize) = VFPExpandImm{}(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let imm : bits(datasize) = VFPExpandImm{}(imm8);
```

### Encoding: `FMOV_S_floatimm`  [Single-precision]  — FMOV

**Assembly:** `FMOV  <Sd>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<imm>` | imm8 (8b) | Is a signed floating-point constant with 3-bit exponent and normalized 4 bits of precision, encoded in the "imm8" field. | let imm : bits(datasize) = VFPExpandImm{}(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let imm : bits(datasize) = VFPExpandImm{}(imm8);
```

### Encoding: `FMOV_D_floatimm`  [Double-precision]  — FMOV

**Assembly:** `FMOV  <Dd>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<imm>` | imm8 (8b) | Is a signed floating-point constant with 3-bit exponent and normalized 4 bits of precision, encoded in the "imm8" field. | let imm : bits(datasize) = VFPExpandImm{}(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);

let datasize : integer{} = 8 << UInt(ftype XOR '10');
let imm : bits(datasize) = VFPExpandImm{}(imm8);
```

---

## FMOV (vector, immediate) -- A64

### Encoding: `FMOV_asimdimm_H_h`  — FMOV

**Assembly:** `FMOV  <Vd>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is a signed floating-point constant with 3-bit exponent and normalized 4 bits of precision, encoded in "a:b:c:d:e:f:g:h" |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let rd : integer = UInt(Rd);

let datasize : integer{} = 64 << UInt(Q);
let imm8 : bits(8) = a::b::c::d::e::f::g::h;
let imm16 : bits(16) = imm8[7]::NOT(imm8[6])::Replicate{2}(imm8[6])::imm8[5:0]::Zeros{6};
let imm : bits(datasize) = Replicate{}(imm16);
```

### Encoding: `FMOV_asimdimm_S_s`  [Single-precision]  — FMOV

**Assembly:** `FMOV  <Vd>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is a signed floating-point constant with 3-bit exponent and normalized 4 bits of precision, encoded in "a:b:c:d:e:f:g:h" |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if cmode::op == '11111' then
    // FMOV Dn,#imm is in main FP instruction set
    if Q == '0' then EndOfDecode(Decode_UNDEF); end;
end;

let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `FMOV_asimdimm_D2_d`  [Double-precision]  — FMOV

**Assembly:** `FMOV  <Vd>.2D, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<imm>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is a signed floating-point constant with 3-bit exponent and normalized 4 bits of precision, encoded in "a:b:c:d:e:f:g:h" |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if cmode::op == '11111' then
    // FMOV Dn,#imm is in main FP instruction set
    if Q == '0' then EndOfDecode(Decode_UNDEF); end;
end;

let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

---

## FMOV (zero, predicated) -- A64 *(alias for cpy_z_p_i)*

### Encoding: `fmov_z_p_0__cpy_z_p_i_`  — CPY

**Assembly:** `FMOV  <Zd>.<T>, <Pg>/M, #0.0 / CPY  <Zd>.<T>, <Pg>/M, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. |  |
| `CPY` | — |  |  |

---

## FMOV (zero, unpredicated) -- A64 *(alias for dup_z_i)*

### Encoding: `fmov_z_0__dup_z_i_`  — DUP

**Assembly:** `FMOV  <Zd>.<T>, #0.0 / DUP  <Zd>.<T>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `DUP` | — |  |  |

---

## FMSB -- A64

### Encoding: `fmsb_z_p_zzz_`  — FMSB

**Assembly:** `FMSB  <Zdn>.<T>, <Pg>/M, <Zm>.<T>, <Za>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Za>` | Za (5b) | Is the name of the third source scalable vector register, encoded in the "Za" field. | let a : integer = UInt(Za); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let a : integer = UInt(Za);
let op1_neg : boolean = TRUE;
let op3_neg : boolean = FALSE;
```

---

## FMSUB -- A64

### Encoding: `FMSUB_H_floatdp3`  [Half-precision]  — FMSUB

**Assembly:** `FMSUB  <Hd>, <Hn>, <Hm>, <Ha>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Ha>` | Ra (5b) | Is the 16-bit name of the third SIMD&FP source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMSUB_S_floatdp3`  [Single-precision]  — FMSUB

**Assembly:** `FMSUB  <Sd>, <Sn>, <Sm>, <Sa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Sa>` | Ra (5b) | Is the 32-bit name of the third SIMD&FP source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMSUB_D_floatdp3`  [Double-precision]  — FMSUB

**Assembly:** `FMSUB  <Dd>, <Dn>, <Dm>, <Da>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Da>` | Ra (5b) | Is the 64-bit name of the third SIMD&FP source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FMUL (by element) -- A64

### Encoding: `FMUL_asisdelem_RH_H`  — FMUL

**Assembly:** `FMUL  <Hd>, <Hn>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (4b) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the name of the second SIMD&FP source registe | let m : integer{} = UInt(Rm); |
| `<index>` | (H :: L :: M) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the element index, in the range 0 to 7, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let idxdsize : integer{} = 64 << UInt(H);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let index : integer = UInt(H::L::M);

let esize : integer{} = 16;
let datasize : integer{} = esize;

let elements : integer = 1;
```

### Encoding: `FMUL_asisdelem_R_SD`  — FMUL

**Assembly:** `FMUL  <V><d>, <V><n>, <Vm>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | (M :: Rm) | For the "Scalar, single-precision and double-precision" and "Vector, single-precision and double-precision" variants: is |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
let Rmhi : bit = M;
case sz::L of
    when '0x' => index = UInt(H::L);
    when '10' => index = UInt(H);
    when '11' => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FMUL_asimdelem_RH_H`  — FMUL

**Assembly:** `FMUL  <Vd>.<T>, <Vn>.<T>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (4b) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the name of the second SIMD&FP source registe | let m : integer{} = UInt(Rm); |
| `<index>` | (H :: L :: M) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the element index, in the range 0 to 7, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let idxdsize : integer{} = 64 << UInt(H);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let index : integer = UInt(H::L::M);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMUL_asimdelem_R_SD`  — FMUL

**Assembly:** `FMUL  <Vd>.<T>, <Vn>.<T>, <Vm>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | (M :: Rm) | For the "Scalar, single-precision and double-precision" and "Vector, single-precision and double-precision" variants: is |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
let Rmhi : bit = M;
case sz::L of
    when '0x' => index = UInt(H::L);
    when '10' => index = UInt(H);
    when '11' => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMUL (immediate) -- A64

### Encoding: `fmul_z_p_zs_`  — FMUL

**Assembly:** `FMUL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let imm : bits(esize) = if i1 == '0' then FPPointFive{esize}('0') else FPTwo{esize}('0');
```

---

## FMUL (indexed) -- A64

### Encoding: `fmul_z_zzi_h`  — FMUL

**Assembly:** `FMUL  <Zd>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "Half-precision" and "Single-precision" variants: is the name of the second source scalable vector register Z0-Z | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "Half-precision" variant: is the immediate index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `fmul_z_zzi_s`  — FMUL

**Assembly:** `FMUL  <Zd>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "Half-precision" and "Single-precision" variants: is the name of the second source scalable vector register Z0-Z | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "Single-precision" variant: is the immediate index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `fmul_z_zzi_d`  — FMUL

**Assembly:** `FMUL  <Zd>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "Double-precision" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "Double-precision" variant: is the immediate index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## FMUL (multiple and single vector) -- A64

### Encoding: `fmul_mz_zzv_2x1`  — FMUL

**Assembly:** `FMUL  { <Zd1>.<T>-<Zd2>.<T> }, { <Zn1>.<T>-<Zn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the first source multi-vector grou | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt(Zd::'0');
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `fmul_mz_zzv_4x1`  — FMUL

**Assembly:** `FMUL  { <Zd1>.<T>-<Zd4>.<T> }, { <Zn1>.<T>-<Zn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the first source multi-vector gro | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt(Zd::'00');
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## FMUL (multiple vectors) -- A64

### Encoding: `fmul_mz_zzw_2x2`  — FMUL

**Assembly:** `FMUL  { <Zd1>.<T>-<Zd2>.<T> }, { <Zn1>.<T>-<Zn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the first source multi-vector grou | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt(Zd::'0');
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `fmul_mz_zzw_4x4`  — FMUL

**Assembly:** `FMUL  { <Zd1>.<T>-<Zd4>.<T> }, { <Zn1>.<T>-<Zn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the first source multi-vector gro | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt(Zd::'00');
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## FMUL (scalar) -- A64

### Encoding: `FMUL_H_floatdp2`  [Half-precision]  — FMUL

**Assembly:** `FMUL  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMUL_S_floatdp2`  [Single-precision]  — FMUL

**Assembly:** `FMUL  <Sd>, <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FMUL_D_floatdp2`  [Double-precision]  — FMUL

**Assembly:** `FMUL  <Dd>, <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FMUL (vector) -- A64

### Encoding: `FMUL_asimdsamefp16_only`  — FMUL

**Assembly:** `FMUL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMUL_asimdsame_only`  — FMUL

**Assembly:** `FMUL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMUL (vectors, predicated) -- A64

### Encoding: `fmul_z_p_zz_`  — FMUL

**Assembly:** `FMUL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FMUL (vectors, unpredicated) -- A64

### Encoding: `fmul_z_zz_`  — FMUL

**Assembly:** `FMUL  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## FMULX (by element) -- A64

### Encoding: `FMULX_asisdelem_RH_H`  — FMULX

**Assembly:** `FMULX  <Hd>, <Hn>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (4b) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the name of the second SIMD&FP source registe | let m : integer{} = UInt(Rm); |
| `<index>` | (H :: L :: M) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the element index, in the range 0 to 7, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let idxdsize : integer{} = 64 << UInt(H);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let index : integer = UInt(H::L::M);

let esize : integer{} = 16;
let datasize : integer{} = esize;

let elements : integer = 1;
```

### Encoding: `FMULX_asisdelem_R_SD`  — FMULX

**Assembly:** `FMULX  <V><d>, <V><n>, <Vm>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | (M :: Rm) | For the "Scalar, single-precision and double-precision" and "Vector, single-precision and double-precision" variants: is |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
let Rmhi : bit = M;
case sz::L of
    when '0x' => index = UInt(H::L);
    when '10' => index = UInt(H);
    when '11' => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FMULX_asimdelem_RH_H`  — FMULX

**Assembly:** `FMULX  <Vd>.<T>, <Vn>.<T>, <Vm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (4b) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the name of the second SIMD&FP source registe | let m : integer{} = UInt(Rm); |
| `<index>` | (H :: L :: M) | For the "Scalar, half-precision" and "Vector, half-precision" variants: is the element index, in the range 0 to 7, encod |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let idxdsize : integer{} = 64 << UInt(H);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let index : integer = UInt(H::L::M);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMULX_asimdelem_R_SD`  — FMULX

**Assembly:** `FMULX  <Vd>.<T>, <Vn>.<T>, <Vm>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | (M :: Rm) | For the "Scalar, single-precision and double-precision" and "Vector, single-precision and double-precision" variants: is |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
let Rmhi : bit = M;
case sz::L of
    when '0x' => index = UInt(H::L);
    when '10' => index = UInt(H);
    when '11' => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMULX -- A64

### Encoding: `FMULX_asisdsamefp16_only`  — FMULX

**Assembly:** `FMULX  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FMULX_asisdsame_only`  — FMULX

**Assembly:** `FMULX  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FMULX_asimdsamefp16_only`  — FMULX

**Assembly:** `FMULX  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FMULX_asimdsame_only`  — FMULX

**Assembly:** `FMULX  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FMULX -- A64

### Encoding: `fmulx_z_p_zz_`  — FMULX

**Assembly:** `FMULX  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FNEG (scalar) -- A64

### Encoding: `FNEG_H_floatdp1`  [Half-precision]  — FNEG

**Assembly:** `FNEG  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FNEG_S_floatdp1`  [Single-precision]  — FNEG

**Assembly:** `FNEG  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FNEG_D_floatdp1`  [Double-precision]  — FNEG

**Assembly:** `FNEG  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FNEG (vector) -- A64

### Encoding: `FNEG_asimdmiscfp16_R`  — FNEG

**Assembly:** `FNEG  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FNEG_asimdmisc_R`  — FNEG

**Assembly:** `FNEG  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FNEG -- A64

### Encoding: `fneg_z_p_z_m`  — FNEG

**Assembly:** `FNEG  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `fneg_z_p_z_z`  — FNEG

**Assembly:** `FNEG  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## FNMAD -- A64

### Encoding: `fnmad_z_p_zzz_`  — FNMAD

**Assembly:** `FNMAD  <Zdn>.<T>, <Pg>/M, <Zm>.<T>, <Za>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Za>` | Za (5b) | Is the name of the third source scalable vector register, encoded in the "Za" field. | let a : integer = UInt(Za); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let a : integer = UInt(Za);
let op1_neg : boolean = TRUE;
let op3_neg : boolean = TRUE;
```

---

## FNMADD -- A64

### Encoding: `FNMADD_H_floatdp3`  [Half-precision]  — FNMADD

**Assembly:** `FNMADD  <Hd>, <Hn>, <Hm>, <Ha>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Ha>` | Ra (5b) | Is the 16-bit name of the third SIMD&FP source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FNMADD_S_floatdp3`  [Single-precision]  — FNMADD

**Assembly:** `FNMADD  <Sd>, <Sn>, <Sm>, <Sa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Sa>` | Ra (5b) | Is the 32-bit name of the third SIMD&FP source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FNMADD_D_floatdp3`  [Double-precision]  — FNMADD

**Assembly:** `FNMADD  <Dd>, <Dn>, <Dm>, <Da>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Da>` | Ra (5b) | Is the 64-bit name of the third SIMD&FP source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FNMLA -- A64

### Encoding: `fnmla_z_p_zzz_`  — FNMLA

**Assembly:** `FNMLA  <Zda>.<T>, <Pg>/M, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = TRUE;
let op3_neg : boolean = TRUE;
```

---

## FNMLS -- A64

### Encoding: `fnmls_z_p_zzz_`  — FNMLS

**Assembly:** `FNMLS  <Zda>.<T>, <Pg>/M, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_neg : boolean = FALSE;
let op3_neg : boolean = TRUE;
```

---

## FNMSB -- A64

### Encoding: `fnmsb_z_p_zzz_`  — FNMSB

**Assembly:** `FNMSB  <Zdn>.<T>, <Pg>/M, <Zm>.<T>, <Za>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Za>` | Za (5b) | Is the name of the third source scalable vector register, encoded in the "Za" field. | let a : integer = UInt(Za); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let a : integer = UInt(Za);
let op1_neg : boolean = FALSE;
let op3_neg : boolean = TRUE;
```

---

## FNMSUB -- A64

### Encoding: `FNMSUB_H_floatdp3`  [Half-precision]  — FNMSUB

**Assembly:** `FNMSUB  <Hd>, <Hn>, <Hm>, <Ha>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Ha>` | Ra (5b) | Is the 16-bit name of the third SIMD&FP source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FNMSUB_S_floatdp3`  [Single-precision]  — FNMSUB

**Assembly:** `FNMSUB  <Sd>, <Sn>, <Sm>, <Sa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Sa>` | Ra (5b) | Is the 32-bit name of the third SIMD&FP source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FNMSUB_D_floatdp3`  [Double-precision]  — FNMSUB

**Assembly:** `FNMSUB  <Dd>, <Dn>, <Dm>, <Da>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Da>` | Ra (5b) | Is the 64-bit name of the third SIMD&FP source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FNMUL (scalar) -- A64

### Encoding: `FNMUL_H_floatdp2`  [Half-precision]  — FNMUL

**Assembly:** `FNMUL  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FNMUL_S_floatdp2`  [Single-precision]  — FNMUL

**Assembly:** `FNMUL  <Sd>, <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FNMUL_D_floatdp2`  [Double-precision]  — FNMUL

**Assembly:** `FNMUL  <Dd>, <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FRECPE -- A64

### Encoding: `FRECPE_asisdmiscfp16_R`  — FRECPE

**Assembly:** `FRECPE  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FRECPE_asisdmisc_R`  — FRECPE

**Assembly:** `FRECPE  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FRECPE_asimdmiscfp16_R`  — FRECPE

**Assembly:** `FRECPE  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FRECPE_asimdmisc_R`  — FRECPE

**Assembly:** `FRECPE  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FRECPE -- A64

### Encoding: `frecpe_z_z_`  — FRECPE

**Assembly:** `FRECPE  <Zd>.<T>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## FRECPS -- A64

### Encoding: `FRECPS_asisdsamefp16_only`  — FRECPS

**Assembly:** `FRECPS  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FRECPS_asisdsame_only`  — FRECPS

**Assembly:** `FRECPS  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FRECPS_asimdsamefp16_only`  — FRECPS

**Assembly:** `FRECPS  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FRECPS_asimdsame_only`  — FRECPS

**Assembly:** `FRECPS  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FRECPS -- A64

### Encoding: `frecps_z_zz_`  — FRECPS

**Assembly:** `FRECPS  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## FRECPX -- A64

### Encoding: `FRECPX_asisdmiscfp16_R`  — FRECPX

**Assembly:** `FRECPX  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 16;
```

### Encoding: `FRECPX_asisdmisc_R`  — FRECPX

**Assembly:** `FRECPX  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
```

---

## FRECPX -- A64

### Encoding: `frecpx_z_p_z_m`  — FRECPX

**Assembly:** `FRECPX  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `frecpx_z_p_z_z`  — FRECPX

**Assembly:** `FRECPX  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## FRINT32X (scalar) -- A64

### Encoding: `FRINT32X_S_floatdp1`  [Single-precision]  — FRINT32X

**Assembly:** `FRINT32X  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if ftype IN {'1x'} then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(ftype[0]);
let intsize : integer{} = 32;
```

### Encoding: `FRINT32X_D_floatdp1`  [Double-precision]  — FRINT32X

**Assembly:** `FRINT32X  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if ftype IN {'1x'} then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(ftype[0]);
let intsize : integer{} = 32;
```

---

## FRINT32X (vector) -- A64

### Encoding: `FRINT32X_asimdmisc_R`  — FRINT32X

**Assembly:** `FRINT32X  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let intsize : integer{} = 32;
```

---

## FRINT32X -- A64

### Encoding: `frint32x_z_p_z_m`  — FRINT32X

**Assembly:** `FRINT32X  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let intsize : integer{} = 32;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `frint32x_z_p_z_z`  — FRINT32X

**Assembly:** `FRINT32X  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let intsize : integer{} = 32;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

---

## FRINT32Z (scalar) -- A64

### Encoding: `FRINT32Z_S_floatdp1`  [Single-precision]  — FRINT32Z

**Assembly:** `FRINT32Z  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if ftype IN {'1x'} then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(ftype[0]);
let intsize : integer{} = 32;

let rounding : FPRounding = FPRounding_ZERO;
```

### Encoding: `FRINT32Z_D_floatdp1`  [Double-precision]  — FRINT32Z

**Assembly:** `FRINT32Z  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if ftype IN {'1x'} then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(ftype[0]);
let intsize : integer{} = 32;

let rounding : FPRounding = FPRounding_ZERO;
```

---

## FRINT32Z (vector) -- A64

### Encoding: `FRINT32Z_asimdmisc_R`  — FRINT32Z

**Assembly:** `FRINT32Z  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let intsize : integer{} = 32;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FRINT32Z -- A64

### Encoding: `frint32z_z_p_z_m`  — FRINT32Z

**Assembly:** `FRINT32Z  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let intsize : integer{} = 32;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `frint32z_z_p_z_z`  — FRINT32Z

**Assembly:** `FRINT32Z  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let intsize : integer{} = 32;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

---

## FRINT64X (scalar) -- A64

### Encoding: `FRINT64X_S_floatdp1`  [Single-precision]  — FRINT64X

**Assembly:** `FRINT64X  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if ftype IN {'1x'} then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(ftype[0]);
let intsize : integer{} = 64;
```

### Encoding: `FRINT64X_D_floatdp1`  [Double-precision]  — FRINT64X

**Assembly:** `FRINT64X  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if ftype IN {'1x'} then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(ftype[0]);
let intsize : integer{} = 64;
```

---

## FRINT64X (vector) -- A64

### Encoding: `FRINT64X_asimdmisc_R`  — FRINT64X

**Assembly:** `FRINT64X  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let intsize : integer{} = 64;
```

---

## FRINT64X -- A64

### Encoding: `frint64x_z_p_z_m`  — FRINT64X

**Assembly:** `FRINT64X  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let intsize : integer{} = 64;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `frint64x_z_p_z_z`  — FRINT64X

**Assembly:** `FRINT64X  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let intsize : integer{} = 64;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

---

## FRINT64Z (scalar) -- A64

### Encoding: `FRINT64Z_S_floatdp1`  [Single-precision]  — FRINT64Z

**Assembly:** `FRINT64Z  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if ftype IN {'1x'} then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(ftype[0]);
let intsize : integer{} = 64;

let rounding : FPRounding = FPRounding_ZERO;
```

### Encoding: `FRINT64Z_D_floatdp1`  [Double-precision]  — FRINT64Z

**Assembly:** `FRINT64Z  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if ftype IN {'1x'} then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(ftype[0]);
let intsize : integer{} = 64;

let rounding : FPRounding = FPRounding_ZERO;
```

---

## FRINT64Z (vector) -- A64

### Encoding: `FRINT64Z_asimdmisc_R`  — FRINT64Z

**Assembly:** `FRINT64Z  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FRINTTS) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let intsize : integer{} = 64;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FRINT64Z -- A64

### Encoding: `frint64z_z_p_z_m`  — FRINT64Z

**Assembly:** `FRINT64Z  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let intsize : integer{} = 64;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `frint64z_z_p_z_z`  — FRINT64Z

**Assembly:** `FRINT64Z  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let intsize : integer{} = 64;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

---

## FRINT<r> -- A64

### Encoding: `frintx_z_p_z_m`  — FRINTX

**Assembly:** `FRINTX  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `frintx_z_p_z_z`  — FRINTX

**Assembly:** `FRINTX  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `frinti_z_p_z_m`  — FRINTI

**Assembly:** `FRINTI  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `frinti_z_p_z_z`  — FRINTI

**Assembly:** `FRINTI  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `frinta_z_p_z_m`  — FRINTA

**Assembly:** `FRINTA  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEAWAY;
let merging : boolean = TRUE;
```

### Encoding: `frinta_z_p_z_z`  — FRINTA

**Assembly:** `FRINTA  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEAWAY;
let merging : boolean = FALSE;
```

### Encoding: `frintn_z_p_z_m`  — FRINTN

**Assembly:** `FRINTN  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEEVEN;
let merging : boolean = TRUE;
```

### Encoding: `frintn_z_p_z_z`  — FRINTN

**Assembly:** `FRINTN  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEEVEN;
let merging : boolean = FALSE;
```

### Encoding: `frintz_z_p_z_m`  — FRINTZ

**Assembly:** `FRINTZ  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = TRUE;
```

### Encoding: `frintz_z_p_z_z`  — FRINTZ

**Assembly:** `FRINTZ  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
let merging : boolean = FALSE;
```

### Encoding: `frintm_z_p_z_m`  — FRINTM

**Assembly:** `FRINTM  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_NEGINF;
let merging : boolean = TRUE;
```

### Encoding: `frintm_z_p_z_z`  — FRINTM

**Assembly:** `FRINTM  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_NEGINF;
let merging : boolean = FALSE;
```

### Encoding: `frintp_z_p_z_m`  — FRINTP

**Assembly:** `FRINTP  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_POSINF;
let merging : boolean = TRUE;
```

### Encoding: `frintp_z_p_z_z`  — FRINTP

**Assembly:** `FRINTP  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_POSINF;
let merging : boolean = FALSE;
```

---

## FRINTA (scalar) -- A64

### Encoding: `FRINTA_H_floatdp1`  [Half-precision]  — FRINTA

**Assembly:** `FRINTA  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEAWAY;
```

### Encoding: `FRINTA_S_floatdp1`  [Single-precision]  — FRINTA

**Assembly:** `FRINTA  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEAWAY;
```

### Encoding: `FRINTA_D_floatdp1`  [Double-precision]  — FRINTA

**Assembly:** `FRINTA  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEAWAY;
```

---

## FRINTA (vector) -- A64

### Encoding: `FRINTA_asimdmiscfp16_R`  — FRINTA

**Assembly:** `FRINTA  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEAWAY;
```

### Encoding: `FRINTA_asimdmisc_R`  — FRINTA

**Assembly:** `FRINTA  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEAWAY;
```

---

## FRINTA -- A64

### Encoding: `frinta_mz_z_2`  — FRINTA

**Assembly:** `FRINTA  { <Zd1>.S-<Zd2>.S }, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the source multi-vector group, enc | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEAWAY;
```

### Encoding: `frinta_mz_z_4`  — FRINTA

**Assembly:** `FRINTA  { <Zd1>.S-<Zd4>.S }, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the source multi-vector group, en | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEAWAY;
```

---

## FRINTI (scalar) -- A64

### Encoding: `FRINTI_H_floatdp1`  [Half-precision]  — FRINTI

**Assembly:** `FRINTI  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
```

### Encoding: `FRINTI_S_floatdp1`  [Single-precision]  — FRINTI

**Assembly:** `FRINTI  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
```

### Encoding: `FRINTI_D_floatdp1`  [Double-precision]  — FRINTI

**Assembly:** `FRINTI  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
```

---

## FRINTI (vector) -- A64

### Encoding: `FRINTI_asimdmiscfp16_R`  — FRINTI

**Assembly:** `FRINTI  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
```

### Encoding: `FRINTI_asimdmisc_R`  — FRINTI

**Assembly:** `FRINTI  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
```

---

## FRINTM (scalar) -- A64

### Encoding: `FRINTM_H_floatdp1`  [Half-precision]  — FRINTM

**Assembly:** `FRINTM  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_NEGINF;
```

### Encoding: `FRINTM_S_floatdp1`  [Single-precision]  — FRINTM

**Assembly:** `FRINTM  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_NEGINF;
```

### Encoding: `FRINTM_D_floatdp1`  [Double-precision]  — FRINTM

**Assembly:** `FRINTM  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_NEGINF;
```

---

## FRINTM (vector) -- A64

### Encoding: `FRINTM_asimdmiscfp16_R`  — FRINTM

**Assembly:** `FRINTM  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_NEGINF;
```

### Encoding: `FRINTM_asimdmisc_R`  — FRINTM

**Assembly:** `FRINTM  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_NEGINF;
```

---

## FRINTM -- A64

### Encoding: `frintm_mz_z_2`  — FRINTM

**Assembly:** `FRINTM  { <Zd1>.S-<Zd2>.S }, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the source multi-vector group, enc | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_NEGINF;
```

### Encoding: `frintm_mz_z_4`  — FRINTM

**Assembly:** `FRINTM  { <Zd1>.S-<Zd4>.S }, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the source multi-vector group, en | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_NEGINF;
```

---

## FRINTN (scalar) -- A64

### Encoding: `FRINTN_H_floatdp1`  [Half-precision]  — FRINTN

**Assembly:** `FRINTN  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEEVEN;
```

### Encoding: `FRINTN_S_floatdp1`  [Single-precision]  — FRINTN

**Assembly:** `FRINTN  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEEVEN;
```

### Encoding: `FRINTN_D_floatdp1`  [Double-precision]  — FRINTN

**Assembly:** `FRINTN  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEEVEN;
```

---

## FRINTN (vector) -- A64

### Encoding: `FRINTN_asimdmiscfp16_R`  — FRINTN

**Assembly:** `FRINTN  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEEVEN;
```

### Encoding: `FRINTN_asimdmisc_R`  — FRINTN

**Assembly:** `FRINTN  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEEVEN;
```

---

## FRINTN -- A64

### Encoding: `frintn_mz_z_2`  — FRINTN

**Assembly:** `FRINTN  { <Zd1>.S-<Zd2>.S }, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the source multi-vector group, enc | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEEVEN;
```

### Encoding: `frintn_mz_z_4`  — FRINTN

**Assembly:** `FRINTN  { <Zd1>.S-<Zd4>.S }, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the source multi-vector group, en | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_TIEEVEN;
```

---

## FRINTP (scalar) -- A64

### Encoding: `FRINTP_H_floatdp1`  [Half-precision]  — FRINTP

**Assembly:** `FRINTP  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_POSINF;
```

### Encoding: `FRINTP_S_floatdp1`  [Single-precision]  — FRINTP

**Assembly:** `FRINTP  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_POSINF;
```

### Encoding: `FRINTP_D_floatdp1`  [Double-precision]  — FRINTP

**Assembly:** `FRINTP  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_POSINF;
```

---

## FRINTP (vector) -- A64

### Encoding: `FRINTP_asimdmiscfp16_R`  — FRINTP

**Assembly:** `FRINTP  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_POSINF;
```

### Encoding: `FRINTP_asimdmisc_R`  — FRINTP

**Assembly:** `FRINTP  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_POSINF;
```

---

## FRINTP -- A64

### Encoding: `frintp_mz_z_2`  — FRINTP

**Assembly:** `FRINTP  { <Zd1>.S-<Zd2>.S }, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the source multi-vector group, enc | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_POSINF;
```

### Encoding: `frintp_mz_z_4`  — FRINTP

**Assembly:** `FRINTP  { <Zd1>.S-<Zd4>.S }, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the source multi-vector group, en | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_POSINF;
```

---

## FRINTX (scalar) -- A64

### Encoding: `FRINTX_H_floatdp1`  [Half-precision]  — FRINTX

**Assembly:** `FRINTX  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = TRUE;
```

### Encoding: `FRINTX_S_floatdp1`  [Single-precision]  — FRINTX

**Assembly:** `FRINTX  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = TRUE;
```

### Encoding: `FRINTX_D_floatdp1`  [Double-precision]  — FRINTX

**Assembly:** `FRINTX  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = TRUE;
```

---

## FRINTX (vector) -- A64

### Encoding: `FRINTX_asimdmiscfp16_R`  — FRINTX

**Assembly:** `FRINTX  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = TRUE;
```

### Encoding: `FRINTX_asimdmisc_R`  — FRINTX

**Assembly:** `FRINTX  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = TRUE;
```

---

## FRINTZ (scalar) -- A64

### Encoding: `FRINTZ_H_floatdp1`  [Half-precision]  — FRINTZ

**Assembly:** `FRINTZ  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

### Encoding: `FRINTZ_S_floatdp1`  [Single-precision]  — FRINTZ

**Assembly:** `FRINTZ  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

### Encoding: `FRINTZ_D_floatdp1`  [Double-precision]  — FRINTZ

**Assembly:** `FRINTZ  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FRINTZ (vector) -- A64

### Encoding: `FRINTZ_asimdmiscfp16_R`  — FRINTZ

**Assembly:** `FRINTZ  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

### Encoding: `FRINTZ_asimdmisc_R`  — FRINTZ

**Assembly:** `FRINTZ  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let exact : boolean = FALSE;
let rounding : FPRounding = FPRounding_ZERO;
```

---

## FRSQRTE -- A64

### Encoding: `FRSQRTE_asisdmiscfp16_R`  — FRSQRTE

**Assembly:** `FRSQRTE  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FRSQRTE_asisdmisc_R`  — FRSQRTE

**Assembly:** `FRSQRTE  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FRSQRTE_asimdmiscfp16_R`  — FRSQRTE

**Assembly:** `FRSQRTE  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FRSQRTE_asimdmisc_R`  — FRSQRTE

**Assembly:** `FRSQRTE  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FRSQRTE -- A64

### Encoding: `frsqrte_z_z_`  — FRSQRTE

**Assembly:** `FRSQRTE  <Zd>.<T>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## FRSQRTS -- A64

### Encoding: `FRSQRTS_asisdsamefp16_only`  — FRSQRTS

**Assembly:** `FRSQRTS  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FRSQRTS_asisdsame_only`  — FRSQRTS

**Assembly:** `FRSQRTS  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `FRSQRTS_asimdsamefp16_only`  — FRSQRTS

**Assembly:** `FRSQRTS  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FRSQRTS_asimdsame_only`  — FRSQRTS

**Assembly:** `FRSQRTS  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FRSQRTS -- A64

### Encoding: `frsqrts_z_zz_`  — FRSQRTS

**Assembly:** `FRSQRTS  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## FSCALE (multiple and single vector) -- A64

### Encoding: `fscale_mz_zzv_2x1`  — FSCALE

**Assembly:** `FSCALE  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `fscale_mz_zzv_4x1`  — FSCALE

**Assembly:** `FSCALE  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## FSCALE (multiple vectors) -- A64

### Encoding: `fscale_mz_zzw_2x2`  — FSCALE

**Assembly:** `FSCALE  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `fscale_mz_zzw_4x4`  — FSCALE

**Assembly:** `FSCALE  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !IsFeatureImplemented(FEAT_FP8) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## FSCALE -- A64

### Encoding: `FSCALE_asimdsamefp16_only`  — FSCALE

**Assembly:** `FSCALE  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = if Q == '1' then 128 else 64;
let elements : integer = datasize DIV esize;
```

### Encoding: `FSCALE_asimdsame_only`  — FSCALE

**Assembly:** `FSCALE  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP8) then EndOfDecode(Decode_UNDEF); end;
if Q == '0' && size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = if Q == '1' then 128 else 64;
let elements : integer = datasize DIV esize;
```

---

## FSCALE -- A64

### Encoding: `fscale_z_p_zz_`  — FSCALE

**Assembly:** `FSCALE  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FSQRT (scalar) -- A64

### Encoding: `FSQRT_H_floatdp1`  [Half-precision]  — FSQRT

**Assembly:** `FSQRT  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FSQRT_S_floatdp1`  [Single-precision]  — FSQRT

**Assembly:** `FSQRT  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FSQRT_D_floatdp1`  [Double-precision]  — FSQRT

**Assembly:** `FSQRT  <Dd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FSQRT (vector) -- A64

### Encoding: `FSQRT_asimdmiscfp16_R`  — FSQRT

**Assembly:** `FSQRT  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FSQRT_asimdmisc_R`  — FSQRT

**Assembly:** `FSQRT  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FSQRT -- A64

### Encoding: `fsqrt_z_p_z_m`  — FSQRT

**Assembly:** `FSQRT  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `fsqrt_z_p_z_z`  — FSQRT

**Assembly:** `FSQRT  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## FSUB (immediate) -- A64

### Encoding: `fsub_z_p_zs_`  — FSUB

**Assembly:** `FSUB  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let imm : bits(esize) = if i1 == '0' then FPPointFive{esize}('0') else FPOne{esize}('0');
```

---

## FSUB (scalar) -- A64

### Encoding: `FSUB_H_floatdp2`  [Half-precision]  — FSUB

**Assembly:** `FSUB  <Hd>, <Hn>, <Hm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Hm>` | Rm (5b) | Is the 16-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FSUB_S_floatdp2`  [Single-precision]  — FSUB

**Assembly:** `FSUB  <Sd>, <Sn>, <Sm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Sm>` | Rm (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

### Encoding: `FSUB_D_floatdp2`  [Double-precision]  — FSUB

**Assembly:** `FSUB  <Dd>, <Dn>, <Dm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Dm>` | Rm (5b) | Is the 64-bit name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(ftype XOR '10');
```

---

## FSUB (vector) -- A64

### Encoding: `FSUB_asimdsamefp16_only`  — FSUB

**Assembly:** `FSUB  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `FSUB_asimdsame_only`  — FSUB

**Assembly:** `FSUB  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## FSUB (vectors, predicated) -- A64

### Encoding: `fsub_z_p_zz_`  — FSUB

**Assembly:** `FSUB  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FSUB (vectors, unpredicated) -- A64

### Encoding: `fsub_z_zz_`  — FSUB

**Assembly:** `FSUB  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## FSUB -- A64

### Encoding: `fsub_za_zw_2x2`  — FSUB

**Assembly:** `FSUB  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" times 2 plus 1. | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fsub_za_zw_2x2_16`  — FSUB

**Assembly:** `FSUB  ZA.H[<Wv>, <offs>{, VGx2}], { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors of half-precision elements" and "Two ZA single-vectors" variants: is the name of the firs | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" times 2 plus 1. | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) && !IsFeatureImplemented(FEAT_SME_F8F16) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `fsub_za_zw_4x4`  — FSUB

**Assembly:** `FSUB  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zm" times 4 plus 3. | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_F64F64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

### Encoding: `fsub_za_zw_4x4_16`  — FSUB

**Assembly:** `FSUB  ZA.H[<Wv>, <offs>{, VGx4}], { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors of half-precision elements" and "Four ZA single-vectors" variants: is the name of the fi | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zm" times 4 plus 3. | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F16F16) && !IsFeatureImplemented(FEAT_SME_F8F16) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 16;
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## FSUBR (immediate) -- A64

### Encoding: `fsubr_z_p_zs_`  — FSUBR

**Assembly:** `FSUBR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let imm : bits(esize) = if i1 == '0' then FPPointFive{esize}('0') else FPOne{esize}('0');
```

---

## FSUBR (vectors) -- A64

### Encoding: `fsubr_z_p_zz_`  — FSUBR

**Assembly:** `FSUBR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);

let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## FTMAD -- A64

### Encoding: `ftmad_z_zzi_`  — FTMAD

**Assembly:** `FTMAD  <Zdn>.<T>, <Zdn>.<T>, <Zm>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | imm3 (3b) | Is the unsigned immediate operand, in the range 0 to 7, encoded in the "imm3" field. | let imm : integer = UInt(imm3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let imm : integer = UInt(imm3);
```

---

## FTMOPA (non-widening) -- A64

### Encoding: `ftmopa_za_zzzi_h2x1`  — FTMOPA

**Assembly:** `FTMOPA  <ZAda>.H, { <Zn1>.H-<Zn2>.H }, <Zm>.H, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | For the "Half-precision" variant: is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) || !IsFeatureImplemented(FEAT_SME_F16F16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
let esize : integer{} = 16;
```

### Encoding: `ftmopa_za_zzzi_s2x1`  — FTMOPA

**Assembly:** `FTMOPA  <ZAda>.S, { <Zn1>.S-<Zn2>.S }, <Zm>.S, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "Single-precision" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
let esize : integer{} = 32;
```

---

## FTMOPA (widening, 2-way, FP16 to FP32) -- A64

### Encoding: `ftmopa_za32_zzzi_h2x1`  — FTMOPA

**Assembly:** `FTMOPA  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
```

---

## FTMOPA (widening, 2-way, FP8 to FP16) -- A64

### Encoding: `ftmopa_za16_z8z8zi_b2x1`  — FTMOPA

**Assembly:** `FTMOPA  <ZAda>.H, { <Zn1>.B-<Zn2>.B }, <Zm>.B, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (1b) | Is the name of the ZA tile ZA0-ZA1, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) || !IsFeatureImplemented(FEAT_SME_F8F16) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
```

---

## FTMOPA (widening, 4-way) -- A64

### Encoding: `ftmopa_za32_z8z8zi_b2x1`  — FTMOPA

**Assembly:** `FTMOPA  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) || !IsFeatureImplemented(FEAT_SME_F8F32) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
```

---

## FTSMUL -- A64

### Encoding: `ftsmul_z_zz_`  — FTSMUL

**Assembly:** `FTSMUL  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## FTSSEL -- A64

### Encoding: `ftssel_z_zz_`  — FTSSEL

**Assembly:** `FTSSEL  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## FVDOT (FP16 to FP32) -- A64

### Encoding: `fvdot_za_zzi_2xi`  — FVDOT

**Assembly:** `FVDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
```

---

## FVDOT (FP8 to FP16) -- A64

### Encoding: `fvdot_za_z8z8i_2xi`  — FVDOT

**Assembly:** `FVDOT  ZA.H[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the immediate index of a pair of 8-bit elements within each 128-bit vector segment, in the range 0 to 7, encoded in t |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F16) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i3h::i3l);
```

---

## FVDOTB -- A64

### Encoding: `fvdotb_za32_z8z8i_2xi`  — FVDOTB

**Assembly:** `FVDOTB  ZA.S[<Wv>, <offs>, VGx4], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i2h :: i2l) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2h::i2l);
```

---

## FVDOTT -- A64

### Encoding: `fvdott_za32_z8z8i_2xi`  — FVDOTT

**Assembly:** `FVDOTT  ZA.S[<Wv>, <offs>, VGx4], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i2h :: i2l) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_F8F32) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2h::i2l);
```

---

## GCSB -- A64

### Encoding: `GCSB_HD_hints`  — GCSB

**Assembly:** `GCSB  DSYNC`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_GCS) then EndOfDecode(Decode_NOP); end;
```

---

## GCSPOPCX -- A64 *(alias for SYS)*

### Encoding: `GCSPOPCX_SYS_CR_systeminstrs`  — SYS

**Assembly:** `GCSPOPCX / SYS  #0, C7, C7, #5{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `SYS` | — |  |  |

---

## GCSPOPM -- A64 *(alias for SYSL)*

### Encoding: `GCSPOPM_SYSL_RC_systeminstrs`  — SYSL

**Assembly:** `GCSPOPM    {<Xt>} / SYSL  <Xt>, #3, C7, C7, #1`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the optional general-purpose destination register, encoded in the "Rt" field. Defaults to XZR if a |  |
| `SYSL` | — |  |  |

---

## GCSPOPX -- A64 *(alias for SYS)*

### Encoding: `GCSPOPX_SYS_CR_systeminstrs`  — SYS

**Assembly:** `GCSPOPX / SYS  #0, C7, C7, #6{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `SYS` | — |  |  |

---

## GCSPUSHM -- A64 *(alias for SYS)*

### Encoding: `GCSPUSHM_SYS_CR_systeminstrs`  — SYS

**Assembly:** `GCSPUSHM  <Xt> / SYS  #3, C7, C7, #0, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## GCSPUSHX -- A64 *(alias for SYS)*

### Encoding: `GCSPUSHX_SYS_CR_systeminstrs`  — SYS

**Assembly:** `GCSPUSHX / SYS  #0, C7, C7, #4{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `SYS` | — |  |  |

---

## GCSSS1 -- A64 *(alias for SYS)*

### Encoding: `GCSSS1_SYS_CR_systeminstrs`  — SYS

**Assembly:** `GCSSS1  <Xt> / SYS  #3, C7, C7, #2, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## GCSSS2 -- A64 *(alias for SYSL)*

### Encoding: `GCSSS2_SYSL_RC_systeminstrs`  — SYSL

**Assembly:** `GCSSS2  <Xt> / SYSL  <Xt>, #3, C7, C7, #3`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rt" field. |  |
| `SYSL` | — |  |  |

---

## GCSSTR -- A64

### Encoding: `GCSSTR_64_ldst_gcs`  — GCSSTR

**Assembly:** `GCSSTR  <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_GCS) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
```

---

## GCSSTTR -- A64

### Encoding: `GCSSTTR_64_ldst_gcs`  — GCSSTTR

**Assembly:** `GCSSTTR  <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_GCS) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
```

---

## GIC -- A64 *(alias for SYS)*

### Encoding: `GIC_SYS_CR_systeminstrs`  — SYS

**Assembly:** `GIC  <gic_op>{, <Xt>} / SYS  #<op1>, C12, <Cm>, #<op2>{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<gic_op>` | — |  |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the optional general-purpose source register, defaulting to '11111', encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## GICR -- A64 *(alias for SYSL)*

### Encoding: `GICR_SYSL_RC_systeminstrs`  — SYSL

**Assembly:** `GICR  <Xt>, <gicr_op> / SYSL  <Xt>, #0, C12, C3, #<op2>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rt" field. |  |
| `<gicr_op>` | — |  |  |
| `SYSL` | — |  |  |

---

## GMI -- A64

### Encoding: `GMI_64G_dp_2src`  — GMI

**Assembly:** `GMI  <Xd>, <Xn|SP>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## GSB -- A64 *(alias for SYS)*

### Encoding: `GSB_SYS_CR_systeminstrs`  — SYS

**Assembly:** `GSB  <gsb_op> / SYS  #0, C12, C0, #<op2>{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<gsb_op>` | — |  |  |
| `SYS` | — |  |  |

---

## HINT -- A64

### Encoding: `HINT_HM_hints`  — HINT

**Assembly:** `HINT  #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | (CRm :: op2) | Is a 7-bit unsigned immediate, in the range 0 to 127, encoded in the "CRm:op2" field.           The encodings that are a |  |

**Pseudocode (Decode):**
```
var op : SystemHintOp;

var stream : boolean;
var priority : boolean;
case CRm::op2 of
    when '0000 000' => op = SystemHintOp_NOP;
    when '0000 001' => op = SystemHintOp_YIELD;
    when '0000 010' => op = SystemHintOp_WFE;
    when '0000 011' => op = SystemHintOp_WFI;
    when '0000 100' => op = SystemHintOp_SEV;
    when '0000 101' => op = SystemHintOp_SEVL;
    when '0000 110' =>
        if !IsFeatureImplemented(FEAT_DGH) then EndOfDecode(Decode_NOP); end;
        op = SystemHintOp_DGH;
    when '0000 111' => See("XPACLRI");
    when '0001 xxx' =>
        case op2 of
            when '000' => See("PACIA1716");
            when '010' => See("PACIB1716");
            when '100' => See("AUTIA1716");
            when '110' => See("AUTIB1716");
            otherwise => EndOfDecode(Decode_NOP);
        end;
    when '0010 000' =>
        if !IsFeatureImplemented(FEAT_RAS) then EndOfDecode(Decode_NOP); end;
        op = SystemHintOp_ESB;
    when '0010 001' =>
        if !IsFeatureImplemented(FEAT_SPE) then EndOfDecode(Decode_NOP); end;
        op = SystemHintOp_PSB;
    when '0010 010' =>
        if !IsFeatureImplemented(FEAT_TRF) then EndOfDecode(Decode_NOP); end;
        op = SystemHintOp_TSB;
    when '0010 011' =>
        if !IsFeatureImplemented(FEAT_GCS) then EndOfDecode(Decode_NOP); end;
        op = SystemHintOp_GCSB;
    when '0010 100' =>
        op = SystemHintOp_CSDB;
    when '0010 110' =>
        if !IsFeatureImplemented(FEAT_CLRBHB) then EndOfDecode(Decode_NOP); end;
        op = SystemHintOp_CLRBHB;
    when '0011 xxx' =>
        case op2 of
            when '000' => See("PACIAZ");
            when '001' => See("PACIASP");
            when '010' => See("PACIBZ");
            when '011' => See("PACIBSP");
            when '100' => See("AUTIAZ");
            when '101' => See("AUTIASP");
            when '110' => See("AUTIBZ");
            when '111' => See("AUTIBSP");
        end;
    when '0100 xx0' =>
        if !IsFeatureImplemented(FEAT_BTI) then EndOfDecode(Decode_NOP); end;

        // Check branch target compatibility between BTI instruction and PSTATE.BTYPE
        SetBTypeCompatible(BTypeCompatible_BTI(op2[2:1]));
        op = SystemHintOp_BTI;
    when '0100 111' => See("PACM");
    when '0101 000' =>
        if !IsFeatureImplemented(FEAT_CHK) then EndOfDecode(Decode_NOP); end;
        op = SystemHintOp_CHKFEAT;
    when '0110 00x' =>
        if !IsFeatureImplemented(FEAT_PCDPHINT) then EndOfDecode(Decode_NOP); end;
        stream = op2[0] == '1';
        op = SystemHintOp_STSHH;
    when '0110 01x' =>
        if !IsFeatureImplemented(FEAT_CMH) then EndOfDecode(Decode_NOP); end;
        priority = op2[0] == '1';

        op = SystemHintOp_SHUH;
    when '0110 100' =>
        if !IsFeatureImplemented(FEAT_CMH) then EndOfDecode(Decode_NOP); end;
        op = SystemHintOp_STCPH;
    otherwise => EndOfDecode(Decode_NOP);
end;
```

---

## HISTCNT -- A64

### Encoding: `histcnt_z_p_zz_`  — HISTCNT

**Assembly:** `HISTCNT  <Zd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let d : integer = UInt(Zd);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
```

---

## HISTSEG -- A64

### Encoding: `histseg_z_zz_`  — HISTSEG

**Assembly:** `HISTSEG  <Zd>.B, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
if size != '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8;
let d : integer = UInt(Zd);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
```

---

## HLT -- A64

### Encoding: `HLT_EX_exception`  — HLT

**Assembly:** `HLT  #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | imm16 (16b) | Is a 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. |  |

**Pseudocode (Decode):**
```
if EDSCR().HDE == '0' || !HaltingAllowed() then
    EndOfDecode(Decode_UNDEF);
elsif IsFeatureImplemented(FEAT_BTI) then
    SetBTypeCompatible(TRUE);
end;
```

---

## HVC -- A64

### Encoding: `HVC_EX_exception`  — HVC

**Assembly:** `HVC  #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | imm16 (16b) | Is a 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let imm : bits(16) = imm16; |

**Pseudocode (Decode):**
```
if !HaveEL(EL2) then EndOfDecode(Decode_UNDEF); end;
let imm : bits(16) = imm16;
```

---

## IC -- A64 *(alias for SYS)*

### Encoding: `IC_SYS_CR_systeminstrs`  — SYS

**Assembly:** `IC  <ic_op>{, <Xt>} / SYS  #<op1>, C7, <Cm>, #<op2>{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ic_op>` | — |  |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the optional general-purpose source register, defaulting to '11111', encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## INCB, INCD, INCH, INCW (scalar) -- A64

### Encoding: `incb_r_rs_`  — INCB

**Assembly:** `INCB  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `incd_r_rs_`  — INCD

**Assembly:** `INCD  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `inch_r_rs_`  — INCH

**Assembly:** `INCH  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `incw_r_rs_`  — INCW

**Assembly:** `INCW  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

---

## INCD, INCH, INCW (vector) -- A64

### Encoding: `incd_z_zs_`  — INCD

**Assembly:** `INCD  <Zdn>.D{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `inch_z_zs_`  — INCH

**Assembly:** `INCH  <Zdn>.H{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

### Encoding: `incw_z_zs_`  — INCW

**Assembly:** `INCW  <Zdn>.S{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;
```

---

## INCP (scalar) -- A64

### Encoding: `incp_r_p_r_`  — INCP

**Assembly:** `INCP  <Xdn>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
```

---

## INCP (vector) -- A64

### Encoding: `incp_z_p_z_`  — INCP

**Assembly:** `INCP  <Zdn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Zdn);
```

---

## INDEX (immediate, scalar) -- A64

### Encoding: `index_z_ir_`  — INDEX

**Assembly:** `INDEX  <Zd>.<T>, #<imm>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<imm>` | imm5 (5b) | Is the signed immediate operand, in the range -16 to 15, encoded in the "imm5" field. | let imm : integer = SInt(imm5); |
| `<R>` | — |  |  |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Rm);
let d : integer = UInt(Zd);
let imm : integer = SInt(imm5);
```

---

## INDEX (immediates) -- A64

### Encoding: `index_z_ii_`  — INDEX

**Assembly:** `INDEX  <Zd>.<T>, #<imm1>, #<imm2>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<imm1>` | imm5 (5b) | Is the first signed immediate operand, in the range -16 to 15, encoded in the "imm5" field. | let imm1 : integer = SInt(imm5); |
| `<imm2>` | imm5b (5b) | Is the second signed immediate operand, in the range -16 to 15, encoded in the "imm5b" field. | let imm2 : integer = SInt(imm5b); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt(Zd);
let imm1 : integer = SInt(imm5);
let imm2 : integer = SInt(imm5b);
```

---

## INDEX (scalar, immediate) -- A64

### Encoding: `index_z_ri_`  — INDEX

**Assembly:** `INDEX  <Zd>.<T>, <R><n>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm5 (5b) | Is the signed immediate operand, in the range -16 to 15, encoded in the "imm5" field. | let imm : integer = SInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Rn);
let d : integer = UInt(Zd);
let imm : integer = SInt(imm5);
```

---

## INDEX (scalars) -- A64

### Encoding: `index_z_rr_`  — INDEX

**Assembly:** `INDEX  <Zd>.<T>, <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Zd);
```

---

## INS (element) -- A64

### Encoding: `INS_asimdins_IV_v`  — INS

**Assembly:** `INS  <Vd>.<Ts>[<index1>], <Vn>.<Ts>[<index2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ts>` | — |  |  |
| `<index1>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<index2>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'x0000' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = LowestSetBitNZ(imm5[3:0]);

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let dst_index : integer = UInt(imm5[4:size+1]);
let src_index : integer = UInt(imm4[3:size]);
let idxdsize : integer{} = 64 << UInt(imm4[3]);
// imm4<size-1:0> is IGNORED
let esize : integer{} = 8 << size;
```

---

## INS (general) -- A64

### Encoding: `INS_asimdins_IR_r`  — INS

**Assembly:** `INS  <Vd>.<Ts>[<index>], <R><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the general-purpose source register or ZR (31), encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'x0000' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = LowestSetBitNZ(imm5[3:0]);

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let index : integer = UInt(imm5[4:size+1]);
let esize : integer{} = 8 << size;
```

---

## INSR (SIMD&FP scalar) -- A64

### Encoding: `insr_z_v_`  — INSR

**Assembly:** `INSR  <Zdn>.<T>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<V>` | — |  |  |
| `<m>` | Vm (5b) | Is the number [0-31] of the source SIMD&FP register, encoded in the "Vm" field. | let m : integer = UInt(Vm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Vm);
```

---

## INSR (scalar) -- A64

### Encoding: `insr_z_r_`  — INSR

**Assembly:** `INSR  <Zdn>.<T>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Rm);
```

---

## IRG -- A64

### Encoding: `IRG_64I_dp_2src`  — IRG

**Assembly:** `IRG  <Xd|SP>, <Xn|SP>{, <Xm>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. Defaults to XZR if absent. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## ISB -- A64

### Encoding: `ISB_BI_barriers`  — ISB

**Assembly:** `ISB  {<option>|#<imm>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<option>` | CRm (4b) | Specifies an optional limitation on the barrier operation. Values are:                                       SY          |  |
| `<imm>` | CRm (4b) | Is an optional 4-bit unsigned immediate, in the range 0 to 15, defaulting to 15 and encoded in the "CRm" field. |  |

**Pseudocode (Decode):**
```
// no additional decoding required
```

---

## LASTA (SIMD&FP scalar) -- A64

### Encoding: `lasta_v_p_z_`  — LASTA

**Assembly:** `LASTA  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let isBefore : boolean = FALSE;
```

---

## LASTA (scalar) -- A64

### Encoding: `lasta_r_p_z_`  — LASTA

**Assembly:** `LASTA  <R><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<R>` | — |  |  |
| `<d>` | Rd (5b) | Is the number [0-30] of the destination general-purpose register or the name ZR (31), encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = if esize < 64 then 32 else 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Rd);
let isBefore : boolean = FALSE;
```

---

## LASTB (SIMD&FP scalar) -- A64

### Encoding: `lastb_v_p_z_`  — LASTB

**Assembly:** `LASTB  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let isBefore : boolean = TRUE;
```

---

## LASTB (scalar) -- A64

### Encoding: `lastb_r_p_z_`  — LASTB

**Assembly:** `LASTB  <R><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<R>` | — |  |  |
| `<d>` | Rd (5b) | Is the number [0-30] of the destination general-purpose register or the name ZR (31), encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = if esize < 64 then 32 else 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Rd);
let isBefore : boolean = TRUE;
```

---

## LASTP -- A64

### Encoding: `lastp_r_p_p_`  — LASTP

**Assembly:** `LASTP  <Xd>, <Pg>, <Pn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let d : integer = UInt(Rd);
```

---

## LD1 (multiple structures) -- A64

### Encoding: `LD1_asisdlse_R1_1v`  [One register]  — LD1

**Assembly:** `LD1  { <Vt>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlse_R2_2v`  [Two registers]  — LD1

**Assembly:** `LD1  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlse_R3_3v`  [Three registers]  — LD1

**Assembly:** `LD1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlse_R4_4v`  [Four registers]  — LD1

**Assembly:** `LD1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsep_I1_i1`  [One register, immediate offset]  — LD1

**Assembly:** `LD1  { <Vt>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsep_R1_r1`  [One register, register offset]  — LD1

**Assembly:** `LD1  { <Vt>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsep_I2_i2`  [Two registers, immediate offset]  — LD1

**Assembly:** `LD1  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsep_R2_r2`  [Two registers, register offset]  — LD1

**Assembly:** `LD1  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsep_I3_i3`  [Three registers, immediate offset]  — LD1

**Assembly:** `LD1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsep_R3_r3`  [Three registers, register offset]  — LD1

**Assembly:** `LD1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsep_I4_i4`  [Four registers, immediate offset]  — LD1

**Assembly:** `LD1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsep_R4_r4`  [Four registers, register offset]  — LD1

**Assembly:** `LD1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD1 (single structure) -- A64

### Encoding: `LD1_asisdlso_B1_1b`  [8-bit]  — LD1

**Assembly:** `LD1  { <Vt>.B }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlso_H1_1h`  [16-bit]  — LD1

**Assembly:** `LD1  { <Vt>.H }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlso_S1_1s`  [32-bit]  — LD1

**Assembly:** `LD1  { <Vt>.S }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlso_D1_1d`  [64-bit]  — LD1

**Assembly:** `LD1  { <Vt>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsop_B1_i1b`  [8-bit, immediate offset]  — LD1

**Assembly:** `LD1  { <Vt>.B }[<index>], [<Xn|SP>], #1`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsop_BX1_r1b`  [8-bit, register offset]  — LD1

**Assembly:** `LD1  { <Vt>.B }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsop_D1_i1d`  [64-bit, immediate offset]  — LD1

**Assembly:** `LD1  { <Vt>.D }[<index>], [<Xn|SP>], #8`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsop_DX1_r1d`  [64-bit, register offset]  — LD1

**Assembly:** `LD1  { <Vt>.D }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsop_H1_i1h`  [16-bit, immediate offset]  — LD1

**Assembly:** `LD1  { <Vt>.H }[<index>], [<Xn|SP>], #2`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsop_HX1_r1h`  [16-bit, register offset]  — LD1

**Assembly:** `LD1  { <Vt>.H }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsop_S1_i1s`  [32-bit, immediate offset]  — LD1

**Assembly:** `LD1  { <Vt>.S }[<index>], [<Xn|SP>], #4`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1_asisdlsop_SX1_r1s`  [32-bit, register offset]  — LD1

**Assembly:** `LD1  { <Vt>.S }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD1B (scalar plus immediate, consecutive registers) -- A64

### Encoding: `ld1b_mz_p_bi_2`  — LD1B

**Assembly:** `LD1B  { <Zt1>.B-<Zt2>.B }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1b_mz_p_bi_4`  — LD1B

**Assembly:** `LD1B  { <Zt1>.B-<Zt4>.B }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## LD1B (scalar plus immediate, single register) -- A64

### Encoding: `ld1b_z_p_bi_u8`  — LD1B

**Assembly:** `LD1B  { <Zt>.B }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1b_z_p_bi_u16`  — LD1B

**Assembly:** `LD1B  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1b_z_p_bi_u32`  — LD1B

**Assembly:** `LD1B  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1b_z_p_bi_u64`  — LD1B

**Assembly:** `LD1B  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

---

## LD1B (scalar plus immediate, strided registers) -- A64

### Encoding: `ld1b_mzx_p_bi_2x8`  — LD1B

**Assembly:** `LD1B  { <Zt1>.B, <Zt2>.B }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1b_mzx_p_bi_4x4`  — LD1B

**Assembly:** `LD1B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## LD1B (scalar plus scalar, consecutive registers) -- A64

### Encoding: `ld1b_mz_p_br_2`  — LD1B

**Assembly:** `LD1B  { <Zt1>.B-<Zt2>.B }, <PNg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 8;
```

### Encoding: `ld1b_mz_p_br_4`  — LD1B

**Assembly:** `LD1B  { <Zt1>.B-<Zt4>.B }, <PNg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 8;
```

---

## LD1B (scalar plus scalar, single register) -- A64

### Encoding: `ld1b_z_p_br_u8`  — LD1B

**Assembly:** `LD1B  { <Zt>.B }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

### Encoding: `ld1b_z_p_br_u16`  — LD1B

**Assembly:** `LD1B  { <Zt>.H }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

### Encoding: `ld1b_z_p_br_u32`  — LD1B

**Assembly:** `LD1B  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

### Encoding: `ld1b_z_p_br_u64`  — LD1B

**Assembly:** `LD1B  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

---

## LD1B (scalar plus scalar, strided registers) -- A64

### Encoding: `ld1b_mzx_p_br_2x8`  — LD1B

**Assembly:** `LD1B  { <Zt1>.B, <Zt2>.B }, <PNg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 8;
```

### Encoding: `ld1b_mzx_p_br_4x4`  — LD1B

**Assembly:** `LD1B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <PNg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 8;
```

---

## LD1B (scalar plus scalar, tile slice) -- A64

### Encoding: `ld1b_za_p_rrr_`  — LD1B

**Assembly:** `LD1B  { ZA0<HV>.B[<Ws>, <offs>] }, <Pg>/Z, [<Xn|SP>{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off4 (4b) | Is the slice index offset, in the range 0 to 15, encoded in the "off4" field. | let offset : integer = UInt(off4); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = 0;
let offset : integer = UInt(off4);
let esize : integer{} = 8;
let vertical : boolean = V == '1';
```

---

## LD1B (scalar plus vector) -- A64

### Encoding: `ld1b_z_p_bz_d_x32_unscaled`  — LD1B

**Assembly:** `LD1B  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1b_z_p_bz_s_x32_unscaled`  — LD1B

**Assembly:** `LD1B  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1b_z_p_bz_d_64_unscaled`  — LD1B

**Assembly:** `LD1B  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LD1B (vector plus immediate) -- A64

### Encoding: `ld1b_z_p_ai_s`  — LD1B

**Assembly:** `LD1B  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

### Encoding: `ld1b_z_p_ai_d`  — LD1B

**Assembly:** `LD1B  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

---

## LD1D (scalar plus immediate, consecutive registers) -- A64

### Encoding: `ld1d_mz_p_bi_2`  — LD1D

**Assembly:** `LD1D  { <Zt1>.D-<Zt2>.D }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1d_mz_p_bi_4`  — LD1D

**Assembly:** `LD1D  { <Zt1>.D-<Zt4>.D }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## LD1D (scalar plus immediate, single register) -- A64

### Encoding: `ld1d_z_p_bi_u64`  — LD1D

**Assembly:** `LD1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1d_z_p_bi_u128`  — LD1D

**Assembly:** `LD1D  { <Zt>.Q }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

---

## LD1D (scalar plus immediate, strided registers) -- A64

### Encoding: `ld1d_mzx_p_bi_2x8`  — LD1D

**Assembly:** `LD1D  { <Zt1>.D, <Zt2>.D }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1d_mzx_p_bi_4x4`  — LD1D

**Assembly:** `LD1D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## LD1D (scalar plus scalar, consecutive registers) -- A64

### Encoding: `ld1d_mz_p_br_2`  — LD1D

**Assembly:** `LD1D  { <Zt1>.D-<Zt2>.D }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 64;
```

### Encoding: `ld1d_mz_p_br_4`  — LD1D

**Assembly:** `LD1D  { <Zt1>.D-<Zt4>.D }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 64;
```

---

## LD1D (scalar plus scalar, single register) -- A64

### Encoding: `ld1d_z_p_br_u64`  — LD1D

**Assembly:** `LD1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
```

### Encoding: `ld1d_z_p_br_u128`  — LD1D

**Assembly:** `LD1D  { <Zt>.Q }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
```

---

## LD1D (scalar plus scalar, strided registers) -- A64

### Encoding: `ld1d_mzx_p_br_2x8`  — LD1D

**Assembly:** `LD1D  { <Zt1>.D, <Zt2>.D }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 64;
```

### Encoding: `ld1d_mzx_p_br_4x4`  — LD1D

**Assembly:** `LD1D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 64;
```

---

## LD1D (scalar plus scalar, tile slice) -- A64

### Encoding: `ld1d_za_p_rrr_`  — LD1D

**Assembly:** `LD1D  { <ZAt><HV>.D[<Ws>, <offs>] }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #3}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAt>` | ZAt (3b) | Is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAt" field. | let t : integer = UInt(ZAt); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | o1 (1b) | Is the slice index offset, in the range 0 to 1, encoded in the "o1" field. | let offset : integer = UInt(o1); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = UInt(ZAt);
let offset : integer = UInt(o1);
let esize : integer{} = 64;
let vertical : boolean = V == '1';
```

---

## LD1D (scalar plus vector) -- A64

### Encoding: `ld1d_z_p_bz_d_x32_scaled`  — LD1D

**Assembly:** `LD1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 3;
```

### Encoding: `ld1d_z_p_bz_d_x32_unscaled`  — LD1D

**Assembly:** `LD1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1d_z_p_bz_d_64_scaled`  — LD1D

**Assembly:** `LD1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 3;
```

### Encoding: `ld1d_z_p_bz_d_64_unscaled`  — LD1D

**Assembly:** `LD1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LD1D (vector plus immediate) -- A64

### Encoding: `ld1d_z_p_ai_d`  — LD1D

**Assembly:** `LD1D  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 8 in the range 0 to 248, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

---

## LD1H (scalar plus immediate, consecutive registers) -- A64

### Encoding: `ld1h_mz_p_bi_2`  — LD1H

**Assembly:** `LD1H  { <Zt1>.H-<Zt2>.H }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1h_mz_p_bi_4`  — LD1H

**Assembly:** `LD1H  { <Zt1>.H-<Zt4>.H }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## LD1H (scalar plus immediate, single register) -- A64

### Encoding: `ld1h_z_p_bi_u16`  — LD1H

**Assembly:** `LD1H  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1h_z_p_bi_u32`  — LD1H

**Assembly:** `LD1H  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1h_z_p_bi_u64`  — LD1H

**Assembly:** `LD1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

---

## LD1H (scalar plus immediate, strided registers) -- A64

### Encoding: `ld1h_mzx_p_bi_2x8`  — LD1H

**Assembly:** `LD1H  { <Zt1>.H, <Zt2>.H }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1h_mzx_p_bi_4x4`  — LD1H

**Assembly:** `LD1H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## LD1H (scalar plus scalar, consecutive registers) -- A64

### Encoding: `ld1h_mz_p_br_2`  — LD1H

**Assembly:** `LD1H  { <Zt1>.H-<Zt2>.H }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 16;
```

### Encoding: `ld1h_mz_p_br_4`  — LD1H

**Assembly:** `LD1H  { <Zt1>.H-<Zt4>.H }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 16;
```

---

## LD1H (scalar plus scalar, single register) -- A64

### Encoding: `ld1h_z_p_br_u16`  — LD1H

**Assembly:** `LD1H  { <Zt>.H }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
```

### Encoding: `ld1h_z_p_br_u32`  — LD1H

**Assembly:** `LD1H  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
```

### Encoding: `ld1h_z_p_br_u64`  — LD1H

**Assembly:** `LD1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
```

---

## LD1H (scalar plus scalar, strided registers) -- A64

### Encoding: `ld1h_mzx_p_br_2x8`  — LD1H

**Assembly:** `LD1H  { <Zt1>.H, <Zt2>.H }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 16;
```

### Encoding: `ld1h_mzx_p_br_4x4`  — LD1H

**Assembly:** `LD1H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 16;
```

---

## LD1H (scalar plus scalar, tile slice) -- A64

### Encoding: `ld1h_za_p_rrr_`  — LD1H

**Assembly:** `LD1H  { <ZAt><HV>.H[<Ws>, <offs>] }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #1}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAt>` | ZAt (1b) | Is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAt" field. | let t : integer = UInt(ZAt); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off3 (3b) | Is the slice index offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = UInt(ZAt);
let offset : integer = UInt(off3);
let esize : integer{} = 16;
let vertical : boolean = V == '1';
```

---

## LD1H (scalar plus vector) -- A64

### Encoding: `ld1h_z_p_bz_s_x32_scaled`  — LD1H

**Assembly:** `LD1H  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `ld1h_z_p_bz_d_x32_scaled`  — LD1H

**Assembly:** `LD1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `ld1h_z_p_bz_d_x32_unscaled`  — LD1H

**Assembly:** `LD1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1h_z_p_bz_s_x32_unscaled`  — LD1H

**Assembly:** `LD1H  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1h_z_p_bz_d_64_scaled`  — LD1H

**Assembly:** `LD1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 1;
```

### Encoding: `ld1h_z_p_bz_d_64_unscaled`  — LD1H

**Assembly:** `LD1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LD1H (vector plus immediate) -- A64

### Encoding: `ld1h_z_p_ai_s`  — LD1H

**Assembly:** `LD1H  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

### Encoding: `ld1h_z_p_ai_d`  — LD1H

**Assembly:** `LD1H  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

---

## LD1Q -- A64

### Encoding: `ld1q_z_p_ar_d_64_unscaled`  — LD1Q

**Assembly:** `LD1Q  { <Zt>.Q }, <Pg>/Z, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
```

---

## LD1Q -- A64

### Encoding: `ld1q_za_p_rrr_`  — LD1Q

**Assembly:** `LD1Q  { <ZAt><HV>.Q[<Ws>, <offs>] }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #4}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAt>` | ZAt (4b) | Is the name of the ZA tile ZA0-ZA15 to be accessed, encoded in the "ZAt" field. | let t : integer = UInt(ZAt); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | — | Is the slice index offset, with implicit value 0. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = UInt(ZAt);
let offset : integer = 0;
let esize : integer{} = 128;
let vertical : boolean = V == '1';
```

---

## LD1R -- A64

### Encoding: `LD1R_asisdlso_R1`  — LD1R

**Assembly:** `LD1R  { <Vt>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1R_asisdlsop_R1_i`  [Immediate offset]  — LD1R

**Assembly:** `LD1R  { <Vt>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD1R_asisdlsop_RX1_r`  [Register offset]  — LD1R

**Assembly:** `LD1R  { <Vt>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD1RB -- A64

### Encoding: `ld1rb_z_p_bi_u8`  — LD1RB

**Assembly:** `LD1RB  { <Zt>.B }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, in the range 0 to 63, defaulting to 0, encoded in the "imm6" field. | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

### Encoding: `ld1rb_z_p_bi_u16`  — LD1RB

**Assembly:** `LD1RB  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, in the range 0 to 63, defaulting to 0, encoded in the "imm6" field. | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

### Encoding: `ld1rb_z_p_bi_u32`  — LD1RB

**Assembly:** `LD1RB  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, in the range 0 to 63, defaulting to 0, encoded in the "imm6" field. | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

### Encoding: `ld1rb_z_p_bi_u64`  — LD1RB

**Assembly:** `LD1RB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, in the range 0 to 63, defaulting to 0, encoded in the "imm6" field. | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

---

## LD1RD -- A64

### Encoding: `ld1rd_z_p_bi_u64`  — LD1RD

**Assembly:** `LD1RD  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, a multiple of 8 in the range 0 to 504, defaulting to 0, encoded in the " | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

---

## LD1RH -- A64

### Encoding: `ld1rh_z_p_bi_u16`  — LD1RH

**Assembly:** `LD1RH  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 126, defaulting to 0, encoded in the " | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

### Encoding: `ld1rh_z_p_bi_u32`  — LD1RH

**Assembly:** `LD1RH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 126, defaulting to 0, encoded in the " | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

### Encoding: `ld1rh_z_p_bi_u64`  — LD1RH

**Assembly:** `LD1RH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 126, defaulting to 0, encoded in the " | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

---

## LD1ROB (scalar plus immediate) -- A64

### Encoding: `ld1rob_z_p_bi_u8`  — LD1ROB

**Assembly:** `LD1ROB  { <Zt>.B }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate byte offset, a multiple of 32 in the range -256 to 224, defaulting to 0, encoded in the | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## LD1ROB (scalar plus scalar) -- A64

### Encoding: `ld1rob_z_p_br_contiguous`  — LD1ROB

**Assembly:** `LD1ROB  { <Zt>.B }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
```

---

## LD1ROD (scalar plus immediate) -- A64

### Encoding: `ld1rod_z_p_bi_u64`  — LD1ROD

**Assembly:** `LD1ROD  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate byte offset, a multiple of 32 in the range -256 to 224, defaulting to 0, encoded in the | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## LD1ROD (scalar plus scalar) -- A64

### Encoding: `ld1rod_z_p_br_contiguous`  — LD1ROD

**Assembly:** `LD1ROD  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
```

---

## LD1ROH (scalar plus immediate) -- A64

### Encoding: `ld1roh_z_p_bi_u16`  — LD1ROH

**Assembly:** `LD1ROH  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate byte offset, a multiple of 32 in the range -256 to 224, defaulting to 0, encoded in the | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## LD1ROH (scalar plus scalar) -- A64

### Encoding: `ld1roh_z_p_br_contiguous`  — LD1ROH

**Assembly:** `LD1ROH  { <Zt>.H }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
```

---

## LD1ROW (scalar plus immediate) -- A64

### Encoding: `ld1row_z_p_bi_u32`  — LD1ROW

**Assembly:** `LD1ROW  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate byte offset, a multiple of 32 in the range -256 to 224, defaulting to 0, encoded in the | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## LD1ROW (scalar plus scalar) -- A64

### Encoding: `ld1row_z_p_br_contiguous`  — LD1ROW

**Assembly:** `LD1ROW  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
```

---

## LD1RQB (scalar plus immediate) -- A64

### Encoding: `ld1rqb_z_p_bi_u8`  — LD1RQB

**Assembly:** `LD1RQB  { <Zt>.B }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate byte offset, a multiple of 16 in the range -128 to 112, defaulting to 0, encoded in the | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## LD1RQB (scalar plus scalar) -- A64

### Encoding: `ld1rqb_z_p_br_contiguous`  — LD1RQB

**Assembly:** `LD1RQB  { <Zt>.B }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
```

---

## LD1RQD (scalar plus immediate) -- A64

### Encoding: `ld1rqd_z_p_bi_u64`  — LD1RQD

**Assembly:** `LD1RQD  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate byte offset, a multiple of 16 in the range -128 to 112, defaulting to 0, encoded in the | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## LD1RQD (scalar plus scalar) -- A64

### Encoding: `ld1rqd_z_p_br_contiguous`  — LD1RQD

**Assembly:** `LD1RQD  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
```

---

## LD1RQH (scalar plus immediate) -- A64

### Encoding: `ld1rqh_z_p_bi_u16`  — LD1RQH

**Assembly:** `LD1RQH  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate byte offset, a multiple of 16 in the range -128 to 112, defaulting to 0, encoded in the | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## LD1RQH (scalar plus scalar) -- A64

### Encoding: `ld1rqh_z_p_br_contiguous`  — LD1RQH

**Assembly:** `LD1RQH  { <Zt>.H }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
```

---

## LD1RQW (scalar plus immediate) -- A64

### Encoding: `ld1rqw_z_p_bi_u32`  — LD1RQW

**Assembly:** `LD1RQW  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate byte offset, a multiple of 16 in the range -128 to 112, defaulting to 0, encoded in the | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## LD1RQW (scalar plus scalar) -- A64

### Encoding: `ld1rqw_z_p_br_contiguous`  — LD1RQW

**Assembly:** `LD1RQW  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
```

---

## LD1RSB -- A64

### Encoding: `ld1rsb_z_p_bi_s16`  — LD1RSB

**Assembly:** `LD1RSB  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, in the range 0 to 63, defaulting to 0, encoded in the "imm6" field. | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm6);
```

### Encoding: `ld1rsb_z_p_bi_s32`  — LD1RSB

**Assembly:** `LD1RSB  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, in the range 0 to 63, defaulting to 0, encoded in the "imm6" field. | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm6);
```

### Encoding: `ld1rsb_z_p_bi_s64`  — LD1RSB

**Assembly:** `LD1RSB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, in the range 0 to 63, defaulting to 0, encoded in the "imm6" field. | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm6);
```

---

## LD1RSH -- A64

### Encoding: `ld1rsh_z_p_bi_s32`  — LD1RSH

**Assembly:** `LD1RSH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 126, defaulting to 0, encoded in the " | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm6);
```

### Encoding: `ld1rsh_z_p_bi_s64`  — LD1RSH

**Assembly:** `LD1RSH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 126, defaulting to 0, encoded in the " | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm6);
```

---

## LD1RSW -- A64

### Encoding: `ld1rsw_z_p_bi_s64`  — LD1RSW

**Assembly:** `LD1RSW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 252, defaulting to 0, encoded in the " | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm6);
```

---

## LD1RW -- A64

### Encoding: `ld1rw_z_p_bi_u32`  — LD1RW

**Assembly:** `LD1RW  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 252, defaulting to 0, encoded in the " | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

### Encoding: `ld1rw_z_p_bi_u64`  — LD1RW

**Assembly:** `LD1RW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 252, defaulting to 0, encoded in the " | let offset : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm6);
```

---

## LD1SB (scalar plus immediate) -- A64

### Encoding: `ld1sb_z_p_bi_s16`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1sb_z_p_bi_s32`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1sb_z_p_bi_s64`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

---

## LD1SB (scalar plus scalar) -- A64

### Encoding: `ld1sb_z_p_br_s16`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.H }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
```

### Encoding: `ld1sb_z_p_br_s32`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
```

### Encoding: `ld1sb_z_p_br_s64`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
```

---

## LD1SB (scalar plus vector) -- A64

### Encoding: `ld1sb_z_p_bz_d_x32_unscaled`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1sb_z_p_bz_s_x32_unscaled`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1sb_z_p_bz_d_64_unscaled`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LD1SB (vector plus immediate) -- A64

### Encoding: `ld1sb_z_p_ai_s`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

### Encoding: `ld1sb_z_p_ai_d`  — LD1SB

**Assembly:** `LD1SB  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

---

## LD1SH (scalar plus immediate) -- A64

### Encoding: `ld1sh_z_p_bi_s32`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1sh_z_p_bi_s64`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

---

## LD1SH (scalar plus scalar) -- A64

### Encoding: `ld1sh_z_p_br_s32`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
```

### Encoding: `ld1sh_z_p_br_s64`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
```

---

## LD1SH (scalar plus vector) -- A64

### Encoding: `ld1sh_z_p_bz_s_x32_scaled`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `ld1sh_z_p_bz_d_x32_scaled`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `ld1sh_z_p_bz_d_x32_unscaled`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1sh_z_p_bz_s_x32_unscaled`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1sh_z_p_bz_d_64_scaled`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 1;
```

### Encoding: `ld1sh_z_p_bz_d_64_unscaled`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LD1SH (vector plus immediate) -- A64

### Encoding: `ld1sh_z_p_ai_s`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

### Encoding: `ld1sh_z_p_ai_d`  — LD1SH

**Assembly:** `LD1SH  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

---

## LD1SW (scalar plus immediate) -- A64

### Encoding: `ld1sw_z_p_bi_s64`  — LD1SW

**Assembly:** `LD1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

---

## LD1SW (scalar plus scalar) -- A64

### Encoding: `ld1sw_z_p_br_s64`  — LD1SW

**Assembly:** `LD1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = FALSE;
```

---

## LD1SW (scalar plus vector) -- A64

### Encoding: `ld1sw_z_p_bz_d_x32_scaled`  — LD1SW

**Assembly:** `LD1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 2;
```

### Encoding: `ld1sw_z_p_bz_d_x32_unscaled`  — LD1SW

**Assembly:** `LD1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1sw_z_p_bz_d_64_scaled`  — LD1SW

**Assembly:** `LD1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 2;
```

### Encoding: `ld1sw_z_p_bz_d_64_unscaled`  — LD1SW

**Assembly:** `LD1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LD1SW (vector plus immediate) -- A64

### Encoding: `ld1sw_z_p_ai_d`  — LD1SW

**Assembly:** `LD1SW  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

---

## LD1W (scalar plus immediate, consecutive registers) -- A64

### Encoding: `ld1w_mz_p_bi_2`  — LD1W

**Assembly:** `LD1W  { <Zt1>.S-<Zt2>.S }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1w_mz_p_bi_4`  — LD1W

**Assembly:** `LD1W  { <Zt1>.S-<Zt4>.S }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## LD1W (scalar plus immediate, single register) -- A64

### Encoding: `ld1w_z_p_bi_u32`  — LD1W

**Assembly:** `LD1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1w_z_p_bi_u64`  — LD1W

**Assembly:** `LD1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1w_z_p_bi_u128`  — LD1W

**Assembly:** `LD1W  { <Zt>.Q }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

---

## LD1W (scalar plus immediate, strided registers) -- A64

### Encoding: `ld1w_mzx_p_bi_2x8`  — LD1W

**Assembly:** `LD1W  { <Zt1>.S, <Zt2>.S }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

### Encoding: `ld1w_mzx_p_bi_4x4`  — LD1W

**Assembly:** `LD1W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## LD1W (scalar plus scalar, consecutive registers) -- A64

### Encoding: `ld1w_mz_p_br_2`  — LD1W

**Assembly:** `LD1W  { <Zt1>.S-<Zt2>.S }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 32;
```

### Encoding: `ld1w_mz_p_br_4`  — LD1W

**Assembly:** `LD1W  { <Zt1>.S-<Zt4>.S }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 32;
```

---

## LD1W (scalar plus scalar, single register) -- A64

### Encoding: `ld1w_z_p_br_u32`  — LD1W

**Assembly:** `LD1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
```

### Encoding: `ld1w_z_p_br_u64`  — LD1W

**Assembly:** `LD1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
```

### Encoding: `ld1w_z_p_br_u128`  — LD1W

**Assembly:** `LD1W  { <Zt>.Q }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
```

---

## LD1W (scalar plus scalar, strided registers) -- A64

### Encoding: `ld1w_mzx_p_br_2x8`  — LD1W

**Assembly:** `LD1W  { <Zt1>.S, <Zt2>.S }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 32;
```

### Encoding: `ld1w_mzx_p_br_4x4`  — LD1W

**Assembly:** `LD1W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 32;
```

---

## LD1W (scalar plus scalar, tile slice) -- A64

### Encoding: `ld1w_za_p_rrr_`  — LD1W

**Assembly:** `LD1W  { <ZAt><HV>.S[<Ws>, <offs>] }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #2}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAt>` | ZAt (2b) | Is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAt" field. | let t : integer = UInt(ZAt); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off2 (2b) | Is the slice index offset, in the range 0 to 3, encoded in the "off2" field. | let offset : integer = UInt(off2); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = UInt(ZAt);
let offset : integer = UInt(off2);
let esize : integer{} = 32;
let vertical : boolean = V == '1';
```

---

## LD1W (scalar plus vector) -- A64

### Encoding: `ld1w_z_p_bz_s_x32_scaled`  — LD1W

**Assembly:** `LD1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 2;
```

### Encoding: `ld1w_z_p_bz_d_x32_scaled`  — LD1W

**Assembly:** `LD1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 2;
```

### Encoding: `ld1w_z_p_bz_d_x32_unscaled`  — LD1W

**Assembly:** `LD1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1w_z_p_bz_s_x32_unscaled`  — LD1W

**Assembly:** `LD1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ld1w_z_p_bz_d_64_scaled`  — LD1W

**Assembly:** `LD1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 2;
```

### Encoding: `ld1w_z_p_bz_d_64_unscaled`  — LD1W

**Assembly:** `LD1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LD1W (vector plus immediate) -- A64

### Encoding: `ld1w_z_p_ai_s`  — LD1W

**Assembly:** `LD1W  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

### Encoding: `ld1w_z_p_ai_d`  — LD1W

**Assembly:** `LD1W  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

---

## LD2 (multiple structures) -- A64

### Encoding: `LD2_asisdlse_R2`  — LD2

**Assembly:** `LD2  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsep_I2_i`  [Immediate offset]  — LD2

**Assembly:** `LD2  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsep_R2_r`  [Register offset]  — LD2

**Assembly:** `LD2  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD2 (single structure) -- A64

### Encoding: `LD2_asisdlso_B2_2b`  [8-bit]  — LD2

**Assembly:** `LD2  { <Vt>.B, <Vt2>.B }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlso_H2_2h`  [16-bit]  — LD2

**Assembly:** `LD2  { <Vt>.H, <Vt2>.H }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlso_S2_2s`  [32-bit]  — LD2

**Assembly:** `LD2  { <Vt>.S, <Vt2>.S }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlso_D2_2d`  [64-bit]  — LD2

**Assembly:** `LD2  { <Vt>.D, <Vt2>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsop_B2_i2b`  [8-bit, immediate offset]  — LD2

**Assembly:** `LD2  { <Vt>.B, <Vt2>.B }[<index>], [<Xn|SP>], #2`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsop_BX2_r2b`  [8-bit, register offset]  — LD2

**Assembly:** `LD2  { <Vt>.B, <Vt2>.B }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsop_H2_i2h`  [16-bit, immediate offset]  — LD2

**Assembly:** `LD2  { <Vt>.H, <Vt2>.H }[<index>], [<Xn|SP>], #4`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsop_HX2_r2h`  [16-bit, register offset]  — LD2

**Assembly:** `LD2  { <Vt>.H, <Vt2>.H }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsop_S2_i2s`  [32-bit, immediate offset]  — LD2

**Assembly:** `LD2  { <Vt>.S, <Vt2>.S }[<index>], [<Xn|SP>], #8`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsop_SX2_r2s`  [32-bit, register offset]  — LD2

**Assembly:** `LD2  { <Vt>.S, <Vt2>.S }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsop_D2_i2d`  [64-bit, immediate offset]  — LD2

**Assembly:** `LD2  { <Vt>.D, <Vt2>.D }[<index>], [<Xn|SP>], #16`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2_asisdlsop_DX2_r2d`  [64-bit, register offset]  — LD2

**Assembly:** `LD2  { <Vt>.D, <Vt2>.D }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD2B (scalar plus immediate) -- A64

### Encoding: `ld2b_z_p_bi_contiguous`  — LD2B

**Assembly:** `LD2B  { <Zt1>.B, <Zt2>.B }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## LD2B (scalar plus scalar) -- A64

### Encoding: `ld2b_z_p_br_contiguous`  — LD2B

**Assembly:** `LD2B  { <Zt1>.B, <Zt2>.B }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let nreg : integer{} = 2;
```

---

## LD2D (scalar plus immediate) -- A64

### Encoding: `ld2d_z_p_bi_contiguous`  — LD2D

**Assembly:** `LD2D  { <Zt1>.D, <Zt2>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## LD2D (scalar plus scalar) -- A64

### Encoding: `ld2d_z_p_br_contiguous`  — LD2D

**Assembly:** `LD2D  { <Zt1>.D, <Zt2>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let nreg : integer{} = 2;
```

---

## LD2H (scalar plus immediate) -- A64

### Encoding: `ld2h_z_p_bi_contiguous`  — LD2H

**Assembly:** `LD2H  { <Zt1>.H, <Zt2>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## LD2H (scalar plus scalar) -- A64

### Encoding: `ld2h_z_p_br_contiguous`  — LD2H

**Assembly:** `LD2H  { <Zt1>.H, <Zt2>.H }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let nreg : integer{} = 2;
```

---

## LD2Q (scalar plus immediate) -- A64

### Encoding: `ld2q_z_p_bi_contiguous`  — LD2Q

**Assembly:** `LD2Q  { <Zt1>.Q, <Zt2>.Q }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## LD2Q (scalar plus scalar) -- A64

### Encoding: `ld2q_z_p_br_contiguous`  — LD2Q

**Assembly:** `LD2Q  { <Zt1>.Q, <Zt2>.Q }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #4]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let nreg : integer{} = 2;
```

---

## LD2R -- A64

### Encoding: `LD2R_asisdlso_R2`  — LD2R

**Assembly:** `LD2R  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2R_asisdlsop_R2_i`  [Immediate offset]  — LD2R

**Assembly:** `LD2R  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD2R_asisdlsop_RX2_r`  [Register offset]  — LD2R

**Assembly:** `LD2R  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD2W (scalar plus immediate) -- A64

### Encoding: `ld2w_z_p_bi_contiguous`  — LD2W

**Assembly:** `LD2W  { <Zt1>.S, <Zt2>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## LD2W (scalar plus scalar) -- A64

### Encoding: `ld2w_z_p_br_contiguous`  — LD2W

**Assembly:** `LD2W  { <Zt1>.S, <Zt2>.S }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let nreg : integer{} = 2;
```

---

## LD3 (multiple structures) -- A64

### Encoding: `LD3_asisdlse_R3`  — LD3

**Assembly:** `LD3  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsep_I3_i`  [Immediate offset]  — LD3

**Assembly:** `LD3  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsep_R3_r`  [Register offset]  — LD3

**Assembly:** `LD3  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD3 (single structure) -- A64

### Encoding: `LD3_asisdlso_B3_3b`  [8-bit]  — LD3

**Assembly:** `LD3  { <Vt>.B, <Vt2>.B, <Vt3>.B }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlso_H3_3h`  [16-bit]  — LD3

**Assembly:** `LD3  { <Vt>.H, <Vt2>.H, <Vt3>.H }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlso_S3_3s`  [32-bit]  — LD3

**Assembly:** `LD3  { <Vt>.S, <Vt2>.S, <Vt3>.S }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlso_D3_3d`  [64-bit]  — LD3

**Assembly:** `LD3  { <Vt>.D, <Vt2>.D, <Vt3>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsop_B3_i3b`  [8-bit, immediate offset]  — LD3

**Assembly:** `LD3  { <Vt>.B, <Vt2>.B, <Vt3>.B }[<index>], [<Xn|SP>], #3`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsop_BX3_r3b`  [8-bit, register offset]  — LD3

**Assembly:** `LD3  { <Vt>.B, <Vt2>.B, <Vt3>.B }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsop_H3_i3h`  [16-bit, immediate offset]  — LD3

**Assembly:** `LD3  { <Vt>.H, <Vt2>.H, <Vt3>.H }[<index>], [<Xn|SP>], #6`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsop_HX3_r3h`  [16-bit, register offset]  — LD3

**Assembly:** `LD3  { <Vt>.H, <Vt2>.H, <Vt3>.H }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsop_S3_i3s`  [32-bit, immediate offset]  — LD3

**Assembly:** `LD3  { <Vt>.S, <Vt2>.S, <Vt3>.S }[<index>], [<Xn|SP>], #12`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsop_SX3_r3s`  [32-bit, register offset]  — LD3

**Assembly:** `LD3  { <Vt>.S, <Vt2>.S, <Vt3>.S }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsop_D3_i3d`  [64-bit, immediate offset]  — LD3

**Assembly:** `LD3  { <Vt>.D, <Vt2>.D, <Vt3>.D }[<index>], [<Xn|SP>], #24`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3_asisdlsop_DX3_r3d`  [64-bit, register offset]  — LD3

**Assembly:** `LD3  { <Vt>.D, <Vt2>.D, <Vt3>.D }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD3B (scalar plus immediate) -- A64

### Encoding: `ld3b_z_p_bi_contiguous`  — LD3B

**Assembly:** `LD3B  { <Zt1>.B, <Zt2>.B, <Zt3>.B }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## LD3B (scalar plus scalar) -- A64

### Encoding: `ld3b_z_p_br_contiguous`  — LD3B

**Assembly:** `LD3B  { <Zt1>.B, <Zt2>.B, <Zt3>.B }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let nreg : integer{} = 3;
```

---

## LD3D (scalar plus immediate) -- A64

### Encoding: `ld3d_z_p_bi_contiguous`  — LD3D

**Assembly:** `LD3D  { <Zt1>.D, <Zt2>.D, <Zt3>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## LD3D (scalar plus scalar) -- A64

### Encoding: `ld3d_z_p_br_contiguous`  — LD3D

**Assembly:** `LD3D  { <Zt1>.D, <Zt2>.D, <Zt3>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let nreg : integer{} = 3;
```

---

## LD3H (scalar plus immediate) -- A64

### Encoding: `ld3h_z_p_bi_contiguous`  — LD3H

**Assembly:** `LD3H  { <Zt1>.H, <Zt2>.H, <Zt3>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## LD3H (scalar plus scalar) -- A64

### Encoding: `ld3h_z_p_br_contiguous`  — LD3H

**Assembly:** `LD3H  { <Zt1>.H, <Zt2>.H, <Zt3>.H }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let nreg : integer{} = 3;
```

---

## LD3Q (scalar plus immediate) -- A64

### Encoding: `ld3q_z_p_bi_contiguous`  — LD3Q

**Assembly:** `LD3Q  { <Zt1>.Q, <Zt2>.Q, <Zt3>.Q }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## LD3Q (scalar plus scalar) -- A64

### Encoding: `ld3q_z_p_br_contiguous`  — LD3Q

**Assembly:** `LD3Q  { <Zt1>.Q, <Zt2>.Q, <Zt3>.Q }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #4]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let nreg : integer{} = 3;
```

---

## LD3R -- A64

### Encoding: `LD3R_asisdlso_R3`  — LD3R

**Assembly:** `LD3R  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3R_asisdlsop_R3_i`  [Immediate offset]  — LD3R

**Assembly:** `LD3R  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD3R_asisdlsop_RX3_r`  [Register offset]  — LD3R

**Assembly:** `LD3R  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD3W (scalar plus immediate) -- A64

### Encoding: `ld3w_z_p_bi_contiguous`  — LD3W

**Assembly:** `LD3W  { <Zt1>.S, <Zt2>.S, <Zt3>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## LD3W (scalar plus scalar) -- A64

### Encoding: `ld3w_z_p_br_contiguous`  — LD3W

**Assembly:** `LD3W  { <Zt1>.S, <Zt2>.S, <Zt3>.S }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let nreg : integer{} = 3;
```

---

## LD4 (multiple structures) -- A64

### Encoding: `LD4_asisdlse_R4`  — LD4

**Assembly:** `LD4  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsep_I4_i`  [Immediate offset]  — LD4

**Assembly:** `LD4  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsep_R4_r`  [Register offset]  — LD4

**Assembly:** `LD4  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD4 (single structure) -- A64

### Encoding: `LD4_asisdlso_B4_4b`  [8-bit]  — LD4

**Assembly:** `LD4  { <Vt>.B, <Vt2>.B, <Vt3>.B, <Vt4>.B }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlso_H4_4h`  [16-bit]  — LD4

**Assembly:** `LD4  { <Vt>.H, <Vt2>.H, <Vt3>.H, <Vt4>.H }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlso_S4_4s`  [32-bit]  — LD4

**Assembly:** `LD4  { <Vt>.S, <Vt2>.S, <Vt3>.S, <Vt4>.S }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlso_D4_4d`  [64-bit]  — LD4

**Assembly:** `LD4  { <Vt>.D, <Vt2>.D, <Vt3>.D, <Vt4>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsop_B4_i4b`  [8-bit, immediate offset]  — LD4

**Assembly:** `LD4  { <Vt>.B, <Vt2>.B, <Vt3>.B, <Vt4>.B }[<index>], [<Xn|SP>], #4`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsop_BX4_r4b`  [8-bit, register offset]  — LD4

**Assembly:** `LD4  { <Vt>.B, <Vt2>.B, <Vt3>.B, <Vt4>.B }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsop_H4_i4h`  [16-bit, immediate offset]  — LD4

**Assembly:** `LD4  { <Vt>.H, <Vt2>.H, <Vt3>.H, <Vt4>.H }[<index>], [<Xn|SP>], #8`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsop_HX4_r4h`  [16-bit, register offset]  — LD4

**Assembly:** `LD4  { <Vt>.H, <Vt2>.H, <Vt3>.H, <Vt4>.H }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsop_S4_i4s`  [32-bit, immediate offset]  — LD4

**Assembly:** `LD4  { <Vt>.S, <Vt2>.S, <Vt3>.S, <Vt4>.S }[<index>], [<Xn|SP>], #16`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsop_SX4_r4s`  [32-bit, register offset]  — LD4

**Assembly:** `LD4  { <Vt>.S, <Vt2>.S, <Vt3>.S, <Vt4>.S }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsop_D4_i4d`  [64-bit, immediate offset]  — LD4

**Assembly:** `LD4  { <Vt>.D, <Vt2>.D, <Vt3>.D, <Vt4>.D }[<index>], [<Xn|SP>], #32`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4_asisdlsop_DX4_r4d`  [64-bit, register offset]  — LD4

**Assembly:** `LD4  { <Vt>.D, <Vt2>.D, <Vt3>.D, <Vt4>.D }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD4B (scalar plus immediate) -- A64

### Encoding: `ld4b_z_p_bi_contiguous`  — LD4B

**Assembly:** `LD4B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## LD4B (scalar plus scalar) -- A64

### Encoding: `ld4b_z_p_br_contiguous`  — LD4B

**Assembly:** `LD4B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let nreg : integer{} = 4;
```

---

## LD4D (scalar plus immediate) -- A64

### Encoding: `ld4d_z_p_bi_contiguous`  — LD4D

**Assembly:** `LD4D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## LD4D (scalar plus scalar) -- A64

### Encoding: `ld4d_z_p_br_contiguous`  — LD4D

**Assembly:** `LD4D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let nreg : integer{} = 4;
```

---

## LD4H (scalar plus immediate) -- A64

### Encoding: `ld4h_z_p_bi_contiguous`  — LD4H

**Assembly:** `LD4H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## LD4H (scalar plus scalar) -- A64

### Encoding: `ld4h_z_p_br_contiguous`  — LD4H

**Assembly:** `LD4H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let nreg : integer{} = 4;
```

---

## LD4Q (scalar plus immediate) -- A64

### Encoding: `ld4q_z_p_bi_contiguous`  — LD4Q

**Assembly:** `LD4Q  { <Zt1>.Q, <Zt2>.Q, <Zt3>.Q, <Zt4>.Q }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## LD4Q (scalar plus scalar) -- A64

### Encoding: `ld4q_z_p_br_contiguous`  — LD4Q

**Assembly:** `LD4Q  { <Zt1>.Q, <Zt2>.Q, <Zt3>.Q, <Zt4>.Q }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #4]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let nreg : integer{} = 4;
```

---

## LD4R -- A64

### Encoding: `LD4R_asisdlso_R4`  — LD4R

**Assembly:** `LD4R  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4R_asisdlsop_R4_i`  [Immediate offset]  — LD4R

**Assembly:** `LD4R  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `LD4R_asisdlsop_RX4_r`  [Register offset]  — LD4R

**Assembly:** `LD4R  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LD4W (scalar plus immediate) -- A64

### Encoding: `ld4w_z_p_bi_contiguous`  — LD4W

**Assembly:** `LD4W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## LD4W (scalar plus scalar) -- A64

### Encoding: `ld4w_z_p_br_contiguous`  — LD4W

**Assembly:** `LD4W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let nreg : integer{} = 4;
```

---

## LD64B -- A64

### Encoding: `LD64B_64L_memop`  — LD64B

**Assembly:** `LD64B  <Xt>, [<Xn|SP> {, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt[4:3] == '11' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LS64) then EndOfDecode(Decode_UNDEF); end;
if Rt[4:3] == '11' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let withstatus : boolean = FALSE;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let tagchecked : boolean = n != 31;
```

---

## LDADD, LDADDA, LDADDAL, LDADDL -- A64

### Encoding: `LDADD_32_memop`  [32-bit no memory ordering]  — LDADD

**Assembly:** `LDADD  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDA_32_memop`  [32-bit acquire]  — LDADDA

**Assembly:** `LDADDA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDAL_32_memop`  [32-bit acquire-release]  — LDADDAL

**Assembly:** `LDADDAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDL_32_memop`  [32-bit release]  — LDADDL

**Assembly:** `LDADDL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADD_64_memop`  [64-bit no memory ordering]  — LDADD

**Assembly:** `LDADD  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDA_64_memop`  [64-bit acquire]  — LDADDA

**Assembly:** `LDADDA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDAL_64_memop`  [64-bit acquire-release]  — LDADDAL

**Assembly:** `LDADDAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDL_64_memop`  [64-bit release]  — LDADDL

**Assembly:** `LDADDL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDADDB, LDADDAB, LDADDALB, LDADDLB -- A64

### Encoding: `LDADDB_32_memop`  [No memory ordering]  — LDADDB

**Assembly:** `LDADDB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDAB_32_memop`  [Acquire]  — LDADDAB

**Assembly:** `LDADDAB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDALB_32_memop`  [Acquire-release]  — LDADDALB

**Assembly:** `LDADDALB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDLB_32_memop`  [Release]  — LDADDLB

**Assembly:** `LDADDLB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDADDH, LDADDAH, LDADDALH, LDADDLH -- A64

### Encoding: `LDADDH_32_memop`  [No memory ordering]  — LDADDH

**Assembly:** `LDADDH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDAH_32_memop`  [Acquire]  — LDADDAH

**Assembly:** `LDADDAH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDALH_32_memop`  [Acquire-release]  — LDADDALH

**Assembly:** `LDADDALH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDADDLH_32_memop`  [Release]  — LDADDLH

**Assembly:** `LDADDLH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDAP -- A64

### Encoding: `LDAP_64_ldiappstilp`  — LDAP

**Assembly:** `LDAP  <Xt1>, <Xt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSCP) then EndOfDecode(Decode_UNDEF); end;

let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = t != 31 && t2 != 31;
let tagchecked : boolean = n != 31;
let ispair : boolean = TRUE;
var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // Result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDAP1 (SIMD&FP) -- A64

### Encoding: `LDAP1_asisdlso_D1`  — LDAP1

**Assembly:** `LDAP1  { <Vt>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | Is the element index, encoded in "Q". |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## LDAPP -- A64

### Encoding: `LDAPP_64_ldiappstilp`  — LDAPP

**Assembly:** `LDAPP  <Xt1>, <Xt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSCP) then EndOfDecode(Decode_UNDEF); end;

let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = t != 31 && t2 != 31;
let tagchecked : boolean = n != 31;
let ispair : boolean = TRUE;
var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // Result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDAPR -- A64

### Encoding: `LDAPR_32L_ldapstl_writeback`  [32-bit]  — LDAPR

**Assembly:** `LDAPR  <Wt>, [<Xn|SP>], #4`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
var wback : boolean = TRUE;
let acquirepc : boolean = t != 31;
let regsize : integer{} = if size == '11' then 64 else 32;
let datasize : integer{} = 8 << UInt(size);
let offset : integer = 1 << UInt(size);

let tagchecked : boolean = TRUE;

var wb_unknown : boolean = FALSE;

if n == t && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_WBOVERLAPLD);
    assert c IN {Constraint_WBSUPPRESS, Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_WBSUPPRESS => wback = FALSE;        // writeback is suppressed
        when Constraint_UNKNOWN =>    wb_unknown = TRUE;    // writeback is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDAPR_64L_ldapstl_writeback`  [64-bit]  — LDAPR

**Assembly:** `LDAPR  <Xt>, [<Xn|SP>], #8`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
var wback : boolean = TRUE;
let acquirepc : boolean = t != 31;
let regsize : integer{} = if size == '11' then 64 else 32;
let datasize : integer{} = 8 << UInt(size);
let offset : integer = 1 << UInt(size);

let tagchecked : boolean = TRUE;

var wb_unknown : boolean = FALSE;

if n == t && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_WBOVERLAPLD);
    assert c IN {Constraint_WBSUPPRESS, Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_WBSUPPRESS => wback = FALSE;        // writeback is suppressed
        when Constraint_UNKNOWN =>    wb_unknown = TRUE;    // writeback is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDAPR_32L_memop`  [32-bit]  — LDAPR

**Assembly:** `LDAPR  <Wt>, [<Xn|SP> {, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let wback : boolean = FALSE;
let offset : integer = 0;
let wb_unknown : boolean = FALSE;
let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let datasize : integer{} = elsize;
let acquirepc : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

### Encoding: `LDAPR_64L_memop`  [64-bit]  — LDAPR

**Assembly:** `LDAPR  <Xt>, [<Xn|SP> {, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let wback : boolean = FALSE;
let offset : integer = 0;
let wb_unknown : boolean = FALSE;
let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let datasize : integer{} = elsize;
let acquirepc : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDAPRB -- A64

### Encoding: `LDAPRB_32L_memop`  — LDAPRB

**Assembly:** `LDAPRB  <Wt>, [<Xn|SP> {, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquirepc : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDAPRH -- A64

### Encoding: `LDAPRH_32L_memop`  — LDAPRH

**Assembly:** `LDAPRH  <Wt>, [<Xn|SP> {, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquirepc : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDAPUR (SIMD&FP) -- A64

### Encoding: `LDAPUR_B_ldapstl_simd`  [8-bit]  — LDAPUR

**Assembly:** `LDAPUR  <Bt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDAPUR_H_ldapstl_simd`  [16-bit]  — LDAPUR

**Assembly:** `LDAPUR  <Ht>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDAPUR_S_ldapstl_simd`  [32-bit]  — LDAPUR

**Assembly:** `LDAPUR  <St>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDAPUR_D_ldapstl_simd`  [64-bit]  — LDAPUR

**Assembly:** `LDAPUR  <Dt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDAPUR_Q_ldapstl_simd`  [128-bit]  — LDAPUR

**Assembly:** `LDAPUR  <Qt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDAPUR -- A64

### Encoding: `LDAPUR_32_ldapstl_unscaled`  [32-bit]  — LDAPUR

**Assembly:** `LDAPUR  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDAPUR_64_ldapstl_unscaled`  [64-bit]  — LDAPUR

**Assembly:** `LDAPUR  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDAPURB -- A64

### Encoding: `LDAPURB_32_ldapstl_unscaled`  — LDAPURB

**Assembly:** `LDAPURB  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDAPURH -- A64

### Encoding: `LDAPURH_32_ldapstl_unscaled`  — LDAPURH

**Assembly:** `LDAPURH  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDAPURSB -- A64

### Encoding: `LDAPURSB_32_ldapstl_unscaled`  [32-bit]  — LDAPURSB

**Assembly:** `LDAPURSB  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDAPURSB_64_ldapstl_unscaled`  [64-bit]  — LDAPURSB

**Assembly:** `LDAPURSB  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDAPURSH -- A64

### Encoding: `LDAPURSH_32_ldapstl_unscaled`  [32-bit]  — LDAPURSH

**Assembly:** `LDAPURSH  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDAPURSH_64_ldapstl_unscaled`  [64-bit]  — LDAPURSH

**Assembly:** `LDAPURSH  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDAPURSW -- A64

### Encoding: `LDAPURSW_64_ldapstl_unscaled`  — LDAPURSW

**Assembly:** `LDAPURSW  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDAR -- A64

### Encoding: `LDAR_LR32_ldstord`  [32-bit]  — LDAR

**Assembly:** `LDAR  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acquire : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

### Encoding: `LDAR_LR64_ldstord`  [64-bit]  — LDAR

**Assembly:** `LDAR  <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acquire : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDARB -- A64

### Encoding: `LDARB_LR32_ldstord`  — LDARB

**Assembly:** `LDARB  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDARH -- A64

### Encoding: `LDARH_LR32_ldstord`  — LDARH

**Assembly:** `LDARH  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDATXR -- A64

### Encoding: `LDATXR_LR32_ldstexclr_unpriv`  [32-bit]  — LDATXR

**Assembly:** `LDATXR  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acqrel : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

### Encoding: `LDATXR_LR64_ldstexclr_unpriv`  [64-bit]  — LDATXR

**Assembly:** `LDATXR  <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acqrel : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDAXP -- A64

### Encoding: `LDAXP_LP32_ldstexclp`  [32-bit]  — LDAXP

**Assembly:** `LDAXP  <Wt1>, <Wt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let datasize : integer{} = elsize * 2;
let acqrel : boolean = t != 31 && t2 != 31;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDAXP_LP64_ldstexclp`  [64-bit]  — LDAXP

**Assembly:** `LDAXP  <Xt1>, <Xt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let datasize : integer{} = elsize * 2;
let acqrel : boolean = t != 31 && t2 != 31;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDAXR -- A64

### Encoding: `LDAXR_LR32_ldstexclr`  [32-bit]  — LDAXR

**Assembly:** `LDAXR  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acqrel : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

### Encoding: `LDAXR_LR64_ldstexclr`  [64-bit]  — LDAXR

**Assembly:** `LDAXR  <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acqrel : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDAXRB -- A64

### Encoding: `LDAXRB_LR32_ldstexclr`  — LDAXRB

**Assembly:** `LDAXRB  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acqrel : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDAXRH -- A64

### Encoding: `LDAXRH_LR32_ldstexclr`  — LDAXRH

**Assembly:** `LDAXRH  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acqrel : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDBFADD, LDBFADDA, LDBFADDAL, LDBFADDL -- A64

### Encoding: `LDBFADD_16`  [No memory ordering]  — LDBFADD

**Assembly:** `LDBFADD  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFADDA_16`  [Acquire]  — LDBFADDA

**Assembly:** `LDBFADDA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFADDAL_16`  [Acquire-release]  — LDBFADDAL

**Assembly:** `LDBFADDAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFADDL_16`  [Release]  — LDBFADDL

**Assembly:** `LDBFADDL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDBFMAX, LDBFMAXA, LDBFMAXAL, LDBFMAXL -- A64

### Encoding: `LDBFMAX_16`  [No memory ordering]  — LDBFMAX

**Assembly:** `LDBFMAX  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMAXA_16`  [Acquire]  — LDBFMAXA

**Assembly:** `LDBFMAXA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMAXAL_16`  [Acquire-release]  — LDBFMAXAL

**Assembly:** `LDBFMAXAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMAXL_16`  [Release]  — LDBFMAXL

**Assembly:** `LDBFMAXL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDBFMAXNM, LDBFMAXNMA, LDBFMAXNMAL, LDBFMAXNML -- A64

### Encoding: `LDBFMAXNM_16`  [No memory ordering]  — LDBFMAXNM

**Assembly:** `LDBFMAXNM  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMAXNMA_16`  [Acquire]  — LDBFMAXNMA

**Assembly:** `LDBFMAXNMA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMAXNMAL_16`  [Acquire-release]  — LDBFMAXNMAL

**Assembly:** `LDBFMAXNMAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMAXNML_16`  [Release]  — LDBFMAXNML

**Assembly:** `LDBFMAXNML  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDBFMIN, LDBFMINA, LDBFMINAL, LDBFMINL -- A64

### Encoding: `LDBFMIN_16`  [No memory ordering]  — LDBFMIN

**Assembly:** `LDBFMIN  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMINA_16`  [Acquire]  — LDBFMINA

**Assembly:** `LDBFMINA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMINAL_16`  [Acquire-release]  — LDBFMINAL

**Assembly:** `LDBFMINAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMINL_16`  [Release]  — LDBFMINL

**Assembly:** `LDBFMINL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDBFMINNM, LDBFMINNMA, LDBFMINNMAL, LDBFMINNML -- A64

### Encoding: `LDBFMINNM_16`  [No memory ordering]  — LDBFMINNM

**Assembly:** `LDBFMINNM  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMINNMA_16`  [Acquire]  — LDBFMINNMA

**Assembly:** `LDBFMINNMA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMINNMAL_16`  [Acquire-release]  — LDBFMINNMAL

**Assembly:** `LDBFMINNMAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDBFMINNML_16`  [Release]  — LDBFMINNML

**Assembly:** `LDBFMINNML  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 16;
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDCLR, LDCLRA, LDCLRAL, LDCLRL -- A64

### Encoding: `LDCLR_32_memop`  [32-bit no memory ordering]  — LDCLR

**Assembly:** `LDCLR  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRA_32_memop`  [32-bit acquire]  — LDCLRA

**Assembly:** `LDCLRA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRAL_32_memop`  [32-bit acquire-release]  — LDCLRAL

**Assembly:** `LDCLRAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRL_32_memop`  [32-bit release]  — LDCLRL

**Assembly:** `LDCLRL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLR_64_memop`  [64-bit no memory ordering]  — LDCLR

**Assembly:** `LDCLR  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRA_64_memop`  [64-bit acquire]  — LDCLRA

**Assembly:** `LDCLRA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRAL_64_memop`  [64-bit acquire-release]  — LDCLRAL

**Assembly:** `LDCLRAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRL_64_memop`  [64-bit release]  — LDCLRL

**Assembly:** `LDCLRL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDCLRB, LDCLRAB, LDCLRALB, LDCLRLB -- A64

### Encoding: `LDCLRB_32_memop`  [No memory ordering]  — LDCLRB

**Assembly:** `LDCLRB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRAB_32_memop`  [Acquire]  — LDCLRAB

**Assembly:** `LDCLRAB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRALB_32_memop`  [Acquire-release]  — LDCLRALB

**Assembly:** `LDCLRALB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRLB_32_memop`  [Release]  — LDCLRLB

**Assembly:** `LDCLRLB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDCLRH, LDCLRAH, LDCLRALH, LDCLRLH -- A64

### Encoding: `LDCLRH_32_memop`  [No memory ordering]  — LDCLRH

**Assembly:** `LDCLRH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRAH_32_memop`  [Acquire]  — LDCLRAH

**Assembly:** `LDCLRAH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRALH_32_memop`  [Acquire-release]  — LDCLRALH

**Assembly:** `LDCLRALH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDCLRLH_32_memop`  [Release]  — LDCLRLH

**Assembly:** `LDCLRLH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDCLRP, LDCLRPA, LDCLRPAL, LDCLRPL -- A64

### Encoding: `LDCLRP_128_memop_128`  [LDCLRP]  — LDCLRP

**Assembly:** `LDCLRP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDCLRPA_128_memop_128`  [LDCLRPA]  — LDCLRPA

**Assembly:** `LDCLRPA  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDCLRPAL_128_memop_128`  [LDCLRPAL]  — LDCLRPAL

**Assembly:** `LDCLRPAL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDCLRPL_128_memop_128`  [LDCLRPL]  — LDCLRPL

**Assembly:** `LDCLRPL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDEOR, LDEORA, LDEORAL, LDEORL -- A64

### Encoding: `LDEOR_32_memop`  [32-bit no memory ordering]  — LDEOR

**Assembly:** `LDEOR  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORA_32_memop`  [32-bit acquire]  — LDEORA

**Assembly:** `LDEORA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORAL_32_memop`  [32-bit acquire-release]  — LDEORAL

**Assembly:** `LDEORAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORL_32_memop`  [32-bit release]  — LDEORL

**Assembly:** `LDEORL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEOR_64_memop`  [64-bit no memory ordering]  — LDEOR

**Assembly:** `LDEOR  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORA_64_memop`  [64-bit acquire]  — LDEORA

**Assembly:** `LDEORA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORAL_64_memop`  [64-bit acquire-release]  — LDEORAL

**Assembly:** `LDEORAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORL_64_memop`  [64-bit release]  — LDEORL

**Assembly:** `LDEORL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDEORB, LDEORAB, LDEORALB, LDEORLB -- A64

### Encoding: `LDEORB_32_memop`  [No memory ordering]  — LDEORB

**Assembly:** `LDEORB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORAB_32_memop`  [Acquire]  — LDEORAB

**Assembly:** `LDEORAB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORALB_32_memop`  [Acquire-release]  — LDEORALB

**Assembly:** `LDEORALB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORLB_32_memop`  [Release]  — LDEORLB

**Assembly:** `LDEORLB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDEORH, LDEORAH, LDEORALH, LDEORLH -- A64

### Encoding: `LDEORH_32_memop`  [No memory ordering]  — LDEORH

**Assembly:** `LDEORH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORAH_32_memop`  [Acquire]  — LDEORAH

**Assembly:** `LDEORAH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORALH_32_memop`  [Acquire-release]  — LDEORALH

**Assembly:** `LDEORALH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDEORLH_32_memop`  [Release]  — LDEORLH

**Assembly:** `LDEORLH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDFADD, LDFADDA, LDFADDAL, LDFADDL -- A64

### Encoding: `LDFADD_16`  [Half-precision no memory ordering]  — LDFADD

**Assembly:** `LDFADD  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADDA_16`  [Half-precision acquire]  — LDFADDA

**Assembly:** `LDFADDA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADDAL_16`  [Half-precision acquire-release]  — LDFADDAL

**Assembly:** `LDFADDAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADDL_16`  [Half-precision release]  — LDFADDL

**Assembly:** `LDFADDL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADD_32`  [Single-precision no memory ordering]  — LDFADD

**Assembly:** `LDFADD  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADDA_32`  [Single-precision acquire]  — LDFADDA

**Assembly:** `LDFADDA  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADDAL_32`  [Single-precision acquire-release]  — LDFADDAL

**Assembly:** `LDFADDAL  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADDL_32`  [Single-precision release]  — LDFADDL

**Assembly:** `LDFADDL  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADD_64`  [Double-precision no memory ordering]  — LDFADD

**Assembly:** `LDFADD  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADDA_64`  [Double-precision acquire]  — LDFADDA

**Assembly:** `LDFADDA  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADDAL_64`  [Double-precision acquire-release]  — LDFADDAL

**Assembly:** `LDFADDAL  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFADDL_64`  [Double-precision release]  — LDFADDL

**Assembly:** `LDFADDL  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDFF1B (scalar plus scalar) -- A64

### Encoding: `ldff1b_z_p_br_u8`  — LDFF1B

**Assembly:** `LDFF1B  { <Zt>.B }, <Pg>/Z, [<Xn|SP>{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

### Encoding: `ldff1b_z_p_br_u16`  — LDFF1B

**Assembly:** `LDFF1B  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

### Encoding: `ldff1b_z_p_br_u32`  — LDFF1B

**Assembly:** `LDFF1B  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

### Encoding: `ldff1b_z_p_br_u64`  — LDFF1B

**Assembly:** `LDFF1B  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

---

## LDFF1B (scalar plus vector) -- A64

### Encoding: `ldff1b_z_p_bz_d_x32_unscaled`  — LDFF1B

**Assembly:** `LDFF1B  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1b_z_p_bz_s_x32_unscaled`  — LDFF1B

**Assembly:** `LDFF1B  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1b_z_p_bz_d_64_unscaled`  — LDFF1B

**Assembly:** `LDFF1B  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LDFF1B (vector plus immediate) -- A64

### Encoding: `ldff1b_z_p_ai_s`  — LDFF1B

**Assembly:** `LDFF1B  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

### Encoding: `ldff1b_z_p_ai_d`  — LDFF1B

**Assembly:** `LDFF1B  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

---

## LDFF1D (scalar plus scalar) -- A64

### Encoding: `ldff1d_z_p_br_u64`  — LDFF1D

**Assembly:** `LDFF1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #3}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
```

---

## LDFF1D (scalar plus vector) -- A64

### Encoding: `ldff1d_z_p_bz_d_x32_scaled`  — LDFF1D

**Assembly:** `LDFF1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 3;
```

### Encoding: `ldff1d_z_p_bz_d_x32_unscaled`  — LDFF1D

**Assembly:** `LDFF1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1d_z_p_bz_d_64_scaled`  — LDFF1D

**Assembly:** `LDFF1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 3;
```

### Encoding: `ldff1d_z_p_bz_d_64_unscaled`  — LDFF1D

**Assembly:** `LDFF1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LDFF1D (vector plus immediate) -- A64

### Encoding: `ldff1d_z_p_ai_d`  — LDFF1D

**Assembly:** `LDFF1D  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 8 in the range 0 to 248, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

---

## LDFF1H (scalar plus scalar) -- A64

### Encoding: `ldff1h_z_p_br_u16`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #1}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
```

### Encoding: `ldff1h_z_p_br_u32`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #1}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
```

### Encoding: `ldff1h_z_p_br_u64`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #1}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
```

---

## LDFF1H (scalar plus vector) -- A64

### Encoding: `ldff1h_z_p_bz_s_x32_scaled`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `ldff1h_z_p_bz_d_x32_scaled`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `ldff1h_z_p_bz_d_x32_unscaled`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1h_z_p_bz_s_x32_unscaled`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1h_z_p_bz_d_64_scaled`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 1;
```

### Encoding: `ldff1h_z_p_bz_d_64_unscaled`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LDFF1H (vector plus immediate) -- A64

### Encoding: `ldff1h_z_p_ai_s`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

### Encoding: `ldff1h_z_p_ai_d`  — LDFF1H

**Assembly:** `LDFF1H  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

---

## LDFF1SB (scalar plus scalar) -- A64

### Encoding: `ldff1sb_z_p_br_s16`  — LDFF1SB

**Assembly:** `LDFF1SB  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
```

### Encoding: `ldff1sb_z_p_br_s32`  — LDFF1SB

**Assembly:** `LDFF1SB  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
```

### Encoding: `ldff1sb_z_p_br_s64`  — LDFF1SB

**Assembly:** `LDFF1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
```

---

## LDFF1SB (scalar plus vector) -- A64

### Encoding: `ldff1sb_z_p_bz_d_x32_unscaled`  — LDFF1SB

**Assembly:** `LDFF1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1sb_z_p_bz_s_x32_unscaled`  — LDFF1SB

**Assembly:** `LDFF1SB  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1sb_z_p_bz_d_64_unscaled`  — LDFF1SB

**Assembly:** `LDFF1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LDFF1SB (vector plus immediate) -- A64

### Encoding: `ldff1sb_z_p_ai_s`  — LDFF1SB

**Assembly:** `LDFF1SB  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

### Encoding: `ldff1sb_z_p_ai_d`  — LDFF1SB

**Assembly:** `LDFF1SB  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

---

## LDFF1SH (scalar plus scalar) -- A64

### Encoding: `ldff1sh_z_p_br_s32`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #1}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
```

### Encoding: `ldff1sh_z_p_br_s64`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #1}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
```

---

## LDFF1SH (scalar plus vector) -- A64

### Encoding: `ldff1sh_z_p_bz_s_x32_scaled`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `ldff1sh_z_p_bz_d_x32_scaled`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `ldff1sh_z_p_bz_d_x32_unscaled`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1sh_z_p_bz_s_x32_unscaled`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1sh_z_p_bz_d_64_scaled`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 1;
```

### Encoding: `ldff1sh_z_p_bz_d_64_unscaled`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LDFF1SH (vector plus immediate) -- A64

### Encoding: `ldff1sh_z_p_ai_s`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

### Encoding: `ldff1sh_z_p_ai_d`  — LDFF1SH

**Assembly:** `LDFF1SH  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

---

## LDFF1SW (scalar plus scalar) -- A64

### Encoding: `ldff1sw_z_p_br_s64`  — LDFF1SW

**Assembly:** `LDFF1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #2}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = FALSE;
```

---

## LDFF1SW (scalar plus vector) -- A64

### Encoding: `ldff1sw_z_p_bz_d_x32_scaled`  — LDFF1SW

**Assembly:** `LDFF1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 2;
```

### Encoding: `ldff1sw_z_p_bz_d_x32_unscaled`  — LDFF1SW

**Assembly:** `LDFF1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1sw_z_p_bz_d_64_scaled`  — LDFF1SW

**Assembly:** `LDFF1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 2;
```

### Encoding: `ldff1sw_z_p_bz_d_64_unscaled`  — LDFF1SW

**Assembly:** `LDFF1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LDFF1SW (vector plus immediate) -- A64

### Encoding: `ldff1sw_z_p_ai_d`  — LDFF1SW

**Assembly:** `LDFF1SW  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = FALSE;
let offset : integer = UInt(imm5);
```

---

## LDFF1W (scalar plus scalar) -- A64

### Encoding: `ldff1w_z_p_br_u32`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #2}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
```

### Encoding: `ldff1w_z_p_br_u64`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, <Xm>, LSL #2}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
```

---

## LDFF1W (scalar plus vector) -- A64

### Encoding: `ldff1w_z_p_bz_s_x32_scaled`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 2;
```

### Encoding: `ldff1w_z_p_bz_d_x32_scaled`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 2;
```

### Encoding: `ldff1w_z_p_bz_d_x32_unscaled`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1w_z_p_bz_s_x32_unscaled`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `ldff1w_z_p_bz_d_64_scaled`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 2;
```

### Encoding: `ldff1w_z_p_bz_d_64_unscaled`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let unsigned : boolean = TRUE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## LDFF1W (vector plus immediate) -- A64

### Encoding: `ldff1w_z_p_ai_s`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

### Encoding: `ldff1w_z_p_ai_d`  — LDFF1W

**Assembly:** `LDFF1W  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = UInt(imm5);
```

---

## LDFMAX, LDFMAXA, LDFMAXAL, LDFMAXL -- A64

### Encoding: `LDFMAX_16`  [Half-precision no memory ordering]  — LDFMAX

**Assembly:** `LDFMAX  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXA_16`  [Half-precision acquire]  — LDFMAXA

**Assembly:** `LDFMAXA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXAL_16`  [Half-precision acquire-release]  — LDFMAXAL

**Assembly:** `LDFMAXAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXL_16`  [Half-precision release]  — LDFMAXL

**Assembly:** `LDFMAXL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAX_32`  [Single-precision no memory ordering]  — LDFMAX

**Assembly:** `LDFMAX  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXA_32`  [Single-precision acquire]  — LDFMAXA

**Assembly:** `LDFMAXA  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXAL_32`  [Single-precision acquire-release]  — LDFMAXAL

**Assembly:** `LDFMAXAL  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXL_32`  [Single-precision release]  — LDFMAXL

**Assembly:** `LDFMAXL  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAX_64`  [Double-precision no memory ordering]  — LDFMAX

**Assembly:** `LDFMAX  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXA_64`  [Double-precision acquire]  — LDFMAXA

**Assembly:** `LDFMAXA  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXAL_64`  [Double-precision acquire-release]  — LDFMAXAL

**Assembly:** `LDFMAXAL  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXL_64`  [Double-precision release]  — LDFMAXL

**Assembly:** `LDFMAXL  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDFMAXNM, LDFMAXNMA, LDFMAXNMAL, LDFMAXNML -- A64

### Encoding: `LDFMAXNM_16`  [Half-precision no memory ordering]  — LDFMAXNM

**Assembly:** `LDFMAXNM  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNMA_16`  [Half-precision acquire]  — LDFMAXNMA

**Assembly:** `LDFMAXNMA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNMAL_16`  [Half-precision acquire-release]  — LDFMAXNMAL

**Assembly:** `LDFMAXNMAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNML_16`  [Half-precision release]  — LDFMAXNML

**Assembly:** `LDFMAXNML  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNM_32`  [Single-precision no memory ordering]  — LDFMAXNM

**Assembly:** `LDFMAXNM  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNMA_32`  [Single-precision acquire]  — LDFMAXNMA

**Assembly:** `LDFMAXNMA  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNMAL_32`  [Single-precision acquire-release]  — LDFMAXNMAL

**Assembly:** `LDFMAXNMAL  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNML_32`  [Single-precision release]  — LDFMAXNML

**Assembly:** `LDFMAXNML  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNM_64`  [Double-precision no memory ordering]  — LDFMAXNM

**Assembly:** `LDFMAXNM  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNMA_64`  [Double-precision acquire]  — LDFMAXNMA

**Assembly:** `LDFMAXNMA  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNMAL_64`  [Double-precision acquire-release]  — LDFMAXNMAL

**Assembly:** `LDFMAXNMAL  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMAXNML_64`  [Double-precision release]  — LDFMAXNML

**Assembly:** `LDFMAXNML  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDFMIN, LDFMINA, LDFMINAL, LDFMINL -- A64

### Encoding: `LDFMIN_16`  [Half-precision no memory ordering]  — LDFMIN

**Assembly:** `LDFMIN  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINA_16`  [Half-precision acquire]  — LDFMINA

**Assembly:** `LDFMINA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINAL_16`  [Half-precision acquire-release]  — LDFMINAL

**Assembly:** `LDFMINAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINL_16`  [Half-precision release]  — LDFMINL

**Assembly:** `LDFMINL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMIN_32`  [Single-precision no memory ordering]  — LDFMIN

**Assembly:** `LDFMIN  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINA_32`  [Single-precision acquire]  — LDFMINA

**Assembly:** `LDFMINA  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINAL_32`  [Single-precision acquire-release]  — LDFMINAL

**Assembly:** `LDFMINAL  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINL_32`  [Single-precision release]  — LDFMINL

**Assembly:** `LDFMINL  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMIN_64`  [Double-precision no memory ordering]  — LDFMIN

**Assembly:** `LDFMIN  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINA_64`  [Double-precision acquire]  — LDFMINA

**Assembly:** `LDFMINA  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINAL_64`  [Double-precision acquire-release]  — LDFMINAL

**Assembly:** `LDFMINAL  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINL_64`  [Double-precision release]  — LDFMINL

**Assembly:** `LDFMINL  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDFMINNM, LDFMINNMA, LDFMINNMAL, LDFMINNML -- A64

### Encoding: `LDFMINNM_16`  [Half-precision no memory ordering]  — LDFMINNM

**Assembly:** `LDFMINNM  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNMA_16`  [Half-precision acquire]  — LDFMINNMA

**Assembly:** `LDFMINNMA  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNMAL_16`  [Half-precision acquire-release]  — LDFMINNMAL

**Assembly:** `LDFMINNMAL  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNML_16`  [Half-precision release]  — LDFMINNML

**Assembly:** `LDFMINNML  <Hs>, <Ht>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNM_32`  [Single-precision no memory ordering]  — LDFMINNM

**Assembly:** `LDFMINNM  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNMA_32`  [Single-precision acquire]  — LDFMINNMA

**Assembly:** `LDFMINNMA  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNMAL_32`  [Single-precision acquire-release]  — LDFMINNMAL

**Assembly:** `LDFMINNMAL  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNML_32`  [Single-precision release]  — LDFMINNML

**Assembly:** `LDFMINNML  <Ss>, <St>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNM_64`  [Double-precision no memory ordering]  — LDFMINNM

**Assembly:** `LDFMINNM  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNMA_64`  [Double-precision acquire]  — LDFMINNMA

**Assembly:** `LDFMINNMA  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNMAL_64`  [Double-precision acquire-release]  — LDFMINNMAL

**Assembly:** `LDFMINNMAL  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDFMINNML_64`  [Double-precision release]  — LDFMINNML

**Assembly:** `LDFMINNML  <Ds>, <Dt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let s : integer = UInt(Rs);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDG -- A64

### Encoding: `LDG_64Loffset_ldsttags`  — LDG

**Assembly:** `LDG  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
```

---

## LDGM -- A64

### Encoding: `LDGM_64bulk_ldsttags`  — LDGM

**Assembly:** `LDGM  <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
```

---

## LDIAPP -- A64

### Encoding: `LDIAPP_32LE_ldiappstilp`  [32-bit post-index]  — LDIAPP

**Assembly:** `LDIAPP  <Wt1>, <Wt2>, [<Xn|SP>], #8`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let ispair : boolean = TRUE;
let postindex : boolean = opc2[0] == '0';
var wback : boolean = opc2[0] == '0';
```

### Encoding: `LDIAPP_32L_ldiappstilp`  [32-bit]  — LDIAPP

**Assembly:** `LDIAPP  <Wt1>, <Wt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let ispair : boolean = TRUE;
let postindex : boolean = opc2[0] == '0';
var wback : boolean = opc2[0] == '0';
```

### Encoding: `LDIAPP_64LS_ldiappstilp`  [64-bit post-index]  — LDIAPP

**Assembly:** `LDIAPP  <Xt1>, <Xt2>, [<Xn|SP>], #16`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let ispair : boolean = TRUE;
let postindex : boolean = opc2[0] == '0';
var wback : boolean = opc2[0] == '0';
```

### Encoding: `LDIAPP_64L_ldiappstilp`  [64-bit]  — LDIAPP

**Assembly:** `LDIAPP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let ispair : boolean = TRUE;
let postindex : boolean = opc2[0] == '0';
var wback : boolean = opc2[0] == '0';
```

---

## LDLAR -- A64

### Encoding: `LDLAR_LR32_ldstord`  [32-bit]  — LDLAR

**Assembly:** `LDLAR  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LOR) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acquire : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

### Encoding: `LDLAR_LR64_ldstord`  [64-bit]  — LDLAR

**Assembly:** `LDLAR  <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LOR) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acquire : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDLARB -- A64

### Encoding: `LDLARB_LR32_ldstord`  — LDLARB

**Assembly:** `LDLARB  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LOR) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDLARH -- A64

### Encoding: `LDLARH_LR32_ldstord`  — LDLARH

**Assembly:** `LDLARH  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LOR) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = t != 31;
let tagchecked : boolean = n != 31;
```

---

## LDNF1B -- A64

### Encoding: `ldnf1b_z_p_bi_u8`  — LDNF1B

**Assembly:** `LDNF1B  { <Zt>.B }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnf1b_z_p_bi_u16`  — LDNF1B

**Assembly:** `LDNF1B  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnf1b_z_p_bi_u32`  — LDNF1B

**Assembly:** `LDNF1B  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnf1b_z_p_bi_u64`  — LDNF1B

**Assembly:** `LDNF1B  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

---

## LDNF1D -- A64

### Encoding: `ldnf1d_z_p_bi_u64`  — LDNF1D

**Assembly:** `LDNF1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

---

## LDNF1H -- A64

### Encoding: `ldnf1h_z_p_bi_u16`  — LDNF1H

**Assembly:** `LDNF1H  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnf1h_z_p_bi_u32`  — LDNF1H

**Assembly:** `LDNF1H  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnf1h_z_p_bi_u64`  — LDNF1H

**Assembly:** `LDNF1H  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

---

## LDNF1SB -- A64

### Encoding: `ldnf1sb_z_p_bi_s16`  — LDNF1SB

**Assembly:** `LDNF1SB  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnf1sb_z_p_bi_s32`  — LDNF1SB

**Assembly:** `LDNF1SB  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnf1sb_z_p_bi_s64`  — LDNF1SB

**Assembly:** `LDNF1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

---

## LDNF1SH -- A64

### Encoding: `ldnf1sh_z_p_bi_s32`  — LDNF1SH

**Assembly:** `LDNF1SH  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnf1sh_z_p_bi_s64`  — LDNF1SH

**Assembly:** `LDNF1SH  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

---

## LDNF1SW -- A64

### Encoding: `ldnf1sw_z_p_bi_s64`  — LDNF1SW

**Assembly:** `LDNF1SW  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = FALSE;
let offset : integer = SInt(imm4);
```

---

## LDNF1W -- A64

### Encoding: `ldnf1w_z_p_bi_u32`  — LDNF1W

**Assembly:** `LDNF1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnf1w_z_p_bi_u64`  — LDNF1W

**Assembly:** `LDNF1W  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
let offset : integer = SInt(imm4);
```

---

## LDNP (SIMD&FP) -- A64

### Encoding: `LDNP_S_ldstnapair_offs`  [32-bit]  — LDNP

**Assembly:** `LDNP  <St1>, <St2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St1>` | Rt (5b) | Is the 32-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<St2>` | Rt2 (5b) | Is the 32-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "32-bit" variant: is the optional signed immediate byte offset, a multiple of 4 in the range -256 to 252, defaul |  |

**Pseudocode (Decode):**
```
// Empty.
```

### Encoding: `LDNP_D_ldstnapair_offs`  [64-bit]  — LDNP

**Assembly:** `LDNP  <Dt1>, <Dt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt1>` | Rt (5b) | Is the 64-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Dt2>` | Rt2 (5b) | Is the 64-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "64-bit" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512 to 504, defaul |  |

**Pseudocode (Decode):**
```
// Empty.
```

### Encoding: `LDNP_Q_ldstnapair_offs`  [128-bit]  — LDNP

**Assembly:** `LDNP  <Qt1>, <Qt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "128-bit" variant: is the optional signed immediate byte offset, a multiple of 16 in the range -1024 to 1008, de |  |

**Pseudocode (Decode):**
```
// Empty.
```

---

## LDNP -- A64

### Encoding: `LDNP_32_ldstnapair_offs`  [32-bit]  — LDNP

**Assembly:** `LDNP  <Wt1>, <Wt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm7 (7b) | For the "32-bit" variant: is the optional signed immediate byte offset, a multiple of 4 in the range -256 to 252, defaul | let offset : bits(64) = LSL(SignExtend{64}(imm7), scale); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let nontemporal : boolean = TRUE;
let scale : integer{} = 2 + UInt(opc[1]);
let datasize : integer{} = 8 << scale;
let offset : bits(64) = LSL(SignExtend{64}(imm7), scale);
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // Result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDNP_64_ldstnapair_offs`  [64-bit]  — LDNP

**Assembly:** `LDNP  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm7 (7b) | For the "64-bit" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512 to 504, defaul | let offset : bits(64) = LSL(SignExtend{64}(imm7), scale); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let nontemporal : boolean = TRUE;
let scale : integer{} = 2 + UInt(opc[1]);
let datasize : integer{} = 8 << scale;
let offset : bits(64) = LSL(SignExtend{64}(imm7), scale);
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // Result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDNT1B (scalar plus immediate, consecutive registers) -- A64

### Encoding: `ldnt1b_mz_p_bi_2`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt1>.B-<Zt2>.B }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnt1b_mz_p_bi_4`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt1>.B-<Zt4>.B }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## LDNT1B (scalar plus immediate, single register) -- A64

### Encoding: `ldnt1b_z_p_bi_contiguous`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt>.B }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## LDNT1B (scalar plus immediate, strided registers) -- A64

### Encoding: `ldnt1b_mzx_p_bi_2x8`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt1>.B, <Zt2>.B }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnt1b_mzx_p_bi_4x4`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## LDNT1B (scalar plus scalar, consecutive registers) -- A64

### Encoding: `ldnt1b_mz_p_br_2`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt1>.B-<Zt2>.B }, <PNg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 8;
```

### Encoding: `ldnt1b_mz_p_br_4`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt1>.B-<Zt4>.B }, <PNg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 8;
```

---

## LDNT1B (scalar plus scalar, single register) -- A64

### Encoding: `ldnt1b_z_p_br_contiguous`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt>.B }, <Pg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
```

---

## LDNT1B (scalar plus scalar, strided registers) -- A64

### Encoding: `ldnt1b_mzx_p_br_2x8`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt1>.B, <Zt2>.B }, <PNg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 8;
```

### Encoding: `ldnt1b_mzx_p_br_4x4`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <PNg>/Z, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 8;
```

---

## LDNT1B (vector plus scalar) -- A64

### Encoding: `ldnt1b_z_p_ar_s_x32_unscaled`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

### Encoding: `ldnt1b_z_p_ar_d_64_unscaled`  — LDNT1B

**Assembly:** `LDNT1B  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = TRUE;
```

---

## LDNT1D (scalar plus immediate, consecutive registers) -- A64

### Encoding: `ldnt1d_mz_p_bi_2`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt1>.D-<Zt2>.D }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnt1d_mz_p_bi_4`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt1>.D-<Zt4>.D }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## LDNT1D (scalar plus immediate, single register) -- A64

### Encoding: `ldnt1d_z_p_bi_contiguous`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## LDNT1D (scalar plus immediate, strided registers) -- A64

### Encoding: `ldnt1d_mzx_p_bi_2x8`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt1>.D, <Zt2>.D }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnt1d_mzx_p_bi_4x4`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## LDNT1D (scalar plus scalar, consecutive registers) -- A64

### Encoding: `ldnt1d_mz_p_br_2`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt1>.D-<Zt2>.D }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 64;
```

### Encoding: `ldnt1d_mz_p_br_4`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt1>.D-<Zt4>.D }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 64;
```

---

## LDNT1D (scalar plus scalar, single register) -- A64

### Encoding: `ldnt1d_z_p_br_contiguous`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
```

---

## LDNT1D (scalar plus scalar, strided registers) -- A64

### Encoding: `ldnt1d_mzx_p_br_2x8`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt1>.D, <Zt2>.D }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 64;
```

### Encoding: `ldnt1d_mzx_p_br_4x4`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 64;
```

---

## LDNT1D (vector plus scalar) -- A64

### Encoding: `ldnt1d_z_p_ar_d_64_unscaled`  — LDNT1D

**Assembly:** `LDNT1D  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let unsigned : boolean = TRUE;
```

---

## LDNT1H (scalar plus immediate, consecutive registers) -- A64

### Encoding: `ldnt1h_mz_p_bi_2`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt1>.H-<Zt2>.H }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnt1h_mz_p_bi_4`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt1>.H-<Zt4>.H }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## LDNT1H (scalar plus immediate, single register) -- A64

### Encoding: `ldnt1h_z_p_bi_contiguous`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt>.H }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## LDNT1H (scalar plus immediate, strided registers) -- A64

### Encoding: `ldnt1h_mzx_p_bi_2x8`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt1>.H, <Zt2>.H }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnt1h_mzx_p_bi_4x4`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## LDNT1H (scalar plus scalar, consecutive registers) -- A64

### Encoding: `ldnt1h_mz_p_br_2`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt1>.H-<Zt2>.H }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 16;
```

### Encoding: `ldnt1h_mz_p_br_4`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt1>.H-<Zt4>.H }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 16;
```

---

## LDNT1H (scalar plus scalar, single register) -- A64

### Encoding: `ldnt1h_z_p_br_contiguous`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt>.H }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
```

---

## LDNT1H (scalar plus scalar, strided registers) -- A64

### Encoding: `ldnt1h_mzx_p_br_2x8`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt1>.H, <Zt2>.H }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 16;
```

### Encoding: `ldnt1h_mzx_p_br_4x4`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 16;
```

---

## LDNT1H (vector plus scalar) -- A64

### Encoding: `ldnt1h_z_p_ar_s_x32_unscaled`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
```

### Encoding: `ldnt1h_z_p_ar_d_64_unscaled`  — LDNT1H

**Assembly:** `LDNT1H  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = TRUE;
```

---

## LDNT1SB -- A64

### Encoding: `ldnt1sb_z_p_ar_s_x32_unscaled`  — LDNT1SB

**Assembly:** `LDNT1SB  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
```

### Encoding: `ldnt1sb_z_p_ar_d_64_unscaled`  — LDNT1SB

**Assembly:** `LDNT1SB  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let unsigned : boolean = FALSE;
```

---

## LDNT1SH -- A64

### Encoding: `ldnt1sh_z_p_ar_s_x32_unscaled`  — LDNT1SH

**Assembly:** `LDNT1SH  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
```

### Encoding: `ldnt1sh_z_p_ar_d_64_unscaled`  — LDNT1SH

**Assembly:** `LDNT1SH  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let unsigned : boolean = FALSE;
```

---

## LDNT1SW -- A64

### Encoding: `ldnt1sw_z_p_ar_d_64_unscaled`  — LDNT1SW

**Assembly:** `LDNT1SW  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = FALSE;
```

---

## LDNT1W (scalar plus immediate, consecutive registers) -- A64

### Encoding: `ldnt1w_mz_p_bi_2`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt1>.S-<Zt2>.S }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnt1w_mz_p_bi_4`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt1>.S-<Zt4>.S }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## LDNT1W (scalar plus immediate, single register) -- A64

### Encoding: `ldnt1w_z_p_bi_contiguous`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## LDNT1W (scalar plus immediate, strided registers) -- A64

### Encoding: `ldnt1w_mzx_p_bi_2x8`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt1>.S, <Zt2>.S }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

### Encoding: `ldnt1w_mzx_p_bi_4x4`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## LDNT1W (scalar plus scalar, consecutive registers) -- A64

### Encoding: `ldnt1w_mz_p_br_2`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt1>.S-<Zt2>.S }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 32;
```

### Encoding: `ldnt1w_mz_p_br_4`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt1>.S-<Zt4>.S }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 32;
```

---

## LDNT1W (scalar plus scalar, single register) -- A64

### Encoding: `ldnt1w_z_p_br_contiguous`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
```

---

## LDNT1W (scalar plus scalar, strided registers) -- A64

### Encoding: `ldnt1w_mzx_p_br_2x8`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt1>.S, <Zt2>.S }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 32;
```

### Encoding: `ldnt1w_mzx_p_br_4x4`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <PNg>/Z, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 32;
```

---

## LDNT1W (vector plus scalar) -- A64

### Encoding: `ldnt1w_z_p_ar_s_x32_unscaled`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt>.S }, <Pg>/Z, [<Zn>.S{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
```

### Encoding: `ldnt1w_z_p_ar_d_64_unscaled`  — LDNT1W

**Assembly:** `LDNT1W  { <Zt>.D }, <Pg>/Z, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let unsigned : boolean = TRUE;
```

---

## LDP (SIMD&FP) -- A64

### Encoding: `LDP_S_ldstpair_post`  [32-bit]  — LDP

**Assembly:** `LDP  <St1>, <St2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St1>` | Rt (5b) | Is the 32-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<St2>` | Rt2 (5b) | Is the 32-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 32-bit" and "Pre-index 32-bit" variants: is the signed immediate byte offset, a multiple of 4 in the |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `LDP_D_ldstpair_post`  [64-bit]  — LDP

**Assembly:** `LDP  <Dt1>, <Dt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt1>` | Rt (5b) | Is the 64-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Dt2>` | Rt2 (5b) | Is the 64-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 64-bit" and "Pre-index 64-bit" variants: is the signed immediate byte offset, a multiple of 8 in the |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `LDP_Q_ldstpair_post`  [128-bit]  — LDP

**Assembly:** `LDP  <Qt1>, <Qt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 128-bit" and "Pre-index 128-bit" variants: is the signed immediate byte offset, a multiple of 16 in  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `LDP_S_ldstpair_pre`  [32-bit]  — LDP

**Assembly:** `LDP  <St1>, <St2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St1>` | Rt (5b) | Is the 32-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<St2>` | Rt2 (5b) | Is the 32-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 32-bit" and "Pre-index 32-bit" variants: is the signed immediate byte offset, a multiple of 4 in the |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `LDP_D_ldstpair_pre`  [64-bit]  — LDP

**Assembly:** `LDP  <Dt1>, <Dt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt1>` | Rt (5b) | Is the 64-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Dt2>` | Rt2 (5b) | Is the 64-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 64-bit" and "Pre-index 64-bit" variants: is the signed immediate byte offset, a multiple of 8 in the |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `LDP_Q_ldstpair_pre`  [128-bit]  — LDP

**Assembly:** `LDP  <Qt1>, <Qt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 128-bit" and "Pre-index 128-bit" variants: is the signed immediate byte offset, a multiple of 16 in  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `LDP_S_ldstpair_off`  [32-bit]  — LDP

**Assembly:** `LDP  <St1>, <St2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St1>` | Rt (5b) | Is the 32-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<St2>` | Rt2 (5b) | Is the 32-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 32-bit" variant: is the optional signed immediate byte offset, a multiple of 4 in the range -256  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

### Encoding: `LDP_D_ldstpair_off`  [64-bit]  — LDP

**Assembly:** `LDP  <Dt1>, <Dt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt1>` | Rt (5b) | Is the 64-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Dt2>` | Rt2 (5b) | Is the 64-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 64-bit" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

### Encoding: `LDP_Q_ldstpair_off`  [128-bit]  — LDP

**Assembly:** `LDP  <Qt1>, <Qt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 128-bit" variant: is the optional signed immediate byte offset, a multiple of 16 in the range -10 |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## LDP -- A64

### Encoding: `LDP_32_ldstpair_post`  [32-bit]  — LDP

**Assembly:** `LDP  <Wt1>, <Wt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 32-bit" and "Pre-index 32-bit" variants: is the signed immediate byte offset, a multiple of 4 in the |  |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `LDP_64_ldstpair_post`  [64-bit]  — LDP

**Assembly:** `LDP  <Xt1>, <Xt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 64-bit" and "Pre-index 64-bit" variants: is the signed immediate byte offset, a multiple of 8 in the |  |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `LDP_32_ldstpair_pre`  [32-bit]  — LDP

**Assembly:** `LDP  <Wt1>, <Wt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 32-bit" and "Pre-index 32-bit" variants: is the signed immediate byte offset, a multiple of 4 in the |  |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `LDP_64_ldstpair_pre`  [64-bit]  — LDP

**Assembly:** `LDP  <Xt1>, <Xt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 64-bit" and "Pre-index 64-bit" variants: is the signed immediate byte offset, a multiple of 8 in the |  |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `LDP_32_ldstpair_off`  [32-bit]  — LDP

**Assembly:** `LDP  <Wt1>, <Wt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 32-bit" variant: is the optional signed immediate byte offset, a multiple of 4 in the range -256  |  |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

### Encoding: `LDP_64_ldstpair_off`  [64-bit]  — LDP

**Assembly:** `LDP  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 64-bit" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512  |  |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## LDPSW -- A64

### Encoding: `LDPSW_64_ldstpair_post`  — LDPSW

**Assembly:** `LDPSW  <Xt1>, <Xt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 4 in the range -256 to |  |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `LDPSW_64_ldstpair_pre`  — LDPSW

**Assembly:** `LDPSW  <Xt1>, <Xt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 4 in the range -256 to |  |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `LDPSW_64_ldstpair_off`  — LDPSW

**Assembly:** `LDPSW  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset" variant: is the optional signed immediate byte offset, a multiple of 4 in the range -256 to 252, |  |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## LDR (array vector) -- A64

### Encoding: `ldr_za_ri_`  — LDR

**Assembly:** `LDR  ZA[<Wv>, <offs>], [<Xn|SP>{, #<offs>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W12-W15, encoded in the "Rv" field. | let v : integer = UInt('011'::Rv); |
| `<offs>` | off4 (4b) | Is the vector select offset and optional memory offset, in the range 0 to 15, defaulting to 0, encoded in the "off4" fie | let offset : integer = UInt(off4); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let v : integer = UInt('011'::Rv);
let offset : integer = UInt(off4);
```

---

## LDR (immediate) -- A64

### Encoding: `LDR_32_ldst_immpost`  [32-bit]  — LDR

**Assembly:** `LDR  <Wt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_64_ldst_immpost`  [64-bit]  — LDR

**Assembly:** `LDR  <Xt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_32_ldst_immpre`  [32-bit]  — LDR

**Assembly:** `LDR  <Wt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_64_ldst_immpre`  [64-bit]  — LDR

**Assembly:** `LDR  <Xt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_32_ldst_pos`  [32-bit]  — LDR

**Assembly:** `LDR  <Wt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "32-bit" variant: is the optional positive immediate byte offset, a multiple of 4 in the range 0 to 16380, defau | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
let scale : integer{} = UInt(size);
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `LDR_64_ldst_pos`  [64-bit]  — LDR

**Assembly:** `LDR  <Xt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "64-bit" variant: is the optional positive immediate byte offset, a multiple of 8 in the range 0 to 32760, defau | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
let scale : integer{} = UInt(size);
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

---

## LDR (immediate, SIMD&FP) -- A64

### Encoding: `LDR_B_ldst_immpost`  [8-bit]  — LDR

**Assembly:** `LDR  <Bt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_H_ldst_immpost`  [16-bit]  — LDR

**Assembly:** `LDR  <Ht>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_S_ldst_immpost`  [32-bit]  — LDR

**Assembly:** `LDR  <St>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_D_ldst_immpost`  [64-bit]  — LDR

**Assembly:** `LDR  <Dt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_Q_ldst_immpost`  [128-bit]  — LDR

**Assembly:** `LDR  <Qt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_B_ldst_immpre`  [8-bit]  — LDR

**Assembly:** `LDR  <Bt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_H_ldst_immpre`  [16-bit]  — LDR

**Assembly:** `LDR  <Ht>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_S_ldst_immpre`  [32-bit]  — LDR

**Assembly:** `LDR  <St>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_D_ldst_immpre`  [64-bit]  — LDR

**Assembly:** `LDR  <Dt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_Q_ldst_immpre`  [128-bit]  — LDR

**Assembly:** `LDR  <Qt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDR_B_ldst_pos`  [8-bit]  — LDR

**Assembly:** `LDR  <Bt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "8-bit" variant: is the optional positive immediate byte offset, in the range 0 to 4095, defaulting to 0 and enc | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `LDR_H_ldst_pos`  [16-bit]  — LDR

**Assembly:** `LDR  <Ht>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "16-bit" variant: is the optional positive immediate byte offset, a multiple of 2 in the range 0 to 8190, defaul | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `LDR_S_ldst_pos`  [32-bit]  — LDR

**Assembly:** `LDR  <St>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "32-bit" variant: is the optional positive immediate byte offset, a multiple of 4 in the range 0 to 16380, defau | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `LDR_D_ldst_pos`  [64-bit]  — LDR

**Assembly:** `LDR  <Dt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "64-bit" variant: is the optional positive immediate byte offset, a multiple of 8 in the range 0 to 32760, defau | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `LDR_Q_ldst_pos`  [128-bit]  — LDR

**Assembly:** `LDR  <Qt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "128-bit" variant: is the optional positive immediate byte offset, a multiple of 16 in the range 0 to 65520, def | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

---

## LDR (literal) -- A64

### Encoding: `LDR_32_loadlit`  [32-bit]  — LDR

**Assembly:** `LDR  <Wt>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label from which the data is to be loaded. Its offset from the address of this instruction, in the range  | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let size : integer{} = 4 << UInt(opc[0]);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = FALSE;

let offset : bits(64) = SignExtend{}(imm19::'00');
```

### Encoding: `LDR_64_loadlit`  [64-bit]  — LDR

**Assembly:** `LDR  <Xt>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label from which the data is to be loaded. Its offset from the address of this instruction, in the range  | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let size : integer{} = 4 << UInt(opc[0]);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = FALSE;

let offset : bits(64) = SignExtend{}(imm19::'00');
```

---

## LDR (literal, SIMD&FP) -- A64

### Encoding: `LDR_S_loadlit`  [32-bit]  — LDR

**Assembly:** `LDR  <St>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label from which the data is to be loaded. Its offset from the address of this instruction, in the range  | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
if opc == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 4 << (UInt(opc) as integer{0..2});
let nontemporal : boolean = FALSE;
let tagchecked : boolean = FALSE;

let offset : bits(64) = SignExtend{}(imm19::'00');
```

### Encoding: `LDR_D_loadlit`  [64-bit]  — LDR

**Assembly:** `LDR  <Dt>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label from which the data is to be loaded. Its offset from the address of this instruction, in the range  | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
if opc == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 4 << (UInt(opc) as integer{0..2});
let nontemporal : boolean = FALSE;
let tagchecked : boolean = FALSE;

let offset : bits(64) = SignExtend{}(imm19::'00');
```

### Encoding: `LDR_Q_loadlit`  [128-bit]  — LDR

**Assembly:** `LDR  <Qt>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label from which the data is to be loaded. Its offset from the address of this instruction, in the range  | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
if opc == '11' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 4 << (UInt(opc) as integer{0..2});
let nontemporal : boolean = FALSE;
let tagchecked : boolean = FALSE;

let offset : bits(64) = SignExtend{}(imm19::'00');
```

---

## LDR (predicate) -- A64

### Encoding: `ldr_p_bi_`  — LDR

**Assembly:** `LDR  <Pt>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pt>` | Pt (4b) | Is the name of the destination scalable predicate register, encoded in the "Pt" field. | let t : integer = UInt(Pt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | (imm9h :: imm9l) | Is the optional signed immediate vector offset, in the range -256 to 255, defaulting to 0, encoded in the "imm9h:imm9l"  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Pt);
let n : integer = UInt(Rn);
let imm : integer = SInt(imm9h::imm9l);
```

---

## LDR (register) -- A64

### Encoding: `LDR_32_ldst_regoff`  [32-bit]  — LDR

**Assembly:** `LDR  <Wt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let scale : integer{} = UInt(size);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `LDR_64_ldst_regoff`  [64-bit]  — LDR

**Assembly:** `LDR  <Xt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let scale : integer{} = UInt(size);
let shift : integer{} = if S == '1' then scale else 0;
```

---

## LDR (register, SIMD&FP) -- A64

### Encoding: `LDR_B_ldst_regoff`  [8-bit with extended register offset]  — LDR

**Assembly:** `LDR  <Bt>, [<Xn|SP>, (<Wm>|<Xm>), <extend> {<amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | S (1b) | For the "8-bit with extended register offset" and "8-bit with shifted register offset" variants: is the index shift amou | let shift : integer{} = if S == '1' then scale else 0; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `LDR_BL_ldst_regoff`  [8-bit with shifted register offset]  — LDR

**Assembly:** `LDR  <Bt>, [<Xn|SP>, <Xm>{, LSL <amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<amount>` | S (1b) | For the "8-bit with extended register offset" and "8-bit with shifted register offset" variants: is the index shift amou | let shift : integer{} = if S == '1' then scale else 0; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `LDR_H_ldst_regoff`  [16-bit]  — LDR

**Assembly:** `LDR  <Ht>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `LDR_S_ldst_regoff`  [32-bit]  — LDR

**Assembly:** `LDR  <St>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `LDR_D_ldst_regoff`  [64-bit]  — LDR

**Assembly:** `LDR  <Dt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `LDR_Q_ldst_regoff`  [128-bit]  — LDR

**Assembly:** `LDR  <Qt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

---

## LDR (table) -- A64

### Encoding: `ldr_zt_br_`  — LDR

**Assembly:** `LDR  ZT0, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
```

---

## LDR (vector) -- A64

### Encoding: `ldr_z_bi_`  — LDR

**Assembly:** `LDR  <Zt>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | (imm9h :: imm9l) | Is the optional signed immediate vector offset, in the range -256 to 255, defaulting to 0, encoded in the "imm9h:imm9l"  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let imm : integer = SInt(imm9h::imm9l);
```

---

## LDRAA, LDRAB -- A64

### Encoding: `LDRAA_64_ldst_pac`  [Key A, offset]  — LDRAA

**Assembly:** `LDRAA  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<simm>` | (S :: imm9) | Is the optional signed immediate byte offset, a multiple of 8 in the range -4096 to 4088, defaulting to 0 and encoded in |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
var wback : boolean = W == '1';
let use_key_a : boolean = M == '0';
let S10 : bits(10) = S::imm9;
let offset : bits(64) = LSL(SignExtend{64}(S10), 3);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;

var wb_unknown : boolean = FALSE;
if wback && n == t && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_WBOVERLAPLD);
    assert c IN {Constraint_WBSUPPRESS, Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_WBSUPPRESS => wback = FALSE;       // writeback is suppressed
        when Constraint_UNKNOWN =>    wb_unknown = TRUE;   // writeback is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDRAA_64W_ldst_pac`  [Key A, pre-indexed]  — LDRAA

**Assembly:** `LDRAA  <Xt>, [<Xn|SP>{, #<simm>}]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<simm>` | (S :: imm9) | Is the optional signed immediate byte offset, a multiple of 8 in the range -4096 to 4088, defaulting to 0 and encoded in |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
var wback : boolean = W == '1';
let use_key_a : boolean = M == '0';
let S10 : bits(10) = S::imm9;
let offset : bits(64) = LSL(SignExtend{64}(S10), 3);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;

var wb_unknown : boolean = FALSE;
if wback && n == t && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_WBOVERLAPLD);
    assert c IN {Constraint_WBSUPPRESS, Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_WBSUPPRESS => wback = FALSE;       // writeback is suppressed
        when Constraint_UNKNOWN =>    wb_unknown = TRUE;   // writeback is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDRAB_64_ldst_pac`  [Key B, offset]  — LDRAB

**Assembly:** `LDRAB  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<simm>` | (S :: imm9) | Is the optional signed immediate byte offset, a multiple of 8 in the range -4096 to 4088, defaulting to 0 and encoded in |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
var wback : boolean = W == '1';
let use_key_a : boolean = M == '0';
let S10 : bits(10) = S::imm9;
let offset : bits(64) = LSL(SignExtend{64}(S10), 3);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;

var wb_unknown : boolean = FALSE;
if wback && n == t && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_WBOVERLAPLD);
    assert c IN {Constraint_WBSUPPRESS, Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_WBSUPPRESS => wback = FALSE;       // writeback is suppressed
        when Constraint_UNKNOWN =>    wb_unknown = TRUE;   // writeback is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDRAB_64W_ldst_pac`  [Key B, pre-indexed]  — LDRAB

**Assembly:** `LDRAB  <Xt>, [<Xn|SP>{, #<simm>}]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<simm>` | (S :: imm9) | Is the optional signed immediate byte offset, a multiple of 8 in the range -4096 to 4088, defaulting to 0 and encoded in |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
var wback : boolean = W == '1';
let use_key_a : boolean = M == '0';
let S10 : bits(10) = S::imm9;
let offset : bits(64) = LSL(SignExtend{64}(S10), 3);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;

var wb_unknown : boolean = FALSE;
if wback && n == t && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_WBOVERLAPLD);
    assert c IN {Constraint_WBSUPPRESS, Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_WBSUPPRESS => wback = FALSE;       // writeback is suppressed
        when Constraint_UNKNOWN =>    wb_unknown = TRUE;   // writeback is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDRB (immediate) -- A64

### Encoding: `LDRB_32_ldst_immpost`  — LDRB

**Assembly:** `LDRB  <Wt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRB_32_ldst_immpre`  — LDRB

**Assembly:** `LDRB  <Wt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRB_32_ldst_pos`  — LDRB

**Assembly:** `LDRB  <Wt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, in the range 0 to 4095, defaulting to 0 and encoded in the "imm12" field | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 0); |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 0);
```

---

## LDRB (register) -- A64

### Encoding: `LDRB_32B_ldst_regoff`  [Extended register]  — LDRB

**Assembly:** `LDRB  <Wt>, [<Xn|SP>, (<Wm>|<Xm>), <extend> {<amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | S (1b) | Is the index shift amount, it must be #0, encoded in "S" as 0 if omitted, or as 1 if present. |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = 0;
```

### Encoding: `LDRB_32BL_ldst_regoff`  [Shifted register]  — LDRB

**Assembly:** `LDRB  <Wt>, [<Xn|SP>, <Xm>{, LSL <amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<amount>` | S (1b) | Is the index shift amount, it must be #0, encoded in "S" as 0 if omitted, or as 1 if present. |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = 0;
```

---

## LDRH (immediate) -- A64

### Encoding: `LDRH_32_ldst_immpost`  — LDRH

**Assembly:** `LDRH  <Wt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRH_32_ldst_immpre`  — LDRH

**Assembly:** `LDRH  <Wt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRH_32_ldst_pos`  — LDRH

**Assembly:** `LDRH  <Wt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, a multiple of 2 in the range 0 to 8190, defaulting to 0 and encoded in t | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 1); |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 1);
```

---

## LDRH (register) -- A64

### Encoding: `LDRH_32_ldst_regoff`  — LDRH

**Assembly:** `LDRH  <Wt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then 1 else 0;
```

---

## LDRSB (immediate) -- A64

### Encoding: `LDRSB_32_ldst_immpost`  [32-bit]  — LDRSB

**Assembly:** `LDRSB  <Wt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSB_64_ldst_immpost`  [64-bit]  — LDRSB

**Assembly:** `LDRSB  <Xt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSB_32_ldst_immpre`  [32-bit]  — LDRSB

**Assembly:** `LDRSB  <Wt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSB_64_ldst_immpre`  [64-bit]  — LDRSB

**Assembly:** `LDRSB  <Xt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSB_32_ldst_pos`  [32-bit]  — LDRSB

**Assembly:** `LDRSB  <Wt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, in the range 0 to 4095, defaulting to 0 and encoded in the "imm12" field | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 0); |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 0);
```

### Encoding: `LDRSB_64_ldst_pos`  [64-bit]  — LDRSB

**Assembly:** `LDRSB  <Xt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, in the range 0 to 4095, defaulting to 0 and encoded in the "imm12" field | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 0); |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 0);
```

---

## LDRSB (register) -- A64

### Encoding: `LDRSB_32B_ldst_regoff`  [32-bit with extended register offset]  — LDRSB

**Assembly:** `LDRSB  <Wt>, [<Xn|SP>, (<Wm>|<Xm>), <extend> {<amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | S (1b) | Is the index shift amount, it must be #0, encoded in "S" as 0 if omitted, or as 1 if present. |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = 0;
```

### Encoding: `LDRSB_32BL_ldst_regoff`  [32-bit with shifted register offset]  — LDRSB

**Assembly:** `LDRSB  <Wt>, [<Xn|SP>, <Xm>{, LSL <amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<amount>` | S (1b) | Is the index shift amount, it must be #0, encoded in "S" as 0 if omitted, or as 1 if present. |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = 0;
```

### Encoding: `LDRSB_64B_ldst_regoff`  [64-bit with extended register offset]  — LDRSB

**Assembly:** `LDRSB  <Xt>, [<Xn|SP>, (<Wm>|<Xm>), <extend> {<amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | S (1b) | Is the index shift amount, it must be #0, encoded in "S" as 0 if omitted, or as 1 if present. |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = 0;
```

### Encoding: `LDRSB_64BL_ldst_regoff`  [64-bit with shifted register offset]  — LDRSB

**Assembly:** `LDRSB  <Xt>, [<Xn|SP>, <Xm>{, LSL <amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<amount>` | S (1b) | Is the index shift amount, it must be #0, encoded in "S" as 0 if omitted, or as 1 if present. |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = 0;
```

---

## LDRSH (immediate) -- A64

### Encoding: `LDRSH_32_ldst_immpost`  [32-bit]  — LDRSH

**Assembly:** `LDRSH  <Wt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSH_64_ldst_immpost`  [64-bit]  — LDRSH

**Assembly:** `LDRSH  <Xt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSH_32_ldst_immpre`  [32-bit]  — LDRSH

**Assembly:** `LDRSH  <Wt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSH_64_ldst_immpre`  [64-bit]  — LDRSH

**Assembly:** `LDRSH  <Xt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSH_32_ldst_pos`  [32-bit]  — LDRSH

**Assembly:** `LDRSH  <Wt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, a multiple of 2 in the range 0 to 8190, defaulting to 0 and encoded in t | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 1); |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 1);
```

### Encoding: `LDRSH_64_ldst_pos`  [64-bit]  — LDRSH

**Assembly:** `LDRSH  <Xt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, a multiple of 2 in the range 0 to 8190, defaulting to 0 and encoded in t | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 1); |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 1);
```

---

## LDRSH (register) -- A64

### Encoding: `LDRSH_32_ldst_regoff`  [32-bit]  — LDRSH

**Assembly:** `LDRSH  <Wt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then 1 else 0;
```

### Encoding: `LDRSH_64_ldst_regoff`  [64-bit]  — LDRSH

**Assembly:** `LDRSH  <Xt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then 1 else 0;
```

---

## LDRSW (immediate) -- A64

### Encoding: `LDRSW_64_ldst_immpost`  — LDRSW

**Assembly:** `LDRSW  <Xt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSW_64_ldst_immpre`  — LDRSW

**Assembly:** `LDRSW  <Xt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDRSW_64_ldst_pos`  — LDRSW

**Assembly:** `LDRSW  <Xt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, a multiple of 4 in the range 0 to 16380, defaulting to 0 and encoded in  | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 2); |

**Pseudocode (Decode):**
```
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 2);
```

---

## LDRSW (literal) -- A64

### Encoding: `LDRSW_64_loadlit`  — LDRSW

**Assembly:** `LDRSW  <Xt>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label from which the data is to be loaded. Its offset from the address of this instruction, in the range  | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = FALSE;

let offset : bits(64) = SignExtend{}(imm19::'00');
```

---

## LDRSW (register) -- A64

### Encoding: `LDRSW_64_ldst_regoff`  — LDRSW

**Assembly:** `LDRSW  <Xt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then 2 else 0;
```

---

## LDSET, LDSETA, LDSETAL, LDSETL -- A64

### Encoding: `LDSET_32_memop`  [32-bit no memory ordering]  — LDSET

**Assembly:** `LDSET  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETA_32_memop`  [32-bit acquire]  — LDSETA

**Assembly:** `LDSETA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETAL_32_memop`  [32-bit acquire-release]  — LDSETAL

**Assembly:** `LDSETAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETL_32_memop`  [32-bit release]  — LDSETL

**Assembly:** `LDSETL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSET_64_memop`  [64-bit no memory ordering]  — LDSET

**Assembly:** `LDSET  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETA_64_memop`  [64-bit acquire]  — LDSETA

**Assembly:** `LDSETA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETAL_64_memop`  [64-bit acquire-release]  — LDSETAL

**Assembly:** `LDSETAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETL_64_memop`  [64-bit release]  — LDSETL

**Assembly:** `LDSETL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDSETB, LDSETAB, LDSETALB, LDSETLB -- A64

### Encoding: `LDSETB_32_memop`  [No memory ordering]  — LDSETB

**Assembly:** `LDSETB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETAB_32_memop`  [Acquire]  — LDSETAB

**Assembly:** `LDSETAB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETALB_32_memop`  [Acquire-release]  — LDSETALB

**Assembly:** `LDSETALB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETLB_32_memop`  [Release]  — LDSETLB

**Assembly:** `LDSETLB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDSETH, LDSETAH, LDSETALH, LDSETLH -- A64

### Encoding: `LDSETH_32_memop`  [No memory ordering]  — LDSETH

**Assembly:** `LDSETH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETAH_32_memop`  [Acquire]  — LDSETAH

**Assembly:** `LDSETAH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETALH_32_memop`  [Acquire-release]  — LDSETALH

**Assembly:** `LDSETALH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSETLH_32_memop`  [Release]  — LDSETLH

**Assembly:** `LDSETLH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDSETP, LDSETPA, LDSETPAL, LDSETPL -- A64

### Encoding: `LDSETP_128_memop_128`  [LDSETP]  — LDSETP

**Assembly:** `LDSETP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDSETPA_128_memop_128`  [LDSETPA]  — LDSETPA

**Assembly:** `LDSETPA  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDSETPAL_128_memop_128`  [LDSETPAL]  — LDSETPAL

**Assembly:** `LDSETPAL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDSETPL_128_memop_128`  [LDSETPL]  — LDSETPL

**Assembly:** `LDSETPL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDSMAX, LDSMAXA, LDSMAXAL, LDSMAXL -- A64

### Encoding: `LDSMAX_32_memop`  [32-bit no memory ordering]  — LDSMAX

**Assembly:** `LDSMAX  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXA_32_memop`  [32-bit acquire]  — LDSMAXA

**Assembly:** `LDSMAXA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXAL_32_memop`  [32-bit acquire-release]  — LDSMAXAL

**Assembly:** `LDSMAXAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXL_32_memop`  [32-bit release]  — LDSMAXL

**Assembly:** `LDSMAXL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAX_64_memop`  [64-bit no memory ordering]  — LDSMAX

**Assembly:** `LDSMAX  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXA_64_memop`  [64-bit acquire]  — LDSMAXA

**Assembly:** `LDSMAXA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXAL_64_memop`  [64-bit acquire-release]  — LDSMAXAL

**Assembly:** `LDSMAXAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXL_64_memop`  [64-bit release]  — LDSMAXL

**Assembly:** `LDSMAXL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDSMAXB, LDSMAXAB, LDSMAXALB, LDSMAXLB -- A64

### Encoding: `LDSMAXB_32_memop`  [No memory ordering]  — LDSMAXB

**Assembly:** `LDSMAXB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXAB_32_memop`  [Acquire]  — LDSMAXAB

**Assembly:** `LDSMAXAB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXALB_32_memop`  [Acquire-release]  — LDSMAXALB

**Assembly:** `LDSMAXALB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXLB_32_memop`  [Release]  — LDSMAXLB

**Assembly:** `LDSMAXLB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDSMAXH, LDSMAXAH, LDSMAXALH, LDSMAXLH -- A64

### Encoding: `LDSMAXH_32_memop`  [No memory ordering]  — LDSMAXH

**Assembly:** `LDSMAXH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXAH_32_memop`  [Acquire]  — LDSMAXAH

**Assembly:** `LDSMAXAH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXALH_32_memop`  [Acquire-release]  — LDSMAXALH

**Assembly:** `LDSMAXALH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMAXLH_32_memop`  [Release]  — LDSMAXLH

**Assembly:** `LDSMAXLH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDSMIN, LDSMINA, LDSMINAL, LDSMINL -- A64

### Encoding: `LDSMIN_32_memop`  [32-bit no memory ordering]  — LDSMIN

**Assembly:** `LDSMIN  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINA_32_memop`  [32-bit acquire]  — LDSMINA

**Assembly:** `LDSMINA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINAL_32_memop`  [32-bit acquire-release]  — LDSMINAL

**Assembly:** `LDSMINAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINL_32_memop`  [32-bit release]  — LDSMINL

**Assembly:** `LDSMINL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMIN_64_memop`  [64-bit no memory ordering]  — LDSMIN

**Assembly:** `LDSMIN  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINA_64_memop`  [64-bit acquire]  — LDSMINA

**Assembly:** `LDSMINA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINAL_64_memop`  [64-bit acquire-release]  — LDSMINAL

**Assembly:** `LDSMINAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINL_64_memop`  [64-bit release]  — LDSMINL

**Assembly:** `LDSMINL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDSMINB, LDSMINAB, LDSMINALB, LDSMINLB -- A64

### Encoding: `LDSMINB_32_memop`  [No memory ordering]  — LDSMINB

**Assembly:** `LDSMINB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINAB_32_memop`  [Acquire]  — LDSMINAB

**Assembly:** `LDSMINAB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINALB_32_memop`  [Acquire-release]  — LDSMINALB

**Assembly:** `LDSMINALB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINLB_32_memop`  [Release]  — LDSMINLB

**Assembly:** `LDSMINLB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDSMINH, LDSMINAH, LDSMINALH, LDSMINLH -- A64

### Encoding: `LDSMINH_32_memop`  [No memory ordering]  — LDSMINH

**Assembly:** `LDSMINH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINAH_32_memop`  [Acquire]  — LDSMINAH

**Assembly:** `LDSMINAH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINALH_32_memop`  [Acquire-release]  — LDSMINALH

**Assembly:** `LDSMINALH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDSMINLH_32_memop`  [Release]  — LDSMINLH

**Assembly:** `LDSMINLH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDTADD, LDTADDA, LDTADDAL, LDTADDL -- A64

### Encoding: `LDTADD_32_memop_unpriv`  [32-bit no memory ordering]  — LDTADD

**Assembly:** `LDTADD  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTADDA_32_memop_unpriv`  [32-bit acquire]  — LDTADDA

**Assembly:** `LDTADDA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTADDAL_32_memop_unpriv`  [32-bit acquire-release]  — LDTADDAL

**Assembly:** `LDTADDAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTADDL_32_memop_unpriv`  [32-bit release]  — LDTADDL

**Assembly:** `LDTADDL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTADD_64_memop_unpriv`  [64-bit no memory ordering]  — LDTADD

**Assembly:** `LDTADD  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTADDA_64_memop_unpriv`  [64-bit acquire]  — LDTADDA

**Assembly:** `LDTADDA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTADDAL_64_memop_unpriv`  [64-bit acquire-release]  — LDTADDAL

**Assembly:** `LDTADDAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTADDL_64_memop_unpriv`  [64-bit release]  — LDTADDL

**Assembly:** `LDTADDL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDTCLR, LDTCLRA, LDTCLRAL, LDTCLRL -- A64

### Encoding: `LDTCLR_32_memop_unpriv`  [32-bit no memory ordering]  — LDTCLR

**Assembly:** `LDTCLR  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTCLRA_32_memop_unpriv`  [32-bit acquire]  — LDTCLRA

**Assembly:** `LDTCLRA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTCLRAL_32_memop_unpriv`  [32-bit acquire-release]  — LDTCLRAL

**Assembly:** `LDTCLRAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTCLRL_32_memop_unpriv`  [32-bit release]  — LDTCLRL

**Assembly:** `LDTCLRL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTCLR_64_memop_unpriv`  [64-bit no memory ordering]  — LDTCLR

**Assembly:** `LDTCLR  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTCLRA_64_memop_unpriv`  [64-bit acquire]  — LDTCLRA

**Assembly:** `LDTCLRA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTCLRAL_64_memop_unpriv`  [64-bit acquire-release]  — LDTCLRAL

**Assembly:** `LDTCLRAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTCLRL_64_memop_unpriv`  [64-bit release]  — LDTCLRL

**Assembly:** `LDTCLRL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDTNP (SIMD&FP) -- A64

### Encoding: `LDTNP_Q_ldstnapair_offs`  — LDTNP

**Assembly:** `LDTNP  <Qt1>, <Qt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm7 (7b) | Is the optional signed immediate byte offset, a multiple of 16 in the range -1024 to 1008, defaulting to 0 and encoded i | let offset : bits(64) = LSL(SignExtend{64}(imm7), 4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LSUI) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let nontemporal : boolean = TRUE;
let datasize : integer{} = 128;
let offset : bits(64) = LSL(SignExtend{64}(imm7), 4);
let tagchecked : boolean = n != 31;
var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // Result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDTNP -- A64

### Encoding: `LDTNP_64_ldstnapair_offs`  — LDTNP

**Assembly:** `LDTNP  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm7 (7b) | Is the optional signed immediate byte offset, a multiple of 8 in the range -512 to 504, defaulting to 0 and encoded in t | let offset : bits(64) = LSL(SignExtend{64}(imm7), 3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let nontemporal : boolean = TRUE;
let datasize : integer{} = 64;
let offset : bits(64) = LSL(SignExtend{64}(imm7), 3);
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // Result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDTP (SIMD&FP) -- A64

### Encoding: `LDTP_Q_ldstpair_post`  — LDTP

**Assembly:** `LDTP  <Qt1>, <Qt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 16 in the range -1024  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LSUI) then
    EndOfDecode(Decode_UNDEF);
end;

let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `LDTP_Q_ldstpair_pre`  — LDTP

**Assembly:** `LDTP  <Qt1>, <Qt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 16 in the range -1024  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LSUI) then
    EndOfDecode(Decode_UNDEF);
end;

let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `LDTP_Q_ldstpair_off`  — LDTP

**Assembly:** `LDTP  <Qt1>, <Qt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset" variant: is the optional signed immediate byte offset, a multiple of 16 in the range -1024 to 10 |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LSUI) then
    EndOfDecode(Decode_UNDEF);
end;

let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## LDTP -- A64

### Encoding: `LDTP_64_ldstpair_post`  — LDTP

**Assembly:** `LDTP  <Xt1>, <Xt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 8 in the range -512 to |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
var wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `LDTP_64_ldstpair_pre`  — LDTP

**Assembly:** `LDTP  <Xt1>, <Xt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 8 in the range -512 to |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
var wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `LDTP_64_ldstpair_off`  — LDTP

**Assembly:** `LDTP  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512 to 504, |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
var wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## LDTR -- A64

### Encoding: `LDTR_32_ldst_unpriv`  [32-bit]  — LDTR

**Assembly:** `LDTR  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDTR_64_ldst_unpriv`  [64-bit]  — LDTR

**Assembly:** `LDTR  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDTRB -- A64

### Encoding: `LDTRB_32_ldst_unpriv`  — LDTRB

**Assembly:** `LDTRB  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDTRH -- A64

### Encoding: `LDTRH_32_ldst_unpriv`  — LDTRH

**Assembly:** `LDTRH  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDTRSB -- A64

### Encoding: `LDTRSB_32_ldst_unpriv`  [32-bit]  — LDTRSB

**Assembly:** `LDTRSB  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDTRSB_64_ldst_unpriv`  [64-bit]  — LDTRSB

**Assembly:** `LDTRSB  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDTRSH -- A64

### Encoding: `LDTRSH_32_ldst_unpriv`  [32-bit]  — LDTRSH

**Assembly:** `LDTRSH  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDTRSH_64_ldst_unpriv`  [64-bit]  — LDTRSH

**Assembly:** `LDTRSH  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDTRSW -- A64

### Encoding: `LDTRSW_64_ldst_unpriv`  — LDTRSW

**Assembly:** `LDTRSW  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDTSET, LDTSETA, LDTSETAL, LDTSETL -- A64

### Encoding: `LDTSET_32_memop_unpriv`  [32-bit no memory ordering]  — LDTSET

**Assembly:** `LDTSET  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTSETA_32_memop_unpriv`  [32-bit acquire]  — LDTSETA

**Assembly:** `LDTSETA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTSETAL_32_memop_unpriv`  [32-bit acquire-release]  — LDTSETAL

**Assembly:** `LDTSETAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTSETL_32_memop_unpriv`  [32-bit release]  — LDTSETL

**Assembly:** `LDTSETL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTSET_64_memop_unpriv`  [64-bit no memory ordering]  — LDTSET

**Assembly:** `LDTSET  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTSETA_64_memop_unpriv`  [64-bit acquire]  — LDTSETA

**Assembly:** `LDTSETA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTSETAL_64_memop_unpriv`  [64-bit acquire-release]  — LDTSETAL

**Assembly:** `LDTSETAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTSETL_64_memop_unpriv`  [64-bit release]  — LDTSETL

**Assembly:** `LDTSETL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDTXR -- A64

### Encoding: `LDTXR_LR32_ldstexclr_unpriv`  [32-bit]  — LDTXR

**Assembly:** `LDTXR  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

### Encoding: `LDTXR_LR64_ldstexclr_unpriv`  [64-bit]  — LDTXR

**Assembly:** `LDTXR  <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## LDUMAX, LDUMAXA, LDUMAXAL, LDUMAXL -- A64

### Encoding: `LDUMAX_32_memop`  [32-bit no memory ordering]  — LDUMAX

**Assembly:** `LDUMAX  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXA_32_memop`  [32-bit acquire]  — LDUMAXA

**Assembly:** `LDUMAXA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXAL_32_memop`  [32-bit acquire-release]  — LDUMAXAL

**Assembly:** `LDUMAXAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXL_32_memop`  [32-bit release]  — LDUMAXL

**Assembly:** `LDUMAXL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAX_64_memop`  [64-bit no memory ordering]  — LDUMAX

**Assembly:** `LDUMAX  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXA_64_memop`  [64-bit acquire]  — LDUMAXA

**Assembly:** `LDUMAXA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXAL_64_memop`  [64-bit acquire-release]  — LDUMAXAL

**Assembly:** `LDUMAXAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXL_64_memop`  [64-bit release]  — LDUMAXL

**Assembly:** `LDUMAXL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDUMAXB, LDUMAXAB, LDUMAXALB, LDUMAXLB -- A64

### Encoding: `LDUMAXB_32_memop`  [No memory ordering]  — LDUMAXB

**Assembly:** `LDUMAXB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXAB_32_memop`  [Acquire]  — LDUMAXAB

**Assembly:** `LDUMAXAB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXALB_32_memop`  [Acquire-release]  — LDUMAXALB

**Assembly:** `LDUMAXALB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXLB_32_memop`  [Release]  — LDUMAXLB

**Assembly:** `LDUMAXLB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDUMAXH, LDUMAXAH, LDUMAXALH, LDUMAXLH -- A64

### Encoding: `LDUMAXH_32_memop`  [No memory ordering]  — LDUMAXH

**Assembly:** `LDUMAXH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXAH_32_memop`  [Acquire]  — LDUMAXAH

**Assembly:** `LDUMAXAH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXALH_32_memop`  [Acquire-release]  — LDUMAXALH

**Assembly:** `LDUMAXALH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMAXLH_32_memop`  [Release]  — LDUMAXLH

**Assembly:** `LDUMAXLH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDUMIN, LDUMINA, LDUMINAL, LDUMINL -- A64

### Encoding: `LDUMIN_32_memop`  [32-bit no memory ordering]  — LDUMIN

**Assembly:** `LDUMIN  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINA_32_memop`  [32-bit acquire]  — LDUMINA

**Assembly:** `LDUMINA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINAL_32_memop`  [32-bit acquire-release]  — LDUMINAL

**Assembly:** `LDUMINAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINL_32_memop`  [32-bit release]  — LDUMINL

**Assembly:** `LDUMINL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMIN_64_memop`  [64-bit no memory ordering]  — LDUMIN

**Assembly:** `LDUMIN  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINA_64_memop`  [64-bit acquire]  — LDUMINA

**Assembly:** `LDUMINA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINAL_64_memop`  [64-bit acquire-release]  — LDUMINAL

**Assembly:** `LDUMINAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINL_64_memop`  [64-bit release]  — LDUMINL

**Assembly:** `LDUMINL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDUMINB, LDUMINAB, LDUMINALB, LDUMINLB -- A64

### Encoding: `LDUMINB_32_memop`  [No memory ordering]  — LDUMINB

**Assembly:** `LDUMINB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINAB_32_memop`  [Acquire]  — LDUMINAB

**Assembly:** `LDUMINAB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINALB_32_memop`  [Acquire-release]  — LDUMINALB

**Assembly:** `LDUMINALB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINLB_32_memop`  [Release]  — LDUMINLB

**Assembly:** `LDUMINLB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDUMINH, LDUMINAH, LDUMINALH, LDUMINLH -- A64

### Encoding: `LDUMINH_32_memop`  [No memory ordering]  — LDUMINH

**Assembly:** `LDUMINH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINAH_32_memop`  [Acquire]  — LDUMINAH

**Assembly:** `LDUMINAH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINALH_32_memop`  [Acquire-release]  — LDUMINALH

**Assembly:** `LDUMINALH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `LDUMINLH_32_memop`  [Release]  — LDUMINLH

**Assembly:** `LDUMINLH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## LDUR (SIMD&FP) -- A64

### Encoding: `LDUR_B_ldst_unscaled`  [8-bit]  — LDUR

**Assembly:** `LDUR  <Bt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDUR_H_ldst_unscaled`  [16-bit]  — LDUR

**Assembly:** `LDUR  <Ht>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDUR_S_ldst_unscaled`  [32-bit]  — LDUR

**Assembly:** `LDUR  <St>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDUR_D_ldst_unscaled`  [64-bit]  — LDUR

**Assembly:** `LDUR  <Dt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDUR_Q_ldst_unscaled`  [128-bit]  — LDUR

**Assembly:** `LDUR  <Qt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDUR -- A64

### Encoding: `LDUR_32_ldst_unscaled`  [32-bit]  — LDUR

**Assembly:** `LDUR  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDUR_64_ldst_unscaled`  [64-bit]  — LDUR

**Assembly:** `LDUR  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDURB -- A64

### Encoding: `LDURB_32_ldst_unscaled`  — LDURB

**Assembly:** `LDURB  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDURH -- A64

### Encoding: `LDURH_32_ldst_unscaled`  — LDURH

**Assembly:** `LDURH  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDURSB -- A64

### Encoding: `LDURSB_32_ldst_unscaled`  [32-bit]  — LDURSB

**Assembly:** `LDURSB  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDURSB_64_ldst_unscaled`  [64-bit]  — LDURSB

**Assembly:** `LDURSB  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDURSH -- A64

### Encoding: `LDURSH_32_ldst_unscaled`  [32-bit]  — LDURSH

**Assembly:** `LDURSH  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `LDURSH_64_ldst_unscaled`  [64-bit]  — LDURSH

**Assembly:** `LDURSH  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDURSW -- A64

### Encoding: `LDURSW_64_ldst_unscaled`  — LDURSW

**Assembly:** `LDURSW  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## LDXP -- A64

### Encoding: `LDXP_LP32_ldstexclp`  [32-bit]  — LDXP

**Assembly:** `LDXP  <Wt1>, <Wt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let datasize : integer{} = elsize * 2;
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `LDXP_LP64_ldstexclp`  [64-bit]  — LDXP

**Assembly:** `LDXP  <Xt1>, <Xt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let datasize : integer{} = elsize * 2;
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LDPOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## LDXR -- A64

### Encoding: `LDXR_LR32_ldstexclr`  [32-bit]  — LDXR

**Assembly:** `LDXR  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

### Encoding: `LDXR_LR64_ldstexclr`  [64-bit]  — LDXR

**Assembly:** `LDXR  <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 8 << UInt(size);
let regsize : integer{} = if elsize == 64 then 64 else 32;
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## LDXRB -- A64

### Encoding: `LDXRB_LR32_ldstexclr`  — LDXRB

**Assembly:** `LDXRB  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## LDXRH -- A64

### Encoding: `LDXRH_LR32_ldstexclr`  — LDXRH

**Assembly:** `LDXRH  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## LSL (immediate) -- A64 *(alias for UBFM)*

### Encoding: `LSL_UBFM_32M_bitfield`  [32-bit]  — UBFM

**Assembly:** `LSL  <Wd>, <Wn>, #<shift> / UBFM  <Wd>, <Wn>, #(-<shift>  MOD  32), #(31-<shift>)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<shift>` | immr (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31. |  |
| `UBFM` | — |  |  |

### Encoding: `LSL_UBFM_64M_bitfield`  [64-bit]  — UBFM

**Assembly:** `LSL  <Xd>, <Xn>, #<shift> / UBFM  <Xd>, <Xn>, #(-<shift>  MOD  64), #(63-<shift>)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<shift>` | immr (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63. |  |
| `UBFM` | — |  |  |

---

## LSL (immediate, predicated) -- A64

### Encoding: `lsl_z_p_zi_`  — LSL

**Assembly:** `LSL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## LSL (immediate, unpredicated) -- A64

### Encoding: `lsl_z_zi_`  — LSL

**Assembly:** `LSL  <Zd>.<T>, <Zn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## LSL (register) -- A64 *(alias for LSLV)*

### Encoding: `LSL_LSLV_32_dp_2src`  [32-bit]  — LSLV

**Assembly:** `LSL  <Wd>, <Wn>, <Wm> / LSLV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding a shift amount from 0 to 31 in its bottom 5 bit |  |
| `LSLV` | — |  |  |

### Encoding: `LSL_LSLV_64_dp_2src`  [64-bit]  — LSLV

**Assembly:** `LSL  <Xd>, <Xn>, <Xm> / LSLV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding a shift amount from 0 to 63 in its bottom 6 bit |  |
| `LSLV` | — |  |  |

---

## LSL (vectors) -- A64

### Encoding: `lsl_z_p_zz_`  — LSL

**Assembly:** `LSL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## LSL (wide elements, predicated) -- A64

### Encoding: `lsl_z_p_zw_`  — LSL

**Assembly:** `LSL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## LSL (wide elements, unpredicated) -- A64

### Encoding: `lsl_z_zw_`  — LSL

**Assembly:** `LSL  <Zd>.<T>, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## LSLR -- A64

### Encoding: `lslr_z_p_zz_`  — LSLR

**Assembly:** `LSLR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## LSLV -- A64

### Encoding: `LSLV_32_dp_2src`  [32-bit]  — LSLV

**Assembly:** `LSLV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding a shift amount from 0 to 31 in its bottom 5 bit | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(op2);
```

### Encoding: `LSLV_64_dp_2src`  [64-bit]  — LSLV

**Assembly:** `LSLV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding a shift amount from 0 to 63 in its bottom 6 bit | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(op2);
```

---

## LSR (immediate) -- A64 *(alias for UBFM)*

### Encoding: `LSR_UBFM_32M_bitfield`  [32-bit]  — UBFM

**Assembly:** `LSR  <Wd>, <Wn>, #<shift> / UBFM  <Wd>, <Wn>, #<shift>, #31`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<shift>` | immr (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, encoded in the "immr" field. |  |
| `UBFM` | — |  |  |

### Encoding: `LSR_UBFM_64M_bitfield`  [64-bit]  — UBFM

**Assembly:** `LSR  <Xd>, <Xn>, #<shift> / UBFM  <Xd>, <Xn>, #<shift>, #63`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<shift>` | immr (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, encoded in the "immr" field. |  |
| `UBFM` | — |  |  |

---

## LSR (immediate, predicated) -- A64

### Encoding: `lsr_z_p_zi_`  — LSR

**Assembly:** `LSR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## LSR (immediate, unpredicated) -- A64

### Encoding: `lsr_z_zi_`  — LSR

**Assembly:** `LSR  <Zd>.<T>, <Zn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## LSR (register) -- A64 *(alias for LSRV)*

### Encoding: `LSR_LSRV_32_dp_2src`  [32-bit]  — LSRV

**Assembly:** `LSR  <Wd>, <Wn>, <Wm> / LSRV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding a shift amount from 0 to 31 in its bottom 5 bit |  |
| `LSRV` | — |  |  |

### Encoding: `LSR_LSRV_64_dp_2src`  [64-bit]  — LSRV

**Assembly:** `LSR  <Xd>, <Xn>, <Xm> / LSRV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding a shift amount from 0 to 63 in its bottom 6 bit |  |
| `LSRV` | — |  |  |

---

## LSR (vectors) -- A64

### Encoding: `lsr_z_p_zz_`  — LSR

**Assembly:** `LSR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## LSR (wide elements, predicated) -- A64

### Encoding: `lsr_z_p_zw_`  — LSR

**Assembly:** `LSR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## LSR (wide elements, unpredicated) -- A64

### Encoding: `lsr_z_zw_`  — LSR

**Assembly:** `LSR  <Zd>.<T>, <Zn>.<T>, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## LSRR -- A64

### Encoding: `lsrr_z_p_zz_`  — LSRR

**Assembly:** `LSRR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## LSRV -- A64

### Encoding: `LSRV_32_dp_2src`  [32-bit]  — LSRV

**Assembly:** `LSRV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding a shift amount from 0 to 31 in its bottom 5 bit | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(op2);
```

### Encoding: `LSRV_64_dp_2src`  [64-bit]  — LSRV

**Assembly:** `LSRV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding a shift amount from 0 to 63 in its bottom 6 bit | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(op2);
```

---

## LUTI2 (8-bit and 16-bit) -- A64

### Encoding: `luti2_z_zz_8`  — LUTI2

**Assembly:** `LUTI2  <Zd>.B, { <Zn>.B }, <Zm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the table vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | For the "Byte" variant: is the vector segment index, in the range 0 to 3, encoded in the "i2" field. | let part : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_LUT)) then EndOfDecode(Decode_UNDEF);
end;
let isize : integer{} = 2;
let esize : integer{} = 8;
let m : integer = UInt(Zm);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let part : integer = UInt(i2);
```

### Encoding: `luti2_z_zz_16`  — LUTI2

**Assembly:** `LUTI2  <Zd>.H, { <Zn>.H }, <Zm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the table vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | (i3h :: i3l) | For the "Halfword" variant: is the vector segment index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_LUT)) then EndOfDecode(Decode_UNDEF);
end;
let isize : integer{} = 2;
let esize : integer{} = 16;
let m : integer = UInt(Zm);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let part : integer = UInt(i3h::i3l);
```

---

## LUTI2 (four registers) -- A64

### Encoding: `luti2_mz4_ztz_1`  — LUTI2

**Assembly:** `LUTI2  { <Zd1>.<T>-<Zd4>.<T> }, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Consecutive" variant: is the name of the first scalable vector register of the destination multi-vector group,  | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | For the "Consecutive" variant: is the name of the fourth scalable vector register of the destination multi-vector group, | let d : integer = UInt(Zd::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i2 (2b) | Is the vector segment index, in the range 0 to 3, encoded in the "i2" field. | let imm : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 2;
let n : integer = UInt(Zn);
let dstride : integer = 1;
let d : integer = UInt(Zd::'00');
let imm : integer = UInt(i2);
let nreg : integer{} = 4;
```

### Encoding: `luti2_mz4_ztz_4`  — LUTI2

**Assembly:** `LUTI2  { <Zd1>.<T>, <Zd2>.<T>, <Zd3>.<T>, <Zd4>.<T> }, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | (D :: Zd) | For the "Strided" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 of the destination multi-v |  |
| `<T>` | — |  |  |
| `<Zd2>` | (D :: Zd) | Is the name of the second scalable vector register Z4-Z7 or Z20-Z23 of the destination multi-vector group, encoded as "D |  |
| `<Zd3>` | (D :: Zd) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 of the destination multi-vector group, encoded as "D |  |
| `<Zd4>` | (D :: Zd) | For the "Strided" variant: is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 of the destination mult |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i2 (2b) | Is the vector segment index, in the range 0 to 3, encoded in the "i2" field. | let imm : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
if size == '10' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 2;
let n : integer = UInt(Zn);
let dstride : integer = 4;
let d : integer = UInt(D::'00'::Zd);
let imm : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## LUTI2 (single) -- A64

### Encoding: `luti2_z_ztz_`  — LUTI2

**Assembly:** `LUTI2  <Zd>.<T>, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i4 (4b) | Is the vector segment index, in the range 0 to 15, encoded in the "i4" field. | let imm : integer = UInt(i4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 2;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let imm : integer = UInt(i4);
let nreg : integer{} = 1;
```

---

## LUTI2 (two registers) -- A64

### Encoding: `luti2_mz2_ztz_1`  — LUTI2

**Assembly:** `LUTI2  { <Zd1>.<T>-<Zd2>.<T> }, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Consecutive" variant: is the name of the first scalable vector register of the destination multi-vector group,  | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | For the "Consecutive" variant: is the name of the second scalable vector register of the destination multi-vector group, | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i3 (3b) | Is the vector segment index, in the range 0 to 7, encoded in the "i3" field. | let imm : integer = UInt(i3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 2;
let n : integer = UInt(Zn);
let dstride : integer = 1;
let d : integer = UInt(Zd::'0');
let imm : integer = UInt(i3);
let nreg : integer{} = 2;
```

### Encoding: `luti2_mz2_ztz_8`  — LUTI2

**Assembly:** `LUTI2  { <Zd1>.<T>, <Zd2>.<T> }, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | (D :: Zd) | For the "Strided" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 of the destination multi-v |  |
| `<T>` | — |  |  |
| `<Zd2>` | (D :: Zd) | For the "Strided" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 of the destination multi |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i3 (3b) | Is the vector segment index, in the range 0 to 7, encoded in the "i3" field. | let imm : integer = UInt(i3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
if size == '10' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 2;
let n : integer = UInt(Zn);
let dstride : integer = 8;
let d : integer = UInt(D::'0'::Zd);
let imm : integer = UInt(i3);
let nreg : integer{} = 2;
```

---

## LUTI2 -- A64

### Encoding: `LUTI2_asimdtbl_L5`  [Byte]  — LUTI2

**Assembly:** `LUTI2  <Vd>.16B, { <Vn>.16B }, <Vm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP table register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<index>` | len (2b) | For the "Byte" variant: is the vector segment index, in the range 0 to 3, encoded in the "len" field. | let part : integer = if op2 == '10' then UInt(len) else UInt(len::op); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_LUT) then
    EndOfDecode(Decode_UNDEF);
end;
if op2 == '10' && op == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let isize : integer{} = 2;
let esize : integer{} = if op2 == '10' then 8 else 16;
let part : integer = if op2 == '10' then UInt(len) else UInt(len::op);
```

### Encoding: `LUTI2_asimdtbl_L6`  [Halfword]  — LUTI2

**Assembly:** `LUTI2  <Vd>.8H, { <Vn>.8H }, <Vm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP table register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<index>` | (len :: op) | For the "Halfword" variant: is the vector segment index, in the range 0 to 7, encoded in the "len:op" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_LUT) then
    EndOfDecode(Decode_UNDEF);
end;
if op2 == '10' && op == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let isize : integer{} = 2;
let esize : integer{} = if op2 == '10' then 8 else 16;
let part : integer = if op2 == '10' then UInt(len) else UInt(len::op);
```

---

## LUTI4 (8-bit and 16-bit) -- A64

### Encoding: `luti4_z_zz_8`  — LUTI4

**Assembly:** `LUTI4  <Zd>.B, { <Zn>.B }, <Zm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the table vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i1 (1b) | For the "Byte, single register table" variant: is the vector segment index, in the range 0 to 1, encoded in the "i1" fie | let part : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_LUT)) then EndOfDecode(Decode_UNDEF);
end;
let isize : integer{} = 4;
let esize : integer{} = 8;
let ntblr : integer = 1;
let m : integer = UInt(Zm);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let part : integer = UInt(i1);
```

### Encoding: `luti4_z_zz_2x16`  — LUTI4

**Assembly:** `LUTI4  <Zd>.H, { <Zn1>.H, <Zn2>.H }, <Zm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (5b) | Is the name of the first table vector register, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second table vector register, encoded as "Zn" plus 1 modulo 32. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | For the "Halfword, single register table" and "Halfword, two register table" variants: is the vector segment index, in t | let part : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_LUT)) then EndOfDecode(Decode_UNDEF);
end;
let isize : integer{} = 4;
let esize : integer{} = 16;
let ntblr : integer = 2;
let m : integer = UInt(Zm);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let part : integer = UInt(i2);
```

### Encoding: `luti4_z_zz_1x16`  — LUTI4

**Assembly:** `LUTI4  <Zd>.H, { <Zn>.H }, <Zm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the table vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i2 (2b) | For the "Halfword, single register table" and "Halfword, two register table" variants: is the vector segment index, in t | let part : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME2)) ||
      !IsFeatureImplemented(FEAT_LUT)) then EndOfDecode(Decode_UNDEF);
end;
if MaxImplementedAnyVL() < 256 then EndOfDecode(Decode_UNDEF); end;
let isize : integer{} = 4;
let esize : integer{} = 16;
let ntblr : integer = 1;
let m : integer = UInt(Zm);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let part : integer = UInt(i2);
```

---

## LUTI4 (four registers, 16-bit and 32-bit) -- A64

### Encoding: `luti4_mz4_ztz_1`  — LUTI4

**Assembly:** `LUTI4  { <Zd1>.<T>-<Zd4>.<T> }, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Consecutive" variant: is the name of the first scalable vector register of the destination multi-vector group,  | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | For the "Consecutive" variant: is the name of the fourth scalable vector register of the destination multi-vector group, | let d : integer = UInt(Zd::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i1 (1b) | Is the vector segment index, in the range 0 to 1, encoded in the "i1" field. | let imm : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '00' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 4;
let n : integer = UInt(Zn);
let dstride : integer = 1;
let d : integer = UInt(Zd::'00');
let imm : integer = UInt(i1);
let nreg : integer{} = 4;
```

### Encoding: `luti4_mz4_ztz_4`  — LUTI4

**Assembly:** `LUTI4  { <Zd1>.H, <Zd2>.H, <Zd3>.H, <Zd4>.H }, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | (D :: Zd) | For the "Strided" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 of the destination multi-v |  |
| `<Zd2>` | (D :: Zd) | Is the name of the second scalable vector register Z4-Z7 or Z20-Z23 of the destination multi-vector group, encoded as "D |  |
| `<Zd3>` | (D :: Zd) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 of the destination multi-vector group, encoded as "D |  |
| `<Zd4>` | (D :: Zd) | For the "Strided" variant: is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 of the destination mult |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i1 (1b) | Is the vector segment index, in the range 0 to 1, encoded in the "i1" field. | let imm : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
if size != '01' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 4;
let n : integer = UInt(Zn);
let dstride : integer = 4;
let d : integer = UInt(D::'00'::Zd);
let imm : integer = UInt(i1);
let nreg : integer{} = 4;
```

---

## LUTI4 (four registers, 8-bit) -- A64

### Encoding: `luti4_mz4_ztmz2_1`  — LUTI4

**Assembly:** `LUTI4  { <Zd1>.B-<Zd4>.B }, ZT0, { <Zn1>-<Zn2> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Consecutive" variant: is the name of the first scalable vector register of the destination multi-vector group,  | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | For the "Consecutive" variant: is the name of the fourth scalable vector register of the destination multi-vector group, | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_LUTv2) then EndOfDecode(Decode_UNDEF); end;
if size != '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 4;
let n : integer = UInt(Zn::'0');
let dstride : integer = 1;
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
```

### Encoding: `luti4_mz4_ztmz2_4`  — LUTI4

**Assembly:** `LUTI4  { <Zd1>.B, <Zd2>.B, <Zd3>.B, <Zd4>.B }, ZT0, { <Zn1>-<Zn2> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | (D :: Zd) | For the "Strided" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 of the destination multi-v |  |
| `<Zd2>` | (D :: Zd) | Is the name of the second scalable vector register Z4-Z7 or Z20-Z23 of the destination multi-vector group, encoded as "D |  |
| `<Zd3>` | (D :: Zd) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 of the destination multi-vector group, encoded as "D |  |
| `<Zd4>` | (D :: Zd) | For the "Strided" variant: is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 of the destination mult |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) || !IsFeatureImplemented(FEAT_SME_LUTv2) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 4;
let n : integer = UInt(Zn::'0');
let dstride : integer = 4;
let d : integer = UInt(D::'00'::Zd);
let nreg : integer{} = 4;
```

---

## LUTI4 (single) -- A64

### Encoding: `luti4_z_ztz_`  — LUTI4

**Assembly:** `LUTI4  <Zd>.<T>, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i3 (3b) | Is the vector segment index, in the range 0 to 7, encoded in the "i3" field. | let imm : integer = UInt(i3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 4;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let imm : integer = UInt(i3);
let nreg : integer{} = 1;
```

---

## LUTI4 (two registers) -- A64

### Encoding: `luti4_mz2_ztz_1`  — LUTI4

**Assembly:** `LUTI4  { <Zd1>.<T>-<Zd2>.<T> }, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Consecutive" variant: is the name of the first scalable vector register of the destination multi-vector group,  | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | For the "Consecutive" variant: is the name of the second scalable vector register of the destination multi-vector group, | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i2 (2b) | Is the vector segment index, in the range 0 to 3, encoded in the "i2" field. | let imm : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 4;
let n : integer = UInt(Zn);
let dstride : integer = 1;
let d : integer = UInt(Zd::'0');
let imm : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `luti4_mz2_ztz_8`  — LUTI4

**Assembly:** `LUTI4  { <Zd1>.<T>, <Zd2>.<T> }, ZT0, <Zn>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | (D :: Zd) | For the "Strided" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 of the destination multi-v |  |
| `<T>` | — |  |  |
| `<Zd2>` | (D :: Zd) | For the "Strided" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 of the destination multi |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<index>` | i2 (2b) | Is the vector segment index, in the range 0 to 3, encoded in the "i2" field. | let imm : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
if size == '10' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let isize : integer{} = 4;
let n : integer = UInt(Zn);
let dstride : integer = 8;
let d : integer = UInt(D::'0'::Zd);
let imm : integer = UInt(i2);
let nreg : integer{} = 2;
```

---

## LUTI4 -- A64

### Encoding: `LUTI4_asimdtbl_L5`  [Byte]  — LUTI4

**Assembly:** `LUTI4  <Vd>.16B, { <Vn>.16B }, <Vm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP table register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<index>` | len (2b) | For the "Byte" variant: is the vector segment index, in the range 0 to 1, encoded in the "len<1>" field. | if len[0] == '0' && op == '0' then EndOfDecode(Decode_UNDEF); end;<br>let part : integer = if op == '0' then UInt(len[1]) else UInt(len); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_LUT) then
    EndOfDecode(Decode_UNDEF);
end;
if len[0] == '0' && op == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let isize : integer{} = 4;
let esize : integer{} = 8 << UInt(op);
let ntblr : integer = 1 << UInt(op);
let part : integer = if op == '0' then UInt(len[1]) else UInt(len);
```

### Encoding: `LUTI4_asimdtbl_L7`  [Halfword]  — LUTI4

**Assembly:** `LUTI4  <Vd>.8H, { <Vn1>.8H, <Vn2>.8H }, <Vm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn1>` | Rn (5b) | Is the name of the first SIMD&FP table register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vn2>` | Rn (5b) | Is the name of the second SIMD&FP table register, encoded as "Rn" plus 1 modulo 32. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<index>` | len (2b) | For the "Halfword" variant: is the vector segment index, in the range 0 to 3, encoded in the "len" field. | if len[0] == '0' && op == '0' then EndOfDecode(Decode_UNDEF); end;<br>let part : integer = if op == '0' then UInt(len[1]) else UInt(len); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_LUT) then
    EndOfDecode(Decode_UNDEF);
end;
if len[0] == '0' && op == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let isize : integer{} = 4;
let esize : integer{} = 8 << UInt(op);
let ntblr : integer = 1 << UInt(op);
let part : integer = if op == '0' then UInt(len[1]) else UInt(len);
```

---

## LUTI6 (16-bit) -- A64

### Encoding: `luti6_z_zzz_16`  — LUTI6

**Assembly:** `LUTI6  <Zd>.H, { <Zn1>.H, <Zn2>.H }, <Zm>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (5b) | Is the name of the first table vector register, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second table vector register, encoded as "Zn" plus 1 modulo 32. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<index>` | i1 (1b) | Is the vector segment index, in the range 0 to 1, encoded in the "i1" field. | let part : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if MaxImplementedAnyVL() < 512 then EndOfDecode(Decode_UNDEF); end;
let isize : integer{} = 6;
let esize : integer{} = 16;
let m : integer = UInt(Zm);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let part : integer = UInt(i1);
```

---

## LUTI6 (8-bit) -- A64

### Encoding: `luti6_z_zzz_8`  — LUTI6

**Assembly:** `LUTI6  <Zd>.B, { <Zn1>.B, <Zn2>.B }, <Zm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (5b) | Is the name of the first table vector register, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second table vector register, encoded as "Zn" plus 1 modulo 32. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) then EndOfDecode(Decode_UNDEF);
end;
if MaxImplementedAnyVL() < 256 then EndOfDecode(Decode_UNDEF); end;
let isize : integer{} = 6;
let esize : integer{} = 8;
let m : integer = UInt(Zm);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## LUTI6 (table, four registers, 8-bit) -- A64

### Encoding: `luti6_mz4_ztmz3_1`  — LUTI6

**Assembly:** `LUTI6  { <Zd1>.B-<Zd4>.B }, ZT0, { <Zn1>-<Zn3> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Consecutive" variant: is the name of the first scalable vector register of the destination multi-vector group,  | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | For the "Consecutive" variant: is the name of the fourth scalable vector register of the destination multi-vector group, | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group Z0-Z7, encoded in the "Zn" field. | let n : integer = UInt('00'::Zn); |
| `<Zn3>` | Zn (3b) | Is the name of the third scalable vector register of the source multi-vector group Z2-Z9, encoded as "Zn" plus 2. | let n : integer = UInt('00'::Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p3) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8;
let isize : integer{} = 6;
let n : integer = UInt('00'::Zn);
let dstride : integer = 1;
let d : integer = UInt(Zd::'00');
```

### Encoding: `luti6_mz4_ztmz3_4`  — LUTI6

**Assembly:** `LUTI6  { <Zd1>.B, <Zd2>.B, <Zd3>.B, <Zd4>.B }, ZT0, { <Zn1>-<Zn3> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | (D :: Zd) | For the "Strided" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 of the destination multi-v |  |
| `<Zd2>` | (D :: Zd) | Is the name of the second scalable vector register Z4-Z7 or Z20-Z23 of the destination multi-vector group, encoded as "D |  |
| `<Zd3>` | (D :: Zd) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 of the destination multi-vector group, encoded as "D |  |
| `<Zd4>` | (D :: Zd) | For the "Strided" variant: is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 of the destination mult |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group Z0-Z7, encoded in the "Zn" field. | let n : integer = UInt('00'::Zn); |
| `<Zn3>` | Zn (3b) | Is the name of the third scalable vector register of the source multi-vector group Z2-Z9, encoded as "Zn" plus 2. | let n : integer = UInt('00'::Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p3) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8;
let isize : integer{} = 6;
let n : integer = UInt('00'::Zn);
let dstride : integer = 4;
let d : integer = UInt(D::'00'::Zd);
```

---

## LUTI6 (table, single, 8-bit) -- A64

### Encoding: `luti6_z_ztz_`  — LUTI6

**Assembly:** `LUTI6  <Zd>.B, ZT0, <Zn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p3) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8;
let isize : integer{} = 6;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## LUTI6 (vector, 16-bit) -- A64

### Encoding: `luti6_mz4_zmz2_1`  — LUTI6

**Assembly:** `LUTI6  { <Zd1>.H-<Zd4>.H }, { <Zn1>.H, <Zn2>.H }, { <Zm1>-<Zm2> }[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Consecutive" variant: is the name of the first scalable vector register of the destination multi-vector group,  | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | For the "Consecutive" variant: is the name of the fourth scalable vector register of the destination multi-vector group, | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first table vector register, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second table vector register, encoded as "Zn" plus 1 modulo 32. | let n : integer = UInt(Zn); |
| `<Zm1>` | Zm (5b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zm". | let m : integer = UInt(Zm); |
| `<Zm2>` | Zm (5b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" plus 1 modulo 32. | let m : integer = UInt(Zm); |
| `<index>` | i1 (1b) | Is the vector segment index, in the range 0 to 1, encoded in the "i1" field. | let select : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p3) then EndOfDecode(Decode_UNDEF); end;
if MaxImplementedSVL() < 512 then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let isize : integer{} = 6;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let dstride : integer = 1;
let d : integer = UInt(Zd::'00');
let select : integer = UInt(i1);
```

### Encoding: `luti6_mz4_zmz2_4`  — LUTI6

**Assembly:** `LUTI6  { <Zd1>.H, <Zd2>.H, <Zd3>.H, <Zd4>.H }, { <Zn1>.H, <Zn2>.H }, { <Zm1>-<Zm2> }[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | (D :: Zd) | For the "Strided" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 of the destination multi-v |  |
| `<Zd2>` | (D :: Zd) | Is the name of the second scalable vector register Z4-Z7 or Z20-Z23 of the destination multi-vector group, encoded as "D |  |
| `<Zd3>` | (D :: Zd) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 of the destination multi-vector group, encoded as "D |  |
| `<Zd4>` | (D :: Zd) | For the "Strided" variant: is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 of the destination mult |  |
| `<Zn1>` | Zn (5b) | Is the name of the first table vector register, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second table vector register, encoded as "Zn" plus 1 modulo 32. | let n : integer = UInt(Zn); |
| `<Zm1>` | Zm (5b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zm". | let m : integer = UInt(Zm); |
| `<Zm2>` | Zm (5b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" plus 1 modulo 32. | let m : integer = UInt(Zm); |
| `<index>` | i1 (1b) | Is the vector segment index, in the range 0 to 1, encoded in the "i1" field. | let select : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p3) then EndOfDecode(Decode_UNDEF); end;
if MaxImplementedSVL() < 512 then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let isize : integer{} = 6;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let dstride : integer = 4;
let d : integer = UInt(D::'00'::Zd);
let select : integer = UInt(i1);
```

---

## MAD -- A64

### Encoding: `mad_z_p_zzz_`  — MAD

**Assembly:** `MAD  <Zdn>.<T>, <Pg>/M, <Zm>.<T>, <Za>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Za>` | Za (5b) | Is the name of the third source scalable vector register, encoded in the "Za" field. | let a : integer = UInt(Za); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let a : integer = UInt(Za);
let sub_op : boolean = FALSE;
```

---

## MADD -- A64

### Encoding: `MADD_32A_dp_3src`  [32-bit]  — MADD

**Assembly:** `MADD  <Wd>, <Wn>, <Wm>, <Wa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Wa>` | Ra (5b) | Is the 32-bit name of the third general-purpose source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `MADD_64A_dp_3src`  [64-bit]  — MADD

**Assembly:** `MADD  <Xd>, <Xn>, <Xm>, <Xa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Xa>` | Ra (5b) | Is the 64-bit name of the third general-purpose source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
let datasize : integer{} = 32 << UInt(sf);
```

---

## MADDPT -- A64

### Encoding: `MADDPT_64A_dp_3src`  — MADDPT

**Assembly:** `MADDPT  <Xd>, <Xn>, <Xm>, <Xa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Xa>` | Ra (5b) | Is the 64-bit name of the third general-purpose source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CPA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
```

---

## MADPT -- A64

### Encoding: `madpt_z_zzz_`  — MADPT

**Assembly:** `MADPT  <Zdn>.D, <Zm>.D, <Za>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer  = UInt(Zm); |
| `<Za>` | Za (5b) | Is the name of the third source scalable vector register, encoded in the "Za" field. | let a : integer  = UInt(Za); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_CPA) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn);
let m : integer  = UInt(Zm);
let a : integer  = UInt(Za);
```

---

## MATCH -- A64

### Encoding: `match_p_p_zz_`  — MATCH

**Assembly:** `MATCH  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
if size IN {'1x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let d : integer = UInt(Pd);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
```

---

## MLA (by element) -- A64

### Encoding: `MLA_asimdelem_R`  — MLA

**Assembly:** `MLA  <Vd>.<T>, <Vn>.<T>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L);   Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## MLA (indexed) -- A64

### Encoding: `mla_z_zzzi_h`  — MLA

**Assembly:** `MLA  <Zda>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "16-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `mla_z_zzzi_s`  — MLA

**Assembly:** `MLA  <Zda>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "32-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `mla_z_zzzi_d`  — MLA

**Assembly:** `MLA  <Zda>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "64-bit" variant: is the element index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## MLA (vector) -- A64

### Encoding: `MLA_asimdsame_only`  — MLA

**Assembly:** `MLA  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## MLA (vectors) -- A64

### Encoding: `mla_z_p_zzz_`  — MLA

**Assembly:** `MLA  <Zda>.<T>, <Pg>/M, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sub_op : boolean = FALSE;
```

---

## MLAPT -- A64

### Encoding: `mlapt_z_zzz_`  — MLAPT

**Assembly:** `MLAPT  <Zda>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_CPA) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## MLBI -- A64 *(alias for SYS)*

### Encoding: `MLBI_SYS_CR_systeminstrs`  — SYS

**Assembly:** `MLBI  <mlbi_op>{, <Xt>} / SYS  #4, C7, C0, #<op2>{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<mlbi_op>` | — |  |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the optional general-purpose source register, defaulting to '11111', encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## MLS (by element) -- A64

### Encoding: `MLS_asimdelem_R`  — MLS

**Assembly:** `MLS  <Vd>.<T>, <Vn>.<T>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L);   Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## MLS (indexed) -- A64

### Encoding: `mls_z_zzzi_h`  — MLS

**Assembly:** `MLS  <Zda>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "16-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `mls_z_zzzi_s`  — MLS

**Assembly:** `MLS  <Zda>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "32-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `mls_z_zzzi_d`  — MLS

**Assembly:** `MLS  <Zda>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "64-bit" variant: is the element index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## MLS (vector) -- A64

### Encoding: `MLS_asimdsame_only`  — MLS

**Assembly:** `MLS  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## MLS (vectors) -- A64

### Encoding: `mls_z_p_zzz_`  — MLS

**Assembly:** `MLS  <Zda>.<T>, <Pg>/M, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sub_op : boolean = TRUE;
```

---

## MNEG -- A64 *(alias for MSUB)*

### Encoding: `MNEG_MSUB_32A_dp_3src`  [32-bit]  — MSUB

**Assembly:** `MNEG  <Wd>, <Wn>, <Wm> / MSUB  <Wd>, <Wn>, <Wm>, WZR`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. |  |
| `MSUB` | — |  |  |

### Encoding: `MNEG_MSUB_64A_dp_3src`  [64-bit]  — MSUB

**Assembly:** `MNEG  <Xd>, <Xn>, <Xm> / MSUB  <Xd>, <Xn>, <Xm>, XZR`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. |  |
| `MSUB` | — |  |  |

---

## MOV (SIMD&FP scalar, predicated) -- A64 *(alias for cpy_z_p_v)*

### Encoding: `mov_z_p_v__cpy_z_p_v_`  — CPY

**Assembly:** `MOV  <Zd>.<T>, <Pg>/M, <V><n> / CPY  <Zd>.<T>, <Pg>/M, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<V>` | — |  |  |
| `<n>` | Vn (5b) | Is the number [0-31] of the source SIMD&FP register, encoded in the "Vn" field. |  |
| `CPY` | — |  |  |

---

## MOV (SIMD&FP scalar, unpredicated) -- A64 *(alias for dup_z_zi)*

### Encoding: `mov_z_v__dup_z_zi_`  — DUP

**Assembly:** `MOV  <Zd>.<T>, <V><n> / DUP  <Zd>.<T>, <Zn>.<T>[0]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<V>` | — |  |  |
| `<n>` | Zn (5b) | Is the number [0-31] of the source SIMD&FP register, encoded in the "Zn" field. |  |
| `DUP` | — |  |  |

### Encoding: `mov_z_zi__dup_z_zi_`  — DUP

**Assembly:** `MOV  <Zd>.<T>, <Zn>.<T>[<imm>] / DUP  <Zd>.<T>, <Zn>.<T>[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. |  |
| `<imm>` | (imm2 :: tsz) | Is the immediate index, in the range 0 to one less than the number of elements in 512 bits, encoded in "imm2:tsz". |  |
| `DUP` | — |  |  |

---

## MOV (array to vector, four registers) -- A64 *(alias for mova_mz_za4)*

### Encoding: `mov_mz_za4_1_mova_mz_za4_1`  — MOVA

**Assembly:** `MOV  { <Zd1>.D-<Zd4>.D }, ZA.D[<Wv>, <offs>{, VGx4}] / MOVA  { <Zd1>.D-<Zd4>.D }, ZA.D[<Wv>, <offs>{, VGx4}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. |  |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. |  |
| `MOVA` | — |  |  |

---

## MOV (array to vector, two registers) -- A64 *(alias for mova_mz_za2)*

### Encoding: `mov_mz_za2_1_mova_mz_za2_1`  — MOVA

**Assembly:** `MOV  { <Zd1>.D-<Zd2>.D }, ZA.D[<Wv>, <offs>{, VGx2}] / MOVA  { <Zd1>.D-<Zd2>.D }, ZA.D[<Wv>, <offs>{, VGx2}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. |  |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. |  |
| `MOVA` | — |  |  |

---

## MOV (bitmask immediate) -- A64 *(alias for ORR_log_imm)*

### Encoding: `MOV_ORR_32_log_imm`  [32-bit]  — ORR

**Assembly:** `MOV  <Wd|WSP>, #<imm> / ORR  <Wd|WSP>, WZR, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd\|WSP>` | Rd (5b) | Is the 32-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. |  |
| `<imm>` | (imms :: immr) | For the "32-bit" variant: is the bitmask immediate, encoded in "imms:immr", but excluding values which could be encoded  |  |
| `ORR` | — |  |  |

### Encoding: `MOV_ORR_64_log_imm`  [64-bit]  — ORR

**Assembly:** `MOV  <Xd|SP>, #<imm> / ORR  <Xd|SP>, XZR, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. |  |
| `<imm>` | (N :: imms :: immr) | For the "64-bit" variant: is the bitmask immediate, encoded in "N:imms:immr", but excluding values which could be encode |  |
| `ORR` | — |  |  |

---

## MOV (element) -- A64 *(alias for INS_advsimd_elt)*

### Encoding: `MOV_INS_asimdins_IV_v`  — INS

**Assembly:** `MOV  <Vd>.<Ts>[<index1>], <Vn>.<Ts>[<index2>] / INS  <Vd>.<Ts>[<index1>], <Vn>.<Ts>[<index2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. |  |
| `<Ts>` | — |  |  |
| `<index1>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. |  |
| `<index2>` | — |  |  |
| `INS` | — |  |  |

---

## MOV (from general) -- A64 *(alias for INS_advsimd_gen)*

### Encoding: `MOV_INS_asimdins_IR_r`  — INS

**Assembly:** `MOV  <Vd>.<Ts>[<index>], <R><n> / INS  <Vd>.<Ts>[<index>], <R><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the general-purpose source register or ZR (31), encoded in the "Rn" field. |  |
| `INS` | — |  |  |

---

## MOV (immediate, merging) -- A64 *(alias for cpy_z_p_i)*

### Encoding: `mov_z_p_i__cpy_z_p_i_`  — CPY

**Assembly:** `MOV  <Zd>.<T>, <Pg>/M, #<imm>{, <shift>} / CPY  <Zd>.<T>, <Pg>/M, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. |  |
| `<imm>` | imm8 (8b) | Is a signed immediate in the range -128 to 127, encoded in the "imm8" field. |  |
| `<shift>` | — |  |  |
| `CPY` | — |  |  |

---

## MOV (immediate, unpredicated) -- A64 *(alias for dup_z_i)*

### Encoding: `mov_z_i__dup_z_i_`  — DUP

**Assembly:** `MOV  <Zd>.<T>, #<imm>{, <shift>} / DUP  <Zd>.<T>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is a signed immediate in the range -128 to 127, encoded in the "imm8" field. |  |
| `<shift>` | — |  |  |
| `DUP` | — |  |  |

---

## MOV (immediate, zeroing) -- A64 *(alias for cpy_z_o_i)*

### Encoding: `mov_z_o_i__cpy_z_o_i_`  — CPY

**Assembly:** `MOV  <Zd>.<T>, <Pg>/Z, #<imm>{, <shift>} / CPY  <Zd>.<T>, <Pg>/Z, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. |  |
| `<imm>` | imm8 (8b) | Is a signed immediate in the range -128 to 127, encoded in the "imm8" field. |  |
| `<shift>` | — |  |  |
| `CPY` | — |  |  |

---

## MOV (inverted wide immediate) -- A64 *(alias for MOVN)*

### Encoding: `MOV_MOVN_32_movewide`  [32-bit]  — MOVN

**Assembly:** `MOV  <Wd>, #<imm> / MOVN  <Wd>, #<imm16>, LSL  #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<imm>` | imm16 (16b) | For the "32-bit" variant: is a 32-bit immediate, the bitwise inverse of which can be encoded in "imm16:hw", but excludin |  |
| `MOVN` | — |  |  |

### Encoding: `MOV_MOVN_64_movewide`  [64-bit]  — MOVN

**Assembly:** `MOV  <Xd>, #<imm> / MOVN  <Xd>, #<imm16>, LSL  #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<imm>` | imm16 (16b) | For the "64-bit" variant: is a 64-bit immediate, the bitwise inverse of which can be encoded in "imm16:hw". |  |
| `MOVN` | — |  |  |

---

## MOV (predicate, merging) -- A64 *(alias for sel_p_p_pp)*

### Encoding: `movm_p_p_p__sel_p_p_pp_`  — SEL

**Assembly:** `MOV  <Pd>.B, <Pg>/M, <Pn>.B / SEL  <Pd>.B, <Pg>, <Pn>.B, <Pd>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. |  |
| `SEL` | — |  |  |

---

## MOV (predicate, zeroing) -- A64 *(alias for and_p_p_pp)*

### Encoding: `movz_p_p_p__and_p_p_pp_z`  — AND

**Assembly:** `MOV  <Pd>.B, <Pg>/Z, <Pn>.B / AND  <Pd>.B, <Pg>/Z, <Pn>.B, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. |  |
| `AND` | — |  |  |

---

## MOV (register) -- A64 *(alias for ORR_log_shift)*

### Encoding: `MOV_ORR_32_log_shift`  [32-bit]  — ORR

**Assembly:** `MOV  <Wd>, <Wm> / ORR  <Wd>, WZR, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `ORR` | — |  |  |

### Encoding: `MOV_ORR_64_log_shift`  [64-bit]  — ORR

**Assembly:** `MOV  <Xd>, <Xm> / ORR  <Xd>, XZR, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `ORR` | — |  |  |

---

## MOV (scalar) -- A64 *(alias for DUP_advsimd_elt)*

### Encoding: `MOV_DUP_asisdone_only`  — DUP

**Assembly:** `MOV  <V><d>, <Vn>.<T>[<index>] / DUP  <V><d>, <Vn>.<T>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. |  |
| `<T>` | — |  |  |
| `<index>` | — |  |  |
| `DUP` | — |  |  |

---

## MOV (scalar, predicated) -- A64 *(alias for cpy_z_p_r)*

### Encoding: `mov_z_p_r__cpy_z_p_r_`  — CPY

**Assembly:** `MOV  <Zd>.<T>, <Pg>/M, <R><n|SP> / CPY  <Zd>.<T>, <Pg>/M, <R><n|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<R>` | — |  |  |
| `<n\|SP>` | Rn (5b) | Is the number [0-30] of the general-purpose source register or the name SP (31), encoded in the "Rn" field. |  |
| `CPY` | — |  |  |

---

## MOV (scalar, unpredicated) -- A64 *(alias for dup_z_r)*

### Encoding: `mov_z_r__dup_z_r_`  — DUP

**Assembly:** `MOV  <Zd>.<T>, <R><n|SP> / DUP  <Zd>.<T>, <R><n|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n\|SP>` | Rn (5b) | Is the number [0-30] of the general-purpose source register or the name SP (31), encoded in the "Rn" field. |  |
| `DUP` | — |  |  |

---

## MOV (tile to vector, four registers) -- A64 *(alias for mova_mz4_za)*

### Encoding: `mov_mz4_za_b1_mova_mz4_za_b1`  [8-bit]  — MOVA

**Assembly:** `MOV  { <Zd1>.B-<Zd4>.B }, ZA0<HV>.B[<Ws>, <offs1>:<offs4>] / MOVA  { <Zd1>.B-<Zd4>.B }, ZA0<HV>.B[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | off2 (2b) | For the "8-bit" variant: is the first slice index offset, encoded as "off2" field times 4. |  |
| `<offs4>` | off2 (2b) | For the "8-bit" variant: is the fourth slice index offset, encoded as "off2" field times 4 plus 3. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_mz4_za_h1_mova_mz4_za_h1`  [16-bit]  — MOVA

**Assembly:** `MOV  { <Zd1>.H-<Zd4>.H }, <ZAn><HV>.H[<Ws>, <offs1>:<offs4>] / MOVA  { <Zd1>.H-<Zd4>.H }, <ZAn><HV>.H[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 |  |
| `<ZAn>` | ZAn (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | o1 (1b) | For the "16-bit" variant: is the first slice index offset, encoded as "o1" field times 4. |  |
| `<offs4>` | o1 (1b) | For the "16-bit" variant: is the fourth slice index offset, encoded as "o1" field times 4 plus 3. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_mz4_za_w1_mova_mz4_za_w1`  [32-bit]  — MOVA

**Assembly:** `MOV  { <Zd1>.S-<Zd4>.S }, <ZAn><HV>.S[<Ws>, <offs1>:<offs4>] / MOVA  { <Zd1>.S-<Zd4>.S }, <ZAn><HV>.S[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 |  |
| `<ZAn>` | ZAn (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_mz4_za_d1_mova_mz4_za_d1`  [64-bit]  — MOVA

**Assembly:** `MOV  { <Zd1>.D-<Zd4>.D }, <ZAn><HV>.D[<Ws>, <offs1>:<offs4>] / MOVA  { <Zd1>.D-<Zd4>.D }, <ZAn><HV>.D[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 |  |
| `<ZAn>` | ZAn (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |
| `MOVA` | — |  |  |

---

## MOV (tile to vector, single) -- A64 *(alias for mova_z_p_rza)*

### Encoding: `mov_z_p_rza_b_mova_z_p_rza_b`  [8-bit]  — MOVA

**Assembly:** `MOV  <Zd>.B, <Pg>/M, ZA0<HV>.B[<Ws>, <offs>] / MOVA  <Zd>.B, <Pg>/M, ZA0<HV>.B[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | off4 (4b) | For the "8-bit" variant: is the slice index offset, in the range 0 to 15, encoded in the "off4" field. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_z_p_rza_h_mova_z_p_rza_h`  [16-bit]  — MOVA

**Assembly:** `MOV  <Zd>.H, <Pg>/M, <ZAn><HV>.H[<Ws>, <offs>] / MOVA  <Zd>.H, <Pg>/M, <ZAn><HV>.H[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<ZAn>` | ZAn (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | off3 (3b) | For the "16-bit" variant: is the slice index offset, in the range 0 to 7, encoded in the "off3" field. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_z_p_rza_w_mova_z_p_rza_w`  [32-bit]  — MOVA

**Assembly:** `MOV  <Zd>.S, <Pg>/M, <ZAn><HV>.S[<Ws>, <offs>] / MOVA  <Zd>.S, <Pg>/M, <ZAn><HV>.S[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<ZAn>` | ZAn (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | off2 (2b) | For the "32-bit" variant: is the slice index offset, in the range 0 to 3, encoded in the "off2" field. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_z_p_rza_d_mova_z_p_rza_d`  [64-bit]  — MOVA

**Assembly:** `MOV  <Zd>.D, <Pg>/M, <ZAn><HV>.D[<Ws>, <offs>] / MOVA  <Zd>.D, <Pg>/M, <ZAn><HV>.D[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<ZAn>` | ZAn (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | o1 (1b) | For the "64-bit" variant: is the slice index offset, in the range 0 to 1, encoded in the "o1" field. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_z_p_rza_q_mova_z_p_rza_q`  [128-bit]  — MOVA

**Assembly:** `MOV  <Zd>.Q, <Pg>/M, <ZAn><HV>.Q[<Ws>, <offs>] / MOVA  <Zd>.Q, <Pg>/M, <ZAn><HV>.Q[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<ZAn>` | ZAn (4b) | For the "128-bit" variant: is the name of the ZA tile ZA0-ZA15 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | — | For the "128-bit" variant: is the slice index offset, with implicit value 0. |  |
| `MOVA` | — |  |  |

---

## MOV (tile to vector, two registers) -- A64 *(alias for mova_mz2_za)*

### Encoding: `mov_mz2_za_b1_mova_mz2_za_b1`  [8-bit]  — MOVA

**Assembly:** `MOV  { <Zd1>.B-<Zd2>.B }, ZA0<HV>.B[<Ws>, <offs1>:<offs2>] / MOVA  { <Zd1>.B-<Zd2>.B }, ZA0<HV>.B[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | off3 (3b) | For the "8-bit" variant: is the first slice index offset, encoded as "off3" field times 2. |  |
| `<offs2>` | off3 (3b) | For the "8-bit" variant: is the second slice index offset, encoded as "off3" field times 2 plus 1. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_mz2_za_h1_mova_mz2_za_h1`  [16-bit]  — MOVA

**Assembly:** `MOV  { <Zd1>.H-<Zd2>.H }, <ZAn><HV>.H[<Ws>, <offs1>:<offs2>] / MOVA  { <Zd1>.H-<Zd2>.H }, <ZAn><HV>.H[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 |  |
| `<ZAn>` | ZAn (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | off2 (2b) | For the "16-bit" variant: is the first slice index offset, encoded as "off2" field times 2. |  |
| `<offs2>` | off2 (2b) | For the "16-bit" variant: is the second slice index offset, encoded as "off2" field times 2 plus 1. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_mz2_za_w1_mova_mz2_za_w1`  [32-bit]  — MOVA

**Assembly:** `MOV  { <Zd1>.S-<Zd2>.S }, <ZAn><HV>.S[<Ws>, <offs1>:<offs2>] / MOVA  { <Zd1>.S-<Zd2>.S }, <ZAn><HV>.S[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 |  |
| `<ZAn>` | ZAn (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | o1 (1b) | For the "32-bit" variant: is the first slice index offset, encoded as "o1" field times 2. |  |
| `<offs2>` | o1 (1b) | For the "32-bit" variant: is the second slice index offset, encoded as "o1" field times 2 plus 1. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_mz2_za_d1_mova_mz2_za_d1`  [64-bit]  — MOVA

**Assembly:** `MOV  { <Zd1>.D-<Zd2>.D }, <ZAn><HV>.D[<Ws>, <offs1>:<offs2>] / MOVA  { <Zd1>.D-<Zd2>.D }, <ZAn><HV>.D[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 |  |
| `<ZAn>` | ZAn (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAn" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | — | For the "64-bit" variant: is the first slice index offset, with implicit value 0. |  |
| `<offs2>` | — | For the "64-bit" variant: is the second slice index offset, with implicit value 1. |  |
| `MOVA` | — |  |  |

---

## MOV (to general) -- A64 *(alias for UMOV_advsimd)*

### Encoding: `MOV_UMOV_asimdins_W_w`  [32-bit]  — UMOV

**Assembly:** `MOV  <Wd>, <Vn>.S[<index>] / UMOV  <Wd>, <Vn>.S[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. |  |
| `<index>` | imm5 (5b) | For the "32-bit" variant: is the element index encoded in "imm5<4:3>". |  |
| `UMOV` | — |  |  |

### Encoding: `MOV_UMOV_asimdins_X_x`  [64-bit]  — UMOV

**Assembly:** `MOV  <Xd>, <Vn>.D[<index>] / UMOV  <Xd>, <Vn>.D[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. |  |
| `<index>` | imm5 (5b) | For the "64-bit" variant: is the element index encoded in "imm5<4>". |  |
| `UMOV` | — |  |  |

---

## MOV (to/from SP) -- A64 *(alias for ADD_addsub_imm)*

### Encoding: `MOV_ADD_32_addsub_imm`  [32-bit]  — ADD

**Assembly:** `MOV  <Wd|WSP>, <Wn|WSP> / ADD  <Wd|WSP>, <Wn|WSP>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd\|WSP>` | Rd (5b) | Is the 32-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. |  |
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `ADD` | — |  |  |

### Encoding: `MOV_ADD_64_addsub_imm`  [64-bit]  — ADD

**Assembly:** `MOV  <Xd|SP>, <Xn|SP> / ADD  <Xd|SP>, <Xn|SP>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. |  |
| `ADD` | — |  |  |

---

## MOV (vector to array, four registers) -- A64 *(alias for mova_za_mz4)*

### Encoding: `mov_za_mz4_1_mova_za_mz4_1`  — MOVA

**Assembly:** `MOV  ZA.D[<Wv>, <offs>{, VGx4}], { <Zn1>.D-<Zn4>.D } / MOVA  ZA.D[<Wv>, <offs>{, VGx4}], { <Zn1>.D-<Zn4>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. |  |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. |  |
| `MOVA` | — |  |  |

---

## MOV (vector to array, two registers) -- A64 *(alias for mova_za_mz2)*

### Encoding: `mov_za_mz2_1_mova_za_mz2_1`  — MOVA

**Assembly:** `MOV  ZA.D[<Wv>, <offs>{, VGx2}], { <Zn1>.D-<Zn2>.D } / MOVA  ZA.D[<Wv>, <offs>{, VGx2}], { <Zn1>.D-<Zn2>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. |  |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. |  |
| `MOVA` | — |  |  |

---

## MOV (vector to tile, four registers) -- A64 *(alias for mova_za4_z)*

### Encoding: `mov_za4_z_b1_mova_za4_z_b1`  [8-bit]  — MOVA

**Assembly:** `MOV  ZA0<HV>.B[<Ws>, <offs1>:<offs4>], { <Zn1>.B-<Zn4>.B } / MOVA  ZA0<HV>.B[<Ws>, <offs1>:<offs4>], { <Zn1>.B-<Zn4>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | off2 (2b) | For the "8-bit" variant: is the first slice index offset, encoded as "off2" field times 4. |  |
| `<offs4>` | off2 (2b) | For the "8-bit" variant: is the fourth slice index offset, encoded as "off2" field times 4 plus 3. |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za4_z_h1_mova_za4_z_h1`  [16-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.H[<Ws>, <offs1>:<offs4>], { <Zn1>.H-<Zn4>.H } / MOVA  <ZAd><HV>.H[<Ws>, <offs1>:<offs4>], { <Zn1>.H-<Zn4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | o1 (1b) | For the "16-bit" variant: is the first slice index offset, encoded as "o1" field times 4. |  |
| `<offs4>` | o1 (1b) | For the "16-bit" variant: is the fourth slice index offset, encoded as "o1" field times 4 plus 3. |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za4_z_w1_mova_za4_z_w1`  [32-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.S[<Ws>, <offs1>:<offs4>], { <Zn1>.S-<Zn4>.S } / MOVA  <ZAd><HV>.S[<Ws>, <offs1>:<offs4>], { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za4_z_d1_mova_za4_z_d1`  [64-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.D[<Ws>, <offs1>:<offs4>], { <Zn1>.D-<Zn4>.D } / MOVA  <ZAd><HV>.D[<Ws>, <offs1>:<offs4>], { <Zn1>.D-<Zn4>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. |  |
| `MOVA` | — |  |  |

---

## MOV (vector to tile, single) -- A64 *(alias for mova_za_p_rz)*

### Encoding: `mov_za_p_rz_b_mova_za_p_rz_b`  [8-bit]  — MOVA

**Assembly:** `MOV  ZA0<HV>.B[<Ws>, <offs>], <Pg>/M, <Zn>.B / MOVA  ZA0<HV>.B[<Ws>, <offs>], <Pg>/M, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | off4 (4b) | For the "8-bit" variant: is the slice index offset, in the range 0 to 15, encoded in the "off4" field. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za_p_rz_h_mova_za_p_rz_h`  [16-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.H[<Ws>, <offs>], <Pg>/M, <Zn>.H / MOVA  <ZAd><HV>.H[<Ws>, <offs>], <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | off3 (3b) | For the "16-bit" variant: is the slice index offset, in the range 0 to 7, encoded in the "off3" field. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za_p_rz_w_mova_za_p_rz_w`  [32-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.S[<Ws>, <offs>], <Pg>/M, <Zn>.S / MOVA  <ZAd><HV>.S[<Ws>, <offs>], <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | off2 (2b) | For the "32-bit" variant: is the slice index offset, in the range 0 to 3, encoded in the "off2" field. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za_p_rz_d_mova_za_p_rz_d`  [64-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.D[<Ws>, <offs>], <Pg>/M, <Zn>.D / MOVA  <ZAd><HV>.D[<Ws>, <offs>], <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | o1 (1b) | For the "64-bit" variant: is the slice index offset, in the range 0 to 1, encoded in the "o1" field. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za_p_rz_q_mova_za_p_rz_q`  [128-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.Q[<Ws>, <offs>], <Pg>/M, <Zn>.Q / MOVA  <ZAd><HV>.Q[<Ws>, <offs>], <Pg>/M, <Zn>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (4b) | For the "128-bit" variant: is the name of the ZA tile ZA0-ZA15 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs>` | — | For the "128-bit" variant: is the slice index offset, with implicit value 0. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. |  |
| `MOVA` | — |  |  |

---

## MOV (vector to tile, two registers) -- A64 *(alias for mova_za2_z)*

### Encoding: `mov_za2_z_b1_mova_za2_z_b1`  [8-bit]  — MOVA

**Assembly:** `MOV  ZA0<HV>.B[<Ws>, <offs1>:<offs2>], { <Zn1>.B-<Zn2>.B } / MOVA  ZA0<HV>.B[<Ws>, <offs1>:<offs2>], { <Zn1>.B-<Zn2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | off3 (3b) | For the "8-bit" variant: is the first slice index offset, encoded as "off3" field times 2. |  |
| `<offs2>` | off3 (3b) | For the "8-bit" variant: is the second slice index offset, encoded as "off3" field times 2 plus 1. |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za2_z_h1_mova_za2_z_h1`  [16-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.H[<Ws>, <offs1>:<offs2>], { <Zn1>.H-<Zn2>.H } / MOVA  <ZAd><HV>.H[<Ws>, <offs1>:<offs2>], { <Zn1>.H-<Zn2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | off2 (2b) | For the "16-bit" variant: is the first slice index offset, encoded as "off2" field times 2. |  |
| `<offs2>` | off2 (2b) | For the "16-bit" variant: is the second slice index offset, encoded as "off2" field times 2 plus 1. |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za2_z_w1_mova_za2_z_w1`  [32-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.S[<Ws>, <offs1>:<offs2>], { <Zn1>.S-<Zn2>.S } / MOVA  <ZAd><HV>.S[<Ws>, <offs1>:<offs2>], { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | o1 (1b) | For the "32-bit" variant: is the first slice index offset, encoded as "o1" field times 2. |  |
| `<offs2>` | o1 (1b) | For the "32-bit" variant: is the second slice index offset, encoded as "o1" field times 2 plus 1. |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. |  |
| `MOVA` | — |  |  |

### Encoding: `mov_za2_z_d1_mova_za2_z_d1`  [64-bit]  — MOVA

**Assembly:** `MOV  <ZAd><HV>.D[<Ws>, <offs1>:<offs2>], { <Zn1>.D-<Zn2>.D } / MOVA  <ZAd><HV>.D[<Ws>, <offs1>:<offs2>], { <Zn1>.D-<Zn2>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAd" field. |  |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. |  |
| `<offs1>` | — | For the "64-bit" variant: is the first slice index offset, with implicit value 0. |  |
| `<offs2>` | — | For the "64-bit" variant: is the second slice index offset, with implicit value 1. |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. |  |
| `MOVA` | — |  |  |

---

## MOV (vector) -- A64 *(alias for ORR_advsimd_reg)*

### Encoding: `MOV_ORR_asimdsame_only`  — ORR

**Assembly:** `MOV  <Vd>.<T>, <Vn>.<T> / ORR  <Vd>.<T>, <Vn>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. |  |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. |  |
| `ORR` | — |  |  |

---

## MOV (vector, predicated) -- A64 *(alias for sel_z_p_zz)*

### Encoding: `mov_z_p_z__sel_z_p_zz_`  — SEL

**Assembly:** `MOV  <Zd>.<T>, <Pv>/M, <Zn>.<T> / SEL  <Zd>.<T>, <Pv>, <Zn>.<T>, <Zd>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<Pv>` | Pv (4b) | Is the name of the vector select predicate register, encoded in the "Pv" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. |  |
| `SEL` | — |  |  |

---

## MOV (vector, unpredicated) -- A64 *(alias for orr_z_zz)*

### Encoding: `mov_z_z__orr_z_zz_`  — ORR

**Assembly:** `MOV  <Zd>.D, <Zn>.D / ORR  <Zd>.D, <Zn>.D, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. |  |
| `ORR` | — |  |  |

---

## MOV (wide immediate) -- A64 *(alias for MOVZ)*

### Encoding: `MOV_MOVZ_32_movewide`  [32-bit]  — MOVZ

**Assembly:** `MOV  <Wd>, #<imm> / MOVZ  <Wd>, #<imm16>, LSL  #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<imm>` | imm16 (16b) | For the "32-bit" variant: is a 32-bit immediate which can be encoded in "imm16:hw". |  |
| `MOVZ` | — |  |  |

### Encoding: `MOV_MOVZ_64_movewide`  [64-bit]  — MOVZ

**Assembly:** `MOV  <Xd>, #<imm> / MOVZ  <Xd>, #<imm16>, LSL  #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<imm>` | imm16 (16b) | For the "64-bit" variant: is a 64-bit immediate which can be encoded in "imm16:hw". |  |
| `MOVZ` | — |  |  |

---

## MOV -- A64 *(alias for dupm_z_i)*

### Encoding: `mov_z_m__dupm_z_i_`  — DUPM

**Assembly:** `MOV  <Zd>.<T>, #<const> / DUPM  <Zd>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. |  |
| `<T>` | — |  |  |
| `<const>` | imm13 (13b) | Is a 64, 32, 16 or 8-bit bitmask consisting of replicated 2, 4, 8, 16, 32 or 64 bit fields, each field containing a rota |  |
| `DUPM` | — |  |  |

---

## MOV -- A64 *(alias for orr_p_p_pp)*

### Encoding: `mov_p_p__orr_p_p_pp_z`  — ORR

**Assembly:** `MOV  <Pd>.B, <Pn>.B / ORR  <Pd>.B, <Pn>/Z, <Pn>.B, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. |  |
| `ORR` | — |  |  |

---

## MOVA (array to vector, four registers) -- A64

### Encoding: `mova_mz_za4_1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.D-<Zd4>.D }, ZA.D[<Wv>, <offs>{, VGx4}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off3);
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
```

---

## MOVA (array to vector, two registers) -- A64

### Encoding: `mova_mz_za2_1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.D-<Zd2>.D }, ZA.D[<Wv>, <offs>{, VGx2}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off3);
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
```

---

## MOVA (tile to vector, four registers) -- A64

### Encoding: `mova_mz4_za_b1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.B-<Zd4>.B }, ZA0<HV>.B[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | off2 (2b) | For the "8-bit" variant: is the first slice index offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "8-bit" variant: is the fourth slice index offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 8;
let d : integer = UInt(Zd::'00');
let n : integer = 0;
let offset : integer = UInt(off2::'00');
let vertical : boolean = V == '1';
```

### Encoding: `mova_mz4_za_h1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.H-<Zd4>.H }, <ZAn><HV>.H[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<ZAn>` | ZAn (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | o1 (1b) | For the "16-bit" variant: is the first slice index offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "16-bit" variant: is the fourth slice index offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 16;
let d : integer = UInt(Zd::'00');
let n : integer = UInt(ZAn);
let offset : integer = UInt(o1::'00');
let vertical : boolean = V == '1';
```

### Encoding: `mova_mz4_za_w1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.S-<Zd4>.S }, <ZAn><HV>.S[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<ZAn>` | ZAn (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 32;
let d : integer = UInt(Zd::'00');
let n : integer = UInt(ZAn);
let offset : integer = 0;
let vertical : boolean = V == '1';
```

### Encoding: `mova_mz4_za_d1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.D-<Zd4>.D }, <ZAn><HV>.D[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<ZAn>` | ZAn (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if MaxImplementedSVL() < 256 then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 64;
let d : integer = UInt(Zd::'00');
let n : integer = UInt(ZAn);
let offset : integer = 0;
let vertical : boolean = V == '1';
```

---

## MOVA (tile to vector, single) -- A64

### Encoding: `mova_z_p_rza_b`  — MOVA

**Assembly:** `MOVA  <Zd>.B, <Pg>/M, ZA0<HV>.B[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off4 (4b) | For the "8-bit" variant: is the slice index offset, in the range 0 to 15, encoded in the "off4" field. | let offset : integer = UInt(off4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = 0;
let offset : integer = UInt(off4);
let esize : integer{} = 8;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

### Encoding: `mova_z_p_rza_h`  — MOVA

**Assembly:** `MOVA  <Zd>.H, <Pg>/M, <ZAn><HV>.H[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<ZAn>` | ZAn (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off3 (3b) | For the "16-bit" variant: is the slice index offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = UInt(ZAn);
let offset : integer = UInt(off3);
let esize : integer{} = 16;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

### Encoding: `mova_z_p_rza_w`  — MOVA

**Assembly:** `MOVA  <Zd>.S, <Pg>/M, <ZAn><HV>.S[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<ZAn>` | ZAn (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off2 (2b) | For the "32-bit" variant: is the slice index offset, in the range 0 to 3, encoded in the "off2" field. | let offset : integer = UInt(off2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = UInt(ZAn);
let offset : integer = UInt(off2);
let esize : integer{} = 32;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

### Encoding: `mova_z_p_rza_d`  — MOVA

**Assembly:** `MOVA  <Zd>.D, <Pg>/M, <ZAn><HV>.D[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<ZAn>` | ZAn (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | o1 (1b) | For the "64-bit" variant: is the slice index offset, in the range 0 to 1, encoded in the "o1" field. | let offset : integer = UInt(o1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = UInt(ZAn);
let offset : integer = UInt(o1);
let esize : integer{} = 64;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

### Encoding: `mova_z_p_rza_q`  — MOVA

**Assembly:** `MOVA  <Zd>.Q, <Pg>/M, <ZAn><HV>.Q[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<ZAn>` | ZAn (4b) | For the "128-bit" variant: is the name of the ZA tile ZA0-ZA15 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | — | For the "128-bit" variant: is the slice index offset, with implicit value 0. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = UInt(ZAn);
let offset : integer = 0;
let esize : integer{} = 128;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

---

## MOVA (tile to vector, two registers) -- A64

### Encoding: `mova_mz2_za_b1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.B-<Zd2>.B }, ZA0<HV>.B[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | off3 (3b) | For the "8-bit" variant: is the first slice index offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "8-bit" variant: is the second slice index offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 8;
let d : integer = UInt(Zd::'0');
let n : integer = 0;
let offset : integer = UInt(off3::'0');
let vertical : boolean = V == '1';
```

### Encoding: `mova_mz2_za_h1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.H-<Zd2>.H }, <ZAn><HV>.H[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<ZAn>` | ZAn (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | off2 (2b) | For the "16-bit" variant: is the first slice index offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "16-bit" variant: is the second slice index offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 16;
let d : integer = UInt(Zd::'0');
let n : integer = UInt(ZAn);
let offset : integer = UInt(off2::'0');
let vertical : boolean = V == '1';
```

### Encoding: `mova_mz2_za_w1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.S-<Zd2>.S }, <ZAn><HV>.S[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<ZAn>` | ZAn (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | o1 (1b) | For the "32-bit" variant: is the first slice index offset, encoded as "o1" field times 2. | let offset : integer = UInt(o1::'0'); |
| `<offs2>` | o1 (1b) | For the "32-bit" variant: is the second slice index offset, encoded as "o1" field times 2 plus 1. | let offset : integer = UInt(o1::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 32;
let d : integer = UInt(Zd::'0');
let n : integer = UInt(ZAn);
let offset : integer = UInt(o1::'0');
let vertical : boolean = V == '1';
```

### Encoding: `mova_mz2_za_d1`  — MOVA

**Assembly:** `MOVA  { <Zd1>.D-<Zd2>.D }, <ZAn><HV>.D[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<ZAn>` | ZAn (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | — | For the "64-bit" variant: is the first slice index offset, with implicit value 0. |  |
| `<offs2>` | — | For the "64-bit" variant: is the second slice index offset, with implicit value 1. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 64;
let d : integer = UInt(Zd::'0');
let n : integer = UInt(ZAn);
let offset : integer = 0;
let vertical : boolean = V == '1';
```

---

## MOVA (vector to array, four registers) -- A64

### Encoding: `mova_za_mz4_1`  — MOVA

**Assembly:** `MOVA  ZA.D[<Wv>, <offs>{, VGx4}], { <Zn1>.D-<Zn4>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off3);
let n : integer = UInt(Zn::'00');
let nreg : integer{} = 4;
```

---

## MOVA (vector to array, two registers) -- A64

### Encoding: `mova_za_mz2_1`  — MOVA

**Assembly:** `MOVA  ZA.D[<Wv>, <offs>{, VGx2}], { <Zn1>.D-<Zn2>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off3);
let n : integer = UInt(Zn::'0');
let nreg : integer{} = 2;
```

---

## MOVA (vector to tile, four registers) -- A64

### Encoding: `mova_za4_z_b1`  — MOVA

**Assembly:** `MOVA  ZA0<HV>.B[<Ws>, <offs1>:<offs4>], { <Zn1>.B-<Zn4>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | off2 (2b) | For the "8-bit" variant: is the first slice index offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "8-bit" variant: is the fourth slice index offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 8;
let n : integer = UInt(Zn::'00');
let d : integer = 0;
let offset : integer = UInt(off2::'00');
let vertical : boolean = V == '1';
```

### Encoding: `mova_za4_z_h1`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.H[<Ws>, <offs1>:<offs4>], { <Zn1>.H-<Zn4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | o1 (1b) | For the "16-bit" variant: is the first slice index offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "16-bit" variant: is the fourth slice index offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(ZAd);
let offset : integer = UInt(o1::'00');
let vertical : boolean = V == '1';
```

### Encoding: `mova_za4_z_w1`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.S[<Ws>, <offs1>:<offs4>], { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(ZAd);
let offset : integer = 0;
let vertical : boolean = V == '1';
```

### Encoding: `mova_za4_z_d1`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.D[<Ws>, <offs1>:<offs4>], { <Zn1>.D-<Zn4>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if MaxImplementedSVL() < 256 then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 64;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(ZAd);
let offset : integer = 0;
let vertical : boolean = V == '1';
```

---

## MOVA (vector to tile, single) -- A64

### Encoding: `mova_za_p_rz_b`  — MOVA

**Assembly:** `MOVA  ZA0<HV>.B[<Ws>, <offs>], <Pg>/M, <Zn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off4 (4b) | For the "8-bit" variant: is the slice index offset, in the range 0 to 15, encoded in the "off4" field. | let offset : integer = UInt(off4); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = UInt(Zn);
let d : integer = 0;
let offset : integer = UInt(off4);
let esize : integer{} = 8;
let vertical : boolean = V == '1';
```

### Encoding: `mova_za_p_rz_h`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.H[<Ws>, <offs>], <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off3 (3b) | For the "16-bit" variant: is the slice index offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = UInt(Zn);
let d : integer = UInt(ZAd);
let offset : integer = UInt(off3);
let esize : integer{} = 16;
let vertical : boolean = V == '1';
```

### Encoding: `mova_za_p_rz_w`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.S[<Ws>, <offs>], <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off2 (2b) | For the "32-bit" variant: is the slice index offset, in the range 0 to 3, encoded in the "off2" field. | let offset : integer = UInt(off2); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = UInt(Zn);
let d : integer = UInt(ZAd);
let offset : integer = UInt(off2);
let esize : integer{} = 32;
let vertical : boolean = V == '1';
```

### Encoding: `mova_za_p_rz_d`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.D[<Ws>, <offs>], <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | o1 (1b) | For the "64-bit" variant: is the slice index offset, in the range 0 to 1, encoded in the "o1" field. | let offset : integer = UInt(o1); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = UInt(Zn);
let d : integer = UInt(ZAd);
let offset : integer = UInt(o1);
let esize : integer{} = 64;
let vertical : boolean = V == '1';
```

### Encoding: `mova_za_p_rz_q`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.Q[<Ws>, <offs>], <Pg>/M, <Zn>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (4b) | For the "128-bit" variant: is the name of the ZA tile ZA0-ZA15 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | — | For the "128-bit" variant: is the slice index offset, with implicit value 0. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let s : integer = UInt('011'::Rs);
let n : integer = UInt(Zn);
let d : integer = UInt(ZAd);
let offset : integer = 0;
let esize : integer{} = 128;
let vertical : boolean = V == '1';
```

---

## MOVA (vector to tile, two registers) -- A64

### Encoding: `mova_za2_z_b1`  — MOVA

**Assembly:** `MOVA  ZA0<HV>.B[<Ws>, <offs1>:<offs2>], { <Zn1>.B-<Zn2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | off3 (3b) | For the "8-bit" variant: is the first slice index offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "8-bit" variant: is the second slice index offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 8;
let n : integer = UInt(Zn::'0');
let d : integer = 0;
let offset : integer = UInt(off3::'0');
let vertical : boolean = V == '1';
```

### Encoding: `mova_za2_z_h1`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.H[<Ws>, <offs1>:<offs2>], { <Zn1>.H-<Zn2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | off2 (2b) | For the "16-bit" variant: is the first slice index offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "16-bit" variant: is the second slice index offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(ZAd);
let offset : integer = UInt(off2::'0');
let vertical : boolean = V == '1';
```

### Encoding: `mova_za2_z_w1`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.S[<Ws>, <offs1>:<offs2>], { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | o1 (1b) | For the "32-bit" variant: is the first slice index offset, encoded as "o1" field times 2. | let offset : integer = UInt(o1::'0'); |
| `<offs2>` | o1 (1b) | For the "32-bit" variant: is the second slice index offset, encoded as "o1" field times 2 plus 1. | let offset : integer = UInt(o1::'0'); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(ZAd);
let offset : integer = UInt(o1::'0');
let vertical : boolean = V == '1';
```

### Encoding: `mova_za2_z_d1`  — MOVA

**Assembly:** `MOVA  <ZAd><HV>.D[<Ws>, <offs1>:<offs2>], { <Zn1>.D-<Zn2>.D }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAd>` | ZAd (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAd" field. | let d : integer = UInt(ZAd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | — | For the "64-bit" variant: is the first slice index offset, with implicit value 0. |  |
| `<offs2>` | — | For the "64-bit" variant: is the second slice index offset, with implicit value 1. |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 64;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(ZAd);
let offset : integer = 0;
let vertical : boolean = V == '1';
```

---

## MOVAZ (array to vector, four registers) -- A64

### Encoding: `movaz_mz_za4_1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.D-<Zd4>.D }, ZA.D[<Wv>, <offs>{, VGx4}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off3);
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
```

---

## MOVAZ (array to vector, two registers) -- A64

### Encoding: `movaz_mz_za2_1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.D-<Zd2>.D }, ZA.D[<Wv>, <offs>{, VGx2}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off3);
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
```

---

## MOVAZ (tile to vector, four registers) -- A64

### Encoding: `movaz_mz4_za_b1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.B-<Zd4>.B }, ZA0<HV>.B[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | off2 (2b) | For the "8-bit" variant: is the first slice index offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "8-bit" variant: is the fourth slice index offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 8;
let d : integer = UInt(Zd::'00');
let n : integer = 0;
let offset : integer = UInt(off2::'00');
let vertical : boolean = V == '1';
```

### Encoding: `movaz_mz4_za_h1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.H-<Zd4>.H }, <ZAn><HV>.H[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<ZAn>` | ZAn (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | o1 (1b) | For the "16-bit" variant: is the first slice index offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "16-bit" variant: is the fourth slice index offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 16;
let d : integer = UInt(Zd::'00');
let n : integer = UInt(ZAn);
let offset : integer = UInt(o1::'00');
let vertical : boolean = V == '1';
```

### Encoding: `movaz_mz4_za_w1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.S-<Zd4>.S }, <ZAn><HV>.S[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<ZAn>` | ZAn (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 32;
let d : integer = UInt(Zd::'00');
let n : integer = UInt(ZAn);
let offset : integer = 0;
let vertical : boolean = V == '1';
```

### Encoding: `movaz_mz4_za_d1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.D-<Zd4>.D }, <ZAn><HV>.D[<Ws>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<ZAn>` | ZAn (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | — | For the "32-bit" and "64-bit" variants: is the first slice index offset, with implicit value 0. |  |
| `<offs4>` | — | For the "32-bit" and "64-bit" variants: is the fourth slice index offset, with implicit value 3. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
if MaxImplementedSVL() < 256 then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 4;
let esize : integer{} = 64;
let d : integer = UInt(Zd::'00');
let n : integer = UInt(ZAn);
let offset : integer = 0;
let vertical : boolean = V == '1';
```

---

## MOVAZ (tile to vector, single) -- A64

### Encoding: `movaz_z_rza_b`  — MOVAZ

**Assembly:** `MOVAZ  <Zd>.B, ZA0<HV>.B[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off4 (4b) | For the "8-bit" variant: is the slice index offset, in the range 0 to 15, encoded in the "off4" field. | let offset : integer = UInt(off4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let n : integer = 0;
let offset : integer = UInt(off4);
let esize : integer{} = 8;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

### Encoding: `movaz_z_rza_h`  — MOVAZ

**Assembly:** `MOVAZ  <Zd>.H, <ZAn><HV>.H[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<ZAn>` | ZAn (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off3 (3b) | For the "16-bit" variant: is the slice index offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let n : integer = UInt(ZAn);
let offset : integer = UInt(off3);
let esize : integer{} = 16;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

### Encoding: `movaz_z_rza_w`  — MOVAZ

**Assembly:** `MOVAZ  <Zd>.S, <ZAn><HV>.S[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<ZAn>` | ZAn (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off2 (2b) | For the "32-bit" variant: is the slice index offset, in the range 0 to 3, encoded in the "off2" field. | let offset : integer = UInt(off2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let n : integer = UInt(ZAn);
let offset : integer = UInt(off2);
let esize : integer{} = 32;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

### Encoding: `movaz_z_rza_d`  — MOVAZ

**Assembly:** `MOVAZ  <Zd>.D, <ZAn><HV>.D[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<ZAn>` | ZAn (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | o1 (1b) | For the "64-bit" variant: is the slice index offset, in the range 0 to 1, encoded in the "o1" field. | let offset : integer = UInt(o1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let n : integer = UInt(ZAn);
let offset : integer = UInt(o1);
let esize : integer{} = 64;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

### Encoding: `movaz_z_rza_q`  — MOVAZ

**Assembly:** `MOVAZ  <Zd>.Q, <ZAn><HV>.Q[<Ws>, <offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<ZAn>` | ZAn (4b) | For the "128-bit" variant: is the name of the ZA tile ZA0-ZA15 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | — | For the "128-bit" variant: is the slice index offset, with implicit value 0. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let n : integer = UInt(ZAn);
let offset : integer = 0;
let esize : integer{} = 128;
let d : integer = UInt(Zd);
let vertical : boolean = V == '1';
```

---

## MOVAZ (tile to vector, two registers) -- A64

### Encoding: `movaz_mz2_za_b1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.B-<Zd2>.B }, ZA0<HV>.B[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | off3 (3b) | For the "8-bit" variant: is the first slice index offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "8-bit" variant: is the second slice index offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 8;
let d : integer = UInt(Zd::'0');
let n : integer = 0;
let offset : integer = UInt(off3::'0');
let vertical : boolean = V == '1';
```

### Encoding: `movaz_mz2_za_h1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.H-<Zd2>.H }, <ZAn><HV>.H[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<ZAn>` | ZAn (1b) | For the "16-bit" variant: is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | off2 (2b) | For the "16-bit" variant: is the first slice index offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "16-bit" variant: is the second slice index offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 16;
let d : integer = UInt(Zd::'0');
let n : integer = UInt(ZAn);
let offset : integer = UInt(off2::'0');
let vertical : boolean = V == '1';
```

### Encoding: `movaz_mz2_za_w1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.S-<Zd2>.S }, <ZAn><HV>.S[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<ZAn>` | ZAn (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | o1 (1b) | For the "32-bit" variant: is the first slice index offset, encoded as "o1" field times 2. | let offset : integer = UInt(o1::'0'); |
| `<offs2>` | o1 (1b) | For the "32-bit" variant: is the second slice index offset, encoded as "o1" field times 2 plus 1. | let offset : integer = UInt(o1::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 32;
let d : integer = UInt(Zd::'0');
let n : integer = UInt(ZAn);
let offset : integer = UInt(o1::'0');
let vertical : boolean = V == '1';
```

### Encoding: `movaz_mz2_za_d1`  — MOVAZ

**Assembly:** `MOVAZ  { <Zd1>.D-<Zd2>.D }, <ZAn><HV>.D[<Ws>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<ZAn>` | ZAn (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAn" field. | let n : integer = UInt(ZAn); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs1>` | — | For the "64-bit" variant: is the first slice index offset, with implicit value 0. |  |
| `<offs2>` | — | For the "64-bit" variant: is the second slice index offset, with implicit value 1. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let s : integer = UInt('011'::Rs);
let nreg : integer{} = 2;
let esize : integer{} = 64;
let d : integer = UInt(Zd::'0');
let n : integer = UInt(ZAn);
let offset : integer = 0;
let vertical : boolean = V == '1';
```

---

## MOVI -- A64

### Encoding: `MOVI_asimdimm_N_b`  [8-bit]  — MOVI

**Assembly:** `MOVI  <Vd>.<T>, #<imm8>{, LSL #0}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `MOVI_asimdimm_L_hl`  [16-bit shifted immediate]  — MOVI

**Assembly:** `MOVI  <Vd>.<T>, #<imm8>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `MOVI_asimdimm_L_sl`  [32-bit shifted immediate]  — MOVI

**Assembly:** `MOVI  <Vd>.<T>, #<imm8>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `MOVI_asimdimm_M_sm`  [32-bit shifting ones]  — MOVI

**Assembly:** `MOVI  <Vd>.<T>, #<imm8>, MSL #<amount>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `MOVI_asimdimm_D_ds`  [64-bit scalar]  — MOVI

**Assembly:** `MOVI  <Dd>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<imm>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is a 64-bit immediate 'aaaaaaaabbbbbbbbccccccccddddddddeeeeeeeeffffffffgggggggghhhhhhhh', encoded in "a:b:c:d:e:f:g:h". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `MOVI_asimdimm_D2_d`  [64-bit vector]  — MOVI

**Assembly:** `MOVI  <Vd>.2D, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<imm>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is a 64-bit immediate 'aaaaaaaabbbbbbbbccccccccddddddddeeeeeeeeffffffffgggggggghhhhhhhh', encoded in "a:b:c:d:e:f:g:h". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

---

## MOVK -- A64

### Encoding: `MOVK_32_movewide`  [32-bit]  — MOVK

**Assembly:** `MOVK  <Wd>, #<imm>{, LSL #<shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<imm>` | imm16 (16b) | Is the 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let imm : bits(16) = imm16; |
| `<shift>` | hw (2b) | For the "32-bit" variant: is the amount by which to shift the immediate left, either 0 (the default) or 16, encoded in t | if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let pos : integer{} = UInt(hw) << 4; |

**Pseudocode (Decode):**
```
if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
let datasize : integer{} = 32 << UInt(sf);
let imm : bits(16) = imm16;
let pos : integer{} = UInt(hw) << 4;
```

### Encoding: `MOVK_64_movewide`  [64-bit]  — MOVK

**Assembly:** `MOVK  <Xd>, #<imm>{, LSL #<shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<imm>` | imm16 (16b) | Is the 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let imm : bits(16) = imm16; |
| `<shift>` | hw (2b) | For the "64-bit" variant: is the amount by which to shift the immediate left, either 0 (the default), 16, 32 or 48, enco | if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let pos : integer{} = UInt(hw) << 4; |

**Pseudocode (Decode):**
```
if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
let datasize : integer{} = 32 << UInt(sf);
let imm : bits(16) = imm16;
let pos : integer{} = UInt(hw) << 4;
```

---

## MOVN -- A64

### Encoding: `MOVN_32_movewide`  [32-bit]  — MOVN

**Assembly:** `MOVN  <Wd>, #<imm>{, LSL #<shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<imm>` | imm16 (16b) | Is the 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let imm : bits(16) = imm16; |
| `<shift>` | hw (2b) | For the "32-bit" variant: is the amount by which to shift the immediate left, either 0 (the default) or 16, encoded in t | if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let pos : integer{} = UInt(hw) << 4; |

**Pseudocode (Decode):**
```
if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
let datasize : integer{} = 32 << UInt(sf);
let imm : bits(16) = imm16;
let pos : integer{} = UInt(hw) << 4;
```

### Encoding: `MOVN_64_movewide`  [64-bit]  — MOVN

**Assembly:** `MOVN  <Xd>, #<imm>{, LSL #<shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<imm>` | imm16 (16b) | Is the 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let imm : bits(16) = imm16; |
| `<shift>` | hw (2b) | For the "64-bit" variant: is the amount by which to shift the immediate left, either 0 (the default), 16, 32 or 48, enco | if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let pos : integer{} = UInt(hw) << 4; |

**Pseudocode (Decode):**
```
if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
let datasize : integer{} = 32 << UInt(sf);
let imm : bits(16) = imm16;
let pos : integer{} = UInt(hw) << 4;
```

---

## MOVPRFX (predicated) -- A64

### Encoding: `movprfx_z_p_z_`  — MOVPRFX

**Assembly:** `MOVPRFX  <Zd>.<T>, <Pg>/<ZM>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<ZM>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = (M == '1');
```

---

## MOVPRFX (unpredicated) -- A64

### Encoding: `movprfx_z_z_`  — MOVPRFX

**Assembly:** `MOVPRFX  <Zd>, <Zn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## MOVS (predicated) -- A64 *(alias for ands_p_p_pp)*

### Encoding: `movzs_p_p_p__ands_p_p_pp_z`  — ANDS

**Assembly:** `MOVS  <Pd>.B, <Pg>/Z, <Pn>.B / ANDS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. |  |
| `ANDS` | — |  |  |

---

## MOVS (unpredicated) -- A64 *(alias for orrs_p_p_pp)*

### Encoding: `movs_p_p__orrs_p_p_pp_z`  — ORRS

**Assembly:** `MOVS  <Pd>.B, <Pn>.B / ORRS  <Pd>.B, <Pn>/Z, <Pn>.B, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. |  |
| `ORRS` | — |  |  |

---

## MOVT (scalar to table) -- A64

### Encoding: `movt_zt_r_`  — MOVT

**Assembly:** `MOVT  ZT0[<offs>], <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<offs>` | off3 (3b) | Is the immediate byte offset, a multiple of 8 in the range of 0 to 56, encoded in the "off3" field as <offs>/8. | let offset : integer = UInt(off3); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !Halted() then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let offset : integer = UInt(off3);
```

---

## MOVT (table to scalar) -- A64

### Encoding: `movt_r_zt_`  — MOVT

**Assembly:** `MOVT  <Xt>, ZT0[<offs>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<offs>` | off3 (3b) | Is the immediate byte offset, a multiple of 8 in the range of 0 to 56, encoded in the "off3" field as <offs>/8. | let offset : integer = UInt(off3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) || !Halted() then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let offset : integer = UInt(off3);
```

---

## MOVT (vector to table) -- A64

### Encoding: `movt_zt_z_`  — MOVT

**Assembly:** `MOVT  ZT0{[<offs>, MUL VL]}, <Zt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<offs>` | off2 (2b) | Is the vector length offset, in the range 0 to 3, defaulting to 0 when omitted, encoded in the "off2" field. | let imm : integer = UInt(off2); |
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_LUTv2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let imm : integer = UInt(off2);
```

---

## MOVZ -- A64

### Encoding: `MOVZ_32_movewide`  [32-bit]  — MOVZ

**Assembly:** `MOVZ  <Wd>, #<imm>{, LSL #<shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<imm>` | imm16 (16b) | Is the 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let imm : bits(16) = imm16; |
| `<shift>` | hw (2b) | For the "32-bit" variant: is the amount by which to shift the immediate left, either 0 (the default) or 16, encoded in t | if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let pos : integer{} = UInt(hw) << 4; |

**Pseudocode (Decode):**
```
if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
let datasize : integer{} = 32 << UInt(sf);
let imm : bits(16) = imm16;
let pos : integer{} = UInt(hw) << 4;
```

### Encoding: `MOVZ_64_movewide`  [64-bit]  — MOVZ

**Assembly:** `MOVZ  <Xd>, #<imm>{, LSL #<shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<imm>` | imm16 (16b) | Is the 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let imm : bits(16) = imm16; |
| `<shift>` | hw (2b) | For the "64-bit" variant: is the amount by which to shift the immediate left, either 0 (the default), 16, 32 or 48, enco | if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let pos : integer{} = UInt(hw) << 4; |

**Pseudocode (Decode):**
```
if sf == '0' && hw[1] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
let datasize : integer{} = 32 << UInt(sf);
let imm : bits(16) = imm16;
let pos : integer{} = UInt(hw) << 4;
```

---

## MRRS -- A64

### Encoding: `MRRS_RS_systemmovepr`  — MRRS

**Assembly:** `MRRS  <Xt>, <Xt+1>, (<systemreg>|S<op0>_<op1>_<Cn>_<Cm>_<op2>)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose destination register, encoded in the "Rt" field. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer       = UInt(Rt);<br>let t2 : integer      = UInt(Rt+1); |
| `<Xt+1>` | Rt (5b) | Is the 64-bit name of the second general-purpose destination register, encoded as "Rt" +1. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer       = UInt(Rt);<br>let t2 : integer      = UInt(Rt+1); |
| `<systemreg>` | (o0 :: op1 :: CRn :: CRm :: op2) | Is a System register name, encoded in "o0:op1:CRn:CRm:op2". |  |
| `<op0>` | — |  |  |
| `<op1>` | op1 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op1" field. | let sys_op1 : bits(3) = op1; |
| `<Cn>` | CRn (4b) | Is a name 'Cn', with 'n' in the range 0 to 15, encoded in the "CRn" field. | let sys_crn : bits(4) = CRn; |
| `<Cm>` | CRm (4b) | Is a name 'Cm', with 'm' in the range 0 to 15, encoded in the "CRm" field. | let sys_crm : bits(4) = CRm; |
| `<op2>` | op2 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op2" field. | let sys_op2 : bits(3) = op2; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SYSREG128) then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;

let t : integer       = UInt(Rt);
let t2 : integer      = UInt(Rt+1);
let sys_L : bits(1)   = L;
let sys_op0 : bits(2) = '1' :: o0;
let sys_op1 : bits(3) = op1;
let sys_op2 : bits(3) = op2;
let sys_crn : bits(4) = CRn;
let sys_crm : bits(4) = CRm;
```

---

## MRS -- A64

### Encoding: `MRS_RS_systemmove`  — MRS

**Assembly:** `MRS  <Xt>, (<systemreg>|S<op0>_<op1>_<Cn>_<Cm>_<op2>)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rt" field. | let t : integer       = UInt(Rt); |
| `<systemreg>` | (o0 :: op1 :: CRn :: CRm :: op2) | Is a System register name, encoded in "o0:op1:CRn:CRm:op2".           The System register names are defined in AArch64 S |  |
| `<op0>` | — |  |  |
| `<op1>` | op1 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op1" field. | let sys_op1 : bits(3) = op1; |
| `<Cn>` | CRn (4b) | Is a name 'Cn', with 'n' in the range 0 to 15, encoded in the "CRn" field. | let sys_crn : bits(4) = CRn; |
| `<Cm>` | CRm (4b) | Is a name 'Cm', with 'm' in the range 0 to 15, encoded in the "CRm" field. | let sys_crm : bits(4) = CRm; |
| `<op2>` | op2 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op2" field. | let sys_op2 : bits(3) = op2; |

**Pseudocode (Decode):**
```
let t : integer       = UInt(Rt);
let sys_L : bits(1)   = L;
let sys_op0 : bits(2) = '1' :: o0;
let sys_op1 : bits(3) = op1;
let sys_op2 : bits(3) = op2;
let sys_crn : bits(4) = CRn;
let sys_crm : bits(4) = CRm;
```

---

## MSB -- A64

### Encoding: `msb_z_p_zzz_`  — MSB

**Assembly:** `MSB  <Zdn>.<T>, <Pg>/M, <Zm>.<T>, <Za>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Za>` | Za (5b) | Is the name of the third source scalable vector register, encoded in the "Za" field. | let a : integer = UInt(Za); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
let a : integer = UInt(Za);
let sub_op : boolean = TRUE;
```

---

## MSR (immediate) -- A64

### Encoding: `MSR_SI_pstate`  — MSR

**Assembly:** `MSR  <pstatefield>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<pstatefield>` | — |  |  |
| `<imm>` | (CRm :: op1 :: op2) | Is a 4-bit unsigned immediate, in the range 0 to 15, encoded in the "CRm" field. Restricted to the range 0 to 1, encoded |  |

**Pseudocode (Decode):**
```
if op1 == '000' && op2 == '000' then See("CFINV"); end;
if op1 == '000' && op2 == '001' then See("XAFLAG"); end;
if op1 == '000' && op2 == '010' then See("AXFLAG"); end;

var min_EL : bits(2);
var need_secure : boolean = FALSE;

case op1 of
    when '00x' =>
        min_EL = EL1;
    when '010' =>
        min_EL = EL1;
    when '011' =>
        min_EL = EL0;
    when '100' =>
        min_EL = EL2;
    when '101' =>
        if !IsFeatureImplemented(FEAT_VHE) then EndOfDecode(Decode_UNDEF); end;
        min_EL = EL2;
    when '110' =>
        min_EL = EL3;
    when '111' =>
        min_EL = EL1;
        need_secure = TRUE;
end;

let operand : bits(4) = CRm;
var field : PSTATEField;
case op1::op2 of
    when '000 011' =>
        if !IsFeatureImplemented(FEAT_UAO) then EndOfDecode(Decode_UNDEF); end;
        field = PSTATEField_UAO;
    when '000 100' =>
        if !IsFeatureImplemented(FEAT_PAN) then EndOfDecode(Decode_UNDEF); end;
        field = PSTATEField_PAN;
    when '000 101' => field = PSTATEField_SP;
    when '001 000' =>
        case CRm of
            when '000x' =>
                if !IsFeatureImplemented(FEAT_NMI) then EndOfDecode(Decode_UNDEF); end;
                field = PSTATEField_ALLINT;
            when '001x' =>
                if !IsFeatureImplemented(FEAT_EBEP) then EndOfDecode(Decode_UNDEF); end;
                field = PSTATEField_PM;
            otherwise =>
                EndOfDecode(Decode_UNDEF);
        end;
    when '011 010' =>
        if !IsFeatureImplemented(FEAT_DIT) then EndOfDecode(Decode_UNDEF); end;
        field = PSTATEField_DIT;
    when '011 011' =>
        case CRm of
            when '001x' =>
                if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
                field = PSTATEField_SVCRSM;
            when '010x' =>
                if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
                field = PSTATEField_SVCRZA;
            when '011x' =>
                if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
                field = PSTATEField_SVCRSMZA;
            otherwise =>
                EndOfDecode(Decode_UNDEF);
        end;
    when '011 100' =>
        if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
        field = PSTATEField_TCO;
    when '011 110' => field = PSTATEField_DAIFSet;
    when '011 111' => field = PSTATEField_DAIFClr;
    when '011 001' =>
        if !IsFeatureImplemented(FEAT_SSBS) then EndOfDecode(Decode_UNDEF); end;
        field = PSTATEField_SSBS;
    otherwise =>      EndOfDecode(Decode_UNDEF);
end;
```

---

## MSR (register) -- A64

### Encoding: `MSR_SR_systemmove`  — MSR

**Assembly:** `MSR  (<systemreg>|S<op0>_<op1>_<Cn>_<Cm>_<op2>), <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<systemreg>` | (o0 :: op1 :: CRn :: CRm :: op2) | Is a System register name, encoded in "o0:op1:CRn:CRm:op2".           The System register names are defined in AArch64 S |  |
| `<op0>` | — |  |  |
| `<op1>` | op1 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op1" field. | let sys_op1 : bits(3) = op1; |
| `<Cn>` | CRn (4b) | Is a name 'Cn', with 'n' in the range 0 to 15, encoded in the "CRn" field. | let sys_crn : bits(4) = CRn; |
| `<Cm>` | CRm (4b) | Is a name 'Cm', with 'm' in the range 0 to 15, encoded in the "CRm" field. | let sys_crm : bits(4) = CRm; |
| `<op2>` | op2 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op2" field. | let sys_op2 : bits(3) = op2; |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. | let t : integer       = UInt(Rt); |

**Pseudocode (Decode):**
```
let t : integer       = UInt(Rt);
let sys_L : bits(1)   = L;
let sys_op0 : bits(2) = '1' :: o0;
let sys_op1 : bits(3) = op1;
let sys_op2 : bits(3) = op2;
let sys_crn : bits(4) = CRn;
let sys_crm : bits(4) = CRm;
```

---

## MSRR -- A64

### Encoding: `MSRR_SR_systemmovepr`  — MSRR

**Assembly:** `MSRR  (<systemreg>|S<op0>_<op1>_<Cn>_<Cm>_<op2>), <Xt>, <Xt+1>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<systemreg>` | (o0 :: op1 :: CRn :: CRm :: op2) | Is a System register name, encoded in "o0:op1:CRn:CRm:op2". |  |
| `<op0>` | — |  |  |
| `<op1>` | op1 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op1" field. | let sys_op1 : bits(3) = op1; |
| `<Cn>` | CRn (4b) | Is a name 'Cn', with 'n' in the range 0 to 15, encoded in the "CRn" field. | let sys_crn : bits(4) = CRn; |
| `<Cm>` | CRm (4b) | Is a name 'Cm', with 'm' in the range 0 to 15, encoded in the "CRm" field. | let sys_crm : bits(4) = CRm; |
| `<op2>` | op2 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op2" field. | let sys_op2 : bits(3) = op2; |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rt" field. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer       = UInt(Rt);<br>let t2 : integer      = UInt(Rt+1); |
| `<Xt+1>` | Rt (5b) | Is the 64-bit name of the second  general-purpose source register, encoded as "Rt" +1. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer       = UInt(Rt);<br>let t2 : integer      = UInt(Rt+1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SYSREG128) then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;

let t : integer       = UInt(Rt);
let t2 : integer      = UInt(Rt+1);
let sys_L : bits(1)   = L;
let sys_op0 : bits(2) = '1' :: o0;
let sys_op1 : bits(3) = op1;
let sys_op2 : bits(3) = op2;
let sys_crn : bits(4) = CRn;
let sys_crm : bits(4) = CRm;
```

---

## MSUB -- A64

### Encoding: `MSUB_32A_dp_3src`  [32-bit]  — MSUB

**Assembly:** `MSUB  <Wd>, <Wn>, <Wm>, <Wa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Wa>` | Ra (5b) | Is the 32-bit name of the third general-purpose source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `MSUB_64A_dp_3src`  [64-bit]  — MSUB

**Assembly:** `MSUB  <Xd>, <Xn>, <Xm>, <Xa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Xa>` | Ra (5b) | Is the 64-bit name of the third general-purpose source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
let datasize : integer{} = 32 << UInt(sf);
```

---

## MSUBPT -- A64

### Encoding: `MSUBPT_64A_dp_3src`  — MSUBPT

**Assembly:** `MSUBPT  <Xd>, <Xn>, <Xm>, <Xa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Xa>` | Ra (5b) | Is the 64-bit name of the third general-purpose source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CPA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
```

---

## MUL (by element) -- A64

### Encoding: `MUL_asimdelem_R`  — MUL

**Assembly:** `MUL  <Vd>.<T>, <Vn>.<T>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L);   Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## MUL (immediate) -- A64

### Encoding: `mul_z_zi_`  — MUL

**Assembly:** `MUL  <Zdn>.<T>, <Zdn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is the signed immediate operand, in the range -128 to 127, encoded in the "imm8" field. | let imm : integer = SInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
let imm : integer = SInt(imm8);
```

---

## MUL (indexed) -- A64

### Encoding: `mul_z_zzi_h`  — MUL

**Assembly:** `MUL  <Zd>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "16-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `mul_z_zzi_s`  — MUL

**Assembly:** `MUL  <Zd>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "32-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `mul_z_zzi_d`  — MUL

**Assembly:** `MUL  <Zd>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "64-bit" variant: is the element index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## MUL (vector) -- A64

### Encoding: `MUL_asimdsame_only`  — MUL

**Assembly:** `MUL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if U == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## MUL (vectors, predicated) -- A64

### Encoding: `mul_z_p_zz_`  — MUL

**Assembly:** `MUL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## MUL (vectors, unpredicated) -- A64

### Encoding: `mul_z_zz_`  — MUL

**Assembly:** `MUL  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## MUL -- A64 *(alias for MADD)*

### Encoding: `MUL_MADD_32A_dp_3src`  [32-bit]  — MADD

**Assembly:** `MUL  <Wd>, <Wn>, <Wm> / MADD  <Wd>, <Wn>, <Wm>, WZR`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. |  |
| `MADD` | — |  |  |

### Encoding: `MUL_MADD_64A_dp_3src`  [64-bit]  — MADD

**Assembly:** `MUL  <Xd>, <Xn>, <Xm> / MADD  <Xd>, <Xn>, <Xm>, XZR`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. |  |
| `MADD` | — |  |  |

---

## MVN -- A64 *(alias for NOT_advsimd)*

### Encoding: `MVN_NOT_asimdmisc_R`  — NOT

**Assembly:** `MVN  <Vd>.<T>, <Vn>.<T> / NOT  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. |  |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. |  |
| `NOT` | — |  |  |

---

## MVN -- A64 *(alias for ORN_log_shift)*

### Encoding: `MVN_ORN_32_log_shift`  [32-bit]  — ORN

**Assembly:** `MVN  <Wd>, <Wm>{, <shift> #<amount>} / ORN  <Wd>, WZR, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. |  |
| `ORN` | — |  |  |

### Encoding: `MVN_ORN_64_log_shift`  [64-bit]  — ORN

**Assembly:** `MVN  <Xd>, <Xm>{, <shift> #<amount>} / ORN  <Xd>, XZR, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. |  |
| `ORN` | — |  |  |

---

## MVNI -- A64

### Encoding: `MVNI_asimdimm_L_hl`  [16-bit shifted immediate]  — MVNI

**Assembly:** `MVNI  <Vd>.<T>, #<imm8>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `MVNI_asimdimm_L_sl`  [32-bit shifted immediate]  — MVNI

**Assembly:** `MVNI  <Vd>.<T>, #<imm8>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `MVNI_asimdimm_M_sm`  [32-bit shifting ones]  — MVNI

**Assembly:** `MVNI  <Vd>.<T>, #<imm8>, MSL #<amount>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

---

## NAND -- A64

### Encoding: `nand_p_p_pp_z`  — NAND

**Assembly:** `NAND  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## NANDS -- A64

### Encoding: `nands_p_p_pp_z`  — NANDS

**Assembly:** `NANDS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## NBSL -- A64

### Encoding: `nbsl_z_zzz_`  — NBSL

**Assembly:** `NBSL  <Zdn>.D, <Zdn>.D, <Zm>.D, <Zk>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | Zk (5b) | Is the name of the third source scalable vector register, encoded in the "Zk" field. | let k : integer = UInt(Zk); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let m : integer = UInt(Zm);
let k : integer = UInt(Zk);
let dn : integer = UInt(Zdn);
```

---

## NEG (shifted register) -- A64 *(alias for SUB_addsub_shift)*

### Encoding: `NEG_SUB_32_addsub_shift`  [32-bit]  — SUB

**Assembly:** `NEG  <Wd>, <Wm>{, <shift> #<amount>} / SUB  <Wd>, WZR, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. |  |
| `SUB` | — |  |  |

### Encoding: `NEG_SUB_64_addsub_shift`  [64-bit]  — SUB

**Assembly:** `NEG  <Xd>, <Xm>{, <shift> #<amount>} / SUB  <Xd>, XZR, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. |  |
| `SUB` | — |  |  |

---

## NEG (vector) -- A64

### Encoding: `NEG_asisdmisc_R`  — NEG

**Assembly:** `NEG  D<d>, D<n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `NEG_asimdmisc_R`  — NEG

**Assembly:** `NEG  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## NEG -- A64

### Encoding: `neg_z_p_z_m`  — NEG

**Assembly:** `NEG  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `neg_z_p_z_z`  — NEG

**Assembly:** `NEG  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## NEGS -- A64 *(alias for SUBS_addsub_shift)*

### Encoding: `NEGS_SUBS_32_addsub_shift`  [32-bit]  — SUBS

**Assembly:** `NEGS  <Wd>, <Wm>{, <shift> #<amount>} / SUBS  <Wd>, WZR, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. |  |
| `SUBS` | — |  |  |

### Encoding: `NEGS_SUBS_64_addsub_shift`  [64-bit]  — SUBS

**Assembly:** `NEGS  <Xd>, <Xm>{, <shift> #<amount>} / SUBS  <Xd>, XZR, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. |  |
| `SUBS` | — |  |  |

---

## NGC -- A64 *(alias for SBC)*

### Encoding: `NGC_SBC_32_addsub_carry`  [32-bit]  — SBC

**Assembly:** `NGC  <Wd>, <Wm> / SBC  <Wd>, WZR, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `SBC` | — |  |  |

### Encoding: `NGC_SBC_64_addsub_carry`  [64-bit]  — SBC

**Assembly:** `NGC  <Xd>, <Xm> / SBC  <Xd>, XZR, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `SBC` | — |  |  |

---

## NGCS -- A64 *(alias for SBCS)*

### Encoding: `NGCS_SBCS_32_addsub_carry`  [32-bit]  — SBCS

**Assembly:** `NGCS  <Wd>, <Wm> / SBCS  <Wd>, WZR, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `SBCS` | — |  |  |

### Encoding: `NGCS_SBCS_64_addsub_carry`  [64-bit]  — SBCS

**Assembly:** `NGCS  <Xd>, <Xm> / SBCS  <Xd>, XZR, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rm" field. |  |
| `SBCS` | — |  |  |

---

## NMATCH -- A64

### Encoding: `nmatch_p_p_zz_`  — NMATCH

**Assembly:** `NMATCH  <Pd>.<T>, <Pg>/Z, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
if size IN {'1x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let d : integer = UInt(Pd);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
```

---

## NOP -- A64

### Encoding: `NOP_HI_hints`  — NOP

**Assembly:** `NOP`

**Pseudocode (Decode):**
```
// Empty.
```

---

## NOR -- A64

### Encoding: `nor_p_p_pp_z`  — NOR

**Assembly:** `NOR  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## NORS -- A64

### Encoding: `nors_p_p_pp_z`  — NORS

**Assembly:** `NORS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## NOT (predicate) -- A64 *(alias for eor_p_p_pp)*

### Encoding: `not_p_p_p_z_eor_p_p_pp_z`  — EOR

**Assembly:** `NOT  <Pd>.B, <Pg>/Z, <Pn>.B / EOR  <Pd>.B, <Pg>/Z, <Pn>.B, <Pg>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. |  |
| `EOR` | — |  |  |

---

## NOT (vector) -- A64

### Encoding: `not_z_p_z_m`  — NOT

**Assembly:** `NOT  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `not_z_p_z_z`  — NOT

**Assembly:** `NOT  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## NOT -- A64

### Encoding: `NOT_asimdmisc_R`  — NOT

**Assembly:** `NOT  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
```

---

## NOTS -- A64 *(alias for eors_p_p_pp)*

### Encoding: `nots_p_p_p_z_eors_p_p_pp_z`  — EORS

**Assembly:** `NOTS  <Pd>.B, <Pg>/Z, <Pn>.B / EORS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pg>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. |  |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. |  |
| `EORS` | — |  |  |

---

## ORN (immediate) -- A64 *(alias for orr_z_zi)*

### Encoding: `orn_z_zi__orr_z_zi_`  — ORR

**Assembly:** `ORN  <Zdn>.<T>, <Zdn>.<T>, #<const> / ORR  <Zdn>.<T>, <Zdn>.<T>, #(-<const> - 1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. |  |
| `<T>` | — |  |  |
| `<const>` | imm13 (13b) | Is a 64, 32, 16 or 8-bit bitmask consisting of replicated 2, 4, 8, 16, 32 or 64 bit fields, each field containing a rota |  |
| `ORR` | — |  |  |

---

## ORN (predicates) -- A64

### Encoding: `orn_p_p_pp_z`  — ORN

**Assembly:** `ORN  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## ORN (shifted register) -- A64

### Encoding: `ORN_32_log_shift`  [32-bit]  — ORN

**Assembly:** `ORN  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `ORN_64_log_shift`  [64-bit]  — ORN

**Assembly:** `ORN  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## ORN (vector) -- A64

### Encoding: `ORN_asimdsame_only`  — ORN

**Assembly:** `ORN  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
```

---

## ORNS -- A64

### Encoding: `orns_p_p_pp_z`  — ORNS

**Assembly:** `ORNS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## ORQV -- A64

### Encoding: `orqv_z_p_z_`  — ORQV

**Assembly:** `ORQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## ORR (immediate) -- A64

### Encoding: `ORR_32_log_imm`  [32-bit]  — ORR

**Assembly:** `ORR  <Wd|WSP>, <Wn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd\|WSP>` | Rd (5b) | Is the 32-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | (imms :: immr) | For the "32-bit" variant: is the bitmask immediate, encoded in "imms:immr". |  |

**Pseudocode (Decode):**
```
if sf == '0' && N != '0' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
var imm : bits(datasize);
(imm, -) = DecodeBitMasks{datasize}(N, imms, immr, TRUE);
```

### Encoding: `ORR_64_log_imm`  [64-bit]  — ORR

**Assembly:** `ORR  <Xd|SP>, <Xn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | (N :: imms :: immr) | For the "64-bit" variant: is the bitmask immediate, encoded in "N:imms:immr". |  |

**Pseudocode (Decode):**
```
if sf == '0' && N != '0' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
var imm : bits(datasize);
(imm, -) = DecodeBitMasks{datasize}(N, imms, immr, TRUE);
```

---

## ORR (immediate) -- A64

### Encoding: `orr_z_zi_`  — ORR

**Assembly:** `ORR  <Zdn>.<T>, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<const>` | imm13 (13b) | Is a 64, 32, 16 or 8-bit bitmask consisting of replicated 2, 4, 8, 16, 32 or 64 bit fields, each field containing a rota | (imm, -) = DecodeBitMasks{64}(imm13[12], imm13[5:0], imm13[11:6], TRUE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let dn : integer = UInt(Zdn);
var imm : bits(64);
(imm, -) = DecodeBitMasks{64}(imm13[12], imm13[5:0], imm13[11:6], TRUE);
```

---

## ORR (predicates) -- A64

### Encoding: `orr_p_p_pp_z`  — ORR

**Assembly:** `ORR  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## ORR (shifted register) -- A64

### Encoding: `ORR_32_log_shift`  [32-bit]  — ORR

**Assembly:** `ORR  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `ORR_64_log_shift`  [64-bit]  — ORR

**Assembly:** `ORR  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## ORR (vector, immediate) -- A64

### Encoding: `ORR_asimdimm_L_hl`  [16-bit]  — ORR

**Assembly:** `ORR  <Vd>.<T>, #<imm8>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

### Encoding: `ORR_asimdimm_L_sl`  [32-bit]  — ORR

**Assembly:** `ORR  <Vd>.<T>, #<imm8>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP register, encoded in the "Rd" field. | let rd : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<imm8>` | (a :: b :: c :: d :: e :: f :: g :: h) | Is an 8-bit immediate encoded in "a:b:c:d:e:f:g:h". |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let rd : integer = UInt(Rd);
let datasize : integer{} = 64 << UInt(Q);
let imm64 : bits(64) = AdvSIMDExpandImm(op, cmode, a::b::c::d::e::f::g::h);
let imm : bits(datasize) = Replicate{}(imm64);
```

---

## ORR (vector, register) -- A64

### Encoding: `ORR_asimdsame_only`  — ORR

**Assembly:** `ORR  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
```

---

## ORR (vectors, predicated) -- A64

### Encoding: `orr_z_p_zz_`  — ORR

**Assembly:** `ORR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## ORR (vectors, unpredicated) -- A64

### Encoding: `orr_z_zz_`  — ORR

**Assembly:** `ORR  <Zd>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ORRS -- A64

### Encoding: `orrs_p_p_pp_z`  — ORRS

**Assembly:** `ORRS  <Pd>.B, <Pg>/Z, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## ORV -- A64

### Encoding: `orv_r_p_z_`  — ORV

**Assembly:** `ORV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## PACDA, PACDZA -- A64

### Encoding: `PACDA_64P_dp_1src`  [PACDA]  — PACDA

**Assembly:** `PACDA  <Xd>, <Xn|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;

var source_is_sp : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if Z == '0' then // PACDA
    if n == 31 then source_is_sp = TRUE; end;
else // PACDZA
    if n != 31 then EndOfDecode(Decode_UNDEF); end;
end;
```

### Encoding: `PACDZA_64Z_dp_1src`  [PACDZA]  — PACDZA

**Assembly:** `PACDZA  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;

var source_is_sp : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if Z == '0' then // PACDA
    if n == 31 then source_is_sp = TRUE; end;
else // PACDZA
    if n != 31 then EndOfDecode(Decode_UNDEF); end;
end;
```

---

## PACDB, PACDZB -- A64

### Encoding: `PACDB_64P_dp_1src`  [PACDB]  — PACDB

**Assembly:** `PACDB  <Xd>, <Xn|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;

var source_is_sp : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if Z == '0' then // PACDB
    if n == 31 then source_is_sp = TRUE; end;
else // PACDZB
    if n != 31 then EndOfDecode(Decode_UNDEF); end;
end;
```

### Encoding: `PACDZB_64Z_dp_1src`  [PACDZB]  — PACDZB

**Assembly:** `PACDZB  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;

var source_is_sp : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if Z == '0' then // PACDB
    if n == 31 then source_is_sp = TRUE; end;
else // PACDZB
    if n != 31 then EndOfDecode(Decode_UNDEF); end;
end;
```

---

## PACGA -- A64

### Encoding: `PACGA_64P_dp_2src`  — PACGA

**Assembly:** `PACGA  <Xd>, <Xn>, <Xm|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm\|SP>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register or stack pointer, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;

var source_is_sp : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);

if m == 31 then source_is_sp = TRUE; end;
```

---

## PACIA, PACIA1716, PACIASP, PACIAZ, PACIZA -- A64

### Encoding: `PACIA_64P_dp_1src`  [PACIA]  — PACIA

**Assembly:** `PACIA  <Xd>, <Xn|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
var source_is_sp : boolean = FALSE;
let pacia1716 : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if Z == '0' then // PACIA
    if n == 31 then source_is_sp = TRUE; end;
else // PACIZA
    if n != 31 then EndOfDecode(Decode_UNDEF); end;
end;
```

### Encoding: `PACIZA_64Z_dp_1src`  [PACIZA]  — PACIZA

**Assembly:** `PACIZA  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
var source_is_sp : boolean = FALSE;
let pacia1716 : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if Z == '0' then // PACIA
    if n == 31 then source_is_sp = TRUE; end;
else // PACIZA
    if n != 31 then EndOfDecode(Decode_UNDEF); end;
end;
```

### Encoding: `PACIA1716_HI_hints`  [PACIA1716]  — PACIA1716

**Assembly:** `PACIA1716`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var pacia1716 : boolean = FALSE;

case CRm::op2 of
    when '0011 000' => // PACIAZ
        d = 30;
        n = 31;
    when '0011 001' => // PACIASP
        d = 30;
        source_is_sp = TRUE;
        if IsFeatureImplemented(FEAT_BTI) then
            // Check for branch target compatibility between PSTATE.BTYPE
            // and implicit branch target of PACIASP instruction.
            let pacinst : PACInstType = PACIxSP;
            SetBTypeCompatible(BTypeCompatible_PAC(pacinst));
        end;
    when '0001 000' => // PACIA1716
        d = 17;
        n = 16;
        pacia1716 = TRUE;
end;
```

### Encoding: `PACIASP_HI_hints`  [PACIASP]  — PACIASP

**Assembly:** `PACIASP`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var pacia1716 : boolean = FALSE;

case CRm::op2 of
    when '0011 000' => // PACIAZ
        d = 30;
        n = 31;
    when '0011 001' => // PACIASP
        d = 30;
        source_is_sp = TRUE;
        if IsFeatureImplemented(FEAT_BTI) then
            // Check for branch target compatibility between PSTATE.BTYPE
            // and implicit branch target of PACIASP instruction.
            let pacinst : PACInstType = PACIxSP;
            SetBTypeCompatible(BTypeCompatible_PAC(pacinst));
        end;
    when '0001 000' => // PACIA1716
        d = 17;
        n = 16;
        pacia1716 = TRUE;
end;
```

### Encoding: `PACIAZ_HI_hints`  [PACIAZ]  — PACIAZ

**Assembly:** `PACIAZ`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var pacia1716 : boolean = FALSE;

case CRm::op2 of
    when '0011 000' => // PACIAZ
        d = 30;
        n = 31;
    when '0011 001' => // PACIASP
        d = 30;
        source_is_sp = TRUE;
        if IsFeatureImplemented(FEAT_BTI) then
            // Check for branch target compatibility between PSTATE.BTYPE
            // and implicit branch target of PACIASP instruction.
            let pacinst : PACInstType = PACIxSP;
            SetBTypeCompatible(BTypeCompatible_PAC(pacinst));
        end;
    when '0001 000' => // PACIA1716
        d = 17;
        n = 16;
        pacia1716 = TRUE;
end;
```

---

## PACIA171615 -- A64

### Encoding: `PACIA171615_64LR_dp_1src`  — PACIA171615

**Assembly:** `PACIA171615`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;
```

---

## PACIASPPC -- A64

### Encoding: `PACIASPPC_64LR_dp_1src`  — PACIASPPC

**Assembly:** `PACIASPPC`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let d : integer = 30;
if IsFeatureImplemented(FEAT_BTI) then
    // Check for branch target compatibility between PSTATE.BTYPE
    // and implicit branch target of PACIxSPPC instruction.
    let pacinst : PACInstType = PACIxSPPC;
    SetBTypeCompatible(BTypeCompatible_PAC(pacinst));
end;
```

---

## PACIB, PACIB1716, PACIBSP, PACIBZ, PACIZB -- A64

### Encoding: `PACIB_64P_dp_1src`  [PACIB]  — PACIB

**Assembly:** `PACIB  <Xd>, <Xn|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
var source_is_sp : boolean = FALSE;
let pacib1716 : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if Z == '0' then // PACIB
    if n == 31 then source_is_sp = TRUE; end;
else // PACIZB
    if n != 31 then EndOfDecode(Decode_UNDEF); end;
end;
```

### Encoding: `PACIZB_64Z_dp_1src`  [PACIZB]  — PACIZB

**Assembly:** `PACIZB  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
var source_is_sp : boolean = FALSE;
let pacib1716 : boolean = FALSE;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if Z == '0' then // PACIB
    if n == 31 then source_is_sp = TRUE; end;
else // PACIZB
    if n != 31 then EndOfDecode(Decode_UNDEF); end;
end;
```

### Encoding: `PACIB1716_HI_hints`  [PACIB1716]  — PACIB1716

**Assembly:** `PACIB1716`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var pacib1716 : boolean = FALSE;

case CRm::op2 of
    when '0011 010' => // PACIBZ
        d = 30;
        n = 31;
    when '0011 011' => // PACIBSP
        d = 30;
        source_is_sp = TRUE;
        if IsFeatureImplemented(FEAT_BTI) then
            // Check for branch target compatibility between PSTATE.BTYPE
            // and implicit branch target of PACIBSP instruction.
            let pacinst : PACInstType = PACIxSP;
            SetBTypeCompatible(BTypeCompatible_PAC(pacinst));
        end;
    when '0001 010' => // PACIB1716
        d = 17;
        n = 16;
        pacib1716 = TRUE;
end;
```

### Encoding: `PACIBSP_HI_hints`  [PACIBSP]  — PACIBSP

**Assembly:** `PACIBSP`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var pacib1716 : boolean = FALSE;

case CRm::op2 of
    when '0011 010' => // PACIBZ
        d = 30;
        n = 31;
    when '0011 011' => // PACIBSP
        d = 30;
        source_is_sp = TRUE;
        if IsFeatureImplemented(FEAT_BTI) then
            // Check for branch target compatibility between PSTATE.BTYPE
            // and implicit branch target of PACIBSP instruction.
            let pacinst : PACInstType = PACIxSP;
            SetBTypeCompatible(BTypeCompatible_PAC(pacinst));
        end;
    when '0001 010' => // PACIB1716
        d = 17;
        n = 16;
        pacib1716 = TRUE;
end;
```

### Encoding: `PACIBZ_HI_hints`  [PACIBZ]  — PACIBZ

**Assembly:** `PACIBZ`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
var d : integer;
var n : integer;
var source_is_sp : boolean = FALSE;
var pacib1716 : boolean = FALSE;

case CRm::op2 of
    when '0011 010' => // PACIBZ
        d = 30;
        n = 31;
    when '0011 011' => // PACIBSP
        d = 30;
        source_is_sp = TRUE;
        if IsFeatureImplemented(FEAT_BTI) then
            // Check for branch target compatibility between PSTATE.BTYPE
            // and implicit branch target of PACIBSP instruction.
            let pacinst : PACInstType = PACIxSP;
            SetBTypeCompatible(BTypeCompatible_PAC(pacinst));
        end;
    when '0001 010' => // PACIB1716
        d = 17;
        n = 16;
        pacib1716 = TRUE;
end;
```

---

## PACIB171615 -- A64

### Encoding: `PACIB171615_64LR_dp_1src`  — PACIB171615

**Assembly:** `PACIB171615`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;
```

---

## PACIBSPPC -- A64

### Encoding: `PACIBSPPC_64LR_dp_1src`  — PACIBSPPC

**Assembly:** `PACIBSPPC`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let d : integer = 30;
if IsFeatureImplemented(FEAT_BTI) then
    // Check for branch target compatibility between PSTATE.BTYPE
    // and implicit branch target of PACIxSPPC instruction.
    let pacinst : PACInstType = PACIxSPPC;
    SetBTypeCompatible(BTypeCompatible_PAC(pacinst));
end;
```

---

## PACM -- A64

### Encoding: `PACM_HI_hints`  — PACM

**Assembly:** `PACM`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_NOP); end;
```

---

## PACNBIASPPC -- A64

### Encoding: `PACNBIASPPC_64LR_dp_1src`  — PACNBIASPPC

**Assembly:** `PACNBIASPPC`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let d : integer = 30;
```

---

## PACNBIBSPPC -- A64

### Encoding: `PACNBIBSPPC_64LR_dp_1src`  — PACNBIBSPPC

**Assembly:** `PACNBIBSPPC`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let d : integer = 30;
```

---

## PEXT (predicate pair) -- A64

### Encoding: `pext_pp_rr_`  — PEXT

**Assembly:** `PEXT  { <Pd1>.<T>, <Pd2>.<T> }, <PNn>[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd1>` | Pd (4b) | Is the name of the first destination scalable predicate register, encoded in the "Pd" field. | let d0 : integer = UInt(Pd);<br>let d1 : integer = (UInt(Pd) + 1) MOD 16; |
| `<T>` | — |  |  |
| `<Pd2>` | Pd (4b) | Is the name of the second destination scalable predicate register, encoded in the "Pd" field. | let d0 : integer = UInt(Pd);<br>let d1 : integer = (UInt(Pd) + 1) MOD 16; |
| `<PNn>` | PNn (3b) | Is the name of the first source scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the | let n : integer = UInt('1'::PNn); |
| `<imm>` | i1 (1b) | Is the portion index, in the range 0 to 1, encoded in the "i1" field. | let part : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt('1'::PNn);
let d0 : integer = UInt(Pd);
let d1 : integer = (UInt(Pd) + 1) MOD 16;
let part : integer = UInt(i1);
```

---

## PEXT (predicate) -- A64

### Encoding: `pext_pn_rr_`  — PEXT

**Assembly:** `PEXT  <Pd>.<T>, <PNn>[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<PNn>` | PNn (3b) | Is the name of the first source scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the | let n : integer = UInt('1'::PNn); |
| `<imm>` | imm2 (2b) | Is the portion index, in the range 0 to 3, encoded in the "imm2" field. | let part : integer = UInt(imm2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt('1'::PNn);
let d : integer = UInt(Pd);
let part : integer = UInt(imm2);
```

---

## PFALSE -- A64

### Encoding: `pfalse_p_`  — PFALSE

**Assembly:** `PFALSE  <Pd>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer = UInt(Pd);
```

---

## PFIRST -- A64

### Encoding: `pfirst_p_p_p_`  — PFIRST

**Assembly:** `PFIRST  <Pdn>.B, <Pg>, <Pdn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pdn>` | Pdn (4b) | Is the name of the source and destination scalable predicate register, encoded in the "Pdn" field. | let dn : integer = UInt(Pdn); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let dn : integer = UInt(Pdn);
```

---

## PMLAL -- A64

### Encoding: `pmlal_mz_zzzw_1x2`  — PMLAL

**Assembly:** `PMLAL  { <Zda1>.Q-<Zda2>.Q }, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda1>` | Zda (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zda" times 2. | let da : integer = UInt(Zda::'0'); |
| `<Zda2>` | Zda (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zda" times 2 plus  | let da : integer = UInt(Zda::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda::'0');
```

---

## PMOV (to predicate) -- A64

### Encoding: `pmov_p_zi_b`  — PMOV

**Assembly:** `PMOV  <Pd>.B, <Zn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let esize : integer{} = 8;
let imm : integer = 0;
```

### Encoding: `pmov_p_zi_d`  — PMOV

**Assembly:** `PMOV  <Pd>.D, <Zn>{[<imm>]}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | (i3h :: i3l) | For the "Doubleword" variant: is the optional portion index, in the range 0 to 7, defaulting to 0, encoded in the "i3h:i |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let esize : integer{} = 64;
let imm : integer = UInt(i3h::i3l);
```

### Encoding: `pmov_p_zi_h`  — PMOV

**Assembly:** `PMOV  <Pd>.H, <Zn>{[<imm>]}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | i1 (1b) | For the "Halfword" variant: is the optional portion index, in the range 0 to 1, defaulting to 0, encoded in the "i1" fie | let imm : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let esize : integer{} = 16;
let imm : integer = UInt(i1);
```

### Encoding: `pmov_p_zi_s`  — PMOV

**Assembly:** `PMOV  <Pd>.S, <Zn>{[<imm>]}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | i2 (2b) | For the "Word" variant: is the optional portion index, in the range 0 to 3, defaulting to 0, encoded in the "i2" field. | let imm : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let d : integer = UInt(Pd);
let esize : integer{} = 32;
let imm : integer = UInt(i2);
```

---

## PMOV (to vector) -- A64

### Encoding: `pmov_z_pi_b`  — PMOV

**Assembly:** `PMOV  <Zd>, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Pn);
let d : integer = UInt(Zd);
let esize : integer{} = 8;
let imm : integer = 0;
```

### Encoding: `pmov_z_pi_d`  — PMOV

**Assembly:** `PMOV  <Zd>{[<imm>]}, <Pn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<imm>` | (i3h :: i3l) | For the "Doubleword" variant: is the optional portion index, in the range 0 to 7, defaulting to 0, encoded in the "i3h:i |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Pn);
let d : integer = UInt(Zd);
let esize : integer{} = 64;
let imm : integer = UInt(i3h::i3l);
```

### Encoding: `pmov_z_pi_h`  — PMOV

**Assembly:** `PMOV  <Zd>{[<imm>]}, <Pn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<imm>` | i1 (1b) | For the "Halfword" variant: is the optional portion index, in the range 0 to 1, defaulting to 0, encoded in the "i1" fie | let imm : integer = UInt(i1); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Pn);
let d : integer = UInt(Zd);
let esize : integer{} = 16;
let imm : integer = UInt(i1);
```

### Encoding: `pmov_z_pi_s`  — PMOV

**Assembly:** `PMOV  <Zd>{[<imm>]}, <Pn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<imm>` | i2 (2b) | For the "Word" variant: is the optional portion index, in the range 0 to 3, defaulting to 0, encoded in the "i2" field. | let imm : integer = UInt(i2); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Pn);
let d : integer = UInt(Zd);
let esize : integer{} = 32;
let imm : integer = UInt(i2);
```

---

## PMUL -- A64

### Encoding: `PMUL_asimdsame_only`  — PMUL

**Assembly:** `PMUL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if U == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## PMUL -- A64

### Encoding: `pmul_z_zz_`  — PMUL

**Assembly:** `PMUL  <Zd>.B, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## PMULL -- A64

### Encoding: `pmull_mz_zzw_1x2`  — PMULL

**Assembly:** `PMULL  { <Zd1>.Q-<Zd2>.Q }, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_AES2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'0');
```

---

## PMULL, PMULL2 -- A64

### Encoding: `PMULL_asimddiff_L`  — PMULL

**Assembly:** `PMULL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '01' || size == '10' then EndOfDecode(Decode_UNDEF); end;
if size == '11' && !IsFeatureImplemented(FEAT_PMULL) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## PMULLB -- A64

### Encoding: `pmullb_z_zz_`  — PMULLB

**Assembly:** `PMULLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size[0] == '0' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `pmullb_z_zz_q`  — PMULLB

**Assembly:** `PMULLB  <Zd>.Q, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_PMULL128) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## PMULLT -- A64

### Encoding: `pmullt_z_zz_`  — PMULLT

**Assembly:** `PMULLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size[0] == '0' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `pmullt_z_zz_q`  — PMULLT

**Assembly:** `PMULLT  <Zd>.Q, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_PMULL128) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## PNEXT -- A64

### Encoding: `pnext_p_p_p_`  — PNEXT

**Assembly:** `PNEXT  <Pdn>.<T>, <Pv>, <Pdn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pdn>` | Pdn (4b) | Is the name of the first source and destination scalable predicate register, encoded in the "Pdn" field. | let dn : integer = UInt(Pdn); |
| `<T>` | — |  |  |
| `<Pv>` | Pv (4b) | Is the name of the vector select predicate register, encoded in the "Pv" field. | let v : integer = UInt(Pv); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let v : integer = UInt(Pv);
let dn : integer = UInt(Pdn);
```

---

## PRFB (scalar plus immediate) -- A64

### Encoding: `prfb_i_p_bi_s`  — PRFB

**Assembly:** `PRFB  <prfop>, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional signed immediate vector offset, in the range -32 to 31, defaulting to 0, encoded in the "imm6" field. | let offset : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 0;
let offset : integer = SInt(imm6);
```

---

## PRFB (scalar plus scalar) -- A64

### Encoding: `prfb_i_p_br_s`  — PRFB

**Assembly:** `PRFB  <prfop>, <Pg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 0;
```

---

## PRFB (scalar plus vector) -- A64

### Encoding: `prfb_i_p_bz_s_x32_scaled`  — PRFB

**Assembly:** `PRFB  <prfop>, <Pg>, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = (xs == '0');
let scale : integer = 0;
```

### Encoding: `prfb_i_p_bz_d_x32_scaled`  — PRFB

**Assembly:** `PRFB  <prfop>, <Pg>, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = (xs == '0');
let scale : integer = 0;
```

### Encoding: `prfb_i_p_bz_d_64_scaled`  — PRFB

**Assembly:** `PRFB  <prfop>, <Pg>, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## PRFB (vector plus immediate) -- A64

### Encoding: `prfb_i_p_ai_s`  — PRFB

**Assembly:** `PRFB  <prfop>, <Pg>, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 0;
let offset : integer = UInt(imm5);
```

### Encoding: `prfb_i_p_ai_d`  — PRFB

**Assembly:** `PRFB  <prfop>, <Pg>, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 0;
let offset : integer = UInt(imm5);
```

---

## PRFD (scalar plus immediate) -- A64

### Encoding: `prfd_i_p_bi_s`  — PRFD

**Assembly:** `PRFD  <prfop>, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional signed immediate vector offset, in the range -32 to 31, defaulting to 0, encoded in the "imm6" field. | let offset : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 3;
let offset : integer = SInt(imm6);
```

---

## PRFD (scalar plus scalar) -- A64

### Encoding: `prfd_i_p_br_s`  — PRFD

**Assembly:** `PRFD  <prfop>, <Pg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 3;
```

---

## PRFD (scalar plus vector) -- A64

### Encoding: `prfd_i_p_bz_s_x32_scaled`  — PRFD

**Assembly:** `PRFD  <prfop>, <Pg>, [<Xn|SP>, <Zm>.S, <mod> #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = (xs == '0');
let scale : integer = 3;
```

### Encoding: `prfd_i_p_bz_d_x32_scaled`  — PRFD

**Assembly:** `PRFD  <prfop>, <Pg>, [<Xn|SP>, <Zm>.D, <mod> #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = (xs == '0');
let scale : integer = 3;
```

### Encoding: `prfd_i_p_bz_d_64_scaled`  — PRFD

**Assembly:** `PRFD  <prfop>, <Pg>, [<Xn|SP>, <Zm>.D, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 3;
```

---

## PRFD (vector plus immediate) -- A64

### Encoding: `prfd_i_p_ai_s`  — PRFD

**Assembly:** `PRFD  <prfop>, <Pg>, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 8 in the range 0 to 248, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 3;
let offset : integer = UInt(imm5);
```

### Encoding: `prfd_i_p_ai_d`  — PRFD

**Assembly:** `PRFD  <prfop>, <Pg>, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 8 in the range 0 to 248, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 3;
let offset : integer = UInt(imm5);
```

---

## PRFH (scalar plus immediate) -- A64

### Encoding: `prfh_i_p_bi_s`  — PRFH

**Assembly:** `PRFH  <prfop>, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional signed immediate vector offset, in the range -32 to 31, defaulting to 0, encoded in the "imm6" field. | let offset : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 1;
let offset : integer = SInt(imm6);
```

---

## PRFH (scalar plus scalar) -- A64

### Encoding: `prfh_i_p_br_s`  — PRFH

**Assembly:** `PRFH  <prfop>, <Pg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 1;
```

---

## PRFH (scalar plus vector) -- A64

### Encoding: `prfh_i_p_bz_s_x32_scaled`  — PRFH

**Assembly:** `PRFH  <prfop>, <Pg>, [<Xn|SP>, <Zm>.S, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = (xs == '0');
let scale : integer = 1;
```

### Encoding: `prfh_i_p_bz_d_x32_scaled`  — PRFH

**Assembly:** `PRFH  <prfop>, <Pg>, [<Xn|SP>, <Zm>.D, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = (xs == '0');
let scale : integer = 1;
```

### Encoding: `prfh_i_p_bz_d_64_scaled`  — PRFH

**Assembly:** `PRFH  <prfop>, <Pg>, [<Xn|SP>, <Zm>.D, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 1;
```

---

## PRFH (vector plus immediate) -- A64

### Encoding: `prfh_i_p_ai_s`  — PRFH

**Assembly:** `PRFH  <prfop>, <Pg>, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 1;
let offset : integer = UInt(imm5);
```

### Encoding: `prfh_i_p_ai_d`  — PRFH

**Assembly:** `PRFH  <prfop>, <Pg>, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 1;
let offset : integer = UInt(imm5);
```

---

## PRFM (immediate) -- A64

### Encoding: `PRFM_P_ldst_pos`  — PRFM

**Assembly:** `PRFM  (<prfop>|#<imm5>), [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<imm5>` | Rt (5b) | Is the prefetch operation encoding as an immediate, in the range 0 to 31, encoded in the "Rt" field.           This synt | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, a multiple of 8 in the range 0 to 32760, defaulting to 0 and encoded in  | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 3); |

**Pseudocode (Decode):**
```
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 3);
let n : integer = UInt(Rn);
let t : integer = UInt(Rt);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = FALSE;
```

---

## PRFM (literal) -- A64

### Encoding: `PRFM_P_loadlit`  — PRFM

**Assembly:** `PRFM  (<prfop>|#<imm5>), <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<imm5>` | Rt (5b) | Is the prefetch operation encoding as an immediate, in the range 0 to 31, encoded in the "Rt" field.           This synt | let t : integer = UInt(Rt); |
| `<label>` | imm19 (19b) | Is the program label from which the data is to be loaded. Its offset from the address of this instruction, in the range  | let offset : bits(64) = SignExtend{}(imm19::'00'); |

**Pseudocode (Decode):**
```
let t : integer = UInt(Rt);

let offset : bits(64) = SignExtend{}(imm19::'00');
```

---

## PRFM (register) -- A64

### Encoding: `PRFM_P_ldst_regoff`  — PRFM

**Assembly:** `PRFM  (<prfop>|#<imm5>), [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<imm5>` | Rt | Is the prefetch operation encoding as an immediate, in the range 0 to 31, encoded in the "Rt" field.           This synt | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then 3 else 0;
let n : integer = UInt(Rn);
let t : integer = UInt(Rt);
let m : integer = UInt(Rm);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = FALSE;
```

---

## PRFUM -- A64

### Encoding: `PRFUM_P_ldst_unscaled`  — PRFUM

**Assembly:** `PRFUM  (<prfop>|#<imm5>), [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<imm5>` | Rt (5b) | Is the prefetch operation encoding as an immediate, in the range 0 to 31, encoded in the "Rt" field.           This synt | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
let n : integer = UInt(Rn);
let t : integer = UInt(Rt);
let nontemporal : boolean = FALSE;
let tagchecked : boolean = FALSE;
```

---

## PRFW (scalar plus immediate) -- A64

### Encoding: `prfw_i_p_bi_s`  — PRFW

**Assembly:** `PRFW  <prfop>, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm6 (6b) | Is the optional signed immediate vector offset, in the range -32 to 31, defaulting to 0, encoded in the "imm6" field. | let offset : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 2;
let offset : integer = SInt(imm6);
```

---

## PRFW (scalar plus scalar) -- A64

### Encoding: `prfw_i_p_br_s`  — PRFW

**Assembly:** `PRFW  <prfop>, <Pg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 2;
```

---

## PRFW (scalar plus vector) -- A64

### Encoding: `prfw_i_p_bz_s_x32_scaled`  — PRFW

**Assembly:** `PRFW  <prfop>, <Pg>, [<Xn|SP>, <Zm>.S, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = (xs == '0');
let scale : integer = 2;
```

### Encoding: `prfw_i_p_bz_d_x32_scaled`  — PRFW

**Assembly:** `PRFW  <prfop>, <Pg>, [<Xn|SP>, <Zm>.D, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = (xs == '0');
let scale : integer = 2;
```

### Encoding: `prfw_i_p_bz_d_64_scaled`  — PRFW

**Assembly:** `PRFW  <prfop>, <Pg>, [<Xn|SP>, <Zm>.D, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 2;
```

---

## PRFW (vector plus immediate) -- A64

### Encoding: `prfw_i_p_ai_s`  — PRFW

**Assembly:** `PRFW  <prfop>, <Pg>, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 2;
let offset : integer = UInt(imm5);
```

### Encoding: `prfw_i_p_ai_d`  — PRFW

**Assembly:** `PRFW  <prfop>, <Pg>, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<prfop>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let level : integer = UInt(prfop[2:1]);
let stream : boolean = (prfop[0] == '1');
let pref_hint : PrefetchHint = if prfop[3] == '0' then Prefetch_READ else Prefetch_WRITE;
let scale : integer = 2;
let offset : integer = UInt(imm5);
```

---

## PSB -- A64

### Encoding: `PSB_HC_hints`  — PSB

**Assembly:** `PSB  CSYNC`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SPE) then EndOfDecode(Decode_NOP); end;
```

---

## PSEL -- A64

### Encoding: `psel_p_ppi_`  — PSEL

**Assembly:** `PSEL  <Pd>, <Pn>, <Pm>.<T>[<Wv>, <imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W12-W15, encoded in the "Rv" field. | let v : integer = UInt('011'::Rv); |
| `<imm>` | (i1 :: tszh :: tszl) | Is the element index, in the range 0 to one less than the number of vector elements in a 128-bit vector register, encode |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let imm5 : bits(5) = i1::tszh::tszl;
var esize : integer;
var imm : integer;
case tszh::tszl of
    when '0000' => EndOfDecode(Decode_UNDEF);
    when '1000' => esize = 64;  imm = UInt(imm5[4]);
    when 'x100' => esize = 32;  imm = UInt(imm5[4:3]);
    when 'xx10' => esize = 16;  imm = UInt(imm5[4:2]);
    when 'xxx1' => esize = 8;   imm = UInt(imm5[4:1]);
end;
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let v : integer = UInt('011'::Rv);
```

---

## PSSBB -- A64 *(alias for DSB)*

### Encoding: `PSSBB_DSB_BO_barriers`  — DSB

**Assembly:** `PSSBB / DSB  #4`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `DSB` | — |  |  |

---

## PTEST -- A64

### Encoding: `ptest__p_p_`  — PTEST

**Assembly:** `PTEST  <Pg>, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
```

---

## PTRUE (predicate as counter) -- A64

### Encoding: `ptrue_pn_i_`  — PTRUE

**Assembly:** `PTRUE  <PNd>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<PNd>` | PNd (3b) | Is the name of the destination scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the  | let d : integer = UInt('1'::PNd); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt('1'::PNd);
```

---

## PTRUE (predicate) -- A64

### Encoding: `ptrue_p_s_`  — PTRUE

**Assembly:** `PTRUE  <Pd>.<T>{, <pattern>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<pattern>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
let pat : bits(5) = pattern;
```

---

## PTRUES -- A64

### Encoding: `ptrues_p_s_`  — PTRUES

**Assembly:** `PTRUES  <Pd>.<T>{, <pattern>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<pattern>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
let pat : bits(5) = pattern;
```

---

## PUNPKHI, PUNPKLO -- A64

### Encoding: `punpkhi_p_p_`  — PUNPKHI

**Assembly:** `PUNPKHI  <Pd>.H, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Pn);
let d : integer = UInt(Pd);
let hi : boolean = TRUE;
```

### Encoding: `punpklo_p_p_`  — PUNPKLO

**Assembly:** `PUNPKLO  <Pd>.H, <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Pn);
let d : integer = UInt(Pd);
let hi : boolean = FALSE;
```

---

## RADDHN, RADDHN2 -- A64

### Encoding: `RADDHN_asimddiff_N`  — RADDHN

**Assembly:** `RADDHN{2}  <Vd>.<Tb>, <Vn>.<Ta>, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
let round : boolean = TRUE;
```

---

## RADDHNB -- A64

### Encoding: `raddhnb_z_zz_`  — RADDHNB

**Assembly:** `RADDHNB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## RADDHNT -- A64

### Encoding: `raddhnt_z_zz_`  — RADDHNT

**Assembly:** `RADDHNT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## RAX1 -- A64

### Encoding: `RAX1_VVV2_cryptosha512_3`  — RAX1

**Assembly:** `RAX1  <Vd>.2D, <Vn>.2D, <Vm>.2D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## RAX1 -- A64

### Encoding: `rax1_z_zz_`  — RAX1

**Assembly:** `RAX1  <Zd>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_SHA3) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## RBIT (vector) -- A64

### Encoding: `RBIT_asimdmisc_R`  — RBIT

**Assembly:** `RBIT  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
```

---

## RBIT -- A64

### Encoding: `RBIT_32_dp_1src`  [32-bit]  — RBIT

**Assembly:** `RBIT  <Wd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `RBIT_64_dp_1src`  [64-bit]  — RBIT

**Assembly:** `RBIT  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
```

---

## RBIT -- A64

### Encoding: `rbit_z_p_z_m`  — RBIT

**Assembly:** `RBIT  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `rbit_z_p_z_z`  — RBIT

**Assembly:** `RBIT  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## RCWCAS, RCWCASA, RCWCASAL, RCWCASL -- A64

### Encoding: `RCWCAS_C64_rcwcomswap`  [RCWCAS]  — RCWCAS

**Assembly:** `RCWCAS  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWCASA_C64_rcwcomswap`  [RCWCASA]  — RCWCASA

**Assembly:** `RCWCASA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWCASAL_C64_rcwcomswap`  [RCWCASAL]  — RCWCASAL

**Assembly:** `RCWCASAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWCASL_C64_rcwcomswap`  [RCWCASL]  — RCWCASL

**Assembly:** `RCWCASL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## RCWCASP, RCWCASPA, RCWCASPAL, RCWCASPL -- A64

### Encoding: `RCWCASP_C64_rcwcomswappr`  [RCWCASP]  — RCWCASP

**Assembly:** `RCWCASP  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let soft : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWCASPA_C64_rcwcomswappr`  [RCWCASPA]  — RCWCASPA

**Assembly:** `RCWCASPA  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let soft : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWCASPAL_C64_rcwcomswappr`  [RCWCASPAL]  — RCWCASPAL

**Assembly:** `RCWCASPAL  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let soft : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWCASPL_C64_rcwcomswappr`  [RCWCASPL]  — RCWCASPL

**Assembly:** `RCWCASPL  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let soft : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## RCWCLR, RCWCLRA, RCWCLRAL, RCWCLRL -- A64

### Encoding: `RCWCLR_64_memop`  [RCWCLR]  — RCWCLR

**Assembly:** `RCWCLR  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWCLRA_64_memop`  [RCWCLRA]  — RCWCLRA

**Assembly:** `RCWCLRA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWCLRAL_64_memop`  [RCWCLRAL]  — RCWCLRAL

**Assembly:** `RCWCLRAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWCLRL_64_memop`  [RCWCLRL]  — RCWCLRL

**Assembly:** `RCWCLRL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## RCWCLRP, RCWCLRPA, RCWCLRPAL, RCWCLRPL -- A64

### Encoding: `RCWCLRP_128_memop_128`  [RCWCLRP]  — RCWCLRP

**Assembly:** `RCWCLRP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWCLRPA_128_memop_128`  [RCWCLRPA]  — RCWCLRPA

**Assembly:** `RCWCLRPA  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWCLRPAL_128_memop_128`  [RCWCLRPAL]  — RCWCLRPAL

**Assembly:** `RCWCLRPAL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWCLRPL_128_memop_128`  [RCWCLRPL]  — RCWCLRPL

**Assembly:** `RCWCLRPL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

---

## RCWSCAS, RCWSCASA, RCWSCASAL, RCWSCASL -- A64

### Encoding: `RCWSCAS_C64_rcwcomswap`  [RCWSCAS]  — RCWSCAS

**Assembly:** `RCWSCAS  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSCASA_C64_rcwcomswap`  [RCWSCASA]  — RCWSCASA

**Assembly:** `RCWSCASA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSCASAL_C64_rcwcomswap`  [RCWSCASAL]  — RCWSCASAL

**Assembly:** `RCWSCASAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSCASL_C64_rcwcomswap`  [RCWSCASL]  — RCWSCASL

**Assembly:** `RCWSCASL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be compared and loaded, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be conditionally stored, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## RCWSCASP, RCWSCASPA, RCWSCASPAL, RCWSCASPL -- A64

### Encoding: `RCWSCASP_C64_rcwcomswappr`  [RCWSCASP]  — RCWSCASP

**Assembly:** `RCWSCASP  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let soft : boolean = TRUE;
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSCASPA_C64_rcwcomswappr`  [RCWSCASPA]  — RCWSCASPA

**Assembly:** `RCWSCASPA  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let soft : boolean = TRUE;
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSCASPAL_C64_rcwcomswappr`  [RCWSCASPAL]  — RCWSCASPAL

**Assembly:** `RCWSCASPAL  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let soft : boolean = TRUE;
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSCASPL_C64_rcwcomswappr`  [RCWSCASPL]  — RCWSCASPL

**Assembly:** `RCWSCASPL  <Xs>, <X(s+1)>, <Xt>, <X(t+1)>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the first general-purpose register to be compared and loaded, encoded in the "Rs" field. <Xs> must | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<X(s+1)>` | Rs (5b) | Is the 64-bit name of the second general-purpose register to be compared and loaded. | if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be conditionally stored, encoded in the "Rt" field. <Xt> mus | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<X(t+1)>` | Rt (5b) | Is the 64-bit name of the second general-purpose register to be conditionally stored. | if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rs[0] == '1' then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let soft : boolean = TRUE;
let tagchecked : boolean = n != 31;
```

---

## RCWSCLR, RCWSCLRA, RCWSCLRAL, RCWSCLRL -- A64

### Encoding: `RCWSCLR_64_memop`  [RCWSCLR]  — RCWSCLR

**Assembly:** `RCWSCLR  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSCLRA_64_memop`  [RCWSCLRA]  — RCWSCLRA

**Assembly:** `RCWSCLRA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSCLRAL_64_memop`  [RCWSCLRAL]  — RCWSCLRAL

**Assembly:** `RCWSCLRAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSCLRL_64_memop`  [RCWSCLRL]  — RCWSCLRL

**Assembly:** `RCWSCLRL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## RCWSCLRP, RCWSCLRPA, RCWSCLRPAL, RCWSCLRPL -- A64

### Encoding: `RCWSCLRP_128_memop_128`  [RCWSCLRP]  — RCWSCLRP

**Assembly:** `RCWSCLRP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSCLRPA_128_memop_128`  [RCWSCLRPA]  — RCWSCLRPA

**Assembly:** `RCWSCLRPA  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSCLRPAL_128_memop_128`  [RCWSCLRPAL]  — RCWSCLRPAL

**Assembly:** `RCWSCLRPAL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSCLRPL_128_memop_128`  [RCWSCLRPL]  — RCWSCLRPL

**Assembly:** `RCWSCLRPL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

---

## RCWSET, RCWSETA, RCWSETAL, RCWSETL -- A64

### Encoding: `RCWSET_64_memop`  [RCWSET]  — RCWSET

**Assembly:** `RCWSET  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSETA_64_memop`  [RCWSETA]  — RCWSETA

**Assembly:** `RCWSETA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSETAL_64_memop`  [RCWSETAL]  — RCWSETAL

**Assembly:** `RCWSETAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSETL_64_memop`  [RCWSETL]  — RCWSETL

**Assembly:** `RCWSETL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## RCWSETP, RCWSETPA, RCWSETPAL, RCWSETPL -- A64

### Encoding: `RCWSETP_128_memop_128`  [RCWSETP]  — RCWSETP

**Assembly:** `RCWSETP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSETPA_128_memop_128`  [RCWSETPA]  — RCWSETPA

**Assembly:** `RCWSETPA  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSETPAL_128_memop_128`  [RCWSETPAL]  — RCWSETPAL

**Assembly:** `RCWSETPAL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSETPL_128_memop_128`  [RCWSETPL]  — RCWSETPL

**Assembly:** `RCWSETPL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

---

## RCWSSET, RCWSSETA, RCWSSETAL, RCWSSETL -- A64

### Encoding: `RCWSSET_64_memop`  [RCWSSET]  — RCWSSET

**Assembly:** `RCWSSET  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSSETA_64_memop`  [RCWSSETA]  — RCWSSETA

**Assembly:** `RCWSSETA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSSETAL_64_memop`  [RCWSSETAL]  — RCWSSETAL

**Assembly:** `RCWSSETAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSSETL_64_memop`  [RCWSSETL]  — RCWSSETL

**Assembly:** `RCWSSETL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## RCWSSETP, RCWSSETPA, RCWSSETPAL, RCWSSETPL -- A64

### Encoding: `RCWSSETP_128_memop_128`  [RCWSSETP]  — RCWSSETP

**Assembly:** `RCWSSETP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSSETPA_128_memop_128`  [RCWSSETPA]  — RCWSSETPA

**Assembly:** `RCWSSETPA  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSSETPAL_128_memop_128`  [RCWSSETPAL]  — RCWSSETPAL

**Assembly:** `RCWSSETPAL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSSETPL_128_memop_128`  [RCWSSETPL]  — RCWSSETPL

**Assembly:** `RCWSSETPL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

---

## RCWSSWP, RCWSSWPA, RCWSSWPAL, RCWSSWPL -- A64

### Encoding: `RCWSSWP_64_memop`  [RCWSSWP]  — RCWSSWP

**Assembly:** `RCWSSWP  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSSWPA_64_memop`  [RCWSSWPA]  — RCWSSWPA

**Assembly:** `RCWSSWPA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSSWPAL_64_memop`  [RCWSSWPAL]  — RCWSSWPAL

**Assembly:** `RCWSSWPAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSSWPL_64_memop`  [RCWSSWPL]  — RCWSSWPL

**Assembly:** `RCWSSWPL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## RCWSSWPP, RCWSSWPPA, RCWSSWPPAL, RCWSSWPPL -- A64

### Encoding: `RCWSSWPP_128_memop_128`  [RCWSSWPP]  — RCWSSWPP

**Assembly:** `RCWSSWPP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSSWPPA_128_memop_128`  [RCWSSWPPA]  — RCWSSWPPA

**Assembly:** `RCWSSWPPA  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSSWPPAL_128_memop_128`  [RCWSSWPPAL]  — RCWSSWPPAL

**Assembly:** `RCWSSWPPAL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSSWPPL_128_memop_128`  [RCWSSWPPL]  — RCWSSWPPL

**Assembly:** `RCWSSWPPL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = TRUE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## RCWSWP, RCWSWPA, RCWSWPAL, RCWSWPL -- A64

### Encoding: `RCWSWP_64_memop`  [RCWSWP]  — RCWSWP

**Assembly:** `RCWSWP  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSWPA_64_memop`  [RCWSWPA]  — RCWSWPA

**Assembly:** `RCWSWPA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSWPAL_64_memop`  [RCWSWPAL]  — RCWSWPAL

**Assembly:** `RCWSWPAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `RCWSWPL_64_memop`  [RCWSWPL]  — RCWSWPL

**Assembly:** `RCWSWPL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_THE) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1' && t != 31;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## RCWSWPP, RCWSWPPA, RCWSWPPAL, RCWSWPPL -- A64

### Encoding: `RCWSWPP_128_memop_128`  [RCWSWPP]  — RCWSWPP

**Assembly:** `RCWSWPP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSWPPA_128_memop_128`  [RCWSWPPA]  — RCWSWPPA

**Assembly:** `RCWSWPPA  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSWPPAL_128_memop_128`  [RCWSWPPAL]  — RCWSWPPAL

**Assembly:** `RCWSWPPAL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `RCWSWPPL_128_memop_128`  [RCWSWPPL]  — RCWSWPPL

**Assembly:** `RCWSWPPL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_D128) || !IsFeatureImplemented(FEAT_THE) then
    EndOfDecode(Decode_UNDEF);
end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let soft : boolean = FALSE;

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## RDFFR (predicated) -- A64

### Encoding: `rdffr_p_p_f_`  — RDFFR

**Assembly:** `RDFFR  <Pd>.B, <Pg>/Z`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let d : integer = UInt(Pd);
let setflags : boolean = FALSE;
```

---

## RDFFR (unpredicated) -- A64

### Encoding: `rdffr_p_f_`  — RDFFR

**Assembly:** `RDFFR  <Pd>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Pd);
```

---

## RDFFRS -- A64

### Encoding: `rdffrs_p_p_f_`  — RDFFRS

**Assembly:** `RDFFRS  <Pd>.B, <Pg>/Z`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let g : integer = UInt(Pg);
let d : integer = UInt(Pd);
let setflags : boolean = TRUE;
```

---

## RDSVL -- A64

### Encoding: `rdsvl_r_i_`  — RDSVL

**Assembly:** `RDSVL  <Xd>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<imm>` | imm6 (6b) | Is the signed immediate operand, in the range -32 to 31, encoded in the "imm6" field. | let imm : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let imm : integer = SInt(imm6);
```

---

## RDVL -- A64

### Encoding: `rdvl_r_i_`  — RDVL

**Assembly:** `RDVL  <Xd>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<imm>` | imm6 (6b) | Is the signed immediate operand, in the range -32 to 31, encoded in the "imm6" field. | let imm : integer = SInt(imm6); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let d : integer = UInt(Rd);
let imm : integer = SInt(imm6);
```

---

## RET -- A64

### Encoding: `RET_64R_branch_reg`  — RET

**Assembly:** `RET  {<Xn>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose register holding the address to be branched to, encoded in the "Rn" field. Def | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let n : integer{} = UInt(Rn);
```

---

## RETAA, RETAB -- A64

### Encoding: `RETAA_64E_branch_reg`  [RETAA]  — RETAA

**Assembly:** `RETAA`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;

let use_key_a : boolean = (M == '0');
let auth_then_branch : boolean = TRUE;
```

### Encoding: `RETAB_64E_branch_reg`  [RETAB]  — RETAB

**Assembly:** `RETAB`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;

let use_key_a : boolean = (M == '0');
let auth_then_branch : boolean = TRUE;
```

---

## RETAASPPC, RETABSPPC -- A64

### Encoding: `RETAASPPC_only_miscbranch`  [RETAASPPC]  — RETAASPPC

**Assembly:** `RETAASPPC  <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<label>` | imm16 (16b) | Is the program label whose address is to be calculated. Its negative offset from the address of this instruction, a mult | let offset : bits(64) = ZeroExtend{}(imm16::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let use_key_a : boolean = opc[0] == '0';
let offset : bits(64) = ZeroExtend{}(imm16::'00');
let auth_then_branch : boolean = TRUE;
```

### Encoding: `RETABSPPC_only_miscbranch`  [RETABSPPC]  — RETABSPPC

**Assembly:** `RETABSPPC  <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<label>` | imm16 (16b) | Is the program label whose address is to be calculated. Its negative offset from the address of this instruction, a mult | let offset : bits(64) = ZeroExtend{}(imm16::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;

let use_key_a : boolean = opc[0] == '0';
let offset : bits(64) = ZeroExtend{}(imm16::'00');
let auth_then_branch : boolean = TRUE;
```

---

## RETAASPPCR, RETABSPPCR -- A64

### Encoding: `RETAASPPCR_64M_branch_reg`  [RETAASPPCR]  — RETAASPPCR

**Assembly:** `RETAASPPCR  <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Rm);
let use_key_a : boolean = M == '0';
let auth_then_branch : boolean = TRUE;
```

### Encoding: `RETABSPPCR_64M_branch_reg`  [RETABSPPCR]  — RETABSPPCR

**Assembly:** `RETABSPPCR  <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose source register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth_LR) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Rm);
let use_key_a : boolean = M == '0';
let auth_then_branch : boolean = TRUE;
```

---

## REV (predicate) -- A64

### Encoding: `rev_p_p_`  — REV

**Assembly:** `REV  <Pd>.<T>, <Pn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Pn);
let d : integer = UInt(Pd);
```

---

## REV (vector) -- A64

### Encoding: `rev_z_z_`  — REV

**Assembly:** `REV  <Zd>.<T>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## REV -- A64

### Encoding: `REV_32_dp_1src`  [32-bit]  — REV

**Assembly:** `REV  <Wd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if opc == '11' && sf == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let container_size : integer{} = 8 << UInt(opc);
```

### Encoding: `REV_64_dp_1src`  [64-bit]  — REV

**Assembly:** `REV  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if opc == '11' && sf == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let container_size : integer{} = 8 << UInt(opc);
```

---

## REV16 (vector) -- A64

### Encoding: `REV16_asimdmisc_R`  — REV16

**Assembly:** `REV16  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let csize : integer{} = 64 >> UInt(o0::U);
let esize : integer{} = 8 << UInt(size);
if csize <= esize then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 64 << UInt(Q);
let containers : integer = datasize DIV csize;
```

---

## REV16 -- A64

### Encoding: `REV16_32_dp_1src`  [32-bit]  — REV16

**Assembly:** `REV16  <Wd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if opc == '11' && sf == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let container_size : integer{} = 8 << UInt(opc);
```

### Encoding: `REV16_64_dp_1src`  [64-bit]  — REV16

**Assembly:** `REV16  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if opc == '11' && sf == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let container_size : integer{} = 8 << UInt(opc);
```

---

## REV32 (vector) -- A64

### Encoding: `REV32_asimdmisc_R`  — REV32

**Assembly:** `REV32  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let csize : integer{} = 64 >> UInt(o0::U);
let esize : integer{} = 8 << UInt(size);
if csize <= esize then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 64 << UInt(Q);
let containers : integer = datasize DIV csize;
```

---

## REV32 -- A64

### Encoding: `REV32_64_dp_1src`  — REV32

**Assembly:** `REV32  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if opc == '11' && sf == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let container_size : integer{} = 8 << UInt(opc);
```

---

## REV64 -- A64

### Encoding: `REV64_asimdmisc_R`  — REV64

**Assembly:** `REV64  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let csize : integer{} = 64 >> UInt(o0::U);
let esize : integer{} = 8 << UInt(size);
if csize <= esize then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 64 << UInt(Q);
let containers : integer = datasize DIV csize;
```

---

## REV64 -- A64 *(alias for REV)*

### Encoding: `REV64_REV_64_dp_1src`  — REV

**Assembly:** `REV64  <Xd>, <Xn> / REV  <Xd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `REV` | — |  |  |

---

## REVB, REVH, REVW -- A64

### Encoding: `revb_z_z_m`  — REVB

**Assembly:** `REVB  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let swsize : integer = 8;
let merging : boolean = TRUE;
```

### Encoding: `revb_z_z_z`  — REVB

**Assembly:** `REVB  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let swsize : integer = 8;
let merging : boolean = FALSE;
```

### Encoding: `revh_z_z_m`  — REVH

**Assembly:** `REVH  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let swsize : integer = 16;
let merging : boolean = TRUE;
```

### Encoding: `revh_z_z_z`  — REVH

**Assembly:** `REVH  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let swsize : integer = 16;
let merging : boolean = FALSE;
```

### Encoding: `revw_z_z_m`  — REVW

**Assembly:** `REVW  <Zd>.D, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let swsize : integer = 32;
let merging : boolean = TRUE;
```

### Encoding: `revw_z_z_z`  — REVW

**Assembly:** `REVW  <Zd>.D, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let swsize : integer = 32;
let merging : boolean = FALSE;
```

---

## REVD -- A64

### Encoding: `revd_z_p_z_m`  — REVD

**Assembly:** `REVD  <Zd>.Q, <Pg>/M, <Zn>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 128;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let swsize : integer = 64;
let merging : boolean = TRUE;
```

### Encoding: `revd_z_p_z_z`  — REVD

**Assembly:** `REVD  <Zd>.Q, <Pg>/Z, <Zn>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 128;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let swsize : integer = 64;
let merging : boolean = FALSE;
```

---

## RMIF -- A64

### Encoding: `RMIF_only_rmif`  — RMIF

**Assembly:** `RMIF  <Xn>, #<shift>, #<mask>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | imm6 (6b) | Is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | let imm : integer{} = UInt(imm6); |
| `<mask>` | mask (4b) | Is the flag bit mask, an immediate in the range 0 to 15, which selects the bits that are inserted into the NZCV conditio | let flagmask : bits(4) = mask; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FlagM) then EndOfDecode(Decode_UNDEF); end;
let imm : integer{} = UInt(imm6);
let flagmask : bits(4) = mask;
let n : integer{} = UInt(Rn);
```

---

## ROR (immediate) -- A64 *(alias for EXTR)*

### Encoding: `ROR_EXTR_32_extract`  [32-bit]  — EXTR

**Assembly:** `ROR  <Wd>, <Ws>, #<shift> / EXTR  <Wd>, <Ws>, <Ws>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Ws>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" and "Rm" fields. |  |
| `<shift>` | imms (6b) | For the "32-bit" variant: is the amount by which to rotate, in the range 0 to 31, encoded in the "imms" field. |  |
| `EXTR` | — |  |  |

### Encoding: `ROR_EXTR_64_extract`  [64-bit]  — EXTR

**Assembly:** `ROR  <Xd>, <Xs>, #<shift> / EXTR  <Xd>, <Xs>, <Xs>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xs>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" and "Rm" fields. |  |
| `<shift>` | imms (6b) | For the "64-bit" variant: is the amount by which to rotate, in the range 0 to 63, encoded in the "imms" field. |  |
| `EXTR` | — |  |  |

---

## ROR (register) -- A64 *(alias for RORV)*

### Encoding: `ROR_RORV_32_dp_2src`  [32-bit]  — RORV

**Assembly:** `ROR  <Wd>, <Wn>, <Wm> / RORV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding a shift amount from 0 to 31 in its bottom 5 bit |  |
| `RORV` | — |  |  |

### Encoding: `ROR_RORV_64_dp_2src`  [64-bit]  — RORV

**Assembly:** `ROR  <Xd>, <Xn>, <Xm> / RORV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding a shift amount from 0 to 63 in its bottom 6 bit |  |
| `RORV` | — |  |  |

---

## RORV -- A64

### Encoding: `RORV_32_dp_2src`  [32-bit]  — RORV

**Assembly:** `RORV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding a shift amount from 0 to 31 in its bottom 5 bit | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(op2);
```

### Encoding: `RORV_64_dp_2src`  [64-bit]  — RORV

**Assembly:** `RORV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding a shift amount from 0 to 63 in its bottom 6 bit | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(op2);
```

---

## RPRFM -- A64

### Encoding: `RPRFM_R_ldst_regoff`  — RPRFM

**Assembly:** `RPRFM  (<rprfop>|#<imm6>), <Xm>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<rprfop>` | — |  |  |
| `<imm6>` | (option :: S :: Rt) | Is the range prefetch operation encoding as an immediate, in the range 0 to 63, encoded in "option<2>:option<0>:S:Rt<2:0 |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose register that holds an encoding of the metadata, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RPRFM) then EndOfDecode(Decode_NOP); end;
let operation : bits(6) = option[2]::option[0]::S::Rt[2:0];
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
```

---

## RSHRN, RSHRN2 -- A64

### Encoding: `RSHRN_asimdshf_N`  — RSHRN

**Assembly:** `RSHRN{2}  <Vd>.<Tb>, <Vn>.<Ta>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = TRUE;
```

---

## RSHRNB -- A64

### Encoding: `rshrnb_z_zi_`  — RSHRNB

**Assembly:** `RSHRNB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## RSHRNT -- A64

### Encoding: `rshrnt_z_zi_`  — RSHRNT

**Assembly:** `RSHRNT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## RSUBHN, RSUBHN2 -- A64

### Encoding: `RSUBHN_asimddiff_N`  — RSUBHN

**Assembly:** `RSUBHN{2}  <Vd>.<Tb>, <Vn>.<Ta>, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
let round : boolean = TRUE;
```

---

## RSUBHNB -- A64

### Encoding: `rsubhnb_z_zz_`  — RSUBHNB

**Assembly:** `RSUBHNB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## RSUBHNT -- A64

### Encoding: `rsubhnt_z_zz_`  — RSUBHNT

**Assembly:** `RSUBHNT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SABA -- A64

### Encoding: `SABA_asimdsame_only`  — SABA

**Assembly:** `SABA  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SABA -- A64

### Encoding: `saba_z_zzz_`  — SABA

**Assembly:** `SABA  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let unsigned : boolean = FALSE;
```

---

## SABAL -- A64

### Encoding: `sabal_z_zz_`  — SABAL

**Assembly:** `SABAL  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SABAL, SABAL2 -- A64

### Encoding: `SABAL_asimddiff_L`  — SABAL

**Assembly:** `SABAL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SABALB -- A64

### Encoding: `sabalb_z_zzz_`  — SABALB

**Assembly:** `SABALB  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SABALT -- A64

### Encoding: `sabalt_z_zzz_`  — SABALT

**Assembly:** `SABALT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SABD -- A64

### Encoding: `SABD_asimdsame_only`  — SABD

**Assembly:** `SABD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SABD -- A64

### Encoding: `sabd_z_p_zz_`  — SABD

**Assembly:** `SABD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SABDL, SABDL2 -- A64

### Encoding: `SABDL_asimddiff_L`  — SABDL

**Assembly:** `SABDL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SABDLB -- A64

### Encoding: `sabdlb_z_zz_`  — SABDLB

**Assembly:** `SABDLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SABDLT -- A64

### Encoding: `sabdlt_z_zz_`  — SABDLT

**Assembly:** `SABDLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SADALP -- A64

### Encoding: `SADALP_asimdmisc_P`  — SADALP

**Assembly:** `SADALP  <Vd>.<Ta>, <Vn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV (2 * esize);
```

---

## SADALP -- A64

### Encoding: `sadalp_z_p_z_`  — SADALP

**Assembly:** `SADALP  <Zda>.<T>, <Pg>/M, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the second source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let da : integer = UInt(Zda);
```

---

## SADDL, SADDL2 -- A64

### Encoding: `SADDL_asimddiff_L`  — SADDL

**Assembly:** `SADDL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SADDLB -- A64

### Encoding: `saddlb_z_zz_`  — SADDLB

**Assembly:** `SADDLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 0;
let sel2 : integer = 0;
let unsigned : boolean = FALSE;
```

---

## SADDLBT -- A64

### Encoding: `saddlbt_z_zz_`  — SADDLBT

**Assembly:** `SADDLBT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 0;
let sel2 : integer = 1;
let unsigned : boolean = FALSE;
```

---

## SADDLP -- A64

### Encoding: `SADDLP_asimdmisc_P`  — SADDLP

**Assembly:** `SADDLP  <Vd>.<Ta>, <Vn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV (2 * esize);
```

---

## SADDLT -- A64

### Encoding: `saddlt_z_zz_`  — SADDLT

**Assembly:** `SADDLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 1;
let sel2 : integer = 1;
let unsigned : boolean = FALSE;
```

---

## SADDLV -- A64

### Encoding: `SADDLV_asimdall_only`  — SADDLV

**Assembly:** `SADDLV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '100' then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = FALSE;
```

---

## SADDV -- A64

### Encoding: `saddv_r_p_z_`  — SADDV

**Assembly:** `SADDV  <Dd>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Vd (5b) | Is the 64-bit name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## SADDW, SADDW2 -- A64

### Encoding: `SADDW_asimddiff_W`  — SADDW

**Assembly:** `SADDW{2}  <Vd>.<Ta>, <Vn>.<Ta>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SADDWB -- A64

### Encoding: `saddwb_z_zz_`  — SADDWB

**Assembly:** `SADDWB  <Zd>.<T>, <Zn>.<T>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SADDWT -- A64

### Encoding: `saddwt_z_zz_`  — SADDWT

**Assembly:** `SADDWT  <Zd>.<T>, <Zn>.<T>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SB -- A64

### Encoding: `SB_only_barriers`  — SB

**Assembly:** `SB`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SB) then EndOfDecode(Decode_UNDEF); end;
```

---

## SBC -- A64

### Encoding: `SBC_32_addsub_carry`  [32-bit]  — SBC

**Assembly:** `SBC  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `SBC_64_addsub_carry`  [64-bit]  — SBC

**Assembly:** `SBC  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## SBCLB -- A64

### Encoding: `sbclb_z_zzz_`  — SBCLB

**Assembly:** `SBCLB  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SBCLT -- A64

### Encoding: `sbclt_z_zzz_`  — SBCLT

**Assembly:** `SBCLT  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SBCS -- A64

### Encoding: `SBCS_32_addsub_carry`  [32-bit]  — SBCS

**Assembly:** `SBCS  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `SBCS_64_addsub_carry`  [64-bit]  — SBCS

**Assembly:** `SBCS  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## SBFIZ -- A64 *(alias for SBFM)*

### Encoding: `SBFIZ_SBFM_32M_bitfield`  [32-bit]  — SBFM

**Assembly:** `SBFIZ  <Wd>, <Wn>, #<lsb>, #<width> / SBFM  <Wd>, <Wn>, #(-<lsb>  MOD  32), #(<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "32-bit" variant: is the bit number of the lsb of the destination bitfield, in the range 0 to 31. |  |
| `<width>` | (imms :: immr) | For the "32-bit" variant: is the width of the bitfield, in the range 1 to 32-<lsb>. |  |
| `SBFM` | — |  |  |

### Encoding: `SBFIZ_SBFM_64M_bitfield`  [64-bit]  — SBFM

**Assembly:** `SBFIZ  <Xd>, <Xn>, #<lsb>, #<width> / SBFM  <Xd>, <Xn>, #(-<lsb>  MOD  64), #(<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "64-bit" variant: is the bit number of the lsb of the destination bitfield, in the range 0 to 63. |  |
| `<width>` | (imms :: immr) | For the "64-bit" variant: is the width of the bitfield, in the range 1 to 64-<lsb>. |  |
| `SBFM` | — |  |  |

---

## SBFM -- A64

### Encoding: `SBFM_32M_bitfield`  [32-bit]  — SBFM

**Assembly:** `SBFM  <Wd>, <Wn>, #<immr>, #<imms>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<immr>` | immr (6b) | For the "32-bit" variant: is the right rotate amount, in the range 0 to 31, encoded in the "immr" field. | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let r : integer = UInt(immr);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |
| `<imms>` | imms (6b) | For the "32-bit" variant: is the leftmost bit number to be moved from the source, in the range 0 to 31, encoded in the " | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let s : integer = UInt(imms);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |

**Pseudocode (Decode):**
```
if sf == '1' && N != '1' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && (N != '0' || immr[5] != '0' || imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let s : integer = UInt(imms);
let r : integer = UInt(immr);

var wmask : bits(datasize);
var tmask : bits(datasize);
(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE);
```

### Encoding: `SBFM_64M_bitfield`  [64-bit]  — SBFM

**Assembly:** `SBFM  <Xd>, <Xn>, #<immr>, #<imms>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<immr>` | immr (6b) | For the "64-bit" variant: is the right rotate amount, in the range 0 to 63, encoded in the "immr" field. | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let r : integer = UInt(immr);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |
| `<imms>` | imms (6b) | For the "64-bit" variant: is the leftmost bit number to be moved from the source, in the range 0 to 63, encoded in the " | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let s : integer = UInt(imms);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |

**Pseudocode (Decode):**
```
if sf == '1' && N != '1' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && (N != '0' || immr[5] != '0' || imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let s : integer = UInt(imms);
let r : integer = UInt(immr);

var wmask : bits(datasize);
var tmask : bits(datasize);
(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE);
```

---

## SBFX -- A64 *(alias for SBFM)*

### Encoding: `SBFX_SBFM_32M_bitfield`  [32-bit]  — SBFM

**Assembly:** `SBFX  <Wd>, <Wn>, #<lsb>, #<width> / SBFM  <Wd>, <Wn>, #<lsb>, #(<lsb>+<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "32-bit" variant: is the bit number of the lsb of the source bitfield, in the range 0 to 31. |  |
| `<width>` | (imms :: immr) | For the "32-bit" variant: is the width of the bitfield, in the range 1 to 32-<lsb>. |  |
| `SBFM` | — |  |  |

### Encoding: `SBFX_SBFM_64M_bitfield`  [64-bit]  — SBFM

**Assembly:** `SBFX  <Xd>, <Xn>, #<lsb>, #<width> / SBFM  <Xd>, <Xn>, #<lsb>, #(<lsb>+<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "64-bit" variant: is the bit number of the lsb of the source bitfield, in the range 0 to 63. |  |
| `<width>` | (imms :: immr) | For the "64-bit" variant: is the width of the bitfield, in the range 1 to 64-<lsb>. |  |
| `SBFM` | — |  |  |

---

## SCLAMP -- A64

### Encoding: `sclamp_mz_zz_2`  — SCLAMP

**Assembly:** `SCLAMP  { <Zd1>.<T>-<Zd2>.<T> }, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
```

### Encoding: `sclamp_mz_zz_4`  — SCLAMP

**Assembly:** `SCLAMP  { <Zd1>.<T>-<Zd4>.<T> }, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
```

---

## SCLAMP -- A64

### Encoding: `sclamp_z_zz_`  — SCLAMP

**Assembly:** `SCLAMP  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SCVTF (bottom, unpredicated) -- A64

### Encoding: `scvtf_z_z_`  — SCVTF

**Assembly:** `SCVTF  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
```

---

## SCVTF (predicated) -- A64

### Encoding: `scvtf_z_p_z_h2fp16`  — SCVTF

**Assembly:** `SCVTF  <Zd>.H, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 16;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `scvtf_z_p_z_h2fp16z`  — SCVTF

**Assembly:** `SCVTF  <Zd>.H, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 16;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `scvtf_z_p_z_w2fp16`  — SCVTF

**Assembly:** `SCVTF  <Zd>.H, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 16;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `scvtf_z_p_z_w2fp16z`  — SCVTF

**Assembly:** `SCVTF  <Zd>.H, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 16;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `scvtf_z_p_z_w2s`  — SCVTF

**Assembly:** `SCVTF  <Zd>.S, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `scvtf_z_p_z_w2sz`  — SCVTF

**Assembly:** `SCVTF  <Zd>.S, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `scvtf_z_p_z_w2d`  — SCVTF

**Assembly:** `SCVTF  <Zd>.D, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `scvtf_z_p_z_w2dz`  — SCVTF

**Assembly:** `SCVTF  <Zd>.D, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `scvtf_z_p_z_x2fp16`  — SCVTF

**Assembly:** `SCVTF  <Zd>.H, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 16;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `scvtf_z_p_z_x2fp16z`  — SCVTF

**Assembly:** `SCVTF  <Zd>.H, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 16;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `scvtf_z_p_z_x2s`  — SCVTF

**Assembly:** `SCVTF  <Zd>.S, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `scvtf_z_p_z_x2sz`  — SCVTF

**Assembly:** `SCVTF  <Zd>.S, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `scvtf_z_p_z_x2d`  — SCVTF

**Assembly:** `SCVTF  <Zd>.D, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `scvtf_z_p_z_x2dz`  — SCVTF

**Assembly:** `SCVTF  <Zd>.D, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 64;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

---

## SCVTF (scalar SIMD&FP) -- A64

### Encoding: `SCVTF_sisd_32H`  [32-bit to half-precision]  — SCVTF

**Assembly:** `SCVTF  <Hd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
let rounding : FPRounding = FPRoundingMode(FPCR());
```

### Encoding: `SCVTF_sisd_32D`  [32-bit to double-precision]  — SCVTF

**Assembly:** `SCVTF  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
let rounding : FPRounding = FPRoundingMode(FPCR());
```

### Encoding: `SCVTF_sisd_64H`  [64-bit to half-precision]  — SCVTF

**Assembly:** `SCVTF  <Hd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
let rounding : FPRounding = FPRoundingMode(FPCR());
```

### Encoding: `SCVTF_sisd_64S`  [64-bit to single-precision]  — SCVTF

**Assembly:** `SCVTF  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
let rounding : FPRounding = FPRoundingMode(FPCR());
```

---

## SCVTF (scalar, fixed-point) -- A64

### Encoding: `SCVTF_H32_float2fix`  [32-bit to half-precision]  — SCVTF

**Assembly:** `SCVTF  <Hd>, <Wn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "32-bit to double-precision", "32-bit to half-precision", and "32-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = FALSE;
```

### Encoding: `SCVTF_H64_float2fix`  [64-bit to half-precision]  — SCVTF

**Assembly:** `SCVTF  <Hd>, <Xn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "64-bit to double-precision", "64-bit to half-precision", and "64-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = FALSE;
```

### Encoding: `SCVTF_S32_float2fix`  [32-bit to single-precision]  — SCVTF

**Assembly:** `SCVTF  <Sd>, <Wn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "32-bit to double-precision", "32-bit to half-precision", and "32-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = FALSE;
```

### Encoding: `SCVTF_S64_float2fix`  [64-bit to single-precision]  — SCVTF

**Assembly:** `SCVTF  <Sd>, <Xn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "64-bit to double-precision", "64-bit to half-precision", and "64-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = FALSE;
```

### Encoding: `SCVTF_D32_float2fix`  [32-bit to double-precision]  — SCVTF

**Assembly:** `SCVTF  <Dd>, <Wn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "32-bit to double-precision", "32-bit to half-precision", and "32-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = FALSE;
```

### Encoding: `SCVTF_D64_float2fix`  [64-bit to double-precision]  — SCVTF

**Assembly:** `SCVTF  <Dd>, <Xn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "64-bit to double-precision", "64-bit to half-precision", and "64-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = FALSE;
```

---

## SCVTF (scalar, integer) -- A64

### Encoding: `SCVTF_H32_float2int`  [32-bit to half-precision]  — SCVTF

**Assembly:** `SCVTF  <Hd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = FALSE;
```

### Encoding: `SCVTF_S32_float2int`  [32-bit to single-precision]  — SCVTF

**Assembly:** `SCVTF  <Sd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = FALSE;
```

### Encoding: `SCVTF_D32_float2int`  [32-bit to double-precision]  — SCVTF

**Assembly:** `SCVTF  <Dd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = FALSE;
```

### Encoding: `SCVTF_H64_float2int`  [64-bit to half-precision]  — SCVTF

**Assembly:** `SCVTF  <Hd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = FALSE;
```

### Encoding: `SCVTF_S64_float2int`  [64-bit to single-precision]  — SCVTF

**Assembly:** `SCVTF  <Sd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = FALSE;
```

### Encoding: `SCVTF_D64_float2int`  [64-bit to double-precision]  — SCVTF

**Assembly:** `SCVTF  <Dd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = FALSE;
```

---

## SCVTF (vector, fixed-point) -- A64

### Encoding: `SCVTF_asisdshf_C`  — SCVTF

**Assembly:** `SCVTF  <V><d>, <V><n>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh IN {'000x'} || (immh IN {'001x'} && !IsFeatureImplemented(FEAT_FP16)) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = if immh IN {'1xxx'} then 64 else if immh IN {'01xx'} then 32 else 16;
let datasize : integer{} = esize;
let elements : integer = 1;

let fracbits : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
```

### Encoding: `SCVTF_asimdshf_C`  — SCVTF

**Assembly:** `SCVTF  <Vd>.<T>, <Vn>.<T>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh IN {'000x'} || (immh IN {'001x'} && !IsFeatureImplemented(FEAT_FP16)) then
    EndOfDecode(Decode_UNDEF);
end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = if immh IN {'1xxx'} then 64 else if immh IN {'01xx'} then 32 else 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let fracbits : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
```

---

## SCVTF (vector, integer) -- A64

### Encoding: `SCVTF_asisdmiscfp16_R`  — SCVTF

**Assembly:** `SCVTF  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;

let unsigned : boolean = FALSE;
```

### Encoding: `SCVTF_asisdmisc_R`  — SCVTF

**Assembly:** `SCVTF  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let unsigned : boolean = FALSE;
```

### Encoding: `SCVTF_asimdmiscfp16_R`  — SCVTF

**Assembly:** `SCVTF  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = FALSE;
```

### Encoding: `SCVTF_asimdmisc_R`  — SCVTF

**Assembly:** `SCVTF  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = FALSE;
```

---

## SCVTF -- A64

### Encoding: `scvtf_mz_z_2`  — SCVTF

**Assembly:** `SCVTF  { <Zd1>.S-<Zd2>.S }, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the source multi-vector group, enc | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
```

### Encoding: `scvtf_mz_z_4`  — SCVTF

**Assembly:** `SCVTF  { <Zd1>.S-<Zd4>.S }, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the source multi-vector group, en | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
```

---

## SCVTFLT -- A64

### Encoding: `scvtflt_z_z_`  — SCVTFLT

**Assembly:** `SCVTFLT  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let rounding : FPRounding = FPRoundingMode(FPCR());
```

---

## SDIV -- A64

### Encoding: `SDIV_32_dp_2src`  [32-bit]  — SDIV

**Assembly:** `SDIV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `SDIV_64_dp_2src`  [64-bit]  — SDIV

**Assembly:** `SDIV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## SDIV -- A64

### Encoding: `sdiv_z_p_zz_`  — SDIV

**Assembly:** `SDIV  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SDIVR -- A64

### Encoding: `sdivr_z_p_zz_`  — SDIVR

**Assembly:** `SDIVR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SDOT (2-way, indexed) -- A64

### Encoding: `sdot_z16_zzzi_h`  — SDOT

**Assembly:** `SDOT  <Zda>.H, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "8-bit to 16-bit" variant: is the immediate index of a pair of 8-bit elements within each 128-bit vector segment |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `sdot_z32_zzzi_`  — SDOT

**Assembly:** `SDOT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "16-bit to 32-bit" variant: is the immediate index of a pair of 16-bit elements within each 128-bit vector segme | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SDOT (2-way, multiple and indexed vector) -- A64

### Encoding: `sdot_za32_zzi_2xi`  — SDOT

**Assembly:** `SDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `sdot_za32_zzi_4xi`  — SDOT

**Assembly:** `SDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## SDOT (2-way, multiple and single vector) -- A64

### Encoding: `sdot_za32_zzv_2x1`  — SDOT

**Assembly:** `SDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `sdot_za32_zzv_4x1`  — SDOT

**Assembly:** `SDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## SDOT (2-way, multiple vectors) -- A64

### Encoding: `sdot_za32_zzw_2x2`  — SDOT

**Assembly:** `SDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `sdot_za32_zzw_4x4`  — SDOT

**Assembly:** `SDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## SDOT (2-way, vectors) -- A64

### Encoding: `sdot_z16_zzz_h`  — SDOT

**Assembly:** `SDOT  <Zda>.H, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `sdot_z32_zzz_`  — SDOT

**Assembly:** `SDOT  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SDOT (4-way, indexed) -- A64

### Encoding: `sdot_z_zzzi_s`  — SDOT

**Assembly:** `SDOT  <Zda>.S, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "8-bit to 32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm"  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "8-bit to 32-bit" variant: is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vec | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `sdot_z_zzzi_d`  — SDOT

**Assembly:** `SDOT  <Zda>.D, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "16-bit to 64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "16-bit to 64-bit" variant: is the immediate index of a 64-bit group of four 16-bit values within each 128-bit v | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SDOT (4-way, multiple and indexed vector) -- A64

### Encoding: `sdot_za_zzi_s2xi`  — SDOT

**Assembly:** `SDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of 32-bit elements" and "Two ZA single-vectors of 64-bit elements" variants: is the name  | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "Four ZA single-vectors of 32-bit elements" and "Two ZA single-vectors of 32-bit elements" variants: is the imme | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `sdot_za_zzi_d2xi`  — SDOT

**Assembly:** `SDOT  ZA.D[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of 32-bit elements" and "Two ZA single-vectors of 64-bit elements" variants: is the name  | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "Four ZA single-vectors of 64-bit elements" and "Two ZA single-vectors of 64-bit elements" variants: is the imme | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
let nreg : integer{} = 2;
```

### Encoding: `sdot_za_zzi_s4xi`  — SDOT

**Assembly:** `SDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of 32-bit elements" and "Four ZA single-vectors of 64-bit elements" variants: is the nam | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "Four ZA single-vectors of 32-bit elements" and "Two ZA single-vectors of 32-bit elements" variants: is the imme | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

### Encoding: `sdot_za_zzi_d4xi`  — SDOT

**Assembly:** `SDOT  ZA.D[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of 32-bit elements" and "Four ZA single-vectors of 64-bit elements" variants: is the nam | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "Four ZA single-vectors of 64-bit elements" and "Two ZA single-vectors of 64-bit elements" variants: is the imme | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
let nreg : integer{} = 4;
```

---

## SDOT (4-way, multiple and single vector) -- A64

### Encoding: `sdot_za_zzv_2x1`  — SDOT

**Assembly:** `SDOT  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `sdot_za_zzv_4x1`  — SDOT

**Assembly:** `SDOT  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## SDOT (4-way, multiple vectors) -- A64

### Encoding: `sdot_za_zzw_2x2`  — SDOT

**Assembly:** `SDOT  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, { <Zm1>.<Tb>-<Zm2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `sdot_za_zzw_4x4`  — SDOT

**Assembly:** `SDOT  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, { <Zm1>.<Tb>-<Zm4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## SDOT (4-way, vectors) -- A64

### Encoding: `sdot_z_zzz_`  — SDOT

**Assembly:** `SDOT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SDOT (by element) -- A64

### Encoding: `SDOT_asimdelem_D`  — SDOT

**Assembly:** `SDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.4B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | (M :: Rm) | Is the name of the second SIMD&FP source register, encoded in the "M:Rm" fields. |  |
| `<index>` | (H :: L) | Is the element index, encoded in the "H:L" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_DotProd) then EndOfDecode(Decode_UNDEF); end;
if size  != '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(M::Rm);
let index : integer = UInt(H::L);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SDOT (vector) -- A64

### Encoding: `SDOT_asimdsame2_D`  — SDOT

**Assembly:** `SDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_DotProd) then EndOfDecode(Decode_UNDEF); end;
if size != '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SEL (predicates) -- A64

### Encoding: `sel_p_p_pp_`  — SEL

**Assembly:** `SEL  <Pd>.B, <Pg>, <Pn>.B, <Pm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<Pg>` | Pg (4b) | Is the name of the governing scalable predicate register, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
```

---

## SEL (vectors) -- A64

### Encoding: `sel_z_p_zz_`  — SEL

**Assembly:** `SEL  <Zd>.<T>, <Pv>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pv>` | Pv (4b) | Is the name of the vector select predicate register, encoded in the "Pv" field. | let v : integer = UInt(Pv); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let v : integer = UInt(Pv);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SEL -- A64

### Encoding: `sel_mz_p_zz_2`  — SEL

**Assembly:** `SEL  { <Zd1>.<T>-<Zd2>.<T> }, <PNv>, { <Zn1>.<T>-<Zn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<PNv>` | PNv (3b) | Is the name of the vector select predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "PNv" f | let v : integer = UInt('1'::PNv); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the first source multi-vector grou | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let d : integer = UInt(Zd::'0');
let v : integer = UInt('1'::PNv);
let nreg : integer{} = 2;
```

### Encoding: `sel_mz_p_zz_4`  — SEL

**Assembly:** `SEL  { <Zd1>.<T>-<Zd4>.<T> }, <PNv>, { <Zn1>.<T>-<Zn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<PNv>` | PNv (3b) | Is the name of the vector select predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "PNv" f | let v : integer = UInt('1'::PNv); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the first source multi-vector gro | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let d : integer = UInt(Zd::'00');
let v : integer = UInt('1'::PNv);
let nreg : integer{} = 4;
```

---

## SETF8, SETF16 -- A64

### Encoding: `SETF8_only_setf`  [SETF8]  — SETF8

**Assembly:** `SETF8  <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FlagM) then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
let n : integer = UInt(Rn);
```

### Encoding: `SETF16_only_setf`  [SETF16]  — SETF16

**Assembly:** `SETF16  <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FlagM) then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = 8 << UInt(sz);
let n : integer = UInt(Rn);
```

---

## SETFFR -- A64

### Encoding: `setffr_f_`  — SETFFR

**Assembly:** `SETFFR`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
```

---

## SETGP, SETGM, SETGE -- A64

### Encoding: `SETGP_SET_memcms`  [Prologue]  — SETGP

**Assembly:** `SETGP  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the destination | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Main" and "Prologue" variants: is the 64-bit name of the general-purpose register that holds the source data in | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETGM_SET_memcms`  [Main]  — SETGM

**Assembly:** `SETGM  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Main" and "Prologue" variants: is the 64-bit name of the general-purpose register that holds the source data in | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETGE_SET_memcms`  [Epilogue]  — SETGE

**Assembly:** `SETGE  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds the source data, encoded in th | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## SETGPN, SETGMN, SETGEN -- A64

### Encoding: `SETGPN_SET_memcms`  [Prologue]  — SETGPN

**Assembly:** `SETGPN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the destination | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Main" and "Prologue" variants: is the 64-bit name of the general-purpose register that holds the source data in | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETGMN_SET_memcms`  [Main]  — SETGMN

**Assembly:** `SETGMN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Main" and "Prologue" variants: is the 64-bit name of the general-purpose register that holds the source data in | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETGEN_SET_memcms`  [Epilogue]  — SETGEN

**Assembly:** `SETGEN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds the source data, encoded in th | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## SETGPT, SETGMT, SETGET -- A64

### Encoding: `SETGPT_SET_memcms`  [Prologue]  — SETGPT

**Assembly:** `SETGPT  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the destination | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Main" and "Prologue" variants: is the 64-bit name of the general-purpose register that holds the source data in | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETGMT_SET_memcms`  [Main]  — SETGMT

**Assembly:** `SETGMT  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Main" and "Prologue" variants: is the 64-bit name of the general-purpose register that holds the source data in | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETGET_SET_memcms`  [Epilogue]  — SETGET

**Assembly:** `SETGET  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds the source data, encoded in th | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## SETGPTN, SETGMTN, SETGETN -- A64

### Encoding: `SETGPTN_SET_memcms`  [Prologue]  — SETGPTN

**Assembly:** `SETGPTN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the destination | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Main" and "Prologue" variants: is the 64-bit name of the general-purpose register that holds the source data in | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETGMTN_SET_memcms`  [Main]  — SETGMTN

**Assembly:** `SETGMTN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Main" and "Prologue" variants: is the 64-bit name of the general-purpose register that holds the source data in | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETGETN_SET_memcms`  [Epilogue]  — SETGETN

**Assembly:** `SETGETN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of b | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds the source data, encoded in th | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || !IsFeatureImplemented(FEAT_MTE) || sz != '00' then
    EndOfDecode(Decode_UNDEF);
end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## SETP, SETM, SETE -- A64

### Encoding: `SETP_SET_memcms`  [Prologue]  — SETP

**Assembly:** `SETP  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETM_SET_memcms`  [Main]  — SETM

**Assembly:** `SETM  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETE_SET_memcms`  [Epilogue]  — SETE

**Assembly:** `SETE  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## SETPN, SETMN, SETEN -- A64

### Encoding: `SETPN_SET_memcms`  [Prologue]  — SETPN

**Assembly:** `SETPN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETMN_SET_memcms`  [Main]  — SETMN

**Assembly:** `SETMN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETEN_SET_memcms`  [Epilogue]  — SETEN

**Assembly:** `SETEN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## SETPT, SETMT, SETET -- A64

### Encoding: `SETPT_SET_memcms`  [Prologue]  — SETPT

**Assembly:** `SETPT  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETMT_SET_memcms`  [Main]  — SETMT

**Assembly:** `SETMT  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETET_SET_memcms`  [Epilogue]  — SETET

**Assembly:** `SETET  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## SETPTN, SETMTN, SETETN -- A64

### Encoding: `SETPTN_SET_memcms`  [Prologue]  — SETPTN

**Assembly:** `SETPTN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the destination address and is | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Prologue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETMTN_SET_memcms`  [Main]  — SETMTN

**Assembly:** `SETMTN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Main" variant: is the 64-bit name of the general-purpose register that holds an encoding of the number of bytes | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

### Encoding: `SETETN_SET_memcms`  [Epilogue]  — SETETN

**Assembly:** `SETETN  [<Xd>]!, <Xn>!, <Xs>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | For the "Epilogue" and "Main" variants: is the 64-bit name of the general-purpose register that holds an encoding of the | memset.d = UInt(Rd); |
| `<Xn>` | Rn (5b) | For the "Epilogue" variant: is the 64-bit name of the general-purpose register that holds the number of bytes to be set  | memset.n = UInt(Rn); |
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register that holds the source data, encoded in the "Rs" field. | memset.s = UInt(Rs); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MOPS) || sz != '00' then EndOfDecode(Decode_UNDEF); end;

var memset : SETParams;
memset.d = UInt(Rd);
memset.s = UInt(Rs);
memset.n = UInt(Rn);
let options : bits(2) = op2[1:0];
let nontemporal : boolean = options[1] == '1';

case op2[3:2] of
    when '00' => memset.stage = MOPSStage_Prologue;
    when '01' => memset.stage = MOPSStage_Main;
    when '10' => memset.stage = MOPSStage_Epilogue;
    otherwise => EndOfDecode(Decode_UNDEF);
end;
```

---

## SEV -- A64

### Encoding: `SEV_HI_hints`  — SEV

**Assembly:** `SEV`

**Pseudocode (Decode):**
```
// Empty.
```

---

## SEVL -- A64

### Encoding: `SEVL_HI_hints`  — SEVL

**Assembly:** `SEVL`

**Pseudocode (Decode):**
```
// Empty.
```

---

## SHA1C -- A64

### Encoding: `SHA1C_QSV_cryptosha3`  — SHA1C

**Assembly:** `SHA1C  <Qd>, <Sn>, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qd>` | Rd (5b) | Is the 128-bit name of the SIMD&FP source and destination, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA1) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHA1H -- A64

### Encoding: `SHA1H_SS_cryptosha2`  — SHA1H

**Assembly:** `SHA1H  <Sd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA1) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## SHA1M -- A64

### Encoding: `SHA1M_QSV_cryptosha3`  — SHA1M

**Assembly:** `SHA1M  <Qd>, <Sn>, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qd>` | Rd (5b) | Is the 128-bit name of the SIMD&FP source and destination, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA1) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHA1P -- A64

### Encoding: `SHA1P_QSV_cryptosha3`  — SHA1P

**Assembly:** `SHA1P  <Qd>, <Sn>, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qd>` | Rd (5b) | Is the 128-bit name of the SIMD&FP source and destination, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA1) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHA1SU0 -- A64

### Encoding: `SHA1SU0_VVV_cryptosha3`  — SHA1SU0

**Assembly:** `SHA1SU0  <Vd>.4S, <Vn>.4S, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA1) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHA1SU1 -- A64

### Encoding: `SHA1SU1_VV_cryptosha2`  — SHA1SU1

**Assembly:** `SHA1SU1  <Vd>.4S, <Vn>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA1) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## SHA256H -- A64

### Encoding: `SHA256H_QQV_cryptosha3`  — SHA256H

**Assembly:** `SHA256H  <Qd>, <Qn>, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qd>` | Rd (5b) | Is the 128-bit name of the SIMD&FP source and destination, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Qn>` | Rn (5b) | Is the 128-bit name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA256) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHA256H2 -- A64

### Encoding: `SHA256H2_QQV_cryptosha3`  — SHA256H2

**Assembly:** `SHA256H2  <Qd>, <Qn>, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qd>` | Rd (5b) | Is the 128-bit name of the SIMD&FP source and destination, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Qn>` | Rn (5b) | Is the 128-bit name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA256) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHA256SU0 -- A64

### Encoding: `SHA256SU0_VV_cryptosha2`  — SHA256SU0

**Assembly:** `SHA256SU0  <Vd>.4S, <Vn>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA256) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## SHA256SU1 -- A64

### Encoding: `SHA256SU1_VVV_cryptosha3`  — SHA256SU1

**Assembly:** `SHA256SU1  <Vd>.4S, <Vn>.4S, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA256) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHA512H -- A64

### Encoding: `SHA512H_QQV_cryptosha512_3`  — SHA512H

**Assembly:** `SHA512H  <Qd>, <Qn>, <Vm>.2D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qd>` | Rd (5b) | Is the 128-bit name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Qn>` | Rn (5b) | Is the 128-bit name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA512) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHA512H2 -- A64

### Encoding: `SHA512H2_QQV_cryptosha512_3`  — SHA512H2

**Assembly:** `SHA512H2  <Qd>, <Qn>, <Vm>.2D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qd>` | Rd (5b) | Is the 128-bit name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Qn>` | Rn (5b) | Is the 128-bit name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA512) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHA512SU0 -- A64

### Encoding: `SHA512SU0_VV2_cryptosha512_2`  — SHA512SU0

**Assembly:** `SHA512SU0  <Vd>.2D, <Vn>.2D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA512) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## SHA512SU1 -- A64

### Encoding: `SHA512SU1_VVV2_cryptosha512_3`  — SHA512SU1

**Assembly:** `SHA512SU1  <Vd>.2D, <Vn>.2D, <Vm>.2D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA512) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SHADD -- A64

### Encoding: `SHADD_asimdsame_only`  — SHADD

**Assembly:** `SHADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SHADD -- A64

### Encoding: `shadd_z_p_zz_`  — SHADD

**Assembly:** `SHADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SHL -- A64

### Encoding: `SHL_asisdshf_R`  — SHL

**Assembly:** `SHL  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the left shift amount, in the range 0 to 63, encoded as UInt("immh:immb") - 64. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;
let shift : integer = UInt(immh::immb) - esize;
```

### Encoding: `SHL_asimdshf_R`  — SHL

**Assembly:** `SHL  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let shift : integer = UInt(immh::immb) - esize;
```

---

## SHLL, SHLL2 -- A64

### Encoding: `SHLL_asimdmisc_S`  — SHLL

**Assembly:** `SHLL{2}  <Vd>.<Ta>, <Vn>.<Tb>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = esize;
```

---

## SHRN, SHRN2 -- A64

### Encoding: `SHRN_asimdshf_N`  — SHRN

**Assembly:** `SHRN{2}  <Vd>.<Tb>, <Vn>.<Ta>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = FALSE;
```

---

## SHRNB -- A64

### Encoding: `shrnb_z_zi_`  — SHRNB

**Assembly:** `SHRNB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SHRNT -- A64

### Encoding: `shrnt_z_zi_`  — SHRNT

**Assembly:** `SHRNT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SHSUB -- A64

### Encoding: `SHSUB_asimdsame_only`  — SHSUB

**Assembly:** `SHSUB  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SHSUB -- A64

### Encoding: `shsub_z_p_zz_`  — SHSUB

**Assembly:** `SHSUB  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SHSUBR -- A64

### Encoding: `shsubr_z_p_zz_`  — SHSUBR

**Assembly:** `SHSUBR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SHUH -- A64

### Encoding: `SHUH_HI_hints`  — SHUH

**Assembly:** `SHUH  {PH}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `PH` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMH) then EndOfDecode(Decode_NOP); end;
let priority : boolean = op2[0] == '1';
```

---

## SLI -- A64

### Encoding: `SLI_asisdshf_R`  — SLI

**Assembly:** `SLI  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the left shift amount, in the range 0 to 63, encoded as UInt("immh:immb") - 64. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;
let shift : integer = UInt(immh::immb) - esize;
```

### Encoding: `SLI_asimdshf_R`  — SLI

**Assembly:** `SLI  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let shift : integer = UInt(immh::immb) - esize;
```

---

## SLI -- A64

### Encoding: `sli_z_zzi_`  — SLI

**Assembly:** `SLI  <Zd>.<T>, <Zn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## SM3PARTW1 -- A64

### Encoding: `SM3PARTW1_VVV4_cryptosha512_3`  — SM3PARTW1

**Assembly:** `SM3PARTW1  <Vd>.4S, <Vn>.4S, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SM3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SM3PARTW2 -- A64

### Encoding: `SM3PARTW2_VVV4_cryptosha512_3`  — SM3PARTW2

**Assembly:** `SM3PARTW2  <Vd>.4S, <Vn>.4S, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SM3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SM3SS1 -- A64

### Encoding: `SM3SS1_VVV4_crypto4`  — SM3SS1

**Assembly:** `SM3SS1  <Vd>.4S, <Vn>.4S, <Vm>.4S, <Va>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Va>` | Ra (5b) | Is the name of the third SIMD&FP source register, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SM3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
```

---

## SM3TT1A -- A64

### Encoding: `SM3TT1A_VVV4_crypto3_imm2`  — SM3TT1A

**Assembly:** `SM3TT1A  <Vd>.4S, <Vn>.4S, <Vm>.S[<imm2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<imm2>` | imm2 (2b) | Is a 32-bit element indexed out of <Vm>, encoded in "imm2". | let i : integer = UInt(imm2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SM3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let i : integer = UInt(imm2);
```

---

## SM3TT1B -- A64

### Encoding: `SM3TT1B_VVV4_crypto3_imm2`  — SM3TT1B

**Assembly:** `SM3TT1B  <Vd>.4S, <Vn>.4S, <Vm>.S[<imm2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<imm2>` | imm2 (2b) | Is a 32-bit element indexed out of <Vm>, encoded in "imm2". | let i : integer = UInt(imm2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SM3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let i : integer = UInt(imm2);
```

---

## SM3TT2A -- A64

### Encoding: `SM3TT2A_VVV4_crypto3_imm2`  — SM3TT2A

**Assembly:** `SM3TT2A  <Vd>.4S, <Vn>.4S, <Vm>.S[<imm2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<imm2>` | imm2 (2b) | Is a 32-bit element indexed out of <Vm>, encoded in "imm2". | let i : integer = UInt(imm2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SM3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let i : integer = UInt(imm2);
```

---

## SM3TT2B -- A64

### Encoding: `SM3TT2B_VVV_crypto3_imm2`  — SM3TT2B

**Assembly:** `SM3TT2B  <Vd>.4S, <Vn>.4S, <Vm>.S[<imm2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the third SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<imm2>` | imm2 (2b) | Is a 32-bit element indexed out of <Vm>, encoded in "imm2". | let i : integer = UInt(imm2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SM3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let i : integer = UInt(imm2);
```

---

## SM4E -- A64

### Encoding: `SM4E_VV4_cryptosha512_2`  — SM4E

**Assembly:** `SM4E  <Vd>.4S, <Vn>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SM4) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
```

---

## SM4E -- A64

### Encoding: `sm4e_z_zz_`  — SM4E

**Assembly:** `SM4E  <Zdn>.S, <Zdn>.S, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_SM4) then EndOfDecode(Decode_UNDEF); end;
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SM4EKEY -- A64

### Encoding: `SM4EKEY_VVV4_cryptosha512_3`  — SM4EKEY

**Assembly:** `SM4EKEY  <Vd>.4S, <Vn>.4S, <Vm>.4S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SM4) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SM4EKEY -- A64

### Encoding: `sm4ekey_z_zz_`  — SM4EKEY

**Assembly:** `SM4EKEY  <Zd>.S, <Zn>.S, <Zm>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE_SM4) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SMADDL -- A64

### Encoding: `SMADDL_64WA_dp_3src`  — SMADDL

**Assembly:** `SMADDL  <Xd>, <Wn>, <Wm>, <Xa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Xa>` | Ra (5b) | Is the 64-bit name of the third general-purpose source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
```

---

## SMAX (immediate) -- A64

### Encoding: `SMAX_32_minmax_imm`  [32-bit]  — SMAX

**Assembly:** `SMAX  <Wd>, <Wn>, #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm8 (8b) | Is a signed immediate, in the range -128 to 127, encoded in the "imm8" field. | let imm : integer = SInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let imm : integer = SInt(imm8);
```

### Encoding: `SMAX_64_minmax_imm`  [64-bit]  — SMAX

**Assembly:** `SMAX  <Xd>, <Xn>, #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm8 (8b) | Is a signed immediate, in the range -128 to 127, encoded in the "imm8" field. | let imm : integer = SInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let imm : integer = SInt(imm8);
```

---

## SMAX (immediate) -- A64

### Encoding: `smax_z_zi_`  — SMAX

**Assembly:** `SMAX  <Zdn>.<T>, <Zdn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is the signed immediate operand, in the range -128 to 127, encoded in the "imm8" field. | let imm : integer = if unsigned then UInt(imm8) else SInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
let unsigned : boolean = FALSE;
let imm : integer = if unsigned then UInt(imm8) else SInt(imm8);
```

---

## SMAX (multiple and single vector) -- A64

### Encoding: `smax_mz_zzv_2x1`  — SMAX

**Assembly:** `SMAX  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
let unsigned : boolean = FALSE;
```

### Encoding: `smax_mz_zzv_4x1`  — SMAX

**Assembly:** `SMAX  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
let unsigned : boolean = FALSE;
```

---

## SMAX (multiple vectors) -- A64

### Encoding: `smax_mz_zzw_2x2`  — SMAX

**Assembly:** `SMAX  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
let unsigned : boolean = FALSE;
```

### Encoding: `smax_mz_zzw_4x4`  — SMAX

**Assembly:** `SMAX  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
let unsigned : boolean = FALSE;
```

---

## SMAX (register) -- A64

### Encoding: `SMAX_32_dp_2src`  [32-bit]  — SMAX

**Assembly:** `SMAX  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `SMAX_64_dp_2src`  [64-bit]  — SMAX

**Assembly:** `SMAX  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## SMAX (vectors) -- A64

### Encoding: `smax_z_p_zz_`  — SMAX

**Assembly:** `SMAX  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SMAX -- A64

### Encoding: `SMAX_asimdsame_only`  — SMAX

**Assembly:** `SMAX  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMAXP -- A64

### Encoding: `SMAXP_asimdsame_only`  — SMAXP

**Assembly:** `SMAXP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMAXP -- A64

### Encoding: `smaxp_z_p_zz_`  — SMAXP

**Assembly:** `SMAXP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SMAXQV -- A64

### Encoding: `smaxqv_z_p_z_`  — SMAXQV

**Assembly:** `SMAXQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let unsigned : boolean = FALSE;
```

---

## SMAXV -- A64

### Encoding: `SMAXV_asimdall_only`  — SMAXV

**Assembly:** `SMAXV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '100' then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = FALSE;
```

---

## SMAXV -- A64

### Encoding: `smaxv_r_p_z_`  — SMAXV

**Assembly:** `SMAXV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let unsigned : boolean = FALSE;
```

---

## SMC -- A64

### Encoding: `SMC_EX_exception`  — SMC

**Assembly:** `SMC  #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | imm16 (16b) | Is a 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let imm : bits(16) = imm16; |

**Pseudocode (Decode):**
```
let imm : bits(16) = imm16;
```

---

## SMIN (immediate) -- A64

### Encoding: `SMIN_32_minmax_imm`  [32-bit]  — SMIN

**Assembly:** `SMIN  <Wd>, <Wn>, #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm8 (8b) | Is a signed immediate, in the range -128 to 127, encoded in the "imm8" field. | let imm : integer = SInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let imm : integer = SInt(imm8);
```

### Encoding: `SMIN_64_minmax_imm`  [64-bit]  — SMIN

**Assembly:** `SMIN  <Xd>, <Xn>, #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm8 (8b) | Is a signed immediate, in the range -128 to 127, encoded in the "imm8" field. | let imm : integer = SInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let imm : integer = SInt(imm8);
```

---

## SMIN (immediate) -- A64

### Encoding: `smin_z_zi_`  — SMIN

**Assembly:** `SMIN  <Zdn>.<T>, <Zdn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is the signed immediate operand, in the range -128 to 127, encoded in the "imm8" field. | let imm : integer = if unsigned then UInt(imm8) else SInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
let unsigned : boolean = FALSE;
let imm : integer = if unsigned then UInt(imm8) else SInt(imm8);
```

---

## SMIN (multiple and single vector) -- A64

### Encoding: `smin_mz_zzv_2x1`  — SMIN

**Assembly:** `SMIN  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
let unsigned : boolean = FALSE;
```

### Encoding: `smin_mz_zzv_4x1`  — SMIN

**Assembly:** `SMIN  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
let unsigned : boolean = FALSE;
```

---

## SMIN (multiple vectors) -- A64

### Encoding: `smin_mz_zzw_2x2`  — SMIN

**Assembly:** `SMIN  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
let unsigned : boolean = FALSE;
```

### Encoding: `smin_mz_zzw_4x4`  — SMIN

**Assembly:** `SMIN  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
let unsigned : boolean = FALSE;
```

---

## SMIN (register) -- A64

### Encoding: `SMIN_32_dp_2src`  [32-bit]  — SMIN

**Assembly:** `SMIN  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `SMIN_64_dp_2src`  [64-bit]  — SMIN

**Assembly:** `SMIN  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## SMIN (vectors) -- A64

### Encoding: `smin_z_p_zz_`  — SMIN

**Assembly:** `SMIN  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SMIN -- A64

### Encoding: `SMIN_asimdsame_only`  — SMIN

**Assembly:** `SMIN  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMINP -- A64

### Encoding: `SMINP_asimdsame_only`  — SMINP

**Assembly:** `SMINP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMINP -- A64

### Encoding: `sminp_z_p_zz_`  — SMINP

**Assembly:** `SMINP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SMINQV -- A64

### Encoding: `sminqv_z_p_z_`  — SMINQV

**Assembly:** `SMINQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let unsigned : boolean = FALSE;
```

---

## SMINV -- A64

### Encoding: `SMINV_asimdall_only`  — SMINV

**Assembly:** `SMINV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '100' then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = FALSE;
```

---

## SMINV -- A64

### Encoding: `sminv_r_p_z_`  — SMINV

**Assembly:** `SMINV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let unsigned : boolean = FALSE;
```

---

## SMLAL (multiple and indexed vector) -- A64

### Encoding: `smlal_za_zzi_1`  — SMLAL

**Assembly:** `SMLAL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `smlal_za_zzi_2xi`  — SMLAL

**Assembly:** `SMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `smlal_za_zzi_4xi`  — SMLAL

**Assembly:** `SMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## SMLAL (multiple and single vector) -- A64

### Encoding: `smlal_za_zzv_1`  — SMLAL

**Assembly:** `SMLAL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let nreg : integer{} = 1;
```

### Encoding: `smlal_za_zzv_2x1`  — SMLAL

**Assembly:** `SMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `smlal_za_zzv_4x1`  — SMLAL

**Assembly:** `SMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## SMLAL (multiple vectors) -- A64

### Encoding: `smlal_za_zzw_2x2`  — SMLAL

**Assembly:** `SMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `smlal_za_zzw_4x4`  — SMLAL

**Assembly:** `SMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## SMLAL, SMLAL2 (by element) -- A64

### Encoding: `SMLAL_asimdelem_L`  — SMLAL

**Assembly:** `SMLAL{2}  <Vd>.<Ta>, <Vn>.<Tb>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMLAL, SMLAL2 (vector) -- A64

### Encoding: `SMLAL_asimddiff_L`  — SMLAL

**Assembly:** `SMLAL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMLALB (indexed) -- A64

### Encoding: `smlalb_z_zzzi_s`  — SMLALB

**Assembly:** `SMLALB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

### Encoding: `smlalb_z_zzzi_d`  — SMLALB

**Assembly:** `SMLALB  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

---

## SMLALB (vectors) -- A64

### Encoding: `smlalb_z_zzz_`  — SMLALB

**Assembly:** `SMLALB  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SMLALL (multiple and indexed vector) -- A64

### Encoding: `smlall_za_zzi_s`  — SMLALL

**Assembly:** `SMLALL  ZA.S[<Wv>, <offs1>:<offs4>], <Zn>.B, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the first vecto | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the fourth vect | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 1;
```

### Encoding: `smlall_za_zzi_d`  — SMLALL

**Assembly:** `SMLALL  ZA.D[<Wv>, <offs1>:<offs4>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the first vecto | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the fourth vect | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `smlall_za_zzi_s2xi`  — SMLALL

**Assembly:** `SMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors of 32-bit elements" and "Two ZA quad-vectors of 64-bit elements" variants: is the name of t | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 2;
```

### Encoding: `smlall_za_zzi_d2xi`  — SMLALL

**Assembly:** `SMLALL  ZA.D[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors of 32-bit elements" and "Two ZA quad-vectors of 64-bit elements" variants: is the name of t | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `smlall_za_zzi_s4xi`  — SMLALL

**Assembly:** `SMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors of 32-bit elements" and "Four ZA quad-vectors of 64-bit elements" variants: is the name of | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 4;
```

### Encoding: `smlall_za_zzi_d4xi`  — SMLALL

**Assembly:** `SMLALL  ZA.D[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors of 32-bit elements" and "Four ZA quad-vectors of 64-bit elements" variants: is the name of | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## SMLALL (multiple and single vector) -- A64

### Encoding: `smlall_za_zzv_1`  — SMLALL

**Assembly:** `SMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>], <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let nreg : integer{} = 1;
```

### Encoding: `smlall_za_zzv_2x1`  — SMLALL

**Assembly:** `SMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `smlall_za_zzv_4x1`  — SMLALL

**Assembly:** `SMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## SMLALL (multiple vectors) -- A64

### Encoding: `smlall_za_zzw_2x2`  — SMLALL

**Assembly:** `SMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, { <Zm1>.<Tb>-<Zm2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vecto | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vect | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `smlall_za_zzw_4x4`  — SMLALL

**Assembly:** `SMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, { <Zm1>.<Tb>-<Zm4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vect | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vec | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## SMLALT (indexed) -- A64

### Encoding: `smlalt_z_zzzi_s`  — SMLALT

**Assembly:** `SMLALT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

### Encoding: `smlalt_z_zzzi_d`  — SMLALT

**Assembly:** `SMLALT  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

---

## SMLALT (vectors) -- A64

### Encoding: `smlalt_z_zzz_`  — SMLALT

**Assembly:** `SMLALT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SMLSL (multiple and indexed vector) -- A64

### Encoding: `smlsl_za_zzi_1`  — SMLSL

**Assembly:** `SMLSL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `smlsl_za_zzi_2xi`  — SMLSL

**Assembly:** `SMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `smlsl_za_zzi_4xi`  — SMLSL

**Assembly:** `SMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## SMLSL (multiple and single vector) -- A64

### Encoding: `smlsl_za_zzv_1`  — SMLSL

**Assembly:** `SMLSL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let nreg : integer{} = 1;
```

### Encoding: `smlsl_za_zzv_2x1`  — SMLSL

**Assembly:** `SMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `smlsl_za_zzv_4x1`  — SMLSL

**Assembly:** `SMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## SMLSL (multiple vectors) -- A64

### Encoding: `smlsl_za_zzw_2x2`  — SMLSL

**Assembly:** `SMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `smlsl_za_zzw_4x4`  — SMLSL

**Assembly:** `SMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## SMLSL, SMLSL2 (by element) -- A64

### Encoding: `SMLSL_asimdelem_L`  — SMLSL

**Assembly:** `SMLSL{2}  <Vd>.<Ta>, <Vn>.<Tb>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMLSL, SMLSL2 (vector) -- A64

### Encoding: `SMLSL_asimddiff_L`  — SMLSL

**Assembly:** `SMLSL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMLSLB (indexed) -- A64

### Encoding: `smlslb_z_zzzi_s`  — SMLSLB

**Assembly:** `SMLSLB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

### Encoding: `smlslb_z_zzzi_d`  — SMLSLB

**Assembly:** `SMLSLB  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

---

## SMLSLB (vectors) -- A64

### Encoding: `smlslb_z_zzz_`  — SMLSLB

**Assembly:** `SMLSLB  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SMLSLL (multiple and indexed vector) -- A64

### Encoding: `smlsll_za_zzi_s`  — SMLSLL

**Assembly:** `SMLSLL  ZA.S[<Wv>, <offs1>:<offs4>], <Zn>.B, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the first vecto | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the fourth vect | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 1;
```

### Encoding: `smlsll_za_zzi_d`  — SMLSLL

**Assembly:** `SMLSLL  ZA.D[<Wv>, <offs1>:<offs4>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the first vecto | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the fourth vect | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `smlsll_za_zzi_s2xi`  — SMLSLL

**Assembly:** `SMLSLL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors of 32-bit elements" and "Two ZA quad-vectors of 64-bit elements" variants: is the name of t | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 2;
```

### Encoding: `smlsll_za_zzi_d2xi`  — SMLSLL

**Assembly:** `SMLSLL  ZA.D[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors of 32-bit elements" and "Two ZA quad-vectors of 64-bit elements" variants: is the name of t | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `smlsll_za_zzi_s4xi`  — SMLSLL

**Assembly:** `SMLSLL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors of 32-bit elements" and "Four ZA quad-vectors of 64-bit elements" variants: is the name of | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 4;
```

### Encoding: `smlsll_za_zzi_d4xi`  — SMLSLL

**Assembly:** `SMLSLL  ZA.D[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors of 32-bit elements" and "Four ZA quad-vectors of 64-bit elements" variants: is the name of | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## SMLSLL (multiple and single vector) -- A64

### Encoding: `smlsll_za_zzv_1`  — SMLSLL

**Assembly:** `SMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>], <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let nreg : integer{} = 1;
```

### Encoding: `smlsll_za_zzv_2x1`  — SMLSLL

**Assembly:** `SMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `smlsll_za_zzv_4x1`  — SMLSLL

**Assembly:** `SMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## SMLSLL (multiple vectors) -- A64

### Encoding: `smlsll_za_zzw_2x2`  — SMLSLL

**Assembly:** `SMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, { <Zm1>.<Tb>-<Zm2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vecto | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vect | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `smlsll_za_zzw_4x4`  — SMLSLL

**Assembly:** `SMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, { <Zm1>.<Tb>-<Zm4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vect | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vec | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## SMLSLT (indexed) -- A64

### Encoding: `smlslt_z_zzzi_s`  — SMLSLT

**Assembly:** `SMLSLT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

### Encoding: `smlslt_z_zzzi_d`  — SMLSLT

**Assembly:** `SMLSLT  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

---

## SMLSLT (vectors) -- A64

### Encoding: `smlslt_z_zzz_`  — SMLSLT

**Assembly:** `SMLSLT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SMMLA (vector) -- A64

### Encoding: `SMMLA_asimdsame2_G`  — SMMLA

**Assembly:** `SMMLA  <Vd>.4S, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_I8MM) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

---

## SMMLA -- A64

### Encoding: `smmla_z_zzz_`  — SMMLA

**Assembly:** `SMMLA  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_I8MM) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

---

## SMNEGL -- A64 *(alias for SMSUBL)*

### Encoding: `SMNEGL_SMSUBL_64WA_dp_3src`  — SMSUBL

**Assembly:** `SMNEGL  <Xd>, <Wn>, <Wm> / SMSUBL  <Xd>, <Wn>, <Wm>, XZR`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. |  |
| `SMSUBL` | — |  |  |

---

## SMOP4A (2-way) -- A64

### Encoding: `smop4a_za32_zz_h1x2`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.S, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za32_zz_h1x1`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za32_zz_h2x1`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za32_zz_h2x2`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

---

## SMOP4A (4-way) -- A64

### Encoding: `smop4a_za_zz_b1x2`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.S, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za_zz_b1x1`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za_zz_b2x1`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za_zz_b2x2`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za_zz_h1x2`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.D, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za_zz_h1x1`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.D, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za_zz_h2x1`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4a_za_zz_h2x2`  — SMOP4A

**Assembly:** `SMOP4A  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

---

## SMOP4S (2-way) -- A64

### Encoding: `smop4s_za32_zz_h1x2`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.S, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za32_zz_h1x1`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za32_zz_h2x1`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za32_zz_h2x2`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

---

## SMOP4S (4-way) -- A64

### Encoding: `smop4s_za_zz_b1x2`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.S, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za_zz_b1x1`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za_zz_b2x1`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za_zz_b2x2`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za_zz_h1x2`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.D, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za_zz_h1x1`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.D, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za_zz_h2x1`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smop4s_za_zz_h2x2`  — SMOP4S

**Assembly:** `SMOP4S  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

---

## SMOPA (2-way) -- A64

### Encoding: `smopa_za32_pp_zz_16`  — SMOPA

**Assembly:** `SMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let unsigned : boolean = FALSE;
```

---

## SMOPA (4-way) -- A64

### Encoding: `smopa_za_pp_zz_32`  — SMOPA

**Assembly:** `SMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smopa_za_pp_zz_64`  — SMOPA

**Assembly:** `SMOPA  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

---

## SMOPS (2-way) -- A64

### Encoding: `smops_za32_pp_zz_16`  — SMOPS

**Assembly:** `SMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let unsigned : boolean = FALSE;
```

---

## SMOPS (4-way) -- A64

### Encoding: `smops_za_pp_zz_32`  — SMOPS

**Assembly:** `SMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `smops_za_pp_zz_64`  — SMOPS

**Assembly:** `SMOPS  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

---

## SMOV -- A64

### Encoding: `SMOV_asimdins_W_w`  [32-bit]  — SMOV

**Assembly:** `SMOV  <Wd>, <Vn>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'xx000' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = LowestSetBitNZ(imm5[2:0]);

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << size;
let datasize : integer{} = 32 << UInt(Q);
if datasize <= esize then EndOfDecode(Decode_UNDEF); end;
let index : integer = UInt(imm5[4:size+1]);
let idxdsize : integer{} = 64 << UInt(imm5[4]);
```

### Encoding: `SMOV_asimdins_X_x`  [64-bit]  — SMOV

**Assembly:** `SMOV  <Xd>, <Vn>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'xx000' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = LowestSetBitNZ(imm5[2:0]);

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << size;
let datasize : integer{} = 32 << UInt(Q);
if datasize <= esize then EndOfDecode(Decode_UNDEF); end;
let index : integer = UInt(imm5[4:size+1]);
let idxdsize : integer{} = 64 << UInt(imm5[4]);
```

---

## SMSTART -- A64 *(alias for MSR_imm)*

### Encoding: `SMSTART_MSR_SI_pstate`  — MSR

**Assembly:** `SMSTART  {<option>} / MSR  <pstatefield>,   #1`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<option>` | — |  |  |
| `MSR` | — |  |  |

---

## SMSTOP -- A64 *(alias for MSR_imm)*

### Encoding: `SMSTOP_MSR_SI_pstate`  — MSR

**Assembly:** `SMSTOP  {<option>} / MSR  <pstatefield>,   #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<option>` | — |  |  |
| `MSR` | — |  |  |

---

## SMSUBL -- A64

### Encoding: `SMSUBL_64WA_dp_3src`  — SMSUBL

**Assembly:** `SMSUBL  <Xd>, <Wn>, <Wm>, <Xa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Xa>` | Ra (5b) | Is the 64-bit name of the third general-purpose source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
```

---

## SMULH (predicated) -- A64

### Encoding: `smulh_z_p_zz_`  — SMULH

**Assembly:** `SMULH  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SMULH (unpredicated) -- A64

### Encoding: `smulh_z_zz_`  — SMULH

**Assembly:** `SMULH  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
```

---

## SMULH -- A64

### Encoding: `SMULH_64_dp_3src`  — SMULH

**Assembly:** `SMULH  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SMULL -- A64 *(alias for SMADDL)*

### Encoding: `SMULL_SMADDL_64WA_dp_3src`  — SMADDL

**Assembly:** `SMULL  <Xd>, <Wn>, <Wm> / SMADDL  <Xd>, <Wn>, <Wm>, XZR`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. |  |
| `SMADDL` | — |  |  |

---

## SMULL, SMULL2 (by element) -- A64

### Encoding: `SMULL_asimdelem_L`  — SMULL

**Assembly:** `SMULL{2}  <Vd>.<Ta>, <Vn>.<Tb>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMULL, SMULL2 (vector) -- A64

### Encoding: `SMULL_asimddiff_L`  — SMULL

**Assembly:** `SMULL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SMULLB (indexed) -- A64

### Encoding: `smullb_z_zzi_s`  — SMULLB

**Assembly:** `SMULLB  <Zd>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 0;
```

### Encoding: `smullb_z_zzi_d`  — SMULLB

**Assembly:** `SMULLB  <Zd>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 0;
```

---

## SMULLB (vectors) -- A64

### Encoding: `smullb_z_zz_`  — SMULLB

**Assembly:** `SMULLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SMULLT (indexed) -- A64

### Encoding: `smullt_z_zzi_s`  — SMULLT

**Assembly:** `SMULLT  <Zd>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 1;
```

### Encoding: `smullt_z_zzi_d`  — SMULLT

**Assembly:** `SMULLT  <Zd>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 1;
```

---

## SMULLT (vectors) -- A64

### Encoding: `smullt_z_zz_`  — SMULLT

**Assembly:** `SMULLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SPLICE -- A64

### Encoding: `splice_z_p_zz_con`  — SPLICE

**Assembly:** `SPLICE  <Zd>.<T>, <Pv>, { <Zn1>.<T>, <Zn2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let dst : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pv>` | Pv (3b) | Is the name of the vector select predicate register P0-P7, encoded in the "Pv" field. | let v : integer = UInt(Pv); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the source multi-vector group, encoded in the "Zn" field. | let s1 : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the source multi-vector group, encoded in the "Zn" field. | let s1 : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let v : integer = UInt(Pv);
let dst : integer = UInt(Zd);
let s1 : integer = UInt(Zn);
let s2 : integer = (s1 + 1) MOD 32;
```

### Encoding: `splice_z_p_zz_des`  — SPLICE

**Assembly:** `SPLICE  <Zdn>.<T>, <Pv>, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dst : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pv>` | Pv (3b) | Is the name of the vector select predicate register P0-P7, encoded in the "Pv" field. | let v : integer = UInt(Pv); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let s2 : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let v : integer = UInt(Pv);
let dst : integer = UInt(Zdn);
let s1 : integer = dst;
let s2 : integer = UInt(Zm);
```

---

## SQABS -- A64

### Encoding: `SQABS_asisdmisc_R`  — SQABS

**Assembly:** `SQABS  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `SQABS_asimdmisc_R`  — SQABS

**Assembly:** `SQABS  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQABS -- A64

### Encoding: `sqabs_z_p_z_m`  — SQABS

**Assembly:** `SQABS  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `sqabs_z_p_z_z`  — SQABS

**Assembly:** `SQABS  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## SQADD (immediate) -- A64

### Encoding: `sqadd_z_zi_`  — SQADD

**Assembly:** `SQADD  <Zdn>.<T>, <Zdn>.<T>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is an unsigned immediate in the range 0 to 255, encoded in the "imm8" field. | var imm : integer = UInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
var imm : integer = UInt(imm8);
if sh == '1' then imm = imm << 8; end;
let unsigned : boolean = FALSE;
```

---

## SQADD (vectors, predicated) -- A64

### Encoding: `sqadd_z_p_zz_`  — SQADD

**Assembly:** `SQADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SQADD (vectors, unpredicated) -- A64

### Encoding: `sqadd_z_zz_`  — SQADD

**Assembly:** `SQADD  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
```

---

## SQADD -- A64

### Encoding: `SQADD_asisdsame_only`  — SQADD

**Assembly:** `SQADD  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let unsigned : boolean = FALSE;
```

### Encoding: `SQADD_asimdsame_only`  — SQADD

**Assembly:** `SQADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let unsigned : boolean = FALSE;
```

---

## SQCADD -- A64

### Encoding: `sqcadd_z_zz_`  — SQCADD

**Assembly:** `SQCADD  <Zdn>.<T>, <Zdn>.<T>, <Zm>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
let sub_i : boolean = (rot == '0');
let sub_r : boolean = (rot == '1');
```

---

## SQCVT (four registers) -- A64

### Encoding: `sqcvt_z_mz4_`  — SQCVT

**Assembly:** `SQCVT  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(sz);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
```

---

## SQCVT (two registers) -- A64

### Encoding: `sqcvt_z_mz2_`  — SQCVT

**Assembly:** `SQCVT  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## SQCVTN -- A64

### Encoding: `sqcvtn_z_mz2_`  — SQCVTN

**Assembly:** `SQCVTN  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## SQCVTN -- A64

### Encoding: `sqcvtn_z_mz4_`  — SQCVTN

**Assembly:** `SQCVTN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(sz);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
```

---

## SQCVTU (four registers) -- A64

### Encoding: `sqcvtu_z_mz4_`  — SQCVTU

**Assembly:** `SQCVTU  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(sz);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
```

---

## SQCVTU (two registers) -- A64

### Encoding: `sqcvtu_z_mz2_`  — SQCVTU

**Assembly:** `SQCVTU  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## SQCVTUN -- A64

### Encoding: `sqcvtun_z_mz2_`  — SQCVTUN

**Assembly:** `SQCVTUN  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## SQCVTUN -- A64

### Encoding: `sqcvtun_z_mz4_`  — SQCVTUN

**Assembly:** `SQCVTUN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(sz);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
```

---

## SQDECB -- A64

### Encoding: `sqdecb_r_rs_sx`  — SQDECB

**Assembly:** `SQDECB  <Xdn>, <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqdecb_r_rs_x`  — SQDECB

**Assembly:** `SQDECB  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQDECD (scalar) -- A64

### Encoding: `sqdecd_r_rs_sx`  — SQDECD

**Assembly:** `SQDECD  <Xdn>, <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqdecd_r_rs_x`  — SQDECD

**Assembly:** `SQDECD  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQDECD (vector) -- A64

### Encoding: `sqdecd_z_zs_`  — SQDECD

**Assembly:** `SQDECD  <Zdn>.D{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
```

---

## SQDECH (scalar) -- A64

### Encoding: `sqdech_r_rs_sx`  — SQDECH

**Assembly:** `SQDECH  <Xdn>, <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqdech_r_rs_x`  — SQDECH

**Assembly:** `SQDECH  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQDECH (vector) -- A64

### Encoding: `sqdech_z_zs_`  — SQDECH

**Assembly:** `SQDECH  <Zdn>.H{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
```

---

## SQDECP (scalar) -- A64

### Encoding: `sqdecp_r_p_r_sx`  — SQDECP

**Assembly:** `SQDECP  <Xdn>, <Pm>.<T>, <Wdn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqdecp_r_p_r_x`  — SQDECP

**Assembly:** `SQDECP  <Xdn>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQDECP (vector) -- A64

### Encoding: `sqdecp_z_p_z_`  — SQDECP

**Assembly:** `SQDECP  <Zdn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Zdn);
let unsigned : boolean = FALSE;
```

---

## SQDECW (scalar) -- A64

### Encoding: `sqdecw_r_rs_sx`  — SQDECW

**Assembly:** `SQDECW  <Xdn>, <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqdecw_r_rs_x`  — SQDECW

**Assembly:** `SQDECW  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQDECW (vector) -- A64

### Encoding: `sqdecw_z_zs_`  — SQDECW

**Assembly:** `SQDECW  <Zdn>.S{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
```

---

## SQDMLAL, SQDMLAL2 (by element) -- A64

### Encoding: `SQDMLAL_asisdelem_L`  — SQDMLAL

**Assembly:** `SQDMLAL  <Va><d>, <Vb><n>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Va>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vb>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;
```

### Encoding: `SQDMLAL_asimdelem_L`  — SQDMLAL

**Assembly:** `SQDMLAL{2}  <Vd>.<Ta>, <Vn>.<Tb>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQDMLAL, SQDMLAL2 (vector) -- A64

### Encoding: `SQDMLAL_asisddiff_only`  — SQDMLAL

**Assembly:** `SQDMLAL  <Va><d>, <Vb><n>, <Vb><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Va>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vb>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '00' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;
```

### Encoding: `SQDMLAL_asimddiff_L`  — SQDMLAL

**Assembly:** `SQDMLAL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '00' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQDMLALB (indexed) -- A64

### Encoding: `sqdmlalb_z_zzzi_s`  — SQDMLALB

**Assembly:** `SQDMLALB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

### Encoding: `sqdmlalb_z_zzzi_d`  — SQDMLALB

**Assembly:** `SQDMLALB  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

---

## SQDMLALB (vectors) -- A64

### Encoding: `sqdmlalb_z_zzz_`  — SQDMLALB

**Assembly:** `SQDMLALB  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel1 : integer = 0;
let sel2 : integer = 0;
```

---

## SQDMLALBT -- A64

### Encoding: `sqdmlalbt_z_zzz_`  — SQDMLALBT

**Assembly:** `SQDMLALBT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel1 : integer = 0;
let sel2 : integer = 1;
```

---

## SQDMLALT (indexed) -- A64

### Encoding: `sqdmlalt_z_zzzi_s`  — SQDMLALT

**Assembly:** `SQDMLALT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

### Encoding: `sqdmlalt_z_zzzi_d`  — SQDMLALT

**Assembly:** `SQDMLALT  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

---

## SQDMLALT (vectors) -- A64

### Encoding: `sqdmlalt_z_zzz_`  — SQDMLALT

**Assembly:** `SQDMLALT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel1 : integer = 1;
let sel2 : integer = 1;
```

---

## SQDMLSL, SQDMLSL2 (by element) -- A64

### Encoding: `SQDMLSL_asisdelem_L`  — SQDMLSL

**Assembly:** `SQDMLSL  <Va><d>, <Vb><n>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Va>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Vb>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;
```

### Encoding: `SQDMLSL_asimdelem_L`  — SQDMLSL

**Assembly:** `SQDMLSL{2}  <Vd>.<Ta>, <Vn>.<Tb>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQDMLSL, SQDMLSL2 (vector) -- A64

### Encoding: `SQDMLSL_asisddiff_only`  — SQDMLSL

**Assembly:** `SQDMLSL  <Va><d>, <Vb><n>, <Vb><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Va>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vb>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '00' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;
```

### Encoding: `SQDMLSL_asimddiff_L`  — SQDMLSL

**Assembly:** `SQDMLSL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '00' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQDMLSLB (indexed) -- A64

### Encoding: `sqdmlslb_z_zzzi_s`  — SQDMLSLB

**Assembly:** `SQDMLSLB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

### Encoding: `sqdmlslb_z_zzzi_d`  — SQDMLSLB

**Assembly:** `SQDMLSLB  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

---

## SQDMLSLB (vectors) -- A64

### Encoding: `sqdmlslb_z_zzz_`  — SQDMLSLB

**Assembly:** `SQDMLSLB  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel1 : integer = 0;
let sel2 : integer = 0;
```

---

## SQDMLSLBT -- A64

### Encoding: `sqdmlslbt_z_zzz_`  — SQDMLSLBT

**Assembly:** `SQDMLSLBT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel1 : integer = 0;
let sel2 : integer = 1;
```

---

## SQDMLSLT (indexed) -- A64

### Encoding: `sqdmlslt_z_zzzi_s`  — SQDMLSLT

**Assembly:** `SQDMLSLT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

### Encoding: `sqdmlslt_z_zzzi_d`  — SQDMLSLT

**Assembly:** `SQDMLSLT  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

---

## SQDMLSLT (vectors) -- A64

### Encoding: `sqdmlslt_z_zzz_`  — SQDMLSLT

**Assembly:** `SQDMLSLT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel1 : integer = 1;
let sel2 : integer = 1;
```

---

## SQDMULH (by element) -- A64

### Encoding: `SQDMULH_asisdelem_R`  — SQDMULH

**Assembly:** `SQDMULH  <V><d>, <V><n>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L);   Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;

let round : boolean = FALSE;
```

### Encoding: `SQDMULH_asimdelem_R`  — SQDMULH

**Assembly:** `SQDMULH  <Vd>.<T>, <Vn>.<T>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L);   Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let round : boolean = FALSE;
```

---

## SQDMULH (indexed) -- A64

### Encoding: `sqdmulh_z_zzi_h`  — SQDMULH

**Assembly:** `SQDMULH  <Zd>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "16-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `sqdmulh_z_zzi_s`  — SQDMULH

**Assembly:** `SQDMULH  <Zd>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "32-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `sqdmulh_z_zzi_d`  — SQDMULH

**Assembly:** `SQDMULH  <Zd>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "64-bit" variant: is the element index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SQDMULH (multiple and single vector) -- A64

### Encoding: `sqdmulh_mz_zzv_2x1`  — SQDMULH

**Assembly:** `SQDMULH  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `sqdmulh_mz_zzv_4x1`  — SQDMULH

**Assembly:** `SQDMULH  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## SQDMULH (multiple vectors) -- A64

### Encoding: `sqdmulh_mz_zzw_2x2`  — SQDMULH

**Assembly:** `SQDMULH  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `sqdmulh_mz_zzw_4x4`  — SQDMULH

**Assembly:** `SQDMULH  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## SQDMULH (vector) -- A64

### Encoding: `SQDMULH_asisdsame_only`  — SQDMULH

**Assembly:** `SQDMULH  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' || size == '00' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : boolean = FALSE;
```

### Encoding: `SQDMULH_asimdsame_only`  — SQDMULH

**Assembly:** `SQDMULH  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' || size == '00' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : boolean = FALSE;
```

---

## SQDMULH (vectors) -- A64

### Encoding: `sqdmulh_z_zz_`  — SQDMULH

**Assembly:** `SQDMULH  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SQDMULL, SQDMULL2 (by element) -- A64

### Encoding: `SQDMULL_asimdelem_L`  — SQDMULL

**Assembly:** `SQDMULL{2}  <Vd>.<Ta>, <Vn>.<Tb>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

### Encoding: `SQDMULL_asisdelem_L`  — SQDMULL

**Assembly:** `SQDMULL  <Va><d>, <Vb><n>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Va>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vb>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;
```

---

## SQDMULL, SQDMULL2 (vector) -- A64

### Encoding: `SQDMULL_asisddiff_only`  — SQDMULL

**Assembly:** `SQDMULL  <Va><d>, <Vb><n>, <Vb><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Va>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vb>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '00' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;
```

### Encoding: `SQDMULL_asimddiff_L`  — SQDMULL

**Assembly:** `SQDMULL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '00' || size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQDMULLB (indexed) -- A64

### Encoding: `sqdmullb_z_zzi_s`  — SQDMULLB

**Assembly:** `SQDMULLB  <Zd>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 0;
```

### Encoding: `sqdmullb_z_zzi_d`  — SQDMULLB

**Assembly:** `SQDMULLB  <Zd>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 0;
```

---

## SQDMULLB (vectors) -- A64

### Encoding: `sqdmullb_z_zz_`  — SQDMULLB

**Assembly:** `SQDMULLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SQDMULLT (indexed) -- A64

### Encoding: `sqdmullt_z_zzi_s`  — SQDMULLT

**Assembly:** `SQDMULLT  <Zd>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 1;
```

### Encoding: `sqdmullt_z_zzi_d`  — SQDMULLT

**Assembly:** `SQDMULLT  <Zd>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 1;
```

---

## SQDMULLT (vectors) -- A64

### Encoding: `sqdmullt_z_zz_`  — SQDMULLT

**Assembly:** `SQDMULLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SQINCB -- A64

### Encoding: `sqincb_r_rs_sx`  — SQINCB

**Assembly:** `SQINCB  <Xdn>, <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqincb_r_rs_x`  — SQINCB

**Assembly:** `SQINCB  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQINCD (scalar) -- A64

### Encoding: `sqincd_r_rs_sx`  — SQINCD

**Assembly:** `SQINCD  <Xdn>, <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqincd_r_rs_x`  — SQINCD

**Assembly:** `SQINCD  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQINCD (vector) -- A64

### Encoding: `sqincd_z_zs_`  — SQINCD

**Assembly:** `SQINCD  <Zdn>.D{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
```

---

## SQINCH (scalar) -- A64

### Encoding: `sqinch_r_rs_sx`  — SQINCH

**Assembly:** `SQINCH  <Xdn>, <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqinch_r_rs_x`  — SQINCH

**Assembly:** `SQINCH  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQINCH (vector) -- A64

### Encoding: `sqinch_z_zs_`  — SQINCH

**Assembly:** `SQINCH  <Zdn>.H{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
```

---

## SQINCP (scalar) -- A64

### Encoding: `sqincp_r_p_r_sx`  — SQINCP

**Assembly:** `SQINCP  <Xdn>, <Pm>.<T>, <Wdn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqincp_r_p_r_x`  — SQINCP

**Assembly:** `SQINCP  <Xdn>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQINCP (vector) -- A64

### Encoding: `sqincp_z_p_z_`  — SQINCP

**Assembly:** `SQINCP  <Zdn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Zdn);
let unsigned : boolean = FALSE;
```

---

## SQINCW (scalar) -- A64

### Encoding: `sqincw_r_rs_sx`  — SQINCW

**Assembly:** `SQINCW  <Xdn>, <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 32;
```

### Encoding: `sqincw_r_rs_x`  — SQINCW

**Assembly:** `SQINCW  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
let  ssize : integer{} = 64;
```

---

## SQINCW (vector) -- A64

### Encoding: `sqincw_z_zs_`  — SQINCW

**Assembly:** `SQINCW  <Zdn>.S{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = FALSE;
```

---

## SQNEG -- A64

### Encoding: `SQNEG_asisdmisc_R`  — SQNEG

**Assembly:** `SQNEG  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `SQNEG_asimdmisc_R`  — SQNEG

**Assembly:** `SQNEG  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQNEG -- A64

### Encoding: `sqneg_z_p_z_m`  — SQNEG

**Assembly:** `SQNEG  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `sqneg_z_p_z_z`  — SQNEG

**Assembly:** `SQNEG  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## SQRDCMLAH (indexed) -- A64

### Encoding: `sqrdcmlah_z_zzzi_h`  — SQRDCMLAH

**Assembly:** `SQRDCMLAH  <Zda>.H, <Zn>.H, <Zm>.H[<imm>], <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "16-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let sub_r : boolean = (rot[0] != rot[1]);
let sub_i : boolean = (rot[1] == '1');
```

### Encoding: `sqrdcmlah_z_zzzi_s`  — SQRDCMLAH

**Assembly:** `SQRDCMLAH  <Zda>.S, <Zn>.S, <Zm>.S[<imm>], <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "32-bit" variant: is the element index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let sub_r : boolean = (rot[0] != rot[1]);
let sub_i : boolean = (rot[1] == '1');
```

---

## SQRDCMLAH (vectors) -- A64

### Encoding: `sqrdcmlah_z_zzz_`  — SQRDCMLAH

**Assembly:** `SQRDCMLAH  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>, <const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<const>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel_a : integer = UInt(rot[0]);
let sel_b : integer = UInt(NOT(rot[0]));
let sub_r : boolean = (rot[0] != rot[1]);
let sub_i : boolean = (rot[1] == '1');
```

---

## SQRDMLAH (by element) -- A64

### Encoding: `SQRDMLAH_asisdelem_R`  — SQRDMLAH

**Assembly:** `SQRDMLAH  <V><d>, <V><n>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RDM) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;

case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;

let rounding : boolean = TRUE;
```

### Encoding: `SQRDMLAH_asimdelem_R`  — SQRDMLAH

**Assembly:** `SQRDMLAH  <Vd>.<T>, <Vn>.<T>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RDM) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;

case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let rounding : boolean = TRUE;
```

---

## SQRDMLAH (indexed) -- A64

### Encoding: `sqrdmlah_z_zzzi_h`  — SQRDMLAH

**Assembly:** `SQRDMLAH  <Zda>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "16-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `sqrdmlah_z_zzzi_s`  — SQRDMLAH

**Assembly:** `SQRDMLAH  <Zda>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "32-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `sqrdmlah_z_zzzi_d`  — SQRDMLAH

**Assembly:** `SQRDMLAH  <Zda>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "64-bit" variant: is the element index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SQRDMLAH (vector) -- A64

### Encoding: `SQRDMLAH_asisdsame2_only`  — SQRDMLAH

**Assembly:** `SQRDMLAH  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RDM) then EndOfDecode(Decode_UNDEF); end;
if size == '11' || size == '00' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : boolean = TRUE;
```

### Encoding: `SQRDMLAH_asimdsame2_only`  — SQRDMLAH

**Assembly:** `SQRDMLAH  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RDM) then EndOfDecode(Decode_UNDEF); end;
if size == '11' || size == '00' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : boolean = TRUE;
```

---

## SQRDMLAH (vectors) -- A64

### Encoding: `sqrdmlah_z_zzz_`  — SQRDMLAH

**Assembly:** `SQRDMLAH  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SQRDMLSH (by element) -- A64

### Encoding: `SQRDMLSH_asisdelem_R`  — SQRDMLSH

**Assembly:** `SQRDMLSH  <V><d>, <V><n>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RDM) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;

case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;

let rounding : boolean = TRUE;
```

### Encoding: `SQRDMLSH_asimdelem_R`  — SQRDMLSH

**Assembly:** `SQRDMLSH  <Vd>.<T>, <Vn>.<T>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RDM) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;

case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let rounding : boolean = TRUE;
```

---

## SQRDMLSH (indexed) -- A64

### Encoding: `sqrdmlsh_z_zzzi_h`  — SQRDMLSH

**Assembly:** `SQRDMLSH  <Zda>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "16-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `sqrdmlsh_z_zzzi_s`  — SQRDMLSH

**Assembly:** `SQRDMLSH  <Zda>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "32-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `sqrdmlsh_z_zzzi_d`  — SQRDMLSH

**Assembly:** `SQRDMLSH  <Zda>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "64-bit" variant: is the element index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SQRDMLSH (vector) -- A64

### Encoding: `SQRDMLSH_asisdsame2_only`  — SQRDMLSH

**Assembly:** `SQRDMLSH  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RDM) then EndOfDecode(Decode_UNDEF); end;
if size == '11' || size == '00' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : boolean = TRUE;
```

### Encoding: `SQRDMLSH_asimdsame2_only`  — SQRDMLSH

**Assembly:** `SQRDMLSH  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_RDM) then EndOfDecode(Decode_UNDEF); end;
if size == '11' || size == '00' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : boolean = TRUE;
```

---

## SQRDMLSH (vectors) -- A64

### Encoding: `sqrdmlsh_z_zzz_`  — SQRDMLSH

**Assembly:** `SQRDMLSH  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SQRDMULH (by element) -- A64

### Encoding: `SQRDMULH_asisdelem_R`  — SQRDMULH

**Assembly:** `SQRDMULH  <V><d>, <V><n>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L);   Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;

let round : boolean = TRUE;
```

### Encoding: `SQRDMULH_asimdelem_R`  — SQRDMULH

**Assembly:** `SQRDMULH  <Vd>.<T>, <Vn>.<T>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L);   Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let round : boolean = TRUE;
```

---

## SQRDMULH (indexed) -- A64

### Encoding: `sqrdmulh_z_zzi_h`  — SQRDMULH

**Assembly:** `SQRDMULH  <Zd>.H, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "16-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `sqrdmulh_z_zzi_s`  — SQRDMULH

**Assembly:** `SQRDMULH  <Zd>.S, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "16-bit" and "32-bit" variants: is the name of the second source scalable vector register Z0-Z7, encoded in the  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "32-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

### Encoding: `sqrdmulh_z_zzi_d`  — SQRDMULH

**Assembly:** `SQRDMULH  <Zd>.D, <Zn>.D, <Zm>.D[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "64-bit" variant: is the element index, in the range 0 to 1, encoded in the "i1" field. | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SQRDMULH (vector) -- A64

### Encoding: `SQRDMULH_asisdsame_only`  — SQRDMULH

**Assembly:** `SQRDMULH  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' || size == '00' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let rounding : boolean = TRUE;
```

### Encoding: `SQRDMULH_asimdsame_only`  — SQRDMULH

**Assembly:** `SQRDMULH  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' || size == '00' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let rounding : boolean = TRUE;
```

---

## SQRDMULH (vectors) -- A64

### Encoding: `sqrdmulh_z_zz_`  — SQRDMULH

**Assembly:** `SQRDMULH  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SQRSHL -- A64

### Encoding: `SQRSHL_asisdsame_only`  — SQRSHL

**Assembly:** `SQRSHL  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if S == '0' && size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let unsigned : boolean = FALSE;
let rounding : boolean = TRUE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `SQRSHL_asimdsame_only`  — SQRSHL

**Assembly:** `SQRSHL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let unsigned : boolean = FALSE;
let rounding : boolean = TRUE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQRSHL -- A64

### Encoding: `sqrshl_z_p_zz_`  — SQRSHL

**Assembly:** `SQRSHL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SQRSHLR -- A64

### Encoding: `sqrshlr_z_p_zz_`  — SQRSHLR

**Assembly:** `SQRSHLR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SQRSHR (four registers) -- A64

### Encoding: `sqrshr_z_mz4_`  — SQRSHR

**Assembly:** `SQRSHR  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |
| `<const>` | (tsize :: imm5) | Is the immediate shift amount, in the range 1 to number of bits per source element, encoded in "tsize:imm5". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if tsize == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
let shift : integer = (8 * esize) - UInt(tsize::imm5);
```

---

## SQRSHR (two registers) -- A64

### Encoding: `sqrshr_z_mz2_`  — SQRSHR

**Assembly:** `SQRSHR  <Zd>.H, { <Zn1>.S-<Zn2>.S }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | imm4 (4b) | Is the immediate shift amount, in the range 1 to 16, encoded in the "imm4" field. | let shift : integer = esize - UInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = esize - UInt(imm4);
```

---

## SQRSHRN -- A64

### Encoding: `sqrshrn_z_mz2_b`  — SQRSHRN

**Assembly:** `SQRSHRN  <Zd>.B, { <Zn1>.H-<Zn2>.H }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | imm3 (3b) | For the "8-bit" variant: is the immediate shift amount, in the range 1 to 8, encoded in the "imm3" field. | let shift : integer = esize - UInt(imm3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = esize - UInt(imm3);
```

### Encoding: `sqrshrn_z_mz2_`  — SQRSHRN

**Assembly:** `SQRSHRN  <Zd>.H, { <Zn1>.S-<Zn2>.S }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | imm4 (4b) | For the "16-bit" variant: is the immediate shift amount, in the range 1 to 16, encoded in the "imm4" field. | let shift : integer = esize - UInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = esize - UInt(imm4);
```

---

## SQRSHRN -- A64

### Encoding: `sqrshrn_z_mz4_`  — SQRSHRN

**Assembly:** `SQRSHRN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |
| `<const>` | (tsize :: imm5) | Is the immediate shift amount, in the range 1 to number of bits per source element, encoded in "tsize:imm5". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if tsize == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
let shift : integer = (8 * esize) - UInt(tsize::imm5);
```

---

## SQRSHRN, SQRSHRN2 -- A64

### Encoding: `SQRSHRN_asisdshf_N`  — SQRSHRN

**Assembly:** `SQRSHRN  <Vb><d>, <Va><n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vb>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Va>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh == '0000' then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = TRUE;
let unsigned : boolean = FALSE;
```

### Encoding: `SQRSHRN_asimdshf_N`  — SQRSHRN

**Assembly:** `SQRSHRN{2}  <Vd>.<Tb>, <Vn>.<Ta>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = TRUE;
let unsigned : boolean = FALSE;
```

---

## SQRSHRNB -- A64

### Encoding: `sqrshrnb_z_zi_`  — SQRSHRNB

**Assembly:** `SQRSHRNB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQRSHRNT -- A64

### Encoding: `sqrshrnt_z_zi_`  — SQRSHRNT

**Assembly:** `SQRSHRNT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQRSHRU (four registers) -- A64

### Encoding: `sqrshru_z_mz4_`  — SQRSHRU

**Assembly:** `SQRSHRU  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |
| `<const>` | (tsize :: imm5) | Is the immediate shift amount, in the range 1 to number of bits per source element, encoded in "tsize:imm5". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if tsize == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
let shift : integer = (8 * esize) - UInt(tsize::imm5);
```

---

## SQRSHRU (two registers) -- A64

### Encoding: `sqrshru_z_mz2_`  — SQRSHRU

**Assembly:** `SQRSHRU  <Zd>.H, { <Zn1>.S-<Zn2>.S }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | imm4 (4b) | Is the immediate shift amount, in the range 1 to 16, encoded in the "imm4" field. | let shift : integer = esize - UInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = esize - UInt(imm4);
```

---

## SQRSHRUN -- A64

### Encoding: `sqrshrun_z_mz2_b`  — SQRSHRUN

**Assembly:** `SQRSHRUN  <Zd>.B, { <Zn1>.H-<Zn2>.H }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | imm3 (3b) | For the "8-bit" variant: is the immediate shift amount, in the range 1 to 8, encoded in the "imm3" field. | let shift : integer = esize - UInt(imm3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = esize - UInt(imm3);
```

### Encoding: `sqrshrun_z_mz2_`  — SQRSHRUN

**Assembly:** `SQRSHRUN  <Zd>.H, { <Zn1>.S-<Zn2>.S }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | imm4 (4b) | For the "16-bit" variant: is the immediate shift amount, in the range 1 to 16, encoded in the "imm4" field. | let shift : integer = esize - UInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = esize - UInt(imm4);
```

---

## SQRSHRUN -- A64

### Encoding: `sqrshrun_z_mz4_`  — SQRSHRUN

**Assembly:** `SQRSHRUN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |
| `<const>` | (tsize :: imm5) | Is the immediate shift amount, in the range 1 to number of bits per source element, encoded in "tsize:imm5". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if tsize == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
let shift : integer = (8 * esize) - UInt(tsize::imm5);
```

---

## SQRSHRUN, SQRSHRUN2 -- A64

### Encoding: `SQRSHRUN_asisdshf_N`  — SQRSHRUN

**Assembly:** `SQRSHRUN  <Vb><d>, <Va><n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vb>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Va>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh == '0000' then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = TRUE;
```

### Encoding: `SQRSHRUN_asimdshf_N`  — SQRSHRUN

**Assembly:** `SQRSHRUN{2}  <Vd>.<Tb>, <Vn>.<Ta>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = TRUE;
```

---

## SQRSHRUNB -- A64

### Encoding: `sqrshrunb_z_zi_`  — SQRSHRUNB

**Assembly:** `SQRSHRUNB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQRSHRUNT -- A64

### Encoding: `sqrshrunt_z_zi_`  — SQRSHRUNT

**Assembly:** `SQRSHRUNT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQSHL (immediate) -- A64

### Encoding: `SQSHL_asisdshf_R`  — SQSHL

**Assembly:** `SQSHL  <V><d>, <V><n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh == '0000' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = esize;
let elements : integer = 1;
let shift : integer = UInt(immh::immb) - esize;

let src_unsigned : boolean = FALSE;
let dst_unsigned : boolean = FALSE;
```

### Encoding: `SQSHL_asimdshf_R`  — SQSHL

**Assembly:** `SQSHL  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let shift : integer = UInt(immh::immb) - esize;

let src_unsigned : boolean = FALSE;
let dst_unsigned : boolean = FALSE;
```

---

## SQSHL (immediate) -- A64

### Encoding: `sqshl_z_p_zi_`  — SQSHL

**Assembly:** `SQSHL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## SQSHL (register) -- A64

### Encoding: `SQSHL_asisdsame_only`  — SQSHL

**Assembly:** `SQSHL  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if S == '0' && size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let unsigned : boolean = FALSE;
let rounding : boolean = FALSE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `SQSHL_asimdsame_only`  — SQSHL

**Assembly:** `SQSHL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let unsigned : boolean = FALSE;
let rounding : boolean = FALSE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQSHL (vectors) -- A64

### Encoding: `sqshl_z_p_zz_`  — SQSHL

**Assembly:** `SQSHL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SQSHLR -- A64

### Encoding: `sqshlr_z_p_zz_`  — SQSHLR

**Assembly:** `SQSHLR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SQSHLU -- A64

### Encoding: `SQSHLU_asisdshf_R`  — SQSHLU

**Assembly:** `SQSHLU  <V><d>, <V><n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh == '0000' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = esize;
let elements : integer = 1;
let shift : integer = UInt(immh::immb) - esize;

let src_unsigned : boolean = FALSE;
let dst_unsigned : boolean = TRUE;
```

### Encoding: `SQSHLU_asimdshf_R`  — SQSHLU

**Assembly:** `SQSHLU  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let shift : integer = UInt(immh::immb) - esize;

let src_unsigned : boolean = FALSE;
let dst_unsigned : boolean = TRUE;
```

---

## SQSHLU -- A64

### Encoding: `sqshlu_z_p_zi_`  — SQSHLU

**Assembly:** `SQSHLU  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## SQSHRN -- A64

### Encoding: `sqshrn_z_mz2_`  — SQSHRN

**Assembly:** `SQSHRN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn2>.<Tb> }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | (tsize :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tsize:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if tsize == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQSHRN, SQSHRN2 -- A64

### Encoding: `SQSHRN_asisdshf_N`  — SQSHRN

**Assembly:** `SQSHRN  <Vb><d>, <Va><n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vb>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Va>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh == '0000' then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = FALSE;
let unsigned : boolean = FALSE;
```

### Encoding: `SQSHRN_asimdshf_N`  — SQSHRN

**Assembly:** `SQSHRN{2}  <Vd>.<Tb>, <Vn>.<Ta>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = FALSE;
let unsigned : boolean = FALSE;
```

---

## SQSHRNB -- A64

### Encoding: `sqshrnb_z_zi_`  — SQSHRNB

**Assembly:** `SQSHRNB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQSHRNT -- A64

### Encoding: `sqshrnt_z_zi_`  — SQSHRNT

**Assembly:** `SQSHRNT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQSHRUN -- A64

### Encoding: `sqshrun_z_mz2_`  — SQSHRUN

**Assembly:** `SQSHRUN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn2>.<Tb> }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | (tsize :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tsize:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if tsize == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQSHRUN, SQSHRUN2 -- A64

### Encoding: `SQSHRUN_asisdshf_N`  — SQSHRUN

**Assembly:** `SQSHRUN  <Vb><d>, <Va><n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vb>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Va>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh == '0000' then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = FALSE;
```

### Encoding: `SQSHRUN_asimdshf_N`  — SQSHRUN

**Assembly:** `SQSHRUN{2}  <Vd>.<Tb>, <Vn>.<Ta>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = FALSE;
```

---

## SQSHRUNB -- A64

### Encoding: `sqshrunb_z_zi_`  — SQSHRUNB

**Assembly:** `SQSHRUNB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQSHRUNT -- A64

### Encoding: `sqshrunt_z_zi_`  — SQSHRUNT

**Assembly:** `SQSHRUNT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SQSUB (immediate) -- A64

### Encoding: `sqsub_z_zi_`  — SQSUB

**Assembly:** `SQSUB  <Zdn>.<T>, <Zdn>.<T>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is an unsigned immediate in the range 0 to 255, encoded in the "imm8" field. | var imm : integer = UInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
var imm : integer = UInt(imm8);
if sh == '1' then imm = imm << 8; end;
let unsigned : boolean = FALSE;
```

---

## SQSUB (vectors, predicated) -- A64

### Encoding: `sqsub_z_p_zz_`  — SQSUB

**Assembly:** `SQSUB  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SQSUB (vectors, unpredicated) -- A64

### Encoding: `sqsub_z_zz_`  — SQSUB

**Assembly:** `SQSUB  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
```

---

## SQSUB -- A64

### Encoding: `SQSUB_asisdsame_only`  — SQSUB

**Assembly:** `SQSUB  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let unsigned : boolean = FALSE;
```

### Encoding: `SQSUB_asimdsame_only`  — SQSUB

**Assembly:** `SQSUB  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let unsigned : boolean = FALSE;
```

---

## SQSUBR -- A64

### Encoding: `sqsubr_z_p_zz_`  — SQSUBR

**Assembly:** `SQSUBR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SQXTN, SQXTN2 -- A64

### Encoding: `SQXTN_asisdmisc_N`  — SQXTN

**Assembly:** `SQXTN  <Vb><d>, <Va><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vb>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Va>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let part : integer = 0;
let elements : integer = 1;
let unsigned : boolean = FALSE;
```

### Encoding: `SQXTN_asimdmisc_N`  — SQXTN

**Assembly:** `SQXTN{2}  <Vd>.<Tb>, <Vn>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
let unsigned : boolean = FALSE;
```

---

## SQXTNB -- A64

### Encoding: `sqxtnb_z_zz_`  — SQXTNB

**Assembly:** `SQXTNB  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if !(tsize IN {'001', '010', '100'}) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## SQXTNT -- A64

### Encoding: `sqxtnt_z_zz_`  — SQXTNT

**Assembly:** `SQXTNT  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if !(tsize IN {'001', '010', '100'}) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## SQXTUN, SQXTUN2 -- A64

### Encoding: `SQXTUN_asisdmisc_N`  — SQXTUN

**Assembly:** `SQXTUN  <Vb><d>, <Va><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vb>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Va>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let part : integer = 0;
let elements : integer = 1;
```

### Encoding: `SQXTUN_asimdmisc_N`  — SQXTUN

**Assembly:** `SQXTUN{2}  <Vd>.<Tb>, <Vn>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SQXTUNB -- A64

### Encoding: `sqxtunb_z_zz_`  — SQXTUNB

**Assembly:** `SQXTUNB  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if !(tsize IN {'001', '010', '100'}) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## SQXTUNT -- A64

### Encoding: `sqxtunt_z_zz_`  — SQXTUNT

**Assembly:** `SQXTUNT  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if !(tsize IN {'001', '010', '100'}) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## SRHADD -- A64

### Encoding: `SRHADD_asimdsame_only`  — SRHADD

**Assembly:** `SRHADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SRHADD -- A64

### Encoding: `srhadd_z_p_zz_`  — SRHADD

**Assembly:** `SRHADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SRI -- A64

### Encoding: `SRI_asisdshf_R`  — SRI

**Assembly:** `SRI  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the right shift amount, in the range 1 to 64, encoded as 128 - UInt("immh:immb"). |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;
let shift : integer = (esize * 2) - UInt(immh::immb);
```

### Encoding: `SRI_asimdshf_R`  — SRI

**Assembly:** `SRI  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let shift : integer = (esize * 2) - UInt(immh::immb);
```

---

## SRI -- A64

### Encoding: `sri_z_zzi_`  — SRI

**Assembly:** `SRI  <Zd>.<T>, <Zn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SRSHL (multiple and single vector) -- A64

### Encoding: `srshl_mz_zzv_2x1`  — SRSHL

**Assembly:** `SRSHL  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `srshl_mz_zzv_4x1`  — SRSHL

**Assembly:** `SRSHL  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## SRSHL (multiple vectors) -- A64

### Encoding: `srshl_mz_zzw_2x2`  — SRSHL

**Assembly:** `SRSHL  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `srshl_mz_zzw_4x4`  — SRSHL

**Assembly:** `SRSHL  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## SRSHL -- A64

### Encoding: `SRSHL_asisdsame_only`  — SRSHL

**Assembly:** `SRSHL  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if S == '0' && size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let rounding : boolean = TRUE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `SRSHL_asimdsame_only`  — SRSHL

**Assembly:** `SRSHL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let rounding : boolean = TRUE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SRSHL -- A64

### Encoding: `srshl_z_p_zz_`  — SRSHL

**Assembly:** `SRSHL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SRSHLR -- A64

### Encoding: `srshlr_z_p_zz_`  — SRSHLR

**Assembly:** `SRSHLR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SRSHR -- A64

### Encoding: `SRSHR_asisdshf_R`  — SRSHR

**Assembly:** `SRSHR  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the right shift amount, in the range 1 to 64, encoded as 128 - UInt("immh:immb"). |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let round : boolean = TRUE;
```

### Encoding: `SRSHR_asimdshf_R`  — SRSHR

**Assembly:** `SRSHR  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let round : boolean = TRUE;
```

---

## SRSHR -- A64

### Encoding: `srshr_z_p_zi_`  — SRSHR

**Assembly:** `SRSHR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SRSRA -- A64

### Encoding: `SRSRA_asisdshf_R`  — SRSRA

**Assembly:** `SRSRA  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the right shift amount, in the range 1 to 64, encoded as 128 - UInt("immh:immb"). |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let round : boolean = TRUE;
```

### Encoding: `SRSRA_asimdshf_R`  — SRSRA

**Assembly:** `SRSRA  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let round : boolean = TRUE;
```

---

## SRSRA -- A64

### Encoding: `srsra_z_zi_`  — SRSRA

**Assembly:** `SRSRA  <Zda>.<T>, <Zn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let da : integer = UInt(Zda);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SSBB -- A64 *(alias for DSB)*

### Encoding: `SSBB_DSB_BO_barriers`  — DSB

**Assembly:** `SSBB / DSB  #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `DSB` | — |  |  |

---

## SSHL -- A64

### Encoding: `SSHL_asisdsame_only`  — SSHL

**Assembly:** `SSHL  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if S == '0' && size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let rounding : boolean = FALSE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `SSHL_asimdsame_only`  — SSHL

**Assembly:** `SSHL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let rounding : boolean = FALSE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SSHLL, SSHLL2 -- A64

### Encoding: `SSHLL_asimdshf_L`  — SSHLL

**Assembly:** `SSHLL{2}  <Vd>.<Ta>, <Vn>.<Tb>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = UInt(immh::immb) - esize;
let unsigned : boolean = FALSE;
```

---

## SSHLLB -- A64

### Encoding: `sshllb_z_zi_`  — SSHLLB

**Assembly:** `SSHLLB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## SSHLLT -- A64

### Encoding: `sshllt_z_zi_`  — SSHLLT

**Assembly:** `SSHLLT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## SSHR -- A64

### Encoding: `SSHR_asisdshf_R`  — SSHR

**Assembly:** `SSHR  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the right shift amount, in the range 1 to 64, encoded as 128 - UInt("immh:immb"). |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let round : boolean = FALSE;
```

### Encoding: `SSHR_asimdshf_R`  — SSHR

**Assembly:** `SSHR  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let round : boolean = FALSE;
```

---

## SSRA -- A64

### Encoding: `SSRA_asisdshf_R`  — SSRA

**Assembly:** `SSRA  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the right shift amount, in the range 1 to 64, encoded as 128 - UInt("immh:immb"). |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let round : boolean = FALSE;
```

### Encoding: `SSRA_asimdshf_R`  — SSRA

**Assembly:** `SSRA  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = FALSE;
let round : boolean = FALSE;
```

---

## SSRA -- A64

### Encoding: `ssra_z_zi_`  — SSRA

**Assembly:** `SSRA  <Zda>.<T>, <Zn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let da : integer = UInt(Zda);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## SSUBL, SSUBL2 -- A64

### Encoding: `SSUBL_asimddiff_L`  — SSUBL

**Assembly:** `SSUBL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SSUBLB -- A64

### Encoding: `ssublb_z_zz_`  — SSUBLB

**Assembly:** `SSUBLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 0;
let sel2 : integer = 0;
let unsigned : boolean = FALSE;
```

---

## SSUBLBT -- A64

### Encoding: `ssublbt_z_zz_`  — SSUBLBT

**Assembly:** `SSUBLBT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 0;
let sel2 : integer = 1;
let unsigned : boolean = FALSE;
```

---

## SSUBLT -- A64

### Encoding: `ssublt_z_zz_`  — SSUBLT

**Assembly:** `SSUBLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 1;
let sel2 : integer = 1;
let unsigned : boolean = FALSE;
```

---

## SSUBLTB -- A64

### Encoding: `ssubltb_z_zz_`  — SSUBLTB

**Assembly:** `SSUBLTB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 1;
let sel2 : integer = 0;
let unsigned : boolean = FALSE;
```

---

## SSUBW, SSUBW2 -- A64

### Encoding: `SSUBW_asimddiff_W`  — SSUBW

**Assembly:** `SSUBW{2}  <Vd>.<Ta>, <Vn>.<Ta>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SSUBWB -- A64

### Encoding: `ssubwb_z_zz_`  — SSUBWB

**Assembly:** `SSUBWB  <Zd>.<T>, <Zn>.<T>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SSUBWT -- A64

### Encoding: `ssubwt_z_zz_`  — SSUBWT

**Assembly:** `SSUBWT  <Zd>.<T>, <Zn>.<T>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## ST1 (multiple structures) -- A64

### Encoding: `ST1_asisdlse_R1_1v`  [One register]  — ST1

**Assembly:** `ST1  { <Vt>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlse_R2_2v`  [Two registers]  — ST1

**Assembly:** `ST1  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlse_R3_3v`  [Three registers]  — ST1

**Assembly:** `ST1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlse_R4_4v`  [Four registers]  — ST1

**Assembly:** `ST1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsep_I1_i1`  [One register, immediate offset]  — ST1

**Assembly:** `ST1  { <Vt>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsep_R1_r1`  [One register, register offset]  — ST1

**Assembly:** `ST1  { <Vt>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsep_I2_i2`  [Two registers, immediate offset]  — ST1

**Assembly:** `ST1  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsep_R2_r2`  [Two registers, register offset]  — ST1

**Assembly:** `ST1  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsep_I3_i3`  [Three registers, immediate offset]  — ST1

**Assembly:** `ST1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsep_R3_r3`  [Three registers, register offset]  — ST1

**Assembly:** `ST1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsep_I4_i4`  [Four registers, immediate offset]  — ST1

**Assembly:** `ST1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsep_R4_r4`  [Four registers, register offset]  — ST1

**Assembly:** `ST1  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## ST1 (single structure) -- A64

### Encoding: `ST1_asisdlso_B1_1b`  [8-bit]  — ST1

**Assembly:** `ST1  { <Vt>.B }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlso_H1_1h`  [16-bit]  — ST1

**Assembly:** `ST1  { <Vt>.H }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlso_S1_1s`  [32-bit]  — ST1

**Assembly:** `ST1  { <Vt>.S }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlso_D1_1d`  [64-bit]  — ST1

**Assembly:** `ST1  { <Vt>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsop_B1_i1b`  [8-bit, immediate offset]  — ST1

**Assembly:** `ST1  { <Vt>.B }[<index>], [<Xn|SP>], #1`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsop_BX1_r1b`  [8-bit, register offset]  — ST1

**Assembly:** `ST1  { <Vt>.B }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsop_H1_i1h`  [16-bit, immediate offset]  — ST1

**Assembly:** `ST1  { <Vt>.H }[<index>], [<Xn|SP>], #2`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsop_HX1_r1h`  [16-bit, register offset]  — ST1

**Assembly:** `ST1  { <Vt>.H }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsop_S1_i1s`  [32-bit, immediate offset]  — ST1

**Assembly:** `ST1  { <Vt>.S }[<index>], [<Xn|SP>], #4`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsop_SX1_r1s`  [32-bit, register offset]  — ST1

**Assembly:** `ST1  { <Vt>.S }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsop_D1_i1d`  [64-bit, immediate offset]  — ST1

**Assembly:** `ST1  { <Vt>.D }[<index>], [<Xn|SP>], #8`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST1_asisdlsop_DX1_r1d`  [64-bit, register offset]  — ST1

**Assembly:** `ST1  { <Vt>.D }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## ST1B (scalar plus immediate, consecutive registers) -- A64

### Encoding: `st1b_mz_p_bi_2`  — ST1B

**Assembly:** `ST1B  { <Zt1>.B-<Zt2>.B }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

### Encoding: `st1b_mz_p_bi_4`  — ST1B

**Assembly:** `ST1B  { <Zt1>.B-<Zt4>.B }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## ST1B (scalar plus immediate, single register) -- A64

### Encoding: `st1b_z_p_bi_`  — ST1B

**Assembly:** `ST1B  { <Zt>.<T> }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8 << UInt(size);
let msize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## ST1B (scalar plus immediate, strided registers) -- A64

### Encoding: `st1b_mzx_p_bi_2x8`  — ST1B

**Assembly:** `ST1B  { <Zt1>.B, <Zt2>.B }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

### Encoding: `st1b_mzx_p_bi_4x4`  — ST1B

**Assembly:** `ST1B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## ST1B (scalar plus scalar, consecutive registers) -- A64

### Encoding: `st1b_mz_p_br_2`  — ST1B

**Assembly:** `ST1B  { <Zt1>.B-<Zt2>.B }, <PNg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 8;
```

### Encoding: `st1b_mz_p_br_4`  — ST1B

**Assembly:** `ST1B  { <Zt1>.B-<Zt4>.B }, <PNg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 8;
```

---

## ST1B (scalar plus scalar, single register) -- A64

### Encoding: `st1b_z_p_br_`  — ST1B

**Assembly:** `ST1B  { <Zt>.<T> }, <Pg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8 << UInt(size);
let msize : integer{} = 8;
```

---

## ST1B (scalar plus scalar, strided registers) -- A64

### Encoding: `st1b_mzx_p_br_2x8`  — ST1B

**Assembly:** `ST1B  { <Zt1>.B, <Zt2>.B }, <PNg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 8;
```

### Encoding: `st1b_mzx_p_br_4x4`  — ST1B

**Assembly:** `ST1B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <PNg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 8;
```

---

## ST1B (scalar plus scalar, tile slice) -- A64

### Encoding: `st1b_za_p_rrr_`  — ST1B

**Assembly:** `ST1B  { ZA0<HV>.B[<Ws>, <offs>] }, <Pg>, [<Xn|SP>{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off4 (4b) | Is the slice index offset, in the range 0 to 15, encoded in the "off4" field. | let offset : integer = UInt(off4); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = 0;
let offset : integer = UInt(off4);
let esize : integer{} = 8;
let vertical : boolean = V == '1';
```

---

## ST1B (scalar plus vector) -- A64

### Encoding: `st1b_z_p_bz_d_x32_unscaled`  — ST1B

**Assembly:** `ST1B  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `st1b_z_p_bz_s_x32_unscaled`  — ST1B

**Assembly:** `ST1B  { <Zt>.S }, <Pg>, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `st1b_z_p_bz_d_64_unscaled`  — ST1B

**Assembly:** `ST1B  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## ST1B (vector plus immediate) -- A64

### Encoding: `st1b_z_p_ai_s`  — ST1B

**Assembly:** `ST1B  { <Zt>.S }, <Pg>, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let offset : integer = UInt(imm5);
```

### Encoding: `st1b_z_p_ai_d`  — ST1B

**Assembly:** `ST1B  { <Zt>.D }, <Pg>, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, in the range 0 to 31, defaulting to 0, encoded in the "imm5" field. | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offset : integer = UInt(imm5);
```

---

## ST1D (scalar plus immediate, consecutive registers) -- A64

### Encoding: `st1d_mz_p_bi_2`  — ST1D

**Assembly:** `ST1D  { <Zt1>.D-<Zt2>.D }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

### Encoding: `st1d_mz_p_bi_4`  — ST1D

**Assembly:** `ST1D  { <Zt1>.D-<Zt4>.D }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## ST1D (scalar plus immediate, single register) -- A64

### Encoding: `st1d_z_p_bi_`  — ST1D

**Assembly:** `ST1D  { <Zt>.D }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offset : integer = SInt(imm4);
```

### Encoding: `st1d_z_p_bi_u128`  — ST1D

**Assembly:** `ST1D  { <Zt>.Q }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let msize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## ST1D (scalar plus immediate, strided registers) -- A64

### Encoding: `st1d_mzx_p_bi_2x8`  — ST1D

**Assembly:** `ST1D  { <Zt1>.D, <Zt2>.D }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

### Encoding: `st1d_mzx_p_bi_4x4`  — ST1D

**Assembly:** `ST1D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## ST1D (scalar plus scalar, consecutive registers) -- A64

### Encoding: `st1d_mz_p_br_2`  — ST1D

**Assembly:** `ST1D  { <Zt1>.D-<Zt2>.D }, <PNg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 64;
```

### Encoding: `st1d_mz_p_br_4`  — ST1D

**Assembly:** `ST1D  { <Zt1>.D-<Zt4>.D }, <PNg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 64;
```

---

## ST1D (scalar plus scalar, single register) -- A64

### Encoding: `st1d_z_p_br_`  — ST1D

**Assembly:** `ST1D  { <Zt>.D }, <Pg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
```

### Encoding: `st1d_z_p_br_u128`  — ST1D

**Assembly:** `ST1D  { <Zt>.Q }, <Pg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let msize : integer{} = 64;
```

---

## ST1D (scalar plus scalar, strided registers) -- A64

### Encoding: `st1d_mzx_p_br_2x8`  — ST1D

**Assembly:** `ST1D  { <Zt1>.D, <Zt2>.D }, <PNg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 64;
```

### Encoding: `st1d_mzx_p_br_4x4`  — ST1D

**Assembly:** `ST1D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <PNg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 64;
```

---

## ST1D (scalar plus scalar, tile slice) -- A64

### Encoding: `st1d_za_p_rrr_`  — ST1D

**Assembly:** `ST1D  { <ZAt><HV>.D[<Ws>, <offs>] }, <Pg>, [<Xn|SP>{, <Xm>, LSL #3}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAt>` | ZAt (3b) | Is the name of the ZA tile ZA0-ZA7 to be accessed, encoded in the "ZAt" field. | let t : integer = UInt(ZAt); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | o1 (1b) | Is the slice index offset, in the range 0 to 1, encoded in the "o1" field. | let offset : integer = UInt(o1); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = UInt(ZAt);
let offset : integer = UInt(o1);
let esize : integer{} = 64;
let vertical : boolean = V == '1';
```

---

## ST1D (scalar plus vector) -- A64

### Encoding: `st1d_z_p_bz_d_x32_scaled`  — ST1D

**Assembly:** `ST1D  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, <mod> #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 3;
```

### Encoding: `st1d_z_p_bz_d_x32_unscaled`  — ST1D

**Assembly:** `ST1D  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `st1d_z_p_bz_d_64_scaled`  — ST1D

**Assembly:** `ST1D  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 3;
```

### Encoding: `st1d_z_p_bz_d_64_unscaled`  — ST1D

**Assembly:** `ST1D  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## ST1D (vector plus immediate) -- A64

### Encoding: `st1d_z_p_ai_d`  — ST1D

**Assembly:** `ST1D  { <Zt>.D }, <Pg>, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 8 in the range 0 to 248, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
let offset : integer = UInt(imm5);
```

---

## ST1H (scalar plus immediate, consecutive registers) -- A64

### Encoding: `st1h_mz_p_bi_2`  — ST1H

**Assembly:** `ST1H  { <Zt1>.H-<Zt2>.H }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

### Encoding: `st1h_mz_p_bi_4`  — ST1H

**Assembly:** `ST1H  { <Zt1>.H-<Zt4>.H }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## ST1H (scalar plus immediate, single register) -- A64

### Encoding: `st1h_z_p_bi_`  — ST1H

**Assembly:** `ST1H  { <Zt>.<T> }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8 << UInt(size);
let msize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## ST1H (scalar plus immediate, strided registers) -- A64

### Encoding: `st1h_mzx_p_bi_2x8`  — ST1H

**Assembly:** `ST1H  { <Zt1>.H, <Zt2>.H }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

### Encoding: `st1h_mzx_p_bi_4x4`  — ST1H

**Assembly:** `ST1H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## ST1H (scalar plus scalar, consecutive registers) -- A64

### Encoding: `st1h_mz_p_br_2`  — ST1H

**Assembly:** `ST1H  { <Zt1>.H-<Zt2>.H }, <PNg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 16;
```

### Encoding: `st1h_mz_p_br_4`  — ST1H

**Assembly:** `ST1H  { <Zt1>.H-<Zt4>.H }, <PNg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 16;
```

---

## ST1H (scalar plus scalar, single register) -- A64

### Encoding: `st1h_z_p_br_`  — ST1H

**Assembly:** `ST1H  { <Zt>.<T> }, <Pg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8 << UInt(size);
let msize : integer{} = 16;
```

---

## ST1H (scalar plus scalar, strided registers) -- A64

### Encoding: `st1h_mzx_p_br_2x8`  — ST1H

**Assembly:** `ST1H  { <Zt1>.H, <Zt2>.H }, <PNg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 16;
```

### Encoding: `st1h_mzx_p_br_4x4`  — ST1H

**Assembly:** `ST1H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <PNg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 16;
```

---

## ST1H (scalar plus scalar, tile slice) -- A64

### Encoding: `st1h_za_p_rrr_`  — ST1H

**Assembly:** `ST1H  { <ZAt><HV>.H[<Ws>, <offs>] }, <Pg>, [<Xn|SP>{, <Xm>, LSL #1}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAt>` | ZAt (1b) | Is the name of the ZA tile ZA0-ZA1 to be accessed, encoded in the "ZAt" field. | let t : integer = UInt(ZAt); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off3 (3b) | Is the slice index offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = UInt(ZAt);
let offset : integer = UInt(off3);
let esize : integer{} = 16;
let vertical : boolean = V == '1';
```

---

## ST1H (scalar plus vector) -- A64

### Encoding: `st1h_z_p_bz_s_x32_scaled`  — ST1H

**Assembly:** `ST1H  { <Zt>.S }, <Pg>, [<Xn|SP>, <Zm>.S, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `st1h_z_p_bz_d_x32_scaled`  — ST1H

**Assembly:** `ST1H  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, <mod> #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 1;
```

### Encoding: `st1h_z_p_bz_d_x32_unscaled`  — ST1H

**Assembly:** `ST1H  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `st1h_z_p_bz_s_x32_unscaled`  — ST1H

**Assembly:** `ST1H  { <Zt>.S }, <Pg>, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `st1h_z_p_bz_d_64_scaled`  — ST1H

**Assembly:** `ST1H  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 1;
```

### Encoding: `st1h_z_p_bz_d_64_unscaled`  — ST1H

**Assembly:** `ST1H  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## ST1H (vector plus immediate) -- A64

### Encoding: `st1h_z_p_ai_s`  — ST1H

**Assembly:** `ST1H  { <Zt>.S }, <Pg>, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
let offset : integer = UInt(imm5);
```

### Encoding: `st1h_z_p_ai_d`  — ST1H

**Assembly:** `ST1H  { <Zt>.D }, <Pg>, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 2 in the range 0 to 62, defaulting to 0, encoded in the "i | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
let offset : integer = UInt(imm5);
```

---

## ST1Q -- A64

### Encoding: `st1q_z_p_ar_d_64_unscaled`  — ST1Q

**Assembly:** `ST1Q  { <Zt>.Q }, <Pg>, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
```

---

## ST1Q -- A64

### Encoding: `st1q_za_p_rrr_`  — ST1Q

**Assembly:** `ST1Q  { <ZAt><HV>.Q[<Ws>, <offs>] }, <Pg>, [<Xn|SP>{, <Xm>, LSL #4}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAt>` | ZAt (4b) | Is the name of the ZA tile ZA0-ZA15 to be accessed, encoded in the "ZAt" field. | let t : integer = UInt(ZAt); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | — | Is the slice index offset, with implicit value 0. |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = UInt(ZAt);
let offset : integer = 0;
let esize : integer{} = 128;
let vertical : boolean = V == '1';
```

---

## ST1W (scalar plus immediate, consecutive registers) -- A64

### Encoding: `st1w_mz_p_bi_2`  — ST1W

**Assembly:** `ST1W  { <Zt1>.S-<Zt2>.S }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

### Encoding: `st1w_mz_p_bi_4`  — ST1W

**Assembly:** `ST1W  { <Zt1>.S-<Zt4>.S }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## ST1W (scalar plus immediate, single register) -- A64

### Encoding: `st1w_z_p_bi_`  — ST1W

**Assembly:** `ST1W  { <Zt>.<T> }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32 << UInt(sz);
let msize : integer{} = 32;
let offset : integer = SInt(imm4);
```

### Encoding: `st1w_z_p_bi_u128`  — ST1W

**Assembly:** `ST1W  { <Zt>.Q }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let msize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## ST1W (scalar plus immediate, strided registers) -- A64

### Encoding: `st1w_mzx_p_bi_2x8`  — ST1W

**Assembly:** `ST1W  { <Zt1>.S, <Zt2>.S }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

### Encoding: `st1w_mzx_p_bi_4x4`  — ST1W

**Assembly:** `ST1W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## ST1W (scalar plus scalar, consecutive registers) -- A64

### Encoding: `st1w_mz_p_br_2`  — ST1W

**Assembly:** `ST1W  { <Zt1>.S-<Zt2>.S }, <PNg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 32;
```

### Encoding: `st1w_mz_p_br_4`  — ST1W

**Assembly:** `ST1W  { <Zt1>.S-<Zt4>.S }, <PNg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 32;
```

---

## ST1W (scalar plus scalar, single register) -- A64

### Encoding: `st1w_z_p_br_`  — ST1W

**Assembly:** `ST1W  { <Zt>.<T> }, <Pg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32 << UInt(sz);
let msize : integer{} = 32;
```

### Encoding: `st1w_z_p_br_u128`  — ST1W

**Assembly:** `ST1W  { <Zt>.Q }, <Pg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) then EndOfDecode(Decode_UNDEF); end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let msize : integer{} = 32;
```

---

## ST1W (scalar plus scalar, strided registers) -- A64

### Encoding: `st1w_mzx_p_br_2x8`  — ST1W

**Assembly:** `ST1W  { <Zt1>.S, <Zt2>.S }, <PNg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 32;
```

### Encoding: `st1w_mzx_p_br_4x4`  — ST1W

**Assembly:** `ST1W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <PNg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 32;
```

---

## ST1W (scalar plus scalar, tile slice) -- A64

### Encoding: `st1w_za_p_rrr_`  — ST1W

**Assembly:** `ST1W  { <ZAt><HV>.S[<Ws>, <offs>] }, <Pg>, [<Xn|SP>{, <Xm>, LSL #2}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAt>` | ZAt (2b) | Is the name of the ZA tile ZA0-ZA3 to be accessed, encoded in the "ZAt" field. | let t : integer = UInt(ZAt); |
| `<HV>` | — |  |  |
| `<Ws>` | Rs (2b) | Is the 32-bit name of the slice index register W12-W15, encoded in the "Rs" field. | let s : integer = UInt('011'::Rs); |
| `<offs>` | off2 (2b) | Is the slice index offset, in the range 0 to 3, encoded in the "off2" field. | let offset : integer = UInt(off2); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt('0'::Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('0'::Pg);
let s : integer = UInt('011'::Rs);
let t : integer = UInt(ZAt);
let offset : integer = UInt(off2);
let esize : integer{} = 32;
let vertical : boolean = V == '1';
```

---

## ST1W (scalar plus vector) -- A64

### Encoding: `st1w_z_p_bz_s_x32_scaled`  — ST1W

**Assembly:** `ST1W  { <Zt>.S }, <Pg>, [<Xn|SP>, <Zm>.S, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 2;
```

### Encoding: `st1w_z_p_bz_d_x32_scaled`  — ST1W

**Assembly:** `ST1W  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, <mod> #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 2;
```

### Encoding: `st1w_z_p_bz_d_x32_unscaled`  — ST1W

**Assembly:** `ST1W  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `st1w_z_p_bz_s_x32_unscaled`  — ST1W

**Assembly:** `ST1W  { <Zt>.S }, <Pg>, [<Xn|SP>, <Zm>.S, <mod>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<mod>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let offs_size : integer{} = 32;
let offs_unsigned : boolean = xs == '0';
let scale : integer = 0;
```

### Encoding: `st1w_z_p_bz_d_64_scaled`  — ST1W

**Assembly:** `ST1W  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 2;
```

### Encoding: `st1w_z_p_bz_d_64_unscaled`  — ST1W

**Assembly:** `ST1W  { <Zt>.D }, <Pg>, [<Xn|SP>, <Zm>.D]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Zm>` | Zm (5b) | Is the name of the offset scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offs_size : integer{} = 64;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

---

## ST1W (vector plus immediate) -- A64

### Encoding: `st1w_z_p_ai_s`  — ST1W

**Assembly:** `ST1W  { <Zt>.S }, <Pg>, [<Zn>.S{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
let offset : integer = UInt(imm5);
```

### Encoding: `st1w_z_p_ai_d`  — ST1W

**Assembly:** `ST1W  { <Zt>.D }, <Pg>, [<Zn>.D{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<imm>` | imm5 (5b) | Is the optional unsigned immediate byte offset, a multiple of 4 in the range 0 to 124, defaulting to 0, encoded in the " | let offset : integer = UInt(imm5); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
let offset : integer = UInt(imm5);
```

---

## ST2 (multiple structures) -- A64

### Encoding: `ST2_asisdlse_R2`  — ST2

**Assembly:** `ST2  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsep_I2_i`  [Immediate offset]  — ST2

**Assembly:** `ST2  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsep_R2_r`  [Register offset]  — ST2

**Assembly:** `ST2  { <Vt>.<T>, <Vt2>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## ST2 (single structure) -- A64

### Encoding: `ST2_asisdlso_B2_2b`  [8-bit]  — ST2

**Assembly:** `ST2  { <Vt>.B, <Vt2>.B }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlso_H2_2h`  [16-bit]  — ST2

**Assembly:** `ST2  { <Vt>.H, <Vt2>.H }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlso_S2_2s`  [32-bit]  — ST2

**Assembly:** `ST2  { <Vt>.S, <Vt2>.S }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlso_D2_2d`  [64-bit]  — ST2

**Assembly:** `ST2  { <Vt>.D, <Vt2>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsop_B2_i2b`  [8-bit, immediate offset]  — ST2

**Assembly:** `ST2  { <Vt>.B, <Vt2>.B }[<index>], [<Xn|SP>], #2`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsop_BX2_r2b`  [8-bit, register offset]  — ST2

**Assembly:** `ST2  { <Vt>.B, <Vt2>.B }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsop_H2_i2h`  [16-bit, immediate offset]  — ST2

**Assembly:** `ST2  { <Vt>.H, <Vt2>.H }[<index>], [<Xn|SP>], #4`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsop_HX2_r2h`  [16-bit, register offset]  — ST2

**Assembly:** `ST2  { <Vt>.H, <Vt2>.H }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsop_S2_i2s`  [32-bit, immediate offset]  — ST2

**Assembly:** `ST2  { <Vt>.S, <Vt2>.S }[<index>], [<Xn|SP>], #8`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsop_SX2_r2s`  [32-bit, register offset]  — ST2

**Assembly:** `ST2  { <Vt>.S, <Vt2>.S }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsop_D2_i2d`  [64-bit, immediate offset]  — ST2

**Assembly:** `ST2  { <Vt>.D, <Vt2>.D }[<index>], [<Xn|SP>], #16`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST2_asisdlsop_DX2_r2d`  [64-bit, register offset]  — ST2

**Assembly:** `ST2  { <Vt>.D, <Vt2>.D }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## ST2B (scalar plus immediate) -- A64

### Encoding: `st2b_z_p_bi_contiguous`  — ST2B

**Assembly:** `ST2B  { <Zt1>.B, <Zt2>.B }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## ST2B (scalar plus scalar) -- A64

### Encoding: `st2b_z_p_br_contiguous`  — ST2B

**Assembly:** `ST2B  { <Zt1>.B, <Zt2>.B }, <Pg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let nreg : integer{} = 2;
```

---

## ST2D (scalar plus immediate) -- A64

### Encoding: `st2d_z_p_bi_contiguous`  — ST2D

**Assembly:** `ST2D  { <Zt1>.D, <Zt2>.D }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## ST2D (scalar plus scalar) -- A64

### Encoding: `st2d_z_p_br_contiguous`  — ST2D

**Assembly:** `ST2D  { <Zt1>.D, <Zt2>.D }, <Pg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let nreg : integer{} = 2;
```

---

## ST2G -- A64

### Encoding: `ST2G_64Spost_ldsttags`  — ST2G

**Assembly:** `ST2G  <Xt|SP>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `ST2G_64Spre_ldsttags`  — ST2G

**Assembly:** `ST2G  <Xt|SP>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `ST2G_64Soffset_ldsttags`  — ST2G

**Assembly:** `ST2G  <Xt|SP>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## ST2H (scalar plus immediate) -- A64

### Encoding: `st2h_z_p_bi_contiguous`  — ST2H

**Assembly:** `ST2H  { <Zt1>.H, <Zt2>.H }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## ST2H (scalar plus scalar) -- A64

### Encoding: `st2h_z_p_br_contiguous`  — ST2H

**Assembly:** `ST2H  { <Zt1>.H, <Zt2>.H }, <Pg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let nreg : integer{} = 2;
```

---

## ST2Q (scalar plus immediate) -- A64

### Encoding: `st2q_z_p_bi_contiguous`  — ST2Q

**Assembly:** `ST2Q  { <Zt1>.Q, <Zt2>.Q }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## ST2Q (scalar plus scalar) -- A64

### Encoding: `st2q_z_p_br_contiguous`  — ST2Q

**Assembly:** `ST2Q  { <Zt1>.Q, <Zt2>.Q }, <Pg>, [<Xn|SP>, <Xm>, LSL #4]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let nreg : integer{} = 2;
```

---

## ST2W (scalar plus immediate) -- A64

### Encoding: `st2w_z_p_bi_contiguous`  — ST2W

**Assembly:** `ST2W  { <Zt1>.S, <Zt2>.S }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
let nreg : integer{} = 2;
```

---

## ST2W (scalar plus scalar) -- A64

### Encoding: `st2w_z_p_br_contiguous`  — ST2W

**Assembly:** `ST2W  { <Zt1>.S, <Zt2>.S }, <Pg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let nreg : integer{} = 2;
```

---

## ST3 (multiple structures) -- A64

### Encoding: `ST3_asisdlse_R3`  — ST3

**Assembly:** `ST3  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsep_I3_i`  [Immediate offset]  — ST3

**Assembly:** `ST3  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsep_R3_r`  [Register offset]  — ST3

**Assembly:** `ST3  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## ST3 (single structure) -- A64

### Encoding: `ST3_asisdlso_B3_3b`  [8-bit]  — ST3

**Assembly:** `ST3  { <Vt>.B, <Vt2>.B, <Vt3>.B }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlso_H3_3h`  [16-bit]  — ST3

**Assembly:** `ST3  { <Vt>.H, <Vt2>.H, <Vt3>.H }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlso_S3_3s`  [32-bit]  — ST3

**Assembly:** `ST3  { <Vt>.S, <Vt2>.S, <Vt3>.S }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlso_D3_3d`  [64-bit]  — ST3

**Assembly:** `ST3  { <Vt>.D, <Vt2>.D, <Vt3>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsop_B3_i3b`  [8-bit, immediate offset]  — ST3

**Assembly:** `ST3  { <Vt>.B, <Vt2>.B, <Vt3>.B }[<index>], [<Xn|SP>], #3`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsop_BX3_r3b`  [8-bit, register offset]  — ST3

**Assembly:** `ST3  { <Vt>.B, <Vt2>.B, <Vt3>.B }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsop_H3_i3h`  [16-bit, immediate offset]  — ST3

**Assembly:** `ST3  { <Vt>.H, <Vt2>.H, <Vt3>.H }[<index>], [<Xn|SP>], #6`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsop_HX3_r3h`  [16-bit, register offset]  — ST3

**Assembly:** `ST3  { <Vt>.H, <Vt2>.H, <Vt3>.H }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsop_S3_i3s`  [32-bit, immediate offset]  — ST3

**Assembly:** `ST3  { <Vt>.S, <Vt2>.S, <Vt3>.S }[<index>], [<Xn|SP>], #12`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsop_SX3_r3s`  [32-bit, register offset]  — ST3

**Assembly:** `ST3  { <Vt>.S, <Vt2>.S, <Vt3>.S }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsop_D3_i3d`  [64-bit, immediate offset]  — ST3

**Assembly:** `ST3  { <Vt>.D, <Vt2>.D, <Vt3>.D }[<index>], [<Xn|SP>], #24`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST3_asisdlsop_DX3_r3d`  [64-bit, register offset]  — ST3

**Assembly:** `ST3  { <Vt>.D, <Vt2>.D, <Vt3>.D }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## ST3B (scalar plus immediate) -- A64

### Encoding: `st3b_z_p_bi_contiguous`  — ST3B

**Assembly:** `ST3B  { <Zt1>.B, <Zt2>.B, <Zt3>.B }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## ST3B (scalar plus scalar) -- A64

### Encoding: `st3b_z_p_br_contiguous`  — ST3B

**Assembly:** `ST3B  { <Zt1>.B, <Zt2>.B, <Zt3>.B }, <Pg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let nreg : integer{} = 3;
```

---

## ST3D (scalar plus immediate) -- A64

### Encoding: `st3d_z_p_bi_contiguous`  — ST3D

**Assembly:** `ST3D  { <Zt1>.D, <Zt2>.D, <Zt3>.D }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## ST3D (scalar plus scalar) -- A64

### Encoding: `st3d_z_p_br_contiguous`  — ST3D

**Assembly:** `ST3D  { <Zt1>.D, <Zt2>.D, <Zt3>.D }, <Pg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let nreg : integer{} = 3;
```

---

## ST3H (scalar plus immediate) -- A64

### Encoding: `st3h_z_p_bi_contiguous`  — ST3H

**Assembly:** `ST3H  { <Zt1>.H, <Zt2>.H, <Zt3>.H }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## ST3H (scalar plus scalar) -- A64

### Encoding: `st3h_z_p_br_contiguous`  — ST3H

**Assembly:** `ST3H  { <Zt1>.H, <Zt2>.H, <Zt3>.H }, <Pg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let nreg : integer{} = 3;
```

---

## ST3Q (scalar plus immediate) -- A64

### Encoding: `st3q_z_p_bi_contiguous`  — ST3Q

**Assembly:** `ST3Q  { <Zt1>.Q, <Zt2>.Q, <Zt3>.Q }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## ST3Q (scalar plus scalar) -- A64

### Encoding: `st3q_z_p_br_contiguous`  — ST3Q

**Assembly:** `ST3Q  { <Zt1>.Q, <Zt2>.Q, <Zt3>.Q }, <Pg>, [<Xn|SP>, <Xm>, LSL #4]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let nreg : integer{} = 3;
```

---

## ST3W (scalar plus immediate) -- A64

### Encoding: `st3w_z_p_bi_contiguous`  — ST3W

**Assembly:** `ST3W  { <Zt1>.S, <Zt2>.S, <Zt3>.S }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 3 in the range -24 to 21, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
let nreg : integer{} = 3;
```

---

## ST3W (scalar plus scalar) -- A64

### Encoding: `st3w_z_p_br_contiguous`  — ST3W

**Assembly:** `ST3W  { <Zt1>.S, <Zt2>.S, <Zt3>.S }, <Pg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let nreg : integer{} = 3;
```

---

## ST4 (multiple structures) -- A64

### Encoding: `ST4_asisdlse_R4`  — ST4

**Assembly:** `ST4  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsep_I4_i`  [Immediate offset]  — ST4

**Assembly:** `ST4  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsep_R4_r`  [Register offset]  — ST4

**Assembly:** `ST4  { <Vt>.<T>, <Vt2>.<T>, <Vt3>.<T>, <Vt4>.<T> }, [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<T>` | — |  |  |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | let t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## ST4 (single structure) -- A64

### Encoding: `ST4_asisdlso_B4_4b`  [8-bit]  — ST4

**Assembly:** `ST4  { <Vt>.B, <Vt2>.B, <Vt3>.B, <Vt4>.B }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlso_H4_4h`  [16-bit]  — ST4

**Assembly:** `ST4  { <Vt>.H, <Vt2>.H, <Vt3>.H, <Vt4>.H }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlso_S4_4s`  [32-bit]  — ST4

**Assembly:** `ST4  { <Vt>.S, <Vt2>.S, <Vt3>.S, <Vt4>.S }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlso_D4_4d`  [64-bit]  — ST4

**Assembly:** `ST4  { <Vt>.D, <Vt2>.D, <Vt3>.D, <Vt4>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsop_B4_i4b`  [8-bit, immediate offset]  — ST4

**Assembly:** `ST4  { <Vt>.B, <Vt2>.B, <Vt3>.B, <Vt4>.B }[<index>], [<Xn|SP>], #4`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsop_BX4_r4b`  [8-bit, register offset]  — ST4

**Assembly:** `ST4  { <Vt>.B, <Vt2>.B, <Vt3>.B, <Vt4>.B }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "8-bit", "8-bit, immediate offset", and "8-bit, register offset" variants: is the element index, encoded in "Q:S |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsop_H4_i4h`  [16-bit, immediate offset]  — ST4

**Assembly:** `ST4  { <Vt>.H, <Vt2>.H, <Vt3>.H, <Vt4>.H }[<index>], [<Xn|SP>], #8`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsop_HX4_r4h`  [16-bit, register offset]  — ST4

**Assembly:** `ST4  { <Vt>.H, <Vt2>.H, <Vt3>.H, <Vt4>.H }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S :: size) | For the "16-bit", "16-bit, immediate offset", and "16-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsop_S4_i4s`  [32-bit, immediate offset]  — ST4

**Assembly:** `ST4  { <Vt>.S, <Vt2>.S, <Vt3>.S, <Vt4>.S }[<index>], [<Xn|SP>], #16`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsop_SX4_r4s`  [32-bit, register offset]  — ST4

**Assembly:** `ST4  { <Vt>.S, <Vt2>.S, <Vt3>.S, <Vt4>.S }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | (Q :: S) | For the "32-bit", "32-bit, immediate offset", and "32-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsop_D4_i4d`  [64-bit, immediate offset]  — ST4

**Assembly:** `ST4  { <Vt>.D, <Vt2>.D, <Vt3>.D, <Vt4>.D }[<index>], [<Xn|SP>], #32`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

### Encoding: `ST4_asisdlsop_DX4_r4d`  [64-bit, register offset]  — ST4

**Assembly:** `ST4  { <Vt>.D, <Vt2>.D, <Vt3>.D, <Vt4>.D }[<index>], [<Xn|SP>], <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<Vt2>` | Rt (5b) | Is the name of the second SIMD&FP register to be transferred, encoded as "Rt" plus 1 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt3>` | Rt (5b) | Is the name of the third SIMD&FP register to be transferred, encoded as "Rt" plus 2 modulo 32. | var t : integer = UInt(Rt); |
| `<Vt4>` | Rt (5b) | Is the name of the fourth SIMD&FP register to be transferred, encoded as "Rt" plus 3 modulo 32. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | For the "64-bit", "64-bit, immediate offset", and "64-bit, register offset" variants: is the element index, encoded in " |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose post-index register, excluding XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let wback : boolean = TRUE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## ST4B (scalar plus immediate) -- A64

### Encoding: `st4b_z_p_bi_contiguous`  — ST4B

**Assembly:** `ST4B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## ST4B (scalar plus scalar) -- A64

### Encoding: `st4b_z_p_br_contiguous`  — ST4B

**Assembly:** `ST4B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <Pg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let nreg : integer{} = 4;
```

---

## ST4D (scalar plus immediate) -- A64

### Encoding: `st4d_z_p_bi_contiguous`  — ST4D

**Assembly:** `ST4D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## ST4D (scalar plus scalar) -- A64

### Encoding: `st4d_z_p_br_contiguous`  — ST4D

**Assembly:** `ST4D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <Pg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let nreg : integer{} = 4;
```

---

## ST4H (scalar plus immediate) -- A64

### Encoding: `st4h_z_p_bi_contiguous`  — ST4H

**Assembly:** `ST4H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## ST4H (scalar plus scalar) -- A64

### Encoding: `st4h_z_p_br_contiguous`  — ST4H

**Assembly:** `ST4H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <Pg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let nreg : integer{} = 4;
```

---

## ST4Q (scalar plus immediate) -- A64

### Encoding: `st4q_z_p_bi_contiguous`  — ST4Q

**Assembly:** `ST4Q  { <Zt1>.Q, <Zt2>.Q, <Zt3>.Q, <Zt4>.Q }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## ST4Q (scalar plus scalar) -- A64

### Encoding: `st4q_z_p_br_contiguous`  — ST4Q

**Assembly:** `ST4Q  { <Zt1>.Q, <Zt2>.Q, <Zt3>.Q, <Zt4>.Q }, <Pg>, [<Xn|SP>, <Xm>, LSL #4]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 128;
let nreg : integer{} = 4;
```

---

## ST4W (scalar plus immediate) -- A64

### Encoding: `st4w_z_p_bi_contiguous`  — ST4W

**Assembly:** `ST4W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28, defaulting to 0, encoded in the  | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
let nreg : integer{} = 4;
```

---

## ST4W (scalar plus scalar) -- A64

### Encoding: `st4w_z_p_br_contiguous`  — ST4W

**Assembly:** `ST4W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <Pg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (5b) | Is the name of the first scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Zt2>` | Zt (5b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" plus 1 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt3>` | Zt (5b) | Is the name of the third scalable vector register to be transferred, encoded as "Zt" plus 2 modulo 32. | let t : integer = UInt(Zt); |
| `<Zt4>` | Zt (5b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" plus 3 modulo 32. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let nreg : integer{} = 4;
```

---

## ST64B -- A64

### Encoding: `ST64B_64L_memop`  — ST64B

**Assembly:** `ST64B  <Xt>, [<Xn|SP> {, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt[4:3] == '11' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LS64) then EndOfDecode(Decode_UNDEF); end;
if Rt[4:3] == '11' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let withstatus : boolean = FALSE;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let tagchecked : boolean = n != 31;
```

---

## ST64BV -- A64

### Encoding: `ST64BV_64_memop`  — ST64BV

**Assembly:** `ST64BV  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register into which the status result of this instruction is written, encoded  | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt[4:3] == '11' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LS64_V) then EndOfDecode(Decode_UNDEF); end;
if Rt[4:3] == '11' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let withstatus : boolean = TRUE;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let tagchecked : boolean = n != 31;
```

---

## ST64BV0 -- A64

### Encoding: `ST64BV0_64_memop`  — ST64BV0

**Assembly:** `ST64BV0  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register into which the status result of this instruction is written, encoded  | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt[4:3] == '11' \|\| Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LS64_ACCDATA) then EndOfDecode(Decode_UNDEF); end;
if Rt[4:3] == '11' || Rt[0] == '1' then EndOfDecode(Decode_UNDEF); end;
let withstatus : boolean = TRUE;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let tagchecked : boolean = n != 31;
```

---

## STADD, STADDL -- A64 *(alias for LDADD)*

### Encoding: `STADD_LDADD_32_memop`  [32-bit no memory ordering]  — LDADD

**Assembly:** `STADD  <Ws>, [<Xn|SP>] / LDADD  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDADD` | — |  |  |

### Encoding: `STADDL_LDADDL_32_memop`  [32-bit release]  — LDADDL

**Assembly:** `STADDL  <Ws>, [<Xn|SP>] / LDADDL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDADDL` | — |  |  |

### Encoding: `STADD_LDADD_64_memop`  [64-bit no memory ordering]  — LDADD

**Assembly:** `STADD  <Xs>, [<Xn|SP>] / LDADD  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDADD` | — |  |  |

### Encoding: `STADDL_LDADDL_64_memop`  [64-bit release]  — LDADDL

**Assembly:** `STADDL  <Xs>, [<Xn|SP>] / LDADDL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDADDL` | — |  |  |

---

## STADDB, STADDLB -- A64 *(alias for LDADDB)*

### Encoding: `STADDB_LDADDB_32_memop`  [No memory ordering]  — LDADDB

**Assembly:** `STADDB  <Ws>, [<Xn|SP>] / LDADDB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDADDB` | — |  |  |

### Encoding: `STADDLB_LDADDLB_32_memop`  [Release]  — LDADDLB

**Assembly:** `STADDLB  <Ws>, [<Xn|SP>] / LDADDLB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDADDLB` | — |  |  |

---

## STADDH, STADDLH -- A64 *(alias for LDADDH)*

### Encoding: `STADDH_LDADDH_32_memop`  [No memory ordering]  — LDADDH

**Assembly:** `STADDH  <Ws>, [<Xn|SP>] / LDADDH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDADDH` | — |  |  |

### Encoding: `STADDLH_LDADDLH_32_memop`  [Release]  — LDADDLH

**Assembly:** `STADDLH  <Ws>, [<Xn|SP>] / LDADDLH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDADDLH` | — |  |  |

---

## STBFADD, STBFADDL -- A64

### Encoding: `STBFADD_16`  [No memory ordering]  — STBFADD

**Assembly:** `STBFADD  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STBFADDL_16`  [Release]  — STBFADDL

**Assembly:** `STBFADDL  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STBFMAX, STBFMAXL -- A64

### Encoding: `STBFMAX_16`  [No memory ordering]  — STBFMAX

**Assembly:** `STBFMAX  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STBFMAXL_16`  [Release]  — STBFMAXL

**Assembly:** `STBFMAXL  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STBFMAXNM, STBFMAXNML -- A64

### Encoding: `STBFMAXNM_16`  [No memory ordering]  — STBFMAXNM

**Assembly:** `STBFMAXNM  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STBFMAXNML_16`  [Release]  — STBFMAXNML

**Assembly:** `STBFMAXNML  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STBFMIN, STBFMINL -- A64

### Encoding: `STBFMIN_16`  [No memory ordering]  — STBFMIN

**Assembly:** `STBFMIN  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STBFMINL_16`  [Release]  — STBFMINL

**Assembly:** `STBFMINL  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STBFMINNM, STBFMINNML -- A64

### Encoding: `STBFMINNM_16`  [No memory ordering]  — STBFMINNM

**Assembly:** `STBFMINNM  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STBFMINNML_16`  [Release]  — STBFMINNML

**Assembly:** `STBFMINNML  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STCLR, STCLRL -- A64 *(alias for LDCLR)*

### Encoding: `STCLR_LDCLR_32_memop`  [32-bit no memory ordering]  — LDCLR

**Assembly:** `STCLR  <Ws>, [<Xn|SP>] / LDCLR  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDCLR` | — |  |  |

### Encoding: `STCLRL_LDCLRL_32_memop`  [32-bit release]  — LDCLRL

**Assembly:** `STCLRL  <Ws>, [<Xn|SP>] / LDCLRL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDCLRL` | — |  |  |

### Encoding: `STCLR_LDCLR_64_memop`  [64-bit no memory ordering]  — LDCLR

**Assembly:** `STCLR  <Xs>, [<Xn|SP>] / LDCLR  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDCLR` | — |  |  |

### Encoding: `STCLRL_LDCLRL_64_memop`  [64-bit release]  — LDCLRL

**Assembly:** `STCLRL  <Xs>, [<Xn|SP>] / LDCLRL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDCLRL` | — |  |  |

---

## STCLRB, STCLRLB -- A64 *(alias for LDCLRB)*

### Encoding: `STCLRB_LDCLRB_32_memop`  [No memory ordering]  — LDCLRB

**Assembly:** `STCLRB  <Ws>, [<Xn|SP>] / LDCLRB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDCLRB` | — |  |  |

### Encoding: `STCLRLB_LDCLRLB_32_memop`  [Release]  — LDCLRLB

**Assembly:** `STCLRLB  <Ws>, [<Xn|SP>] / LDCLRLB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDCLRLB` | — |  |  |

---

## STCLRH, STCLRLH -- A64 *(alias for LDCLRH)*

### Encoding: `STCLRH_LDCLRH_32_memop`  [No memory ordering]  — LDCLRH

**Assembly:** `STCLRH  <Ws>, [<Xn|SP>] / LDCLRH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDCLRH` | — |  |  |

### Encoding: `STCLRLH_LDCLRLH_32_memop`  [Release]  — LDCLRLH

**Assembly:** `STCLRLH  <Ws>, [<Xn|SP>] / LDCLRLH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDCLRLH` | — |  |  |

---

## STCPH -- A64

### Encoding: `STCPH_HI_hints`  — STCPH

**Assembly:** `STCPH`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CMH) then EndOfDecode(Decode_NOP); end;
```

---

## STEOR, STEORL -- A64 *(alias for LDEOR)*

### Encoding: `STEOR_LDEOR_32_memop`  [32-bit no memory ordering]  — LDEOR

**Assembly:** `STEOR  <Ws>, [<Xn|SP>] / LDEOR  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDEOR` | — |  |  |

### Encoding: `STEORL_LDEORL_32_memop`  [32-bit release]  — LDEORL

**Assembly:** `STEORL  <Ws>, [<Xn|SP>] / LDEORL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDEORL` | — |  |  |

### Encoding: `STEOR_LDEOR_64_memop`  [64-bit no memory ordering]  — LDEOR

**Assembly:** `STEOR  <Xs>, [<Xn|SP>] / LDEOR  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDEOR` | — |  |  |

### Encoding: `STEORL_LDEORL_64_memop`  [64-bit release]  — LDEORL

**Assembly:** `STEORL  <Xs>, [<Xn|SP>] / LDEORL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDEORL` | — |  |  |

---

## STEORB, STEORLB -- A64 *(alias for LDEORB)*

### Encoding: `STEORB_LDEORB_32_memop`  [No memory ordering]  — LDEORB

**Assembly:** `STEORB  <Ws>, [<Xn|SP>] / LDEORB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDEORB` | — |  |  |

### Encoding: `STEORLB_LDEORLB_32_memop`  [Release]  — LDEORLB

**Assembly:** `STEORLB  <Ws>, [<Xn|SP>] / LDEORLB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDEORLB` | — |  |  |

---

## STEORH, STEORLH -- A64 *(alias for LDEORH)*

### Encoding: `STEORH_LDEORH_32_memop`  [No memory ordering]  — LDEORH

**Assembly:** `STEORH  <Ws>, [<Xn|SP>] / LDEORH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDEORH` | — |  |  |

### Encoding: `STEORLH_LDEORLH_32_memop`  [Release]  — LDEORLH

**Assembly:** `STEORLH  <Ws>, [<Xn|SP>] / LDEORLH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDEORLH` | — |  |  |

---

## STFADD, STFADDL -- A64

### Encoding: `STFADD_16`  [Half-precision no memory ordering]  — STFADD

**Assembly:** `STFADD  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFADDL_16`  [Half-precision release]  — STFADDL

**Assembly:** `STFADDL  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFADD_32`  [Single-precision no memory ordering]  — STFADD

**Assembly:** `STFADD  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFADDL_32`  [Single-precision release]  — STFADDL

**Assembly:** `STFADDL  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFADD_64`  [Double-precision no memory ordering]  — STFADD

**Assembly:** `STFADD  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFADDL_64`  [Double-precision release]  — STFADDL

**Assembly:** `STFADDL  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STFMAX, STFMAXL -- A64

### Encoding: `STFMAX_16`  [Half-precision no memory ordering]  — STFMAX

**Assembly:** `STFMAX  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAXL_16`  [Half-precision release]  — STFMAXL

**Assembly:** `STFMAXL  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAX_32`  [Single-precision no memory ordering]  — STFMAX

**Assembly:** `STFMAX  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAXL_32`  [Single-precision release]  — STFMAXL

**Assembly:** `STFMAXL  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAX_64`  [Double-precision no memory ordering]  — STFMAX

**Assembly:** `STFMAX  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAXL_64`  [Double-precision release]  — STFMAXL

**Assembly:** `STFMAXL  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STFMAXNM, STFMAXNML -- A64

### Encoding: `STFMAXNM_16`  [Half-precision no memory ordering]  — STFMAXNM

**Assembly:** `STFMAXNM  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAXNML_16`  [Half-precision release]  — STFMAXNML

**Assembly:** `STFMAXNML  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAXNM_32`  [Single-precision no memory ordering]  — STFMAXNM

**Assembly:** `STFMAXNM  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAXNML_32`  [Single-precision release]  — STFMAXNML

**Assembly:** `STFMAXNML  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAXNM_64`  [Double-precision no memory ordering]  — STFMAXNM

**Assembly:** `STFMAXNM  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMAXNML_64`  [Double-precision release]  — STFMAXNML

**Assembly:** `STFMAXNML  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STFMIN, STFMINL -- A64

### Encoding: `STFMIN_16`  [Half-precision no memory ordering]  — STFMIN

**Assembly:** `STFMIN  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMINL_16`  [Half-precision release]  — STFMINL

**Assembly:** `STFMINL  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMIN_32`  [Single-precision no memory ordering]  — STFMIN

**Assembly:** `STFMIN  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMINL_32`  [Single-precision release]  — STFMINL

**Assembly:** `STFMINL  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMIN_64`  [Double-precision no memory ordering]  — STFMIN

**Assembly:** `STFMIN  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMINL_64`  [Double-precision release]  — STFMINL

**Assembly:** `STFMINL  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STFMINNM, STFMINNML -- A64

### Encoding: `STFMINNM_16`  [Half-precision no memory ordering]  — STFMINNM

**Assembly:** `STFMINNM  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMINNML_16`  [Half-precision release]  — STFMINNML

**Assembly:** `STFMINNML  <Hs>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hs>` | Rs (5b) | Is the 16-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMINNM_32`  [Single-precision no memory ordering]  — STFMINNM

**Assembly:** `STFMINNM  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMINNML_32`  [Single-precision release]  — STFMINNML

**Assembly:** `STFMINNML  <Ss>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ss>` | Rs (5b) | Is the 32-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMINNM_64`  [Double-precision no memory ordering]  — STFMINNM

**Assembly:** `STFMINNM  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `STFMINNML_64`  [Double-precision release]  — STFMINNML

**Assembly:** `STFMINNML  <Ds>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ds>` | Rs (5b) | Is the 64-bit name of the SIMD&FP register holding the data value to be operated on with the contents of the memory loca | let s : integer = UInt(Rs); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSFE) then EndOfDecode(Decode_UNDEF); end;

let s : integer = UInt(Rs);
let n : integer = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## STG -- A64

### Encoding: `STG_64Spost_ldsttags`  — STG

**Assembly:** `STG  <Xt|SP>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STG_64Spre_ldsttags`  — STG

**Assembly:** `STG  <Xt|SP>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STG_64Soffset_ldsttags`  — STG

**Assembly:** `STG  <Xt|SP>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## STGM -- A64

### Encoding: `STGM_64bulk_ldsttags`  — STGM

**Assembly:** `STGM  <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
```

---

## STGP -- A64

### Encoding: `STGP_64_ldstpair_post`  — STGP

**Assembly:** `STGP  <Xt1>, <Xt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | simm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate offset, a multiple of 16 in the range -1024 to 10 | let offset : bits(64) = LSL(SignExtend{64}(simm7), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(simm7), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STGP_64_ldstpair_pre`  — STGP

**Assembly:** `STGP  <Xt1>, <Xt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | simm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate offset, a multiple of 16 in the range -1024 to 10 | let offset : bits(64) = LSL(SignExtend{64}(simm7), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(simm7), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STGP_64_ldstpair_off`  — STGP

**Assembly:** `STGP  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | simm7 (7b) | For the "Signed offset" variant: is the optional signed immediate offset, a multiple of 16 in the range -1024 to 1008, d | let offset : bits(64) = LSL(SignExtend{64}(simm7), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(simm7), LOG2_TAG_GRANULE);
let writeback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## STILP -- A64

### Encoding: `STILP_32SE_ldiappstilp`  [32-bit pre-index]  — STILP

**Assembly:** `STILP  <Wt1>, <Wt2>, [<Xn|SP>, #-8]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let ispair : boolean = TRUE;
let wback : boolean = opc2[0] == '0';
```

### Encoding: `STILP_32S_ldiappstilp`  [32-bit]  — STILP

**Assembly:** `STILP  <Wt1>, <Wt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let ispair : boolean = TRUE;
let wback : boolean = opc2[0] == '0';
```

### Encoding: `STILP_64SS_ldiappstilp`  [64-bit pre-index]  — STILP

**Assembly:** `STILP  <Xt1>, <Xt2>, [<Xn|SP>, #-16]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let ispair : boolean = TRUE;
let wback : boolean = opc2[0] == '0';
```

### Encoding: `STILP_64S_ldiappstilp`  [64-bit]  — STILP

**Assembly:** `STILP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let ispair : boolean = TRUE;
let wback : boolean = opc2[0] == '0';
```

---

## STL1 (SIMD&FP) -- A64

### Encoding: `STL1_asisdlso_D1`  — STL1

**Assembly:** `STL1  { <Vt>.D }[<index>], [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vt>` | Rt (5b) | Is the name of the first or only SIMD&FP register to be transferred, encoded in the "Rt" field. | var t : integer = UInt(Rt); |
| `<index>` | Q (1b) | Is the element index, encoded in "Q". |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
var t : integer = UInt(Rt);
let n : integer = UInt(Rn);
let m : integer = ARBITRARY : integer;
let wback : boolean = FALSE;
let nontemporal : boolean = FALSE;
let tagchecked : boolean = wback || n != 31;
```

---

## STLLR -- A64

### Encoding: `STLLR_SL32_ldstord`  [32-bit]  — STLLR

**Assembly:** `STLLR  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LOR) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let elsize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

### Encoding: `STLLR_SL64_ldstord`  [64-bit]  — STLLR

**Assembly:** `STLLR  <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LOR) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let elsize : integer{} = 8 << UInt(size);
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## STLLRB -- A64

### Encoding: `STLLRB_SL32_ldstord`  — STLLRB

**Assembly:** `STLLRB  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LOR) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## STLLRH -- A64

### Encoding: `STLLRH_SL32_ldstord`  — STLLRH

**Assembly:** `STLLRH  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LOR) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## STLP -- A64

### Encoding: `STLP_64_ldiappstilp`  — STLP

**Assembly:** `STLP  <Xt1>, <Xt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSCP) then EndOfDecode(Decode_UNDEF); end;

let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
let ispair : boolean = TRUE;
```

---

## STLR -- A64

### Encoding: `STLR_SL32_ldstord`  [32-bit]  — STLR

**Assembly:** `STLR  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let wback : boolean = FALSE;
let offset : integer = 0;
let rt_unknown : boolean = FALSE;

let elsize : integer{} = 8 << UInt(size);
let datasize : integer{} = elsize;
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

### Encoding: `STLR_SL64_ldstord`  [64-bit]  — STLR

**Assembly:** `STLR  <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let wback : boolean = FALSE;
let offset : integer = 0;
let rt_unknown : boolean = FALSE;

let elsize : integer{} = 8 << UInt(size);
let datasize : integer{} = elsize;
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

### Encoding: `STLR_32S_ldapstl_writeback`  [32-bit]  — STLR

**Assembly:** `STLR  <Wt>, [<Xn|SP>, #-4]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let wback : boolean = TRUE;

let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let offset : integer = -1 * (1 << UInt(size));
let acquire : boolean = FALSE;
let tagchecked : boolean = TRUE;

var rt_unknown : boolean = FALSE;

if n == t && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_WBOVERLAPST);
    assert c IN {Constraint_NONE, Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_NONE =>    rt_unknown = FALSE;   // value stored is original value
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // value stored is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `STLR_64S_ldapstl_writeback`  [64-bit]  — STLR

**Assembly:** `STLR  <Xt>, [<Xn|SP>, #-8]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC3) then EndOfDecode(Decode_UNDEF); end;
let wback : boolean = TRUE;

let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let offset : integer = -1 * (1 << UInt(size));
let acquire : boolean = FALSE;
let tagchecked : boolean = TRUE;

var rt_unknown : boolean = FALSE;

if n == t && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_WBOVERLAPST);
    assert c IN {Constraint_NONE, Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_NONE =>    rt_unknown = FALSE;   // value stored is original value
        when Constraint_UNKNOWN => rt_unknown = TRUE;    // value stored is UNKNOWN
        when Constraint_UNDEF =>   EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>     EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STLRB -- A64

### Encoding: `STLRB_SL32_ldstord`  — STLRB

**Assembly:** `STLRB  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## STLRH -- A64

### Encoding: `STLRH_SL32_ldstord`  — STLRH

**Assembly:** `STLRH  <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## STLTXR -- A64

### Encoding: `STLTXR_SR32_ldstexclr_unpriv`  [32-bit]  — STLTXR

**Assembly:** `STLTXR  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let acqrel : boolean = TRUE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `STLTXR_SR64_ldstexclr_unpriv`  [64-bit]  — STLTXR

**Assembly:** `STLTXR  <Ws>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let acqrel : boolean = TRUE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STLUR (SIMD&FP) -- A64

### Encoding: `STLUR_B_ldapstl_simd`  [8-bit]  — STLUR

**Assembly:** `STLUR  <Bt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STLUR_H_ldapstl_simd`  [16-bit]  — STLUR

**Assembly:** `STLUR  <Ht>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STLUR_S_ldapstl_simd`  [32-bit]  — STLUR

**Assembly:** `STLUR  <St>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STLUR_D_ldapstl_simd`  [64-bit]  — STLUR

**Assembly:** `STLUR  <Dt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STLUR_Q_ldapstl_simd`  [128-bit]  — STLUR

**Assembly:** `STLUR  <Qt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LRCPC3) then
    EndOfDecode(Decode_UNDEF);
end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

---

## STLUR -- A64

### Encoding: `STLUR_32_ldapstl_unscaled`  [32-bit]  — STLUR

**Assembly:** `STLUR  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
let n : integer = UInt(Rn);
let t : integer = UInt(Rt);

let datasize : integer{} = 8 << scale;
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

### Encoding: `STLUR_64_ldapstl_unscaled`  [64-bit]  — STLUR

**Assembly:** `STLUR  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
let n : integer = UInt(Rn);
let t : integer = UInt(Rt);

let datasize : integer{} = 8 << scale;
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## STLURB -- A64

### Encoding: `STLURB_32_ldapstl_unscaled`  — STLURB

**Assembly:** `STLURB  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm9);
let n : integer = UInt(Rn);
let t : integer = UInt(Rt);

let datasize : integer{} = 8;
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## STLURH -- A64

### Encoding: `STLURH_32_ldapstl_unscaled`  — STLURH

**Assembly:** `STLURH  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LRCPC2) then EndOfDecode(Decode_UNDEF); end;
let offset : bits(64) = SignExtend{}(imm9);
let n : integer = UInt(Rn);
let t : integer = UInt(Rt);

let datasize : integer{} = 16;
let acquire : boolean = FALSE;
let tagchecked : boolean = n != 31;
```

---

## STLXP -- A64

### Encoding: `STLXP_SP32_ldstexclp`  [32-bit]  — STLXP

**Assembly:** `STLXP  <Ws>, <Wt1>, <Wt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let datasize : integer{} = elsize * 2;
let acqrel : boolean = TRUE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t || (s == t2) then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `STLXP_SP64_ldstexclp`  [64-bit]  — STLXP

**Assembly:** `STLXP  <Ws>, <Xt1>, <Xt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let datasize : integer{} = elsize * 2;
let acqrel : boolean = TRUE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t || (s == t2) then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STLXR -- A64

### Encoding: `STLXR_SR32_ldstexclr`  [32-bit]  — STLXR

**Assembly:** `STLXR  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 8 << UInt(size);
let acqrel : boolean = TRUE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `STLXR_SR64_ldstexclr`  [64-bit]  — STLXR

**Assembly:** `STLXR  <Ws>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 8 << UInt(size);
let acqrel : boolean = TRUE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STLXRB -- A64

### Encoding: `STLXRB_SR32_ldstexclr`  — STLXRB

**Assembly:** `STLXRB  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acqrel : boolean = TRUE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STLXRH -- A64

### Encoding: `STLXRH_SR32_ldstexclr`  — STLXRH

**Assembly:** `STLXRH  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acqrel : boolean = TRUE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STMOPA (2-way) -- A64

### Encoding: `stmopa_za32_zzzi_h2x1`  — STMOPA

**Assembly:** `STMOPA  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
let unsigned : boolean = FALSE;
```

---

## STMOPA (4-way) -- A64

### Encoding: `stmopa_za_zzzi_b2x1`  — STMOPA

**Assembly:** `STMOPA  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = FALSE;
```

---

## STNP (SIMD&FP) -- A64

### Encoding: `STNP_S_ldstnapair_offs`  [32-bit]  — STNP

**Assembly:** `STNP  <St1>, <St2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St1>` | Rt (5b) | Is the 32-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<St2>` | Rt2 (5b) | Is the 32-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "32-bit" variant: is the optional signed immediate byte offset, a multiple of 4 in the range -256 to 252, defaul |  |

**Pseudocode (Decode):**
```
// Empty.
```

### Encoding: `STNP_D_ldstnapair_offs`  [64-bit]  — STNP

**Assembly:** `STNP  <Dt1>, <Dt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt1>` | Rt (5b) | Is the 64-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Dt2>` | Rt2 (5b) | Is the 64-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "64-bit" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512 to 504, defaul |  |

**Pseudocode (Decode):**
```
// Empty.
```

### Encoding: `STNP_Q_ldstnapair_offs`  [128-bit]  — STNP

**Assembly:** `STNP  <Qt1>, <Qt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "128-bit" variant: is the optional signed immediate byte offset, a multiple of 16 in the range -1024 to 1008, de |  |

**Pseudocode (Decode):**
```
// Empty.
```

---

## STNP -- A64

### Encoding: `STNP_32_ldstnapair_offs`  [32-bit]  — STNP

**Assembly:** `STNP  <Wt1>, <Wt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm7 (7b) | For the "32-bit" variant: is the optional signed immediate byte offset, a multiple of 4 in the range -256 to 252, defaul | let offset : bits(64) = LSL(SignExtend{64}(imm7), scale); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let nontemporal : boolean = TRUE;
let scale : integer{} = 2 + UInt(opc[1]);
let datasize : integer{} = 8 << scale;
let offset : bits(64) = LSL(SignExtend{64}(imm7), scale);
let tagchecked : boolean = n != 31;
```

### Encoding: `STNP_64_ldstnapair_offs`  [64-bit]  — STNP

**Assembly:** `STNP  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm7 (7b) | For the "64-bit" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512 to 504, defaul | let offset : bits(64) = LSL(SignExtend{64}(imm7), scale); |

**Pseudocode (Decode):**
```
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let nontemporal : boolean = TRUE;
let scale : integer{} = 2 + UInt(opc[1]);
let datasize : integer{} = 8 << scale;
let offset : bits(64) = LSL(SignExtend{64}(imm7), scale);
let tagchecked : boolean = n != 31;
```

---

## STNT1B (scalar plus immediate, consecutive registers) -- A64

### Encoding: `stnt1b_mz_p_bi_2`  — STNT1B

**Assembly:** `STNT1B  { <Zt1>.B-<Zt2>.B }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

### Encoding: `stnt1b_mz_p_bi_4`  — STNT1B

**Assembly:** `STNT1B  { <Zt1>.B-<Zt4>.B }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## STNT1B (scalar plus immediate, single register) -- A64

### Encoding: `stnt1b_z_p_bi_contiguous`  — STNT1B

**Assembly:** `STNT1B  { <Zt>.B }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## STNT1B (scalar plus immediate, strided registers) -- A64

### Encoding: `stnt1b_mzx_p_bi_2x8`  — STNT1B

**Assembly:** `STNT1B  { <Zt1>.B, <Zt2>.B }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

### Encoding: `stnt1b_mzx_p_bi_4x4`  — STNT1B

**Assembly:** `STNT1B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 8;
let offset : integer = SInt(imm4);
```

---

## STNT1B (scalar plus scalar, consecutive registers) -- A64

### Encoding: `stnt1b_mz_p_br_2`  — STNT1B

**Assembly:** `STNT1B  { <Zt1>.B-<Zt2>.B }, <PNg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 8;
```

### Encoding: `stnt1b_mz_p_br_4`  — STNT1B

**Assembly:** `STNT1B  { <Zt1>.B-<Zt4>.B }, <PNg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 8;
```

---

## STNT1B (scalar plus scalar, single register) -- A64

### Encoding: `stnt1b_z_p_br_contiguous`  — STNT1B

**Assembly:** `STNT1B  { <Zt>.B }, <Pg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 8;
```

---

## STNT1B (scalar plus scalar, strided registers) -- A64

### Encoding: `stnt1b_mzx_p_br_2x8`  — STNT1B

**Assembly:** `STNT1B  { <Zt1>.B, <Zt2>.B }, <PNg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 8;
```

### Encoding: `stnt1b_mzx_p_br_4x4`  — STNT1B

**Assembly:** `STNT1B  { <Zt1>.B, <Zt2>.B, <Zt3>.B, <Zt4>.B }, <PNg>, [<Xn|SP>, <Xm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 8;
```

---

## STNT1B (vector plus scalar) -- A64

### Encoding: `stnt1b_z_p_ar_s_x32_unscaled`  — STNT1B

**Assembly:** `STNT1B  { <Zt>.S }, <Pg>, [<Zn>.S{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
```

### Encoding: `stnt1b_z_p_ar_d_64_unscaled`  — STNT1B

**Assembly:** `STNT1B  { <Zt>.D }, <Pg>, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
```

---

## STNT1D (scalar plus immediate, consecutive registers) -- A64

### Encoding: `stnt1d_mz_p_bi_2`  — STNT1D

**Assembly:** `STNT1D  { <Zt1>.D-<Zt2>.D }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

### Encoding: `stnt1d_mz_p_bi_4`  — STNT1D

**Assembly:** `STNT1D  { <Zt1>.D-<Zt4>.D }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## STNT1D (scalar plus immediate, single register) -- A64

### Encoding: `stnt1d_z_p_bi_contiguous`  — STNT1D

**Assembly:** `STNT1D  { <Zt>.D }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## STNT1D (scalar plus immediate, strided registers) -- A64

### Encoding: `stnt1d_mzx_p_bi_2x8`  — STNT1D

**Assembly:** `STNT1D  { <Zt1>.D, <Zt2>.D }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

### Encoding: `stnt1d_mzx_p_bi_4x4`  — STNT1D

**Assembly:** `STNT1D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 64;
let offset : integer = SInt(imm4);
```

---

## STNT1D (scalar plus scalar, consecutive registers) -- A64

### Encoding: `stnt1d_mz_p_br_2`  — STNT1D

**Assembly:** `STNT1D  { <Zt1>.D-<Zt2>.D }, <PNg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 64;
```

### Encoding: `stnt1d_mz_p_br_4`  — STNT1D

**Assembly:** `STNT1D  { <Zt1>.D-<Zt4>.D }, <PNg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 64;
```

---

## STNT1D (scalar plus scalar, single register) -- A64

### Encoding: `stnt1d_z_p_br_contiguous`  — STNT1D

**Assembly:** `STNT1D  { <Zt>.D }, <Pg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
```

---

## STNT1D (scalar plus scalar, strided registers) -- A64

### Encoding: `stnt1d_mzx_p_br_2x8`  — STNT1D

**Assembly:** `STNT1D  { <Zt1>.D, <Zt2>.D }, <PNg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 64;
```

### Encoding: `stnt1d_mzx_p_br_4x4`  — STNT1D

**Assembly:** `STNT1D  { <Zt1>.D, <Zt2>.D, <Zt3>.D, <Zt4>.D }, <PNg>, [<Xn|SP>, <Xm>, LSL #3]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 64;
```

---

## STNT1D (vector plus scalar) -- A64

### Encoding: `stnt1d_z_p_ar_d_64_unscaled`  — STNT1D

**Assembly:** `STNT1D  { <Zt>.D }, <Pg>, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 64;
```

---

## STNT1H (scalar plus immediate, consecutive registers) -- A64

### Encoding: `stnt1h_mz_p_bi_2`  — STNT1H

**Assembly:** `STNT1H  { <Zt1>.H-<Zt2>.H }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

### Encoding: `stnt1h_mz_p_bi_4`  — STNT1H

**Assembly:** `STNT1H  { <Zt1>.H-<Zt4>.H }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## STNT1H (scalar plus immediate, single register) -- A64

### Encoding: `stnt1h_z_p_bi_contiguous`  — STNT1H

**Assembly:** `STNT1H  { <Zt>.H }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## STNT1H (scalar plus immediate, strided registers) -- A64

### Encoding: `stnt1h_mzx_p_bi_2x8`  — STNT1H

**Assembly:** `STNT1H  { <Zt1>.H, <Zt2>.H }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

### Encoding: `stnt1h_mzx_p_bi_4x4`  — STNT1H

**Assembly:** `STNT1H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 16;
let offset : integer = SInt(imm4);
```

---

## STNT1H (scalar plus scalar, consecutive registers) -- A64

### Encoding: `stnt1h_mz_p_br_2`  — STNT1H

**Assembly:** `STNT1H  { <Zt1>.H-<Zt2>.H }, <PNg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 16;
```

### Encoding: `stnt1h_mz_p_br_4`  — STNT1H

**Assembly:** `STNT1H  { <Zt1>.H-<Zt4>.H }, <PNg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 16;
```

---

## STNT1H (scalar plus scalar, single register) -- A64

### Encoding: `stnt1h_z_p_br_contiguous`  — STNT1H

**Assembly:** `STNT1H  { <Zt>.H }, <Pg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 16;
```

---

## STNT1H (scalar plus scalar, strided registers) -- A64

### Encoding: `stnt1h_mzx_p_br_2x8`  — STNT1H

**Assembly:** `STNT1H  { <Zt1>.H, <Zt2>.H }, <PNg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 16;
```

### Encoding: `stnt1h_mzx_p_br_4x4`  — STNT1H

**Assembly:** `STNT1H  { <Zt1>.H, <Zt2>.H, <Zt3>.H, <Zt4>.H }, <PNg>, [<Xn|SP>, <Xm>, LSL #1]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 16;
```

---

## STNT1H (vector plus scalar) -- A64

### Encoding: `stnt1h_z_p_ar_s_x32_unscaled`  — STNT1H

**Assembly:** `STNT1H  { <Zt>.S }, <Pg>, [<Zn>.S{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 16;
```

### Encoding: `stnt1h_z_p_ar_d_64_unscaled`  — STNT1H

**Assembly:** `STNT1H  { <Zt>.D }, <Pg>, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 16;
```

---

## STNT1W (scalar plus immediate, consecutive registers) -- A64

### Encoding: `stnt1w_mz_p_bi_2`  — STNT1W

**Assembly:** `STNT1W  { <Zt1>.S-<Zt2>.S }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

### Encoding: `stnt1w_mz_p_bi_4`  — STNT1W

**Assembly:** `STNT1W  { <Zt1>.S-<Zt4>.S }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## STNT1W (scalar plus immediate, single register) -- A64

### Encoding: `stnt1w_z_p_bi_contiguous`  — STNT1W

**Assembly:** `STNT1W  { <Zt>.S }, <Pg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | Is the optional signed immediate vector offset, in the range -8 to 7, defaulting to 0, encoded in the "imm4" field. | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## STNT1W (scalar plus immediate, strided registers) -- A64

### Encoding: `stnt1w_mzx_p_bi_2x8`  — STNT1W

**Assembly:** `STNT1W  { <Zt1>.S, <Zt2>.S }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Two registers" variant: is the optional signed immediate vector offset, a multiple of 2 in the range -16 to 14, | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

### Encoding: `stnt1w_mzx_p_bi_4x4`  — STNT1W

**Assembly:** `STNT1W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <PNg>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | imm4 (4b) | For the "Four registers" variant: is the optional signed immediate vector offset, a multiple of 4 in the range -32 to 28 | let offset : integer = SInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 32;
let offset : integer = SInt(imm4);
```

---

## STNT1W (scalar plus scalar, consecutive registers) -- A64

### Encoding: `stnt1w_mz_p_br_2`  — STNT1W

**Assembly:** `STNT1W  { <Zt1>.S-<Zt2>.S }, <PNg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (4b) | For the "Two registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" ti | let t : integer = UInt(Zt::'0'); |
| `<Zt2>` | Zt (4b) | Is the name of the second scalable vector register to be transferred, encoded as "Zt" times 2 plus 1. | let t : integer = UInt(Zt::'0'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let t : integer = UInt(Zt::'0');
let esize : integer{} = 32;
```

### Encoding: `stnt1w_mz_p_br_4`  — STNT1W

**Assembly:** `STNT1W  { <Zt1>.S-<Zt4>.S }, <PNg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | Zt (3b) | For the "Four registers" variant: is the name of the first scalable vector register to be transferred, encoded as "Zt" t | let t : integer = UInt(Zt::'00'); |
| `<Zt4>` | Zt (3b) | Is the name of the fourth scalable vector register to be transferred, encoded as "Zt" times 4 plus 3. | let t : integer = UInt(Zt::'00'); |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let t : integer = UInt(Zt::'00');
let esize : integer{} = 32;
```

---

## STNT1W (scalar plus scalar, single register) -- A64

### Encoding: `stnt1w_z_p_br_contiguous`  — STNT1W

**Assembly:** `STNT1W  { <Zt>.S }, <Pg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if Rm == '11111' then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
```

---

## STNT1W (scalar plus scalar, strided registers) -- A64

### Encoding: `stnt1w_mzx_p_br_2x8`  — STNT1W

**Assembly:** `STNT1W  { <Zt1>.S, <Zt2>.S }, <PNg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Two registers" variant: is the name of the first scalable vector register Z0-Z7 or Z16-Z23 to be transferred, e |  |
| `<Zt2>` | (T :: Zt) | For the "Two registers" variant: is the name of the second scalable vector register Z8-Z15 or Z24-Z31 to be transferred, |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 2;
let tstride : integer = 8;
let t : integer = UInt(T::'0'::Zt);
let esize : integer{} = 32;
```

### Encoding: `stnt1w_mzx_p_br_4x4`  — STNT1W

**Assembly:** `STNT1W  { <Zt1>.S, <Zt2>.S, <Zt3>.S, <Zt4>.S }, <PNg>, [<Xn|SP>, <Xm>, LSL #2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt1>` | (T :: Zt) | For the "Four registers" variant: is the name of the first scalable vector register Z0-Z3 or Z16-Z19 to be transferred,  |  |
| `<Zt2>` | (T :: Zt) | For the "Four registers" variant: is the name of the second scalable vector register Z4-Z7 or Z20-Z23 to be transferred, |  |
| `<Zt3>` | (T :: Zt) | Is the name of the third scalable vector register Z8-Z11 or Z24-Z27 to be transferred, encoded as "T:'10':Zt". |  |
| `<Zt4>` | (T :: Zt) | Is the name of the fourth scalable vector register Z12-Z15 or Z28-Z31 to be transferred, encoded as "T:'11':Zt". |  |
| `<PNg>` | PNg (3b) | Is the name of the governing scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the "P | let g : integer = UInt('1'::PNg); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the general-purpose offset register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let g : integer = UInt('1'::PNg);
let nreg : integer{} = 4;
let tstride : integer = 4;
let t : integer = UInt(T::'00'::Zt);
let esize : integer{} = 32;
```

---

## STNT1W (vector plus scalar) -- A64

### Encoding: `stnt1w_z_p_ar_s_x32_unscaled`  — STNT1W

**Assembly:** `STNT1W  { <Zt>.S }, <Pg>, [<Zn>.S{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 32;
```

### Encoding: `stnt1w_z_p_ar_d_64_unscaled`  — STNT1W

**Assembly:** `STNT1W  { <Zt>.D }, <Pg>, [<Zn>.D{, <Xm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the base scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Xm>` | Rm (5b) | Is the optional 64-bit name of the general-purpose offset register, defaulting to XZR, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Zn);
let m : integer = UInt(Rm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 32;
```

---

## STP (SIMD&FP) -- A64

### Encoding: `STP_S_ldstpair_post`  [32-bit]  — STP

**Assembly:** `STP  <St1>, <St2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St1>` | Rt (5b) | Is the 32-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<St2>` | Rt2 (5b) | Is the 32-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 32-bit" and "Pre-index 32-bit" variants: is the signed immediate byte offset, a multiple of 4 in the |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STP_D_ldstpair_post`  [64-bit]  — STP

**Assembly:** `STP  <Dt1>, <Dt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt1>` | Rt (5b) | Is the 64-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Dt2>` | Rt2 (5b) | Is the 64-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 64-bit" and "Pre-index 64-bit" variants: is the signed immediate byte offset, a multiple of 8 in the |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STP_Q_ldstpair_post`  [128-bit]  — STP

**Assembly:** `STP  <Qt1>, <Qt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 128-bit" and "Pre-index 128-bit" variants: is the signed immediate byte offset, a multiple of 16 in  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STP_S_ldstpair_pre`  [32-bit]  — STP

**Assembly:** `STP  <St1>, <St2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St1>` | Rt (5b) | Is the 32-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<St2>` | Rt2 (5b) | Is the 32-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 32-bit" and "Pre-index 32-bit" variants: is the signed immediate byte offset, a multiple of 4 in the |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STP_D_ldstpair_pre`  [64-bit]  — STP

**Assembly:** `STP  <Dt1>, <Dt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt1>` | Rt (5b) | Is the 64-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Dt2>` | Rt2 (5b) | Is the 64-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 64-bit" and "Pre-index 64-bit" variants: is the signed immediate byte offset, a multiple of 8 in the |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STP_Q_ldstpair_pre`  [128-bit]  — STP

**Assembly:** `STP  <Qt1>, <Qt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 128-bit" and "Pre-index 128-bit" variants: is the signed immediate byte offset, a multiple of 16 in  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STP_S_ldstpair_off`  [32-bit]  — STP

**Assembly:** `STP  <St1>, <St2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St1>` | Rt (5b) | Is the 32-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<St2>` | Rt2 (5b) | Is the 32-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 32-bit" variant: is the optional signed immediate byte offset, a multiple of 4 in the range -256  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

### Encoding: `STP_D_ldstpair_off`  [64-bit]  — STP

**Assembly:** `STP  <Dt1>, <Dt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt1>` | Rt (5b) | Is the 64-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Dt2>` | Rt2 (5b) | Is the 64-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 64-bit" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

### Encoding: `STP_Q_ldstpair_off`  [128-bit]  — STP

**Assembly:** `STP  <Qt1>, <Qt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 128-bit" variant: is the optional signed immediate byte offset, a multiple of 16 in the range -10 |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;

let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## STP -- A64

### Encoding: `STP_32_ldstpair_post`  [32-bit]  — STP

**Assembly:** `STP  <Wt1>, <Wt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 32-bit" and "Pre-index 32-bit" variants: is the signed immediate byte offset, a multiple of 4 in the |  |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STP_64_ldstpair_post`  [64-bit]  — STP

**Assembly:** `STP  <Xt1>, <Xt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 64-bit" and "Pre-index 64-bit" variants: is the signed immediate byte offset, a multiple of 8 in the |  |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STP_32_ldstpair_pre`  [32-bit]  — STP

**Assembly:** `STP  <Wt1>, <Wt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 32-bit" and "Pre-index 32-bit" variants: is the signed immediate byte offset, a multiple of 4 in the |  |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STP_64_ldstpair_pre`  [64-bit]  — STP

**Assembly:** `STP  <Xt1>, <Xt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index 64-bit" and "Pre-index 64-bit" variants: is the signed immediate byte offset, a multiple of 8 in the |  |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STP_32_ldstpair_off`  [32-bit]  — STP

**Assembly:** `STP  <Wt1>, <Wt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 32-bit" variant: is the optional signed immediate byte offset, a multiple of 4 in the range -256  |  |

**Pseudocode (Decode):**
```
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

### Encoding: `STP_64_ldstpair_off`  [64-bit]  — STP

**Assembly:** `STP  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset 64-bit" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512  |  |

**Pseudocode (Decode):**
```
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## STR (array vector) -- A64

### Encoding: `str_za_ri_`  — STR

**Assembly:** `STR  ZA[<Wv>, <offs>], [<Xn|SP>{, #<offs>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W12-W15, encoded in the "Rv" field. | let v : integer = UInt('011'::Rv); |
| `<offs>` | off4 (4b) | Is the vector select offset and optional memory offset, in the range 0 to 15, defaulting to 0, encoded in the "off4" fie | let offset : integer = UInt(off4); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let v : integer = UInt('011'::Rv);
let offset : integer = UInt(off4);
```

---

## STR (immediate) -- A64

### Encoding: `STR_32_ldst_immpost`  [32-bit]  — STR

**Assembly:** `STR  <Wt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_64_ldst_immpost`  [64-bit]  — STR

**Assembly:** `STR  <Xt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_32_ldst_immpre`  [32-bit]  — STR

**Assembly:** `STR  <Wt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_64_ldst_immpre`  [64-bit]  — STR

**Assembly:** `STR  <Xt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_32_ldst_pos`  [32-bit]  — STR

**Assembly:** `STR  <Wt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "32-bit" variant: is the optional positive immediate byte offset, a multiple of 4 in the range 0 to 16380, defau | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let scale : integer{} = UInt(size);
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `STR_64_ldst_pos`  [64-bit]  — STR

**Assembly:** `STR  <Xt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "64-bit" variant: is the optional positive immediate byte offset, a multiple of 8 in the range 0 to 32760, defau | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let scale : integer{} = UInt(size);
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

---

## STR (immediate, SIMD&FP) -- A64

### Encoding: `STR_B_ldst_immpost`  [8-bit]  — STR

**Assembly:** `STR  <Bt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_H_ldst_immpost`  [16-bit]  — STR

**Assembly:** `STR  <Ht>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_S_ldst_immpost`  [32-bit]  — STR

**Assembly:** `STR  <St>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_D_ldst_immpost`  [64-bit]  — STR

**Assembly:** `STR  <Dt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_Q_ldst_immpost`  [128-bit]  — STR

**Assembly:** `STR  <Qt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_B_ldst_immpre`  [8-bit]  — STR

**Assembly:** `STR  <Bt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_H_ldst_immpre`  [16-bit]  — STR

**Assembly:** `STR  <Ht>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_S_ldst_immpre`  [32-bit]  — STR

**Assembly:** `STR  <St>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_D_ldst_immpre`  [64-bit]  — STR

**Assembly:** `STR  <Dt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_Q_ldst_immpre`  [128-bit]  — STR

**Assembly:** `STR  <Qt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STR_B_ldst_pos`  [8-bit]  — STR

**Assembly:** `STR  <Bt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "8-bit" variant: is the optional positive immediate byte offset, in the range 0 to 4095, defaulting to 0 and enc | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `STR_H_ldst_pos`  [16-bit]  — STR

**Assembly:** `STR  <Ht>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "16-bit" variant: is the optional positive immediate byte offset, a multiple of 2 in the range 0 to 8190, defaul | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `STR_S_ldst_pos`  [32-bit]  — STR

**Assembly:** `STR  <St>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "32-bit" variant: is the optional positive immediate byte offset, a multiple of 4 in the range 0 to 16380, defau | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `STR_D_ldst_pos`  [64-bit]  — STR

**Assembly:** `STR  <Dt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "64-bit" variant: is the optional positive immediate byte offset, a multiple of 8 in the range 0 to 32760, defau | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

### Encoding: `STR_Q_ldst_pos`  [128-bit]  — STR

**Assembly:** `STR  <Qt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | For the "128-bit" variant: is the optional positive immediate byte offset, a multiple of 16 in the range 0 to 65520, def | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), scale);
```

---

## STR (predicate) -- A64

### Encoding: `str_p_bi_`  — STR

**Assembly:** `STR  <Pt>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pt>` | Pt (4b) | Is the name of the scalable predicate transfer register, encoded in the "Pt" field. | let t : integer = UInt(Pt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | (imm9h :: imm9l) | Is the optional signed immediate vector offset, in the range -256 to 255, defaulting to 0, encoded in the "imm9h:imm9l"  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Pt);
let n : integer = UInt(Rn);
let imm : integer = SInt(imm9h::imm9l);
```

---

## STR (register) -- A64

### Encoding: `STR_32_ldst_regoff`  [32-bit]  — STR

**Assembly:** `STR  <Wt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let scale : integer{} = UInt(size);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `STR_64_ldst_regoff`  [64-bit]  — STR

**Assembly:** `STR  <Xt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let scale : integer{} = UInt(size);
let shift : integer{} = if S == '1' then scale else 0;
```

---

## STR (register, SIMD&FP) -- A64

### Encoding: `STR_B_ldst_regoff`  [8-bit with extended register offset]  — STR

**Assembly:** `STR  <Bt>, [<Xn|SP>, (<Wm>|<Xm>), <extend> {<amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | S (1b) | For the "8-bit with extended register offset" and "8-bit with shifted register offset" variants: is the index shift amou | let shift : integer{} = if S == '1' then scale else 0; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `STR_BL_ldst_regoff`  [8-bit with shifted register offset]  — STR

**Assembly:** `STR  <Bt>, [<Xn|SP>, <Xm>{, LSL <amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<amount>` | S (1b) | For the "8-bit with extended register offset" and "8-bit with shifted register offset" variants: is the index shift amou | let shift : integer{} = if S == '1' then scale else 0; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `STR_H_ldst_regoff`  [16-bit]  — STR

**Assembly:** `STR  <Ht>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `STR_S_ldst_regoff`  [32-bit]  — STR

**Assembly:** `STR  <St>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `STR_D_ldst_regoff`  [64-bit]  — STR

**Assembly:** `STR  <Dt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

### Encoding: `STR_Q_ldst_regoff`  [128-bit]  — STR

**Assembly:** `STR  <Qt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then scale else 0;
```

---

## STR (table) -- A64

### Encoding: `str_zt_br_`  — STR

**Assembly:** `STR  ZT0, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
```

---

## STR (vector) -- A64

### Encoding: `str_z_bi_`  — STR

**Assembly:** `STR  <Zt>, [<Xn|SP>{, #<imm>, MUL VL}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zt>` | Zt (5b) | Is the name of the scalable vector register to be transferred, encoded in the "Zt" field. | let t : integer = UInt(Zt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<imm>` | (imm9h :: imm9l) | Is the optional signed immediate vector offset, in the range -256 to 255, defaulting to 0, encoded in the "imm9h:imm9l"  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let imm : integer = SInt(imm9h::imm9l);
```

---

## STRB (immediate) -- A64

### Encoding: `STRB_32_ldst_immpost`  — STRB

**Assembly:** `STRB  <Wt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STRB_32_ldst_immpre`  — STRB

**Assembly:** `STRB  <Wt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STRB_32_ldst_pos`  — STRB

**Assembly:** `STRB  <Wt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, in the range 0 to 4095, defaulting to 0 and encoded in the "imm12" field | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 0); |

**Pseudocode (Decode):**
```
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 0);
```

---

## STRB (register) -- A64

### Encoding: `STRB_32B_ldst_regoff`  [Extended register]  — STRB

**Assembly:** `STRB  <Wt>, [<Xn|SP>, (<Wm>|<Xm>), <extend> {<amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | S (1b) | Is the index shift amount, it must be #0, encoded in "S" as 0 if omitted, or as 1 if present. |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = 0;
```

### Encoding: `STRB_32BL_ldst_regoff`  [Shifted register]  — STRB

**Assembly:** `STRB  <Wt>, [<Xn|SP>, <Xm>{, LSL <amount>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<amount>` | S (1b) | Is the index shift amount, it must be #0, encoded in "S" as 0 if omitted, or as 1 if present. |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = 0;
```

---

## STRH (immediate) -- A64

### Encoding: `STRH_32_ldst_immpost`  — STRH

**Assembly:** `STRH  <Wt>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STRH_32_ldst_immpre`  — STRH

**Assembly:** `STRH  <Wt>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the signed immediate byte offset, in the range -256 to 255, encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STRH_32_ldst_pos`  — STRH

**Assembly:** `STRH  <Wt>, [<Xn|SP>{, #<pimm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<pimm>` | imm12 (12b) | Is the optional positive immediate byte offset, a multiple of 2 in the range 0 to 8190, defaulting to 0 and encoded in t | let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 1); |

**Pseudocode (Decode):**
```
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
let offset : bits(64) = LSL(ZeroExtend{64}(imm12), 1);
```

---

## STRH (register) -- A64

### Encoding: `STRH_32_ldst_regoff`  — STRH

**Assembly:** `STRH  <Wt>, [<Xn|SP>, (<Wm>|<Xm>){, <extend> {<amount>}}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | When option<0> is set to 0, is the 32-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<Xm>` | Rm (5b) | When option<0> is set to 1, is the 64-bit name of the general-purpose index register, encoded in the "Rm" field. |  |
| `<extend>` | — |  |  |
| `<amount>` | — |  |  |

**Pseudocode (Decode):**
```
if option[1] == '0' then EndOfDecode(Decode_UNDEF); end;         // sub-word index
let extend_type : ExtendType = DecodeRegExtend(option);
let shift : integer{} = if S == '1' then 1 else 0;
```

---

## STSET, STSETL -- A64 *(alias for LDSET)*

### Encoding: `STSET_LDSET_32_memop`  [32-bit no memory ordering]  — LDSET

**Assembly:** `STSET  <Ws>, [<Xn|SP>] / LDSET  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSET` | — |  |  |

### Encoding: `STSETL_LDSETL_32_memop`  [32-bit release]  — LDSETL

**Assembly:** `STSETL  <Ws>, [<Xn|SP>] / LDSETL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSETL` | — |  |  |

### Encoding: `STSET_LDSET_64_memop`  [64-bit no memory ordering]  — LDSET

**Assembly:** `STSET  <Xs>, [<Xn|SP>] / LDSET  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSET` | — |  |  |

### Encoding: `STSETL_LDSETL_64_memop`  [64-bit release]  — LDSETL

**Assembly:** `STSETL  <Xs>, [<Xn|SP>] / LDSETL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSETL` | — |  |  |

---

## STSETB, STSETLB -- A64 *(alias for LDSETB)*

### Encoding: `STSETB_LDSETB_32_memop`  [No memory ordering]  — LDSETB

**Assembly:** `STSETB  <Ws>, [<Xn|SP>] / LDSETB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSETB` | — |  |  |

### Encoding: `STSETLB_LDSETLB_32_memop`  [Release]  — LDSETLB

**Assembly:** `STSETLB  <Ws>, [<Xn|SP>] / LDSETLB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSETLB` | — |  |  |

---

## STSETH, STSETLH -- A64 *(alias for LDSETH)*

### Encoding: `STSETH_LDSETH_32_memop`  [No memory ordering]  — LDSETH

**Assembly:** `STSETH  <Ws>, [<Xn|SP>] / LDSETH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSETH` | — |  |  |

### Encoding: `STSETLH_LDSETLH_32_memop`  [Release]  — LDSETLH

**Assembly:** `STSETLH  <Ws>, [<Xn|SP>] / LDSETLH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSETLH` | — |  |  |

---

## STSHH -- A64

### Encoding: `STSHH_HI_hints`  — STSHH

**Assembly:** `STSHH  <policy>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<policy>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PCDPHINT) then EndOfDecode(Decode_NOP); end;
let stream : boolean = op2[0] == '1';
```

---

## STSMAX, STSMAXL -- A64 *(alias for LDSMAX)*

### Encoding: `STSMAX_LDSMAX_32_memop`  [32-bit no memory ordering]  — LDSMAX

**Assembly:** `STSMAX  <Ws>, [<Xn|SP>] / LDSMAX  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMAX` | — |  |  |

### Encoding: `STSMAXL_LDSMAXL_32_memop`  [32-bit release]  — LDSMAXL

**Assembly:** `STSMAXL  <Ws>, [<Xn|SP>] / LDSMAXL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMAXL` | — |  |  |

### Encoding: `STSMAX_LDSMAX_64_memop`  [64-bit no memory ordering]  — LDSMAX

**Assembly:** `STSMAX  <Xs>, [<Xn|SP>] / LDSMAX  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMAX` | — |  |  |

### Encoding: `STSMAXL_LDSMAXL_64_memop`  [64-bit release]  — LDSMAXL

**Assembly:** `STSMAXL  <Xs>, [<Xn|SP>] / LDSMAXL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMAXL` | — |  |  |

---

## STSMAXB, STSMAXLB -- A64 *(alias for LDSMAXB)*

### Encoding: `STSMAXB_LDSMAXB_32_memop`  [No memory ordering]  — LDSMAXB

**Assembly:** `STSMAXB  <Ws>, [<Xn|SP>] / LDSMAXB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMAXB` | — |  |  |

### Encoding: `STSMAXLB_LDSMAXLB_32_memop`  [Release]  — LDSMAXLB

**Assembly:** `STSMAXLB  <Ws>, [<Xn|SP>] / LDSMAXLB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMAXLB` | — |  |  |

---

## STSMAXH, STSMAXLH -- A64 *(alias for LDSMAXH)*

### Encoding: `STSMAXH_LDSMAXH_32_memop`  [No memory ordering]  — LDSMAXH

**Assembly:** `STSMAXH  <Ws>, [<Xn|SP>] / LDSMAXH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMAXH` | — |  |  |

### Encoding: `STSMAXLH_LDSMAXLH_32_memop`  [Release]  — LDSMAXLH

**Assembly:** `STSMAXLH  <Ws>, [<Xn|SP>] / LDSMAXLH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMAXLH` | — |  |  |

---

## STSMIN, STSMINL -- A64 *(alias for LDSMIN)*

### Encoding: `STSMIN_LDSMIN_32_memop`  [32-bit no memory ordering]  — LDSMIN

**Assembly:** `STSMIN  <Ws>, [<Xn|SP>] / LDSMIN  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMIN` | — |  |  |

### Encoding: `STSMINL_LDSMINL_32_memop`  [32-bit release]  — LDSMINL

**Assembly:** `STSMINL  <Ws>, [<Xn|SP>] / LDSMINL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMINL` | — |  |  |

### Encoding: `STSMIN_LDSMIN_64_memop`  [64-bit no memory ordering]  — LDSMIN

**Assembly:** `STSMIN  <Xs>, [<Xn|SP>] / LDSMIN  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMIN` | — |  |  |

### Encoding: `STSMINL_LDSMINL_64_memop`  [64-bit release]  — LDSMINL

**Assembly:** `STSMINL  <Xs>, [<Xn|SP>] / LDSMINL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMINL` | — |  |  |

---

## STSMINB, STSMINLB -- A64 *(alias for LDSMINB)*

### Encoding: `STSMINB_LDSMINB_32_memop`  [No memory ordering]  — LDSMINB

**Assembly:** `STSMINB  <Ws>, [<Xn|SP>] / LDSMINB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMINB` | — |  |  |

### Encoding: `STSMINLB_LDSMINLB_32_memop`  [Release]  — LDSMINLB

**Assembly:** `STSMINLB  <Ws>, [<Xn|SP>] / LDSMINLB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMINLB` | — |  |  |

---

## STSMINH, STSMINLH -- A64 *(alias for LDSMINH)*

### Encoding: `STSMINH_LDSMINH_32_memop`  [No memory ordering]  — LDSMINH

**Assembly:** `STSMINH  <Ws>, [<Xn|SP>] / LDSMINH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMINH` | — |  |  |

### Encoding: `STSMINLH_LDSMINLH_32_memop`  [Release]  — LDSMINLH

**Assembly:** `STSMINLH  <Ws>, [<Xn|SP>] / LDSMINLH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDSMINLH` | — |  |  |

---

## STTADD, STTADDL -- A64 *(alias for LDTADD)*

### Encoding: `STTADD_LDTADD_32_memop_unpriv`  [32-bit no memory ordering]  — LDTADD

**Assembly:** `STTADD  <Ws>, [<Xn|SP>] / LDTADD  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTADD` | — |  |  |

### Encoding: `STTADDL_LDTADDL_32_memop_unpriv`  [32-bit release]  — LDTADDL

**Assembly:** `STTADDL  <Ws>, [<Xn|SP>] / LDTADDL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTADDL` | — |  |  |

### Encoding: `STTADD_LDTADD_64_memop_unpriv`  [64-bit no memory ordering]  — LDTADD

**Assembly:** `STTADD  <Xs>, [<Xn|SP>] / LDTADD  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTADD` | — |  |  |

### Encoding: `STTADDL_LDTADDL_64_memop_unpriv`  [64-bit release]  — LDTADDL

**Assembly:** `STTADDL  <Xs>, [<Xn|SP>] / LDTADDL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTADDL` | — |  |  |

---

## STTCLR, STTCLRL -- A64 *(alias for LDTCLR)*

### Encoding: `STTCLR_LDTCLR_32_memop_unpriv`  [32-bit no memory ordering]  — LDTCLR

**Assembly:** `STTCLR  <Ws>, [<Xn|SP>] / LDTCLR  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTCLR` | — |  |  |

### Encoding: `STTCLRL_LDTCLRL_32_memop_unpriv`  [32-bit release]  — LDTCLRL

**Assembly:** `STTCLRL  <Ws>, [<Xn|SP>] / LDTCLRL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTCLRL` | — |  |  |

### Encoding: `STTCLR_LDTCLR_64_memop_unpriv`  [64-bit no memory ordering]  — LDTCLR

**Assembly:** `STTCLR  <Xs>, [<Xn|SP>] / LDTCLR  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTCLR` | — |  |  |

### Encoding: `STTCLRL_LDTCLRL_64_memop_unpriv`  [64-bit release]  — LDTCLRL

**Assembly:** `STTCLRL  <Xs>, [<Xn|SP>] / LDTCLRL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTCLRL` | — |  |  |

---

## STTNP (SIMD&FP) -- A64

### Encoding: `STTNP_Q_ldstnapair_offs`  — STTNP

**Assembly:** `STTNP  <Qt1>, <Qt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm7 (7b) | Is the optional signed immediate byte offset, a multiple of 16 in the range -1024 to 1008, defaulting to 0 and encoded i | let offset : bits(64) = LSL(SignExtend{64}(imm7), 4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LSUI) then
    EndOfDecode(Decode_UNDEF);
end;
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let nontemporal : boolean = TRUE;
let datasize : integer{} = 128;
let offset : bits(64) = LSL(SignExtend{64}(imm7), 4);
let tagchecked : boolean = n != 31;
```

---

## STTNP -- A64

### Encoding: `STTNP_64_ldstnapair_offs`  — STTNP

**Assembly:** `STTNP  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm7 (7b) | Is the optional signed immediate byte offset, a multiple of 8 in the range -512 to 504, defaulting to 0 and encoded in t | let offset : bits(64) = LSL(SignExtend{64}(imm7), 3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);
let nontemporal : boolean = TRUE;
let datasize : integer{} = 64;
let offset : bits(64) = LSL(SignExtend{64}(imm7), 3);
let tagchecked : boolean = n != 31;
```

---

## STTP (SIMD&FP) -- A64

### Encoding: `STTP_Q_ldstpair_post`  — STTP

**Assembly:** `STTP  <Qt1>, <Qt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 16 in the range -1024  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LSUI) then
    EndOfDecode(Decode_UNDEF);
end;

let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STTP_Q_ldstpair_pre`  — STTP

**Assembly:** `STTP  <Qt1>, <Qt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 16 in the range -1024  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LSUI) then
    EndOfDecode(Decode_UNDEF);
end;

let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STTP_Q_ldstpair_off`  — STTP

**Assembly:** `STTP  <Qt1>, <Qt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt1>` | Rt (5b) | Is the 128-bit name of the first SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Qt2>` | Rt2 (5b) | Is the 128-bit name of the second SIMD&FP register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset" variant: is the optional signed immediate byte offset, a multiple of 16 in the range -1024 to 10 |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) || !IsFeatureImplemented(FEAT_LSUI) then
    EndOfDecode(Decode_UNDEF);
end;

let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## STTP -- A64

### Encoding: `STTP_64_ldstpair_post`  — STTP

**Assembly:** `STTP  <Xt1>, <Xt2>, [<Xn|SP>], #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 8 in the range -512 to |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let wback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STTP_64_ldstpair_pre`  — STTP

**Assembly:** `STTP  <Xt1>, <Xt2>, [<Xn|SP>, #<imm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Post-index" and "Pre-index" variants: is the signed immediate byte offset, a multiple of 8 in the range -512 to |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let wback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STTP_64_ldstpair_off`  — STTP

**Assembly:** `STTP  <Xt1>, <Xt2>, [<Xn|SP>{, #<imm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<imm>` | imm7 (7b) | For the "Signed offset" variant: is the optional signed immediate byte offset, a multiple of 8 in the range -512 to 504, |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let wback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## STTR -- A64

### Encoding: `STTR_32_ldst_unpriv`  [32-bit]  — STTR

**Assembly:** `STTR  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STTR_64_ldst_unpriv`  [64-bit]  — STTR

**Assembly:** `STTR  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

---

## STTRB -- A64

### Encoding: `STTRB_32_ldst_unpriv`  — STTRB

**Assembly:** `STTRB  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## STTRH -- A64

### Encoding: `STTRH_32_ldst_unpriv`  — STTRH

**Assembly:** `STTRH  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## STTSET, STTSETL -- A64 *(alias for LDTSET)*

### Encoding: `STTSET_LDTSET_32_memop_unpriv`  [32-bit no memory ordering]  — LDTSET

**Assembly:** `STTSET  <Ws>, [<Xn|SP>] / LDTSET  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTSET` | — |  |  |

### Encoding: `STTSETL_LDTSETL_32_memop_unpriv`  [32-bit release]  — LDTSETL

**Assembly:** `STTSETL  <Ws>, [<Xn|SP>] / LDTSETL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTSETL` | — |  |  |

### Encoding: `STTSET_LDTSET_64_memop_unpriv`  [64-bit no memory ordering]  — LDTSET

**Assembly:** `STTSET  <Xs>, [<Xn|SP>] / LDTSET  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTSET` | — |  |  |

### Encoding: `STTSETL_LDTSETL_64_memop_unpriv`  [64-bit release]  — LDTSETL

**Assembly:** `STTSETL  <Xs>, [<Xn|SP>] / LDTSETL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDTSETL` | — |  |  |

---

## STTXR -- A64

### Encoding: `STTXR_SR32_ldstexclr_unpriv`  [32-bit]  — STTXR

**Assembly:** `STTXR  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `STTXR_SR64_ldstexclr_unpriv`  [64-bit]  — STTXR

**Assembly:** `STTXR  <Ws>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STUMAX, STUMAXL -- A64 *(alias for LDUMAX)*

### Encoding: `STUMAX_LDUMAX_32_memop`  [32-bit no memory ordering]  — LDUMAX

**Assembly:** `STUMAX  <Ws>, [<Xn|SP>] / LDUMAX  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMAX` | — |  |  |

### Encoding: `STUMAXL_LDUMAXL_32_memop`  [32-bit release]  — LDUMAXL

**Assembly:** `STUMAXL  <Ws>, [<Xn|SP>] / LDUMAXL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMAXL` | — |  |  |

### Encoding: `STUMAX_LDUMAX_64_memop`  [64-bit no memory ordering]  — LDUMAX

**Assembly:** `STUMAX  <Xs>, [<Xn|SP>] / LDUMAX  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMAX` | — |  |  |

### Encoding: `STUMAXL_LDUMAXL_64_memop`  [64-bit release]  — LDUMAXL

**Assembly:** `STUMAXL  <Xs>, [<Xn|SP>] / LDUMAXL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMAXL` | — |  |  |

---

## STUMAXB, STUMAXLB -- A64 *(alias for LDUMAXB)*

### Encoding: `STUMAXB_LDUMAXB_32_memop`  [No memory ordering]  — LDUMAXB

**Assembly:** `STUMAXB  <Ws>, [<Xn|SP>] / LDUMAXB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMAXB` | — |  |  |

### Encoding: `STUMAXLB_LDUMAXLB_32_memop`  [Release]  — LDUMAXLB

**Assembly:** `STUMAXLB  <Ws>, [<Xn|SP>] / LDUMAXLB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMAXLB` | — |  |  |

---

## STUMAXH, STUMAXLH -- A64 *(alias for LDUMAXH)*

### Encoding: `STUMAXH_LDUMAXH_32_memop`  [No memory ordering]  — LDUMAXH

**Assembly:** `STUMAXH  <Ws>, [<Xn|SP>] / LDUMAXH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMAXH` | — |  |  |

### Encoding: `STUMAXLH_LDUMAXLH_32_memop`  [Release]  — LDUMAXLH

**Assembly:** `STUMAXLH  <Ws>, [<Xn|SP>] / LDUMAXLH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMAXLH` | — |  |  |

---

## STUMIN, STUMINL -- A64 *(alias for LDUMIN)*

### Encoding: `STUMIN_LDUMIN_32_memop`  [32-bit no memory ordering]  — LDUMIN

**Assembly:** `STUMIN  <Ws>, [<Xn|SP>] / LDUMIN  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMIN` | — |  |  |

### Encoding: `STUMINL_LDUMINL_32_memop`  [32-bit release]  — LDUMINL

**Assembly:** `STUMINL  <Ws>, [<Xn|SP>] / LDUMINL  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMINL` | — |  |  |

### Encoding: `STUMIN_LDUMIN_64_memop`  [64-bit no memory ordering]  — LDUMIN

**Assembly:** `STUMIN  <Xs>, [<Xn|SP>] / LDUMIN  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMIN` | — |  |  |

### Encoding: `STUMINL_LDUMINL_64_memop`  [64-bit release]  — LDUMINL

**Assembly:** `STUMINL  <Xs>, [<Xn|SP>] / LDUMINL  <Xs>, XZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMINL` | — |  |  |

---

## STUMINB, STUMINLB -- A64 *(alias for LDUMINB)*

### Encoding: `STUMINB_LDUMINB_32_memop`  [No memory ordering]  — LDUMINB

**Assembly:** `STUMINB  <Ws>, [<Xn|SP>] / LDUMINB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMINB` | — |  |  |

### Encoding: `STUMINLB_LDUMINLB_32_memop`  [Release]  — LDUMINLB

**Assembly:** `STUMINLB  <Ws>, [<Xn|SP>] / LDUMINLB  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMINLB` | — |  |  |

---

## STUMINH, STUMINLH -- A64 *(alias for LDUMINH)*

### Encoding: `STUMINH_LDUMINH_32_memop`  [No memory ordering]  — LDUMINH

**Assembly:** `STUMINH  <Ws>, [<Xn|SP>] / LDUMINH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMINH` | — |  |  |

### Encoding: `STUMINLH_LDUMINLH_32_memop`  [Release]  — LDUMINLH

**Assembly:** `STUMINLH  <Ws>, [<Xn|SP>] / LDUMINLH  <Ws>, WZR, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register holding the data value to be operated on with the contents of the mem |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `LDUMINLH` | — |  |  |

---

## STUR (SIMD&FP) -- A64

### Encoding: `STUR_B_ldst_unscaled`  [8-bit]  — STUR

**Assembly:** `STUR  <Bt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Bt>` | Rt (5b) | Is the 8-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STUR_H_ldst_unscaled`  [16-bit]  — STUR

**Assembly:** `STUR  <Ht>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ht>` | Rt (5b) | Is the 16-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STUR_S_ldst_unscaled`  [32-bit]  — STUR

**Assembly:** `STUR  <St>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<St>` | Rt (5b) | Is the 32-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STUR_D_ldst_unscaled`  [64-bit]  — STUR

**Assembly:** `STUR  <Dt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dt>` | Rt (5b) | Is the 64-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STUR_Q_ldst_unscaled`  [128-bit]  — STUR

**Assembly:** `STUR  <Qt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Qt>` | Rt (5b) | Is the 128-bit name of the SIMD&FP register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if opc[1] == '1' && size != '00' then EndOfDecode(Decode_UNDEF); end;
let scale : integer{} = if opc[1] == '1' then 4 else UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

---

## STUR -- A64

### Encoding: `STUR_32_ldst_unscaled`  [32-bit]  — STUR

**Assembly:** `STUR  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

### Encoding: `STUR_64_ldst_unscaled`  [64-bit]  — STUR

**Assembly:** `STUR  <Xt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let scale : integer{} = UInt(size);
let offset : bits(64) = SignExtend{}(imm9);
```

---

## STURB -- A64

### Encoding: `STURB_32_ldst_unscaled`  — STURB

**Assembly:** `STURB  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## STURH -- A64

### Encoding: `STURH_32_ldst_unscaled`  — STURH

**Assembly:** `STURH  <Wt>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. |  |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. |  |
| `<simm>` | imm9 (9b) | Is the optional signed immediate byte offset, in the range -256 to 255, defaulting to 0 and encoded in the "imm9" field. | let offset : bits(64) = SignExtend{}(imm9); |

**Pseudocode (Decode):**
```
let offset : bits(64) = SignExtend{}(imm9);
```

---

## STXP -- A64

### Encoding: `STXP_SP32_ldstexclp`  [32-bit]  — STXP

**Assembly:** `STXP  <Ws>, <Wt1>, <Wt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt1>` | Rt (5b) | Is the 32-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Wt2>` | Rt2 (5b) | Is the 32-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let datasize : integer{} = elsize * 2;
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t || (s == t2) then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `STXP_SP64_ldstexclp`  [64-bit]  — STXP

**Assembly:** `STXP  <Ws>, <Xt1>, <Xt2>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 32 << UInt(sz);
let datasize : integer{} = elsize * 2;
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t || (s == t2) then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STXR -- A64

### Encoding: `STXR_SR32_ldstexclr`  [32-bit]  — STXR

**Assembly:** `STXR  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 8 << UInt(size);
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `STXR_SR64_ldstexclr`  [64-bit]  — STXR

**Assembly:** `STXR  <Ws>, <Xt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let elsize : integer{} = 8 << UInt(size);
let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STXRB -- A64

### Encoding: `STXRB_SR32_ldstexclr`  — STXRB

**Assembly:** `STXRB  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STXRH -- A64

### Encoding: `STXRH_SR32_ldstexclr`  — STXRH

**Assembly:** `STXRH  <Ws>, <Wt>, [<Xn|SP>{, #0}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register into which the status result of the store exclusive is written, encod | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be transferred, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acqrel : boolean = FALSE;
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;
var rn_unknown : boolean = FALSE;
if s == t then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_DATAOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // store UNKNOWN value
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
if s == n && n != 31 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_BASEOVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rn_unknown = TRUE;    // address is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## STZ2G -- A64

### Encoding: `STZ2G_64Spost_ldsttags`  — STZ2G

**Assembly:** `STZ2G  <Xt|SP>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STZ2G_64Spre_ldsttags`  — STZ2G

**Assembly:** `STZ2G  <Xt|SP>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STZ2G_64Soffset_ldsttags`  — STZ2G

**Assembly:** `STZ2G  <Xt|SP>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## STZG -- A64

### Encoding: `STZG_64Spost_ldsttags`  — STZG

**Assembly:** `STZG  <Xt|SP>, [<Xn|SP>], #<simm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = TRUE;
```

### Encoding: `STZG_64Spre_ldsttags`  — STZG

**Assembly:** `STZG  <Xt|SP>, [<Xn|SP>, #<simm>]!`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = TRUE;
let postindex : boolean = FALSE;
```

### Encoding: `STZG_64Soffset_ldsttags`  — STZG

**Assembly:** `STZG  <Xt|SP>, [<Xn|SP>{, #<simm>}]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt\|SP>` | Rt (5b) | Is the 64-bit name of the general-purpose source register or stack pointer, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<simm>` | imm9 (9b) | Is the optional signed immediate offset, a multiple of 16 in the range -4096 to 4080, defaulting to 0 and encoded in the | let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
let offset : bits(64) = LSL(SignExtend{64}(imm9), LOG2_TAG_GRANULE);
let writeback : boolean = FALSE;
let postindex : boolean = FALSE;
```

---

## STZGM -- A64

### Encoding: `STZGM_64bulk_ldsttags`  — STZGM

**Assembly:** `STZGM  <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. | let t : integer{} = UInt(Rt); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE2) then EndOfDecode(Decode_UNDEF); end;
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);
```

---

## SUB (extended register) -- A64

### Encoding: `SUB_32_addsub_ext`  [32-bit]  — SUB

**Assembly:** `SUB  <Wd|WSP>, <Wn|WSP>, <Wm>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd\|WSP>` | Rd (5b) | Is the 32-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field | if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;<br>let shift : integer{} = UInt(imm3) as integer{0..4}; |

**Pseudocode (Decode):**
```
if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer{} = UInt(imm3) as integer{0..4};
let datasize : integer{} = 32 << UInt(sf);
let extend_type : ExtendType = DecodeRegExtend(option);
```

### Encoding: `SUB_64_addsub_ext`  [64-bit]  — SUB

**Assembly:** `SUB  <Xd|SP>, <Xn|SP>, <R><m>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<R>` | — |  |  |
| `<m>` | Rm (5b) | Is the number [0-30] of the second general-purpose source register or the name ZR (31), encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field | if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;<br>let shift : integer{} = UInt(imm3) as integer{0..4}; |

**Pseudocode (Decode):**
```
if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer{} = UInt(imm3) as integer{0..4};
let datasize : integer{} = 32 << UInt(sf);
let extend_type : ExtendType = DecodeRegExtend(option);
```

---

## SUB (immediate) -- A64

### Encoding: `SUB_32_addsub_imm`  [32-bit]  — SUB

**Assembly:** `SUB  <Wd|WSP>, <Wn|WSP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd\|WSP>` | Rd (5b) | Is the 32-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. | let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12}; |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);

let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12};
```

### Encoding: `SUB_64_addsub_imm`  [64-bit]  — SUB

**Assembly:** `SUB  <Xd|SP>, <Xn|SP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. | let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12}; |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);

let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12};
```

---

## SUB (immediate) -- A64

### Encoding: `sub_z_zi_`  — SUB

**Assembly:** `SUB  <Zdn>.<T>, <Zdn>.<T>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is an unsigned immediate in the range 0 to 255, encoded in the "imm8" field. | var imm : integer = UInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
var imm : integer = UInt(imm8);
if sh == '1' then imm = imm << 8; end;
```

---

## SUB (shifted register) -- A64

### Encoding: `SUB_32_addsub_shift`  [32-bit]  — SUB

**Assembly:** `SUB  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if shift == '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `SUB_64_addsub_shift`  [64-bit]  — SUB

**Assembly:** `SUB  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if shift == '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## SUB (to array, array and multiple vectors) -- A64

### Encoding: `sub_za_zw_2x2`  — SUB

**Assembly:** `SUB  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the source multi-vector gr | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zm" times 2 plus 1. | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `sub_za_zw_4x4`  — SUB

**Assembly:** `SUB  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the source multi-vector g | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zm" times 4 plus 3. | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## SUB (to array, multiple and single vector) -- A64

### Encoding: `sub_za_zzv_2x1`  — SUB

**Assembly:** `SUB  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<T>-<Zn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `sub_za_zzv_4x1`  — SUB

**Assembly:** `SUB  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<T>-<Zn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## SUB (to array, multiple vectors) -- A64

### Encoding: `sub_za_zzw_2x2`  — SUB

**Assembly:** `SUB  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<T>-<Zn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `sub_za_zzw_4x4`  — SUB

**Assembly:** `SUB  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<T>-<Zn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## SUB (vector) -- A64

### Encoding: `SUB_asisdsame_only`  — SUB

**Assembly:** `SUB  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `SUB_asimdsame_only`  — SUB

**Assembly:** `SUB  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## SUB (vectors, predicated) -- A64

### Encoding: `sub_z_p_zz_`  — SUB

**Assembly:** `SUB  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SUB (vectors, unpredicated) -- A64

### Encoding: `sub_z_zz_`  — SUB

**Assembly:** `SUB  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SUBG -- A64

### Encoding: `SUBG_64_addsub_immtags`  — SUBG

**Assembly:** `SUBG  <Xd|SP>, <Xn|SP>, #<uimm6>, #<uimm4>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the destination general-purpose register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<uimm6>` | imm6 (6b) | Is an unsigned immediate, a multiple of 16 in the range 0 to 1008, encoded in the "imm6" field. | let offset : bits(64) = LSL(ZeroExtend{64}(imm6), LOG2_TAG_GRANULE); |
| `<uimm4>` | imm4 (4b) | Is an unsigned immediate, in the range 0 to 15, encoded in the "imm4" field. | let tag_offset : bits(4) = imm4; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let tag_offset : bits(4) = imm4;
let offset : bits(64) = LSL(ZeroExtend{64}(imm6), LOG2_TAG_GRANULE);
```

---

## SUBHN, SUBHN2 -- A64

### Encoding: `SUBHN_asimddiff_N`  — SUBHN

**Assembly:** `SUBHN{2}  <Vd>.<Tb>, <Vn>.<Ta>, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
let round : boolean = FALSE;
```

---

## SUBHNB -- A64

### Encoding: `subhnb_z_zz_`  — SUBHNB

**Assembly:** `SUBHNB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SUBHNT -- A64

### Encoding: `subhnt_z_zz_`  — SUBHNT

**Assembly:** `SUBHNT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SUBP -- A64

### Encoding: `SUBP_64S_dp_2src`  — SUBP

**Assembly:** `SUBP  <Xd>, <Xn|SP>, <Xm|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm\|SP>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register or stack pointer, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SUBP -- A64

### Encoding: `subp_z_p_zz_`  — SUBP

**Assembly:** `SUBP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## SUBPS -- A64

### Encoding: `SUBPS_64S_dp_2src`  — SUBPS

**Assembly:** `SUBPS  <Xd>, <Xn|SP>, <Xm|SP>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm\|SP>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register or stack pointer, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_MTE) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## SUBPT (predicated) -- A64

### Encoding: `subpt_z_p_zz_`  — SUBPT

**Assembly:** `SUBPT  <Zdn>.D, <Pg>/M, <Zdn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer =  UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer =  UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_CPA) then
    EndOfDecode(Decode_UNDEF);
end;
let g : integer =  UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer =  UInt(Zm);
```

---

## SUBPT (unpredicated) -- A64

### Encoding: `subpt_z_zz_`  — SUBPT

**Assembly:** `SUBPT  <Zd>.D, <Zn>.D, <Zm>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_CPA) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## SUBPT -- A64

### Encoding: `SUBPT_64_addsub_pt`  — SUBPT

**Assembly:** `SUBPT  <Xd|SP>, <Xn|SP>, <Xm>{, LSL #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd\|SP>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register or stack pointer, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<amount>` | imm3 (3b) | Is the left shift amount, in the range 0 to 7, defaulting to 0, encoded in the "imm3" field. | let shift : integer = UInt(imm3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CPA) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer = UInt(imm3);
```

---

## SUBR (immediate) -- A64

### Encoding: `subr_z_zi_`  — SUBR

**Assembly:** `SUBR  <Zdn>.<T>, <Zdn>.<T>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is an unsigned immediate in the range 0 to 255, encoded in the "imm8" field. | var imm : integer = UInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
var imm : integer = UInt(imm8);
if sh == '1' then imm = imm << 8; end;
```

---

## SUBR (vectors) -- A64

### Encoding: `subr_z_p_zz_`  — SUBR

**Assembly:** `SUBR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SUBS (extended register) -- A64

### Encoding: `SUBS_32S_addsub_ext`  [32-bit]  — SUBS

**Assembly:** `SUBS  <Wd>, <Wn|WSP>, <Wm>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field | if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;<br>let shift : integer{} = UInt(imm3) as integer{0..4}; |

**Pseudocode (Decode):**
```
if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer{} = UInt(imm3) as integer{0..4};
let datasize : integer{} = 32 << UInt(sf);
let extend_type : ExtendType = DecodeRegExtend(option);
```

### Encoding: `SUBS_64S_addsub_ext`  [64-bit]  — SUBS

**Assembly:** `SUBS  <Xd>, <Xn|SP>, <R><m>{, <extend> {#<amount>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<R>` | — |  |  |
| `<m>` | Rm (5b) | Is the number [0-30] of the second general-purpose source register or the name ZR (31), encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<extend>` | — |  |  |
| `<amount>` | imm3 (3b) | Is the left shift amount to be applied after extension in the range 0 to 4, defaulting to 0, encoded in the "imm3" field | if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;<br>let shift : integer{} = UInt(imm3) as integer{0..4}; |

**Pseudocode (Decode):**
```
if imm3 IN {'101', '110', '111'} then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let shift : integer{} = UInt(imm3) as integer{0..4};
let datasize : integer{} = 32 << UInt(sf);
let extend_type : ExtendType = DecodeRegExtend(option);
```

---

## SUBS (immediate) -- A64

### Encoding: `SUBS_32S_addsub_imm`  [32-bit]  — SUBS

**Assembly:** `SUBS  <Wd>, <Wn|WSP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn\|WSP>` | Rn (5b) | Is the 32-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. | let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12}; |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);

let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12};
```

### Encoding: `SUBS_64S_addsub_imm`  [64-bit]  — SUBS

**Assembly:** `SUBS  <Xd>, <Xn|SP>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the source general-purpose register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<imm>` | imm12 (12b) | Is an unsigned immediate, in the range 0 to 4095, encoded in the "imm12" field. | let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12}; |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);

let imm : bits(24) = if sh == '0' then Zeros{12}::imm12 else imm12::Zeros{12};
```

---

## SUBS (shifted register) -- A64

### Encoding: `SUBS_32_addsub_shift`  [32-bit]  — SUBS

**Assembly:** `SUBS  <Wd>, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if shift == '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

### Encoding: `SUBS_64_addsub_shift`  [64-bit]  — SUBS

**Assembly:** `SUBS  <Xd>, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. | if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;<br>let shift_amount : integer = UInt(imm6); |

**Pseudocode (Decode):**
```
if shift == '11' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && imm6[5] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
let shift_type : ShiftType = DecodeShift(shift);
let shift_amount : integer = UInt(imm6);
```

---

## SUDOT (4-way, multiple and indexed vector) -- A64

### Encoding: `sudot_za_zzi_s2xi`  — SUDOT

**Assembly:** `SUDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `sudot_za_zzi_s4xi`  — SUDOT

**Assembly:** `SUDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## SUDOT (4-way, multiple and single vector) -- A64

### Encoding: `sudot_za_zzv_s2x1`  — SUDOT

**Assembly:** `SUDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `sudot_za_zzv_s4x1`  — SUDOT

**Assembly:** `SUDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## SUDOT (by element) -- A64

### Encoding: `SUDOT_asimdelem_D`  — SUDOT

**Assembly:** `SUDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.4B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | (M :: Rm) | Is the name of the second SIMD&FP source register, encoded in the "M:Rm" fields. |  |
| `<index>` | (H :: L) | Is the immediate index of a 32-bit group of four 8-bit values, in the range 0 to 3, encoded in the "H:L" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_I8MM) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(M::Rm);
let d : integer = UInt(Rd);
let i : integer = UInt(H::L);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 32;
```

---

## SUDOT -- A64

### Encoding: `sudot_z_zzzi_s`  — SUDOT

**Assembly:** `SUDOT  <Zda>.S, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_I8MM)) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## SUMLALL (multiple and indexed vector) -- A64

### Encoding: `sumlall_za_zzi_s`  — SUMLALL

**Assembly:** `SUMLALL  ZA.S[<Wv>, <offs1>:<offs4>], <Zn>.B, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | Is the element index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 1;
```

### Encoding: `sumlall_za_zzi_s2xi`  — SUMLALL

**Assembly:** `SUMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vecto | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | Is the element index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 2;
```

### Encoding: `sumlall_za_zzi_s4xi`  — SUMLALL

**Assembly:** `SUMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vect | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | Is the element index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 4;
```

---

## SUMLALL (multiple and single vector) -- A64

### Encoding: `sumlall_za_zzv_s2x1`  — SUMLALL

**Assembly:** `SUMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `sumlall_za_zzv_s4x1`  — SUMLALL

**Assembly:** `SUMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## SUMOP4A -- A64

### Encoding: `sumop4a_za_zz_b1x2`  — SUMOP4A

**Assembly:** `SUMOP4A  <ZAda>.S, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4a_za_zz_b1x1`  — SUMOP4A

**Assembly:** `SUMOP4A  <ZAda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4a_za_zz_b2x1`  — SUMOP4A

**Assembly:** `SUMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4a_za_zz_b2x2`  — SUMOP4A

**Assembly:** `SUMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4a_za_zz_h1x2`  — SUMOP4A

**Assembly:** `SUMOP4A  <ZAda>.D, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4a_za_zz_h1x1`  — SUMOP4A

**Assembly:** `SUMOP4A  <ZAda>.D, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4a_za_zz_h2x1`  — SUMOP4A

**Assembly:** `SUMOP4A  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4a_za_zz_h2x2`  — SUMOP4A

**Assembly:** `SUMOP4A  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

---

## SUMOP4S -- A64

### Encoding: `sumop4s_za_zz_b1x2`  — SUMOP4S

**Assembly:** `SUMOP4S  <ZAda>.S, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4s_za_zz_b1x1`  — SUMOP4S

**Assembly:** `SUMOP4S  <ZAda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4s_za_zz_b2x1`  — SUMOP4S

**Assembly:** `SUMOP4S  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4s_za_zz_b2x2`  — SUMOP4S

**Assembly:** `SUMOP4S  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4s_za_zz_h1x2`  — SUMOP4S

**Assembly:** `SUMOP4S  <ZAda>.D, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4s_za_zz_h1x1`  — SUMOP4S

**Assembly:** `SUMOP4S  <ZAda>.D, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4s_za_zz_h2x1`  — SUMOP4S

**Assembly:** `SUMOP4S  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumop4s_za_zz_h2x2`  — SUMOP4S

**Assembly:** `SUMOP4S  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

---

## SUMOPA (4-way) -- A64

### Encoding: `sumopa_za_pp_zz_32`  — SUMOPA

**Assembly:** `SUMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumopa_za_pp_zz_64`  — SUMOPA

**Assembly:** `SUMOPA  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

---

## SUMOPS -- A64

### Encoding: `sumops_za_pp_zz_32`  — SUMOPS

**Assembly:** `SUMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `sumops_za_pp_zz_64`  — SUMOPS

**Assembly:** `SUMOPS  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

---

## SUNPK -- A64

### Encoding: `sunpk_mz_z_2`  — SUNPK

**Assembly:** `SUNPK  { <Zd1>.<T>-<Zd2>.<T> }, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let unsigned : boolean = FALSE;
```

### Encoding: `sunpk_mz_z_4`  — SUNPK

**Assembly:** `SUNPK  { <Zd1>.<T>-<Zd4>.<T> }, { <Zn1>.<Tb>-<Zn2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let unsigned : boolean = FALSE;
```

---

## SUNPKHI, SUNPKLO -- A64

### Encoding: `sunpkhi_z_z_`  — SUNPKHI

**Assembly:** `SUNPKHI  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let hi : boolean = TRUE;
```

### Encoding: `sunpklo_z_z_`  — SUNPKLO

**Assembly:** `SUNPKLO  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let hi : boolean = FALSE;
```

---

## SUQADD -- A64

### Encoding: `SUQADD_asisdmisc_R`  — SUQADD

**Assembly:** `SUQADD  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let unsigned : boolean = FALSE;
```

### Encoding: `SUQADD_asimdmisc_R`  — SUQADD

**Assembly:** `SUQADD  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let unsigned : boolean = FALSE;
```

---

## SUQADD -- A64

### Encoding: `suqadd_z_p_zz_`  — SUQADD

**Assembly:** `SUQADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## SUTMOPA -- A64

### Encoding: `sutmopa_za_zzzi_b2x1`  — SUTMOPA

**Assembly:** `SUTMOPA  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = FALSE;
let op2_unsigned : boolean = TRUE;
```

---

## SUVDOT -- A64

### Encoding: `suvdot_za_zzi_s4xi`  — SUVDOT

**Assembly:** `SUVDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
```

---

## SVC -- A64

### Encoding: `SVC_EX_exception`  — SVC

**Assembly:** `SVC  #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | imm16 (16b) | Is a 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. | let imm : bits(16) = imm16; |

**Pseudocode (Decode):**
```
let imm : bits(16) = imm16;
```

---

## SVDOT (2-way) -- A64

### Encoding: `svdot_za32_zzi_2xi`  — SVDOT

**Assembly:** `SVDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
```

---

## SVDOT (4-way) -- A64

### Encoding: `svdot_za_zzi_s4xi`  — SVDOT

**Assembly:** `SVDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "8-bit to 32-bit" variant: is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vec | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
```

### Encoding: `svdot_za_zzi_d4xi`  — SVDOT

**Assembly:** `SVDOT  ZA.D[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "16-bit to 64-bit" variant: is the immediate index of a 64-bit group of four 16-bit values within each 128-bit v | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
```

---

## SWP, SWPA, SWPAL, SWPL -- A64

### Encoding: `SWP_32_memop`  [32-bit SWP]  — SWP

**Assembly:** `SWP  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPA_32_memop`  [32-bit SWPA]  — SWPA

**Assembly:** `SWPA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPAL_32_memop`  [32-bit SWPAL]  — SWPAL

**Assembly:** `SWPAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPL_32_memop`  [32-bit SWPL]  — SWPL

**Assembly:** `SWPL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWP_64_memop`  [64-bit SWP]  — SWP

**Assembly:** `SWP  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPA_64_memop`  [64-bit SWPA]  — SWPA

**Assembly:** `SWPA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPAL_64_memop`  [64-bit SWPAL]  — SWPAL

**Assembly:** `SWPAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPL_64_memop`  [64-bit SWPL]  — SWPL

**Assembly:** `SWPL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 8 << UInt(size);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## SWPB, SWPAB, SWPALB, SWPLB -- A64

### Encoding: `SWPB_32_memop`  [SWPB]  — SWPB

**Assembly:** `SWPB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPAB_32_memop`  [SWPAB]  — SWPAB

**Assembly:** `SWPAB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPALB_32_memop`  [SWPALB]  — SWPALB

**Assembly:** `SWPALB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPLB_32_memop`  [SWPLB]  — SWPLB

**Assembly:** `SWPLB  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## SWPH, SWPAH, SWPALH, SWPLH -- A64

### Encoding: `SWPH_32_memop`  [SWPH]  — SWPH

**Assembly:** `SWPH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPAH_32_memop`  [SWPAH]  — SWPAH

**Assembly:** `SWPAH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPALH_32_memop`  [SWPALH]  — SWPALH

**Assembly:** `SWPALH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPLH_32_memop`  [SWPLH]  — SWPLH

**Assembly:** `SWPLH  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## SWPP, SWPPA, SWPPAL, SWPPL -- A64

### Encoding: `SWPP_128_memop_128`  [SWPP]  — SWPP

**Assembly:** `SWPP  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;

let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `SWPPA_128_memop_128`  [SWPPA]  — SWPPA

**Assembly:** `SWPPA  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;

let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `SWPPAL_128_memop_128`  [SWPPAL]  — SWPPAL

**Assembly:** `SWPPAL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;

let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

### Encoding: `SWPPL_128_memop_128`  [SWPPL]  — SWPPL

**Assembly:** `SWPPL  <Xt1>, <Xt2>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first general-purpose register to be transferred, encoded in the "Rt" field. | if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer{} = UInt(Rt); |
| `<Xt2>` | Rt2 (5b) | Is the 64-bit name of the second general-purpose register to be transferred, encoded in the "Rt2" field. | if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t2 : integer{} = UInt(Rt2); |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSE128) then EndOfDecode(Decode_UNDEF); end;
if Rt  == '11111' then EndOfDecode(Decode_UNDEF); end;
if Rt2 == '11111' then EndOfDecode(Decode_UNDEF); end;

let t : integer{} = UInt(Rt);
let t2 : integer{} = UInt(Rt2);
let n : integer{} = UInt(Rn);

let acquire : boolean = A == '1';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;

var rt_unknown : boolean = FALSE;

if t == t2 then
    let c : Constraint = ConstrainUnpredictable(Unpredictable_LSE128OVERLAP);
    assert c IN {Constraint_UNKNOWN, Constraint_UNDEF, Constraint_NOP};
    case c of
        when Constraint_UNKNOWN =>    rt_unknown = TRUE;    // result is UNKNOWN
        when Constraint_UNDEF =>      EndOfDecode(Decode_UNDEF);
        when Constraint_NOP =>        EndOfDecode(Decode_NOP);
    end;
end;
```

---

## SWPT, SWPTA, SWPTAL, SWPTL -- A64

### Encoding: `SWPT_32_memop_unpriv`  [32-bit SWPT]  — SWPT

**Assembly:** `SWPT  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPTA_32_memop_unpriv`  [32-bit SWPTA]  — SWPTA

**Assembly:** `SWPTA  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPTAL_32_memop_unpriv`  [32-bit SWPTAL]  — SWPTAL

**Assembly:** `SWPTAL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPTL_32_memop_unpriv`  [32-bit SWPTL]  — SWPTL

**Assembly:** `SWPTL  <Ws>, <Wt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Ws>` | Rs (5b) | Is the 32-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Wt>` | Rt (5b) | Is the 32-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPT_64_memop_unpriv`  [64-bit SWPT]  — SWPT

**Assembly:** `SWPT  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPTA_64_memop_unpriv`  [64-bit SWPTA]  — SWPTA

**Assembly:** `SWPTA  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPTAL_64_memop_unpriv`  [64-bit SWPTAL]  — SWPTAL

**Assembly:** `SWPTAL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

### Encoding: `SWPTL_64_memop_unpriv`  [64-bit SWPTL]  — SWPTL

**Assembly:** `SWPTL  <Xs>, <Xt>, [<Xn|SP>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xs>` | Rs (5b) | Is the 64-bit name of the general-purpose register to be stored, encoded in the "Rs" field. | let s : integer{} = UInt(Rs); |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose register to be loaded, encoded in the "Rt" field. | let t : integer{} = UInt(Rt);<br>let acquire : boolean = A == '1' && Rt != '11111'; |
| `<Xn\|SP>` | Rn (5b) | Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_LSUI) then EndOfDecode(Decode_UNDEF); end;

let s : integer{} = UInt(Rs);
let t : integer{} = UInt(Rt);
let n : integer{} = UInt(Rn);

let datasize : integer{} = 32 << UInt(sz);
let regsize : integer{} = if datasize == 64 then 64 else 32;
let acquire : boolean = A == '1' && Rt != '11111';
let release : boolean = R == '1';
let tagchecked : boolean = n != 31;
```

---

## SXTB -- A64 *(alias for SBFM)*

### Encoding: `SXTB_SBFM_32M_bitfield`  [32-bit]  — SBFM

**Assembly:** `SXTB  <Wd>, <Wn> / SBFM  <Wd>, <Wn>, #0, #7`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `SBFM` | — |  |  |

### Encoding: `SXTB_SBFM_64M_bitfield`  [64-bit]  — SBFM

**Assembly:** `SXTB  <Xd>, <Wn> / SBFM  <Xd>, <Xn>, #0, #7`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `SBFM` | — |  |  |

---

## SXTB, SXTH, SXTW -- A64

### Encoding: `sxtb_z_p_z_m`  — SXTB

**Assembly:** `SXTB  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let merging : boolean = TRUE;
```

### Encoding: `sxtb_z_p_z_z`  — SXTB

**Assembly:** `SXTB  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let merging : boolean = FALSE;
```

### Encoding: `sxth_z_p_z_m`  — SXTH

**Assembly:** `SXTH  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let merging : boolean = TRUE;
```

### Encoding: `sxth_z_p_z_z`  — SXTH

**Assembly:** `SXTH  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let merging : boolean = FALSE;
```

### Encoding: `sxtw_z_p_z_m`  — SXTW

**Assembly:** `SXTW  <Zd>.D, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let merging : boolean = TRUE;
```

### Encoding: `sxtw_z_p_z_z`  — SXTW

**Assembly:** `SXTW  <Zd>.D, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = FALSE;
let merging : boolean = FALSE;
```

---

## SXTH -- A64 *(alias for SBFM)*

### Encoding: `SXTH_SBFM_32M_bitfield`  [32-bit]  — SBFM

**Assembly:** `SXTH  <Wd>, <Wn> / SBFM  <Wd>, <Wn>, #0, #15`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `SBFM` | — |  |  |

### Encoding: `SXTH_SBFM_64M_bitfield`  [64-bit]  — SBFM

**Assembly:** `SXTH  <Xd>, <Wn> / SBFM  <Xd>, <Xn>, #0, #15`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `SBFM` | — |  |  |

---

## SXTL, SXTL2 -- A64 *(alias for SSHLL_advsimd)*

### Encoding: `SXTL_SSHLL_asimdshf_L`  — SSHLL

**Assembly:** `SXTL{2}  <Vd>.<Ta>, <Vn>.<Tb> / SSHLL{2}  <Vd>.<Ta>, <Vn>.<Tb>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. |  |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. |  |
| `<Tb>` | — |  |  |
| `SSHLL` | — |  |  |

---

## SXTW -- A64 *(alias for SBFM)*

### Encoding: `SXTW_SBFM_64M_bitfield`  — SBFM

**Assembly:** `SXTW  <Xd>, <Wn> / SBFM  <Xd>, <Xn>, #0, #31`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `SBFM` | — |  |  |

---

## SYS -- A64

### Encoding: `SYS_CR_systeminstrs`  — SYS

**Assembly:** `SYS  #<op1>, <Cn>, <Cm>, #<op2>{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<op1>` | op1 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op1" field. | let sys_op1 : bits(3) = op1; |
| `<Cn>` | CRn (4b) | Is a name 'Cn', with 'n' in the range 0 to 15, encoded in the "CRn" field. | let sys_crn : bits(4) = CRn; |
| `<Cm>` | CRm (4b) | Is a name 'Cm', with 'm' in the range 0 to 15, encoded in the "CRm" field. | let sys_crm : bits(4) = CRm; |
| `<op2>` | op2 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op2" field. | let sys_op2 : bits(3) = op2; |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the optional general-purpose source register, defaulting to '11111', encoded in the "Rt" field. | let t : integer       = UInt(Rt); |

**Pseudocode (Decode):**
```
let t : integer       = UInt(Rt);
let sys_L : bits(1)   = L;
let sys_op0 : bits(2) = '01';
let sys_op1 : bits(3) = op1;
let sys_op2 : bits(3) = op2;
let sys_crn : bits(4) = CRn;
let sys_crm : bits(4) = CRm;
```

---

## SYSL -- A64

### Encoding: `SYSL_RC_systeminstrs`  — SYSL

**Assembly:** `SYSL  <Xt>, #<op1>, <Cn>, <Cm>, #<op2>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rt" field. | let t : integer       = UInt(Rt); |
| `<op1>` | op1 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op1" field. | let sys_op1 : bits(3) = op1; |
| `<Cn>` | CRn (4b) | Is a name 'Cn', with 'n' in the range 0 to 15, encoded in the "CRn" field. | let sys_crn : bits(4) = CRn; |
| `<Cm>` | CRm (4b) | Is a name 'Cm', with 'm' in the range 0 to 15, encoded in the "CRm" field. | let sys_crm : bits(4) = CRm; |
| `<op2>` | op2 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op2" field. | let sys_op2 : bits(3) = op2; |

**Pseudocode (Decode):**
```
let t : integer       = UInt(Rt);
let sys_L : bits(1)   = L;
let sys_op0 : bits(2) = '01';
let sys_op1 : bits(3) = op1;
let sys_op2 : bits(3) = op2;
let sys_crn : bits(4) = CRn;
let sys_crm : bits(4) = CRm;
```

---

## SYSP -- A64

### Encoding: `SYSP_CR_syspairinstrs`  — SYSP

**Assembly:** `SYSP  #<op1>, <Cn>, <Cm>, #<op2>{, <Xt1>, <Xt2>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<op1>` | op1 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 6, encoded in the "op1" field. | let sys_op1 : bits(3) = op1; |
| `<Cn>` | CRn (4b) | Is a name 'Cn', with 'n' in the range 8 to 9, encoded in the "CRn" field. | let sys_crn : bits(4) = CRn; |
| `<Cm>` | CRm (4b) | Is a name 'Cm', with 'm' in the range 0 to 7, encoded in the "CRm" field. | let sys_crm : bits(4) = CRm; |
| `<op2>` | op2 (3b) | Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the "op2" field. | let sys_op2 : bits(3) = op2; |
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first optional general-purpose source register, defaulting to '11111', encoded in the "Rt" fie | if Rt[0] == '1' && Rt != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer       = UInt(Rt); |
| `<Xt2>` | Rt (5b) | Is the 64-bit name of the second optional general-purpose source register, defaulting to '11111', encoded as "Rt" +1. De | if Rt[0] == '1' && Rt != '11111' then EndOfDecode(Decode_UNDEF); end;<br>let t : integer       = UInt(Rt); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SYSINSTR128) then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == '1' && Rt != '11111' then EndOfDecode(Decode_UNDEF); end;

let t : integer       = UInt(Rt);
let t2 : integer      = if t == 31 then 31 else t + 1;
let sys_L : bits(1)   = L;
let sys_op0 : bits(2) = '01';
let sys_op1 : bits(3) = op1;
let sys_op2 : bits(3) = op2;
let sys_crn : bits(4) = CRn;
let sys_crm : bits(4) = CRm;
```

---

## TBL -- A64

### Encoding: `TBL_asimdtbl_L1_1`  [Single register table]  — TBL

**Assembly:** `TBL  <Vd>.<Ta>, { <Vn>.16B }, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | For the "Single register table" variant: is the name of the SIMD&FP table register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);

let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
let regs : integer{} = UInt(len) + 1;
let is_tbl : boolean = (op == '0');
```

### Encoding: `TBL_asimdtbl_L2_2`  [Two register table]  — TBL

**Assembly:** `TBL  <Vd>.<Ta>, { <Vn>.16B, <Vn+1>.16B }, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | For the "Four register table", "Three register table", and "Two register table" variants: is the name of the first SIMD& | let n : integer = UInt(Rn); |
| `<Vn+1>` | Rn (5b) | Is the name of the second SIMD&FP table register, encoded as "Rn" plus 1 modulo 32. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);

let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
let regs : integer{} = UInt(len) + 1;
let is_tbl : boolean = (op == '0');
```

### Encoding: `TBL_asimdtbl_L3_3`  [Three register table]  — TBL

**Assembly:** `TBL  <Vd>.<Ta>, { <Vn>.16B, <Vn+1>.16B, <Vn+2>.16B }, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | For the "Four register table", "Three register table", and "Two register table" variants: is the name of the first SIMD& | let n : integer = UInt(Rn); |
| `<Vn+1>` | Rn (5b) | Is the name of the second SIMD&FP table register, encoded as "Rn" plus 1 modulo 32. | let n : integer = UInt(Rn); |
| `<Vn+2>` | Rn (5b) | Is the name of the third SIMD&FP table register, encoded as "Rn" plus 2 modulo 32. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);

let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
let regs : integer{} = UInt(len) + 1;
let is_tbl : boolean = (op == '0');
```

### Encoding: `TBL_asimdtbl_L4_4`  [Four register table]  — TBL

**Assembly:** `TBL  <Vd>.<Ta>, { <Vn>.16B, <Vn+1>.16B, <Vn+2>.16B, <Vn+3>.16B }, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | For the "Four register table", "Three register table", and "Two register table" variants: is the name of the first SIMD& | let n : integer = UInt(Rn); |
| `<Vn+1>` | Rn (5b) | Is the name of the second SIMD&FP table register, encoded as "Rn" plus 1 modulo 32. | let n : integer = UInt(Rn); |
| `<Vn+2>` | Rn (5b) | Is the name of the third SIMD&FP table register, encoded as "Rn" plus 2 modulo 32. | let n : integer = UInt(Rn); |
| `<Vn+3>` | Rn (5b) | Is the name of the fourth SIMD&FP table register, encoded as "Rn" plus 3 modulo 32. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);

let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
let regs : integer{} = UInt(len) + 1;
let is_tbl : boolean = (op == '0');
```

---

## TBL -- A64

### Encoding: `tbl_z_zz_1`  — TBL

**Assembly:** `TBL  <Zd>.<T>, { <Zn>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let double_table : boolean = FALSE;
```

### Encoding: `tbl_z_zz_2`  — TBL

**Assembly:** `TBL  <Zd>.<T>, { <Zn1>.<T>, <Zn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let double_table : boolean = TRUE;
```

---

## TBLQ -- A64

### Encoding: `tblq_z_zz_`  — TBLQ

**Assembly:** `TBLQ  <Zd>.<T>, { <Zn>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## TBNZ -- A64

### Encoding: `TBNZ_only_testbranch`  — TBNZ

**Assembly:** `TBNZ  <R><t>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<R>` | — |  |  |
| `<t>` | Rt (5b) | Is the number [0-30] of the general-purpose register to be tested or the name ZR (31), encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | (b5 :: b40) | Is the bit number to be tested, in the range 0 to 63, encoded in "b5:b40". |  |
| `<label>` | imm14 (14b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range +/-3 | let offset : bits(64) = SignExtend{}(imm14::'00'); |

**Pseudocode (Decode):**
```
let t : integer = UInt(Rt);

let datasize : integer{} = 32 << UInt(b5);
let bit_pos : integer = UInt(b5::b40);
let offset : bits(64) = SignExtend{}(imm14::'00');
```

---

## TBX -- A64

### Encoding: `TBX_asimdtbl_L1_1`  [Single register table]  — TBX

**Assembly:** `TBX  <Vd>.<Ta>, { <Vn>.16B }, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | For the "Single register table" variant: is the name of the SIMD&FP table register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);

let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
let regs : integer{} = UInt(len) + 1;
let is_tbl : boolean = (op == '0');
```

### Encoding: `TBX_asimdtbl_L2_2`  [Two register table]  — TBX

**Assembly:** `TBX  <Vd>.<Ta>, { <Vn>.16B, <Vn+1>.16B }, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | For the "Four register table", "Three register table", and "Two register table" variants: is the name of the first SIMD& | let n : integer = UInt(Rn); |
| `<Vn+1>` | Rn (5b) | Is the name of the second SIMD&FP table register, encoded as "Rn" plus 1 modulo 32. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);

let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
let regs : integer{} = UInt(len) + 1;
let is_tbl : boolean = (op == '0');
```

### Encoding: `TBX_asimdtbl_L3_3`  [Three register table]  — TBX

**Assembly:** `TBX  <Vd>.<Ta>, { <Vn>.16B, <Vn+1>.16B, <Vn+2>.16B }, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | For the "Four register table", "Three register table", and "Two register table" variants: is the name of the first SIMD& | let n : integer = UInt(Rn); |
| `<Vn+1>` | Rn (5b) | Is the name of the second SIMD&FP table register, encoded as "Rn" plus 1 modulo 32. | let n : integer = UInt(Rn); |
| `<Vn+2>` | Rn (5b) | Is the name of the third SIMD&FP table register, encoded as "Rn" plus 2 modulo 32. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);

let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
let regs : integer{} = UInt(len) + 1;
let is_tbl : boolean = (op == '0');
```

### Encoding: `TBX_asimdtbl_L4_4`  [Four register table]  — TBX

**Assembly:** `TBX  <Vd>.<Ta>, { <Vn>.16B, <Vn+1>.16B, <Vn+2>.16B, <Vn+3>.16B }, <Vm>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | For the "Four register table", "Three register table", and "Two register table" variants: is the name of the first SIMD& | let n : integer = UInt(Rn); |
| `<Vn+1>` | Rn (5b) | Is the name of the second SIMD&FP table register, encoded as "Rn" plus 1 modulo 32. | let n : integer = UInt(Rn); |
| `<Vn+2>` | Rn (5b) | Is the name of the third SIMD&FP table register, encoded as "Rn" plus 2 modulo 32. | let n : integer = UInt(Rn); |
| `<Vn+3>` | Rn (5b) | Is the name of the fourth SIMD&FP table register, encoded as "Rn" plus 3 modulo 32. | let n : integer = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the SIMD&FP index register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);

let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 8;
let regs : integer{} = UInt(len) + 1;
let is_tbl : boolean = (op == '0');
```

---

## TBX -- A64

### Encoding: `tbx_z_zz_`  — TBX

**Assembly:** `TBX  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## TBXQ -- A64

### Encoding: `tbxq_z_zz_`  — TBXQ

**Assembly:** `TBXQ  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## TBZ -- A64

### Encoding: `TBZ_only_testbranch`  — TBZ

**Assembly:** `TBZ  <R><t>, #<imm>, <label>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<R>` | — |  |  |
| `<t>` | Rt (5b) | Is the number [0-30] of the general-purpose register to be tested or the name ZR (31), encoded in the "Rt" field. | let t : integer = UInt(Rt); |
| `<imm>` | (b5 :: b40) | Is the bit number to be tested, in the range 0 to 63, encoded in "b5:b40". |  |
| `<label>` | imm14 (14b) | Is the program label to be conditionally branched to. Its offset from the address of this instruction, in the range +/-3 | let offset : bits(64) = SignExtend{}(imm14::'00'); |

**Pseudocode (Decode):**
```
let t : integer = UInt(Rt);

let datasize : integer{} = 32 << UInt(b5);
let bit_pos : integer = UInt(b5::b40);
let offset : bits(64) = SignExtend{}(imm14::'00');
```

---

## TLBI -- A64 *(alias for SYS)*

### Encoding: `TLBI_SYS_CR_systeminstrs`  — SYS

**Assembly:** `TLBI  <tlbi_op>{, <Xt>} / SYS  #<op1>, <Cn>, <Cm>, #<op2>{, <Xt>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<tlbi_op>` | — |  |  |
| `<Xt>` | Rt (5b) | Is the 64-bit name of the optional general-purpose source register, defaulting to '11111', encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## TLBIP -- A64 *(alias for SYSP)*

### Encoding: `TLBIP_SYSP_CR_syspairinstrs`  — SYSP

**Assembly:** `TLBIP  <tlbip_op>{, <Xt1>, <Xt2>} / SYSP  #<op1>, <Cn>, <Cm>, #<op2>{, <Xt1>, <Xt2>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<tlbip_op>` | — |  |  |
| `<Xt1>` | Rt (5b) | Is the 64-bit name of the first optional general-purpose source register, defaulting to '11111', encoded in the "Rt" fie |  |
| `<Xt2>` | Rt (5b) | Is the 64-bit name of the second optional general-purpose source register, defaulting to '11111', encoded as "Rt" +1. De |  |
| `SYSP` | — |  |  |

---

## TRCIT -- A64 *(alias for SYS)*

### Encoding: `TRCIT_SYS_CR_systeminstrs`  — SYS

**Assembly:** `TRCIT  <Xt> / SYS  #3, C7, C2, #7, <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rt (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rt" field. |  |
| `SYS` | — |  |  |

---

## TRN1 -- A64

### Encoding: `TRN1_asimdperm_only`  — TRN1

**Assembly:** `TRN1  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = UInt(op);
let pairs : integer = elements DIV 2;
```

---

## TRN1, TRN2 (predicates) -- A64

### Encoding: `trn1_p_pp_`  — TRN1

**Assembly:** `TRN1  <Pd>.<T>, <Pn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let part : integer = 0;
```

### Encoding: `trn2_p_pp_`  — TRN2

**Assembly:** `TRN2  <Pd>.<T>, <Pn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let part : integer = 1;
```

---

## TRN1, TRN2 (vectors) -- A64

### Encoding: `trn1_z_zz_`  — TRN1

**Assembly:** `TRN1  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 0;
```

### Encoding: `trn1_z_zz_q`  — TRN1

**Assembly:** `TRN1  <Zd>.Q, <Zn>.Q, <Zm>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 0;
```

### Encoding: `trn2_z_zz_`  — TRN2

**Assembly:** `TRN2  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 1;
```

### Encoding: `trn2_z_zz_q`  — TRN2

**Assembly:** `TRN2  <Zd>.Q, <Zn>.Q, <Zm>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 1;
```

---

## TRN2 -- A64

### Encoding: `TRN2_asimdperm_only`  — TRN2

**Assembly:** `TRN2  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = UInt(op);
let pairs : integer = elements DIV 2;
```

---

## TSB -- A64

### Encoding: `TSB_HC_hints`  — TSB

**Assembly:** `TSB  CSYNC`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_TRF) then EndOfDecode(Decode_NOP); end;
```

---

## TST (immediate) -- A64 *(alias for ANDS_log_imm)*

### Encoding: `TST_ANDS_32S_log_imm`  [32-bit]  — ANDS

**Assembly:** `TST  <Wn>, #<imm> / ANDS  WZR, <Wn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<imm>` | (imms :: immr) | For the "32-bit" variant: is the bitmask immediate, encoded in "imms:immr". |  |
| `ANDS` | — |  |  |

### Encoding: `TST_ANDS_64S_log_imm`  [64-bit]  — ANDS

**Assembly:** `TST  <Xn>, #<imm> / ANDS  XZR, <Xn>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<imm>` | (N :: imms :: immr) | For the "64-bit" variant: is the bitmask immediate, encoded in "N:imms:immr". |  |
| `ANDS` | — |  |  |

---

## TST (shifted register) -- A64 *(alias for ANDS_log_shift)*

### Encoding: `TST_ANDS_32_log_shift`  [32-bit]  — ANDS

**Assembly:** `TST  <Wn>, <Wm>{, <shift> #<amount>} / ANDS  WZR, <Wn>, <Wm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "32-bit" variant: is the shift amount, in the range 0 to 31, defaulting to 0 and encoded in the "imm6" field. |  |
| `ANDS` | — |  |  |

### Encoding: `TST_ANDS_64_log_shift`  [64-bit]  — ANDS

**Assembly:** `TST  <Xn>, <Xm>{, <shift> #<amount>} / ANDS  XZR, <Xn>, <Xm>{, <shift> #<amount>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. |  |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. |  |
| `<shift>` | — |  |  |
| `<amount>` | imm6 (6b) | For the "64-bit" variant: is the shift amount, in the range 0 to 63, defaulting to 0 and encoded in the "imm6" field. |  |
| `ANDS` | — |  |  |

---

## UABA -- A64

### Encoding: `UABA_asimdsame_only`  — UABA

**Assembly:** `UABA  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UABA -- A64

### Encoding: `uaba_z_zzz_`  — UABA

**Assembly:** `UABA  <Zda>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let unsigned : boolean = TRUE;
```

---

## UABAL -- A64

### Encoding: `uabal_z_zz_`  — UABAL

**Assembly:** `UABAL  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UABAL, UABAL2 -- A64

### Encoding: `UABAL_asimddiff_L`  — UABAL

**Assembly:** `UABAL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UABALB -- A64

### Encoding: `uabalb_z_zzz_`  — UABALB

**Assembly:** `UABALB  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UABALT -- A64

### Encoding: `uabalt_z_zzz_`  — UABALT

**Assembly:** `UABALT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UABD -- A64

### Encoding: `UABD_asimdsame_only`  — UABD

**Assembly:** `UABD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UABD -- A64

### Encoding: `uabd_z_p_zz_`  — UABD

**Assembly:** `UABD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UABDL, UABDL2 -- A64

### Encoding: `UABDL_asimddiff_L`  — UABDL

**Assembly:** `UABDL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UABDLB -- A64

### Encoding: `uabdlb_z_zz_`  — UABDLB

**Assembly:** `UABDLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## UABDLT -- A64

### Encoding: `uabdlt_z_zz_`  — UABDLT

**Assembly:** `UABDLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## UADALP -- A64

### Encoding: `UADALP_asimdmisc_P`  — UADALP

**Assembly:** `UADALP  <Vd>.<Ta>, <Vn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV (2 * esize);
```

---

## UADALP -- A64

### Encoding: `uadalp_z_p_z_`  — UADALP

**Assembly:** `UADALP  <Zda>.<T>, <Pg>/M, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the second source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let da : integer = UInt(Zda);
```

---

## UADDL, UADDL2 -- A64

### Encoding: `UADDL_asimddiff_L`  — UADDL

**Assembly:** `UADDL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UADDLB -- A64

### Encoding: `uaddlb_z_zz_`  — UADDLB

**Assembly:** `UADDLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 0;
let sel2 : integer = 0;
let unsigned : boolean = TRUE;
```

---

## UADDLP -- A64

### Encoding: `UADDLP_asimdmisc_P`  — UADDLP

**Assembly:** `UADDLP  <Vd>.<Ta>, <Vn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV (2 * esize);
```

---

## UADDLT -- A64

### Encoding: `uaddlt_z_zz_`  — UADDLT

**Assembly:** `UADDLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 1;
let sel2 : integer = 1;
let unsigned : boolean = TRUE;
```

---

## UADDLV -- A64

### Encoding: `UADDLV_asimdall_only`  — UADDLV

**Assembly:** `UADDLV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '100' then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = TRUE;
```

---

## UADDV -- A64

### Encoding: `uaddv_r_p_z_`  — UADDV

**Assembly:** `UADDV  <Dd>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Vd (5b) | Is the 64-bit name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
```

---

## UADDW, UADDW2 -- A64

### Encoding: `UADDW_asimddiff_W`  — UADDW

**Assembly:** `UADDW{2}  <Vd>.<Ta>, <Vn>.<Ta>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UADDWB -- A64

### Encoding: `uaddwb_z_zz_`  — UADDWB

**Assembly:** `UADDWB  <Zd>.<T>, <Zn>.<T>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## UADDWT -- A64

### Encoding: `uaddwt_z_zz_`  — UADDWT

**Assembly:** `UADDWT  <Zd>.<T>, <Zn>.<T>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## UBFIZ -- A64 *(alias for UBFM)*

### Encoding: `UBFIZ_UBFM_32M_bitfield`  [32-bit]  — UBFM

**Assembly:** `UBFIZ  <Wd>, <Wn>, #<lsb>, #<width> / UBFM  <Wd>, <Wn>, #(-<lsb>  MOD  32), #(<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "32-bit" variant: is the bit number of the lsb of the destination bitfield, in the range 0 to 31. |  |
| `<width>` | (imms :: immr) | For the "32-bit" variant: is the width of the bitfield, in the range 1 to 32-<lsb>. |  |
| `UBFM` | — |  |  |

### Encoding: `UBFIZ_UBFM_64M_bitfield`  [64-bit]  — UBFM

**Assembly:** `UBFIZ  <Xd>, <Xn>, #<lsb>, #<width> / UBFM  <Xd>, <Xn>, #(-<lsb>  MOD  64), #(<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "64-bit" variant: is the bit number of the lsb of the destination bitfield, in the range 0 to 63. |  |
| `<width>` | (imms :: immr) | For the "64-bit" variant: is the width of the bitfield, in the range 1 to 64-<lsb>. |  |
| `UBFM` | — |  |  |

---

## UBFM -- A64

### Encoding: `UBFM_32M_bitfield`  [32-bit]  — UBFM

**Assembly:** `UBFM  <Wd>, <Wn>, #<immr>, #<imms>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<immr>` | immr (6b) | For the "32-bit" variant: is the right rotate amount, in the range 0 to 31, encoded in the "immr" field. | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let r : integer = UInt(immr);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |
| `<imms>` | imms (6b) | For the "32-bit" variant: is the leftmost bit number to be moved from the source, in the range 0 to 31, encoded in the " | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let s : integer = UInt(imms);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |

**Pseudocode (Decode):**
```
if sf == '1' && N != '1' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && (N != '0' || immr[5] != '0' || imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let s : integer = UInt(imms);
let r : integer = UInt(immr);

var wmask : bits(datasize);
var tmask : bits(datasize);
(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE);
```

### Encoding: `UBFM_64M_bitfield`  [64-bit]  — UBFM

**Assembly:** `UBFM  <Xd>, <Xn>, #<immr>, #<imms>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<immr>` | immr (6b) | For the "64-bit" variant: is the right rotate amount, in the range 0 to 63, encoded in the "immr" field. | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let r : integer = UInt(immr);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |
| `<imms>` | imms (6b) | For the "64-bit" variant: is the leftmost bit number to be moved from the source, in the range 0 to 63, encoded in the " | if sf == '0' && (N != '0' \|\| immr[5] != '0' \|\| imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;<br>let s : integer = UInt(imms);<br>(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE); |

**Pseudocode (Decode):**
```
if sf == '1' && N != '1' then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && (N != '0' || immr[5] != '0' || imms[5] != '0') then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let s : integer = UInt(imms);
let r : integer = UInt(immr);

var wmask : bits(datasize);
var tmask : bits(datasize);
(wmask, tmask) = DecodeBitMasks{datasize}(N, imms, immr, FALSE);
```

---

## UBFX -- A64 *(alias for UBFM)*

### Encoding: `UBFX_UBFM_32M_bitfield`  [32-bit]  — UBFM

**Assembly:** `UBFX  <Wd>, <Wn>, #<lsb>, #<width> / UBFM  <Wd>, <Wn>, #<lsb>, #(<lsb>+<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "32-bit" variant: is the bit number of the lsb of the source bitfield, in the range 0 to 31. |  |
| `<width>` | (imms :: immr) | For the "32-bit" variant: is the width of the bitfield, in the range 1 to 32-<lsb>. |  |
| `UBFM` | — |  |  |

### Encoding: `UBFX_UBFM_64M_bitfield`  [64-bit]  — UBFM

**Assembly:** `UBFX  <Xd>, <Xn>, #<lsb>, #<width> / UBFM  <Xd>, <Xn>, #<lsb>, #(<lsb>+<width>-1)`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `<lsb>` | immr (6b) | For the "64-bit" variant: is the bit number of the lsb of the source bitfield, in the range 0 to 63. |  |
| `<width>` | (imms :: immr) | For the "64-bit" variant: is the width of the bitfield, in the range 1 to 64-<lsb>. |  |
| `UBFM` | — |  |  |

---

## UCLAMP -- A64

### Encoding: `uclamp_mz_zz_2`  — UCLAMP

**Assembly:** `UCLAMP  { <Zd1>.<T>-<Zd2>.<T> }, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
```

### Encoding: `uclamp_mz_zz_4`  — UCLAMP

**Assembly:** `UCLAMP  { <Zd1>.<T>-<Zd4>.<T> }, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
```

---

## UCLAMP -- A64

### Encoding: `uclamp_z_zz_`  — UCLAMP

**Assembly:** `UCLAMP  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## UCVTF (bottom, unpredicated) -- A64

### Encoding: `ucvtf_z_z_`  — UCVTF

**Assembly:** `UCVTF  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
```

---

## UCVTF (predicated) -- A64

### Encoding: `ucvtf_z_p_z_h2fp16`  — UCVTF

**Assembly:** `UCVTF  <Zd>.H, <Pg>/M, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 16;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `ucvtf_z_p_z_h2fp16z`  — UCVTF

**Assembly:** `UCVTF  <Zd>.H, <Pg>/Z, <Zn>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 16;
let d_esize : integer{} = 16;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `ucvtf_z_p_z_w2fp16`  — UCVTF

**Assembly:** `UCVTF  <Zd>.H, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 16;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `ucvtf_z_p_z_w2fp16z`  — UCVTF

**Assembly:** `UCVTF  <Zd>.H, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 16;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `ucvtf_z_p_z_w2s`  — UCVTF

**Assembly:** `UCVTF  <Zd>.S, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `ucvtf_z_p_z_w2sz`  — UCVTF

**Assembly:** `UCVTF  <Zd>.S, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `ucvtf_z_p_z_w2d`  — UCVTF

**Assembly:** `UCVTF  <Zd>.D, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `ucvtf_z_p_z_w2dz`  — UCVTF

**Assembly:** `UCVTF  <Zd>.D, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 32;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `ucvtf_z_p_z_x2fp16`  — UCVTF

**Assembly:** `UCVTF  <Zd>.H, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 16;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `ucvtf_z_p_z_x2fp16z`  — UCVTF

**Assembly:** `UCVTF  <Zd>.H, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 16;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `ucvtf_z_p_z_x2s`  — UCVTF

**Assembly:** `UCVTF  <Zd>.S, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `ucvtf_z_p_z_x2sz`  — UCVTF

**Assembly:** `UCVTF  <Zd>.S, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 32;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

### Encoding: `ucvtf_z_p_z_x2d`  — UCVTF

**Assembly:** `UCVTF  <Zd>.D, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = TRUE;
```

### Encoding: `ucvtf_z_p_z_x2dz`  — UCVTF

**Assembly:** `UCVTF  <Zd>.D, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let s_esize : integer{} = 64;
let d_esize : integer{} = 64;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
let merging : boolean = FALSE;
```

---

## UCVTF (scalar SIMD&FP) -- A64

### Encoding: `UCVTF_sisd_32H`  [32-bit to half-precision]  — UCVTF

**Assembly:** `UCVTF  <Hd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
let rounding : FPRounding = FPRoundingMode(FPCR());
```

### Encoding: `UCVTF_sisd_32D`  [32-bit to double-precision]  — UCVTF

**Assembly:** `UCVTF  <Dd>, <Sn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Sn>` | Rn (5b) | Is the 32-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
let rounding : FPRounding = FPRoundingMode(FPCR());
```

### Encoding: `UCVTF_sisd_64H`  [64-bit to half-precision]  — UCVTF

**Assembly:** `UCVTF  <Hd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
let rounding : FPRounding = FPRoundingMode(FPCR());
```

### Encoding: `UCVTF_sisd_64S`  [64-bit to single-precision]  — UCVTF

**Assembly:** `UCVTF  <Sd>, <Dn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Dn>` | Rn (5b) | Is the 64-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FPRCVT) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let fltsize : integer{} = 8 << UInt(ftype XOR '10');
let rounding : FPRounding = FPRoundingMode(FPCR());
```

---

## UCVTF (scalar, fixed-point) -- A64

### Encoding: `UCVTF_H32_float2fix`  [32-bit to half-precision]  — UCVTF

**Assembly:** `UCVTF  <Hd>, <Wn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "32-bit to double-precision", "32-bit to half-precision", and "32-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = TRUE;
```

### Encoding: `UCVTF_H64_float2fix`  [64-bit to half-precision]  — UCVTF

**Assembly:** `UCVTF  <Hd>, <Xn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "64-bit to double-precision", "64-bit to half-precision", and "64-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = TRUE;
```

### Encoding: `UCVTF_S32_float2fix`  [32-bit to single-precision]  — UCVTF

**Assembly:** `UCVTF  <Sd>, <Wn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "32-bit to double-precision", "32-bit to half-precision", and "32-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = TRUE;
```

### Encoding: `UCVTF_S64_float2fix`  [64-bit to single-precision]  — UCVTF

**Assembly:** `UCVTF  <Sd>, <Xn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "64-bit to double-precision", "64-bit to half-precision", and "64-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = TRUE;
```

### Encoding: `UCVTF_D32_float2fix`  [32-bit to double-precision]  — UCVTF

**Assembly:** `UCVTF  <Dd>, <Wn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "32-bit to double-precision", "32-bit to half-precision", and "32-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = TRUE;
```

### Encoding: `UCVTF_D64_float2fix`  [64-bit to double-precision]  — UCVTF

**Assembly:** `UCVTF  <Dd>, <Xn>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | scale (6b) | For the "64-bit to double-precision", "64-bit to half-precision", and "64-bit to single-precision" variants: is the numb | if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;<br>let fracbits : integer = 64 - UInt(scale); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;
if sf == '0' && scale[5] == '0' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');

let fracbits : integer = 64 - UInt(scale);

let unsigned : boolean = TRUE;
```

---

## UCVTF (scalar, integer) -- A64

### Encoding: `UCVTF_H32_float2int`  [32-bit to half-precision]  — UCVTF

**Assembly:** `UCVTF  <Hd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = TRUE;
```

### Encoding: `UCVTF_S32_float2int`  [32-bit to single-precision]  — UCVTF

**Assembly:** `UCVTF  <Sd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = TRUE;
```

### Encoding: `UCVTF_D32_float2int`  [32-bit to double-precision]  — UCVTF

**Assembly:** `UCVTF  <Dd>, <Wn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = TRUE;
```

### Encoding: `UCVTF_H64_float2int`  [64-bit to half-precision]  — UCVTF

**Assembly:** `UCVTF  <Hd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = TRUE;
```

### Encoding: `UCVTF_S64_float2int`  [64-bit to single-precision]  — UCVTF

**Assembly:** `UCVTF  <Sd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Sd>` | Rd (5b) | Is the 32-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = TRUE;
```

### Encoding: `UCVTF_D64_float2int`  [64-bit to double-precision]  — UCVTF

**Assembly:** `UCVTF  <Dd>, <Xn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Dd>` | Rd (5b) | Is the 64-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FP) then EndOfDecode(Decode_UNDEF); end;
if ftype == '10' then EndOfDecode(Decode_UNDEF); end;
if ftype == '11' && !IsFeatureImplemented(FEAT_FP16) then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let intsize : integer{} = 32 << UInt(sf);
let decode_fltsize : integer{} = 8 << UInt(ftype XOR '10');
let unsigned : boolean    = TRUE;
```

---

## UCVTF (vector, fixed-point) -- A64

### Encoding: `UCVTF_asisdshf_C`  — UCVTF

**Assembly:** `UCVTF  <V><d>, <V><n>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh IN {'000x'} || (immh IN {'001x'} && !IsFeatureImplemented(FEAT_FP16)) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = if immh IN {'1xxx'} then 64 else if immh IN {'01xx'} then 32 else 16;
let datasize : integer{} = esize;
let elements : integer = 1;

let fracbits : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
```

### Encoding: `UCVTF_asimdshf_C`  — UCVTF

**Assembly:** `UCVTF  <Vd>.<T>, <Vn>.<T>, #<fbits>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<fbits>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh IN {'000x'} || (immh IN {'001x'} && !IsFeatureImplemented(FEAT_FP16)) then
    EndOfDecode(Decode_UNDEF);
end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = if immh IN {'1xxx'} then 64 else if immh IN {'01xx'} then 32 else 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let fracbits : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
```

---

## UCVTF (vector, integer) -- A64

### Encoding: `UCVTF_asisdmiscfp16_R`  — UCVTF

**Assembly:** `UCVTF  <Hd>, <Hn>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Hd>` | Rd (5b) | Is the 16-bit name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Hn>` | Rn (5b) | Is the 16-bit name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let esize : integer{} = 16;
let datasize : integer{} = esize;
let elements : integer = 1;

let unsigned : boolean = TRUE;
```

### Encoding: `UCVTF_asisdmisc_R`  — UCVTF

**Assembly:** `UCVTF  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = esize;
let elements : integer = 1;
let unsigned : boolean = TRUE;
```

### Encoding: `UCVTF_asimdmiscfp16_R`  — UCVTF

**Assembly:** `UCVTF  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) || !IsFeatureImplemented(FEAT_FP16) then
    EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

let esize : integer{} = 16;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = TRUE;
```

### Encoding: `UCVTF_asimdmisc_R`  — UCVTF

**Assembly:** `UCVTF  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if sz::Q == '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = TRUE;
```

---

## UCVTF -- A64

### Encoding: `ucvtf_mz_z_2`  — UCVTF

**Assembly:** `UCVTF  { <Zd1>.S-<Zd2>.S }, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the source multi-vector group, enc | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
```

### Encoding: `ucvtf_mz_z_4`  — UCVTF

**Assembly:** `UCVTF  { <Zd1>.S-<Zd4>.S }, { <Zn1>.S-<Zn4>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the source multi-vector group, en | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
```

---

## UCVTFLT -- A64

### Encoding: `ucvtflt_z_z_`  — UCVTFLT

**Assembly:** `UCVTFLT  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let rounding : FPRounding = FPRoundingMode(FPCR());
```

---

## UDF -- A64

### Encoding: `UDF_only_perm_undef`  — UDF

**Assembly:** `UDF  #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<imm>` | imm16 (16b) | is a 16-bit unsigned immediate, in the range 0 to 65535, encoded in the "imm16" field. The PE ignores the value of this  | // The imm16 field is ignored by hardware. |

**Pseudocode (Decode):**
```
// The imm16 field is ignored by hardware.
EndOfDecode(Decode_UNDEF);
```

---

## UDIV -- A64

### Encoding: `UDIV_32_dp_2src`  [32-bit]  — UDIV

**Assembly:** `UDIV  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `UDIV_64_dp_2src`  [64-bit]  — UDIV

**Assembly:** `UDIV  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## UDIV -- A64

### Encoding: `udiv_z_p_zz_`  — UDIV

**Assembly:** `UDIV  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UDIVR -- A64

### Encoding: `udivr_z_p_zz_`  — UDIVR

**Assembly:** `UDIVR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UDOT (2-way, indexed) -- A64

### Encoding: `udot_z16_zzzi_h`  — UDOT

**Assembly:** `UDOT  <Zda>.H, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "8-bit to 16-bit" variant: is the immediate index of a pair of 8-bit elements within each 128-bit vector segment |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `udot_z32_zzzi_`  — UDOT

**Assembly:** `UDOT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "16-bit to 32-bit" variant: is the immediate index of a pair of 16-bit elements within each 128-bit vector segme | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UDOT (2-way, multiple and indexed vector) -- A64

### Encoding: `udot_za32_zzi_2xi`  — UDOT

**Assembly:** `UDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `udot_za32_zzi_4xi`  — UDOT

**Assembly:** `UDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## UDOT (2-way, multiple and single vector) -- A64

### Encoding: `udot_za32_zzv_2x1`  — UDOT

**Assembly:** `UDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `udot_za32_zzv_4x1`  — UDOT

**Assembly:** `UDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## UDOT (2-way, multiple vectors) -- A64

### Encoding: `udot_za32_zzw_2x2`  — UDOT

**Assembly:** `UDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `udot_za32_zzw_4x4`  — UDOT

**Assembly:** `UDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## UDOT (2-way, vectors) -- A64

### Encoding: `udot_z16_zzz_h`  — UDOT

**Assembly:** `UDOT  <Zda>.H, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `udot_z32_zzz_`  — UDOT

**Assembly:** `UDOT  <Zda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UDOT (4-way, indexed) -- A64

### Encoding: `udot_z_zzzi_s`  — UDOT

**Assembly:** `UDOT  <Zda>.S, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "8-bit to 32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm"  | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | For the "8-bit to 32-bit" variant: is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vec | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

### Encoding: `udot_z_zzzi_d`  — UDOT

**Assembly:** `UDOT  <Zda>.D, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "16-bit to 64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm | let m : integer = UInt(Zm); |
| `<imm>` | i1 (1b) | For the "16-bit to 64-bit" variant: is the immediate index of a 64-bit group of four 16-bit values within each 128-bit v | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let index : integer = UInt(i1);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UDOT (4-way, multiple and indexed vector) -- A64

### Encoding: `udot_za_zzi_s2xi`  — UDOT

**Assembly:** `UDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of 32-bit elements" and "Two ZA single-vectors of 64-bit elements" variants: is the name  | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "Four ZA single-vectors of 32-bit elements" and "Two ZA single-vectors of 32-bit elements" variants: is the imme | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `udot_za_zzi_d2xi`  — UDOT

**Assembly:** `UDOT  ZA.D[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors of 32-bit elements" and "Two ZA single-vectors of 64-bit elements" variants: is the name  | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "Four ZA single-vectors of 64-bit elements" and "Two ZA single-vectors of 64-bit elements" variants: is the imme | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
let nreg : integer{} = 2;
```

### Encoding: `udot_za_zzi_s4xi`  — UDOT

**Assembly:** `UDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of 32-bit elements" and "Four ZA single-vectors of 64-bit elements" variants: is the nam | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "Four ZA single-vectors of 32-bit elements" and "Two ZA single-vectors of 32-bit elements" variants: is the imme | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

### Encoding: `udot_za_zzi_d4xi`  — UDOT

**Assembly:** `UDOT  ZA.D[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors of 32-bit elements" and "Four ZA single-vectors of 64-bit elements" variants: is the nam | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "Four ZA single-vectors of 64-bit elements" and "Two ZA single-vectors of 64-bit elements" variants: is the imme | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
let nreg : integer{} = 4;
```

---

## UDOT (4-way, multiple and single vector) -- A64

### Encoding: `udot_za_zzv_2x1`  — UDOT

**Assembly:** `UDOT  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `udot_za_zzv_4x1`  — UDOT

**Assembly:** `UDOT  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## UDOT (4-way, multiple vectors) -- A64

### Encoding: `udot_za_zzw_2x2`  — UDOT

**Assembly:** `UDOT  ZA.<T>[<Wv>, <offs>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, { <Zm1>.<Tb>-<Zm2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `udot_za_zzw_4x4`  — UDOT

**Assembly:** `UDOT  ZA.<T>[<Wv>, <offs>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, { <Zm1>.<Tb>-<Zm4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32 << UInt(sz);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## UDOT (4-way, vectors) -- A64

### Encoding: `udot_z_zzz_`  — UDOT

**Assembly:** `UDOT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UDOT (by element) -- A64

### Encoding: `UDOT_asimdelem_D`  — UDOT

**Assembly:** `UDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.4B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | (M :: Rm) | Is the name of the second SIMD&FP source register, encoded in the "M:Rm" fields. |  |
| `<index>` | (H :: L) | Is the element index, encoded in the "H:L" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_DotProd) then EndOfDecode(Decode_UNDEF); end;
if size  != '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(M::Rm);
let index : integer = UInt(H::L);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UDOT (vector) -- A64

### Encoding: `UDOT_asimdsame2_D`  — UDOT

**Assembly:** `UDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_DotProd) then EndOfDecode(Decode_UNDEF); end;
if size != '10' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UHADD -- A64

### Encoding: `UHADD_asimdsame_only`  — UHADD

**Assembly:** `UHADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UHADD -- A64

### Encoding: `uhadd_z_p_zz_`  — UHADD

**Assembly:** `UHADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UHSUB -- A64

### Encoding: `UHSUB_asimdsame_only`  — UHSUB

**Assembly:** `UHSUB  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UHSUB -- A64

### Encoding: `uhsub_z_p_zz_`  — UHSUB

**Assembly:** `UHSUB  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UHSUBR -- A64

### Encoding: `uhsubr_z_p_zz_`  — UHSUBR

**Assembly:** `UHSUBR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UMADDL -- A64

### Encoding: `UMADDL_64WA_dp_3src`  — UMADDL

**Assembly:** `UMADDL  <Xd>, <Wn>, <Wm>, <Xa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Xa>` | Ra (5b) | Is the 64-bit name of the third general-purpose source register holding the addend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
```

---

## UMAX (immediate) -- A64

### Encoding: `UMAX_32U_minmax_imm`  [32-bit]  — UMAX

**Assembly:** `UMAX  <Wd>, <Wn>, #<uimm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<uimm>` | imm8 (8b) | Is an unsigned immediate, in the range 0 to 255, encoded in the "imm8" field. | let imm : integer = UInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let imm : integer = UInt(imm8);
```

### Encoding: `UMAX_64U_minmax_imm`  [64-bit]  — UMAX

**Assembly:** `UMAX  <Xd>, <Xn>, #<uimm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<uimm>` | imm8 (8b) | Is an unsigned immediate, in the range 0 to 255, encoded in the "imm8" field. | let imm : integer = UInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let imm : integer = UInt(imm8);
```

---

## UMAX (immediate) -- A64

### Encoding: `umax_z_zi_`  — UMAX

**Assembly:** `UMAX  <Zdn>.<T>, <Zdn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is the unsigned immediate operand, in the range 0 to 255, encoded in the "imm8" field. | let imm : integer = if unsigned then UInt(imm8) else SInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
let unsigned : boolean = TRUE;
let imm : integer = if unsigned then UInt(imm8) else SInt(imm8);
```

---

## UMAX (multiple and single vector) -- A64

### Encoding: `umax_mz_zzv_2x1`  — UMAX

**Assembly:** `UMAX  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
let unsigned : boolean = TRUE;
```

### Encoding: `umax_mz_zzv_4x1`  — UMAX

**Assembly:** `UMAX  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
let unsigned : boolean = TRUE;
```

---

## UMAX (multiple vectors) -- A64

### Encoding: `umax_mz_zzw_2x2`  — UMAX

**Assembly:** `UMAX  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
let unsigned : boolean = TRUE;
```

### Encoding: `umax_mz_zzw_4x4`  — UMAX

**Assembly:** `UMAX  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
let unsigned : boolean = TRUE;
```

---

## UMAX (register) -- A64

### Encoding: `UMAX_32_dp_2src`  [32-bit]  — UMAX

**Assembly:** `UMAX  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `UMAX_64_dp_2src`  [64-bit]  — UMAX

**Assembly:** `UMAX  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## UMAX (vectors) -- A64

### Encoding: `umax_z_p_zz_`  — UMAX

**Assembly:** `UMAX  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UMAX -- A64

### Encoding: `UMAX_asimdsame_only`  — UMAX

**Assembly:** `UMAX  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMAXP -- A64

### Encoding: `UMAXP_asimdsame_only`  — UMAXP

**Assembly:** `UMAXP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMAXP -- A64

### Encoding: `umaxp_z_p_zz_`  — UMAXP

**Assembly:** `UMAXP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## UMAXQV -- A64

### Encoding: `umaxqv_z_p_z_`  — UMAXQV

**Assembly:** `UMAXQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let unsigned : boolean = TRUE;
```

---

## UMAXV -- A64

### Encoding: `UMAXV_asimdall_only`  — UMAXV

**Assembly:** `UMAXV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '100' then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = TRUE;
```

---

## UMAXV -- A64

### Encoding: `umaxv_r_p_z_`  — UMAXV

**Assembly:** `UMAXV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let unsigned : boolean = TRUE;
```

---

## UMIN (immediate) -- A64

### Encoding: `UMIN_32U_minmax_imm`  [32-bit]  — UMIN

**Assembly:** `UMIN  <Wd>, <Wn>, #<uimm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<uimm>` | imm8 (8b) | Is an unsigned immediate, in the range 0 to 255, encoded in the "imm8" field. | let imm : integer = UInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let imm : integer = UInt(imm8);
```

### Encoding: `UMIN_64U_minmax_imm`  [64-bit]  — UMIN

**Assembly:** `UMIN  <Xd>, <Xn>, #<uimm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<uimm>` | imm8 (8b) | Is an unsigned immediate, in the range 0 to 255, encoded in the "imm8" field. | let imm : integer = UInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let datasize : integer{} = 32 << UInt(sf);
let imm : integer = UInt(imm8);
```

---

## UMIN (immediate) -- A64

### Encoding: `umin_z_zi_`  — UMIN

**Assembly:** `UMIN  <Zdn>.<T>, <Zdn>.<T>, #<imm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is the unsigned immediate operand, in the range 0 to 255, encoded in the "imm8" field. | let imm : integer = if unsigned then UInt(imm8) else SInt(imm8); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
let unsigned : boolean = TRUE;
let imm : integer = if unsigned then UInt(imm8) else SInt(imm8);
```

---

## UMIN (multiple and single vector) -- A64

### Encoding: `umin_mz_zzv_2x1`  — UMIN

**Assembly:** `UMIN  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
let unsigned : boolean = TRUE;
```

### Encoding: `umin_mz_zzv_4x1`  — UMIN

**Assembly:** `UMIN  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
let unsigned : boolean = TRUE;
```

---

## UMIN (multiple vectors) -- A64

### Encoding: `umin_mz_zzw_2x2`  — UMIN

**Assembly:** `UMIN  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
let unsigned : boolean = TRUE;
```

### Encoding: `umin_mz_zzw_4x4`  — UMIN

**Assembly:** `UMIN  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
let unsigned : boolean = TRUE;
```

---

## UMIN (register) -- A64

### Encoding: `UMIN_32_dp_2src`  [32-bit]  — UMIN

**Assembly:** `UMIN  <Wd>, <Wn>, <Wm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

### Encoding: `UMIN_64_dp_2src`  [64-bit]  — UMIN

**Assembly:** `UMIN  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_CSSC) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 32 << UInt(sf);
```

---

## UMIN (vectors) -- A64

### Encoding: `umin_z_p_zz_`  — UMIN

**Assembly:** `UMIN  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UMIN -- A64

### Encoding: `UMIN_asimdsame_only`  — UMIN

**Assembly:** `UMIN  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMINP -- A64

### Encoding: `UMINP_asimdsame_only`  — UMINP

**Assembly:** `UMINP  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMINP -- A64

### Encoding: `uminp_z_p_zz_`  — UMINP

**Assembly:** `UMINP  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## UMINQV -- A64

### Encoding: `uminqv_z_p_z_`  — UMINQV

**Assembly:** `UMINQV  <Vd>.<T>, <Pg>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Vd (5b) | Is the name of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let unsigned : boolean = TRUE;
```

---

## UMINV -- A64

### Encoding: `UMINV_asimdall_only`  — UMINV

**Assembly:** `UMINV  <V><d>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '100' then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let unsigned : boolean = TRUE;
```

---

## UMINV -- A64

### Encoding: `uminv_r_p_z_`  — UMINV

**Assembly:** `UMINV  <V><d>, <Pg>, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Vd (5b) | Is the number [0-31] of the destination SIMD&FP register, encoded in the "Vd" field. | let d : integer = UInt(Vd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Vd);
let unsigned : boolean = TRUE;
```

---

## UMLAL (multiple and indexed vector) -- A64

### Encoding: `umlal_za_zzi_1`  — UMLAL

**Assembly:** `UMLAL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `umlal_za_zzi_2xi`  — UMLAL

**Assembly:** `UMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `umlal_za_zzi_4xi`  — UMLAL

**Assembly:** `UMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## UMLAL (multiple and single vector) -- A64

### Encoding: `umlal_za_zzv_1`  — UMLAL

**Assembly:** `UMLAL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let nreg : integer{} = 1;
```

### Encoding: `umlal_za_zzv_2x1`  — UMLAL

**Assembly:** `UMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `umlal_za_zzv_4x1`  — UMLAL

**Assembly:** `UMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## UMLAL (multiple vectors) -- A64

### Encoding: `umlal_za_zzw_2x2`  — UMLAL

**Assembly:** `UMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `umlal_za_zzw_4x4`  — UMLAL

**Assembly:** `UMLAL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## UMLAL, UMLAL2 (by element) -- A64

### Encoding: `UMLAL_asimdelem_L`  — UMLAL

**Assembly:** `UMLAL{2}  <Vd>.<Ta>, <Vn>.<Tb>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMLAL, UMLAL2 (vector) -- A64

### Encoding: `UMLAL_asimddiff_L`  — UMLAL

**Assembly:** `UMLAL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMLALB (indexed) -- A64

### Encoding: `umlalb_z_zzzi_s`  — UMLALB

**Assembly:** `UMLALB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

### Encoding: `umlalb_z_zzzi_d`  — UMLALB

**Assembly:** `UMLALB  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

---

## UMLALB (vectors) -- A64

### Encoding: `umlalb_z_zzz_`  — UMLALB

**Assembly:** `UMLALB  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UMLALL (multiple and indexed vector) -- A64

### Encoding: `umlall_za_zzi_s`  — UMLALL

**Assembly:** `UMLALL  ZA.S[<Wv>, <offs1>:<offs4>], <Zn>.B, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the first vecto | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the fourth vect | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 1;
```

### Encoding: `umlall_za_zzi_d`  — UMLALL

**Assembly:** `UMLALL  ZA.D[<Wv>, <offs1>:<offs4>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the first vecto | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the fourth vect | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `umlall_za_zzi_s2xi`  — UMLALL

**Assembly:** `UMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors of 32-bit elements" and "Two ZA quad-vectors of 64-bit elements" variants: is the name of t | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 2;
```

### Encoding: `umlall_za_zzi_d2xi`  — UMLALL

**Assembly:** `UMLALL  ZA.D[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors of 32-bit elements" and "Two ZA quad-vectors of 64-bit elements" variants: is the name of t | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `umlall_za_zzi_s4xi`  — UMLALL

**Assembly:** `UMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors of 32-bit elements" and "Four ZA quad-vectors of 64-bit elements" variants: is the name of | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 4;
```

### Encoding: `umlall_za_zzi_d4xi`  — UMLALL

**Assembly:** `UMLALL  ZA.D[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors of 32-bit elements" and "Four ZA quad-vectors of 64-bit elements" variants: is the name of | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## UMLALL (multiple and single vector) -- A64

### Encoding: `umlall_za_zzv_1`  — UMLALL

**Assembly:** `UMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>], <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let nreg : integer{} = 1;
```

### Encoding: `umlall_za_zzv_2x1`  — UMLALL

**Assembly:** `UMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `umlall_za_zzv_4x1`  — UMLALL

**Assembly:** `UMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## UMLALL (multiple vectors) -- A64

### Encoding: `umlall_za_zzw_2x2`  — UMLALL

**Assembly:** `UMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, { <Zm1>.<Tb>-<Zm2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vecto | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vect | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `umlall_za_zzw_4x4`  — UMLALL

**Assembly:** `UMLALL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, { <Zm1>.<Tb>-<Zm4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vect | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vec | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## UMLALT (indexed) -- A64

### Encoding: `umlalt_z_zzzi_s`  — UMLALT

**Assembly:** `UMLALT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

### Encoding: `umlalt_z_zzzi_d`  — UMLALT

**Assembly:** `UMLALT  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

---

## UMLALT (vectors) -- A64

### Encoding: `umlalt_z_zzz_`  — UMLALT

**Assembly:** `UMLALT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UMLSL (multiple and indexed vector) -- A64

### Encoding: `umlsl_za_zzi_1`  — UMLSL

**Assembly:** `UMLSL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `umlsl_za_zzi_2xi`  — UMLSL

**Assembly:** `UMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `umlsl_za_zzi_4xi`  — UMLSL

**Assembly:** `UMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | Is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## UMLSL (multiple and single vector) -- A64

### Encoding: `umlsl_za_zzv_1`  — UMLSL

**Assembly:** `UMLSL  ZA.S[<Wv>, <offs1>:<offs2>], <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3::'0');
let nreg : integer{} = 1;
```

### Encoding: `umlsl_za_zzv_2x1`  — UMLSL

**Assembly:** `UMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `umlsl_za_zzv_4x1`  — UMLSL

**Assembly:** `UMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## UMLSL (multiple vectors) -- A64

### Encoding: `umlsl_za_zzw_2x2`  — UMLSL

**Assembly:** `UMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx2}], { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 2;
```

### Encoding: `umlsl_za_zzw_4x4`  — UMLSL

**Assembly:** `UMLSL  ZA.S[<Wv>, <offs1>:<offs2>{, VGx4}], { <Zn1>.H-<Zn4>.H }, { <Zm1>.H-<Zm4>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | Is the first vector select offset, encoded as "off2" field times 2. | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | Is the second vector select offset, encoded as "off2" field times 2 plus 1. | let offset : integer = UInt(off2::'0'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA double-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off2::'0');
let nreg : integer{} = 4;
```

---

## UMLSL, UMLSL2 (by element) -- A64

### Encoding: `UMLSL_asimdelem_L`  — UMLSL

**Assembly:** `UMLSL{2}  <Vd>.<Ta>, <Vn>.<Tb>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMLSL, UMLSL2 (vector) -- A64

### Encoding: `UMLSL_asimddiff_L`  — UMLSL

**Assembly:** `UMLSL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMLSLB (indexed) -- A64

### Encoding: `umlslb_z_zzzi_s`  — UMLSLB

**Assembly:** `UMLSLB  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

### Encoding: `umlslb_z_zzzi_d`  — UMLSLB

**Assembly:** `UMLSLB  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 0;
```

---

## UMLSLB (vectors) -- A64

### Encoding: `umlslb_z_zzz_`  — UMLSLB

**Assembly:** `UMLSLB  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UMLSLL (multiple and indexed vector) -- A64

### Encoding: `umlsll_za_zzi_s`  — UMLSLL

**Assembly:** `UMLSLL  ZA.S[<Wv>, <offs1>:<offs4>], <Zn>.B, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the first vecto | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the fourth vect | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 1;
```

### Encoding: `umlsll_za_zzi_d`  — UMLSLL

**Assembly:** `UMLSLL  ZA.D[<Wv>, <offs1>:<offs4>], <Zn>.H, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the first vecto | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector of 32-bit elements" and "One ZA quad-vector of 64-bit elements" variants: is the fourth vect | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 1;
```

### Encoding: `umlsll_za_zzi_s2xi`  — UMLSLL

**Assembly:** `UMLSLL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors of 32-bit elements" and "Two ZA quad-vectors of 64-bit elements" variants: is the name of t | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 2;
```

### Encoding: `umlsll_za_zzi_d2xi`  — UMLSLL

**Assembly:** `UMLSLL  ZA.D[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors of 32-bit elements" and "Two ZA quad-vectors of 64-bit elements" variants: is the name of t | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 2;
```

### Encoding: `umlsll_za_zzi_s4xi`  — UMLSLL

**Assembly:** `UMLSLL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors of 32-bit elements" and "Four ZA quad-vectors of 64-bit elements" variants: is the name of | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | For the "Four ZA quad-vectors of 32-bit elements", "One ZA quad-vector of 32-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 4;
```

### Encoding: `umlsll_za_zzi_d4xi`  — UMLSLL

**Assembly:** `UMLSLL  ZA.D[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors of 32-bit elements", "Four ZA quad-vectors of 64-bit elements", "Two ZA quad-vectors of 32 | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors of 32-bit elements" and "Four ZA quad-vectors of 64-bit elements" variants: is the name of | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i3h :: i3l) | For the "Four ZA quad-vectors of 64-bit elements", "One ZA quad-vector of 64-bit elements", and "Two ZA quad-vectors of  |  |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i3h::i3l);
let nreg : integer{} = 4;
```

---

## UMLSLL (multiple and single vector) -- A64

### Encoding: `umlsll_za_zzv_1`  — UMLSLL

**Assembly:** `UMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>], <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let nreg : integer{} = 1;
```

### Encoding: `umlsll_za_zzv_2x1`  — UMLSLL

**Assembly:** `UMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `umlsll_za_zzv_4x1`  — UMLSLL

**Assembly:** `UMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## UMLSLL (multiple vectors) -- A64

### Encoding: `umlsll_za_zzw_2x2`  — UMLSLL

**Assembly:** `UMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.<Tb>-<Zn2>.<Tb> }, { <Zm1>.<Tb>-<Zm2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vecto | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vect | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `umlsll_za_zzw_4x4`  — UMLSLL

**Assembly:** `UMLSLL  ZA.<T>[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.<Tb>-<Zn4>.<Tb> }, { <Zm1>.<Tb>-<Zm4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<T>` | — |  |  |
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vect | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vec | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if sz == '1' && !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32 << UInt(sz);
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## UMLSLT (indexed) -- A64

### Encoding: `umlslt_z_zzzi_s`  — UMLSLT

**Assembly:** `UMLSLT  <Zda>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

### Encoding: `umlslt_z_zzzi_d`  — UMLSLT

**Assembly:** `UMLSLT  <Zda>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let sel : integer = 1;
```

---

## UMLSLT (vectors) -- A64

### Encoding: `umlslt_z_zzz_`  — UMLSLT

**Assembly:** `UMLSLT  <Zda>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## UMMLA (vector) -- A64

### Encoding: `UMMLA_asimdsame2_G`  — UMMLA

**Assembly:** `UMMLA  <Vd>.4S, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_I8MM) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

---

## UMMLA -- A64

### Encoding: `ummla_z_zzz_`  — UMMLA

**Assembly:** `UMMLA  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_I8MM) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

---

## UMNEGL -- A64 *(alias for UMSUBL)*

### Encoding: `UMNEGL_UMSUBL_64WA_dp_3src`  — UMSUBL

**Assembly:** `UMNEGL  <Xd>, <Wn>, <Wm> / UMSUBL  <Xd>, <Wn>, <Wm>, XZR`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. |  |
| `UMSUBL` | — |  |  |

---

## UMOP4A (2-way) -- A64

### Encoding: `umop4a_za32_zz_h1x2`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.S, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za32_zz_h1x1`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za32_zz_h2x1`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za32_zz_h2x2`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

---

## UMOP4A (4-way) -- A64

### Encoding: `umop4a_za_zz_b1x2`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.S, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za_zz_b1x1`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za_zz_b2x1`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za_zz_b2x2`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za_zz_h1x2`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.D, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za_zz_h1x1`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.D, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za_zz_h2x1`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4a_za_zz_h2x2`  — UMOP4A

**Assembly:** `UMOP4A  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

---

## UMOP4S (2-way) -- A64

### Encoding: `umop4s_za32_zz_h1x2`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.S, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za32_zz_h1x1`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.S, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za32_zz_h2x1`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za32_zz_h2x2`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

---

## UMOP4S (4-way) -- A64

### Encoding: `umop4s_za_zz_b1x2`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.S, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za_zz_b1x1`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za_zz_b2x1`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za_zz_b2x2`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za_zz_h1x2`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.D, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za_zz_h1x1`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.D, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za_zz_h2x1`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umop4s_za_zz_h2x2`  — UMOP4S

**Assembly:** `UMOP4S  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

---

## UMOPA (2-way) -- A64

### Encoding: `umopa_za32_pp_zz_16`  — UMOPA

**Assembly:** `UMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let unsigned : boolean = TRUE;
```

---

## UMOPA (4-way) -- A64

### Encoding: `umopa_za_pp_zz_32`  — UMOPA

**Assembly:** `UMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umopa_za_pp_zz_64`  — UMOPA

**Assembly:** `UMOPA  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

---

## UMOPS (2-way) -- A64

### Encoding: `umops_za32_pp_zz_16`  — UMOPS

**Assembly:** `UMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let unsigned : boolean = TRUE;
```

---

## UMOPS (4-way) -- A64

### Encoding: `umops_za_pp_zz_32`  — UMOPS

**Assembly:** `UMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

### Encoding: `umops_za_pp_zz_64`  — UMOPS

**Assembly:** `UMOPS  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

---

## UMOV -- A64

### Encoding: `UMOV_asimdins_W_w`  [32-bit]  — UMOV

**Assembly:** `UMOV  <Wd>, <Vn>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'x0000' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = LowestSetBitNZ(imm5[3:0]);

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << size;
let datasize : integer{} = 32 << UInt(Q);
if datasize == 64 && esize < 64 then EndOfDecode(Decode_UNDEF); end;
if datasize == 32 && esize >= 64 then EndOfDecode(Decode_UNDEF); end;
let index : integer = UInt(imm5[4:size+1]);
let idxdsize : integer{} = 64 << UInt(imm5[4]);
```

### Encoding: `UMOV_asimdins_X_x`  [64-bit]  — UMOV

**Assembly:** `UMOV  <Xd>, <Vn>.D[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<index>` | imm5 (5b) | For the "64-bit" variant: is the element index encoded in "imm5<4>". | if imm5 == 'x0000' then EndOfDecode(Decode_UNDEF); end;<br>let size : integer{} = LowestSetBitNZ(imm5[3:0]);<br>let index : integer = UInt(imm5[4:size+1]);<br>let idxdsize : integer{} = 64 << UInt(imm5[4]); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if imm5 == 'x0000' then EndOfDecode(Decode_UNDEF); end;
let size : integer{} = LowestSetBitNZ(imm5[3:0]);

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << size;
let datasize : integer{} = 32 << UInt(Q);
if datasize == 64 && esize < 64 then EndOfDecode(Decode_UNDEF); end;
if datasize == 32 && esize >= 64 then EndOfDecode(Decode_UNDEF); end;
let index : integer = UInt(imm5[4:size+1]);
let idxdsize : integer{} = 64 << UInt(imm5[4]);
```

---

## UMSUBL -- A64

### Encoding: `UMSUBL_64WA_dp_3src`  — UMSUBL

**Assembly:** `UMSUBL  <Xd>, <Wn>, <Wm>, <Xa>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Xa>` | Ra (5b) | Is the 64-bit name of the third general-purpose source register holding the minuend, encoded in the "Ra" field. | let a : integer{} = UInt(Ra); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let a : integer{} = UInt(Ra);
```

---

## UMULH (predicated) -- A64

### Encoding: `umulh_z_p_zz_`  — UMULH

**Assembly:** `UMULH  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UMULH (unpredicated) -- A64

### Encoding: `umulh_z_zz_`  — UMULH

**Assembly:** `UMULH  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
```

---

## UMULH -- A64

### Encoding: `UMULH_64_dp_3src`  — UMULH

**Assembly:** `UMULH  <Xd>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## UMULL -- A64 *(alias for UMADDL)*

### Encoding: `UMULL_UMADDL_64WA_dp_3src`  — UMADDL

**Assembly:** `UMULL  <Xd>, <Wn>, <Wm> / UMADDL  <Xd>, <Wn>, <Wm>, XZR`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the first general-purpose source register holding the multiplicand, encoded in the "Rn" field. |  |
| `<Wm>` | Rm (5b) | Is the 32-bit name of the second general-purpose source register holding the multiplier, encoded in the "Rm" field. |  |
| `UMADDL` | — |  |  |

---

## UMULL, UMULL2 (by element) -- A64

### Encoding: `UMULL_asimdelem_L`  — UMULL

**Assembly:** `UMULL{2}  <Vd>.<Ta>, <Vn>.<Tb>, V<m>.<Ts>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<m>` | — |  |  |
| `<Ts>` | — |  |  |
| `<index>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let idxdsize : integer{} = 64 << UInt(H);
var index : integer;
var Rmhi : bit;
case size of
    when '01' => index = UInt(H::L::M); Rmhi = '0';
    when '10' => index = UInt(H::L); Rmhi = M;
    otherwise => EndOfDecode(Decode_UNDEF);
end;

let d : integer = UInt(Rd);
let n : integer = UInt(Rn);
let m : integer = UInt(Rmhi::Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMULL, UMULL2 (vector) -- A64

### Encoding: `UMULL_asimddiff_L`  — UMULL

**Assembly:** `UMULL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UMULLB (indexed) -- A64

### Encoding: `umullb_z_zzi_s`  — UMULLB

**Assembly:** `UMULLB  <Zd>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 0;
```

### Encoding: `umullb_z_zzi_d`  — UMULLB

**Assembly:** `UMULLB  <Zd>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 0;
```

---

## UMULLB (vectors) -- A64

### Encoding: `umullb_z_zz_`  — UMULLB

**Assembly:** `UMULLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## UMULLT (indexed) -- A64

### Encoding: `umullt_z_zzi_s`  — UMULLT

**Assembly:** `UMULLT  <Zd>.S, <Zn>.H, <Zm>.H[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | For the "32-bit" variant: is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i3h :: i3l) | For the "32-bit" variant: is the element index, in the range 0 to 7, encoded in the "i3h:i3l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let index : integer = UInt(i3h::i3l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 1;
```

### Encoding: `umullt_z_zzi_d`  — UMULLT

**Assembly:** `UMULLT  <Zd>.D, <Zn>.S, <Zm>.S[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | For the "64-bit" variant: is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | (i2h :: i2l) | For the "64-bit" variant: is the element index, in the range 0 to 3, encoded in the "i2h:i2l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let index : integer = UInt(i2h::i2l);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel : integer = 1;
```

---

## UMULLT (vectors) -- A64

### Encoding: `umullt_z_zz_`  — UMULLT

**Assembly:** `UMULLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## UQADD (immediate) -- A64

### Encoding: `uqadd_z_zi_`  — UQADD

**Assembly:** `UQADD  <Zdn>.<T>, <Zdn>.<T>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is an unsigned immediate in the range 0 to 255, encoded in the "imm8" field. | var imm : integer = UInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
var imm : integer = UInt(imm8);
if sh == '1' then imm = imm << 8; end;
let unsigned : boolean = TRUE;
```

---

## UQADD (vectors, predicated) -- A64

### Encoding: `uqadd_z_p_zz_`  — UQADD

**Assembly:** `UQADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UQADD (vectors, unpredicated) -- A64

### Encoding: `uqadd_z_zz_`  — UQADD

**Assembly:** `UQADD  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
```

---

## UQADD -- A64

### Encoding: `UQADD_asisdsame_only`  — UQADD

**Assembly:** `UQADD  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let unsigned : boolean = TRUE;
```

### Encoding: `UQADD_asimdsame_only`  — UQADD

**Assembly:** `UQADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let unsigned : boolean = TRUE;
```

---

## UQCVT (four registers) -- A64

### Encoding: `uqcvt_z_mz4_`  — UQCVT

**Assembly:** `UQCVT  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(sz);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
```

---

## UQCVT (two registers) -- A64

### Encoding: `uqcvt_z_mz2_`  — UQCVT

**Assembly:** `UQCVT  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## UQCVTN -- A64

### Encoding: `uqcvtn_z_mz2_`  — UQCVTN

**Assembly:** `UQCVTN  <Zd>.H, { <Zn1>.S-<Zn2>.S }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
```

---

## UQCVTN -- A64

### Encoding: `uqcvtn_z_mz4_`  — UQCVTN

**Assembly:** `UQCVTN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(sz);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
```

---

## UQDECB -- A64

### Encoding: `uqdecb_r_rs_uw`  — UQDECB

**Assembly:** `UQDECB  <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqdecb_r_rs_x`  — UQDECB

**Assembly:** `UQDECB  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQDECD (scalar) -- A64

### Encoding: `uqdecd_r_rs_uw`  — UQDECD

**Assembly:** `UQDECD  <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqdecd_r_rs_x`  — UQDECD

**Assembly:** `UQDECD  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQDECD (vector) -- A64

### Encoding: `uqdecd_z_zs_`  — UQDECD

**Assembly:** `UQDECD  <Zdn>.D{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
```

---

## UQDECH (scalar) -- A64

### Encoding: `uqdech_r_rs_uw`  — UQDECH

**Assembly:** `UQDECH  <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqdech_r_rs_x`  — UQDECH

**Assembly:** `UQDECH  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQDECH (vector) -- A64

### Encoding: `uqdech_z_zs_`  — UQDECH

**Assembly:** `UQDECH  <Zdn>.H{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
```

---

## UQDECP (scalar) -- A64

### Encoding: `uqdecp_r_p_r_uw`  — UQDECP

**Assembly:** `UQDECP  <Wdn>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqdecp_r_p_r_x`  — UQDECP

**Assembly:** `UQDECP  <Xdn>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQDECP (vector) -- A64

### Encoding: `uqdecp_z_p_z_`  — UQDECP

**Assembly:** `UQDECP  <Zdn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Zdn);
let unsigned : boolean = TRUE;
```

---

## UQDECW (scalar) -- A64

### Encoding: `uqdecw_r_rs_uw`  — UQDECW

**Assembly:** `UQDECW  <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqdecw_r_rs_x`  — UQDECW

**Assembly:** `UQDECW  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQDECW (vector) -- A64

### Encoding: `uqdecw_z_zs_`  — UQDECW

**Assembly:** `UQDECW  <Zdn>.S{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
```

---

## UQINCB -- A64

### Encoding: `uqincb_r_rs_uw`  — UQINCB

**Assembly:** `UQINCB  <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqincb_r_rs_x`  — UQINCB

**Assembly:** `UQINCB  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQINCD (scalar) -- A64

### Encoding: `uqincd_r_rs_uw`  — UQINCD

**Assembly:** `UQINCD  <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqincd_r_rs_x`  — UQINCD

**Assembly:** `UQINCD  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQINCD (vector) -- A64

### Encoding: `uqincd_z_zs_`  — UQINCD

**Assembly:** `UQINCD  <Zdn>.D{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
```

---

## UQINCH (scalar) -- A64

### Encoding: `uqinch_r_rs_uw`  — UQINCH

**Assembly:** `UQINCH  <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqinch_r_rs_x`  — UQINCH

**Assembly:** `UQINCH  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQINCH (vector) -- A64

### Encoding: `uqinch_z_zs_`  — UQINCH

**Assembly:** `UQINCH  <Zdn>.H{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
```

---

## UQINCP (scalar) -- A64

### Encoding: `uqincp_r_p_r_uw`  — UQINCP

**Assembly:** `UQINCP  <Wdn>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqincp_r_p_r_x`  — UQINCP

**Assembly:** `UQINCP  <Xdn>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |
| `<T>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Rdn);
let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQINCP (vector) -- A64

### Encoding: `uqincp_z_p_z_`  — UQINCP

**Assembly:** `UQINCP  <Zdn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pm>` | Pm (4b) | Is the name of the source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let m : integer = UInt(Pm);
let dn : integer = UInt(Zdn);
let unsigned : boolean = TRUE;
```

---

## UQINCW (scalar) -- A64

### Encoding: `uqincw_r_rs_uw`  — UQINCW

**Assembly:** `UQINCW  <Wdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wdn>` | Rdn (5b) | Is the 32-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 32;
```

### Encoding: `uqincw_r_rs_x`  — UQINCW

**Assembly:** `UQINCW  <Xdn>{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xdn>` | Rdn (5b) | Is the 64-bit name of the source and destination general-purpose register, encoded in the "Rdn" field. | let dn : integer = UInt(Rdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Rdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
let  ssize : integer{} = 64;
```

---

## UQINCW (vector) -- A64

### Encoding: `uqincw_z_zs_`  — UQINCW

**Assembly:** `UQINCW  <Zdn>.S{, <pattern>{, MUL #<imm>}}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<pattern>` | — |  |  |
| `<imm>` | imm4 (4b) | Is the immediate multiplier, in the range 1 to 16, defaulting to 1, encoded in the "imm4" field. | let imm : integer = UInt(imm4) + 1; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 32;
let dn : integer = UInt(Zdn);
let pat : bits(5) = pattern;
let imm : integer = UInt(imm4) + 1;

let unsigned : boolean = TRUE;
```

---

## UQRSHL -- A64

### Encoding: `UQRSHL_asisdsame_only`  — UQRSHL

**Assembly:** `UQRSHL  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if S == '0' && size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let unsigned : boolean = TRUE;
let rounding : boolean = TRUE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `UQRSHL_asimdsame_only`  — UQRSHL

**Assembly:** `UQRSHL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let unsigned : boolean = TRUE;
let rounding : boolean = TRUE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UQRSHL -- A64

### Encoding: `uqrshl_z_p_zz_`  — UQRSHL

**Assembly:** `UQRSHL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## UQRSHLR -- A64

### Encoding: `uqrshlr_z_p_zz_`  — UQRSHLR

**Assembly:** `UQRSHLR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## UQRSHR (four registers) -- A64

### Encoding: `uqrshr_z_mz4_`  — UQRSHR

**Assembly:** `UQRSHR  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |
| `<const>` | (tsize :: imm5) | Is the immediate shift amount, in the range 1 to number of bits per source element, encoded in "tsize:imm5". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if tsize == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
let shift : integer = (8 * esize) - UInt(tsize::imm5);
```

---

## UQRSHR (two registers) -- A64

### Encoding: `uqrshr_z_mz2_`  — UQRSHR

**Assembly:** `UQRSHR  <Zd>.H, { <Zn1>.S-<Zn2>.S }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | imm4 (4b) | Is the immediate shift amount, in the range 1 to 16, encoded in the "imm4" field. | let shift : integer = esize - UInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = esize - UInt(imm4);
```

---

## UQRSHRN -- A64

### Encoding: `uqrshrn_z_mz2_b`  — UQRSHRN

**Assembly:** `UQRSHRN  <Zd>.B, { <Zn1>.H-<Zn2>.H }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | imm3 (3b) | For the "8-bit" variant: is the immediate shift amount, in the range 1 to 8, encoded in the "imm3" field. | let shift : integer = esize - UInt(imm3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = esize - UInt(imm3);
```

### Encoding: `uqrshrn_z_mz2_`  — UQRSHRN

**Assembly:** `UQRSHRN  <Zd>.H, { <Zn1>.S-<Zn2>.S }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | imm4 (4b) | For the "16-bit" variant: is the immediate shift amount, in the range 1 to 16, encoded in the "imm4" field. | let shift : integer = esize - UInt(imm4); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 16;
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = esize - UInt(imm4);
```

---

## UQRSHRN -- A64

### Encoding: `uqrshrn_z_mz4_`  — UQRSHRN

**Assembly:** `UQRSHRN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn4>.<Tb> }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Tb>` | — |  |  |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |
| `<const>` | (tsize :: imm5) | Is the immediate shift amount, in the range 1 to number of bits per source element, encoded in "tsize:imm5". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if tsize == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd);
let shift : integer = (8 * esize) - UInt(tsize::imm5);
```

---

## UQRSHRN, UQRSHRN2 -- A64

### Encoding: `UQRSHRN_asisdshf_N`  — UQRSHRN

**Assembly:** `UQRSHRN  <Vb><d>, <Va><n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vb>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Va>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh == '0000' then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = TRUE;
let unsigned : boolean = TRUE;
```

### Encoding: `UQRSHRN_asimdshf_N`  — UQRSHRN

**Assembly:** `UQRSHRN{2}  <Vd>.<Tb>, <Vn>.<Ta>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = TRUE;
let unsigned : boolean = TRUE;
```

---

## UQRSHRNB -- A64

### Encoding: `uqrshrnb_z_zi_`  — UQRSHRNB

**Assembly:** `UQRSHRNB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## UQRSHRNT -- A64

### Encoding: `uqrshrnt_z_zi_`  — UQRSHRNT

**Assembly:** `UQRSHRNT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## UQSHL (immediate) -- A64

### Encoding: `UQSHL_asisdshf_R`  — UQSHL

**Assembly:** `UQSHL  <V><d>, <V><n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh == '0000' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = esize;
let elements : integer = 1;
let shift : integer = UInt(immh::immb) - esize;

let src_unsigned : boolean = TRUE;
let dst_unsigned : boolean = TRUE;
```

### Encoding: `UQSHL_asimdshf_R`  — UQSHL

**Assembly:** `UQSHL  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let shift : integer = UInt(immh::immb) - esize;

let src_unsigned : boolean = TRUE;
let dst_unsigned : boolean = TRUE;
```

---

## UQSHL (immediate) -- A64

### Encoding: `uqshl_z_p_zi_`  — UQSHL

**Assembly:** `UQSHL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## UQSHL (register) -- A64

### Encoding: `UQSHL_asisdsame_only`  — UQSHL

**Assembly:** `UQSHL  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if S == '0' && size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let unsigned : boolean = TRUE;
let rounding : boolean = FALSE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `UQSHL_asimdsame_only`  — UQSHL

**Assembly:** `UQSHL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let unsigned : boolean = TRUE;
let rounding : boolean = FALSE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## UQSHL (vectors) -- A64

### Encoding: `uqshl_z_p_zz_`  — UQSHL

**Assembly:** `UQSHL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## UQSHLR -- A64

### Encoding: `uqshlr_z_p_zz_`  — UQSHLR

**Assembly:** `UQSHLR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## UQSHRN -- A64

### Encoding: `uqshrn_z_mz2_`  — UQSHRN

**Assembly:** `UQSHRN  <Zd>.<T>, { <Zn1>.<Tb>-<Zn2>.<Tb> }, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |
| `<const>` | (tsize :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tsize:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p3) && !IsFeatureImplemented(FEAT_SME2p3) then
    EndOfDecode(Decode_UNDEF);
end;
if tsize == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## UQSHRN, UQSHRN2 -- A64

### Encoding: `UQSHRN_asisdshf_N`  — UQSHRN

**Assembly:** `UQSHRN  <Vb><d>, <Va><n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vb>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Va>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh == '0000' then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = esize;
let elements : integer = 1;
let part : integer = 0;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = FALSE;
let unsigned : boolean = TRUE;
```

### Encoding: `UQSHRN_asimdshf_N`  — UQSHRN

**Assembly:** `UQSHRN{2}  <Vd>.<Tb>, <Vn>.<Ta>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (2 * esize) - UInt(immh::immb);
let round : boolean = FALSE;
let unsigned : boolean = TRUE;
```

---

## UQSHRNB -- A64

### Encoding: `uqshrnb_z_zi_`  — UQSHRNB

**Assembly:** `UQSHRNB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## UQSHRNT -- A64

### Encoding: `uqshrnt_z_zi_`  — UQSHRNT

**Assembly:** `UQSHRNT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## UQSUB (immediate) -- A64

### Encoding: `uqsub_z_zi_`  — UQSUB

**Assembly:** `UQSUB  <Zdn>.<T>, <Zdn>.<T>, #<imm>{, <shift>}`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<imm>` | imm8 (8b) | Is an unsigned immediate in the range 0 to 255, encoded in the "imm8" field. | var imm : integer = UInt(imm8); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size::sh == '001' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn);
var imm : integer = UInt(imm8);
if sh == '1' then imm = imm << 8; end;
let unsigned : boolean = TRUE;
```

---

## UQSUB (vectors, predicated) -- A64

### Encoding: `uqsub_z_p_zz_`  — UQSUB

**Assembly:** `UQSUB  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UQSUB (vectors, unpredicated) -- A64

### Encoding: `uqsub_z_zz_`  — UQSUB

**Assembly:** `UQSUB  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
```

---

## UQSUB -- A64

### Encoding: `UQSUB_asisdsame_only`  — UQSUB

**Assembly:** `UQSUB  <V><d>, <V><n>, <V><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let unsigned : boolean = TRUE;
```

### Encoding: `UQSUB_asimdsame_only`  — UQSUB

**Assembly:** `UQSUB  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let unsigned : boolean = TRUE;
```

---

## UQSUBR -- A64

### Encoding: `uqsubr_z_p_zz_`  — UQSUBR

**Assembly:** `UQSUBR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## UQXTN, UQXTN2 -- A64

### Encoding: `UQXTN_asisdmisc_N`  — UQXTN

**Assembly:** `UQXTN  <Vb><d>, <Va><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vb>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Va>` | — |  |  |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let part : integer = 0;
let elements : integer = 1;
let unsigned : boolean = TRUE;
```

### Encoding: `UQXTN_asimdmisc_N`  — UQXTN

**Assembly:** `UQXTN{2}  <Vd>.<Tb>, <Vn>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
let unsigned : boolean = TRUE;
```

---

## UQXTNB -- A64

### Encoding: `uqxtnb_z_zz_`  — UQXTNB

**Assembly:** `UQXTNB  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if !(tsize IN {'001', '010', '100'}) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## UQXTNT -- A64

### Encoding: `uqxtnt_z_zz_`  — UQXTNT

**Assembly:** `UQXTNT  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if !(tsize IN {'001', '010', '100'}) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 16 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
```

---

## URECPE -- A64

### Encoding: `URECPE_asimdmisc_R`  — URECPE

**Assembly:** `URECPE  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## URECPE -- A64

### Encoding: `urecpe_z_p_z_m`  — URECPE

**Assembly:** `URECPE  <Zd>.S, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `urecpe_z_p_z_z`  — URECPE

**Assembly:** `URECPE  <Zd>.S, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## URHADD -- A64

### Encoding: `URHADD_asimdsame_only`  — URHADD

**Assembly:** `URHADD  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## URHADD -- A64

### Encoding: `urhadd_z_p_zz_`  — URHADD

**Assembly:** `URHADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## URSHL (multiple and single vector) -- A64

### Encoding: `urshl_mz_zzv_2x1`  — URSHL

**Assembly:** `URSHL  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 2;
```

### Encoding: `urshl_mz_zzv_4x1`  — URSHL

**Assembly:** `URSHL  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt('0'::Zm);
let nreg : integer{} = 4;
```

---

## URSHL (multiple vectors) -- A64

### Encoding: `urshl_mz_zzw_2x2`  — URSHL

**Assembly:** `URSHL  { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zdn1>.<T>-<Zdn2>.<T> }, { <Zm1>.<T>-<Zm2>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination and first source m | let dn : integer = UInt(Zdn::'0'); |
| `<T>` | — |  |  |
| `<Zdn2>` | Zdn (4b) | Is the name of the second scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the second source multi-vector gro | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'0');
let m : integer = UInt(Zm::'0');
let nreg : integer{} = 2;
```

### Encoding: `urshl_mz_zzw_4x4`  — URSHL

**Assembly:** `URSHL  { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zdn1>.<T>-<Zdn4>.<T> }, { <Zm1>.<T>-<Zm4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn1>` | Zdn (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination and first source  | let dn : integer = UInt(Zdn::'00'); |
| `<T>` | — |  |  |
| `<Zdn4>` | Zdn (3b) | Is the name of the fourth scalable vector register of the destination and first source multi-vector group, encoded as "Z | let dn : integer = UInt(Zdn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the second source multi-vector gr | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let dn : integer = UInt(Zdn::'00');
let m : integer = UInt(Zm::'00');
let nreg : integer{} = 4;
```

---

## URSHL -- A64

### Encoding: `URSHL_asisdsame_only`  — URSHL

**Assembly:** `URSHL  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if S == '0' && size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let rounding : boolean = TRUE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `URSHL_asimdsame_only`  — URSHL

**Assembly:** `URSHL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let rounding : boolean = TRUE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## URSHL -- A64

### Encoding: `urshl_z_p_zz_`  — URSHL

**Assembly:** `URSHL  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## URSHLR -- A64

### Encoding: `urshlr_z_p_zz_`  — URSHLR

**Assembly:** `URSHLR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
```

---

## URSHR -- A64

### Encoding: `URSHR_asisdshf_R`  — URSHR

**Assembly:** `URSHR  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the right shift amount, in the range 1 to 64, encoded as 128 - UInt("immh:immb"). |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let round : boolean = TRUE;
```

### Encoding: `URSHR_asimdshf_R`  — URSHR

**Assembly:** `URSHR  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let round : boolean = TRUE;
```

---

## URSHR -- A64

### Encoding: `urshr_z_p_zi_`  — URSHR

**Assembly:** `URSHR  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## URSQRTE -- A64

### Encoding: `URSQRTE_asimdmisc_R`  — URSQRTE

**Assembly:** `URSQRTE  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer = UInt(Rd);
let n : integer = UInt(Rn);

if sz == '1' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## URSQRTE -- A64

### Encoding: `ursqrte_z_p_z_m`  — URSQRTE

**Assembly:** `URSQRTE  <Zd>.S, <Pg>/M, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = TRUE;
```

### Encoding: `ursqrte_z_p_z_z`  — URSQRTE

**Assembly:** `URSQRTE  <Zd>.S, <Pg>/Z, <Zn>.S`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '10' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let merging : boolean = FALSE;
```

---

## URSRA -- A64

### Encoding: `URSRA_asisdshf_R`  — URSRA

**Assembly:** `URSRA  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the right shift amount, in the range 1 to 64, encoded as 128 - UInt("immh:immb"). |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let round : boolean = TRUE;
```

### Encoding: `URSRA_asimdshf_R`  — URSRA

**Assembly:** `URSRA  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let round : boolean = TRUE;
```

---

## URSRA -- A64

### Encoding: `ursra_z_zi_`  — URSRA

**Assembly:** `URSRA  <Zda>.<T>, <Zn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let da : integer = UInt(Zda);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## USDOT (4-way, multiple and indexed vector) -- A64

### Encoding: `usdot_za_zzi_s2xi`  — USDOT

**Assembly:** `USDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 2;
```

### Encoding: `usdot_za_zzi_s4xi`  — USDOT

**Assembly:** `USDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
let nreg : integer{} = 4;
```

---

## USDOT (4-way, multiple and single vector) -- A64

### Encoding: `usdot_za_zzv_s2x1`  — USDOT

**Assembly:** `USDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `usdot_za_zzv_s4x1`  — USDOT

**Assembly:** `USDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## USDOT (4-way, multiple vectors) -- A64

### Encoding: `usdot_za_zzw_s2x2`  — USDOT

**Assembly:** `USDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-vec | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-ve | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(off3);
let nreg : integer{} = 2;
```

### Encoding: `usdot_za_zzw_s4x4`  — USDOT

**Assembly:** `USDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, { <Zm1>.B-<Zm4>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the first source multi-ve | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA single-vectors" variant: is the name of the first scalable vector register of the second source multi-v | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(off3);
let nreg : integer{} = 4;
```

---

## USDOT (by element) -- A64

### Encoding: `USDOT_asimdelem_D`  — USDOT

**Assembly:** `USDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.4B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | (M :: Rm) | Is the name of the second SIMD&FP source register, encoded in the "M:Rm" fields. |  |
| `<index>` | (H :: L) | Is the immediate index of a 32-bit group of four 8-bit values, in the range 0 to 3, encoded in the "H:L" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_I8MM) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Rn);
let m : integer = UInt(M::Rm);
let d : integer = UInt(Rd);
let i : integer = UInt(H::L);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 32;
```

---

## USDOT (indexed) -- A64

### Encoding: `usdot_z_zzzi_s`  — USDOT

**Assembly:** `USDOT  <Zda>.S, <Zn>.B, <Zm>.B[<imm>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register Z0-Z7, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<imm>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_I8MM)) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let index : integer = UInt(i2);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## USDOT (vector) -- A64

### Encoding: `USDOT_asimdsame2_D`  — USDOT

**Assembly:** `USDOT  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_I8MM) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV 32;
```

---

## USDOT (vectors) -- A64

### Encoding: `usdot_z_zzz_s`  — USDOT

**Assembly:** `USDOT  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if ((!IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME)) ||
    !IsFeatureImplemented(FEAT_I8MM)) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
```

---

## USHL -- A64

### Encoding: `USHL_asisdsame_only`  — USHL

**Assembly:** `USHL  D<d>, D<n>, D<m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if S == '0' && size != '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let rounding : boolean = FALSE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
```

### Encoding: `USHL_asimdsame_only`  — USHL

**Assembly:** `USHL  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let rounding : boolean = FALSE;
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## USHLL, USHLL2 -- A64

### Encoding: `USHLL_asimdshf_L`  — USHLL

**Assembly:** `USHLL{2}  <Vd>.<Ta>, <Vn>.<Tb>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] == '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh[2:0]);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = UInt(immh::immb) - esize;
let unsigned : boolean = TRUE;
```

---

## USHLLB -- A64

### Encoding: `ushllb_z_zi_`  — USHLLB

**Assembly:** `USHLLB  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## USHLLT -- A64

### Encoding: `ushllt_z_zi_`  — USHLLT

**Assembly:** `USHLLT  <Zd>.<T>, <Zn>.<Tb>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 0 to number of bits per element minus 1, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(3) = tszh::tszl;
if tsize == '000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let shift : integer = UInt(tsize::imm3) - esize;
```

---

## USHR -- A64

### Encoding: `USHR_asisdshf_R`  — USHR

**Assembly:** `USHR  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the right shift amount, in the range 1 to 64, encoded as 128 - UInt("immh:immb"). |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let round : boolean = FALSE;
```

### Encoding: `USHR_asimdshf_R`  — USHR

**Assembly:** `USHR  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let round : boolean = FALSE;
```

---

## USMLALL (multiple and indexed vector) -- A64

### Encoding: `usmlall_za_zzi_s`  — USMLALL

**Assembly:** `USMLALL  ZA.S[<Wv>, <offs1>:<offs4>], <Zn>.B, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | Is the element index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 1;
```

### Encoding: `usmlall_za_zzi_s2xi`  — USMLALL

**Assembly:** `USMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vecto | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | Is the element index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 2;
```

### Encoding: `usmlall_za_zzi_s4xi`  — USMLALL

**Assembly:** `USMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vect | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | (i4h :: i4l) | Is the element index, in the range 0 to 15, encoded in the "i4h:i4l" fields. |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let index : integer = UInt(i4h::i4l);
let nreg : integer{} = 4;
```

---

## USMLALL (multiple and single vector) -- A64

### Encoding: `usmlall_za_zzv_s`  — USMLALL

**Assembly:** `USMLALL  ZA.S[<Wv>, <offs1>:<offs4>], <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off2::'00');
let nreg : integer{} = 1;
```

### Encoding: `usmlall_za_zzv_s2x1`  — USMLALL

**Assembly:** `USMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn2>` | Zn (5b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" plus 1 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `usmlall_za_zzv_s4x1`  — USMLALL

**Assembly:** `USMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (5b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn". | let n : integer = UInt(Zn); |
| `<Zn4>` | Zn (5b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" plus 3 modulo | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn);
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## USMLALL (multiple vectors) -- A64

### Encoding: `usmlall_za_zzw_s2x2`  — USMLALL

**Assembly:** `USMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx2}], { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vecto | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm1>` | Zm (4b) | For the "Two ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vect | let m : integer = UInt(Zm::'0'); |
| `<Zm2>` | Zm (4b) | Is the name of the second scalable vector register of the second source multi-vector group, encoded as "Zm" times 2 plus | let m : integer = UInt(Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm::'0');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 2;
```

### Encoding: `usmlall_za_zzw_s4x4`  — USMLALL

**Assembly:** `USMLALL  ZA.S[<Wv>, <offs1>:<offs4>{, VGx4}], { <Zn1>.B-<Zn4>.B }, { <Zm1>.B-<Zm4>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | Is the first vector select offset, encoded as "o1" field times 4. | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | Is the fourth vector select offset, encoded as "o1" field times 4 plus 3. | let offset : integer = UInt(o1::'00'); |
| `<Zn1>` | Zn (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the first source multi-vect | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm1>` | Zm (3b) | For the "Four ZA quad-vectors" variant: is the name of the first scalable vector register of the second source multi-vec | let m : integer = UInt(Zm::'00'); |
| `<Zm4>` | Zm (3b) | Is the name of the fourth scalable vector register of the second source multi-vector group, encoded as "Zm" times 4 plus | let m : integer = UInt(Zm::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let v : integer = UInt('010'::Rv);
let n : integer = UInt(Zn::'00');
let m : integer = UInt(Zm::'00');
let offset : integer = UInt(o1::'00');
let nreg : integer{} = 4;
```

---

## USMMLA (vector) -- A64

### Encoding: `USMMLA_asimdsame2_G`  — USMMLA

**Assembly:** `USMMLA  <Vd>.4S, <Vn>.16B, <Vm>.16B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP third source and destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_I8MM) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

---

## USMMLA -- A64

### Encoding: `usmmla_z_zzz_`  — USMMLA

**Assembly:** `USMMLA  <Zda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) || !IsFeatureImplemented(FEAT_I8MM) then
    EndOfDecode(Decode_UNDEF);
end;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(Zda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

---

## USMOP4A -- A64

### Encoding: `usmop4a_za_zz_b1x2`  — USMOP4A

**Assembly:** `USMOP4A  <ZAda>.S, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4a_za_zz_b1x1`  — USMOP4A

**Assembly:** `USMOP4A  <ZAda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4a_za_zz_b2x1`  — USMOP4A

**Assembly:** `USMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4a_za_zz_b2x2`  — USMOP4A

**Assembly:** `USMOP4A  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4a_za_zz_h1x2`  — USMOP4A

**Assembly:** `USMOP4A  <ZAda>.D, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4a_za_zz_h1x1`  — USMOP4A

**Assembly:** `USMOP4A  <ZAda>.D, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4a_za_zz_h2x1`  — USMOP4A

**Assembly:** `USMOP4A  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4a_za_zz_h2x2`  — USMOP4A

**Assembly:** `USMOP4A  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = FALSE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

---

## USMOP4S -- A64

### Encoding: `usmop4s_za_zz_b1x2`  — USMOP4S

**Assembly:** `USMOP4S  <ZAda>.S, <Zn>.B, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4s_za_zz_b1x1`  — USMOP4S

**Assembly:** `USMOP4S  <ZAda>.S, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4s_za_zz_b2x1`  — USMOP4S

**Assembly:** `USMOP4S  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4s_za_zz_b2x2`  — USMOP4S

**Assembly:** `USMOP4S  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, { <Zm1>.B-<Zm2>.B }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit, multiple and single vectors", "32-bit, multiple vectors", "32-bit, single and multiple vectors", and "3 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4s_za_zz_h1x2`  — USMOP4S

**Assembly:** `USMOP4S  <ZAda>.D, <Zn>.H, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4s_za_zz_h1x1`  — USMOP4S

**Assembly:** `USMOP4S  <ZAda>.D, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn>` | Zn (3b) | Is the name of the first source scalable vector register, registers in the range Z0-Z15, encoded as "Zn" times 2. | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 1;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4s_za_zz_h2x1`  — USMOP4S

**Assembly:** `USMOP4S  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm>` | Zm (3b) | Is the name of the second source scalable vector register, registers in the range Z16-Z31, encoded as "Zm" times 2 plus  | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 1;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmop4s_za_zz_h2x2`  — USMOP4S

**Assembly:** `USMOP4S  <ZAda>.D, { <Zn1>.H-<Zn2>.H }, { <Zm1>.H-<Zm2>.H }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit, multiple and single vectors", "64-bit, multiple vectors", "64-bit, single and multiple vectors", and "6 | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded a | let n : integer = UInt('0'::Zn::'0'); |
| `<Zn2>` | Zn (3b) | Is the name of the second scalable vector register of the first source multi-vector group, in the range Z0-Z15, encoded  | let n : integer = UInt('0'::Zn::'0'); |
| `<Zm1>` | Zm (3b) | Is the name of the first scalable vector register of the second source multi-vector group, in the range Z16-Z31, encoded | let m : integer = UInt('1'::Zm::'0'); |
| `<Zm2>` | Zm (3b) | Is the name of the second scalable vector register of the second source multi-vector group, in the range Z16-Z31, encode | let m : integer = UInt('1'::Zm::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_MOP4) || !IsFeatureImplemented(FEAT_SME_I16I64) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 64;
let n : integer = UInt('0'::Zn::'0');
let m : integer = UInt('1'::Zm::'0');
let nreg : integer{} = 2;
let mreg : integer = 2;
let da : integer = UInt(ZAda);
let sub_op : boolean = TRUE;
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

---

## USMOPA (4-way) -- A64

### Encoding: `usmopa_za_pp_zz_32`  — USMOPA

**Assembly:** `USMOPA  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmopa_za_pp_zz_64`  — USMOPA

**Assembly:** `USMOPA  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

---

## USMOPS -- A64

### Encoding: `usmops_za_pp_zz_32`  — USMOPS

**Assembly:** `USMOPS  <ZAda>.S, <Pn>/M, <Pm>/M, <Zn>.B, <Zm>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | For the "32-bit" variant: is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 32;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

### Encoding: `usmops_za_pp_zz_64`  — USMOPS

**Assembly:** `USMOPS  <ZAda>.D, <Pn>/M, <Pm>/M, <Zn>.H, <Zm>.H`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (3b) | For the "64-bit" variant: is the name of the ZA tile ZA0-ZA7, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Pn>` | Pn (3b) | Is the name of the first governing scalable predicate register P0-P7, encoded in the "Pn" field. | let a : integer = UInt(Pn); |
| `<Pm>` | Pm (3b) | Is the name of the second governing scalable predicate register P0-P7, encoded in the "Pm" field. | let b : integer = UInt(Pm); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_I16I64) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 64;
let a : integer = UInt(Pn);
let b : integer = UInt(Pm);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

---

## USQADD -- A64

### Encoding: `USQADD_asisdmisc_R`  — USQADD

**Assembly:** `USQADD  <V><d>, <V><n>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<V>` | — |  |  |
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = esize;
let elements : integer = 1;
let unsigned : boolean = TRUE;
```

### Encoding: `USQADD_asimdmisc_R`  — USQADD

**Assembly:** `USQADD  <Vd>.<T>, <Vn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let unsigned : boolean = TRUE;
```

---

## USQADD -- A64

### Encoding: `usqadd_z_p_zz_`  — USQADD

**Assembly:** `USQADD  <Zdn>.<T>, <Pg>/M, <Zdn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let g : integer = UInt(Pg);
let dn : integer = UInt(Zdn);
let m : integer = UInt(Zm);
```

---

## USRA -- A64

### Encoding: `USRA_asisdshf_R`  — USRA

**Assembly:** `USRA  D<d>, D<n>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<d>` | Rd (5b) | Is the number of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<n>` | Rn (5b) | Is the number of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | (immh :: immb) | For the "Scalar" variant: is the right shift amount, in the range 1 to 64, encoded as 128 - UInt("immh:immb"). |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3] != '1' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << 3;
let datasize : integer{} = esize;
let elements : integer = 1;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let round : boolean = FALSE;
```

### Encoding: `USRA_asimdshf_R`  — USRA

**Assembly:** `USRA  <Vd>.<T>, <Vn>.<T>, #<shift>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<shift>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if immh[3]::Q == '10' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let esize : integer{} = 8 << HighestSetBitNZ(immh);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;

let shift : integer = (esize * 2) - UInt(immh::immb);
let unsigned : boolean = TRUE;
let round : boolean = FALSE;
```

---

## USRA -- A64

### Encoding: `usra_z_zi_`  — USRA

**Assembly:** `USRA  <Zda>.<T>, <Zn>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zda>` | Zda (5b) | Is the name of the third source and destination scalable vector register, encoded in the "Zda" field. | let da : integer = UInt(Zda); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let n : integer = UInt(Zn);
let da : integer = UInt(Zda);
let shift : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## USTMOPA -- A64

### Encoding: `ustmopa_za_zzzi_b2x1`  — USTMOPA

**Assembly:** `USTMOPA  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = FALSE;
```

---

## USUBL, USUBL2 -- A64

### Encoding: `USUBL_asimddiff_L`  — USUBL

**Assembly:** `USUBL{2}  <Vd>.<Ta>, <Vn>.<Tb>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Tb>` | — |  |  |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## USUBLB -- A64

### Encoding: `usublb_z_zz_`  — USUBLB

**Assembly:** `USUBLB  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 0;
let sel2 : integer = 0;
let unsigned : boolean = TRUE;
```

---

## USUBLT -- A64

### Encoding: `usublt_z_zz_`  — USUBLT

**Assembly:** `USUBLT  <Zd>.<T>, <Zn>.<Tb>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let sel1 : integer = 1;
let sel2 : integer = 1;
let unsigned : boolean = TRUE;
```

---

## USUBW, USUBW2 -- A64

### Encoding: `USUBW_asimddiff_W`  — USUBW

**Assembly:** `USUBW{2}  <Vd>.<Ta>, <Vn>.<Ta>, <Vm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## USUBWB -- A64

### Encoding: `usubwb_z_zz_`  — USUBWB

**Assembly:** `USUBWB  <Zd>.<T>, <Zn>.<T>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## USUBWT -- A64

### Encoding: `usubwt_z_zz_`  — USUBWT

**Assembly:** `USUBWT  <Zd>.<T>, <Zn>.<T>, <Zm>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
```

---

## USVDOT -- A64

### Encoding: `usvdot_za_zzi_s4xi`  — USVDOT

**Assembly:** `USVDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vector segment, in the range 0 to 3, e | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
```

---

## UTMOPA (2-way) -- A64

### Encoding: `utmopa_za32_zzzi_h2x1`  — UTMOPA

**Assembly:** `UTMOPA  <ZAda>.S, { <Zn1>.H-<Zn2>.H }, <Zm>.H, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
let unsigned : boolean = TRUE;
```

---

## UTMOPA (4-way) -- A64

### Encoding: `utmopa_za_zzzi_b2x1`  — UTMOPA

**Assembly:** `UTMOPA  <ZAda>.S, { <Zn1>.B-<Zn2>.B }, <Zm>.B, <Zk>[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<ZAda>` | ZAda (2b) | Is the name of the ZA tile ZA0-ZA3, encoded in the "ZAda" field. | let da : integer = UInt(ZAda); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<Zk>` | (K :: Zk) | Is the name of the control vector register Z20-Z23 or Z28-Z31, encoded in the "K:Zk" fields. |  |
| `<index>` | i2 (2b) | Is the control segment index, in the range 0 to 3, encoded in the "i2" field. | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME_TMOP) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Zn::'0');
let m : integer = UInt(Zm);
let k : integer = UInt('1'::K::'1'::Zk);
let index : integer = UInt(i2);
let da : integer = UInt(ZAda);
let op1_unsigned : boolean = TRUE;
let op2_unsigned : boolean = TRUE;
```

---

## UUNPK -- A64

### Encoding: `uunpk_mz_z_2`  — UUNPK

**Assembly:** `UUNPK  { <Zd1>.<T>-<Zd2>.<T> }, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | For the "Two registers" variant: is the name of the first scalable vector register of the destination multi-vector group | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd::'0');
let nreg : integer{} = 2;
let unsigned : boolean = TRUE;
```

### Encoding: `uunpk_mz_z_4`  — UUNPK

**Assembly:** `UUNPK  { <Zd1>.<T>-<Zd4>.<T> }, { <Zn1>.<Tb>-<Zn2>.<Tb> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | For the "Four registers" variant: is the name of the first scalable vector register of the destination multi-vector grou | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Tb>` | — |  |  |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the source multi-vector group, encoded as "Zn" times 2 plus 1. | let n : integer = UInt(Zn::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn::'0');
let d : integer = UInt(Zd::'00');
let nreg : integer{} = 4;
let unsigned : boolean = TRUE;
```

---

## UUNPKHI, UUNPKLO -- A64

### Encoding: `uunpkhi_z_z_`  — UUNPKHI

**Assembly:** `UUNPKHI  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let hi : boolean = TRUE;
```

### Encoding: `uunpklo_z_z_`  — UUNPKLO

**Assembly:** `UUNPKLO  <Zd>.<T>, <Zn>.<Tb>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Tb>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let hi : boolean = FALSE;
```

---

## UVDOT (2-way) -- A64

### Encoding: `uvdot_za32_zzi_2xi`  — UVDOT

**Assembly:** `UVDOT  ZA.S[<Wv>, <offs>{, VGx2}], { <Zn1>.H-<Zn2>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (4b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 2. | let n : integer = UInt(Zn::'0'); |
| `<Zn2>` | Zn (4b) | Is the name of the second scalable vector register of the first source multi-vector group, encoded as "Zn" times 2 plus  | let n : integer = UInt(Zn::'0'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | Is the immediate index of a pair of 16-bit elements within each 128-bit vector segment, in the range 0 to 3, encoded in  | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'0');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
```

---

## UVDOT (4-way) -- A64

### Encoding: `uvdot_za_zzi_s4xi`  — UVDOT

**Assembly:** `UVDOT  ZA.S[<Wv>, <offs>{, VGx4}], { <Zn1>.B-<Zn4>.B }, <Zm>.B[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i2 (2b) | For the "8-bit to 32-bit" variant: is the immediate index of a 32-bit group of four 8-bit values within each 128-bit vec | let index : integer = UInt(i2); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 32;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i2);
```

### Encoding: `uvdot_za_zzi_d4xi`  — UVDOT

**Assembly:** `UVDOT  ZA.D[<Wv>, <offs>{, VGx4}], { <Zn1>.H-<Zn4>.H }, <Zm>.H[<index>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the first source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the first source multi-vector group, encoded as "Zn" times 4 plus  | let n : integer = UInt(Zn::'00'); |
| `<Zm>` | Zm (4b) | Is the name of the second source scalable vector register Z0-Z15, encoded in the "Zm" field. | let m : integer = UInt('0'::Zm); |
| `<index>` | i1 (1b) | For the "16-bit to 64-bit" variant: is the immediate index of a 64-bit group of four 16-bit values within each 128-bit v | let index : integer = UInt(i1); |

**Pseudocode (Decode):**
```
if !(IsFeatureImplemented(FEAT_SME2) && IsFeatureImplemented(FEAT_SME_I16I64)) then
    EndOfDecode(Decode_UNDEF);
end;
let v : integer = UInt('010'::Rv);
let esize : integer{} = 64;
let n : integer = UInt(Zn::'00');
let m : integer = UInt('0'::Zm);
let offset : integer = UInt(off3);
let index : integer = UInt(i1);
```

---

## UXTB -- A64 *(alias for UBFM)*

### Encoding: `UXTB_UBFM_32M_bitfield`  — UBFM

**Assembly:** `UXTB  <Wd>, <Wn> / UBFM  <Wd>, <Wn>, #0, #7`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `UBFM` | — |  |  |

---

## UXTB, UXTH, UXTW -- A64

### Encoding: `uxtb_z_p_z_m`  — UXTB

**Assembly:** `UXTB  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let merging : boolean = TRUE;
```

### Encoding: `uxtb_z_p_z_z`  — UXTB

**Assembly:** `UXTB  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size == '00' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 8;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let merging : boolean = FALSE;
```

### Encoding: `uxth_z_p_z_m`  — UXTH

**Assembly:** `UXTH  <Zd>.<T>, <Pg>/M, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let merging : boolean = TRUE;
```

### Encoding: `uxth_z_p_z_z`  — UXTH

**Assembly:** `UXTH  <Zd>.<T>, <Pg>/Z, <Zn>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size IN {'0x'} then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 16;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let merging : boolean = FALSE;
```

### Encoding: `uxtw_z_p_z_m`  — UXTW

**Assembly:** `UXTW  <Zd>.D, <Pg>/M, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let merging : boolean = TRUE;
```

### Encoding: `uxtw_z_p_z_z`  — UXTW

**Assembly:** `UXTW  <Zd>.D, <Pg>/Z, <Zn>.D`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Pg>` | Pg (3b) | Is the name of the governing scalable predicate register P0-P7, encoded in the "Pg" field. | let g : integer = UInt(Pg); |
| `<Zn>` | Zn (5b) | Is the name of the source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p2) && !IsFeatureImplemented(FEAT_SME2p2) then
    EndOfDecode(Decode_UNDEF);
end;
if size != '11' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let s_esize : integer{} = 32;
let g : integer = UInt(Pg);
let n : integer = UInt(Zn);
let d : integer = UInt(Zd);
let unsigned : boolean = TRUE;
let merging : boolean = FALSE;
```

---

## UXTH -- A64 *(alias for UBFM)*

### Encoding: `UXTH_UBFM_32M_bitfield`  — UBFM

**Assembly:** `UXTH  <Wd>, <Wn> / UBFM  <Wd>, <Wn>, #0, #15`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wd>` | Rd (5b) | Is the 32-bit name of the general-purpose destination register, encoded in the "Rd" field. |  |
| `<Wn>` | Rn (5b) | Is the 32-bit name of the general-purpose source register, encoded in the "Rn" field. |  |
| `UBFM` | — |  |  |

---

## UXTL, UXTL2 -- A64 *(alias for USHLL_advsimd)*

### Encoding: `UXTL_USHLL_asimdshf_L`  — USHLL

**Assembly:** `UXTL{2}  <Vd>.<Ta>, <Vn>.<Tb> / USHLL{2}  <Vd>.<Ta>, <Vn>.<Tb>, #0`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. |  |
| `<Ta>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. |  |
| `<Tb>` | — |  |  |
| `USHLL` | — |  |  |

---

## UZP (four registers) -- A64

### Encoding: `uzp_mz_z_4`  — UZP

**Assembly:** `UZP  { <Zd1>.<T>-<Zd4>.<T> }, { <Zn1>.<T>-<Zn4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '11' && MaxImplementedSVL() < 256 then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
```

### Encoding: `uzp_mz_z_4q`  — UZP

**Assembly:** `UZP  { <Zd1>.Q-<Zd4>.Q }, { <Zn1>.Q-<Zn4>.Q }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if MaxImplementedSVL() < 512 then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
```

---

## UZP (two registers) -- A64

### Encoding: `uzp_mz_zz_2`  — UZP

**Assembly:** `UZP  { <Zd1>.<T>-<Zd2>.<T> }, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'0');
```

### Encoding: `uzp_mz_zz_2q`  — UZP

**Assembly:** `UZP  { <Zd1>.Q-<Zd2>.Q }, <Zn>.Q, <Zm>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if MaxImplementedSVL() < 256 then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'0');
```

---

## UZP1 -- A64

### Encoding: `UZP1_asimdperm_only`  — UZP1

**Assembly:** `UZP1  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = UInt(op);
```

---

## UZP1, UZP2 (predicates) -- A64

### Encoding: `uzp1_p_pp_`  — UZP1

**Assembly:** `UZP1  <Pd>.<T>, <Pn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let part : integer = 0;
```

### Encoding: `uzp2_p_pp_`  — UZP2

**Assembly:** `UZP2  <Pd>.<T>, <Pn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let part : integer = 1;
```

---

## UZP1, UZP2 (vectors) -- A64

### Encoding: `uzp1_z_zz_`  — UZP1

**Assembly:** `UZP1  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 0;
```

### Encoding: `uzp1_z_zz_q`  — UZP1

**Assembly:** `UZP1  <Zd>.Q, <Zn>.Q, <Zm>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 0;
```

### Encoding: `uzp2_z_zz_`  — UZP2

**Assembly:** `UZP2  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 1;
```

### Encoding: `uzp2_z_zz_q`  — UZP2

**Assembly:** `UZP2  <Zd>.Q, <Zn>.Q, <Zm>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 1;
```

---

## UZP2 -- A64

### Encoding: `UZP2_asimdperm_only`  — UZP2

**Assembly:** `UZP2  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = UInt(op);
```

---

## UZPQ1 -- A64

### Encoding: `uzpq1_z_zz_`  — UZPQ1

**Assembly:** `UZPQ1  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 0;
```

---

## UZPQ2 -- A64

### Encoding: `uzpq2_z_zz_`  — UZPQ2

**Assembly:** `UZPQ2  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 1;
```

---

## WFE -- A64

### Encoding: `WFE_HI_hints`  — WFE

**Assembly:** `WFE`

**Pseudocode (Decode):**
```
// Empty.
```

---

## WFET -- A64

### Encoding: `WFET_only_systeminstrswithreg`  — WFET

**Assembly:** `WFET  <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rd (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_WFxT) then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
```

---

## WFI -- A64

### Encoding: `WFI_HI_hints`  — WFI

**Assembly:** `WFI`

**Pseudocode (Decode):**
```
// Empty.
```

---

## WFIT -- A64

### Encoding: `WFIT_only_systeminstrswithreg`  — WFIT

**Assembly:** `WFIT  <Xt>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xt>` | Rd (5b) | Is the 64-bit name of the general-purpose source register, encoded in the "Rd" field. | let d : integer = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_WFxT) then EndOfDecode(Decode_UNDEF); end;

let d : integer = UInt(Rd);
```

---

## WHILEGE (predicate as counter) -- A64

### Encoding: `whilege_pn_rr_`  — WHILEGE

**Assembly:** `WHILEGE  <PNd>.<T>, <Xn>, <Xm>, <vl>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<PNd>` | PNd (3b) | Is the name of the destination scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the  | let d : integer = UInt('1'::PNd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<vl>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt('1'::PNd);
let unsigned : boolean = FALSE;
let invert : boolean = TRUE;
let op : CmpOp = Cmp_GE;
let width : integer = 2 << UInt(vl);
```

---

## WHILEGE (predicate pair) -- A64

### Encoding: `whilege_pp_rr_`  — WHILEGE

**Assembly:** `WHILEGE  { <Pd1>.<T>, <Pd2>.<T> }, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd1>` | Pd (3b) | Is the name of the first destination scalable predicate register, encoded as "Pd" times 2. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<T>` | — |  |  |
| `<Pd2>` | Pd (3b) | Is the name of the second destination scalable predicate register, encoded as "Pd" times 2 plus 1. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d0 : integer = UInt(Pd::'0');
let d1 : integer = UInt(Pd::'1');
let unsigned : boolean = FALSE;
let op : CmpOp = Cmp_GE;
```

---

## WHILEGE (predicate) -- A64

### Encoding: `whilege_p_p_rr_`  — WHILEGE

**Assembly:** `WHILEGE  <Pd>.<T>, <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 32 << UInt(sf);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
let unsigned : boolean = FALSE;
let op : CmpOp = Cmp_GE;
```

---

## WHILEGT (predicate as counter) -- A64

### Encoding: `whilegt_pn_rr_`  — WHILEGT

**Assembly:** `WHILEGT  <PNd>.<T>, <Xn>, <Xm>, <vl>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<PNd>` | PNd (3b) | Is the name of the destination scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the  | let d : integer = UInt('1'::PNd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<vl>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt('1'::PNd);
let unsigned : boolean = FALSE;
let invert : boolean = TRUE;
let op : CmpOp = Cmp_GT;
let width : integer = 2 << UInt(vl);
```

---

## WHILEGT (predicate pair) -- A64

### Encoding: `whilegt_pp_rr_`  — WHILEGT

**Assembly:** `WHILEGT  { <Pd1>.<T>, <Pd2>.<T> }, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd1>` | Pd (3b) | Is the name of the first destination scalable predicate register, encoded as "Pd" times 2. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<T>` | — |  |  |
| `<Pd2>` | Pd (3b) | Is the name of the second destination scalable predicate register, encoded as "Pd" times 2 plus 1. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d0 : integer = UInt(Pd::'0');
let d1 : integer = UInt(Pd::'1');
let unsigned : boolean = FALSE;
let op : CmpOp = Cmp_GT;
```

---

## WHILEGT (predicate) -- A64

### Encoding: `whilegt_p_p_rr_`  — WHILEGT

**Assembly:** `WHILEGT  <Pd>.<T>, <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 32 << UInt(sf);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
let unsigned : boolean = FALSE;
let op : CmpOp = Cmp_GT;
```

---

## WHILEHI (predicate as counter) -- A64

### Encoding: `whilehi_pn_rr_`  — WHILEHI

**Assembly:** `WHILEHI  <PNd>.<T>, <Xn>, <Xm>, <vl>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<PNd>` | PNd (3b) | Is the name of the destination scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the  | let d : integer = UInt('1'::PNd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<vl>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt('1'::PNd);
let unsigned : boolean = TRUE;
let invert : boolean = TRUE;
let op : CmpOp = Cmp_GT;
let width : integer = 2 << UInt(vl);
```

---

## WHILEHI (predicate pair) -- A64

### Encoding: `whilehi_pp_rr_`  — WHILEHI

**Assembly:** `WHILEHI  { <Pd1>.<T>, <Pd2>.<T> }, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd1>` | Pd (3b) | Is the name of the first destination scalable predicate register, encoded as "Pd" times 2. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<T>` | — |  |  |
| `<Pd2>` | Pd (3b) | Is the name of the second destination scalable predicate register, encoded as "Pd" times 2 plus 1. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d0 : integer = UInt(Pd::'0');
let d1 : integer = UInt(Pd::'1');
let unsigned : boolean = TRUE;
let op : CmpOp = Cmp_GT;
```

---

## WHILEHI (predicate) -- A64

### Encoding: `whilehi_p_p_rr_`  — WHILEHI

**Assembly:** `WHILEHI  <Pd>.<T>, <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 32 << UInt(sf);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
let unsigned : boolean = TRUE;
let op : CmpOp = Cmp_GT;
```

---

## WHILEHS (predicate as counter) -- A64

### Encoding: `whilehs_pn_rr_`  — WHILEHS

**Assembly:** `WHILEHS  <PNd>.<T>, <Xn>, <Xm>, <vl>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<PNd>` | PNd (3b) | Is the name of the destination scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the  | let d : integer = UInt('1'::PNd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<vl>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt('1'::PNd);
let unsigned : boolean = TRUE;
let invert : boolean = TRUE;
let op : CmpOp = Cmp_GE;
let width : integer = 2 << UInt(vl);
```

---

## WHILEHS (predicate pair) -- A64

### Encoding: `whilehs_pp_rr_`  — WHILEHS

**Assembly:** `WHILEHS  { <Pd1>.<T>, <Pd2>.<T> }, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd1>` | Pd (3b) | Is the name of the first destination scalable predicate register, encoded as "Pd" times 2. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<T>` | — |  |  |
| `<Pd2>` | Pd (3b) | Is the name of the second destination scalable predicate register, encoded as "Pd" times 2 plus 1. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d0 : integer = UInt(Pd::'0');
let d1 : integer = UInt(Pd::'1');
let unsigned : boolean = TRUE;
let op : CmpOp = Cmp_GE;
```

---

## WHILEHS (predicate) -- A64

### Encoding: `whilehs_p_p_rr_`  — WHILEHS

**Assembly:** `WHILEHS  <Pd>.<T>, <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 32 << UInt(sf);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
let unsigned : boolean = TRUE;
let op : CmpOp = Cmp_GE;
```

---

## WHILELE (predicate as counter) -- A64

### Encoding: `whilele_pn_rr_`  — WHILELE

**Assembly:** `WHILELE  <PNd>.<T>, <Xn>, <Xm>, <vl>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<PNd>` | PNd (3b) | Is the name of the destination scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the  | let d : integer = UInt('1'::PNd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<vl>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt('1'::PNd);
let unsigned : boolean = FALSE;
let invert : boolean = FALSE;
let op : CmpOp = Cmp_LE;
let width : integer = 2 << UInt(vl);
```

---

## WHILELE (predicate pair) -- A64

### Encoding: `whilele_pp_rr_`  — WHILELE

**Assembly:** `WHILELE  { <Pd1>.<T>, <Pd2>.<T> }, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd1>` | Pd (3b) | Is the name of the first destination scalable predicate register, encoded as "Pd" times 2. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<T>` | — |  |  |
| `<Pd2>` | Pd (3b) | Is the name of the second destination scalable predicate register, encoded as "Pd" times 2 plus 1. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d0 : integer = UInt(Pd::'0');
let d1 : integer = UInt(Pd::'1');
let unsigned : boolean = FALSE;
let op : CmpOp = Cmp_LE;
```

---

## WHILELE (predicate) -- A64

### Encoding: `whilele_p_p_rr_`  — WHILELE

**Assembly:** `WHILELE  <Pd>.<T>, <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 32 << UInt(sf);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
let unsigned : boolean = FALSE;
let op : CmpOp = Cmp_LE;
```

---

## WHILELO (predicate as counter) -- A64

### Encoding: `whilelo_pn_rr_`  — WHILELO

**Assembly:** `WHILELO  <PNd>.<T>, <Xn>, <Xm>, <vl>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<PNd>` | PNd (3b) | Is the name of the destination scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the  | let d : integer = UInt('1'::PNd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<vl>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt('1'::PNd);
let unsigned : boolean = TRUE;
let invert : boolean = FALSE;
let op : CmpOp = Cmp_LT;
let width : integer = 2 << UInt(vl);
```

---

## WHILELO (predicate pair) -- A64

### Encoding: `whilelo_pp_rr_`  — WHILELO

**Assembly:** `WHILELO  { <Pd1>.<T>, <Pd2>.<T> }, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd1>` | Pd (3b) | Is the name of the first destination scalable predicate register, encoded as "Pd" times 2. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<T>` | — |  |  |
| `<Pd2>` | Pd (3b) | Is the name of the second destination scalable predicate register, encoded as "Pd" times 2 plus 1. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d0 : integer = UInt(Pd::'0');
let d1 : integer = UInt(Pd::'1');
let unsigned : boolean = TRUE;
let op : CmpOp = Cmp_LT;
```

---

## WHILELO (predicate) -- A64

### Encoding: `whilelo_p_p_rr_`  — WHILELO

**Assembly:** `WHILELO  <Pd>.<T>, <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 32 << UInt(sf);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
let unsigned : boolean = TRUE;
let op : CmpOp = Cmp_LT;
```

---

## WHILELS (predicate as counter) -- A64

### Encoding: `whilels_pn_rr_`  — WHILELS

**Assembly:** `WHILELS  <PNd>.<T>, <Xn>, <Xm>, <vl>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<PNd>` | PNd (3b) | Is the name of the destination scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the  | let d : integer = UInt('1'::PNd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<vl>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt('1'::PNd);
let unsigned : boolean = TRUE;
let invert : boolean = FALSE;
let op : CmpOp = Cmp_LE;
let width : integer = 2 << UInt(vl);
```

---

## WHILELS (predicate pair) -- A64

### Encoding: `whilels_pp_rr_`  — WHILELS

**Assembly:** `WHILELS  { <Pd1>.<T>, <Pd2>.<T> }, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd1>` | Pd (3b) | Is the name of the first destination scalable predicate register, encoded as "Pd" times 2. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<T>` | — |  |  |
| `<Pd2>` | Pd (3b) | Is the name of the second destination scalable predicate register, encoded as "Pd" times 2 plus 1. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d0 : integer = UInt(Pd::'0');
let d1 : integer = UInt(Pd::'1');
let unsigned : boolean = TRUE;
let op : CmpOp = Cmp_LE;
```

---

## WHILELS (predicate) -- A64

### Encoding: `whilels_p_p_rr_`  — WHILELS

**Assembly:** `WHILELS  <Pd>.<T>, <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 32 << UInt(sf);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
let unsigned : boolean = TRUE;
let op : CmpOp = Cmp_LE;
```

---

## WHILELT (predicate as counter) -- A64

### Encoding: `whilelt_pn_rr_`  — WHILELT

**Assembly:** `WHILELT  <PNd>.<T>, <Xn>, <Xm>, <vl>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<PNd>` | PNd (3b) | Is the name of the destination scalable predicate register PN8-PN15, with predicate-as-counter encoding, encoded in the  | let d : integer = UInt('1'::PNd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |
| `<vl>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt('1'::PNd);
let unsigned : boolean = FALSE;
let invert : boolean = FALSE;
let op : CmpOp = Cmp_LT;
let width : integer = 2 << UInt(vl);
```

---

## WHILELT (predicate pair) -- A64

### Encoding: `whilelt_pp_rr_`  — WHILELT

**Assembly:** `WHILELT  { <Pd1>.<T>, <Pd2>.<T> }, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd1>` | Pd (3b) | Is the name of the first destination scalable predicate register, encoded as "Pd" times 2. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<T>` | — |  |  |
| `<Pd2>` | Pd (3b) | Is the name of the second destination scalable predicate register, encoded as "Pd" times 2 plus 1. | let d0 : integer = UInt(Pd::'0');<br>let d1 : integer = UInt(Pd::'1'); |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) && !IsFeatureImplemented(FEAT_SVE2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 64;
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d0 : integer = UInt(Pd::'0');
let d1 : integer = UInt(Pd::'1');
let unsigned : boolean = FALSE;
let op : CmpOp = Cmp_LT;
```

---

## WHILELT (predicate) -- A64

### Encoding: `whilelt_p_p_rr_`  — WHILELT

**Assembly:** `WHILELT  <Pd>.<T>, <R><n>, <R><m>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<R>` | — |  |  |
| `<n>` | Rn (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<m>` | Rm (5b) | Is the number [0-30] of the source general-purpose register or the name ZR (31), encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let rsize : integer{} = 32 << UInt(sf);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
let unsigned : boolean = FALSE;
let op : CmpOp = Cmp_LT;
```

---

## WHILERW -- A64

### Encoding: `whilerw_p_rr_`  — WHILERW

**Assembly:** `WHILERW  <Pd>.<T>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
```

---

## WHILEWR -- A64

### Encoding: `whilewr_p_rr_`  — WHILEWR

**Assembly:** `WHILEWR  <Pd>.<T>, <Xn>, <Xm>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Xn>` | Rn (5b) | Is the 64-bit name of the first source general-purpose register, encoded in the "Rn" field. | let n : integer = UInt(Rn); |
| `<Xm>` | Rm (5b) | Is the 64-bit name of the second source general-purpose register, encoded in the "Rm" field. | let m : integer = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Rn);
let m : integer = UInt(Rm);
let d : integer = UInt(Pd);
```

---

## WRFFR -- A64

### Encoding: `wrffr_f_p_`  — WRFFR

**Assembly:** `WRFFR  <Pn>.B`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pn>` | Pn (4b) | Is the name of the source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let n : integer = UInt(Pn);
```

---

## XAFLAG -- A64

### Encoding: `XAFLAG_M_pstate`  — XAFLAG

**Assembly:** `XAFLAG`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_FlagM2) then EndOfDecode(Decode_UNDEF); end;
```

---

## XAR -- A64

### Encoding: `XAR_VVV2_crypto3_imm6`  — XAR

**Assembly:** `XAR  <Vd>.2D, <Vn>.2D, <Vm>.2D, #<imm6>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |
| `<imm6>` | imm6 (6b) | Is a rotation right, encoded in "imm6". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SHA3) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
```

---

## XAR -- A64

### Encoding: `xar_z_zzi_`  — XAR

**Assembly:** `XAR  <Zdn>.<T>, <Zdn>.<T>, <Zm>.<T>, #<const>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zdn>` | Zdn (5b) | Is the name of the first source and destination scalable vector register, encoded in the "Zdn" field. | let dn : integer = UInt(Zdn); |
| `<T>` | — |  |  |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |
| `<const>` | (tszh :: tszl :: imm3) | Is the immediate shift amount, in the range 1 to number of bits per element, encoded in "tszh:tszl:imm3". |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let tsize : bits(4) = tszh::tszl;
if tsize == '0000' then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << HighestSetBitNZ(tsize);
let m : integer = UInt(Zm);
let dn : integer = UInt(Zdn);
let rot : integer = (2 * esize) - UInt(tsize::imm3);
```

---

## XPACD, XPACI, XPACLRI -- A64

### Encoding: `XPACD_64Z_dp_1src`  [XPACD]  — XPACD

**Assembly:** `XPACD  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let data : boolean = (D == '1');
```

### Encoding: `XPACI_64Z_dp_1src`  [XPACI]  — XPACI

**Assembly:** `XPACI  <Xd>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Xd>` | Rd (5b) | Is the 64-bit name of the general-purpose destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let data : boolean = (D == '1');
```

### Encoding: `XPACLRI_HI_hints`  — XPACLRI

**Assembly:** `XPACLRI`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_PAuth) then EndOfDecode(Decode_NOP); end;
let d : integer = 30;
let data : boolean = FALSE;
```

---

## XTN, XTN2 -- A64

### Encoding: `XTN_asimdmisc_N`  — XTN

**Assembly:** `XTN{2}  <Vd>.<Tb>, <Vn>.<Ta>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `2` | — |  |  |
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<Tb>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Ta>` | — |  |  |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size == '11' then EndOfDecode(Decode_UNDEF); end;
let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);

let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64;
let part : integer = UInt(Q);
let elements : integer = datasize DIV esize;
```

---

## YIELD -- A64

### Encoding: `YIELD_HI_hints`  — YIELD

**Assembly:** `YIELD`

**Pseudocode (Decode):**
```
// Empty.
```

---

## ZERO (double-vector) -- A64

### Encoding: `zero_za2_ri_1`  — ZERO

**Assembly:** `ZERO  ZA.D[<Wv>, <offs1>:<offs2>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off3 (3b) | For the "One ZA double-vector" variant: is the first vector select offset, encoded as "off3" field times 2. | let offset : integer = UInt(off3::'0'); |
| `<offs2>` | off3 (3b) | For the "One ZA double-vector" variant: is the second vector select offset, encoded as "off3" field times 2 plus 1. | let offset : integer = UInt(off3::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off3::'0');
let ngrp : integer = 1;
let nvec : integer = 2;
```

### Encoding: `zero_za2_ri_2`  — ZERO

**Assembly:** `ZERO  ZA.D[<Wv>, <offs1>:<offs2>, VGx2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off2::'0');
let ngrp : integer = 2;
let nvec : integer = 2;
```

### Encoding: `zero_za2_ri_4`  — ZERO

**Assembly:** `ZERO  ZA.D[<Wv>, <offs1>:<offs2>, VGx4]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the first vector select offset, encoded as "of | let offset : integer = UInt(off2::'0'); |
| `<offs2>` | off2 (2b) | For the "Four ZA double-vectors" and "Two ZA double-vectors" variants: is the second vector select offset, encoded as "o | let offset : integer = UInt(off2::'0'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off2::'0');
let ngrp : integer = 4;
let nvec : integer = 2;
```

---

## ZERO (quad-vector) -- A64

### Encoding: `zero_za4_ri_1`  — ZERO

**Assembly:** `ZERO  ZA.D[<Wv>, <offs1>:<offs4>]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | off2 (2b) | For the "One ZA quad-vector" variant: is the first vector select offset, encoded as "off2" field times 4. | let offset : integer = UInt(off2::'00'); |
| `<offs4>` | off2 (2b) | For the "One ZA quad-vector" variant: is the fourth vector select offset, encoded as "off2" field times 4 plus 3. | let offset : integer = UInt(off2::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off2::'00');
let ngrp : integer = 1;
let nvec : integer = 4;
```

### Encoding: `zero_za4_ri_2`  — ZERO

**Assembly:** `ZERO  ZA.D[<Wv>, <offs1>:<offs4>, VGx2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(o1::'00');
let ngrp : integer = 2;
let nvec : integer = 4;
```

### Encoding: `zero_za4_ri_4`  — ZERO

**Assembly:** `ZERO  ZA.D[<Wv>, <offs1>:<offs4>, VGx4]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs1>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the first vector select offset, encoded as "o1" fi | let offset : integer = UInt(o1::'00'); |
| `<offs4>` | o1 (1b) | For the "Four ZA quad-vectors" and "Two ZA quad-vectors" variants: is the fourth vector select offset, encoded as "o1" f | let offset : integer = UInt(o1::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(o1::'00');
let ngrp : integer = 4;
let nvec : integer = 4;
```

---

## ZERO (single-vector) -- A64

### Encoding: `zero_za1_ri_2`  — ZERO

**Assembly:** `ZERO  ZA.D[<Wv>, <offs>, VGx2]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off3);
let ngrp : integer = 2;
```

### Encoding: `zero_za1_ri_4`  — ZERO

**Assembly:** `ZERO  ZA.D[<Wv>, <offs>, VGx4]`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Wv>` | Rv (2b) | Is the 32-bit name of the vector select register W8-W11, encoded in the "Rv" field. | let v : integer = UInt('010'::Rv); |
| `<offs>` | off3 (3b) | Is the vector select offset, in the range 0 to 7, encoded in the "off3" field. | let offset : integer = UInt(off3); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2p1) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt('010'::Rv);
let offset : integer = UInt(off3);
let ngrp : integer = 4;
```

---

## ZERO (table) -- A64

### Encoding: `zero_zt_i_`  — ZERO

**Assembly:** `ZERO  { ZT0 }`

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
```

---

## ZERO (tiles) -- A64

### Encoding: `zero_za_i_`  — ZERO

**Assembly:** `ZERO  { {<mask>} }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<mask>` | imm8 (8b) | Is the optional list of up to eight 64-bit element tile names separated by commas, encoded in the "imm8" field. | let mask : bits(8) = imm8; |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME) then EndOfDecode(Decode_UNDEF); end;
let mask : bits(8) = imm8;
let esize : integer{} = 64;
```

---

## ZIP (four registers) -- A64

### Encoding: `zip_mz_z_4`  — ZIP

**Assembly:** `ZIP  { <Zd1>.<T>-<Zd4>.<T> }, { <Zn1>.<T>-<Zn4>.<T> }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<T>` | — |  |  |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if size == '11' && MaxImplementedSVL() < 256 then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
```

### Encoding: `zip_mz_z_4q`  — ZIP

**Assembly:** `ZIP  { <Zd1>.Q-<Zd4>.Q }, { <Zn1>.Q-<Zn4>.Q }`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (3b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 4. | let d : integer = UInt(Zd::'00'); |
| `<Zd4>` | Zd (3b) | Is the name of the fourth scalable vector register of the destination multi-vector group, encoded as "Zd" times 4 plus 3 | let d : integer = UInt(Zd::'00'); |
| `<Zn1>` | Zn (3b) | Is the name of the first scalable vector register of the source multi-vector group, encoded as "Zn" times 4. | let n : integer = UInt(Zn::'00'); |
| `<Zn4>` | Zn (3b) | Is the name of the fourth scalable vector register of the source multi-vector group, encoded as "Zn" times 4 plus 3. | let n : integer = UInt(Zn::'00'); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if MaxImplementedSVL() < 512 then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn::'00');
let d : integer = UInt(Zd::'00');
```

---

## ZIP (two registers) -- A64

### Encoding: `zip_mz_zz_2`  — ZIP

**Assembly:** `ZIP  { <Zd1>.<T>-<Zd2>.<T> }, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<T>` | — |  |  |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'0');
```

### Encoding: `zip_mz_zz_2q`  — ZIP

**Assembly:** `ZIP  { <Zd1>.Q-<Zd2>.Q }, <Zn>.Q, <Zm>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd1>` | Zd (4b) | Is the name of the first scalable vector register of the destination multi-vector group, encoded as "Zd" times 2. | let d : integer = UInt(Zd::'0'); |
| `<Zd2>` | Zd (4b) | Is the name of the second scalable vector register of the destination multi-vector group, encoded as "Zd" times 2 plus 1 | let d : integer = UInt(Zd::'0'); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
if MaxImplementedSVL() < 256 then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd::'0');
```

---

## ZIP1 -- A64

### Encoding: `ZIP1_asimdperm_only`  — ZIP1

**Assembly:** `ZIP1  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = UInt(op);
let pairs : integer = elements DIV 2;
```

---

## ZIP1, ZIP2 (predicates) -- A64

### Encoding: `zip2_p_pp_`  — ZIP2

**Assembly:** `ZIP2  <Pd>.<T>, <Pn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let part : integer = 1;
```

### Encoding: `zip1_p_pp_`  — ZIP1

**Assembly:** `ZIP1  <Pd>.<T>, <Pn>.<T>, <Pm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Pd>` | Pd (4b) | Is the name of the destination scalable predicate register, encoded in the "Pd" field. | let d : integer = UInt(Pd); |
| `<T>` | — |  |  |
| `<Pn>` | Pn (4b) | Is the name of the first source scalable predicate register, encoded in the "Pn" field. | let n : integer = UInt(Pn); |
| `<Pm>` | Pm (4b) | Is the name of the second source scalable predicate register, encoded in the "Pm" field. | let m : integer = UInt(Pm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Pn);
let m : integer = UInt(Pm);
let d : integer = UInt(Pd);
let part : integer = 0;
```

---

## ZIP1, ZIP2 (vectors) -- A64

### Encoding: `zip2_z_zz_`  — ZIP2

**Assembly:** `ZIP2  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 1;
```

### Encoding: `zip2_z_zz_q`  — ZIP2

**Assembly:** `ZIP2  <Zd>.Q, <Zn>.Q, <Zm>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 1;
```

### Encoding: `zip1_z_zz_`  — ZIP1

**Assembly:** `ZIP1  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE) && !IsFeatureImplemented(FEAT_SME) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 0;
```

### Encoding: `zip1_z_zz_q`  — ZIP1

**Assembly:** `ZIP1  <Zd>.Q, <Zn>.Q, <Zm>.Q`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_F64MM) then EndOfDecode(Decode_UNDEF); end;
let esize : integer{} = 128;
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 0;
```

---

## ZIP2 -- A64

### Encoding: `ZIP2_asimdperm_only`  — ZIP2

**Assembly:** `ZIP2  <Vd>.<T>, <Vn>.<T>, <Vm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Vd>` | Rd (5b) | Is the name of the SIMD&FP destination register, encoded in the "Rd" field. | let d : integer{} = UInt(Rd); |
| `<T>` | — |  |  |
| `<Vn>` | Rn (5b) | Is the name of the first SIMD&FP source register, encoded in the "Rn" field. | let n : integer{} = UInt(Rn); |
| `<Vm>` | Rm (5b) | Is the name of the second SIMD&FP source register, encoded in the "Rm" field. | let m : integer{} = UInt(Rm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_AdvSIMD) then EndOfDecode(Decode_UNDEF); end;
if size::Q == '110' then EndOfDecode(Decode_UNDEF); end;

let d : integer{} = UInt(Rd);
let n : integer{} = UInt(Rn);
let m : integer{} = UInt(Rm);
let esize : integer{} = 8 << UInt(size);
let datasize : integer{} = 64 << UInt(Q);
let elements : integer = datasize DIV esize;
let part : integer = UInt(op);
let pairs : integer = elements DIV 2;
```

---

## ZIPQ1 -- A64

### Encoding: `zipq1_z_zz_`  — ZIPQ1

**Assembly:** `ZIPQ1  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 0;
```

---

## ZIPQ2 -- A64

### Encoding: `zipq2_z_zz_`  — ZIPQ2

**Assembly:** `ZIPQ2  <Zd>.<T>, <Zn>.<T>, <Zm>.<T>`

**Operands:**

| Symbol | Field | encoding-notes | pseudocode |
|--------|-------|----------------|------------|
| `<Zd>` | Zd (5b) | Is the name of the destination scalable vector register, encoded in the "Zd" field. | let d : integer = UInt(Zd); |
| `<T>` | — |  |  |
| `<Zn>` | Zn (5b) | Is the name of the first source scalable vector register, encoded in the "Zn" field. | let n : integer = UInt(Zn); |
| `<Zm>` | Zm (5b) | Is the name of the second source scalable vector register, encoded in the "Zm" field. | let m : integer = UInt(Zm); |

**Pseudocode (Decode):**
```
if !IsFeatureImplemented(FEAT_SVE2p1) && !IsFeatureImplemented(FEAT_SME2p1) then
    EndOfDecode(Decode_UNDEF);
end;
let esize : integer{} = 8 << UInt(size);
let n : integer = UInt(Zn);
let m : integer = UInt(Zm);
let d : integer = UInt(Zd);
let part : integer = 1;
```

---

